# P1 ORM Audit — 2026-05-17

Owner: orchestrator (Claude). Scope: enforce P1 invariants from goal prompt.

## Invariant: zero `@mikro-orm` imports outside archived migrations

Search: `rg -l '@mikro-orm' --type ts --type js`

Result: all hits live under `services/platform-core/src/infrastructure/application-database/_archived-mikro-migrations/Migration*.ts`. No matches outside the archived folder.

Status: **PASS**.

## Invariant: no `mikro-orm.config.ts` / `PGliteKyselyDriver` test imports

- `rg 'mikro-orm.config'` → no matches
- `rg 'PGliteKyselyDriver'` → no matches

Status: **PASS**.

## Invariant: web/CLI/TUI = invocation + visualization only (no direct ORM imports)

`rg "from ['\"]@mikro-orm|from ['\"]typeorm|from ['\"]kysely|from ['\"]drizzle"` per surface:

- `apps/web/src` → 2 hits, both `import type { EntityManager } from "typeorm"` in test files:
  - `apps/web/src/routes/page.server.test.ts`
  - `apps/web/src/lib/server/doc-links.test.ts`
- `apps/cli/src` → 0
- `apps/tui/src` → 0

Status: **PARTIAL**. Type-only imports in tests are inert at runtime, but they violate the literal “invocation + visualization only” invariant. Track as residual cleanup.

Remaining: rewrite both tests to use the tRPC caller + service interfaces and drop the typeorm type import.

## Invariant: one ORM, one server framework, no `.sql` migrations

- `fd -e sql services/ apps/` returned no migration files.
- Server framework: `apps/server` (NestJS) bootstraps the only HTTP/tRPC runtime. No Hono / parallel server.
- ORM: TypeORM (`@nestjs/typeorm`, `pg`). MikroORM remains only inside `_archived-mikro-migrations/`.

Status: **PASS**.

## Invariant: NestJS-native tRPC

- `apps/server/src/trpc/{trpc.ts,trpc.service.ts,trpc.router.ts,trpc.module.ts}` present.
- `apps/server/src/app.module.ts` wires the tRPC module.
- Routers live under `apps/server/src/trpc/routers/**` with co-located tests.

Status: **PASS**.

## Invariant: stub service decisions

- `services/inference-runtime/` is a Cargo workspace (`Cargo.toml`, `inference-core`, `inference-embed`, `inference-generate`, `inference-server`). Not a TypeScript stub. Decision: **keep**; document as Rust-side inference plane consumed via gRPC/HTTP from NestJS.
- All other `services/*` are real TypeScript modules with `src/{domain,application,infrastructure,interface}`.

Status: **PASS** (no deletions required this iteration).

## Invariant: platform-core split into bounded services

Active platform-core surface areas:
- `application/{agent-hooks, backup, cli-tui, component-lifecycle, db, error-reporting, feature-flags, health-checks, inference, init, input-bindings, interface-parity, jobs, legacy, localization, platform-operations, platform-primitives, runtime, runtime-support, secrets, settings, skill-supply, skills, subscriptions, telemetry, tenancy}`
- `infrastructure/application-database/{_archived-mikro-migrations,...}`
- `interface/http/dto/` (DTO root exists)

Status: **IN PROGRESS** (legacy + cli-tui residual scopes remain inside platform-core). Tracked separately.

## CI gate

`bun run ci` (run 1) failed at `ci:codegen` because `apps/cli/src/generated/tasks.ts` lagged behind two automated-feedback-loop options (`--iteration-timeout-ms`, `--stale-threshold-ms`). Regenerated via `bun run scripts/cli/codegen.ts`. Rerun pending.

## Outstanding follow-ups for this iteration

1. ✓ Done: `bun run ci` -> exit 0 with regenerated `tasks.ts`, repaired Svelte 5 binding, repaired scope import paths, dropped obsolete history tests, extended `DocumentCreateBodyDto` with the `parentId`/`sortPosition` fields the controller already wrote into the create input.
2. ✓ Done: P1 orchestration follow-up committed as `f609131bf` and verified post-commit.
3. Residual: the two `EntityManager` type-only imports in `apps/web/src/routes/page.server.test.ts` and `apps/web/src/lib/server/doc-links.test.ts` remain. The deeper coupling is that the corresponding production helpers under `apps/web/src/lib/server/{dashboard,documents,doc-links}.ts` accept an `EntityManager` directly. The clean fix is to migrate those helpers onto the public document API and have the tests drive them through the tRPC caller. That refactor is its own slice — log it for the next iteration rather than bundle it with the closure follow-up.
4. Residual: 94-file in-flight working tree includes a `parent_id`/`sort_position` doc-tree slice across `services/knowledge-workspace/src/{interface,infrastructure}` plus an intake/modules PM slice across `apps/web/src/routes/projects/[id]` and `services/work-management`. They are coherent slices that should be authored to completion (route svelte + tests + service updates) before commit; do not commit the orphan halves.
