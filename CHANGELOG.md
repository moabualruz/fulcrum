# Changelog

All notable changes to Fulcrum will be tracked here.

<!--
TODO: Generate this file via `git-cliff -o CHANGELOG.md` (or `bun run changelog`)
once `git-cliff` is installed locally.

Install options:
  - macOS:   brew install git-cliff
  - Cargo:   cargo install git-cliff
  - Release: https://github.com/orhun/git-cliff/releases

For the initial v0.1.0 entry use:
  git-cliff --tag v0.1.0 -o CHANGELOG.md

The `cliff.toml` next to this file is already configured with conventional-commit
parsing, group ordering, and Fulcrum-specific skips (release commits, merges).
-->

## [unreleased]

_Entries will be populated by `git-cliff` on first run._
