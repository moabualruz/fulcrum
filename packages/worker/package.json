{
  "name": "@fulcrum/worker",
  "version": "0.0.1",
  "description": "Worker — pluggable agent executor that spawns subordinate agents, tracks lifecycle, and persists results to the agent_runs table",
  "keywords": ["worker", "agent-executor", "pluggable-adapter", "agent-runs", "subprocess"],
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@fulcrum/core": "workspace:*"
  },
  "devDependencies": {
    "better-sqlite3": "^12.0.0",
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
