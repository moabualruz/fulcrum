{
  "name": "@fulcrum/core",
  "version": "0.0.1",
  "description": "Core persistence and orchestration layer — SQLite tasks, agent runs, policy enforcement, workspace status, handoffs, events, and embedding providers",
  "keywords": ["agent-os", "tasks", "sqlite", "policy", "embeddings", "handoffs", "agent-lifecycle", "wip-limits"],
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "better-sqlite3": "^12.0.0",
    "sqlite-vec": "^0.1.6",
    "@huggingface/transformers": "^3.0.0",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
