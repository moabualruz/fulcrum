# Live RAG Eval Suite

Workspace-bound evals for live RAG coverage and retrieval quality.

Live cases must be opt-in when they require model-heavy, accelerator-heavy, or
large-corpus execution. Reports and traces must redact secrets, raw environment
values, and non-operator absolute paths.
