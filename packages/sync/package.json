{
  "name": "@fulcrum/sync",
  "version": "0.0.1",
  "description": "Bidirectional sync adapter — Plane project management integration, conflict detection, and sync state management",
  "keywords": ["sync", "plane", "project-management", "bidirectional-sync", "conflict-detection", "integration"],
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@fulcrum/core": "workspace:*",
    "@fulcrum/policy": "workspace:*",
    "ulidx": "^2.3.0"
  },
  "devDependencies": {
    "better-sqlite3": "^12.0.0",
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
