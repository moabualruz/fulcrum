.claude/
node_modules/
dist/
*.tsbuildinfo
eval-results/
.DS_Store

# Vendor agent-integration files written into project root by tools like
# `graphify install` / `npx skills add`. Same rule text lives in
# rules/AGENTS.md → spliced user-globally by `fulcrum install`; the
# vendor's project-root copy is redundant for this repo.
/.gemini/
/.opencode/

# Vendor project-index outputs (vendor-controlled paths).
/graphify-out/
.claude/settings.local.json
.claude/.cache/
.codex/
coverage/
test-results/
memory:/
tests/.tmp-*/

# Preserved temporary upstream repositories and baseline-copy experiments for
# Phase 9.6 copy-first workflow/value mapping. Do not track archive folders;
# track only copied/adapted files after they land in Fulcrum's real source tree.
/.scratch/upstream-product-replacement/
