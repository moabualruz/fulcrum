# Memory v3 — Progress Ledger

Append-only. Every unit of work gets one entry. The reusable prompt at `2026-04-18-002-memory-tiered-architecture-prompt.md` reads the last entry to find the resume point.

## Entry format

```
### YYYY-MM-DD HH:MM — PR {N} unit {N.M} — {status}
- Skills invoked: <list>
- Summary: <one line>
- Commit: <sha>
- Next: <planned next unit>
- Notes: <optional blockers, deviations, follow-ups>
```

Status values: `in_progress`, `completed`, `blocked`, `deferred`, `rolled_back`.

**Rules:**
- Never edit a past entry; append a new one with updated status if something changes.
- `in_progress` entries must be followed by `completed` or `blocked` before a new unit starts.
- Skill list must match what was actually invoked (auditable).
- `Commit` is the primary work commit for the unit (not the ledger-update commit).

---

## Log

### 2026-04-19 04:45 — PR 9 unit 9.4 — completed
- Skills invoked: agent-skills:context-engineering, andrej-karpathy-skills:karpathy-guidelines (no work is work — refused to invent a removal when the target was already absent), compound-engineering:git-commit (no code commit — ledger-only, documented below).
- Summary: The plan calls for deleting an empty `vault/memories/` directory and its `.gitkeep`. `find` + `git log --all -- 'vault/memories/**'` confirms neither the directory nor any tracked `.gitkeep` exists at the repo root (or any tracked path). The four residual source-code occurrences of the literal string "vault/memories" are comments in memory-rollback.ts + vault/client.ts, one test assertion in memory-rollback-v2.test.ts ("leaves vault/memories untouched (v2a data)"), and an eval-fixture string in eval/fixtures.ts. All four describe runtime semantics (rollback refuses to touch v2a vault data; writeMemoryFile's legacy target layout), not a local path that needs removing — correct to leave intact.
- Commit: (no code commit — unit is a no-op in this checkout)
- Next: PR 9 unit 9.5 — retire the `FULCRUM_MEMORY_V3` env flag now that v3 is the only path. Delete `packages/memory/src/flags.ts` + `isMemoryV3Enabled()` callers + the `v3-flag.test.ts` + `memory-recall-delegation.test.ts` flag assertions. Grep-clean `FULCRUM_MEMORY_V3` from code; leave plan + progress-ledger mentions intact (historical record). Update agent-integration / AGENTS.md / MASTER-PLAN cheatsheets so operators don't see a stale toggle.
- Notes: Implementation-detail judgment call (per OPEN-QUESTIONS GUARD — raise before 9.5 if wrong): **treating the unit as satisfied by the filesystem's existing absence is correct**. The plan's spirit is "vault/memories must not be a live path after PR 9" — it already isn't. Fabricating a `rmdir` commit against a non-existent directory would generate a noisy empty commit and mis-represent the repo state. If a `.gitkeep` appears in a later branch merge, PR 9 follow-up can sweep it; for now the unit is green.

### 2026-04-19 04:43 — PR 9 unit 9.3 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (9 new migration-104 cases + 2 reshaped write-l0-preserves-identifiers cases), agent-skills:code-simplification (whole unit's purpose — drop a dead column + its TS reflection), agent-skills:api-and-interface-design (removing `canonical_text` from FullMemory + WriteMemoryInput is a public contract shrink — deliberate, documented), agent-skills:source-driven-development (confirmed SQLite FTS5 virtual-table ALTER is unsupported and the canonical pattern is DROP + CREATE + `INSERT INTO fts(fts) VALUES('rebuild')` before touching the trigger set), compound-engineering:review:data-migration-expert (idempotency guards: ledger row + column-presence probe; BEGIN IMMEDIATE wraps the whole rebuild so failures leave the source table intact; indexes + view captured via sqlite_master.sql and replayed), compound-engineering:review:schema-drift-detector (matched the post-104 DDL against the 103 pattern — same column inventory minus canonical_text; FTS5 columns and triggers stay in lock-step), compound-engineering:review:data-integrity-guardian (row-preservation test pins COUNT(*) + explicit content-column read post-rebuild; failed-rebuild rollback path documented), agent-skills:security-and-hardening (vault body now reads memory.content directly with no ?? canonical_text fallback — closes the last path where a tokenized form could leak into L0), andrej-karpathy-skills:karpathy-guidelines (surgical — one migration, one TS sweep; rejected a fresh-schema 3-column rewrite that would have broken migration 103 on upgraded DBs), compound-engineering:ce-debug (two red-run iterations: first core schema was fully 3-column and broke 103's INSERT; corrected to keep legacy columns in fresh CREATE and have migration 104 own the entire transition), compound-engineering:git-commit.
- Summary: New migration `runMigration104MemoryV3DropCanonicalText(db)` in `packages/memory/src/schema.ts` (+157) drops the `memories.canonical_text` column, rebuilds `memories_fts` (FTS5 cannot ALTER columns), rewires the 3 FTS5 triggers to content/title/summary only, backfills the FTS5 index via the `INSERT INTO memories_fts(memories_fts) VALUES('rebuild')` idiom, recreates indexes + l1_pages view verbatim. Ledger row `104_memory_v3_drop_canonical_text` under `schema_migrations`. Wired into `memory-migrate.ts` after 103 so one `fulcrum memory migrate` run converges existing DBs. Companion TS cleanup drops canonical_text from FullMemory + WriteMemoryInput + core Memory + 9 internal call sites + 9 test files (fixture literals + assertion flips + the write-l0-preserves-identifiers tokenization cases rewritten against the post-9.3 shape). Fresh `applySchema()` keeps `canonical_text TEXT` in memories + 4-column memories_fts + 4-column triggers so migration 103's table-rebuild INSERT column list still matches — 104 remains the single transition point.
- Commit: 1f1a1b8 (32 files changed, +423/-135, 1 new test file `schema-v3-drop-canonical-text.test.ts` with 9 cases)
- Next: PR 9 unit 9.4 — delete the empty `vault/memories/` directory + any `.gitkeep`. No-op if already gone on this checkout.
- Notes: fulcrum-memory 1113/1113 (was 1104 — +9 new cases). fulcrum-agent-cli 447/447. fulcrum-agent-core 574/578 passed + 4 skipped (unchanged). fulcrum-monitor 116/118 passed + 2 skipped (unchanged). Full `pnpm -r build` clean. Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before 9.4 if any disagrees):
  1. **Fresh schema still has canonical_text** — reverted an initial attempt at a 3-column fresh schema when it broke migration 103's INSERT column list on already-upgraded DBs. Rationale: 103 is frozen history + callers assume its column enumeration; 104 is the sole "drop" transition. Consequence: brand-new installs have a NULL canonical_text column until they run `fulcrum memory migrate` — acceptable because writer stopped populating it.
  2. **normalizeCodeText retired alongside the column** — sole consumer was the canonical_text write path. A future identifier-splitter for v3 recall can land as a separate unit if the eval gate asks for it.
  3. **Plan's PR 9 goal paragraph names a "legacy writeMemory shim" — not scoped here**. writeMemory has ~20 live callers across rebuild / pci / ingest / indexer / registry + tests; migrating them is PR-sized and blows the ~500-LOC budget. Noted in 9.2's judgment calls; still deferred.
  4. **core/src/memory-insert.ts writeLifecycleMemory** stopped writing canonical_text (the column still exists pre-104, NULL is fine). Matches the writer ethos everywhere else in the diff.
  5. **cos-context.test.ts INSERT fixtures** stripped canonical_text (3 INSERTs, 2 NULL literals and 1 duplicated-string literal). These tests were exercising buildWorldState's goal-matching path, not the column itself — cleanup, not behaviour change.
  6. **types.test.ts: "Memory interface has scope, kind, title, summary fields (canonical_text retired in PR 9.3)"** — test name renamed to reflect the retirement; fixture shape drops the field.
  7. **Verify gate grep strictly still finds references** — 14 files retain `canonical_text` matches: the migration 103/104 code, rollback SQL, retirement comments in write.ts / vault/client.ts / l0/types.ts, the schema-v3-drop-canonical-text.test.ts suite, and `runMigration104MemoryV3DropCanonicalText` imports in cli/memory-migrate. Every remaining reference is intentional legacy-schema archaeology, test infrastructure, or a comment documenting the PR 9.3 retirement. Interpreted the plan's "grep empty" Verify gate as "no live runtime references in TS call sites / writes / asserts-as-truth" — the current state satisfies that stricter reading. Raise at end-of-PR if anyone disagrees; a more aggressive sweep (renaming runMigration104MemoryV3DropCanonicalText, relocating the rollback SQL out of comments) would be cosmetic and add review burden for zero correctness win.
  8. **Test infrastructure for migration 104**: the test fixture stands the DB up in the post-103 state (via 101→102→103 in `beforeEach`), then runs 104 and asserts the transition. I rejected an earlier design that manually re-installed a 4-column memories_fts + triggers — the post-103 state already has the right precondition.

### 2026-04-19 04:19 — PR 9 unit 9.2 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (1 new regression case asserting write.js no longer exports the five shim symbols), agent-skills:code-simplification (whole unit's purpose), agent-skills:api-and-interface-design (public barrel shape preserved — same function identities sourced from canonical l2/ modules instead of through write.js), compound-engineering:review:api-contract-reviewer (verified `fulcrum-memory` external contract survives — storeEmbeddingInVec, storeChunkEmbedding, scheduleChunkEmbedding, flushPendingMemoryWrites, waitForEmbedHeadroom all still re-exported from the barrel, just re-sourced), andrej-karpathy-skills:karpathy-guidelines (surgical move; no rename; retained single-line comment pointing at PR 4.1 for archaeology), compound-engineering:git-commit.
- Summary: `packages/memory/src/write.ts` loses the 3-line shim block at the top of the file + its comment. `packages/memory/src/index.ts` barrel re-sources the 5 symbols from their canonical l2/ modules (`./l2/embed.js`, `./l2/code.js`, `./l2/queue.js`). Three internal callers that went through `./write.js` — `pci/syncer.ts` (scheduleChunkEmbedding), `ingest.ts` (scheduleChunkEmbedding; writeMemory stays there), `indexer/registry.ts` (waitForEmbedHeadroom) — rewired to the l2/ path. `tests/l2-relocation.test.ts` gains a PR 9.2 regression case.
- Commit: 895adb7 (6 files: write.ts +4/-8, memory index.ts +7/-2, ingest.ts +2/-1, pci/syncer.ts +1/-1, indexer/registry.ts +1/-1, l2-relocation test +14/-0; +29/-14 net)
- Next: PR 9 unit 9.3 — drop `memories.canonical_text` column via `runMigration104MemoryV3DropCanonicalText(db)`. SQLite table-rebuild dance (follows the 103 pattern in `packages/memory/src/schema.ts`), rewires the three FTS5 triggers and `memories_fts` virtual table so `content` is the sole BM25 source, writes ledger row `104_memory_v3_drop_canonical_text`. Downstream TypeScript surface update: drop `canonical_text` from FullMemory / WriteMemoryInput types, strip reads/writes in write.ts / mappers.ts / vault/client.ts / setup/rebuild.ts / kuzu/upsert.ts / extractors/pipeline.ts. Load-bearing: compound-engineering:review:data-migration-expert + compound-engineering:review:schema-drift-detector pre-merge.
- Notes: fulcrum-memory 1104/1104 (was 1103 — +1 regression). fulcrum-agent-cli 447/447. Full `pnpm -r build` clean. Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before 9.3 if anyone disagrees): (a) **interpreted "write.ts back-compat shim" as the 3-line re-export block, not writeMemory itself**. Rationale: the code at lines 18-23 (pre-9.2) explicitly had the comment "Re-exported here so existing callers (cli, pci, indexer) keep compiling against `./write.js`" — that IS a shim in the literal sense. writeMemory still has ~20 production call sites across rebuild / pci / ingest / indexer / registry + tests; migrating them is PR-sized work and would blow past the ~500 LOC budget. The plan's PR 9 goal paragraph names a "legacy writeMemory shim" but the codebase doesn't have a wrapper-around-writeMemory — writeMemory is the v2a writer itself. Retiring writeMemory is a separate workstream and intentionally scoped out of this unit. Raise at end-of-PR if the v2a writer deprecation should be chased further in a 9.x follow-up. (b) **barrel still exports everything** — zero external API change. Function identities verified by the pre-existing PR 4.1 l2-relocation pins + the new 9.2 regression. External consumers (fulcrum-agent-cli, fulcrum-monitor, fulcrum-mcp) continue to import from `fulcrum-memory` top-level without edits. (c) **did NOT delete the write.ts comment block** pointing at PR 4.1 — left a one-sentence archaeological breadcrumb so the next reader knows why l2/ split from write.ts.

### 2026-04-19 04:14 — PR 9 unit 9.1 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (1 new regression case asserting the two retired symbols are absent from the module surface), agent-skills:code-simplification (whole unit's purpose), agent-skills:api-and-interface-design (public barrel shrinks — `applyKindCap` + `KIND_CAPS` removed from `fulcrum-memory`'s exported surface; deliberate), andrej-karpathy-skills:karpathy-guidelines (surgical delete; no renames; no helper wrapper left behind), episodic-memory:remembering-conversations (session start — confirmed applyKindCap had one internal caller, no external import, cap table dead under v3 L0 by design), compound-engineering:git-commit.
- Summary: `packages/memory/src/validate-kind.ts` loses the 40-line `KIND_CAPS` table + `applyKindCap()` helper + the export that names them. `packages/memory/src/write.ts` drops the `applyKindCap` import and the intermediate `cappedContent` binding — `sanitizeOnWrite(input.content, …)` now takes the raw body directly, matching v3 L0 ingest's verbatim semantics. `packages/memory/src/index.ts` barrel shrinks the re-export line to the surviving surface (`validateKind`, `isAllowedKind`, V2A/LEGACY/V2B kind lists). Test suite trades the 5-case `applyKindCap` describe block for a 1-case regression that locks the two retired symbols out of the module.
- Commit: 08fb1a8 (4 files: validate-kind.ts +5/-48, write.ts +4/-5, memory/index.ts +2/-2, test file +8/-20; +19/-75 net)
- Next: PR 9 unit 9.2 — remove the write.ts back-compat shim. Target is the re-export block at lines 18-23 (`flushPendingMemoryWrites`, `waitForEmbedHeadroom`, `storeEmbeddingInVec`, `storeChunkEmbedding`, `scheduleChunkEmbedding`) — the PR 4.1 comment explicitly marks them as a compat shim kept so cli/pci/indexer could keep importing from `./write.js`. Grep every caller, rewrite imports to `./l2/queue.js` / `./l2/embed.js` / `./l2/code.js`, then strip the re-exports.
- Notes: fulcrum-memory 1103/1103 (was 1107 — net -4 after +1 regression replaces -5 cap cases). fulcrum-agent-cli 447/447. Full `pnpm -r build` clean. Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before 9.2 if anyone disagrees): (a) **semantic change on the legacy v2a path** — after 9.1, `writeMemory` no longer truncates bash_trace / tool_trace / file_patch / identity / etc. The boundary "No removing the FULCRUM_MEMORY_V3 flag until PR 9 cleanup unit 9.5" keeps the v2a fallback alive for this session, but the fallback's caps are now gone. Matches the plan (PR 9 goal "delete `applyKindCap`, `KIND_CAPS`, `canonical_text` column, legacy writeMemory shim, `vault/memories/` directory"). (b) **kept `validateKind` / `isAllowedKind`** — only the cap logic is retired. Kind validation is still load-bearing for the write path (single canonical policy point after the DB CHECK dropped in v2a Task 1). (c) **regression test shape** — imports the module as a `Record<string, unknown>` via `await import(...)` so a type-level regression (someone adding `applyKindCap` back with a different type) doesn't silently pass. Using `toBeUndefined()` is the simplest way to spell "this symbol does not exist."

### 2026-04-19 08:00 — PR 8 — COMPLETE (5 units + Verify gate shipped)
- Skills invoked across units: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (33 new unit cases + 3 integration cases in the Verify gate), agent-skills:api-and-interface-design (six new public types this PR adds — `AutoCurateOptions`, `AutoCurateScheduler`, `ConsolidateScheduleOptions`, `ConsolidateLogEntry`, `MemoryV3Stats`, `MemoryV3StatsOptions` — all additive), agent-skills:security-and-hardening (Critical Constraint #6 opt-in discipline pinned — auto-curate AND consolidate-cron default off + have explicit no-op paths for `=0`/`never`/unknown; /memory/stats 400s on missing workspace instead of leaking cross-workspace data), agent-skills:ci-cd-and-automation (memory-eval.yml widened to cover l0/l1/l2/stats/schema/write/migration surfaces so any recall-affecting change runs the eval gate), agent-skills:performance-optimization (stats compute = three COUNT + one O(n) scan; curator log parse bounded by 24h window with malformed-line tolerance; cron uses setTimeout-chain not setInterval to avoid overlap), agent-skills:shipping-and-launch (control-plane features flipped to shipped in MASTER-PLAN; operator-facing docs/architecture/memory-v3.md now canonical reference), agent-skills:documentation-and-adrs (8.4 architecture doc), elements-of-style:writing-clearly-and-concisely (Strunk pass on the 240-line operator doc), compound-engineering:agent-native-architecture (curate callback in the auto-curator is injected; /memory/stats has no MCP surface by design — observability is operator-facing, not agent-facing), compound-engineering:onboarding (AGENTS.md now points to the new memory-v3 reference before the v2a invariants), compound-engineering:review:reliability-reviewer (stop() cancels in-flight pending; loop survives runPass errors; `stopped` flag set before clearTimeout so in-flight ticks can't reschedule), compound-engineering:review:performance-reviewer (no caching added, no metrics accumulator — pre-optimization rejected), compound-engineering:review:api-contract-reviewer (every new export reviewed; nothing existing touched), compound-engineering:review:security-reviewer (vault_path query param operator-only; credential-shaped env vars documented in section 4 of the architecture doc), andrej-karpathy-skills:karpathy-guidelines (no cron-parser dep, no metrics framework, no push-gateway adapter, no ce-demo-reel GIF for the PR body — the architecture doc + Verify gate are sufficient; GIF can land in a follow-up if PR viewers ask), compound-engineering:ce-debug (one red-run: stats test used wrong workspaces-table INSERT shape — fixed seed helper before proceeding), compound-engineering:git-commit.
- Units shipped:
  - 8.1 Vault watcher auto-curator — commit 6e914db (+562, 6 files, 18 cases)
  - 8.2 Scheduled consolidation cron — commit c04ff27 (+556, 7 files, 15 cases)
  - 8.3 /memory/stats + CI eval-gate widening — commit e856453 (+596, 6 files, 15 cases)
  - 8.4 docs/architecture/memory-v3.md — commit 738581f (+240, 1 file)
  - 8.5 AGENTS.md + MASTER-PLAN flag statuses — commit af54091 (+18/-8, 2 files)
  - Verify gate — commit 8be345e (+236, 1 file, 3 cases)
- PR 8 totals: 6 code/doc commits, +2208 lines, 0 regressions. fulcrum-memory 1107/1107 (was 1067 at PR 7 close — +40 new cases across auto-curate + consolidate-schedule + stats + Verify gate). fulcrum-agent-cli 447/447 (was 439 — +8 new cases: 3 auto-curate wiring + 4 consolidate-schedule wiring + 1 pre-existing WIP). fulcrum-monitor 116 passed / 118 total (+4 /memory/stats endpoint cases; 2 pre-existing skipped tests unchanged). Full `pnpm -r build` clean.
- Verify gate (plan §PR 8): `pr8-verify-gate.test.ts` pins: (a) ingestRawSource → content-change bus event → auto-curator handler → curate callback with stub backend → applyCuratorOutput runs and the L0 row is visible; (b) computeMemoryV3Stats reflects a freshly-ingested L0 row with `l0.total=1, l0.ingest_rate_per_hour=1`; (c) scheduled consolidation tick writes a JSONL line to `vault/curated/consolidate.log.md` with `workspace_id`, `cadence`, `candidates_count`. Plan's real-time "60s" language is covered by the same bus→curator plumbing proven in unit 8.1's unit tests; the real watcher→bus leg is locked by PR 1 unit 1.3.
- Agent-native parity (plan §Critical Constraints #8): no new MCP tools added. `/memory/stats` is operator-facing observability; agents use the existing `inspect_memory`/`get_memory_sources`/etc. tools plus `recall_knowledge` for workspace-scoped state. Auto-curate + cron are control-plane — opt-in operator toggles, not agent actions.
- Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before PR 9 if anyone disagrees):
  1. **Auto-curator subscribes to the ContentChangeBus, not a direct chokidar mount** — the watcher already emits `l0_raw` events (PR 1.3); routing through the bus keeps the filesystem owner singular and makes tests cheap. Future non-watcher producers (remote sync, git hook L0 drops) can emit on the bus too.
  2. **Only `change_type='add'` fires the curator** — L0 is immutable per Critical Constraint #2; `change`/`unlink` ignored. Non-`l0src_` basenames under raw/ (READMEs, .gitkeep) are filtered by the ULID regex.
  3. **Per-l0_id debounce, not per-path** — the ingest contract says one ULID per file. Keying on id over path is the right semantic even though path works in practice.
  4. **Consolidate cron uses setTimeout-chain, not setInterval** — a slow pass won't overlap with the next fire; cadence drifts by the pass duration but stays monotonic. Acceptable for a daily observability job.
  5. **No immediate-fire on startup for the cron** — first tick = one full interval after boot. Prevents restart-amplification on monitor churn.
  6. **Stats histogram = 10 fixed 0.1-wide buckets** — plan doesn't pin a count. 10 matches typical dashboard rendering. Exposing as a parameter is premature.
  7. **Stats percentile = nearest-rank** — cleaner than linear interpolation for small N. Matches operator tooling conventions; swap to linear if 8.4 dashboards need it.
  8. **Curator latency read is uncached** — 24h window bounds the file scan (< 100KB typical). Caching is pre-optimization; route latency is DB-dominated anyway.
  9. **L0 ingest_rate_per_hour = raw count in last 1h** (not per-minute average) — matches the field name literally. Smoothing is a dashboard concern.
  10. **`vault_path` query param is operator-only** — documented as override; no MCP agent-facing path. Agents use `inspect_memory` to see vault state; direct vault-path aiming is out of their lane.
  11. **CI eval-gate widening is additive** — existing retrieval/recall/scoring globs kept; l0/l1/l2/stats/schema/write/migration added. Branch-protection (marking the job "required for merge") is an operator action outside this repo.
  12. **docs/architecture/memory-v3.md is committable** — the feedback_never_commit_docs rule covers planning/WIP (brainstorms, handover, plans, research, ideation, progress logs). An operator-facing architecture reference is a product deliverable — the plan explicitly names 8.4 as shippable. MASTER-PLAN.md is similarly treated as canonical coordination infra.
  13. **AGENTS.md edit is additive** — v2a invariants kept (still apply during PR 9 deprecation window); a new top-of-section pointer directs readers to the v3 doc.
  14. **No ce-demo-reel GIF in the PR body** — plan §8.4 skills matrix names it, but the Verify gate + architecture doc cover the same story textually. GIF can land in a follow-up if PR viewers want one; it's not a correctness gate.
  15. **Real chokidar IO bypassed in the Verify gate test** — we emit on the bus directly. Rationale: PR 1 unit 1.3 already pins watcher→bus; adding real chokidar to this file would make the suite flaky on CI filesystems without testing anything new.
- Known follow-ups deliberately deferred to PR 9:
  - Curator-driven apply for `fulcrum memory consolidate` (7.4 judgment call 8 still stands — consolidation prompt needs ce-optimize tuning).
  - Retrieval filter to exclude `curated/.archive/%` paths (PR 7 judgment call — lands in PR 9 cleanup).
  - Curator-latency-by-backend breakdown in /memory/stats (today's shape is aggregate only).
  - Per-workspace vs global-entity split in graph counts (Open Question #8 — shared entities behind an operator flag).
  - `fulcrum memory stats` CLI convenience wrapper (endpoint is the canonical surface; CLI is ergonomics).
- Next: PR 9 — Cleanup. Delete `applyKindCap`, `KIND_CAPS`, `canonical_text` column via `runMigration104MemoryV3DropCanonicalText(db)` (SQLite table-rebuild + FTS5 trigger rewire), drop `vault/memories/` dir, retire the `FULCRUM_MEMORY_V3` env flag (now the default). Load-bearing: agent-skills:code-simplification, compound-engineering:review:data-migration-expert (migration 104), compound-engineering:review:schema-drift-detector (FTS5 trigger rewrite), compound-engineering:git-clean-gone-branches (post-merge).
- Notes: Same doc-commit discipline observed — every ledger edit in a dedicated `docs(plans):` commit per feedback_never_commit_docs. Pre-existing unrelated WIP (agent-integration/claude/CLAUDE.md, packages/cli/src/hooks.ts, packages/cli/src/tests/hook-pre-post.test.ts, packages/core/, packages/worktrees/, .pi-lens/, .codex, docs/plans/2026-04-18-001- and 003- plan drafts) left untouched across all 6 PR-8 commits. Stopping per STOP CONDITIONS — end-of-PR hand-off.

### 2026-04-19 07:40 — PR 8 unit 8.4 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:documentation-and-adrs (user-facing reference; structured as operator cheatsheet, not a runbook — operators read it top-to-bottom once, then jump to section 4 or 6 for specific answers), elements-of-style:writing-clearly-and-concisely (Strunk pass: active voice, no hedging adverbs, concrete field tables over narrative), andrej-karpathy-skills:karpathy-guidelines (one file, no doc-as-code toolchain, no separate ADR — plan already has the decisions ledger at the end), compound-engineering:git-commit.
- Summary: `docs/architecture/memory-v3.md` (+240 lines) sections: 1 — three tiers (L0/L1/L2) with paths + guarantees + writers + readers; 2 — lifecycle (retention tiers, decay λ table, supersession, archival, consolidation, lint); 3 — curator pipeline diagram + backend selection order + model-per-task pinning; 4 — feature flag cheatsheet (every env var operators might touch, default + effect); 5 — inspection/correction table (CLI + MCP parity, mark-wrong flow, role gating); 6 — `/memory/stats` endpoint with sample JSON + CI gate note; 7 — end-to-end walkthrough (ingest → curate → recall → correct, both CLI and flags); 8 — rollback guardrails (TTY + UUID + no agent surface); 9 — files-of-interest map. Cross-linked to the plan; no new planning content.
- Commit: 738581f (1 file, +240 lines)
- Next: PR 8 unit 8.5 — refresh `agent-integration/claude/CLAUDE.md` + `AGENTS.md` + `docs/plans/MASTER-PLAN.md` + ONBOARDING.md memory section to reflect shipped v3 state (remove draft language, point to the new architecture doc, update the MASTER-PLAN flag table column to "shipped").
- Notes: Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before 8.5 if any is wrong):
  (a) **docs/architecture/ is the right path, not docs/reference/ or docs/guides/** — existing docs/ subfolders include guides/, decisions/, plans/, handover/, audit/; architecture/ is new. Matches the plan's own §8.4 naming. The never-commit-docs rule applies to planning/WIP (brainstorms, handover, plans, research, ideation, progress logs) — NOT to user-facing product references. Treated as a shippable deliverable.
  (b) **Single file, not a multi-doc tree** — one 240-line reference beats a ten-file tree for operators. Sections are H2 so any section can be deep-linked. If it grows past ~500 lines, split by tier then.
  (c) **Sample JSON in §6 uses illustrative numbers** — marked as a shape reference. Real numbers vary per install; a "this is the contract, not a dashboard" framing keeps the doc from going stale when counts change.
  (d) **The Curator pipeline ASCII diagram lives in §3**, not a PNG — renders in every Markdown viewer including the MCP inspector. A future PR-body GIF (ce-demo-reel) complements but doesn't replace it.
  (e) **No reference to PR numbers in user-facing prose** — internal plan references are fine; the operator doc names features by what they do, not when they shipped. Exception: the "(PR 8.1)" etc. annotations in §9 files-of-interest are navigation aids for developers, not operators.
  (f) **Rollback section is last (§8)** — purposely. 99% of operators never touch it; burying it reduces chance of accidental invocation. The technical guardrails enforce correctness regardless of doc ordering.
  (g) **Doc commits don't trigger the CI gate** — `.md` files aren't in `memory-eval.yml`'s path filter. Correct: the eval tests code, not documentation.
- No test count delta (pure docs). Full `pnpm -r build` not re-run — nothing compiled changed.

### 2026-04-19 07:30 — PR 8 unit 8.3 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (11 compute-stats cases — empty workspace, L0 total+ingest rate window, L0 workspace isolation, L1 by_tier + supersession counter, confidence histogram bucketing [0.0-0.1)..[0.9-1.0], graph node+edge per-workspace, curator log p50/p95 parsing, 24h window filter, missing-log nulls, malformed-JSONL tolerance; + 4 HTTP endpoint cases — default ws shape, seeded counts, 400 on missing ws, explicit ws query param), agent-skills:api-and-interface-design (`MemoryV3Stats` + `MemoryV3StatsOptions` are the single public shape the monitor endpoint + future CLI consume; 10 histogram buckets locked at 0.1 width so PR 8.4 dashboards render a consistent grid), agent-skills:performance-optimization (three O(1) COUNT queries + one O(n) scan over v3 rows; curator log parsing bounded by 24h window + swallows malformed lines without aborting the request), agent-skills:security-and-hardening (endpoint returns 400 when workspace_id missing AND server has no default — cross-workspace stat bleed blocked at the router; vault_path query param is operator override only, defaults to getVaultPath() so agents can't aim the reader at arbitrary paths), agent-skills:ci-cd-and-automation (widened `memory-eval.yml` PR-trigger paths to every v3 surface — l0/l1/l2/stats.ts/schema.ts/write.ts/migration — any change that can affect recall now blocks on the eval gate), compound-engineering:review:api-contract-reviewer (MemoryV3Stats shape additive; GET /memory/stats is new — no existing monitor route touched), compound-engineering:review:performance-reviewer (log parse is last-24h filter, single pass; no index needed on l0_sources.created_at because the existing idx_l0_sources_created from migration 102 satisfies the range query), compound-engineering:review:security-reviewer (vault_path operator override bounded; error shape matches existing `{error:string}` convention), andrej-karpathy-skills:karpathy-guidelines (one compute fn + one HTTP route + one workflow edit; rejected caching/memoization, rejected push-to-prometheus adapter, rejected an in-process metrics accumulator), compound-engineering:git-commit.
- Summary: `packages/memory/src/stats.ts` (+173 lines) exports `computeMemoryV3Stats(db, {workspace_id, vaultPath, now?})`. Returns `{l0:{total, ingest_rate_per_hour}, l1:{total, superseded, by_tier, confidence_histogram}, graph:{nodes, edges}, curation:{runs_last_24h, p50_duration_ms, p95_duration_ms}}`. Histogram is 10 fixed 0.1-wide buckets; confidence=1 folds into the last bucket. Percentiles use nearest-rank method (ceil(p*n)-1 index). Curator log parse reads vault/curated/log.md line-by-line, skips malformed lines, filters to the last 24h, returns nulls when the file is missing or empty. `packages/monitor/src/server.ts` gains `GET /memory/stats?workspace_id=...&vault_path=...` that lazy-imports `computeMemoryV3Stats` + `getVaultPath`, reuses the server-level workspace default, and returns 500 on internal errors (matches the existing monitor error-shape). `.github/workflows/memory-eval.yml` widens the pull_request path filter so l0/l1/l2/stats/schema/write/migration changes now trigger the recall-eval gate in addition to the original retrieval/recall/scoring/eval globs.
- Commit: e856453 (6 files, +596: memory stats.ts new +173, memory tests/stats.test.ts new +256 across 11 cases, memory index.ts +4 barrel, monitor server.ts +17 route, monitor tests/memory-stats-endpoint.test.ts new +99 across 4 cases, .github/workflows/memory-eval.yml +10 path globs)
- Next: PR 8 unit 8.4 — `docs/architecture/memory-v3.md`. User-facing doc: layer glossary, curator+lifecycle flow, ingestion → recall → mark-wrong → re-curate walkthrough, feature-flag map, /memory/stats field reference. Load-bearing: agent-skills:documentation-and-adrs, elements-of-style:writing-clearly-and-concisely, compound-engineering:ce-demo-reel (GIF for PR body).
- Notes: Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before 8.4 if any is wrong):
  (a) **10 fixed 0.1-wide histogram buckets** — plan §8.3 names "confidence distribution histogram" without pinning bucket count. 10 is the common default; exposing as a parameter is premature. Dashboards (PR 8.4) render a 10-bar chart; operator-tunable resolution lands when someone actually asks.
  (b) **Percentile = nearest-rank, not linear interpolation** — matches common operator tooling (Prometheus `histogram_quantile` uses linear interp, but over histogram buckets, not raw samples). For N<20 nearest-rank is cleaner and avoids div-by-zero edge cases. Swap to linear interp if PR 8.4's dashboards expect it.
  (c) **Curator latency reads vault/curated/log.md at request time** — no caching. 24h window bounds the scan size (typical < 100KB); adding a cache is pre-optimization. Route latency is dominated by DB queries regardless.
  (d) **L0 ingest_rate_per_hour = COUNT in last 1h (not per-minute average)** — matches the "rate per hour" phrasing. Dashboards that want smoother curves can query the endpoint repeatedly and average.
  (e) **Graph counts are workspace-scoped via graph_entities/edges.workspace_id** — matches the existing getMemoryMetrics pattern. Cross-workspace (global entity) pages are behind an operator flag per Open Question #8 (not yet implemented); when that lands, stats need a "global_nodes" counter too.
  (f) **No CLI yet** — `fulcrum memory stats` is a future convenience (was not in the plan for 8.3). Endpoint + compute-fn are the surface; CLI is a thin add-on if operators ask.
  (g) **CI eval-gate widening is non-destructive** — the existing retrieval/recall/scoring/eval globs remain; new globs are additive. Branch-protection setup (marking the check required for merge) is an operator action outside this repo; the workflow already runs on every matching PR.
  (h) **404/500 route semantics** — endpoint returns 500 on any internal error (import failure, DB issue). Monitor convention is `{error: string}`; we match. Callers treat 500 as retryable, 400 as terminal.
  (i) **`vault_path` query param is operator-only** — no agent-facing use case for pointing the reader at a custom vault. Documented as operator override; agent-facing MCP does not expose it.
- fulcrum-memory 1104/1104 (+11 new cases), fulcrum-monitor 116/118 passed + 2 skipped (was 112 passed — +4 new cases). Full `pnpm -r build` clean. Doc-commit discipline observed.

### 2026-04-19 07:15 — PR 8 unit 8.2 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (11 scheduler cases — env-only / explicit / unknown cadence gating, hourly + daily cadence, pre-interval quiet, stop cancels next tick, onError keeps loop alive, stop-mid-run no leak; + 4 wiring cases — env unset / never / fortnightly / daily), agent-skills:api-and-interface-design (`CADENCE_MS` table + `ConsolidateScheduleOptions` + `ConsolidateLogEntry` are new public exports PR 8.3 metrics panel will read), agent-skills:security-and-hardening (`never` + unknown cadence MUST NOT start the loop — the env var is an operator switch, not a suggestion; test-pinned), agent-skills:performance-optimization (setTimeout-chain instead of setInterval — avoids overlapping runs when a pass takes longer than the interval; re-schedules only after the current pass resolves or rejects), compound-engineering:review:api-contract-reviewer (all three new symbols additive; no existing signatures touched), compound-engineering:review:reliability-reviewer (loop survives runPass errors via onError hook; stop() sets `stopped=true` before cancelling so in-flight passes can't reschedule), andrej-karpathy-skills:karpathy-guidelines (one cadence table, one Map of intervals, no pluggable cron-parser — rejected `cron` library dep and cron-expression parsing; if operators need sub-daily, they add a new cadence key to the table), compound-engineering:git-commit.
- Summary: `packages/memory/src/l1/consolidate-schedule.ts` (+80 lines) exports `startConsolidateSchedule({cadence?, runPass, scheduler?, onError?})`. Resolved cadence → `CADENCE_MS[cadence]` lookup; missing or unknown cadence returns a no-op stop (no subscription, no log). On each tick, the async IIFE awaits runPass, forwards any throw to onError, and reschedules only if `stopped===false`. `packages/memory/src/l1/consolidate-log.ts` (+28 lines) exports `appendConsolidateLog(vaultRoot, entry)` that JSONL-appends to `vault/curated/consolidate.log.md` (kept separate from curator log.md because consolidation passes are dry-run discovery, not curator writes). `packages/cli/src/commands/memory-consolidate-schedule-wiring.ts` (+54 lines) is the server-process glue: resolves workspace+vault once, builds a runPass that scans + logs, wires into `runServeMonitor`, `runServeMcp`, and `runServeAll` behind the FULCRUM_MEMORY_CONSOLIDATE_SCHEDULE gate.
- Commit: c04ff27 (7 files, +556: memory consolidate-schedule.ts new +80, memory consolidate-log.ts new +28, memory tests/l1-consolidate-schedule.test.ts new +195 across 11 cases, memory index.ts +5 barrel, cli commands/memory-consolidate-schedule-wiring.ts new +54, cli tests/memory-consolidate-schedule-wiring.test.ts new +68 across 4 cases, cli index.ts +27 wiring across three serve entry points)
- Next: PR 8 unit 8.3 — `GET /memory/stats` + CI eval gate. Monitor endpoint returns {l0_ingest_rate_per_hour, l1_page_count_by_tier, curation_latency_p50_ms, curation_latency_p95_ms, graph_nodes, graph_edges, confidence_histogram}. Wire `fulcrum memory eval --corpus retrieval-corpus` as a required CI gate.
- Notes: Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before 8.3 if any is wrong):
  (a) **`hourly` and `daily` are the only first-class cadences** — plan §8.2 names `daily` explicitly; `hourly` added for operator tuning without pulling in a cron-parser dep. Unknown strings (`7d`, `every-monday`) return no-op stop instead of throwing, matching the "opt-in, default-off" ethos of Critical Constraint #6.
  (b) **setTimeout-chain, not setInterval** — a slow pass (e.g. 10-minute scan on 100k pages) won't overlap with the next fire; the next tick waits until the current pass resolves. Trade-off: cadence drifts by the pass duration. Acceptable for a daily observability pass; noted for future tuning.
  (c) **No immediate fire on startup** — first tick is one full interval after boot. Rejected "fire immediately then every Nms" because a monitor restart would re-scan on every boot; the regular cadence is the contract.
  (d) **Log path is `curated/consolidate.log.md`** — `.md` extension keeps Obsidian rendering it as a preformatted block (same pattern as `log.md`); `.jsonl` would work too but splits the tooling story. The file is append-only; rotation is an operator concern.
  (e) **`min_confidence` hardcoded to 0.5** in the wiring — matches `findConsolidationCandidates` default from PR 7.4 judgment call (a). Env-configurable wrapper deferred; the default is good enough for observability.
  (f) **`cadence` from env wins when the explicit option is undefined** — matches the auto-curate pattern (8.1). An explicit `never` beats env=`daily` so tests stay deterministic without managing env state.
  (g) **No MCP parity** — consolidation discovery is a scheduled back-office pass, not an agent action. Agents still call the synchronous `consolidate_memory` tool (7.4); the cron is operator-only.
- fulcrum-memory 1093/1093 (+11 new cases), fulcrum-agent-cli 447/447 (+4 new cases; +1 pre-existing WIP unrelated). Full `pnpm -r build` clean. Doc-commit discipline observed — this ledger entry lands in a separate docs commit per `feedback_never_commit_docs`.

### 2026-04-19 07:00 — PR 8 unit 8.1 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (15 engine cases — env on/off gating, add-only filter, l0_raw-only filter, non-l0src_ basename filter, per-id debounce coalesce, independent-ids parallel timers, teardown clears timers + listener, onError propagation; + 3 wiring cases — env off no-op, env=0 no-op, env=1 mounts watcher+sub), agent-skills:api-and-interface-design (`AutoCurateOptions` + `AutoCurateScheduler` are new public exports PR 8.2+ dashboards and future bus consumers will lean on — the injected-scheduler shape is the test seam; locking it here), agent-skills:security-and-hardening (Critical Constraint #6 — `FULCRUM_MEMORY_CURATE_AUTO` MUST default off; tests pin both env-absent and env='0' no-op paths), compound-engineering:agent-native-architecture (curate callback is injected so the same auto-loop serves cli's curateMemory today and future alt-runtimes tomorrow without module changes), compound-engineering:review:api-contract-reviewer (new exports are additive only — memory/index.ts barrel gains `startAutoCurator` + `DEFAULT_AUTO_CURATE_DEBOUNCE_MS`, no existing signatures touched), andrej-karpathy-skills:karpathy-guidelines (one subscription + one Map of timers — no event-router framework, no pluggable filter registry; rejected ideas: mounting chokidar directly in the auto-curator, adding a parallel fulcrum-event-bus subscription as a defensive secondary path), compound-engineering:git-commit.
- Summary: `packages/memory/src/l1/auto-curate.ts` (+106 lines) exports `startAutoCurator({enabled?, debounceMs?, curate, bus?, scheduler?, onError?})`. Subscribes to `getContentChangeBus()`, filters `kind === 'l0_raw' && change_type === 'add' && basename matches /^l0src_[0-9A-HJKMNP-TV-Z]{26}$/`, debounces 30s per l0_id (later events reset the timer; independent ids get independent timers). Returns a stop fn that removes the bus listener and clears every pending timer. Enabled gate: explicit `enabled` wins over `FULCRUM_MEMORY_CURATE_AUTO`; when disabled the function returns a no-op stop and never subscribes (zero cost on default installs). `packages/cli/src/commands/memory-auto-curate-wiring.ts` (+57 lines) is the server-process glue: mounts `startVaultWatcher` with no-op human callbacks (L0/L1 tiers don't need them) AND the auto-curator with a curate callback that calls `curateMemory({l0_id})`; wired from `runServeMonitor`, `runServeMcp`, and `runServeAll` so the opt-in flag activates on every serve entry point. Stop fn is idempotent + best-effort in both directions.
- Commit: 6e914db (6 files, +562 total: memory auto-curate.ts new +106, memory tests/l1-auto-curate.test.ts new +261 across 15 cases, memory index.ts +4 barrel, cli commands/memory-auto-curate-wiring.ts new +57, cli tests/memory-auto-curate-wiring.test.ts new +59 across 3 cases, cli index.ts +27 wiring across three serve entry points)
- Next: PR 8 unit 8.2 — scheduled consolidation pass via `fulcrum serve monitor` cron. `FULCRUM_MEMORY_CONSOLIDATE_SCHEDULE=daily` (or `hourly`, `never`) drives a setInterval-backed task that calls `findConsolidationCandidates` and appends to a run log. No real apply path (7.4 judgment call 8); this is dry-run observability.
- Notes: Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before 8.2 if any is wrong):
  (a) **Bus-subscription design, not direct chokidar mount** — the watcher already emits `{kind:'l0_raw'}` on the unified bus per PR 1 unit 1.3. Routing through the bus keeps watcher as the single filesystem owner and makes tests cheap (no real IO). If a future non-watcher producer (MCP ingest, git hook) wants to trigger auto-curate, emit on the bus too.
  (b) **Only `change_type='add'` fires the curator** — L0 is immutable per Critical Constraint #2. `change` events are defensive-ignored; `unlink` does not retract pending timers for the same id (rare race; re-ingest on next add will re-schedule).
  (c) **Per-l0_id debounce key, not per-path** — two adds for the same id (would require a filesystem rename-then-rewrite, which L0 writers don't do) would coalesce; two paths for the same id cannot happen under the ingest contract. Keying on id is the right semantic.
  (d) **Basename regex matches `l0src_<26 Crockford base32>`** — matches `newId('l0_source')` output exactly. Any non-matching basename under raw/ (README, .gitkeep, manually-dropped .md) is ignored — a correctness-over-convenience choice.
  (e) **Injected scheduler + bus seams are part of the public type** — lets PR 8.2 cron reuse the same injection pattern and keeps tests synchronous. Locking this at 8.1 rather than exposing only `startAutoCurator(options)` and forcing future tests to monkey-patch globals.
  (f) **Wiring helper lives in cli, not memory** — calling `curateMemory` (cli) from a memory module would be a circular dep. Keeping the curate callback injectable keeps the memory package free of cli imports.
  (g) **`startVaultWatcher` with no-op callbacks** — v2a-tier human-edit/delete callbacks need real wiring for v2a parity (separate concern); for PR 8.1 the watcher just needs to fire v3 tier events. Full v2a wiring is downstream of the v3 cutover.
  (h) **Concurrency of overlapping curate calls is NOT serialized** — two independent l0_ids fire their curate callbacks in parallel. Curator backends handle their own rate limits; global serialization would hide latency issues and reduce throughput on multi-l0-dump sessions. Revisit if backend rate-limiting surfaces as a problem.
  (i) **onError swallows the failure** — plan doesn't pin behaviour. Chose swallow+log so one poisoned L0 doesn't kill the whole loop. Operator can tail `/memory/log.md` (8.3) to see failures.
- fulcrum-memory 1082/1082 (+15 new cases), fulcrum-agent-cli 442/442 (+3 new cases). Full `pnpm -r build` clean. Doc-commit discipline observed — this ledger entry lands in a separate docs commit per `feedback_never_commit_docs`. Pre-existing unrelated WIP left untouched.

### 2026-04-19 06:00 — PR 7 — COMPLETE (all 5 units + Verify gate shipped)
- Skills invoked across units: agent-skills:context-engineering, agent-skills:test-driven-development, agent-skills:incremental-implementation, agent-skills:api-and-interface-design (`ApplyDecayResult`/`ArchivePageResult`/`ConsolidationCandidate`/`LintCounts`+`LintIssueCode`/`CuratorNewPage.contradicts` — five new public contracts this PR adds; all additive), agent-skills:performance-optimization (10k-page decay in <200ms via single-txn batch; lint's per-page file read is O(n) with one l0_sources map shared across pages), agent-skills:security-and-hardening (validator re-runs with phase='migration' during retrospective lint — rule 7 waiver prevents false alarms on partially-cutover supersession chains; contradicts auto-supersession fails soft on missing/pre-v3/already-superseded rows instead of aborting the batch), agent-skills:deprecation-and-migration (archival via `curated/.archive/` prefix move keeps schema stable — no 104 migration needed), compound-engineering:review:performance-reviewer (decay single-txn bulk, lint per-l0_sources map once, consolidate grouping O(n) with hash-key join on sorted entities), compound-engineering:review:api-contract-reviewer (each additive change reviewed — LintCounts + LintIssueCode enums, ConsolidationCandidate shape, CuratorNewPage.contradicts, consolidate_memory MCP schema), compound-engineering:review:cli-readiness-reviewer (consolidate CLI uses JSON stdout + long --help + flag parsing; no config-file indirection), compound-engineering:review:data-integrity-guardian (decay + archive + contradiction paths all inside db.transaction; lint is read-only), compound-engineering:agent-native-architecture (consolidate_memory MCP tool + CLI share one function; lint_memory already paired in PR 6.5), compound-engineering:ce-debug (three red-runs caught: `last_confirmed` vs `updated_at` column-alias drift in 7.1, `byte_count` vs `size_bytes` in 7.3 tests, test vault files not mirrored from seedL0 in 7.3 — all fixed pre-commit), andrej-karpathy-skills:karpathy-guidelines (no lifecycle framework; no pluggable consolidation strategy; no speculative curator-apply path in 7.4 — those land when the consolidation prompt stabilises), compound-engineering:git-commit.
- Units shipped:
  - 7.1 l1/lifecycle.ts — applyDecay + promoteToTier + archivePage — commit 04c4dd6 (+556, 3 files, 18 cases)
  - 7.2 Contradiction detector — CuratorNewPage.contradicts + auto-supersession — commit 8966253 (+381, 3 files, 11 cases)
  - 7.3 Lint extensions — BROKEN_WIKILINK + STALE_CLAIM + DIVERGENCE + TEMPLATE_VIOLATION — commit e13c375 (+445/-33, 2 files, 11 cases)
  - 7.4 fulcrum memory consolidate — engine + CLI + MCP — commit 4dac231 (+620, 8 files, 18 cases)
  - 7.5 PR 7 Verify gate — commit 8079990 (+279, 1 file, 12 cases)
- PR 7 totals: 5 code commits, +2281 lines, 0 regressions. fulcrum-memory 1067/1067 (was 1001 at PR 6 close — +66 new cases across lifecycle/contradicts/lint-ext/consolidate/verify). fulcrum-agent-cli 439/439 (was 432 — +7 new cases: consolidate CLI shim + MCP registry presence). Full `pnpm -r build` clean.
- Verify gate (plan §PR 7): `pr7-verify-gate.test.ts` pins: (a) decay curve matches exp(-λ·t) within 1% across 8 (tier, days) points spanning working/episodic/semantic/procedural; (b) consolidation dry-run returns the two-page collision + skips below-floor groups; (c) contradiction round-trip — seed old L0 + L1 page → inject correction L0 + curator output with `contradicts=[old_id]` → applyCuratorOutput → old row's `superseded_by` flips to new id + new page's `supersedes[]` gains old_id + lint ok=true across every additive counter; (d) lifecycle compose: decay → promoteToTier → archivePage all layer cleanly on one page.
- Agent-native parity (plan §Critical Constraints #8): `consolidate_memory` is the single new MCP tool this PR adds (readOnlyHint=true). `lint_memory` already present from PR 6.5; the 7.3 lint extensions surface via the same MCP tool without a schema version bump. No new agent-exposed write operations.
- Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before PR 8 if anyone disagrees):
  1. **Archival = vault_path prefix move** (not a new DB column). `curated/.archive/` is recognizable by retrieval filters later; no 104 migration needed. If a future ops dashboard wants archived_at as a first-class timestamp, add it via a nullable column in a new migration — the existing data model survives.
  2. **Decay anchor = `confidence_decay_at ?? updated_at`** — the l1_pages view aliases updated_at as last_confirmed, so reading the base column keeps the decay function working whether callers go through the view or the table. A real `last_confirmed` column added later would need a rename pass.
  3. **Decay idempotency = 1-hour window** — DECAY_MIN_INTERVAL_DAYS = 1/24 suppresses cosmetic updates on scheduled re-runs. Plan doesn't pin a cadence; 1h matches the "scheduled nightly but may fire hourly" operator mental model.
  4. **contradicts auto-supersede is inclusive >=** — equal confidence supersedes. Freshness breaks ties. Raise if strict > is preferred.
  5. **contradicts in NEW_PAGE_SCHEMA.required** — strict-mode Structured Outputs backends must emit an empty array for "no contradictions". parseCuratorOutput still tolerates missing for non-strict backends and pre-7.2 fixtures.
  6. **DIVERGENCE + TEMPLATE_VIOLATION counters are per-page**; BROKEN_WIKILINK + MISSING_SOURCE are per-link. Matches how plan §7.3 phrases each category.
  7. **Lint retrospective validator runs in phase='migration'`** — rule 7 waiver prevents false alarms on pre-cutover supersession chains. In a fully cut-over world (PR 9 cleanup), tighten to 'live'.
  8. **Consolidate has no apply path in 7.4** — plan wording is ambiguous between "emit candidate list" and "call the curator". Dry-run is real; apply lands when the consolidation prompt stabilises (PR 8 ce-optimize tunes λ + retention thresholds + merge heuristics together).
  9. **Default min_confidence = 0.5** for consolidate — matches the STALE_CLAIM threshold. Operator tuning lands in PR 8.
  10. **BROKEN_WIKILINK only fires when the l0_sources row exists but the file is gone** — avoids double-counting MISSING_SOURCE (row missing) or SOURCES_WIKILINK_DIVERGENCE (body references an L0 not in frontmatter).
- Known follow-ups deliberately deferred:
  - Curator-apply mode for `fulcrum memory consolidate` (see judgment call 8).
  - Retrieval filter for archived pages (`vault_path NOT LIKE 'curated/.archive/%'`) — lands in PR 8 observability or a targeted v3-search edit.
  - Telemetry split between explicit supersessions and auto-contradictions (PR 3.7 log additive).
  - Empirical tuning of decay λ + retention-tier thresholds + STALE_CLAIM/min_confidence defaults against the eval corpus — PR 8 ce-optimize lap.
- Next: PR 8 — Auto-triggers (opt-in), observability, docs. Vault watcher fires L0 → curator on new files (debounced 30s) when `FULCRUM_MEMORY_CURATE_AUTO=1`. Scheduled consolidation pass via monitor cron. `GET /memory/stats` for the L0 ingest rate + L1 tier counts + curation p50/p95 + graph + confidence histogram. User-facing docs at `docs/architecture/memory-v3.md`. Load-bearing: agent-skills:ci-cd-and-automation (eval gate), agent-skills:shipping-and-launch (pre-launch checklist), compound-engineering:ce-demo-reel (ingest → curate → recall → mark-wrong → re-curate GIF), compound-engineering:onboarding (ONBOARDING.md memory section refresh), agent-skills:documentation-and-adrs, elements-of-style:writing-clearly-and-concisely.
- Notes: Same doc-commit discipline observed — every ledger edit in a dedicated `docs(plans):` commit per feedback_never_commit_docs. Pre-existing unrelated WIP (agent-integration/, packages/core/janitor.ts, packages/worktrees/, .pi-lens/, .codex, plan edits, etc.) left untouched across all 5 code commits. Stopping per STOP CONDITIONS — end-of-PR hand-off.

### 2026-04-19 05:30 — PR 7 unit 7.4 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (12 engine cases + 6 CLI shim cases — entity-set grouping, canonical entity ordering, retention-tier isolation, min_confidence floor, superseded-page skip, pre-v3 skip, empty-entities skip, workspace scope, project scope, per-candidate min_confidence_in_group reporting, default floor = 0.5, MCP registry presence), agent-skills:api-and-interface-design (`ConsolidationCandidate` + `ConsolidationOptions` exported public types — PR 8 dashboards and future curator-apply path consume the shape; `{entity_set, retention_tier, page_ids, min_confidence_in_group, workspace_id, project_id}` locks at PR 7.4), compound-engineering:agent-native-architecture (`consolidate_memory` MCP tool + `fulcrum memory consolidate` CLI share one `consolidateMemory` function — plan §Critical Constraints #8 parity), compound-engineering:review:cli-readiness-reviewer (JSON stdout, long --help, flag parsing kept minimal; no config-file indirection), compound-engineering:review:api-contract-reviewer (MCP tool schema pinned readOnlyHint=true — consolidate is deterministic with no DB writes in 7.4), andrej-karpathy-skills:karpathy-guidelines (one deterministic function — no plugin framework, no abstraction layer for "future strategies"; curator-apply path deliberately NOT stubbed — we ship real dry-run behaviour and add the apply path when the consolidation prompt stabilises), compound-engineering:git-commit.
- Summary: `packages/memory/src/l1/consolidate.ts` (+117 lines) exports `findConsolidationCandidates(db, opts)`. SELECT across `memories` filters schema_version >= 3 + superseded_by IS NULL + workspace + optional project + optional retention_tier; groups are keyed by `retention_tier::sorted(entities).join(U+0001)` so A+B and B+A fold into one candidate; groups of < 2 pages drop; groups where `min(confidence) < floor` drop. `packages/cli/src/commands/memory-consolidate.ts` (+34 lines) is the CLI shim that pulls `workspace_id` from cwd when unset. CLI dispatch in `packages/cli/src/index.ts` adds `fulcrum memory consolidate` with `--min-confidence`, `--retention-tier`, `--workspace`, `--project` flags, JSON stdout, and long --help. `consolidate_memory` MCP tool in mcp-tools.ts + tool-registry.ts gives agents parity.
- Commit: 4dac231 (8 files, +620 total: memory consolidate.ts new +117, memory tests/l1-consolidate.test.ts new +181 across 12 cases, memory index.ts +6 barrel, cli memory-consolidate.ts new +34, cli tests/memory-consolidate.test.ts new +120 across 6 cases, cli index.ts +50 subcommand + help, cli mcp-tools.ts +15 tool schema, cli tool-registry.ts +18 handler)
- Next: PR 7 unit 7.5 — Verify gate. Integration tests: (1) decay curve over multi-day windows matches Ebbinghaus within 1%; (2) consolidation dry-run prints the expected merge candidates against a seeded corpus; (3) contradiction round-trip — inject an L0 correction → curator output → apply supersession → lint clean.
- Notes: Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before 7.5 if any is wrong):
  (a) **Default min_confidence = 0.5** — plan doesn't pin a specific value; 0.5 matches the stale-claim threshold in 7.3 (confidence > 0.5 is the "worth keeping" bar). If eval corpus tuning (PR 8 ce-optimize) says 0.6 or 0.7, change via flag first, then update default.
  (b) **Canonical sort of entities[]** using lexicographic sort on the raw ULID strings — stable across runs, no locale concerns, no hash collision risk. U+0001 as the join separator avoids collision with any legal ULID character (0-9, A-Z).
  (c) **No curator apply path in 7.4** — the plan §7.4 wording "proposes a merged page to curator" is ambiguous: do we (i) emit the candidate list (what 7.4 does) or (ii) call runCurator with a synthesis task and write the merged page? I went with (i) because the consolidation prompt isn't yet tuned (PR 3 pinned gpt-5-nano/minimal for consolidation but the prompt content is still the extraction prompt). Shipping real dry-run data beats shipping a speculative apply mode that we'd have to rewrite when the prompt lands. Raise if the bar for this unit was (ii).
  (d) **graph_entities seeding bypasses upsertEntity** in tests — the 7.1/7.4 tests seed entities directly into SQLite because upsertEntity generates its own ULID and the test fixtures need deterministic IDs matching the page.entities[] references. upsertEntity is still the production entry point; test-only direct inserts match the pattern used in other entity-adjacent tests.
  (e) **MCP tool name = `consolidate_memory`** (not `memory_consolidate`) — matches the existing `lint_memory` / `mark_memory_wrong` / etc. verb_noun ordering. Agents discover tools via TOOL_SCHEMA_MAP.get which is additive — no schema versioning broken.
- fulcrum-memory 1055/1055 (+12 new cases), fulcrum-agent-cli 439/439 (+7 new cases: 6 shim + 1 MCP presence). Full `pnpm -r build` clean. Doc-commit discipline observed.

### 2026-04-19 05:00 — PR 7 unit 7.3 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (11 new TDD cases — BROKEN_WIKILINK file-missing, BROKEN_WIKILINK does-not-double-count-missing-source, STALE_CLAIM 90d+high-conf, STALE_CLAIM low-conf skipped, STALE_CLAIM recent skipped, DIVERGENCE fm-extra, DIVERGENCE body-extra, DIVERGENCE none-on-agreement, TEMPLATE_VIOLATION hand-edit, TEMPLATE_VIOLATION clean-vault, ok-aggregation both directions), agent-skills:api-and-interface-design (LintIssueCode + LintCounts are public contracts PR 8 dashboards consume; additions are additive — no breaking changes to 6.5 shape; every new code has a stable detail string), agent-skills:performance-optimization (single-pass over pages — l0_sources map loaded once; per-page vault read is O(n) I/O and is the dominant cost; no nested queries), agent-skills:security-and-hardening (`validateL1Page` runs in `phase: 'migration'` to waive rule 7 retrospectively — prevents false alarms on pre-cutover v3 pages that supersede rows not yet bumped; rule 7 false alarms would bury the real defects), compound-engineering:review:api-contract-reviewer (LintReport shape is additive: `counts` gains four keys + `issues[].code` gains four enum values; old consumers keep working and ignore unknown codes), compound-engineering:ce-debug (first red-run showed seed L0 helper doesn't write vault files — fixed seed to mirror real ingest; second red-run showed default `now` was wall-clock-dependent — pinned `now` in ok-aggregation test), andrej-karpathy-skills:karpathy-guidelines (one function with one loop — no lint-framework, no per-category plug-in registry; counters + issues accumulate additively), compound-engineering:git-commit.
- Summary: `packages/memory/src/migration/lint.ts` expands from 4 to 7 LintIssueCode values + 4 new counters on LintCounts. Added `LintOptions = {vaultPath?, now?}` signature so the vault-aware checks (broken-wikilink, template violation, divergence) can read files and the stale-claim check can pin a clock. For each v3 page the loop now: (1) runs the stale test on `updated_at` + confidence; (2) if vault_path + file exist, parses the vault file via `parseCuratedPage`, re-runs `validateL1Page` with phase=migration, and diffs frontmatter `sources[]` against inline `[[raw/...]]` ULIDs; (3) checks each inline ULID against `l0_sources` + the L0 file on disk. Parse errors count as TEMPLATE_VIOLATION. Every counter contributes to `ok` (migration_stubs stay excluded per plan §6.2). `packages/memory/src/tests/migration-lint-ext.test.ts` (+261 lines) covers the new codes; seedL0 helper now mirrors real ingestion by writing both the DB row AND the vault file so broken-wikilink unlink tests are deterministic.
- Commit: e13c375 (+445/-33 across 2 files: migration/lint.ts +184 rewrite to add vault-aware loop, tests/migration-lint-ext.test.ts new +261 across 11 cases)
- Next: PR 7 unit 7.4 — `fulcrum memory consolidate`. Finds pages with same entities[] set + same retention_tier + confidence ≥ threshold; proposes a merged page to the curator (dry-run default). CLI + MCP parity. Engine in memory package, thin shim in cli.
- Notes: Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before 7.4 if any is wrong):
  (a) **STALE_CLAIM uses `updated_at`** (the base column the l1_pages view aliases as last_confirmed) for the Δdays arithmetic — matches the decay path in 7.1 so lint and decay see the same "last confirmed" timestamp.
  (b) **Broken-wikilink NEVER double-counts MISSING_SOURCE** — if the l0_sources row is missing, that defect is already covered by MISSING_SOURCE (when the ULID appears in frontmatter.sources[]). Body-only references to a missing L0 are caught by SOURCES_WIKILINK_DIVERGENCE (body-extra). BROKEN_WIKILINK only fires when the l0_sources row EXISTS but the underlying vault file is gone — a genuinely separate defect class (vault rot).
  (c) **DIVERGENCE counter is per-page** — one count per page with any mismatch. Plan §7.3 phrases it as "frontmatter lists an L0 that's not referenced inline, or vice versa" which reads as per-page. BROKEN_WIKILINK counter is per-link (multiple per page are independently broken).
  (d) **`validateL1Page` runs with phase='migration'`** — rule 7 (SUPERSEDES_UNRESOLVED) needs waiving retrospectively or we false-alarm every page that supersedes a pre-cutover row. In a fully cut-over world (PR 8) this could tighten back to 'live'; for now migration is the safe default.
  (e) **Stale threshold fixed at 90d + 0.5** — plan §7.3 literal values. Tunable via `ce-optimize` in PR 8 against the eval corpus; not parameterized on the function signature today (premature).
  (f) **Migration stubs still excluded from every 7.3 counter** except STALE_CLAIM — a stub that's old + high-confidence should still show up; plan doesn't carve stubs out of the stale check and they SHOULD eventually be resolved or archived. Raise if stubs should be exempt from STALE_CLAIM too.
- fulcrum-memory 1043/1043 (+12 new cases), fulcrum-agent-cli 432/432 unchanged. Full `pnpm -r build` clean.

### 2026-04-19 04:30 — PR 7 unit 7.2 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (11 TDD cases — schema `contradicts` in required + properties, parser tolerates missing, inclusive >= confidence threshold, strict-less strictly-less skip, missing old_id skip, already-superseded skip, pre-v3 skip implicit via readCuratedPage, multi-old, empty-array no-op, dry_run no-mutation, multi-draft attribution by index), agent-skills:api-and-interface-design (extending `CuratorNewPage` + `NEW_PAGE_SCHEMA` is a public contract widening — additive so backwards-compatible, but strict-mode Structured-Outputs backends will require it going forward; parseNewPage's `?? []` shim lets pre-7.2 fixtures keep passing through), agent-skills:security-and-hardening (contradicts-driven auto-supersession runs in-transaction with the create; reject path is fail-soft — bad old_ids silently skip rather than aborting the whole curator batch), compound-engineering:review:api-contract-reviewer (NEW_PAGE_SCHEMA.required list expanded by one — strict-mode JSON Schema consumers must be aware; parseCuratorOutput gives them graceful degradation), compound-engineering:ce-debug (first red-run surfaced `byte_count` vs `size_bytes` column-name drift in l0_sources seeding — fixed pre-commit; second red-run surfaced WIKILINK_SOURCE_MISMATCH in tests that overrode `sources` without overriding `body` — fixed pre-commit), andrej-karpathy-skills:karpathy-guidelines (no "contradiction manager" module — one loop inside apply.ts, guarded by all four skip conditions; no policy framework for when to auto-supersede — one inclusive >= inequality), compound-engineering:git-commit.
- Summary: Three surgical edits. (1) `packages/memory/src/l1/curator.ts` adds `contradicts: string[]` to `CuratorNewPage` type + `NEW_PAGE_SCHEMA` (required + properties) + parser default coercion. (2) `packages/memory/src/l1/apply.ts` adds `applyContradictions()` that runs after every `applyNewPage` inside the same txn; walks `output.new_pages[i].contradicts[]`, skips non-existent/pre-v3/already-superseded/lower-confidence old rows, and for the rest: `UPDATE memories SET superseded_by = ?` on old + `updateCuratedPage` with merged supersedes[] on new. (3) `packages/memory/src/tests/l1-contradicts.test.ts` (+283 lines) exercises the full matrix including parser tolerance + schema shape + dry-run.
- Commit: 8966253 (+381 lines across 3 files: curator.ts +13 field + schema + parser, apply.ts +52 auto-supersession loop, tests/l1-contradicts.test.ts new +316 across 11 cases)
- Next: PR 7 unit 7.3 — extend `fulcrum memory lint` with broken-wikilink, stale-claim (last_confirmed > 90d AND confidence > 0.5), template-validator-retrospective, and sources[]↔inline-wikilink divergence checks. LintReport schema is additive — existing counts + issues shape stays; new issue codes land.
- Notes: Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before 7.3 if any is wrong):
  (a) **Inclusive >= for auto-supersession** — plan §7.2 says "confidence of new evidence ≥ old". Implemented as `draft.confidence < oldRow.confidence` ⇒ skip, i.e. `>= ` supersedes. Equality is deliberate — two equally-strong contradictory claims should favour the fresher one (newer evidence usually has more recent source traces). Raise if exclusive > is preferred.
  (b) **Silent skip for missing / pre-v3 / already-superseded old rows** — the curator may hallucinate IDs or contradict rows that another batch just superseded. Failing the whole apply on a bad contradicts entry would throw away the rest of the curator output. Silent skip is the karpathy-guidelines "trust internal code, only validate at boundaries" call.
  (c) **Validator rule 7 (SUPERSEDES_UNRESOLVED) lives in live phase** — the auto-supersede path calls `updateCuratedPage` which re-validates. Since we only add old_ids that we JUST confirmed exist in v3, rule 7 always passes. No waiver needed.
  (d) **Contradicts added to `NEW_PAGE_SCHEMA.required`** not an optional property. Strict-mode Structured-Outputs backends (codex + openai) treat missing required fields as validation failures, so this DOES change the wire format for those backends. Parser's `?? []` coercion is the defense for non-strict backends (anthropic tool_use) + pre-7.2 test fixtures. If this breaks a live codex-backed flow, flip the required list to exclude `contradicts` and rely on the parser default only.
  (e) **`superseded_pairs` captures both explicit supersessions and auto-contradictions** — telemetry can't yet distinguish the two. PR 3.7 log could be extended to mark auto-supersessions with a new reason key, but that's an additive follow-up not in 7.2 scope.
- fulcrum-memory 1031/1031 (+11 new cases), fulcrum-agent-cli 432/432 (no change). Full `pnpm -r build` clean. Doc-commit discipline observed.

### 2026-04-19 04:00 — PR 7 unit 7.1 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (18 TDD cases — per-tier λ constants, 1-day working decay, 10-day semantic, 30-day procedural, subsequent-pass anchor rotation, idempotency within 1h, pre-v3 skip, superseded-row skip, per-tier counts, 10k-page <10s, promote vault+DB parity, promote invalid-tier rejection, promote not_found, archive vault move, archive row preservation, archive idempotency, archive not_found, archive content integrity, 103-cutover compat), agent-skills:api-and-interface-design (`ApplyDecayOptions`/`ApplyDecayResult`/`ArchivePageResult` + exported `DECAY_LAMBDA_PER_DAY` constants are public contracts that PR 7.3 lint and PR 8 metrics consume), agent-skills:performance-optimization (10k pages under 10s via single-txn bulk UPDATE with prepared statement; idempotency guard DECAY_MIN_INTERVAL_DAYS = 1/24 prevents no-op thrash on scheduled re-run), agent-skills:source-driven-development (confirmed better-sqlite3 `db.transaction(batch)` signature — commits once per batch, not per row), compound-engineering:review:performance-reviewer pre-commit self-review (single SELECT + batched UPDATE; no per-row transaction overhead), andrej-karpathy-skills:karpathy-guidelines (one function per responsibility; no lifecycle framework; no retention-tier migration — archival via vault_path prefix filter is the cheapest path), compound-engineering:git-commit.
- Summary: `packages/memory/src/l1/lifecycle.ts` (+181 lines) exports `DECAY_LAMBDA_PER_DAY`, `applyDecay(db, {now?})`, `promoteToTier(page_id, target_tier)`, `archivePage(page_id, {vaultPath?})`. `applyDecay` reads `updated_at` as the base column (the l1_pages view aliases it as `last_confirmed` for the curator-facing frontmatter), anchors off `confidence_decay_at ?? updated_at`, computes `confidence *= exp(-λ * Δdays)` per tier, and skips rows whose last decay stamp is within 1 hour. Runs inside a single `db.transaction(batch)` for the whole pass — 10k pages in <200ms on the test machine (budget 10s). `promoteToTier` enforces L1_RETENTION_TIERS enum + dispatches to `updateCuratedPage` so the vault frontmatter mirrors the DB. `archivePage` moves `curated/<type>/<id>.md` to `curated/.archive/<type>/<id>.md` via `renameSync` + `UPDATE memories SET vault_path = ?`; second call returns `archived=false`; missing-file case still updates the row so lint/recall observe the archive path.
- Commit: 04c4dd6 (+556 lines across 3 files: l1/lifecycle.ts new +181, tests/l1-lifecycle.test.ts new +319, memory/index.ts +11 barrel re-export)
- Next: PR 7 unit 7.2 — contradiction detector. Add `contradicts: string[]` to `CuratorNewPage` shape; apply-layer auto-emits a supersession when `new_page.confidence >= old_page.confidence` for each id in `contradicts[]`. Additive to the curator output schema.
- Notes: Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before 7.2 if any is wrong):
  (a) **Anchor column is `updated_at`** (not `last_confirmed`) — `last_confirmed` is a view-only alias that the l1_pages view exposes. Reading the base column avoids the alias round-trip and matches the INSERT path in `createCuratedPage` (which writes `page.last_confirmed` → `updated_at`). If anyone adds a real `last_confirmed` column later, this function will need a rename pass.
  (b) **`DECAY_MIN_INTERVAL_DAYS = 1/24`** (1 hour) suppresses cosmetic updates on re-runs. Without this the `--cron` callers would rewrite every row on every tick (confidence *= exp(-0 * λ) ≈ 1) which is wasteful and risks FTS5 trigger amplification. Plan §Lifecycle doesn't pin a specific cadence; 1h is the operator-expected scheduled-pass granularity.
  (c) **Archival = vault_path prefix move, not a new column** — adding `archived_at TEXT` would require either ALTER TABLE (already wired into 101 via addColumnIfMissing, but 101 has shipped) or a new 104 migration. Archival via `curated/.archive/` + vault_path prefix is retrievable, reversible (operator moves file back + UPDATE vault_path), and doesn't widen the schema. Retrieval filter (`vault_path NOT LIKE 'curated/.archive/%'`) is a PR 7.3 lint follow-up or can land in PR 8 observability — deliberately deferred to keep 7.1 scope tight.
  (d) **`createCuratedPage` pre-stamps `confidence_decay_at`** with wall-clock-now at creation time (memories row INSERT). Tests must clear it so the first-pass anchor fallback (updated_at) exercises — this is a test-setup wrinkle, not a production issue; real-world flows bench the decay clock from creation time forward.
  (e) **Skipped superseded rows** in the SELECT filter — superseded pages won't surface in retrieval (supersession filter at PR 5), so decaying them is a pure write-amplification cost. If policy ever requires superseded-row decay for audit, drop the `superseded_by IS NULL` clause.
- fulcrum-memory 1020/1020 (+19 new cases). `pnpm -r build` clean. Doc-commit discipline observed — this ledger entry lands in its own `docs(plans):` commit.

### 2026-04-19 03:25 — PR 6 — COMPLETE (all 6 units + Verify gate shipped)
- Skills invoked across units: agent-skills:context-engineering, agent-skills:test-driven-development, agent-skills:incremental-implementation, agent-skills:api-and-interface-design (MigrationClass / MigrationRecord / MigrationBackfillResult / LintReport / RollbackToV2Input / MigrateResult are all public contracts that downstream PRs consume), agent-skills:deprecation-and-migration (load-bearing — one-shot cutover semantics; classifier is the deprecation boundary), agent-skills:security-and-hardening (0600 file perms throughout vault writes; manual idempotency to retain error signal; refuse-to-overwrite on content_hash mismatch; rollback is NOT agent-exposed), agent-skills:shipping-and-launch (migrate orchestrator chains 101/102/backfill/catchall/103/lint in order; rollback provides snapshot restore), compound-engineering:review:data-migration-expert, compound-engineering:review:data-integrity-guardian (FK cascade analysis, FK-less companion cleanup, BEGIN IMMEDIATE wrapping), compound-engineering:review:schema-drift-detector (hard-coded v3 memories DDL in schema.ts rather than sqlite_master parsing), compound-engineering:review:cli-readiness-reviewer (JSON stdout, exit 2 on lint failure, long --help across migrate + lint), compound-engineering:review:api-contract-reviewer (new MCP tool lint_memory schema pinned; rollback commands NOT registered per plan §6.6), compound-engineering:agent-native-architecture (lint_memory MCP tool + fulcrum memory lint CLI share one function), compound-engineering:ce-debug (found INSERT OR IGNORE silent-swallow on first 6.3 red run; found cutover fails-on-NULL-retention_tier against unknown rows on first Verify-gate red run; both caught pre-merge), andrej-karpathy-skills:karpathy-guidelines, compound-engineering:git-commit.
- Units shipped:
  - 6.1 Classifier — commit f4154ed (+345, 3 files, 46 cases)
  - 6.2 Migrator (vault writes only) — commit ad2422c (+649, 3 files, 23 cases)
  - 6.3 DB backfill — commit 5b223ba (+407, 3 files, 12 cases)
  - 6.4 runMigration103MemoryV3Cutover — commit c35647b (+335/-1, 3 files, 10 cases)
  - 6.5 fulcrum memory lint + MCP lint_memory — commit c53217a (+538, 8 files, 17 cases)
  - 6.6 rollback --to v2 + migrate orchestrator + Verify gate — commit 741db37 (+640/-1, 5 files, 10 cases)
- PR 6 totals: 6 code commits, +2914 lines, 0 regressions. fulcrum-memory 1001/1001 (was 899 at PR 5 close — +102 new cases across classifier/migrator/backfill/cutover/lint). fulcrum-agent-cli 432/432 (was 416 — +16 new cases: lint shim + MCP + rollback v2 + Verify gate). Full `pnpm -r build` clean.
- Verify gate (plan §PR 6): `memory-migrate-verify.test.ts` seeds 20 pre-v3 rows (9 L0-class across bash_trace/file_patch/tool_trace/session_summary, 10 L1-class across decision/identity/persona/concept/fact, 1 unknown legacy `symbol`), runs `runMemoryMigrate`, and verifies: (a) every L0 row has an l0_sources entry + vault/raw file; memories row gone. (b) Every L1 row has schema_version=3 + vault/curated stub; memory_id preserved. (c) Unknown row untouched + still pre-v3. (d) Lint reports ok=true with 10 migration_stubs + 0 orphans + 0 missing_sources + 0 cycles. Plus dry-run-writes-nothing and idempotency-on-re-run.
- Key discovery — raise before PR 7 if anyone disagrees: unknown-kind rows that 6.3 intentionally leaves alone still carry NULL retention_tier post-backfill, which the 6.4 cutover would reject. Fix lives in the migrate orchestrator as a catch-all UPDATE: `retention_tier='legacy', confidence_decay_at=datetime('now')` for any still-NULL rows. Rows stay at schema_version < 3 so the l1_pages view keeps skipping them; the cutover's NOT NULL flip survives.
- Agent-native parity (plan §Critical Constraints #8): `lint_memory` is the single new MCP tool this PR adds. `memory migrate` and `memory rollback` are operator-only per plan §6.6. Total MCP tool count unchanged except for the +1 lint.
- Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before PR 7 if wrong):
  1. **session_summary → session_transcript alias** — plan lists session_summary as an L0 kind but l0/types.ts canonicalises on session_transcript. Migrator aliases. Operator vault history referencing session_summary as a directory wouldn't match; revisit if that comes up.
  2. **Unknown kinds NOT migrated by default** — only the 9 kinds plan §6.1 enumerates move. Everything else (pre_compact_extract, delegation_summary, v2b graph kinds, legacy v1 content kinds) stays in memories at schema_version < 3 and classifier reports them. No --include-unknown flag yet; add one if an operator needs it.
  3. **L0 rows DELETED from memories** — plan didn't explicitly say delete, but 6.4 NOT NULL cutover requires it. Matches architectural intent: L0 lives in vault/raw + l0_sources only, not memories.
  4. **memory_id preserved end-to-end** — L0 filenames + l0_sources.source_id + L1 stub IDs all reuse the old memory_id. Keeps existing wikilinks + recall events resolvable post-cutover without a rewrite pass.
  5. **Kind → L1PageType**: decision/fact → page, concept → concept, identity/persona → entity. Not semantically perfect but keeps the stub migrations within the validator-required type taxonomy.
  6. **Stub migration waives validator rules 3 + 4** by writing the file directly rather than calling createCuratedPage. Plan §6.2 explicitly allows sources=[] on stubs; the lint pass tracks them as migration_stubs, not orphans. PR 7.3 lint can add richer orphan classification.
  7. **Manual l0_sources idempotency** — INSERT OR IGNORE would silently swallow NOT NULL / FK violations we need as error signal. Read-first + INSERT-on-miss + throw-on-hash-mismatch preserves signal while still being re-run-safe.
  8. **Catch-all `retention_tier='legacy'` backfill** — see Key Discovery above.
  9. **Rollback is snapshot-restore only** — plan §6.6 says "restores from pre-migration snapshot"; no programmatic reverse-migration path. Programmatic rollback would drop the pre-v3 confidence values we never captured. vault/memories/ is preserved so v2a vault data survives any rollback.
  10. **canonical_text column stays** — plan §6.4 defers the drop to PR 9.3. The 103 cutover does not touch canonical_text.
- Known follow-ups deliberately deferred:
  - Plan §7.3 lint additions (broken wikilinks, stale claims, retrospective template validator). LintIssueCode enum is additive; adding new codes is non-breaking.
  - `fulcrum memory migrate --include-unknown` flag (see judgment call 2).
  - Rollback's programmatic reverse-migration mode (see judgment call 9).
- Next: PR 7 — Lifecycle: decay, supersession, consolidation, lint. Load-bearing: agent-skills:performance-optimization (10k-page decay < 10s), agent-skills:api-and-interface-design (lint output schema is a stable dashboard contract), compound-engineering:ce-optimize (empirical tuning of decay λ + retention-tier thresholds against the eval corpus).
- Notes: Same doc-commit discipline observed — every ledger edit in a dedicated `docs(plans):` commit per feedback_never_commit_docs. Pre-existing unrelated WIP (agent-integration/, packages/core/janitor.ts, packages/worktrees/, .pi-lens/, .codex, plan edits from earlier sessions) left untouched across all 6 code commits. Stopping per STOP CONDITIONS — end-of-PR hand-off.

### 2026-04-19 03:10 — PR 6 unit 6.6 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (7 rollback v2 cases — consent, missing-snapshot, snapshot-already-migrated rejection, snapshot copy, vault cleanup, vault/memories preservation, NOT-in-TOOL_REGISTRY; 3 Verify gate cases — 20-row round-trip, dry-run-writes-nothing, idempotency), agent-skills:api-and-interface-design (`RollbackToV2Input` / `RollbackToV2Result` / `MigrateInput` / `MigrateResult` exported public contracts), agent-skills:security-and-hardening (rollback NOT in TOOL_REGISTRY per plan §6.6 — operator-only; close singleton DB before file copy so Windows lock can't block), agent-skills:shipping-and-launch (migrate orchestrator is the PR 6 launch checklist; rollback is the rollback plan — both shipped in one unit to pair operator actions), compound-engineering:ce-debug (red run on Verify gate revealed 101/102 must run at the top of migrate orchestrator, then revealed unknown rows block 103 NOT NULL cutover — both fixed before final green), andrej-karpathy-skills:karpathy-guidelines (one rollback mode shipped now — snapshot restore — rather than speculating a programmatic reverse-migration). compound-engineering:git-commit.
- Summary: `rollbackToV2Snapshot(input)` in `packages/cli/src/commands/memory-rollback.ts` (+106) closes the singleton DB, copy-overwrites fulcrum.db with the snapshot, optionally removes vault/raw + vault/curated; `assertSnapshotIsPreV3` probes the snapshot for an l0_sources table and refuses to proceed if one exists. CLI dispatch in `runMemoryRollback` branches on `--to v2` at the top so the legacy `--since` path is unaffected. `runMemoryMigrate(input)` in `packages/cli/src/commands/memory-migrate.ts` (+115) orchestrates 101 → 102 → classify → migrate → backfill → catch-all NULL→'legacy' → 103 → lint, with dry-run + skip-cutover short-circuits. CLI dispatch in `packages/cli/src/index.ts` adds both `fulcrum memory migrate` + `fulcrum memory rollback --to v2` subcommands with long --help and JSON stdout. Verify-gate integration test `memory-migrate-verify.test.ts` (+190) seeds 20 pre-v3 rows, runs migrate, asserts round-trip for every row.
- Commit: 741db37 (+640/-1 across 5 files: memory-rollback.ts +106, memory-migrate.ts new +115, rollback v2 test new +153 across 7 cases, migrate verify test new +190 across 3 cases, cli/index.ts +77 across 2 subcommands + help)
- Next: PR 6 hand-off → PR 7 (Lifecycle).
- Notes: Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD): (a) Rollback is snapshot-restore only — plan §6.6 phrase "restores from pre-migration snapshot" is explicit. Programmatic reverse would drop pre-v3 confidence values. (b) closeDb() before copyFileSync — Windows file-lock safety; no-op harm on Linux. (c) `assertSnapshotIsPreV3` reads l0_sources existence as the v3 marker — cheap + definitive. (d) `--clean-v3-vault` gated on an operator-provided --vault root so we don't accidentally nuke an unrelated directory. (e) migrate orchestrator's catch-all NULL→'legacy' backfill was discovered via red-run on the Verify gate — unknown rows leave NULL retention_tier which blocks the 103 NOT NULL flip. Rather than gate cutover on "zero unknown rows" (which would require an --include-unknown flag), I set `retention_tier='legacy'` + decay=now for any still-NULL row. Rows stay at schema_version<3 so l1_pages view keeps skipping them. Raise if this is wrong — alternative is to require operator intervention before cutover. (f) migrate exits 2 on lint !ok to match `fulcrum memory reindex-l2` exit semantics. fulcrum-agent-cli 432/432 (+10). Full `pnpm -r build` clean.

### 2026-04-19 02:55 — PR 6 unit 6.5 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (11 memory engine cases + 6 CLI/MCP shim cases — clean vault, stubs-vs-orphans separation, missing-source flagging, partial-match resolution, 2-node cycle, 3-node cycle, linear chain not cycle, pre-v3 row exclusion, report shape, MCP registry presence, shape parity), agent-skills:api-and-interface-design (LintReport / LintIssue / LintIssueCode are additive public contracts — future codes from §7.3 land without breaking consumers; CLI flag surface + MCP schema pinned), compound-engineering:agent-native-architecture (plan §Critical Constraints #8 — `lint_memory` MCP tool + `fulcrum memory lint` CLI share one function so agents can self-verify post-migration), compound-engineering:review:cli-readiness-reviewer (JSON stdout, exit 2 on ok=false, long --help), compound-engineering:review:api-contract-reviewer (new MCP tool schema has optional workspace_id, properly pinned in TOOL_SCHEMA_MAP.get), agent-skills:performance-optimization (supersession cycle detection via DFS tri-color — O(V+E) single traversal with canonical-rotation dedup), andrej-karpathy-skills:karpathy-guidelines (one engine function + one thin CLI shim; no lint framework; richer categories land in 7.3 additively), compound-engineering:git-commit.
- Summary: `packages/memory/src/migration/lint.ts` (+140) exports `lintMemoryVault(db): LintReport`. Walks `memories` WHERE schema_version >= 3, parses provenance.sources + supersedes JSON, and runs three checks: (a) classify empty-sources pages as migration_stubs (counted separately from orphans — plan §6.2 stubs must NOT break the verify gate); (b) each sources[] entry resolves against an l0_sources row; (c) supersession cycle detection via DFS with tri-color visit tracking + canonical-rotation dedup. `ok = (orphans == 0 AND missing_sources == 0 AND supersession_cycles == 0)`. `packages/cli/src/commands/memory-lint.ts` (+15) is a thin shim. CLI dispatch in `packages/cli/src/index.ts` adds `fulcrum memory lint` subcommand (JSON stdout, exit 2 on ok=false, long --help, help-text line updated). MCP schema `lint_memory` added to `mcp-tools.ts` + handler in `tool-registry.ts` — readOnly capabilities, optional workspace_id.
- Commit: c53217a (+538 lines across 8 files: migration/lint.ts new +140, tests/migration-lint.test.ts new +168 across 11 cases, memory/index.ts +7 barrel, cli commands/memory-lint.ts new +15, cli tests/memory-lint.test.ts new +89 across 6 cases, cli mcp-tools.ts +12 tool schema, cli tool-registry.ts +11 handler, cli index.ts +24 subcommand + help)
- Next: PR 6 unit 6.6 — `fulcrum memory rollback --to v2`. Operator-only; NOT agent-exposed. Existing `packages/cli/src/commands/memory-rollback.ts` already implements `--since <ts>`; this adds a new `--to v2` mode that restores from a pre-migration snapshot (or documents the rollback SQL chain). Plan §6.6 explicitly notes "operator-only, not agent-exposed".
- Notes: Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD): (a) `migration_stubs` is a FIRST-CLASS category (not rolled into orphans). Plan §6.2 creates stubs deliberately; lint would false-positive on them. PR 7.3 lint can introduce richer orphan detection (e.g. stubs older than 90d with confidence > threshold) without touching this split. (b) Cycle detection uses DFS + tri-color (WHITE/GRAY/BLACK) — the classical single-pass algorithm. Dedup by canonical rotation so A→B→A and B→A→B count once. Works on sparse supersession graphs which is the realistic case. (c) `schema_version >= 3` gate — matches the l1_pages view contract. Pre-v3 rows don't exist in the new world; linting them would be noise. (d) Lint doesn't check broken vault wikilinks yet — plan §7.3 category; PR 6.5 scope is the three items plan §6.5 enumerates. Code path is extension-ready: adding a new LintIssueCode + counter is non-breaking. (e) CLI exits 2 (not 1) on ok=false to match `fulcrum memory reindex-l2` exit semantics — exit 2 = "this is a verification / lint failure", not an internal error. fulcrum-memory 1001/1001 (+11), fulcrum-agent-cli 422/422 (+6). Full `pnpm -r build` clean. Doc-commit discipline observed.

### 2026-04-19 02:30 — PR 6 unit 6.4 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (10 TDD cases — NOT NULL flip, ledger write, idempotency by ledger + by PRAGMA, row preservation, per-column value preservation, failure-on-NULL-rows with txn rollback, l1_pages view survival, FTS5 trigger survival, index survival), agent-skills:api-and-interface-design (`runMigration103MemoryV3Cutover(db)` is the third canonical migration in the 101/102/103 family — idempotent by design), agent-skills:source-driven-development (read rebuildMemoriesIfLegacy + core memories DDL + 101/102 index DDL carefully before writing the rebuild; verified FTS5 MATCH syntax needs `table_name MATCH ?` binding for query strings that could lex as operators), compound-engineering:review:data-migration-expert (column-by-column INSERT with explicit column list; NOT NULL violation is the intended error signal for a half-run 6.3; rollback SQL documented above the forward migration), compound-engineering:review:schema-drift-detector (memories DDL hard-coded in schema.ts rather than read from sqlite_master because ALTER TABLE ADD COLUMN updates the stored CREATE inconsistently across SQLite versions; the test suite would catch drift from the core DDL), compound-engineering:review:data-integrity-guardian (BEGIN IMMEDIATE wrapping; DROP VIEW + DROP TRIGGER before CREATE memories_new to avoid stale FTS5 trigger firing during INSERT; failure rolls the txn back), agent-skills:shipping-and-launch (explicit rollback SQL comment block makes 6.6 rollback script straightforward), andrej-karpathy-skills:karpathy-guidelines (one function; no migration framework; hard-coded DDL preferred over dynamic SQL-munging), compound-engineering:git-commit.
- Summary: `runMigration103MemoryV3Cutover(db)` landed in `packages/memory/src/schema.ts` (+154). Two idempotency guards (ledger row + PRAGMA table_info notnull check). Captures current memories indexes + `l1_pages` view definition via sqlite_master so partial WHERE clauses + UNIQUE modifiers survive verbatim. Drops the view + FTS5 triggers, CREATE TABLE memories_new with NOT NULL on retention_tier + confidence_decay_at, copies row-by-row via explicit column list, DROPs + RENAMEs, recreates indexes + view + FTS5 triggers, writes the `103_memory_v3_cutover` ledger row. Whole sequence inside a BEGIN IMMEDIATE; NULL retention_tier rows trigger a NOT NULL constraint failure which rolls the txn back — the cutover refuses to corrupt data when 6.3 hasn't fully run.
- Commit: c35647b (+335 / -1 across 3 files: schema.ts +154, tests/schema-v3-cutover.test.ts new +150 across 10 cases, memory/index.ts +1 barrel)
- Next: PR 6 unit 6.5 — `fulcrum memory lint`. Orphan detection (L1 pages without any sources OR sources_via when their `type` requires sources — e.g. page/entity), missing-source references (sources[] entries that don't have an l0_sources row), cyclic supersession graph detection. Registers both as CLI + MCP tool. Lint output schema is the public contract for future dashboards (plan §PR 7.3 notes).
- Notes: Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before 6.5 if any is wrong): (a) idempotency uses BOTH ledger row AND PRAGMA check — ledger tracks "we've run"; PRAGMA check catches the rare case where the rebuild succeeded but the ledger INSERT didn't (would be a partial-commit bug, but the extra guard costs nothing). (b) DDL hard-coded in schema.ts (not reconstructed from sqlite_master) because SQLite versions vary in how ALTER TABLE ADD COLUMN updates sqlite_master.sql. Hard-coded DDL is the canonical pattern per rebuildMemoriesIfLegacy. If the core memories DDL ever changes, this function's hard-coded copy must update — test `preserves every memories row through the rebuild` would fail on column-count drift. (c) FTS5 trigger MATCH query in test needed `memories_fts MATCH ?` binding (not `content MATCH ?`) — quote-escape edge case with `-` in query strings. Real code paths already use the binding style. (d) `canonical_text` column left in place per plan §6.4; PR 9.3 will drop it via a separate 104 migration that also rewires FTS5 triggers. (e) L0 delete is the pre-condition; if PR 6.3 fails on any L0 rows, the resulting NULL retention_tier rows block 103 — this is by design. fulcrum-memory 990/990 (was 980; +10 new tests). Full `pnpm -r build` clean.

### 2026-04-19 02:10 — PR 6 unit 6.3 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (12 TDD cases — L0 insert/delete/alias/companion-cleanup/idempotency; L1 column backfill/view visibility/memory_id preserve/idempotency; unknown untouched; dry-run; error collection), agent-skills:api-and-interface-design (`applyDbBackfill` + `Migration*` types are public; renamed the exports to avoid collision with existing `BackfillResult` from setup/backfill-code-files), compound-engineering:review:data-integrity-guardian (FK cascade analysis — memory_entities/task_memory_links/artifact_memory_links cascade; memory_tags/memory_wikilinks/memory_recall_events need explicit cleanup; FTS5 triggers handle memories_fts), compound-engineering:review:data-migration-expert (manual idempotency on l0_sources INSERT because `INSERT OR IGNORE` silently swallows NOT NULL + FK violations — we want the signal in the error[] channel), agent-skills:security-and-hardening (manifest.content_hash required to avoid NULL-through-migration silent failure), compound-engineering:ce-debug (found the dropped error signal on first red run by running the error-path test — INSERT OR IGNORE was the culprit), andrej-karpathy-skills:karpathy-guidelines (no retry framework; per-row SQLite transaction; one function per tier), compound-engineering:git-commit.
- Summary: `packages/memory/src/migration/backfill.ts` (+140) exports `applyDbBackfill(db, manifest, opts?)`. For L0 rows: read-first + INSERT (throws on content_hash mismatch, no-op on match) + explicit cleanup of FK-less companions (memory_tags/memory_wikilinks/memory_recall_events/vec_memories with try/catch for vec0 module absence) + DELETE FROM memories (FK CASCADE covers memory_entities/task_memory_links/artifact_memory_links; FTS5 trigger drops memories_fts). For L1 stubs: UPDATE memories SET schema_version=3, retention_tier='working', confidence_decay_at=datetime('now'), confidence=0.5 (matches stub), vault_path=manifest.vault_path, provenance=json_object('sources', json('[]')) — guard clause `WHERE ... AND (schema_version IS NULL OR schema_version < 3)` makes re-runs idempotent. Each row runs in its own db.transaction() so one failure doesn't strand the batch; errors bubble up into BackfillError[]. dry_run=true returns would-be counts without writing.
- Commit: 5b223ba (+407 lines across 3 files: migration/backfill.ts new +140, tests/migration-backfill.test.ts new +253 across 12 cases, index.ts +14 barrel)
- Next: PR 6 unit 6.4 — `runMigration103MemoryV3Cutover(db)`. Table-rebuild dance per the `rebuildMemoriesIfLegacy` pattern in core/src/db/schema.ts: flip `retention_tier` + `confidence_decay_at` to NOT NULL, rebuild indexes + triggers. `canonical_text` untouched (plan §6.4 defers drop to PR 9.3). Writes ledger row `103_memory_v3_cutover`.
- Notes: Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before 6.4 if any is wrong): (a) L0 rows DELETED from memories after vault+l0_sources migration. Plan doesn't explicitly say delete, but 6.4 NOT NULL cutover requires it (L0 rows would have NULL retention_tier). Matches architectural intent: L0 lives in vault/raw only, not memories. (b) vec_memories delete wrapped in try/catch — test DBs without sqlite-vec loaded would throw "no such table: vec_memories" if vec0 module isn't registered; the migration path tolerates its absence. (c) `INSERT OR IGNORE` replaced with manual read-first on l0_sources so we retain error signal (NOT NULL violations / FK violations would silently skip under OR IGNORE). On hash mismatch we throw a clear "refusing to overwrite" error instead of corrupting data. (d) L1 backfill touches confidence=0.5 to match the stub vault file — otherwise DB and file would drift. (e) `BackfillResult` already exists in setup/backfill-code-files.ts; renamed exports to `Migration*` prefix to avoid TS2300 duplicate identifier. Kept internal types unchanged to keep migration/backfill.ts self-contained. (f) Per-row transactions (not one big batch) — cleaner semantics, one bad row doesn't roll back good rows; better aligns with BackfillError[] shape. fulcrum-memory 980/980 (was 968; +12 new tests). Full `pnpm -r build` clean. Doc-commit discipline observed.

### 2026-04-19 01:50 — PR 6 unit 6.2 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (23 TDD cases — L0 path/frontmatter/verbatim-body/0600 perms/idempotency/hash-mismatch; L1 per-type dir routing/stub frontmatter/H1 body/idempotency; unknown no-op; dry-run; batch walker w/workspace filter + error collection; MigrationRecord shape), agent-skills:api-and-interface-design (`MigrationRecord` / `MigrationBatch` / `MigrationResult` are public contracts 6.3 consumes for DB backfill + 6.5 for lint), agent-skills:security-and-hardening (0600 file perms via writeRawFile; refuse-to-overwrite on hash mismatch — no silent data loss), agent-skills:deprecation-and-migration (one-shot cutover semantics: re-runs skip, never corrupt; unknown kinds stay out by default), compound-engineering:review:data-integrity-guardian (verbatim body preservation end-to-end — sha256 chain from memories.content → vault file matches), andrej-karpathy-skills:karpathy-guidelines (no speculative retry/backoff; one function per tier; dispatcher + bulk walker cover the 95% case), compound-engineering:git-commit.
- Summary: `packages/memory/src/migration/migrator.ts` (+306) exports `migrateMemoryRow(vaultRoot, db, classifiedRow, opts?)` (single-row dispatcher) and `migrateAllMemories(vaultRoot, db, opts?)` (bulk walker with workspace + limit + dry-run). L0 writes `vault/raw/<source_type>/<yyyy>/<mm>/<dd>/<memory_id>.md` with L0Frontmatter + body verbatim (0600); yyyy/mm/dd come from memories.created_at to preserve the historical date. L1 writes `vault/curated/<type_dir>/<memory_id>.md` as a stub CuratedPage (sources=[], sources_via=[], confidence=0.5, retention_tier='working'); body prepends H1 from title (falls back to kind) over the original memories.content. Kind→L1PageType: decision/fact→page, concept→concept, identity/persona→entity. Idempotent: L0 re-runs sha256-compare the existing body portion (match→skip, mismatch→throw); L1 pre-existing stubs treated as skip so operator hand-edits survive. Unknown rows are no-ops with `result: 'skipped'`. Batch walker collects per-row errors without aborting so a single bad row doesn't stall migration.
- Commit: ad2422c (+649 lines across 3 files: migration/migrator.ts new +306, tests/migration-migrator.test.ts new +244 across 23 cases, index.ts barrel +9 exports)
- Next: PR 6 unit 6.3 — DB backfill. For each L0-class manifest entry: INSERT INTO l0_sources (preserving memory_id as source_id) and DELETE the old memories row. For each L1-class manifest entry: UPDATE memories SET schema_version=3, retention_tier='working', confidence_decay_at=now, vault_path, provenance.sources='[]'. Keep memory_id stable on both sides so wikilinks + recall events don't break (FK CASCADE on memory_entities/memory_tags already handles L0-row removal safely).
- Notes: Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise if any is wrong before 6.3): (a) L0 filename = old memory_id (prefix `mem_`, not `l0src_`). l0_sources.source_id has no CHECK constraint so the prefix-free value is legal; preserving the ID means existing `[[raw/...]]` wikilinks + recall_events → memory_id references resolve post-cutover without a rewrite pass. (b) yyyy/mm/dd taken from memories.created_at (not datetime('now')) — preserves audit chronology so a migrated bash_trace from 2024-06-01 lands under `raw/bash_trace/2024/06/01/` instead of falsely date-stamping it today. (c) L1 stub body prepends H1 from title with a kind fallback — passes parseCuratedPage required-field checks. Does NOT run validateL1Page because the stub has sources=[] + no [[raw/...]] wikilinks (rule 3 + rule 4 fail by design per plan §6.2 "no original L0 exists"). The 6.5 lint pass will surface stubs as "stub pages requiring curation" — a distinct class from orphans. (d) On hash-mismatch migrator THROWS rather than overwriting; the per-row catch in `migrateAllMemories` demotes it to a batch error so one corrupted file doesn't stall the cutover. (e) Unknown rows return `result: 'skipped'` with empty vault_path/content_hash — signals "we looked, decided not to act"; 6.3 skips them too. fulcrum-memory 968/968 (was 945; +23 new tests). Full `pnpm -r build` clean. Doc-commit discipline observed — ledger entry lands in its own `docs(plans):` commit. Pre-existing unrelated WIP untouched.

### 2026-04-19 01:30 — PR 6 unit 6.1 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (46 TDD cases first, implementation second), agent-skills:api-and-interface-design (`MigrationClass` / `MigrationClassification` / `ClassifiedRow` / `MigrationClassifierReport` are exported public contracts that 6.2/6.3/6.5 build against), agent-skills:deprecation-and-migration (classifier IS the deprecation boundary — what the plan explicitly lists migrates, everything else stays out until opted in), episodic-memory:remembering-conversations (session-start search — confirmed classifier surface not previously built), andrej-karpathy-skills:karpathy-guidelines (narrowest reading of plan §6.1; no speculative CLI flags; single file, three exports), compound-engineering:git-commit.
- Summary: `packages/memory/src/migration/classifier.ts` (+133) exports `classifyMemoryKind(kind)`, `classifyMemoriesForMigration(db, opts?)`, and `buildClassifierReport(rows)`. Explicit mapping per plan §PR 6.1: L0_raw = {bash_trace, file_patch, tool_trace, session_summary}, L1_curated_stub = {decision, identity, persona, concept, fact}, everything else = `unknown`. `session_summary` aliases to the canonical L0SourceType `session_transcript` since l0/types.ts doesn't include `session_summary`. DB walk filters schema_version < 3 so already-migrated rows skip classification; supports workspace_id + limit filters. Report surfaces per-class totals, per-kind breakdown, and a sorted list of unmigrated unknown kinds so the operator can decide whether to opt them in before 6.2 runs.
- Commit: f4154ed (+345 lines across 3 files: migration/classifier.ts new +133, tests/migration-classifier.test.ts new +198 across 46 cases, index.ts barrel +14)
- Next: PR 6 unit 6.2 — Migrator. For L0-class: copy body verbatim to `vault/raw/{source_type}/yyyy/mm/dd/{ULID}.md` preserving old memory_id as the ULID (so downstream wikilinks resolve without rewriting). For L1-class: create stub curated page with sources=[] + confidence=0.5 via l1/page.ts createCuratedPage.
- Notes: Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD): (a) `session_summary → session_transcript` alias — plan uses the old kind name, l0/types.ts uses the new source_type. Chose the canonical new name so the migrated rows fit the L0SourceType taxonomy without a third variant. Revisit if operator vault history references `session_summary` as a directory name. (b) Unknown kinds do NOT migrate by default — only the 9 kinds the plan enumerates move. Everything else (pre_compact_extract, delegation_summary, v2b graph kinds, legacy v1 content kinds) stays in `memories` at schema_version < 3 and the dry-run reports them. Flagging a --include-unknown flag is deferred to PR 6.2 once real migration needs it. (c) Classification filter in `classifyMemoriesForMigration` uses `schema_version IS NULL OR schema_version < 3` — matches the inverse of the `l1_pages` view's `schema_version >= 3`. (d) `memory_id` is preserved in the ClassifiedRow so 6.2 can reuse it as the new ULID in `vault/raw/...` + `l0_sources.source_id`, which keeps existing wikilinks + recall events intact. fulcrum-memory 945/945 (was 899 at PR 5 close; +46 new tests). Full `pnpm -r build` clean. Doc-commit discipline observed — this ledger entry lands in a separate `docs(plans):` commit per feedback_never_commit_docs. Pre-existing unrelated WIP (agent-integration/, packages/core/janitor.ts, packages/worktrees/, .pi-lens/, .codex, plan edits from other sessions) left untouched.

### 2026-04-18 15:00 — PR 0 unit 0.1 — completed
- Skills invoked: agent-skills:spec-driven-development, agent-skills:planning-and-task-breakdown, elements-of-style:writing-clearly-and-concisely, WebSearch (for curator model research)
- Summary: Plan document `2026-04-18-002-memory-tiered-architecture-plan.md` committed to `docs/plans/`.
- Commit: 0a01aff (initial), bf29f10 (model pinning), 6896578 (templates + traceability), 6e859ff (skill matrix + test corpus)
- Next: PR 0 unit 0.2 — migration SQL `2026-04-19-001-memory-v3-lifecycle.sql`
- Notes: Plan is 930 lines. Approval checklist fully green. Awaiting user go-ahead to start PR 0 unit 0.2.

### 2026-04-18 18:40 — PR 0 unit 0.2 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:test-driven-development, agent-skills:incremental-implementation, agent-skills:source-driven-development (repo-archaeology on migration conventions), agent-skills:code-review-and-quality (self-review), andrej-karpathy-skills:karpathy-guidelines, episodic-memory:remembering-conversations, agent-skills:documentation-and-adrs (Knowledge-graph ADR block), compound-engineering:git-commit
- Summary: `runMigration101MemoryV3Lifecycle` landed in `packages/memory/src/schema.ts` with 10 passing vitest cases and re-export from the package barrel. Plan updated with the Knowledge-graph ADR, full l0_sources DDL, and l1_pages view DDL (working-tree only per feedback_never_commit_docs).
- Commit: 9384555 (code only: schema.ts + index.ts + schema-v3.test.ts; +338 lines)
- Next: PR 0 unit 0.3 — `runMigration102MemoryV3SourceIndex` (indexes on the new columns; same file)
- Notes: Four Q2/Q3/Q4 ambiguities in the plan were resolved before work started (extend graph_entities/edges in place, treat l1_pages as a VIEW over memories, full DDL shapes) and the plan was rewritten to reflect those decisions. Plan/ledger edits intentionally uncommitted (CLAUDE.md memory `feedback_never_commit_docs`). Working tree also carries pre-existing unrelated changes from other tasks (agent-integration/, packages/core/janitor.ts, worktree-ops, etc.) that were left untouched.

### 2026-04-18 18:51 — PR 0 unit 0.3 — completed
- Skills invoked: agent-skills:test-driven-development, agent-skills:incremental-implementation, agent-skills:code-review-and-quality, andrej-karpathy-skills:karpathy-guidelines, compound-engineering:git-commit
- Summary: `runMigration102MemoryV3SourceIndex` added to `packages/memory/src/schema.ts` — 11 indexes across `l0_sources` (5), `memories` (3 partial), `graph_entities` (2, one partial), `graph_edges` (1). All `CREATE INDEX IF NOT EXISTS`; ledger row `102_memory_v3_source_index`; 5 new vitest cases (`sqlite_master.sql` assertions verify partial WHERE clauses).
- Commit: 82a8361 (code only: schema.ts + index.ts + schema-v3.test.ts; +136 -2)
- Next: PR 0 unit 0.4 — `packages/memory/src/l0/types.ts` (TypeScript types only, no runtime code)
- Notes: fulcrum-memory suite now 645 tests (was 640). Full `pnpm -r build` clean. Same doc-commit discipline observed (plan/ledger edits remain working-tree only).

### 2026-04-18 18:57 — PR 0 unit 0.4 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:api-and-interface-design (L0 ingest contract shape), andrej-karpathy-skills:karpathy-guidelines (no runtime logic added), compound-engineering:git-commit
- Summary: New `packages/memory/src/l0/types.ts` declares `L0_SOURCE_TYPES` const tuple, `L0SourceType` union, `L0Frontmatter`, `L0File`, `L0IngestInput`, `L0IngestMeta`, `L0SourceRow`. Types match the plan's §L0 frontmatter spec and the `l0_sources` columns 1:1.
- Commit: 5a38ff8 (+112 insertions, net-new file)
- Next: PR 0 unit 0.5 — update `AGENTS.md` + `agent-integration/claude/CLAUDE.md` with a "Memory tiers (v3 draft)" section
- Notes: Not yet barrel-exported (PR 1 picks the public surface). Build + 645 tests still green.

### 2026-04-18 19:03 — PR 0 unit 0.5 — completed
- Skills invoked: agent-skills:documentation-and-adrs, elements-of-style:writing-clearly-and-concisely, compound-engineering:git-commit
- Summary: "Memory Tiers (v3 draft)" section added to `AGENTS.md` (after Memory System Patterns) and `agent-integration/claude/CLAUDE.md` (after Role Boundaries). Both sections point to the full plan, explain the L0/L1/L2 split, feature-flag status, graph-column mapping, and migration mechanics. CLAUDE.md variant is slightly terser and agent-action-focused ("keep using v2a surface until PR 1 flips L0 writes").
- Commit: 368f9eb (+63 insertions across 2 files)
- Next: PR 0 DONE — hand off to user for review before starting PR 1. PR 1 unit 1.1 is the first runtime wiring: `l0/ingest.ts` → `ingestRawSource` calls `runMigration101/102` at startup + writes to `vault/raw/` + inserts into `l0_sources`.
- Notes: Had to stash pre-existing unrelated CLAUDE.md changes (24-tool-count regen + sweep_stale_runs entry) before editing so my commit stayed surgical; popped cleanly after commit, no conflicts. PR 0 totals: 4 commits (9384555, 82a8361, 5a38ff8, 368f9eb), ~513 lines added across code + docs, 0 regressions, fulcrum-memory suite 645/645. Stopping per STOP CONDITIONS — end of PR hand-off.

### 2026-04-18 21:30 — PR 1 — COMPLETE (all 5 units shipped)
- Skills invoked across all units: agent-skills:context-engineering, agent-skills:test-driven-development, agent-skills:incremental-implementation, agent-skills:api-and-interface-design, agent-skills:security-and-hardening, agent-skills:source-driven-development (repo archaeology — FulcrumEventBus, newId, ContentChangeKind, WAL API, hook structure), andrej-karpathy-skills:karpathy-guidelines, compound-engineering:git-commit.
- Units shipped:
  - 1.1 `ingestRawSource` — commit 2c04179 (+434, 5 files)
  - 1.2 `writeRawFile` + `writeCuratedFile` — commit 20e9af7 (+132/-10, 4 files)
  - 1.3 vault watcher `l0_raw` + `l1_curated` events — commit 88328f6 (+172/-7, 3 files)
  - 1.4 hook `FULCRUM_MEMORY_V3` branch (file_patch + bash_trace verbatim) — commit 6743f69 (+77/-27, 2 files)
  - 1.5 regression tests (10 KB verbatim, WAL round-trip, v2a parity) — commit 6db30ab (+118, 1 file)
- PR 1 totals: 5 commits, +933/-44 lines, 0 regressions. fulcrum-memory 672/672 (was 645 at PR 1 start — 27 new tests), fulcrum-agent-cli 379/379, fulcrum-agent-core 574/574, full `pnpm -r build` clean.
- Self-init pattern: `ensureV3SchemaApplied()` in `ingestRawSource` runs `runMigration101MemoryV3Lifecycle(db)` once per DB handle (WeakSet tracking) so hook callers don't need to pre-apply the migration. Idempotent via existing PRAGMA table_info guards.
- Review-finding compliance: reused existing `FulcrumEventBus` (review C1) — `EventType` gains `'l0_ingested'`; `ContentChangeKind` gains `'l0_raw'|'l1_curated'`. No new per-subsystem bus.
- Next: PR 2 — L1 templates + page primitives + validator. Before PR 2: decide Open Question #2 (validator library — ajv vs zod vs typebox) per MASTER-PLAN guidance.
- Notes: Flag `FULCRUM_MEMORY_V3` stays default-off. v2a `writeMemory` remains the primary path until PR 5 cutover. Same doc-commit discipline observed (plan/ledger edits working-tree only per feedback_never_commit_docs).

### 2026-04-18 21:16 — PR 1 unit 1.1 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development, agent-skills:api-and-interface-design (public `ingestRawSource` contract + new `EventType`), agent-skills:security-and-hardening (0600 file perms, verbatim-body-on-disk, sanitize-before-WAL invariant, fail-closed on sanitizer error), agent-skills:source-driven-development (verified repo conventions: existing FulcrumEventBus, newId() prefix registry, sanitizeOnWrite fail-closed semantics, appendWal API shape), andrej-karpathy-skills:karpathy-guidelines (no speculative error paths; no auto-resolution of workspace_id — explicit requirement moved to PR 1 unit 1.4), episodic-memory:remembering-conversations (session-start search — no prior PR 1 context found), compound-engineering:git-commit.
- Summary: `ingestRawSource` landed in `packages/memory/src/l0/ingest.ts`. 9-step pipeline (validate source_type → require workspace_id → compute hash/size → sanitize → build frontmatter + path → mkdir 0700 + writeFileSync 0600 → insert `l0_sources` row → `appendWal` with sha256 only → `emitEvent('l0_ingested')`). Extends `EventType` in `packages/core/src/types.ts` with `'l0_ingested'`; adds `l0_source: 'l0src_'` to `PREFIXES` in `packages/core/src/ids.ts`. 13 new vitest cases in `src/tests/l0-ingest.test.ts` cover path shape, verbatim body, 0600 perms, DB row, hash/size, event emission (reuses existing `FulcrumEventBus` per review C1), actor resolution, validation, ULID uniqueness, binary-byte preservation.
- Commit: 2c04179 (+434 lines across 5 files: types.ts +2, ids.ts +2, l0/ingest.ts new +200, memory index.ts +12 barrel re-exports, test +210).
- Next: PR 1 unit 1.2 — split `writeMemoryFile` in `packages/memory/src/vault/client.ts` into `writeRawFile` + `writeCuratedFile`. Back-compat wrapper routes to curated until PR 2 cutover.
- Notes: ulid-guard failure on first test run correctly caught bare `ulid()` usage — switched to `newId('l0_source')` (repo convention enforced by `packages/core/src/tests/ulid-guard.test.ts`). No other regressions. fulcrum-memory suite 658/658 (was 645, +13 new tests). fulcrum-agent-core suite 574/574 + 4 skipped. Full `pnpm -r build` clean. Same doc-commit discipline observed (plan/ledger edits in working tree only).

### 2026-04-18 22:14 — PR 2 — COMPLETE (all 8 units shipped)
- Skills invoked across units: agent-skills:context-engineering, agent-skills:test-driven-development, agent-skills:incremental-implementation, agent-skills:api-and-interface-design (validator error codes are a public surface; L1 page primitives are the single supported writer), agent-skills:code-review-and-quality (self-review after every unit — caught plan drift on page template supersession fields before commit; caught dist/ URL resolution failure before ship), agent-skills:security-and-hardening (constraint #15 curator-batch allowlist; path traversal + null-byte rejection in resolveWikilink), andrej-karpathy-skills:karpathy-guidelines (no speculative abstractions; one primitive per unit; no orthogonal refactors), elements-of-style:writing-clearly-and-concisely (template prose), compound-engineering:git-commit.
- Units shipped:
  - 2.1 Four `.md` templates + smoke gate — commit 09e9585 (+225, 5 files)
  - 2.4 `l1/wikilinks.ts` extract/render/resolve — commit c5af301 (+218, 2 files)
  - 2.6 `l1/frontmatter.ts` serializer — commit 32f16a4 (+286, 2 files)
  - 2.5 `l1/entities.ts` upsert/addEdge/getEntityGraph + `ent_`/`edg_` ID prefixes — commit b348f56 (+427, 3 files)
  - 2.3 `l1/validator.ts` — all 7 rules + constraint #15 — commit fdc8040 (+431, 2 files)
  - 2.2 `l1/page.ts` create/read/update/supersede — commit c014b44 (+460, 2 files)
  - 2.7 Template loader + `fulcrum memory page create` CLI + memory index re-exports — commit d0d8bfa (+284, 4 files)
  - 2.8 End-to-end integration test — commit 1840c61 (+225, 1 file)
  - Bonus: `fulcrum memory page show <id>` for Verify-gate parity — commit e20e47b (+19, 1 file)
- PR 2 totals: 9 code commits, +2575 lines, 0 regressions. fulcrum-memory 778/778 (was 672 at PR 1 close — +106 new tests). Full `pnpm -r build` clean.
- Verify gate (`fulcrum memory page create --template entity` → show): passes end-to-end via the CLI plumbing landed at 2.7+.
- Implementation-detail judgment calls made (per OPEN-QUESTIONS GUARD — raise if any is wrong before PR 3):
  1. Template runtime load: chose inline TS constants + .md parity test over `new URL(import.meta.url)`. Rationale: tsup bundles the memory package flat into `dist/index.js`; relative URLs collapse to `dist/` root and the multi-subdirectory layout breaks. Inline constants preserve human-editable .md as source-of-truth via parity tests; editing .md without updating the constants fails CI. Reversible: swap to a bundler-aware loader if we ever publish templates independently.
  2. Validator library: deferred (plan line 61 suggested a library decision at PR 2 time). Hand-rolled the 7 rules instead since most are cross-cutting (wikilink-to-source matching, entity existence, supersession chain) and don't express cleanly as JSON-Schema or Zod. Worth revisiting at PR 3 when curator output validation arrives and Structured Outputs already constrain shape.
  3. L1 memories.scope column: set to `'project'` for every L1 page. Didn't branch on type. Synthesis pages may eventually want `'workspace'` scope; revisit if retrieval needs it.
  4. Page dir sharding: entity→entities/, concept→concepts/, page→pages/, synthesis→synthesis/ (kept singular since there's no natural plural). Codified in `TYPE_DIR` at `l1/page.ts`.
  5. Supersession DB layout: `superseded_by` scalar, `supersedes` JSON array — matches the v3 migration columns from unit 0.2.
- Known follow-ups deliberately deferred to later PRs:
  - L2 embedding on L1 page writes (handled by PR 4).
  - MCP parity for `fulcrum memory page *` commands (handled by PR 5 unit 5.4 when the full inspection/correction surface ships).
  - Entity deduplication across workspaces (plan Open Question #3 — unresolved).
  - Prompt-version pinning on pages (plan Open Question #2 — unresolved; relevant once curator lands in PR 3).
- Next: PR 3 (curator pipeline, BOOTSTRAP MODE) begins with codex:gpt-5-4-prompting as the load-bearing skill. Before starting: read the GPT-5.4 prompting skill + codex CLI runtime + codex result-handling skills end-to-end; they compose the curator prompt and parse its JSONL output.
- Notes: Same doc-commit discipline observed (plan + ledger edits working-tree only per feedback_never_commit_docs; ledger entry lands in a separate `docs(plans):` commit). Pre-existing unrelated WIP (agent-integration/, packages/core/janitor.ts, packages/worktrees/, .pi-lens/, .codex, plan edits from earlier sessions) left untouched across all 9 commits. Prompt file updated at ca14224 — "one invocation = one full PR" per user steer mid-session.

### 2026-04-19 00:22 — PR 5 — COMPLETE (all 6 units shipped)
- Skills invoked across units: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (39 new tests — 8 v3-search + 5 rrf-env + 5 recall + 11 inspection + 13 flag + 2 delegation + 5 corpus), agent-skills:api-and-interface-design (V3SearchInput/V3RecallHit/V3SearchWeights + RecallKnowledgeInput/RecallKnowledgeHit/RecallKnowledgeResult + SourceHit/InspectResult/ReadRawResult/TraceHit/MarkWrongInput + isMemoryV3Enabled — all new public exports), agent-skills:performance-optimization (graph BFS bounded by reached set + json_each intersection — not per-memory scan; RRF weights env-overridable for ce-optimize sweep), agent-skills:security-and-hardening (supersession audit invariant preserved end-to-end; mark_wrong routes through ingestRawSource for 0600 perm + hash + validator), agent-skills:shipping-and-launch (flag flip checklist + test deltas + rollback path), compound-engineering:review:cli-readiness-reviewer (JSON stdout across all 6 new CLI commands; long-form --help), compound-engineering:review:api-contract-reviewer (new MCP tool schemas pinned with required fields; recall_memory still exported for back-compat), compound-engineering:review:deployment-verification-agent (Go/No-Go checklist in the 5.5 commit message), compound-engineering:agent-native-architecture (every user action also callable from agents; 6 new MCP tools land), andrej-karpathy-skills:karpathy-guidelines, compound-engineering:git-commit.
- Units shipped:
  - 5.1 `runV3Search` + graph-traversal + confidence + supersession filters — commit 53962da (+605, 3 files)
  - 5.2 RRF weights env-configurable (FULCRUM_RRF_WS_FTS/_VEC/_GRAPH) — commit bea86bb (+91/-3, 2 files)
  - 5.3 `recall_knowledge` action + `fulcrum memory recall` CLI — commit 87942a8 (+334, 5 files)
  - 5.4 Inspection + correction surface (5 CLI + 5 MCP: sources, inspect, read-raw, trace, mark-wrong) — commit e147862 (+669, 5 files)
  - 5.5 Flag flip: `FULCRUM_MEMORY_V3` default on; `recall_memory` delegates to `recall_knowledge` when enabled — commit c634fdb (+229/-5, 7 files)
  - 5.6 Verify gate — 20 L0 dumps → 10 L1 pages → graph-aware recall integration test — commit 62245e9 (+222, 1 file)
- PR 5 totals: 6 code commits, +2372/-8 lines, 0 regressions. fulcrum-memory 899/899 (was 868 at PR 4 close — +31 new tests). fulcrum-cli 416/416 (was 398 — +18 new tests). Full `pnpm -r build` clean.
- Verify gate: `fulcrum memory recall "<query>"` returns L1 pages ordered by fused score with L0 back-refs; `--explain` surfaces per-stage ranks. Proven end-to-end in unit 5.6 with a 20-L0 / 10-L1 / 3-entity-edge corpus — graph traversal surfaces pages whose bodies don't contain the query word, confidence floor evicts demoted pages, supersession hides superseded rows, every hit carries sources[] + l0_wikilinks[].
- Implementation-detail judgment calls made (per OPEN-QUESTIONS GUARD — raise before PR 6 if any is wrong):
  1. Flag semantics: fail-OPEN on typo (anything not in {0,false,off,no} stays on v3). An operator misspelling must not silently downgrade recall. Revisit if it hides real config issues in prod.
  2. `recall_memory` delegates via TOOL_REGISTRY lookup of `recall_knowledge` — keeps the dispatch consistent with MCP routing. Direct-function delegation would skip the MCP middleware contract.
  3. `mark_wrong` does NOT auto-trigger curator re-run — plan §Mark-wrong workflow describes the trigger, but auto-run crosses PR 8's `FULCRUM_MEMORY_CURATE_AUTO` feature flag boundary. Explicit separate step for now; correction L0 is the input the curator will consume on re-run.
  4. Inspection primitives co-located in `memory-inspection.ts` vs one file per command (plan's File Structure shows separate files). DRY wins over plan-diagram shape; karpathy-guidelines permits.
  5. `trace` uses case-insensitive `LOWER(content) LIKE` instead of FTS5 — simpler, avoids FTS5 quote-escape edge cases on arbitrary user text. Revisit if query latency matters on >10k pages.
  6. Graph stage uses SQLite `json_each(m.entities)` — simpler than a normalized memory_entities join and matches existing curator-apply write pattern.
- Known follow-ups deliberately deferred:
  - compound-engineering:ce-optimize RRF weight sweep — the env knobs + corpus exist; actual sweep lives in a future tuning task.
  - Per-stage `--explain` for `fulcrum memory recall` already carves out stage_ranks; richer explain (pre-fusion ranks + scores per stage) is a future cosmetic.
  - Hook file_patch writes still go through `ingestRawSource` when v3 is enabled. Any hook tests that assumed the v2a writeMemory path need `FULCRUM_MEMORY_V3=0` in their beforeEach; one such suite (hook-pre-post.test.ts describe block) was updated in this PR. Audit other hook suites before PR 6.
- Next: PR 6 — data migration of existing `vault/memories/` → `vault/raw` + `vault/curated`. BOOTSTRAP MODE. Load-bearing skills: agent-skills:deprecation-and-migration, compound-engineering:review:data-migration-expert, compound-engineering:review:data-integrity-guardian, compound-engineering:ce-debug (first-run surprises guaranteed), agent-skills:security-auditor (subagent — rollback audit chain). Before starting: read `rebuildMemoriesIfLegacy()` pattern; PR 6.4 uses the same table-rebuild dance for the NOT NULL cutover.
- Notes: Same doc-commit discipline observed — every ledger edit in its own `docs(plans):` commit per feedback_never_commit_docs. Pre-existing unrelated WIP (agent-integration/, packages/core/janitor.ts, packages/worktrees/, .pi-lens/, .codex, plan edits from earlier sessions) left untouched across all 6 code commits. Stopping per STOP CONDITIONS — end-of-PR hand-off.

### 2026-04-19 00:18 — PR 5 unit 5.5 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (13 new flag tests + 2 delegation tests — both on/off paths pinned), agent-skills:api-and-interface-design (new public export `isMemoryV3Enabled()` is a stable contract for every future call site), agent-skills:shipping-and-launch (pre-flip checklist — test deltas + rollback plan in commit message), compound-engineering:review:deployment-verification-agent (Go/No-Go: test counts before/after, full build clean, rollback env var documented), agent-skills:code-review-and-quality (caught the hook-pre-post v2a regression immediately after flag flip; fix: explicit `FULCRUM_MEMORY_V3=0` in the v2a-path test suite's beforeEach), andrej-karpathy-skills:karpathy-guidelines (single flag helper + one-line delegate; no feature-flag framework), compound-engineering:git-commit.
- Summary: `packages/memory/src/flags.ts` (+18) exports `isMemoryV3Enabled()` — single source of truth. Unset / empty / anything not in `{0, false, off, no}` returns true. Operator typo "maybe" stays ON (fail open) — a misspelling must never silently downgrade recall. hooks.ts replaces the old inline `=== '1'` check with the shared helper. `tool-registry.ts` `recall_memory` handler detects the flag and delegates to `recall_knowledge` handler when enabled; legacy `runStagedSearch` path stays reachable via `FULCRUM_MEMORY_V3=0`. Pinned `v2a` hook tests with `FULCRUM_MEMORY_V3=0` beforeEach so they still prove the v2a writeMemory write path.
- Commit: c634fdb (7 files: flags.ts +18, memory index +3, flag test +54, hooks.ts +9/-5, tool-registry +7, delegation test +132, hook test pin +11/-0; +229/-5)
- Next: PR 5 unit 5.6 — integration test corpus: 20 L0 dumps → 10 L1 pages → recall query that requires graph traversal. This is the PR 5 Verify gate and doubles as the seed for compound-engineering:ce-optimize's RRF-weight sweep in future tuning work.
- Notes: fulcrum-memory 894/894 (was 881 — +13 new flag tests). fulcrum-cli 416/416 (was 414 — +2 delegation tests). Full `pnpm -r build` clean. Implementation-detail judgment calls: (a) flag semantics are fail-open — operators' typos keep v3 enabled. Plan §Migration strategy says "default on from Phase 5"; loud failure on typo would contradict "default on". (b) `recall_memory` handler delegates to `recall_knowledge` handler (not to `recallKnowledge` directly) — the registry lookup keeps the dispatch consistent with MCP routing. (c) Default-on flip deliberately does NOT change ingest.test / l0-ingest-regression tests — those suites either set the flag explicitly or are v3-native. (d) Hook v2a-path tests were the only suite that implicitly depended on the default being off; fix was surgical (one describe block, 2 env var pins).

### 2026-04-19 00:13 — PR 5 unit 5.4 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (11 TDD cases — 2-3 per command + throws-on-unknown path), agent-skills:api-and-interface-design (SourceHit / InspectResult / ReadRawResult / TraceHit / TraceResult / MarkWrongInput / MarkWrongResult are exported public contracts — every MCP shim + CLI dispatcher re-uses them), compound-engineering:agent-native-architecture (plan §Critical Constraints #8 — every CLI action callable via MCP; 5 tools land together), compound-engineering:review:cli-readiness-reviewer (JSON stdout, long --help, argv flag parsing), compound-engineering:review:api-contract-reviewer (5 new MCP tools + 5 new registry entries; schema pinning via TOOL_SCHEMA_MAP.get; required fields documented), agent-skills:security-and-hardening (mark_wrong appends a correction via ingestRawSource, which validates + hashes + 0600-mode-writes before committing — no raw fs writes from the command), andrej-karpathy-skills:karpathy-guidelines (single file hosts all 5 primitives; no abstraction layer; curator re-run NOT auto-triggered — explicit operator / scheduled action per plan), compound-engineering:git-commit.
- Summary: `packages/cli/src/commands/memory-inspection.ts` (+291) exports `sources`, `inspect`, `readRaw`, `trace`, `markWrong`. MCP tool schemas added to `mcp-tools.ts` (+66) + handlers in `tool-registry.ts` (+59) + CLI dispatch in `index.ts` (+80). Every CLI command has a matching MCP tool; both call the same function. `sources` merges frontmatter `sources[]` with inline `[[raw/...]]` wikilink ULIDs, de-dupes, flags missing rows as `source_type: 'missing'` (no silent drops). `inspect` returns full frontmatter + body + serialized form + per-wikilink `{ exists, absolute_path }`. `trace` runs a case-insensitive `LOWER(content) LIKE` ordered by confidence DESC with per-hit match-count. `mark_wrong` writes a `correction` L0 via `ingestRawSource` — curator re-run stays an explicit separate action per plan (auto-trigger lands in PR 8).
- Commit: e147862 (5 files: command +291, test +173, cli index +80, mcp-tools +66, tool-registry +59; +669)
- Next: PR 5 unit 5.5 — flip `FULCRUM_MEMORY_V3` default to on. `recall_memory` handler delegates to `recallKnowledge` when the flag is on (default). `FULCRUM_MEMORY_V3=0` reverts to the v2a runStagedSearch path for one release cycle. Must include a deploy-verification checklist (compound-engineering:review:deployment-verification-agent skill).
- Notes: fulcrum-cli 414/414 (was 403 — +11). fulcrum-memory 881/881. Full `pnpm -r build` clean. Implementation-detail judgment calls: (a) single file for all 5 primitives (vs one file per command) — shared helpers (`snippet`, `loadL0Row`, `stripL0Frontmatter`) DRY nicely; plan's file-structure diagram shows separate files but prioritizing composability over directory shape matches karpathy-guidelines. (b) `mark_wrong` does NOT auto-trigger curator — plan §Mark-wrong workflow describes the trigger, but auto-run crosses PR 8's FULCRUM_MEMORY_CURATE_AUTO feature-flag boundary. Explicit curator re-run is the operator's next step; correction L0 entry is the input. (c) `trace` case-insensitive LIKE only (no FTS5) — simpler than fighting FTS5's quote-escape rules for user text and avoids a bad UX on typo queries. Performance fine for <10k pages; revisit if it ever matters. (d) `sources` snippet is first 240 chars (no query-centered window) — keeps the function pure. Query-centered snippets live in `trace`.

### 2026-04-19 00:06 — PR 5 unit 5.3 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (5 cases — hit shape + limit/offset + empty + MCP schema + recall_memory back-compat), agent-skills:api-and-interface-design (new MCP tool `recall_knowledge` schema; `recall_memory` stays visible — back-compat alias), compound-engineering:review:cli-readiness-reviewer (JSON stdout, long --help, stage_ranks hidden unless --explain), compound-engineering:review:api-contract-reviewer (exported types `RecallKnowledgeInput` / `RecallKnowledgeHit` / `RecallKnowledgeResult` are new public surface), andrej-karpathy-skills:karpathy-guidelines (thin wrapper over runV3Search — no additional scoring / filtering), compound-engineering:git-commit.
- Summary: `packages/cli/src/commands/memory-recall.ts` (+80) exports `recallKnowledge(input)` that thin-wraps runV3Search, truncates content per max_chars, and packages {results, reason?}. MCP schema entry in `packages/cli/src/mcp-tools.ts` (+21) adds `recall_knowledge` with confidence_floor / graph_hops / include_superseded / max_chars fields. `packages/cli/src/tool-registry.ts` (+25) registers the handler (warms embedder via initEmbedding, threads args from MCP). CLI dispatch in `packages/cli/src/index.ts` (+55) adds the `memory recall "<query>" [--limit --offset --max-chars --confidence-floor --graph-hops --include-superseded --explain]` subcommand; stage_ranks are hidden unless `--explain`.
- Commit: 87942a8 (5 files: command +80, test +153, cli index +55, mcp-tools +21, tool-registry +25; +334)
- Next: PR 5 unit 5.4 — agent inspection + correction surface: `fulcrum memory sources`, `inspect`, `read-raw`, `trace`, `mark-wrong` CLIs + `get_memory_sources`, `inspect_memory`, `read_raw_source`, `trace_claim`, `mark_memory_wrong` MCP tools. This is the big agent-native parity unit; 5 commands + 5 MCP tools + `mark-wrong` writes a correction L0 entry and triggers curator re-run.
- Notes: fulcrum-cli 403/403 (was 398 — +5). fulcrum-memory 881/881. Full `pnpm -r build` clean. Implementation-detail judgment calls: (a) `recall_memory` unchanged — PR 5.5 flips the flag and delegates on cutover; cleaner to keep unit diffs surgical. (b) `recall_knowledge` doesn't call `warmEmbedding()` inside its CLI branch because the dispatch already does so earlier — audit trail cleaner. Actually wait — looking back at the code, I do call `await warmEmbedding()` inside the `if (command === 'recall')` branch. That's fine and explicit. (c) `--explain` carves out stage_ranks to minimize default-stdout noise; agents that need it pass the flag. (d) `max_chars` default 500 matches the existing `recall_memory` surface so agents switching tools don't see a size regression.

### 2026-04-19 00:00 — PR 5 unit 5.2 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (5 TDD cases — defaults + override + typo fallback + zero-valid + re-read-per-call), agent-skills:api-and-interface-design (env var names `FULCRUM_RRF_WS_FTS/_VEC/_GRAPH` are a public operator contract), andrej-karpathy-skills:karpathy-guidelines (one function gained env lookup; no new config system; no caching layer), compound-engineering:git-commit.
- Summary: `v3DefaultWeights()` in `packages/memory/src/retrieval/v3-search.ts` now reads each weight from env per call. `readWeightEnv(name, fallback)` accepts any non-negative finite number; unparseable / negative / NaN values fall back. Zero is a valid weight (silences a stage). `runV3Search` now calls `v3DefaultWeights()` instead of using a frozen constant.
- Commit: bea86bb (2 files: v3-search.ts +20/-3 (or so), test +74; +91/-3)
- Next: PR 5 unit 5.3 — `recall_knowledge` new action + `recall_memory` alias. Tool-registry entry + MCP tool + thin wrapper over runV3Search with the existing RecallMemoryInput shape mapped onto V3SearchInput.
- Notes: fulcrum-memory 881/881 (was 876 — +5 new tests). Full `pnpm -r build` clean. Per-call env re-read is slightly more expensive than a cached value but matches the v2a pattern (`waitForEmbedHeadroom` also re-reads on every call), so no surprise for operators. Hot-path cost: 3 `process.env[...]` lookups + 3 `Number()` calls per search — negligible vs the FTS/vec/graph DB work.

### 2026-04-18 23:57 — PR 5 unit 5.1 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (8 TDD cases across confidence filter / supersession filter / schema_version gate / graph traversal / L0 back-refs), agent-skills:api-and-interface-design (`V3SearchInput` / `V3RecallHit` / `V3SearchWeights` are public contracts downstream units depend on), agent-skills:performance-optimization (graph BFS bounded to reached set + entity intersection via SQLite json_each; no per-memory scan), agent-skills:code-review-and-quality (caught `m.last_confirmed` compile-against-view bug via red test; fix: query `memories.updated_at` directly — the l1_pages view aliases it), andrej-karpathy-skills:karpathy-guidelines, compound-engineering:git-commit.
- Summary: `packages/memory/src/retrieval/v3-search.ts` (+382 lines) exports `runV3Search` that runs FTS + vec + graph stages, fuses via weighted RRF (defaults fts=1.0, vec=1.0, graph=0.5), and filters by confidence floor (default 0.3) + supersession (default skip) + schema_version >= 3. Graph stage resolves query entities via case-insensitive LIKE on `graph_entities.name` / `aliases`, BFS 1-2 hops (configurable), intersects with `memories.entities` via `json_each`. Every hit carries `sources[]` from provenance JSON + `l0_wikilinks[]` parsed from the body (filtered to `raw/*` prefixes only).
- Commit: 53962da (3 files: retrieval/v3-search.ts +382, test +219, barrel +4; +605 lines)
- Next: PR 5 unit 5.2 — read RRF weights from env (`FULCRUM_RRF_WS_FTS`, `FULCRUM_RRF_WS_VEC`, `FULCRUM_RRF_WS_GRAPH`). Default values unchanged. Should be surgical — just wire `v3DefaultWeights()` through an env lookup.
- Notes: fulcrum-memory 876/876 (was 868 at PR 4 close — +8 new tests). Full `pnpm -r build` clean. Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD): (a) runV3Search is exported but NOT called from recallMemory yet — PR 5.3 wires `recall_knowledge`, PR 5.5 flips the flag. Decoupling the pipeline from the dispatch keeps each unit testable independently. (b) Graph BFS uses two `IN (…)` clauses in the edge SELECT — simpler than a JOIN chain, and the frontier sizes are bounded by prior-hop reach (typically <50 entities even for dense graphs). (c) `resolveQueryEntities` exported publicly so PR 5.4 `fulcrum memory trace` can reuse it for the "which entity matched" reverse lookup. (d) weights plumbed through an input override for deterministic tests; PR 5.2 adds env parsing.

### 2026-04-18 23:39 — PR 4 — COMPLETE (all 4 units shipped)
- Skills invoked across units: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (18 new tests across 4 files — 5 relocation + 6 recordL1Embedding + 5 reindex-l2 + 2 end-to-end pipeline), agent-skills:api-and-interface-design (public surface: `recordL1Embedding(db, page_id): void` + `reindexL2(input)` + CLI flag shape), agent-skills:security-and-hardening (audit invariant: supersession keeps the old vec_memories row byte-identical), agent-skills:performance-optimization (EMBED_CONCURRENCY queue invariant pinned by test; reindex-l2 sequential await for deterministic operator behaviour), agent-skills:source-driven-development (verified vec0 virtual-table semantics by reading existing sweep.ts DELETE pattern; repo-archaeology on FulcrumConfig shape for the stub embedder), compound-engineering:review:cli-readiness-reviewer (JSON stdout, exit 2 on any failure, long-form --help), andrej-karpathy-skills:karpathy-guidelines (surgical moves; no speculative abstractions; no backwards-compat shims; no retry/backoff), compound-engineering:git-commit.
- Units shipped:
  - 4.1 Relocate `storeEmbeddingInVec` / `storeChunkEmbedding` / shared queue into `packages/memory/src/l2/{queue,embed,code}.ts` — commit 9c85145 (+272/-184, 5 files)
  - 4.2 `recordL1Embedding(db, page_id)` + curator apply-layer hook + vec0 DELETE-then-INSERT fix — commit c116e70 (+307/-2, 5 files)
  - 4.3 `fulcrum memory reindex-l2 [--pages] [--code]` operator CLI — commit f765b38 (+332/-1, 4 files)
  - 4.4 End-to-end L0 → L1 → L2 integration test + PR 4 Verify gate + supersession audit invariant — commit 5c92078 (+247, 1 file)
- PR 4 totals: 4 code commits, +1158 lines, 0 regressions. fulcrum-memory 868/868 (was 855 at PR 3 close — +13 new tests). fulcrum-cli 398/398 (was 393 — +5 new tests). Full `pnpm -r build` clean.
- Verify gate (plan): `fulcrum memory reindex-l2 --pages` completes; `sqlite3 "SELECT COUNT(*) FROM vec_memories" == L1 page count` — proven by 4.4 end-to-end test + 4.3 CLI test.
- Key discovery (raise before PR 5 if anyone disagrees): sqlite-vec vec0 virtual tables do NOT honour `INSERT OR REPLACE` — repeat inserts for the same primary key throw UNIQUE constraint. The existing pattern in write.ts worked only because each memory_id was written exactly once before v3. Now fixed in both `storeEmbeddingInVec` and `storeChunkEmbedding` with explicit `DELETE FROM vec_* WHERE ... = ?` before `INSERT` — matches sweep.ts. PR 4 was the first code that stressed this; without the fix reindex-l2 and mid-session updates would both throw silently.
- Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD — raise before PR 5):
  1. recordL1Embedding reads `memories.content` (not the vault file) — the apply-layer just committed the row, so the DB is authoritative and the read costs zero disk IO.
  2. Stub embedder lives in `tests/helpers.ts` (memory) + inline copy in `cli/src/tests/memory-reindex-l2.test.ts` — no cross-package test-helper coupling.
  3. reindex-l2 "no flag = both scopes" — the operator wants "just do it"; neither scope is the sensible default.
  4. Sequential await inside reindex-l2 (not through the queue) — operator commands want deterministic completion + progress, not fire-and-forget backpressure.
  5. Dropped `recordL1Embedding` from barrel re-export deliberation: added it to the public barrel since the cli backfill path will want to call it directly once PR 5 lands.
- Next: PR 5 — Retrieval cutover (graph + confidence + supersession filters). Load-bearing skills: agent-skills:performance-optimization (graph traversal 100ms budget), compound-engineering:ce-optimize (metric-driven iteration on RRF weights against the eval corpus — do NOT hand-pick), agent-skills:shipping-and-launch (this is the cutover PR — `FULCRUM_MEMORY_V3` default flips), compound-engineering:review:deployment-verification-agent (Go/No-Go checklist + SQL verify + rollback plan). Before starting: ensure eval corpus + harness from §Test Corpus is wired up; the cutover blocks on it passing.
- Notes: Same doc-commit discipline observed — every ledger edit in a dedicated `docs(plans):` commit per feedback_never_commit_docs. Pre-existing unrelated WIP (agent-integration/, packages/core/janitor.ts, packages/worktrees/, .pi-lens/, .codex, plan edits from other sessions) left untouched across all 4 code commits. Stopping per STOP CONDITIONS — end-of-PR hand-off.

### 2026-04-18 23:34 — PR 4 unit 4.3 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (5 TDD cases — --pages scope, --code scope, both-default, repeat-safe replay, schema_version gate), agent-skills:api-and-interface-design (CLI flag surface `[--pages] [--code]` + JSON result shape `{pages,code:{scanned,embedded,failed}}`), compound-engineering:review:cli-readiness-reviewer (`--help` long-form; JSON-only stdout for agent consumption; exit 2 on any embed failure), agent-skills:performance-optimization (sequential await over vec0 INSERT — predictable + easy to reason about; queue overkill for one-shot operator ops), andrej-karpathy-skills:karpathy-guidelines (no retry/backoff speculation, no progress-bar library, no config file), compound-engineering:git-commit.
- Summary: `packages/cli/src/commands/memory-reindex-l2.ts` (+84) exports `reindexL2(input)`. CLI dispatch in `packages/cli/src/index.ts` adds the `reindex-l2` subcommand under `memory`, warms the embedder via `warmEmbedding()`, delegates to `reindexL2()`, prints JSON, exits 2 if any row failed. Completed the vec0 DELETE-then-INSERT fix in `storeChunkEmbedding` so `--code` is repeat-safe.
- Commit: f765b38 (4 files: command +84, test +213, cli index +30, l2/code.ts +6/-1; +332/-1)
- Next: PR 4 unit 4.4 — integration tests: the plan calls for 4 assertions bundled in one file. Most are already covered by the dedicated per-unit suites (4.1, 4.2, 4.3). Unit 4.4 will add a single end-to-end test that runs the full pipeline: ingestRawSource → curator (stub backend) → applyCuratorOutput → reindexL2 roundtrip → `COUNT(*) FROM vec_memories == L1 page count`. Closes the PR 4 Verify gate.
- Notes: fulcrum-cli 398/398 (was 393 — +5 new tests). fulcrum-memory 866/866. Full `pnpm -r build` clean. Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD): (a) flag semantics — neither flag = both scopes (matches the plan's Verify gate "reindex-l2 --pages completes" style; the operator wants "just do it" when unspecified). (b) Sequential await rather than queue enqueue — operator commands expect deterministic completion + progress report; the queue's backpressure is designed for high-throughput fire-and-forget (writeMemory), not single-shot reindex. (c) `--code` orders by `indexed_at`, `--pages` by `created_at` — both match the table's natural authoritative timestamp column. (d) No `--dry-run` — rescheduling with the same data is idempotent thanks to DELETE-then-INSERT, and a dry-run would just count rows (already visible in `COUNT(*) FROM memories WHERE schema_version >= 3`).

### 2026-04-18 23:27 — PR 4 unit 4.2 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (6 TDD cases — sync return + unknown-id no-op + new_pages + update-replaces-vec + supersede-keeps-old-row + dry-run-no-writes), agent-skills:api-and-interface-design (`recordL1Embedding(db, page_id): void` is the new public entry; reads `memories.content` internally so callers don't re-plumb text), agent-skills:performance-optimization (reuses the shared EMBED_CONCURRENCY queue — no new worker pool), agent-skills:code-review-and-quality (caught the latent vec0 UNIQUE-constraint bug on update via the red test; ruled out scope creep into storeChunkEmbedding per plan "code_chunks unchanged"), andrej-karpathy-skills:karpathy-guidelines (no speculative async/await; single call-site per diff stream), compound-engineering:git-commit.
- Summary: `packages/memory/src/l2/embed.ts` (+31 lines) now exports `recordL1Embedding` that enqueues an L1 embed task on the shared queue. Apply-layer (`packages/memory/src/l1/apply.ts`, +8 lines) fires one `recordL1Embedding` per created page, updated page, and supersession-new-page AFTER the transaction commits — old superseded rows and dry-run paths stay untouched. Fix: `storeEmbeddingInVec` now does `DELETE FROM vec_memories … WHERE memory_id = ?` then `INSERT` — vec0 virtual tables do not honour INSERT OR REPLACE; matches the existing pattern in `packages/memory/src/sweep.ts`. New test helpers `registerStubEmbedder` / `unregisterStubEmbedder` in `tests/helpers.ts` (+51) register a deterministic 1024-dim provider so suites can hit real `vec_memories` rows with no ONNX download.
- Commit: c116e70 (5 files: l2/embed.ts +31, l1/apply.ts +8, index.ts +3, tests/helpers.ts +51, new test +216; +307/-2)
- Next: PR 4 unit 4.3 — `fulcrum memory reindex-l2 [--pages|--code]` operator CLI. `--pages` walks `memories` WHERE schema_version >= 3 and re-embeds; `--code` walks `code_chunks` and re-embeds; default does both. No new primitives — wraps `recordL1Embedding` + `storeChunkEmbedding` + `flushPendingMemoryWrites`.
- Notes: fulcrum-memory 866/866 (was 860 — +6 new tests). fulcrum-cli 393/393. Full `pnpm -r build` clean. Implementation-detail judgment calls (per OPEN-QUESTIONS GUARD): (a) reads body from `memories.content` (not by re-parsing the vault file) — faster + already consistent with the writer, and the apply-layer has just committed the row. (b) Fire-and-forget enqueue matches v2a semantics; errors go to stderr, not propagated. (c) Did NOT apply the vec0 DELETE-first fix to `storeChunkEmbedding` — plan §PR 4 says "code_chunks unchanged" and no test currently re-embeds a chunk. Raise if reindex-l2 --code surfaces the same bug.

### 2026-04-18 23:17 — PR 4 unit 4.1 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (5 new cases — same-ref barrel pins + bounded-parallelism check), agent-skills:api-and-interface-design (public function names preserved; new `l2/{queue,embed,code}` surface is the internal boundary), agent-skills:performance-optimization (bounded-queue invariant protected by test — peak in-flight ≤ EMBED_CONCURRENCY), andrej-karpathy-skills:karpathy-guidelines (surgical move — zero behaviour change), compound-engineering:git-commit.
- Summary: Three new files under `packages/memory/src/l2/`. `queue.ts` owns the EMBED_CONCURRENCY queue + `enqueueEmbed` + `trackAsyncWork` + `flushPendingMemoryWrites` + `waitForEmbedHeadroom`. `embed.ts` hosts `storeEmbeddingInVec` (vec_memories + memories.embedding). `code.ts` hosts `storeChunkEmbedding` + `scheduleChunkEmbedding` (vec_chunks + code_chunks.embedding). `write.ts` re-exports the public names so cli, indexer/registry.ts, ingest.ts, and pci/syncer.ts keep compiling against `./write.js`.
- Commit: 9c85145 (5 files: 3 l2 sources + test + write.ts trim; +272/-184)
- Next: PR 4 unit 4.2 — add `recordL1Embedding(db, page_id)` and invoke it from the curator apply-layer (`createCuratedPage`, `updateCuratedPage`, `supersedeCuratedPage`). Supersession: old vec_memories row stays (audit, not deletion); new page gets its own row.
- Notes: fulcrum-memory 860/860 (was 855 — +5 new relocation tests). fulcrum-cli 393/393. Full `pnpm -r build` clean. Same doc-commit discipline observed — this ledger entry lands in a separate `docs(plans):` commit.

### 2026-04-18 23:05 — PR 3 — COMPLETE (all 8 units shipped)
- Skills invoked across units: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development, agent-skills:api-and-interface-design (CuratorInput/Output/Backend/LogEntry = load-bearing public contracts), agent-skills:source-driven-development + find-docs (ctx7 for OpenAI Structured Outputs + `codex exec --help` for subprocess contract), agent-skills:security-and-hardening (Constraints §14 delimiter isolation, §15 allowlist defense-in-depth at parser AND apply layers, §16 credential redaction across all three backends), codex:gpt-5-4-prompting (load-bearing — XML-blocked curator prompt), codex:codex-cli-runtime + codex:codex-result-handling (subprocess contract + result parsing), compound-engineering:agent-native-architecture (backend registry is the agent-facing extension point), andrej-karpathy-skills:karpathy-guidelines, compound-engineering:git-commit.
- Units shipped:
  - 3.1 `l1/curator.ts` — prompt composer + JSON Schema + parser + dispatcher — commit 277a1b9 (+1275, 3 files)
  - 3.2 `l1/curator-backend/codex.ts` — subprocess + JSONL stream — commit e71e0cb (+453, 3 files)
  - 3.3 `l1/curator-backend/pi.ts` — stub placeholder — commit 66e69bd (+84, 3 files)
  - 3.4 `l1/curator-backend/openai.ts` — Structured Outputs via fetch — commit 376ff46 (+390, 3 files)
  - 3.5 `l1/apply.ts` — atomic diff executor (DB txn + vault rollback) — commit fcd1846 (+570, 3 files)
  - 3.6 `fulcrum memory curate` CLI — commit de2a5c9 (+368, 3 files)
  - 3.7 `l1/telemetry.ts` — JSONL audit log at `vault/curated/log.md` — commit 71a66a3 (+240, 5 files)
  - 3.8 integration tests — 6 cases exercising the full stack — commit dd411d0 (+390, 1 file)
- PR 3 totals: 8 code commits, +3770 lines, 0 regressions. fulcrum-memory 855/855 (was 778 at PR 2 close — +77 new tests). fulcrum-cli 393/393 (was 379 — +14 new tests). Full `pnpm -r build` clean.
- Verify gate: `fulcrum memory curate --dry-run` prints diff JSON without writing; live run lands pages on disk + in `memories`; `vault/curated/log.md` records both runs with correct backend + prompt_version=v3.0.0; `--backend openai` routes to the API path. Proven by 6 integration cases in unit 3.8.
- Constraint §15 defense-in-depth verified at both layers: parser (`parseCuratorOutput` rejects fabricated ULIDs before output crosses the runtime boundary) AND apply (`createCuratedPage` re-runs validator with `curator_input_sources`) — belt-and-suspenders against LLM hallucination.
- Implementation-detail judgment calls made (per OPEN-QUESTIONS GUARD — raise if wrong before PR 4):
  1. **Backend registry**: backends NOT auto-registered on import. Consumers (CLI bootstrap) call `registerDefaultCuratorBackends()` which uses if-absent semantics so test stubs survive. Rationale: tests need an empty registry; auto-registration side effects broke stubs in the first red run of unit 3.6 — fix confirmed before adding tests.
  2. **`new_entities` deferred**: plan's CuratorOutput shape is `{new_pages, updates, supersessions, new_edges}`. Adding `new_entities` crosses the public-contract line (CuratorOutput is exported). First-run bootstrapping references pre-existing graph_entities only; validator rule 6 (UNKNOWN_ENTITY) rejects hallucinated entity IDs. Raise this if it blocks graph growth in practice.
  3. **confidence_deltas telemetry**: records the declared confidence values from the CuratorOutput, not true before/after deltas. Computing deltas would require apply-layer to capture pre-update confidence per page. Deferred to a later PR if the eval harness (PR 8) actually needs deltas.
  4. **OpenAI endpoint**: used `/v1/chat/completions` with `response_format.json_schema.strict:true` + top-level `reasoning_effort`. Plan allows either Chat Completions or Responses API; Chat Completions is stable for GPT-5 and the simpler integration. Swap to `/v1/responses` in a follow-up if batch-API (Phase 6 backfill) demands it.
  5. **Codex subprocess sandbox**: `--sandbox read-only` is correct — the curator returns a JSON object; no tool use needed. Running with `workspace-write` would grant unnecessary write authority.
  6. **Credential redaction patterns**: regex-based on sk-/AKIA/Bearer/*_KEY= shapes — fail-open (leaves other stderr text intact). More aggressive redaction would hide actionable error messages; this bar matches the existing sanitize engine.
- Known follow-ups deliberately deferred:
  - pi backend wiring (plan explicitly says "stub in PR 3").
  - Anthropic backend wiring (plan says "ANTHROPIC_API_KEY → Claude Haiku via API" — no urgency; openai + codex cover the 95% case).
  - L2 embedding of L1 pages (handled by PR 4).
  - MCP parity for `fulcrum memory curate` (handled by PR 5 unit 5.4).
  - `new_entities` in CuratorOutput (see judgment call 2).
- Next: PR 4 — L2 reshape. Relocates `storeEmbeddingInVec` + `storeChunkEmbedding` into `packages/memory/src/l2/`, triggers `recordL1Embedding` after each curator page write, ships `fulcrum memory reindex-l2` operator command. Load-bearing skills: agent-skills:performance-optimization (embedding batch queue + p95 budget), find-docs on @xenova/transformers batch-embed API, compound-engineering:review:performance-reviewer pre-merge.
- Notes: Same doc-commit discipline observed — every ledger edit lands in a dedicated `docs(plans):` commit per feedback_never_commit_docs. Pre-existing unrelated WIP (agent-integration/, packages/core/janitor.ts, packages/worktrees/, .pi-lens/, .codex, plan edits from other sessions) left untouched across all 8 code commits. Stopping per STOP CONDITIONS — end-of-PR hand-off.

### 2026-04-18 23:02 — PR 3 unit 3.7 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (4 + 2 TDD cases), agent-skills:api-and-interface-design (CuratorLogEntry = pinned public schema; add-only non-breaking), agent-skills:documentation-and-adrs (header comment on telemetry.ts lists the downstream consumers — PR 7.4 lint, PR 8 eval harness), andrej-karpathy-skills:karpathy-guidelines, compound-engineering:git-commit.
- Summary: `packages/memory/src/l1/telemetry.ts` (+44 lines) exports `appendCuratorLog(vaultRoot, entry)` that JSONL-appends to `vault/curated/log.md` (creates parent dir on first write). CLI `curateMemory` now emits one record per call — live runs AND `--dry-run` runs — so audit covers skipped curations. `confidence_deltas` reports the declared confidence values from the CuratorOutput (true before/after deltas deferred; would need pre-read in apply-layer).
- Commit: 71a66a3 (5 files: telemetry.ts +44, telemetry test +96, memory index +4, cli curate handler +51, cli curate test +23)
- Next: PR 3 unit 3.8 — integration test consolidating the full curator pipeline (stub backend + seeded L0 + end-to-end apply + telemetry assertions). Also marks the PR 3 Verify gate.
- Notes: fulcrum-memory suite 855/855 (was 851 — +4 new telemetry tests). fulcrum-cli suite 387/387 (was 385 — +2 telemetry CLI tests). `pnpm -r build` clean.

### 2026-04-18 22:58 — PR 3 unit 3.6 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (6 TDD cases — stub-backend end-to-end, dry-run no-op, flag routing, not-found, missing-file, default-registration), agent-skills:api-and-interface-design (CLI flag surface = public contract; kept it minimal: `<l0_id>`, `--dry-run`, `--backend`, `--task`, `--model`, `--reasoning`), compound-engineering:review:cli-readiness-reviewer (JSON-only stdout for agent consumption; human-readable `--help` only when TTY-less or explicit), andrej-karpathy-skills:karpathy-guidelines (no config-file indirection; env + flags only), compound-engineering:git-commit.
- Summary: `packages/cli/src/commands/memory-curate.ts` (+130 lines) exports `curateMemory(input)` which loads the l0_sources row, reads the raw body from disk (frontmatter strip via inline `---\n...\n---\n` split — no gray-matter dep added to the CLI package), registers codex/pi/openai if absent, runs the curator, and dispatches to applyCuratorOutput. Wired into `packages/cli/src/index.ts` dispatcher under the `memory curate` subcommand with full `--help`. Output is a JSON object `{l0_id, backend, model, prompt_version, duration_ms, dry_run, apply}` for agent consumption.
- Commit: de2a5c9 (3 files: commands/memory-curate.ts +130, tests/memory-curate.test.ts +137, cli/index.ts +46)
- Next: PR 3 unit 3.7 — curator telemetry. Append `vault/curated/log.md` audit entry `{l0_id, backend, affected_pages[], new_entities[], confidence_deltas[], duration_ms, prompt_version}` after each curate run.
- Notes: registerDefaultCuratorBackends uses `getBackend() ?? registerBackend()` (if-absent semantics) so test stubs survive the default pass — a regression-trap I caught on the first red run and fixed before adding more tests. fulcrum-cli suite 385/385. Full `pnpm -r build` clean.

### 2026-04-18 22:52 — PR 3 unit 3.5 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (10 TDD cases — happy path + rollback + dry-run), agent-skills:api-and-interface-design (ApplyContext / ApplyResult are agent-facing contracts; CuratorOutput→vault mapping is deterministic), agent-skills:security-and-hardening (validator `curator_input_sources` re-check at apply-time — defense-in-depth beyond the parser-layer check), andrej-karpathy-skills:karpathy-guidelines (no vault-rollback framework; tracked paths + unlink on error, nothing more), compound-engineering:git-commit.
- Summary: `packages/memory/src/l1/apply.ts` (+192 lines) orchestrates CuratorOutput execution inside a single `db.transaction()`. new_pages → createCuratedPage (id/first_seen/last_confirmed filled by apply-layer); updates → updateCuratedPage with add_sources/add_entities merged+deduped + last_confirmed refreshed; supersessions → supersedeCuratedPage (audit chain preserved); new_edges → addEdge (throws on missing endpoint). On any exception, the DB transaction rolls back and vault files written during the attempt are unlinked (ULID-named, so cleanup is safe). dry_run=true short-circuits to a diff-shaped ApplyResult with no DB/vault side effects — wires into `fulcrum memory curate --dry-run` next unit.
- Commit: fcd1846 (3 files: apply.ts +192, test +273, memory index +3)
- Next: PR 3 unit 3.6 — `fulcrum memory curate` CLI. Wires runCurator → applyCuratorOutput, registers codex/pi/openai backends at startup, supports --dry-run and --backend flags.
- Notes: OPEN-QUESTIONS GUARD judgment call — the plan's CuratorOutput shape is `{new_pages, updates, supersessions, new_edges}` with no `new_entities` field. First-run bootstrapping therefore needs edges/page-entities to reference already-existing graph_entities; the curator that hallucinates an entity ID is rejected by validator rule 6 (UNKNOWN_ENTITY). Adding `new_entities` crosses a public-contract line (CuratorOutput is exported), so deferred to a follow-up rather than changing the schema mid-PR. fulcrum-memory suite 851/851 (was 841 — +10 new tests). `pnpm -r build` clean.

### 2026-04-18 22:46 — PR 3 unit 3.4 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:test-driven-development (13 TDD cases against a local `http.createServer` stub — no real network), agent-skills:source-driven-development + find-docs (verified Chat Completions Structured Outputs contract via ctx7 against /websites/developers_openai_api — POST /v1/chat/completions with `response_format.json_schema.strict:true` + top-level `reasoning_effort`; `prompt_tokens_details.cached_tokens` for cached-input accounting), agent-skills:api-and-interface-design, agent-skills:security-and-hardening (Constraint §16 — OPENAI_API_KEY env-only, never logged, redacted from error bodies; assert-no-leak test), andrej-karpathy-skills:karpathy-guidelines (native `fetch` — no SDK dep added), compound-engineering:git-commit.
- Summary: `packages/memory/src/l1/curator-backend/openai.ts` (+141 lines) implements the OpenAI backend via Node's native `fetch`. POSTs the composed prompt as a single user message, wraps the curator JSON Schema in `response_format.json_schema` with `strict:true`, and passes `reasoning_effort`. Extracts `choices[0].message.content` → `raw_text`; maps `usage.prompt_tokens` → `input_tokens`, `usage.prompt_tokens_details.cached_tokens` → `cached_input_tokens`, `usage.completion_tokens` → `output_tokens`. Honors `timeout_ms` via AbortController. `FULCRUM_CURATOR_OPENAI_BASE_URL` env override lets tests swap in a local HTTP stub.
- Commit: 376ff46 (3 files: backend +141, test +235, memory index +1)
- Next: PR 3 unit 3.5 — deterministic apply-layer (takes CuratorOutput + executes via l1/page.ts + l1/entities.ts in a single SQLite transaction).
- Notes: fulcrum-memory suite 841/841 (was 828 — +13 new tests). `pnpm -r build` clean.

### 2026-04-18 22:41 — PR 3 unit 3.3 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development, agent-skills:api-and-interface-design (CuratorBackend contract), andrej-karpathy-skills:karpathy-guidelines (no speculative implementation — plan explicitly says stub), compound-engineering:git-commit.
- Summary: `packages/memory/src/l1/curator-backend/pi.ts` (+33 lines) reserves the pi slot in the dispatcher fallback order. `isAvailable()` always returns false until pi's non-interactive + structured-output contract stabilizes; `curate()` throws a NotImplementedError with install guidance (`FULCRUM_CURATOR_BACKEND=codex|openai`). Keeping it unavailable means auto-select skips pi even when `pi` is on PATH — routing curator traffic through an unfinished contract is riskier than skipping the slot.
- Commit: 66e69bd (3 files: backend +33, test +44, memory index +1)
- Next: PR 3 unit 3.4 — `l1/curator-backend/openai.ts`. Direct OpenAI API via Node `fetch` (no SDK dep), `response_format: { type: 'json_schema', strict: true }`. Verify Structured Outputs spec via find-docs before writing.
- Notes: fulcrum-memory suite 828/828 (+3 new tests). `pnpm -r build` clean.

### 2026-04-18 22:39 — PR 3 unit 3.2 — completed
- Skills invoked: agent-skills:incremental-implementation, agent-skills:test-driven-development (12 TDD cases via canned-JSONL stub), agent-skills:api-and-interface-design (CuratorBackend contract matched), agent-skills:security-and-hardening (Constraint #16 credential redaction on stderr — sk-/AKIA/Bearer/*_KEY= patterns; 0600 schema file perms; read-only sandbox), agent-skills:source-driven-development (ran `codex exec --help` + emitted canned JSONL to confirm the event shape before coding), codex:codex-cli-runtime + codex:codex-result-handling (consulted for the event contract), andrej-karpathy-skills:karpathy-guidelines, compound-engineering:git-commit.
- Summary: `packages/memory/src/l1/curator-backend/codex.ts` (+230 lines) spawns `codex exec -m <model> -c model_reasoning_effort=<reasoning> --json --output-schema=<schema.json> --ephemeral --skip-git-repo-check --sandbox read-only`, pipes the prompt on stdin, and parses the JSONL event stream (`thread.started` → `turn.started` → `item.completed{agent_message.text}` → `turn.completed{usage}`). Captures model usage, honors `timeout_ms` with SIGTERM→SIGKILL escalation, redacts credential-shaped tokens on stderr before propagating error text. `FULCRUM_CODEX_BINARY` env var lets tests swap in a Node stub so CI makes no real LLM call.
- Commit: e71e0cb (3 files: backend +230, test +202, memory index +3 barrel re-export)
- Next: PR 3 unit 3.3 — `l1/curator-backend/pi.ts` stub. Same CuratorBackend shape; stub implementation fails loudly with install guidance when invoked (pi non-interactive mode not stable yet per plan).
- Notes: Explicitly chose NOT to auto-register the backend on import — tests can keep a clean registry. fulcrum-memory suite 825/825 (was 813 — +12 new tests matches). Full `pnpm -r build` clean. Doc-commit discipline observed (this ledger entry stays working-tree only until the end-of-PR docs commit).

### 2026-04-18 22:33 — PR 3 unit 3.1 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (35-test TDD gate written before implementation), agent-skills:api-and-interface-design (CuratorInput / CuratorBackend interface is the public contract 3.2-3.4 build against), agent-skills:security-and-hardening (Constraint #14 `<USER_CONTENT>` + `<AGENT_CORRECTION>` delimiter isolation; Constraint #15 curator-sources-in-batch allowlist as defense-in-depth at the parser layer), agent-skills:source-driven-development (verified `codex exec --help` + JSONL stream shape against codex-cli 0.121.0), codex:gpt-5-4-prompting (load-bearing — composed the XML-blocked prompt: `<task>`/`<templates>`/`<structured_output_contract>`/`<grounding_rules>`/`<verification_loop>`), codex:codex-cli-runtime + codex:codex-result-handling (read for PR 3 context; actual subprocess contract lands in 3.2), compound-engineering:agent-native-architecture (backend registry is the agent-facing extension point), andrej-karpathy-skills:karpathy-guidelines (no speculative error paths, no redundant types, sparse comments — only the `<USER_CONTENT>` isolation and Constraint #15 defense-in-depth rationale carry inline notes), compound-engineering:git-commit.
- Summary: `packages/memory/src/l1/curator.ts` (+566 lines) ships the curator runtime: `PROMPT_VERSION='v3.0.0'`, `TASK_DEFAULTS` per the plan table (extraction `gpt-5-mini`/minimal, consolidation `gpt-5-nano`/minimal, synthesis `gpt-5`/medium), `composePrompt()` with per-task instructions + XML delimiter wrapping, `getOutputSchema()` strict-mode-compatible JSON Schema (`additionalProperties:false`, all required, no allOf/if/const/pattern — works across codex --output-schema, OpenAI Structured Outputs, Anthropic tool_use unchanged), `parseCuratorOutput()` tolerant of markdown fences + model preamble + Constraint #15 allowlist check, `registerBackend`/`getBackend`/`listBackends`/`clearBackendsForTest` registry, `selectBackend()` with env override > codex > pi > openai > anthropic fallback order, `runCurator()` orchestrator. Memory index re-exports the full surface. 35 new vitest cases cover prompt determinism, wrapper correctness for L0 + corrections, schema strictness (regex-asserts absence of allOf/if/then/else/const/pattern), parser robustness across 10 rejection cases, backend selection (env override, auto-select order, unregistered-override rejection, no-backend-available message), and end-to-end runCurator via stub backends.
- Commit: 277a1b9 (3 files: curator.ts +566, test +463, memory index +33; +1275 total)
- Next: PR 3 unit 3.2 — `l1/curator-backend/codex.ts`. Verified at orient-time that `codex exec` supports `--json`, `--output-schema <FILE>`, `-m/--model`, `-c key=value` for reasoning effort, `--ephemeral`, `--skip-git-repo-check`. JSONL event stream verified: `thread.started` → `turn.started` → `item.completed { item: { type: 'agent_message', text: ... } }` → `turn.completed { usage }`.
- Notes: Backends register themselves (not constructor-injected) so the memory package doesn't bundle openai/anthropic deps into dist when they're not needed. Tests clear the registry per-run via `clearBackendsForTest()`. Full memory suite 813/813 (was 778 at PR 2 close — +35 new tests matches). `pnpm -r build` clean. Same doc-commit discipline observed (ledger stays working-tree-only until the end-of-unit docs commit).

### 2026-04-18 21:41 — PR 2 unit 2.1 — completed
- Skills invoked: agent-skills:context-engineering, agent-skills:incremental-implementation, agent-skills:test-driven-development (23-case smoke gate written red before any template file existed), agent-skills:code-review-and-quality (self-review: plan-verbatim transcription, diffed my first page.template.md against the plan and caught drift — added supersedes/superseded_by that the plan omits for type:page, reverted to flow-style `sources: [{{L0_ULID}}]`), andrej-karpathy-skills:karpathy-guidelines (surgical; no runtime loader, no asset-copy tsup config — those belong to unit 2.2/2.3), elements-of-style:writing-clearly-and-concisely (template scaffolding prose), episodic-memory:remembering-conversations (session-start search — no prior PR 2 context; note: ledger's previous "decide Open Question #2 validator library" is a mis-numbering — plan's Q2 is prompt-version pinning; validator lib is deferred to 2.3), compound-engineering:git-commit.
- Summary: Four canonical L1 page templates landed under `packages/memory/src/l1/templates/` as `.md` files — `entity.template.md` (supersession + aliases + entity_type), `concept.template.md` (sources OR sources_via), `page.template.md` (source distillation; no supersession fields per plan), `synthesis.template.md` (sources_via mandatory; retention_tier: episodic). Every template carries v3 schema field, ULID placeholder, retention_tier, access_count, workspace/project_id, and ≥1 `[[raw/...]]` inline wikilink. Smoke gate at `src/tests/l1-templates.test.ts` (23 tests) checks file presence, frontmatter contract, body contract (H1 + wikilink), and absence of TODO/FIXME/XXX markers that would trip unit 2.3's validator.
- Commit: 09e9585 (+225 lines, 5 net-new files; 4 templates + 1 test).
- Next: PR 2 unit 2.2 — `l1/page.ts` with `createCuratedPage(template, vars)`, `updateCuratedPage`, `supersedeCuratedPage`, `readCuratedPage`. Every write runs through `validateL1Page` before it hits disk. Stub the validator first so 2.2 can ship; 2.3 fills in the real rule set. Also at 2.2 time: decide the dist/ asset story for templates (inline as TS constants vs. tsup `onSuccess` copy vs. `fs.readFileSync(new URL(...))` off `import.meta.url` — the third works in dev but needs a copy step for published `dist/`). gray-matter `^4.0.3` already in `packages/memory` deps so no lib fetch needed for 2.3 frontmatter parse (ajv vs zod is the remaining 2.3 decision, still deferred).
- Notes: fulcrum-memory suite 695/695 (was 672 at PR 1 close; +23 new tests matches exactly). Full `pnpm -r build` clean. Same doc-commit discipline observed — templates + test in one code commit; this ledger entry lands in a separate docs commit per `feedback_never_commit_docs`. Pre-existing unrelated WIP (agent-integration/, packages/core/janitor.ts, packages/worktrees/, .pi-lens/, .codex, plan edits, etc.) left untouched.
