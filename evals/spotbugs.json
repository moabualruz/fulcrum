[
  {"query": "Find any null-pointer dereference bugs in this compiled Java jar — it's target/myservice-1.0.jar built from: ```java\npublic class OrderService {\n  public String getStatus(Order order) {\n    return order.getCustomer().getName().toUpperCase();\n  }\n}\n```", "should_trigger": true},
  {"query": "Audit the build output of our Java service for concurrency bugs before release — project compiles to target/myapp.jar with classes including: ```java\npublic class Counter {\n  private int count = 0;\n  public void increment() { count++; }\n  public int get() { return count; }\n}\n```", "should_trigger": true},
  {"query": "Run a bytecode static analyzer on target/myapp.jar and produce a report — the jar contains classes like: ```java\npublic class ResourceLoader {\n  InputStream open(String path) throws IOException {\n    return new FileInputStream(path); // never closed\n  }\n}\n```", "should_trigger": true},
  {"query": "Produce a SARIF report from this compiled Java jar so I can upload it to GitHub Code Scanning (jar is at build/libs/app.jar, built from standard Gradle layout).", "should_trigger": true},
  {"query": "We're still using findbugs in our pipeline — what's the modern replacement and how do we migrate the config?", "should_trigger": true},
  {"query": "Generate an HTML bug report for the compiled classes under build/classes — the project has typical Maven structure with these bug-prone classes: ```java\npublic class DBConn {\n  Connection c = DriverManager.getConnection(url);\n  // connection never closed\n}\n```", "should_trigger": true},
  {"query": "Add the find-sec-bugs plugin to our Java static analysis to catch SQL injection and weak crypto in compiled code (Maven project, pom.xml has spotbugs-maven-plugin configured).", "should_trigger": true},
  {"query": "Set up a CI gate that fails the build when high-confidence correctness bugs land in the jar (Gradle project with build/libs/*.jar output).", "should_trigger": true},
  {"query": "How do I suppress a specific spotbugs rule (NP_NULL_ON_SOME_PATH) on one method without disabling it globally in this class: ```java\npublic class Processor {\n  // want to suppress NP_NULL_ON_SOME_PATH here only\n  public void run(Input input) {\n    process(input.getData());\n  }\n}\n```", "should_trigger": true},
  {"query": "Tune the effort level so PR scans are fast but nightly catches deeper issues — project builds to target/app.jar with a spotbugs config at config/spotbugs.xml.", "should_trigger": true},
  {"query": "Write an exclude filter XML so generated classes in com.example.generated.* aren't reported as buggy.", "should_trigger": true},
  {"query": "Detect potential infinite recursion or unclosed streams in our compiled Java application at build/output/app.jar.", "should_trigger": true},
  {"query": "Format this Java source file according to the Google Java style guide.", "should_trigger": false},
  {"query": "Find code smells and structural issues in our Java source tree.", "should_trigger": false},
  {"query": "Lint a Kotlin codebase against the official Kotlin coding conventions.", "should_trigger": false},
  {"query": "Scan our Maven dependencies for known CVEs.", "should_trigger": false},
  {"query": "Audit a Docker image for vulnerabilities before publishing.", "should_trigger": false},
  {"query": "Check our package-lock.json for outdated dependencies.", "should_trigger": false},
  {"query": "Run cyclomatic complexity analysis across our Java source files.", "should_trigger": false},
  {"query": "Format Python code with Black.", "should_trigger": false},
  {"query": "Find duplicated code blocks across the repo.", "should_trigger": false}
]
