[
  {"query": "Use ruff to fix style issues and remove unused imports from this Python file: ```python\nimport os, sys, json, re\nfrom pathlib import Path\n\ndef get_files(dir):\n    files = []\n    for f in os.listdir(dir):\n        files.append(f)\n    return files\n```", "should_trigger": true},
  {"query": "Run ruff to format these Python sources to a consistent style: ```python\ndef   process( items,limit=10 ):\n  result=[]\n  for i in items:\n      if i>0: result.append(i)\n  return result\n```", "should_trigger": true},
  {"query": "Run ruff to lint this Python module for undefined names and dead code: ```python\nfrom utils import helper\n\ndef compute(x, y):\n    total = x + y\n    unused_var = 42\n    return total\n\ndef _private():\n    pass\n```", "should_trigger": true},
  {"query": "Use ruff to show me what would change before reformatting this Python file — don't write anything yet (show the ruff format --diff command): ```python\nimport os,sys\ndef foo(x,y,z):\n  return x+y+z\nclass Bar:\n  def __init__(self): self.x=1\n```", "should_trigger": true},
  {"query": "I want to replace flake8, black, and isort with one tool in my Python project (has pyproject.toml and src/ layout).", "should_trigger": true},
  {"query": "Use ruff to auto-sort the imports in this Python file (show the command): ```python\nimport sys\nimport os\nfrom collections import defaultdict\nimport json\nfrom pathlib import Path\nimport re\n\nprint(os.getcwd())\n```", "should_trigger": true},
  {"query": "Configure pyproject.toml so this Python codebase has a 100-character line length and ignores E501 in tests: ```toml\n[tool.ruff]\nline-length = 88\n```", "should_trigger": true},
  {"query": "Run a Python linter in CI mode that fails the build if any file needs reformatting (project has src/ and tests/ directories).", "should_trigger": true},
  {"query": "Apply only safe autofixes to this Python file, then show me the diff of unsafe fixes: ```python\nimport os\nimport os.path\nx = map(lambda i: i*2, [1,2,3])\nprint(list(x))\n```", "should_trigger": true},
  {"query": "Use ruff to pyupgrade this Python file to modern syntax for Python 3.12 (ruff handles pyupgrade via the UP rule set): ```python\nfrom typing import List, Dict, Optional, Tuple\n\ndef process(items: List[str]) -> Optional[Dict[str, int]]:\n    result: Dict[str, int] = {}\n    for item in items:\n        result[item] = len(item)\n    return result if result else None\n```", "should_trigger": true},
  {"query": "I get JSON output from ruff and want to count findings by rule code — here's a sample: ```json\n[{\"code\":\"E501\",\"message\":\"line too long\"},{\"code\":\"F401\",\"message\":\"unused import\"},{\"code\":\"E501\",\"message\":\"line too long\"},{\"code\":\"F841\",\"message\":\"local variable unused\"}]\n```", "should_trigger": true},
  {"query": "Add a pre-commit step that lints and formats Python before each commit (project uses pre-commit with .pre-commit-config.yaml).", "should_trigger": true},
  {"query": "Format JavaScript files with prettier.", "should_trigger": false},
  {"query": "Lint a Go file for unused variables.", "should_trigger": false},
  {"query": "Run mypy to type-check my Python project.", "should_trigger": false},
  {"query": "Lint a Rust file with clippy.", "should_trigger": false},
  {"query": "Type-check a Python codebase with pyright.", "should_trigger": false},
  {"query": "Format a YAML file with consistent indentation.", "should_trigger": false},
  {"query": "Run pytest against my Python test suite.", "should_trigger": false},
  {"query": "Scan Python dependencies for known CVEs.", "should_trigger": false}
]
