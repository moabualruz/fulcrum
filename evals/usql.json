[
  {"query": "Run a quick SELECT on our postgres database from the shell: ```sql\nSELECT id, name, email FROM users WHERE active = true LIMIT 5;\n```\nDSN: postgres://admin:pass@localhost:5432/mydb", "should_trigger": true},
  {"query": "Query a SQLite file with one tool — list tables and grab a few rows from this schema: ```sql\nCREATE TABLE products (id INTEGER PRIMARY KEY, name TEXT, price REAL);\nINSERT INTO products VALUES (1,'Widget',9.99),(2,'Gadget',24.99);\n```\nFile: data/inventory.db", "should_trigger": true},
  {"query": "Run this ad-hoc SQL script against our mysql staging database: ```sql\nSELECT o.id, u.name, SUM(i.price) AS total\nFROM orders o\nJOIN users u ON u.id = o.user_id\nJOIN order_items i ON i.order_id = o.id\nGROUP BY o.id\nLIMIT 10;\n```\nDSN: mysql://root:pass@staging-db:3306/shop", "should_trigger": true},
  {"query": "Connect to a Snowflake warehouse and run this query from the command line: ```sql\nSELECT warehouse_name, credits_used_cloud_services\nFROM snowflake.account_usage.warehouse_metering_history\nWHERE start_time >= DATEADD(day, -7, CURRENT_DATE)\nORDER BY credits_used_cloud_services DESC;\n```", "should_trigger": true},
  {"query": "I have a DATABASE_URL in my env — run this query and pipe the results into jq: ```sql\nSELECT id, config::json FROM feature_flags WHERE enabled = true;\n```", "should_trigger": true},
  {"query": "Open an interactive SQL REPL against the cockroachdb cluster and inspect the schema — DSN is cockroach://root@localhost:26257/defaultdb?sslmode=disable.", "should_trigger": true},
  {"query": "I need a psql-like CLI that also works for mysql and sqlite — what should I use?", "should_trigger": true},
  {"query": "Run this SELECT count(*) against the production redshift cluster from a CI step: ```sql\nSELECT table_name, COUNT(*) as row_count\nFROM information_schema.tables\nWHERE table_schema = 'public'\nGROUP BY table_name;\n```", "should_trigger": true},
  {"query": "Spin up a duckdb in-memory session and query this parquet file: ```sql\nSELECT region, SUM(revenue) AS total\nFROM read_parquet('sales_2024.parquet')\nGROUP BY region\nORDER BY total DESC;\n```", "should_trigger": true},
  {"query": "Switch between postgres and mysql connections and run the same query on both: ```sql\nSELECT COUNT(*) FROM orders WHERE created_at > NOW() - INTERVAL '1 day';\n```\nPostgres DSN: postgres://user:pass@pg-host/db\nMySQL DSN: mysql://user:pass@mysql-host/db", "should_trigger": true},
  {"query": "Get the result of this SQL query as CSV so I can paste it into a spreadsheet: ```sql\nSELECT month, product, revenue, units_sold\nFROM sales_summary\nWHERE year = 2024\nORDER BY month, revenue DESC;\n```", "should_trigger": true},
  {"query": "Run a database migration to create a new users table on production.", "should_trigger": false},
  {"query": "Bulk import a 10GB CSV into postgres for a production load.", "should_trigger": false},
  {"query": "Run a kafka consumer to read messages from a topic.", "should_trigger": false},
  {"query": "Connect to redis and look up a key.", "should_trigger": false},
  {"query": "Run a graphql query against our internal API.", "should_trigger": false},
  {"query": "Set up version-tracked schema migrations with rollback support.", "should_trigger": false},
  {"query": "Search elasticsearch for documents matching a phrase.", "should_trigger": false},
  {"query": "Generate a Prisma migration from a schema change.", "should_trigger": false},
  {"query": "Browse a mongo collection and find documents matching a filter.", "should_trigger": false}
]
