[
  {"query": "What tasks does this repo expose? It has this justfile at the root:\n```make\ntest:\n\tpytest -q\nlint:\n\truff check .\nbuild:\n\tpython -m build\n```", "should_trigger": true},
  {"query": "Show the exact just command only; do not run it. Run the build recipe with the argument 'release' in this justfile:\n```make\nbuild mode='debug':\n\tcargo build --{{mode}}\n```", "should_trigger": true},
  {"query": "Show the exact just command only; do not run it. List all the project tasks I can run from this justfile:\n```make\ntest:\n\tpytest\nformat:\n\tblack .\ndeploy env:\n\tansible-playbook deploy.yml -e env={{env}}\n```", "should_trigger": true},
  {"query": "Show the exact just command only; do not run it. Show the body of the deploy recipe before executing it. Justfile:\n```make\ndeploy:\n\t@echo Deploying...\n\tgit push origin main\n\tssh prod 'systemctl restart app'\n```", "should_trigger": true},
  {"query": "This repo has a Justfile — how do I run the lint task?\n```make\nlint:\n\truff check . && mypy src/\n```", "should_trigger": true},
  {"query": "Show the exact just command only; do not run it. From a subdirectory of a project that uses just, run the `test` task from the project root.", "should_trigger": true},
  {"query": "How do I pass a parameter to a recipe in this justfile?\n```make\ngreet name:\n\t@echo Hello, {{name}}!\n```", "should_trigger": true},
  {"query": "What's the modern Makefile alternative this repo seems to be using? I see a `justfile` with recipes like `test`, `lint`, `build`.", "should_trigger": true},
  {"query": "How do I see all the variables a justfile defines and their resolved values? This one has:\n```make\nversion := `git describe --tags`\nenv := 'dev'\n```", "should_trigger": true},
  {"query": "Show the exact just command only; do not run it. Override the `env` variable in this justfile from the command line:\n```make\nenv := 'dev'\ndeploy:\n\tscp dist/ {{env}}-server:/app\n```", "should_trigger": true},
  {"query": "Show the exact just command only; do not run it. Run the test recipe and then the build recipe, fail fast if either errors. Justfile:\n```make\ntest:\n\tpytest -q\nbuild:\n\tpython -m build\n```", "should_trigger": true},
  {"query": "How do I make a recipe depend on another recipe so it always runs first? Example:\n```make\nbuild: test\n\tcargo build\n```", "should_trigger": true},
  {"query": "Run the makefile target build using make.", "should_trigger": false},
  {"query": "Run the npm script called dev.", "should_trigger": false},
  {"query": "List all npm scripts defined in package.json.", "should_trigger": false},
  {"query": "Execute a shell script located at scripts/release.sh.", "should_trigger": false},
  {"query": "Run a Python module from the command line with python -m.", "should_trigger": false},
  {"query": "Run a cargo task like cargo test --release.", "should_trigger": false},
  {"query": "What does the Taskfile.yml in this repo expose? I want to run a task with `task`.", "should_trigger": false},
  {"query": "Run a gradle task in this Java project.", "should_trigger": false}
]
