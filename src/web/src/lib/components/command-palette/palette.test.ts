import { describe, it } from "vitest";
describe("CommandPalette", () => {
  it.todo("dispatches 15+ commands");
  it.todo("search mode activates on 2+ char input");
  it.todo("bulk actions shown only when selection exists");
});
