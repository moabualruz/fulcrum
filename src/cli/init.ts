// fulcrum init — stub. Full impl in next phase.
export async function run(args: string[]): Promise<void> {
  console.error(`fulcrum init ${args.join(" ")}: not yet ported to TS (stub)`);
  process.exit(2);
}
