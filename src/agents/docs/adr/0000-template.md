# ADR-0000: Template

> Copy this file to `docs/adr/NNNN-short-title.md` (or `src/<context>/docs/adr/NNNN-short-title.md` for context-scoped decisions). Number sequentially, lowercase kebab-case slug.

- **Status:** proposed | accepted | superseded by ADR-NNNN | deprecated
- **Date:** YYYY-MM-DD
- **Deciders:** name(s) or role(s)
- **Context:** system-wide (`docs/adr/`) or `<context>` (e.g. `src/components/docs/adr/`)

## Context

What forced the decision? Constraint, trade-off, prior pain, or external dependency that must be respected.

## Decision

What we chose. State it as a directive: "We will …".

## Consequences

What follows from this choice — both the wins and the costs we're accepting. Include any invariants future code must preserve.

## Alternatives considered

Briefly: each option, why it lost. Especially for options that look obvious but have hidden costs.

## References

- Links to PRs, related issues, prior ADRs, external specs, vendor docs.
