---
name: fulcrum-workspace-status
description: Show current workspace status — running agents, tasks, blockers
---

Get a snapshot of the current workspace:

`fulcrum action exec get_workspace_status`

Shows: running agents, blocked runs, WIP count, queued tasks, recent events.
