{
  "_note": "This file documents the PI Agent OS extension. The real extension is the TypeScript cockpit in ./cockpit/. Install with: pi install ./cockpit",
  "name": "pi-os",
  "version": "1.0.0",
  "description": "PI Agent OS control plane integration for PI — dashboard widget, task management, memory, lifecycle, and policy.",
  "cockpit": "./cockpit/index.ts",
  "contextFileName": "PI.md",
  "hooks": {
    "BeforeTool": {
      "command": "python -m pi_agent_os.hooks.pi_hook",
      "_note": "Standalone hook for environments without the TypeScript cockpit"
    }
  },
  "lifecycleTools": {
    "_note": "When using the cockpit extension, these are native PI tools (pi_os_*). When using MCP-only, they are mcp__pi-os__* tools.",
    "onRunStart": "pi_os_start_run",
    "onHeartbeat": "pi_os_heartbeat",
    "onRunComplete": "pi_os_complete_run",
    "onRunBlocked": "pi_os_block_run",
    "onCoSDispatch": "pi_os_build_cos_context",
    "onStatusQuery": "pi_os_workspace_status"
  },
  "monitorPort": 4721,
  "controlApiBase": "http://127.0.0.1:4721/api/v1/control"
}
