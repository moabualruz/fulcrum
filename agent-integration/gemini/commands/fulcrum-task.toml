prompt = """
{{args}}

Current task board:
```
!{fulcrum task list --json 2>/dev/null}
```

If the user wants to list tasks, show the board above with id, title, status, and priority.
If the user provided a task title to create, run:
  `fulcrum task create --title "<title>" [--priority <level>] [--assigned-to <role>]`
  Then confirm with the returned task_id.
If the user wants to update a task status, run:
  `fulcrum task update --id <task_id> --status <status>`
"""
