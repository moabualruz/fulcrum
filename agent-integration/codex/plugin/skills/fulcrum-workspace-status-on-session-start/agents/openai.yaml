# Auto-emitted by Fulcrum agent-fanout (PR 6.5). Do not hand-edit.
# Canonical source: agent-integration/skills/workspace-status-on-session-start/SKILL.md
interface:
  display_name: Workspace Status On Session Start
  short_description: 'Fetch workspace status at the start of every session to see running agents, WIP budget, and blocked runs'
  brand_color: '#4F46E5'
dependencies:
  tools:
    - value: mcp__fulcrum__build_cos_context
      description: Fulcrum MCP tool (build_cos_context)
    - value: mcp__fulcrum__get_current_context
      description: Fulcrum MCP tool (get_current_context)
    - value: mcp__fulcrum__get_workspace_status
      description: Fulcrum MCP tool (get_workspace_status)
policy:
  allow_implicit_invocation: true
