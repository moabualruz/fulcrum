# Auto-emitted by Fulcrum agent-fanout (PR 6.5). Do not hand-edit.
# Canonical source: agent-integration/skills/block-when-stuck/SKILL.md
interface:
  display_name: Block When Stuck
  short_description: Call block_agent_run instead of guessing when you cannot proceed
  brand_color: '#4F46E5'
dependencies:
  tools:
    - value: mcp__fulcrum__block_agent_run
      description: Fulcrum MCP tool (block_agent_run)
policy:
  allow_implicit_invocation: false
