# Auto-emitted by Fulcrum agent-fanout (PR 6.5). Do not hand-edit.
# Canonical source: agent-integration/skills/session-start/SKILL.md
interface:
  display_name: Session Start
  short_description: Initialize a Fulcrum agent run at the start of every working session
  brand_color: '#4F46E5'
dependencies:
  tools:
    - value: mcp__fulcrum__get_current_context
      description: Fulcrum MCP tool (get_current_context)
    - value: mcp__fulcrum__get_workspace_status
      description: Fulcrum MCP tool (get_workspace_status)
    - value: mcp__fulcrum__recall_memory
      description: Fulcrum MCP tool (recall_memory)
    - value: mcp__fulcrum__start_agent_run
      description: Fulcrum MCP tool (start_agent_run)
policy:
  allow_implicit_invocation: true
