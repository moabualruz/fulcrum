# Auto-emitted by Fulcrum agent-fanout (PR 6.5). Do not hand-edit.
# Canonical source: agent-integration/skills/secret-hygiene/SKILL.md
interface:
  display_name: Secret Hygiene
  short_description: 'Never include credentials, API keys, tokens, passwords in tool inputs or memory writes'
  brand_color: '#4F46E5'
dependencies:
  tools:
    - value: mcp__fulcrum__block_agent_run
      description: Fulcrum MCP tool (block_agent_run)
policy:
  allow_implicit_invocation: true
