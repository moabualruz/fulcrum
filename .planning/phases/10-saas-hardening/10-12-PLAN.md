---
phase: 10-saas-hardening
plan: 12
type: execute
wave: 7
depends_on: [10-03, 10-04, 10-05, 10-10, 10-11]
requirements: [CLOSURE-03, CLOSURE-13, CLOSURE-14, CLOSURE-15, CLOSURE-16, CLOSURE-17]
autonomous: true
files_modified:
  - package.json
  - src/connectors/slack.ts
  - src/connectors/discord.ts
  - src/connectors/inbound-email.ts
  - src/connectors/external-trackers.ts
  - src/billing/stripe-adapter.ts
  - src/billing/entitlements.ts
  - src/api/rate-limit.ts
  - src/artifacts/attestation.ts
  - src/artifacts/preview.ts
  - src/repos/remote-cache.ts
  - src/observability/otel.ts
  - src/audit/siem-export.ts
  - tests/closure/integrations.test.ts
must_haves:
  truths:
    - "Decisions covered: D-37, D-40, D-46, D-47, D-48, D-49, D-50, D-55."
    - "Hosted integrations are gated adapters; local-first PGlite remains default."
    - "No external connector may claim enabled without config and health evidence."
---

<objective>
Close deferred integration and SaaS enterprise items: Linear/GitHub dispatch parity, Slack/Discord/email intake, notification workflow designer, artifact previews/attestations, hosted repo cache, API gateway/billing/quotas, observability/SIEM, theme builder/l10n, and encryption verification.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@.planning/phases/10-saas-hardening/10-RESEARCH-DEFERRED-CLOSURE.md
@src/connectors
@src/notifications
@src/artifacts
@src/repos
@src/api
@src/audit
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add connector adapters for intake and dispatch</name>
  <files>package.json, src/connectors/slack.ts, src/connectors/discord.ts, src/connectors/inbound-email.ts, src/connectors/external-trackers.ts, tests/closure/integrations.test.ts</files>
  <read_first>src/connectors, src/services/TaskService.ts, src/notifications, .planning/phases/03-symphony-sandcastle/03-CONTEXT.md</read_first>
  <action>
    Add `@slack/bolt@4.7.2`, `discord.js@14.26.4`, optional Cloudflare/Postmark inbound email normalizer, and external tracker dispatch adapter contract.
    Slack/Discord/email create tasks through `TaskService.createFromIntake()`.
    Linear/GitHub Issues must either support dispatch or return tested `DISPATCH_UNSUPPORTED` without claiming parity.
  </action>
  <verify>bun test tests/closure/integrations.test.ts</verify>
  <acceptance_criteria>
    - Slack signing-secret verification is tested.
    - Discord interaction verification is tested.
    - Inbound email normalization is tested.
    - External tracker dispatch capability is explicit per adapter.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add notification workflow designer and channels</name>
  <files>src/notifications, src/web/src/routes/notifications/workflows/+page.svelte, src/cli/commands/notifications.ts, src/tui/screens/notifications.ts, tests/closure/notification-workflows.test.ts</files>
  <read_first>src/notifications/rules.ts, src/notifications/delivery-worker.ts, .planning/phases/07-repos-artifacts-notifications/07-CONTEXT.md</read_first>
  <action>
    Add workflow designer CRUD and Slack/Discord/ntfy channels behind config gates.
    Channel health appears in Web/CLI/TUI/API.
  </action>
  <verify>bun test tests/closure/notification-workflows.test.ts</verify>
  <acceptance_criteria>
    - Disabled channel cannot send and reports missing config.
    - Workflow designer writes same rule model used by delivery workers.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add artifact, repo cache, billing, and observability adapters</name>
  <files>package.json, src/artifacts/attestation.ts, src/artifacts/preview.ts, src/repos/remote-cache.ts, src/billing/stripe-adapter.ts, src/billing/entitlements.ts, src/api/rate-limit.ts, src/observability/otel.ts, src/audit/siem-export.ts, tests/closure/enterprise-hardening.test.ts</files>
  <read_first>src/artifacts, src/repos, src/api, src/audit, .planning/phases/09-cross-cutting-testing/09-CONTEXT.md</read_first>
  <action>
    Add optional `sigstore@4.1.0` attestation verify/sign support, binary/media preview pipeline, hosted remote cache adapter, Stripe billing adapter, local entitlement/quota model, OTel API spans, JSON/CSV SIEM export, and vault encryption verification.
    Add `argon2@0.44.0` only if tests prove PBKDF2 scheme must migrate.
  </action>
  <verify>bun test tests/closure/enterprise-hardening.test.ts</verify>
  <acceptance_criteria>
    - Local mode works with no Stripe/Sigstore/OTel config.
    - API responses include rate-limit/quota headers where applicable.
    - SIEM export is deterministic JSON/CSV.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Close CLI framework and formatting decision</name>
  <files>package.json, src/cli/index.ts, src/cli/format.ts, tests/cli/formatting.test.ts</files>
  <read_first>src/cli/index.ts, .planning/phases/08-surface-delivery/08-CONTEXT.md</read_first>
  <action>
    Decide between incremental `commander@14.0.3` migration and keeping current parser with extracted formatting helpers.
    Add universal `--jq` and `--template` formatting after `--json` schema generation is stable.
    Do not rewrite CLI wholesale unless tests prove current parser blocks closure requirements.
  </action>
  <verify>bun test tests/cli/formatting.test.ts</verify>
  <acceptance_criteria>
    - Every closure CLI command supports `--json`.
    - `--jq` and `--template` work from JSON output.
    - Decision rationale is recorded in `10-UAT.md`.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Hosted adapters compromise local-first usage | All adapters disabled without config; local tests run with no external secrets. |
| Webhook spoofing creates tasks | Slack/Discord/email signature verification tests. |
| Billing entitlements bypassed by API keys | Quotas keyed by org/user/API key and surfaced in headers. |
</threat_model>

<verification>
Run `bun test tests/closure/integrations.test.ts tests/closure/notification-workflows.test.ts tests/closure/enterprise-hardening.test.ts tests/cli/formatting.test.ts`.
</verification>

<success_criteria>
CLOSURE-03, CLOSURE-13, CLOSURE-14, CLOSURE-15, CLOSURE-16, and CLOSURE-17 have gated, local-first implementations and parity evidence.
</success_criteria>
