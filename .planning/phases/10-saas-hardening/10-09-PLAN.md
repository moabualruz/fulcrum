---
phase: 10-saas-hardening
plan: 9
type: execute
wave: 4
depends_on: [10-03, 10-04, 10-05, 10-06, 10-07, 10-08]
requirements: [SAS-01, SAS-02, SAS-03, SAS-04, SAS-05, SAS-06]
autonomous: true
files_modified:
  - scripts/ci.ts
  - tests/platform/gate-regressions.test.ts
  - .planning/phases/10-saas-hardening/10-UAT.md
must_haves:
  truths:
    - "Decisions covered: D-04, D-33, D-34, D-35, D-36."
    - "scripts/ci.ts remains local CI source of truth."
    - "Phase 10 completion must record a green PostgreSQL integration run."
---

<objective>
Wire PostgreSQL integration and SaaS parity gates into CI, then record UAT evidence.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@scripts/ci.ts
@tests/platform/gate-regressions.test.ts
@tests/postgres
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add PostgreSQL integration CI stage</name>
  <files>scripts/ci.ts, tests/platform/gate-regressions.test.ts</files>
  <read_first>scripts/ci.ts, tests/platform/gate-regressions.test.ts, package.json</read_first>
  <action>
    Add CI stage named exactly `postgres:integration`.
    Stage runs `bun test tests/postgres` when `FULCRUM_TEST_DATABASE_URL` is set or Testcontainers/Docker probe passes.
    If unavailable, stage prints `PostgreSQL integration skipped: set FULCRUM_TEST_DATABASE_URL or start Docker` and marks skipped, not failed.
    Update gate regression test to assert `scripts/ci.ts` contains `postgres:integration`.
  </action>
  <verify>bun test tests/platform/gate-regressions.test.ts</verify>
  <acceptance_criteria>
    - `scripts/ci.ts` contains `postgres:integration`.
    - `scripts/ci.ts` contains `FULCRUM_TEST_DATABASE_URL`.
    - `tests/platform/gate-regressions.test.ts` asserts `postgres:integration`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run final verification and write UAT</name>
  <files>.planning/phases/10-saas-hardening/10-UAT.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/10-saas-hardening/10-CONTEXT.md, scripts/ci.ts</read_first>
  <action>
    Run:
    - `bun test tests/postgres`
    - `bun run ci`
    Write `.planning/phases/10-saas-hardening/10-UAT.md` with sections:
    - `## SAS-01 Multi-org isolation` with pass evidence
    - `## SAS-02 PostgreSQL pooling` with pass evidence
    - `## SAS-03 Injectable EventBus` with pass evidence
    - `## SAS-04 Org switching/member management` with pass evidence
    - `## SAS-05 Job queue coordination` with pass evidence
    - `## SAS-06 PostgreSQL integration tests` with command/date/status
    If PostgreSQL tests skip because no Docker/URL, do not mark SAS-06 complete; record blocker.
  </action>
  <verify>bun run ci</verify>
  <acceptance_criteria>
    - `10-UAT.md` contains `SAS-01 Multi-org isolation`.
    - `10-UAT.md` contains `SAS-06 PostgreSQL integration tests`.
    - `10-UAT.md` contains `bun run ci`.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| SaaS hardening silently skipped in local CI | Named `postgres:integration` stage and gate regression. |
| Phase marked complete without real PostgreSQL | UAT refuses SAS-06 completion on skip. |
</threat_model>

<verification>
Run `bun test tests/platform/gate-regressions.test.ts`, `bun test tests/postgres`, and `bun run ci`.
</verification>

<success_criteria>
Phase 10 has CI-visible PostgreSQL integration and UAT evidence for SAS-01..06.
</success_criteria>
