---
phase: 10-saas-hardening
plan: 3
type: execute
wave: 2
depends_on: [10-01, 10-02]
requirements: [SAS-01, SAS-06]
autonomous: true
files_modified:
  - src/db/tenant-scope.ts
  - src/db/migrations/Migration20260506000000_TenantRls.ts
  - src/db/repositories/tenant-audit.ts
  - tests/postgres/tenant-isolation.test.ts
must_haves:
  truths:
    - "Decisions covered: D-08, D-09, D-10, D-11, D-12."
    - "Shared-schema org_id tenancy remains v1 SaaS model."
    - "RLS uses transaction-local tenant identity where feasible."
---

<objective>
Prove zero cross-org leakage with application scoping plus PostgreSQL RLS or explicit exceptions.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@.planning/phases/10-saas-hardening/10-RESEARCH-INTEGRATION.md
@src/db/mikro-orm.config.ts
@src/trpc/context.ts
@tests/postgres/tenant-isolation.test.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add tenant scope helper for PostgreSQL requests</name>
  <files>src/db/tenant-scope.ts, tests/postgres/tenant-isolation.test.ts</files>
  <read_first>src/trpc/middleware.ts, src/db/mikro-orm.config.ts, tests/postgres/tenant-isolation.test.ts</read_first>
  <action>
    Create `src/db/tenant-scope.ts` exporting `withTenantScope(em, orgId, fn)`.
    In PostgreSQL mode, it must run `SET LOCAL fulcrum.org_id = '<orgId>'` inside the same transaction as `fn`.
    In PGlite mode, it must call `fn` without `SET LOCAL`.
    Reject missing or non-UUID org IDs with error message `Tenant scope requires orgId`.
  </action>
  <verify>bun test tests/postgres/tenant-isolation.test.ts</verify>
  <acceptance_criteria>
    - `src/db/tenant-scope.ts` contains `SET LOCAL fulcrum.org_id`.
    - `src/db/tenant-scope.ts` contains `Tenant scope requires orgId`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add RLS migration and exception audit</name>
  <files>src/db/migrations/Migration20260506000000_TenantRls.ts, src/db/repositories/tenant-audit.ts, tests/postgres/tenant-isolation.test.ts</files>
  <read_first>src/db/migrations, src/db/entities, .planning/phases/10-saas-hardening/10-RESEARCH-INTEGRATION.md</read_first>
  <action>
    Add migration enabling RLS and FORCE RLS where feasible for tenant-owned tables listed in `10-RESEARCH-INTEGRATION.md`.
    Policies use `org_id = current_setting('fulcrum.org_id', true)::uuid` with `USING` and `WITH CHECK`.
    Create `src/db/repositories/tenant-audit.ts` exporting a table list with status `rls` or `application_scope_exception`.
    Every exception must include reason string and test file path.
  </action>
  <verify>bun test tests/postgres/tenant-isolation.test.ts tests/db/migration-downgrade.test.ts</verify>
  <acceptance_criteria>
    - Migration file contains `ENABLE ROW LEVEL SECURITY`.
    - Migration file contains `FORCE ROW LEVEL SECURITY`.
    - `src/db/repositories/tenant-audit.ts` contains `application_scope_exception`.
    - `tests/postgres/tenant-isolation.test.ts` checks org A cannot read org B task and document rows.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Cross-org data leak through missed `orgId` filter | RLS default-deny plus integration matrix. |
| PgBouncer transaction pooling drops session tenant state | Use `SET LOCAL` inside transaction, not session-level `SET`. |
</threat_model>

<verification>
Run `bun test tests/postgres/tenant-isolation.test.ts tests/db/migration-downgrade.test.ts`.
</verification>

<success_criteria>
SAS-01 passes cross-org negative tests on real PostgreSQL with documented RLS coverage.
</success_criteria>
