---
phase: 10-saas-hardening
plan: 15
type: execute
wave: 8
depends_on: [10-03, 10-04, 10-05, 10-14]
requirements: [CLOSURE-15, CLOSURE-16, CLOSURE-17]
autonomous: true
files_modified:
  - package.json
  - src/artifacts/attestation.ts
  - src/artifacts/preview.ts
  - src/repos/remote-cache.ts
  - src/billing/stripe-adapter.ts
  - src/billing/entitlements.ts
  - src/api/rate-limit.ts
  - src/observability/otel.ts
  - src/audit/siem-export.ts
  - src/security/encryption-verification.ts
  - tests/closure/enterprise-hardening.test.ts
must_haves:
  truths:
    - "Decisions covered: D-37, D-40, D-49, D-50, D-55."
    - "Hosted integrations are gated adapters; local-first PGlite remains default."
    - "Billing, hosted cache, Sigstore, OTel, and SIEM features must work disabled without secrets."
---

<objective>
Close artifact/repo/billing/observability deferred items: previews, Sigstore attestation, hosted repo cache, API gateway/billing/quotas, OTel/SIEM, theme/l10n gates, and encryption verification.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@.planning/phases/10-saas-hardening/10-RESEARCH-DEFERRED-CLOSURE.md
@.planning/phases/07-repos-artifacts-notifications/07-CONTEXT.md
@.planning/phases/09-cross-cutting-testing/09-CONTEXT.md
@src/artifacts
@src/repos
@src/api
@src/audit
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add artifact preview and attestation adapters</name>
  <files>package.json, src/artifacts/attestation.ts, src/artifacts/preview.ts, tests/closure/enterprise-hardening.test.ts</files>
  <read_first>src/artifacts, .planning/phases/07-repos-artifacts-notifications/07-CONTEXT.md</read_first>
  <action>
    Add general binary/media preview pipeline and optional `sigstore@4.1.0` attestation verify/sign support.
    Disabled Sigstore mode must expose capability status without failing local-first artifact flows.
  </action>
  <verify>bun test tests/closure/enterprise-hardening.test.ts</verify>
  <acceptance_criteria>
    - Local mode previews supported files without external services.
    - Sigstore enabled status requires config/health evidence.
    - Attestation payloads are tenant-scoped and audited.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add hosted cache, billing, quotas, and API gateway limits</name>
  <files>src/repos/remote-cache.ts, src/billing/stripe-adapter.ts, src/billing/entitlements.ts, src/api/rate-limit.ts, tests/closure/enterprise-hardening.test.ts</files>
  <read_first>src/repos, src/api, src/auth, .planning/phases/09-cross-cutting-testing/09-CONTEXT.md</read_first>
  <action>
    Add gated hosted repo cache adapter, Stripe billing adapter, local entitlement/quota model, and API rate-limit headers.
    Billing and quotas are keyed by org/user/API key and cannot bypass org membership checks.
  </action>
  <verify>bun test tests/closure/enterprise-hardening.test.ts</verify>
  <acceptance_criteria>
    - Local mode works with no Stripe config.
    - API responses include rate-limit/quota headers where applicable.
    - Quota enforcement tests include org/user/API key boundaries.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add observability, SIEM, theme/l10n, and encryption verification</name>
  <files>src/observability/otel.ts, src/audit/siem-export.ts, src/security/encryption-verification.ts, tests/closure/enterprise-hardening.test.ts</files>
  <read_first>src/audit, src/security, src/theme, .planning/phases/09-cross-cutting-testing/09-CONTEXT.md</read_first>
  <action>
    Add optional OTel spans, deterministic JSON/CSV SIEM export, representative l10n/theme coverage gates, and vault encryption verification.
    Add `argon2@0.44.0` only if tests prove PBKDF2 scheme must migrate.
  </action>
  <verify>bun test tests/closure/enterprise-hardening.test.ts</verify>
  <acceptance_criteria>
    - OTel is disabled by default and never required in local mode.
    - SIEM export is deterministic JSON/CSV.
    - Encryption verification records algorithm/status without exposing secrets.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add enterprise parity rows</name>
  <files>src/web/src/routes, src/cli/commands, src/tui/screens, tests/closure/product-surface-parity.test.ts</files>
  <read_first>.planning/phases/10-saas-hardening/10-UI-SPEC.md, src/web/src/routes, src/cli/commands, src/tui/screens</read_first>
  <action>
    Add Web/CLI/TUI/API parity rows for CLOSURE-15, hosted/billing/API-gateway portion of CLOSURE-16, and CLOSURE-17.
    Status screens/commands must show disabled, configured, unhealthy, and healthy states.
  </action>
  <verify>bun test tests/closure/product-surface-parity.test.ts</verify>
  <acceptance_criteria>
    - Parity matrix covers CLOSURE-15, CLOSURE-16 enterprise items, and CLOSURE-17.
    - No external adapter claims enabled without health evidence.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Hosted adapters compromise local-first usage | All adapters disabled without config; tests run with no external secrets. |
| Billing entitlements bypassed by API keys | Quotas keyed by org/user/API key and surfaced in headers. |
| Observability or SIEM leaks secrets | Redaction tests and deterministic export schemas. |
</threat_model>

<verification>
Run `bun test tests/closure/enterprise-hardening.test.ts tests/closure/product-surface-parity.test.ts`.
</verification>

<success_criteria>
CLOSURE-15, hosted/billing/API-gateway portion of CLOSURE-16, and CLOSURE-17 have gated local-first implementations and parity evidence.
</success_criteria>
