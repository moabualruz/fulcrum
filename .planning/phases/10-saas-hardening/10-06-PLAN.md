---
phase: 10-saas-hardening
plan: 6
type: execute
wave: 3
depends_on: [10-02, 10-03]
requirements: [SAS-01, SAS-04]
autonomous: true
files_modified:
  - src/web/src/routes/settings/organization/+page.server.ts
  - src/web/src/routes/settings/organization/+page.svelte
  - src/web/src/lib/components/org/OrgSwitcher.svelte
  - src/web/tests/e2e/organization.spec.ts
  - src/web/tests/a11y/organization.spec.ts
must_haves:
  truths:
    - "Decisions covered: D-04, D-05, D-06, D-16, D-17."
    - "Web is full admin UX for SaaS org management."
---

<objective>
Expose organization switching and member management in Web settings with accessibility coverage.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@src/web/src/routes/settings
@src/server/trpc/routers/orgs.ts
@src/web/tests/e2e
@src/web/tests/a11y
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Build Web organization settings surface</name>
  <files>src/web/src/routes/settings/organization/+page.server.ts, src/web/src/routes/settings/organization/+page.svelte, src/web/src/lib/components/org/OrgSwitcher.svelte</files>
  <read_first>src/web/src/routes/settings/theme/+page.svelte, src/web/src/routes/settings/notifications/+page.svelte, src/server/trpc/routers/orgs.ts</read_first>
  <action>
    Add organization settings route with:
    - active org switcher showing org name, slug, role, `active` marker
    - member table with email/name/role/joined date
    - role update action for owner/admin
    - add/invite member form
    - remove member action
    - empty state text `No other members`
    All server actions must call `orgs` tRPC/router path; no direct SQL.
  </action>
  <verify>cd src/web && bun test src/routes/settings/organization/+page.svelte.test.ts</verify>
  <acceptance_criteria>
    - `+page.svelte` contains `No other members`.
    - `+page.svelte` contains `owner`, `admin`, and `member`.
    - `+page.server.ts` imports no `src/db/entities`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Web e2e and a11y tests</name>
  <files>src/web/tests/e2e/organization.spec.ts, src/web/tests/a11y/organization.spec.ts</files>
  <read_first>src/web/tests/e2e/settings.spec.ts, src/web/tests/a11y/settings.spec.ts</read_first>
  <action>
    Add Playwright journey:
    - open organization settings
    - switch active org
    - verify active org label changes
    - update member role
    - reject non-member org switch with visible error text `Organization membership required`
    Add axe a11y test for organization settings page.
  </action>
  <verify>cd src/web && bun run web:e2e -- organization.spec.ts && bun run web:a11y -- organization.spec.ts</verify>
  <acceptance_criteria>
    - `organization.spec.ts` contains `Organization membership required`.
    - a11y test imports `@axe-core/playwright`.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Org admin UI bypasses shared authorization | Server actions call tRPC org router only. |
| Role controls inaccessible by keyboard/screen reader | Playwright and axe tests cover settings page. |
</threat_model>

<verification>
Run web organization unit/e2e/a11y tests.
</verification>

<success_criteria>
Web has complete SAS org management parity through shared backend paths.
</success_criteria>
