---
phase: 10-saas-hardening
plan: 0
type: tdd
wave: 0
depends_on: []
requirements: [SAS-01, SAS-02, SAS-03, SAS-04, SAS-05, SAS-06]
autonomous: true
files_modified:
  - tests/postgres/test-utils.ts
  - tests/postgres/testcontainers-smoke.test.ts
  - tests/postgres/tenant-isolation.test.ts
  - tests/postgres/auth-orgs.test.ts
  - tests/postgres/pool.test.ts
  - tests/postgres/event-bus.test.ts
  - tests/postgres/graphile-worker.test.ts
  - tests/postgres/migrations.test.ts
  - package.json
must_haves:
  truths:
    - "Decisions covered: D-01, D-02, D-03, D-08, D-11, D-22, D-26, D-32, D-33, D-34, D-35, D-36."
    - "PostgreSQL integration tests use @testcontainers/postgresql@11.6.0 or FULCRUM_TEST_DATABASE_URL."
    - "PGlite-only tests do not satisfy Phase 10 completion."
    - "Wave 0 verifies Bun can start @testcontainers/postgresql before later plans depend on it."
---

<objective>
Create RED PostgreSQL integration tests and shared test harness before SaaS implementation.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@.planning/phases/10-saas-hardening/10-RESEARCH.md
@.planning/phases/10-saas-hardening/10-RESEARCH-INTEGRATION.md
@src/test-utils/db.ts
@tests/db/migrator-service.test.ts
@tests/api/rest-parity.test.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add PostgreSQL test harness</name>
  <files>tests/postgres/test-utils.ts, package.json</files>
  <read_first>package.json, src/test-utils/db.ts, tests/db/migrator-service.test.ts, .planning/phases/10-saas-hardening/10-RESEARCH-DEPENDENCIES.md</read_first>
  <action>
    Add dev dependencies `@testcontainers/postgresql@11.6.0` and `testcontainers@11.6.0`.
    Create `tests/postgres/test-utils.ts` exporting `withPostgresTestDb`, `createPostgresOrm`, `seedTwoOrgs`, and `expectNoCrossOrgLeak`.
    `withPostgresTestDb` must prefer `FULCRUM_TEST_DATABASE_URL`; if absent and Docker/Testcontainers unavailable, it must return a typed skip reason string containing `PostgreSQL integration skipped`.
    `createPostgresOrm` must call `MikroORM.init(createOrmConfig())` with a real PostgreSQL connection string and run migrations/schema setup through existing migrator path.
    Add `tests/postgres/testcontainers-smoke.test.ts` proving Bun can import `@testcontainers/postgresql`, start or skip with the typed skip reason, and expose a PostgreSQL URL.
  </action>
  <verify>bun test tests/postgres/testcontainers-smoke.test.ts tests/postgres/test-utils.ts</verify>
  <acceptance_criteria>
    - `package.json` contains `"@testcontainers/postgresql": "11.6.0"`.
    - `tests/postgres/test-utils.ts` contains `FULCRUM_TEST_DATABASE_URL`.
    - `tests/postgres/test-utils.ts` exports `seedTwoOrgs`.
    - `tests/postgres/test-utils.ts` contains the string `PostgreSQL integration skipped`.
    - `tests/postgres/testcontainers-smoke.test.ts` contains `PostgreSqlContainer`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED integration coverage for SAS-01..06</name>
  <files>tests/postgres/tenant-isolation.test.ts, tests/postgres/auth-orgs.test.ts, tests/postgres/pool.test.ts, tests/postgres/event-bus.test.ts, tests/postgres/graphile-worker.test.ts, tests/postgres/migrations.test.ts</files>
  <read_first>tests/api/rest-parity.test.ts, tests/auth/saas-auth.test.ts, tests/subscriptions/event-bus.test.ts, tests/workers/registry.test.ts, tests/db/migration-downgrade.test.ts</read_first>
  <action>
    Create RED tests:
    - `tenant-isolation.test.ts`: org A cannot list/get/update/delete org B records for tasks, docs, memory, runs, repos, artifacts, notifications, audit/events, credentials, tenant settings, and search.
    - `auth-orgs.test.ts`: user can list orgs, switch active org, list members, add member, update role, remove member; non-member switch returns forbidden.
    - `pool.test.ts`: `FULCRUM_DB_POOL_MAX=2` caps concurrent PostgreSQL clients and exposes waiting/timeout behavior.
    - `event-bus.test.ts`: two EventBus instances sharing PostgreSQL deliver `org.<id>.notifications` across instances.
    - `graphile-worker.test.ts`: two workers process one deduped job once using same `jobKey`.
    - `migrations.test.ts`: migrate up, migrate down one step, migrate up again against PostgreSQL.
  </action>
  <verify>bun test tests/postgres</verify>
  <acceptance_criteria>
    - `tests/postgres/tenant-isolation.test.ts` contains `ORG_A` and `ORG_B`.
    - `tests/postgres/auth-orgs.test.ts` contains `switch active org`.
    - `tests/postgres/pool.test.ts` contains `FULCRUM_DB_POOL_MAX`.
    - `tests/postgres/event-bus.test.ts` contains `org.${`.
    - `tests/postgres/graphile-worker.test.ts` contains `jobKey`.
    - `tests/postgres/migrations.test.ts` contains `down`.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| False-positive SaaS completion from PGlite-only tests | Real PostgreSQL tests are created first and plugged into CI later. |
| Testcontainers unavailable locally | URL fallback and explicit skip reason preserve deterministic developer workflow. |
</threat_model>

<verification>
Run `bun test tests/postgres`. Expected RED evidence is acceptable only when failures are behavior assertions for missing SaaS implementation; harness import/startup failures must be fixed in Wave 0 before later plans run.
</verification>

<success_criteria>
RED PostgreSQL tests exist for SAS-01..06 and fail for missing implementation rather than missing harness.
</success_criteria>
