---
phase: 10-saas-hardening
plan: 4
type: execute
wave: 2
depends_on: [10-01]
requirements: [SAS-03, SAS-06]
autonomous: true
files_modified:
  - src/subscriptions/event-bus.ts
  - src/subscriptions/postgres-event-bus.ts
  - src/subscriptions/index.ts
  - src/subscriptions/procedures.ts
  - src/db/db.module.ts
  - tests/subscriptions/event-bus.test.ts
  - tests/postgres/event-bus.test.ts
must_haves:
  truths:
    - "Decisions covered: D-23, D-24, D-25, D-26, D-27."
    - "Topic names remain stable."
    - "Required adapters: in-process and PostgreSQL NOTIFY."
---

<objective>
Make EventBus injectable and prove cross-instance PostgreSQL NOTIFY delivery.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@src/subscriptions/event-bus.ts
@src/subscriptions/pglite-bridge.ts
@src/subscriptions/procedures.ts
@src/db/db.module.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Introduce EventBus port and DI token</name>
  <files>src/subscriptions/event-bus.ts, src/subscriptions/index.ts, src/db/db.module.ts, tests/subscriptions/event-bus.test.ts</files>
  <read_first>src/subscriptions/event-bus.ts, src/db/db.module.ts, tests/subscriptions/event-bus.test.ts</read_first>
  <action>
    Refactor `EventBus` into an interface-compatible port with concrete `InProcessEventBus`.
    Export `EVENT_BUS_TOKEN` from `src/subscriptions/event-bus.ts` or `src/db/db.module.ts`.
    Keep `getEventBus()` as compatibility shim returning the process singleton `InProcessEventBus`.
    Register `EVENT_BUS_TOKEN` in `registerDbBindings` unless caller supplies an override.
  </action>
  <verify>bun test tests/subscriptions/event-bus.test.ts</verify>
  <acceptance_criteria>
    - `src/subscriptions/event-bus.ts` contains `InProcessEventBus`.
    - `src/subscriptions/event-bus.ts` contains `EVENT_BUS_TOKEN`.
    - `src/db/db.module.ts` binds `EVENT_BUS_TOKEN`.
    - `getEventBus` still exists.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add PostgreSQL NOTIFY adapter and wire subscriptions</name>
  <files>src/subscriptions/postgres-event-bus.ts, src/subscriptions/procedures.ts, src/subscriptions/index.ts, tests/postgres/event-bus.test.ts</files>
  <read_first>src/subscriptions/pglite-bridge.ts, src/subscriptions/procedures.ts, tests/postgres/event-bus.test.ts</read_first>
  <action>
    Create `PostgresNotifyEventBus` implementing publish/subscribe/listenerCount/removeAllListeners.
    Publish must use `NOTIFY` with JSON payload `{ "topic": "...", "data": ... }`.
    Subscribe must LISTEN on mapped channels and filter by topic.
    Listener connections must use a dedicated long-lived `pg.Client` or equivalent raw PostgreSQL client outside MikroORM's request pool. Do not run LISTEN through transaction-pooled MikroORM/PgBouncer paths.
    Implement reconnect/backoff, payload size guard, and per-topic backpressure/drop metrics for slow consumers.
    Update `src/subscriptions/procedures.ts` to prefer `ctx.container.get(EVENT_BUS_TOKEN)` and fall back to `getEventBus()`.
    The cross-instance test must create bus A and bus B on separate PostgreSQL connections and, where the harness allows, separate Node processes; bus B receives payload published by bus A on `org.<orgId>.notifications`.
  </action>
  <verify>bun test tests/postgres/event-bus.test.ts tests/subscriptions/procedures.test.ts</verify>
  <acceptance_criteria>
    - `src/subscriptions/postgres-event-bus.ts` contains `LISTEN`.
    - `src/subscriptions/postgres-event-bus.ts` contains `NOTIFY`.
    - `src/subscriptions/postgres-event-bus.ts` contains a dedicated listener client, not only MikroORM EntityManager calls.
    - `src/subscriptions/postgres-event-bus.ts` contains reconnect/backoff handling.
    - `src/subscriptions/procedures.ts` contains `EVENT_BUS_TOKEN`.
    - `tests/postgres/event-bus.test.ts` contains `busA` and `busB`.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Multi-instance notifications vanish | PostgreSQL NOTIFY adapter tested with two bus instances. |
| Local-first subscriptions regress | Compatibility `getEventBus()` and existing tests remain. |
</threat_model>

<verification>
Run `bun test tests/postgres/event-bus.test.ts tests/subscriptions/event-bus.test.ts tests/subscriptions/procedures.test.ts`.
</verification>

<success_criteria>
SAS-03 EventBus is injectable and cross-instance delivery works on PostgreSQL.
</success_criteria>
