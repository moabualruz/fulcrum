---
phase: 10-saas-hardening
plan: 16
type: execute
wave: 8
depends_on: [10-07, 10-11, 10-12, 10-13, 10-14, 10-15]
requirements: [CLOSURE-16]
autonomous: true
files_modified:
  - package.json
  - src/cli/index.ts
  - src/cli/format.ts
  - tests/cli/formatting.test.ts
  - tests/closure/product-surface-parity.test.ts
must_haves:
  truths:
    - "Decisions covered: D-37, D-55."
    - "Do not rewrite CLI wholesale unless tests prove current parser blocks closure requirements."
    - "Universal `--jq` and `--template` operate on stable JSON output."
---

<objective>
Close CLI framework/formatting deferred item with a measured parser decision and universal JSON-derived formatting.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@.planning/phases/08-surface-delivery/08-CONTEXT.md
@src/cli/index.ts
@src/cli/commands
@tests/cli
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add parser decision test and rationale</name>
  <files>package.json, src/cli/index.ts, tests/cli/formatting.test.ts</files>
  <read_first>src/cli/index.ts, src/cli/commands, tests/cli</read_first>
  <action>
    Decide between incremental `commander@14.0.3` migration and keeping current parser with extracted formatting helpers.
    Prefer keeping current parser unless tests prove nested commands/help/flag parsing block closure requirements.
    If commander is added, pin `"commander": "14.0.3"` and migrate incrementally command group by command group.
  </action>
  <verify>bun test tests/cli/formatting.test.ts</verify>
  <acceptance_criteria>
    - Decision rationale is test-backed and recorded in source comments or test names, not UAT-only prose.
    - No command loses existing `--json` behavior.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add universal JSON-derived formatting</name>
  <files>src/cli/format.ts, src/cli/index.ts, tests/cli/formatting.test.ts</files>
  <read_first>src/cli, tests/cli</read_first>
  <action>
    Add universal `--jq` and `--template` support after command handlers produce stable JSON.
    `--jq` filters JSON output; `--template` renders from JSON with explicit missing-key behavior.
  </action>
  <verify>bun test tests/cli/formatting.test.ts</verify>
  <acceptance_criteria>
    - `--jq` and `--template` work from JSON output.
    - Missing template keys fail with typed error.
    - Formatting helpers are shared by closure CLI commands.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify closure CLI parity</name>
  <files>tests/closure/product-surface-parity.test.ts, tests/cli/formatting.test.ts</files>
  <read_first>src/cli/commands, .planning/phases/10-saas-hardening/10-UI-SPEC.md</read_first>
  <action>
    Assert every closure CLI command supports default readable output, `--json`, `--jq`, `--template`, typed exit codes, and non-interactive automation flags for automation-critical actions.
  </action>
  <verify>bun test tests/closure/product-surface-parity.test.ts tests/cli/formatting.test.ts</verify>
  <acceptance_criteria>
    - Every closure CLI command supports `--json`.
    - Automation-critical actions have non-interactive flags.
    - No command is interactive-only unless explicitly read-only.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Parser rewrite destabilizes all CLI commands | Test-backed incremental decision, no wholesale rewrite by default. |
| Formatting features parse human output | `--jq`/`--template` operate only on JSON. |
| CLI falls behind Web/TUI closure surfaces | Product parity test covers every closure command. |
</threat_model>

<verification>
Run `bun test tests/cli/formatting.test.ts tests/closure/product-surface-parity.test.ts`.
</verification>

<success_criteria>
CLOSURE-16 CLI framework/formatting item is closed with stable JSON-derived formatting and no unsupported parser rewrite.
</success_criteria>
