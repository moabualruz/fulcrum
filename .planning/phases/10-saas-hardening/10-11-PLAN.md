---
phase: 10-saas-hardening
plan: 11
type: execute
wave: 6
depends_on: [10-03, 10-06, 10-07, 10-08, 10-10]
requirements: [CLOSURE-06, CLOSURE-07]
autonomous: true
files_modified:
  - src/db/entities/docs/DocumentVersionTag.ts
  - src/services/DocumentVersionTagService.ts
  - src/search/meilisearch-adapter.ts
  - src/search/ai-qa.ts
  - src/server/trpc/routers/docs.ts
  - src/server/trpc/routers/search.ts
  - tests/closure/docs-search.test.ts
  - tests/search/meilisearch-adapter.test.ts
  - tests/search/ai-qa.test.ts
must_haves:
  truths:
    - "Decisions covered: D-37, D-44, D-45, D-55."
    - "Optional Meilisearch and AI Q&A do not replace deterministic PGlite/Orama defaults."
    - "Named document version tags and search/Q&A must have Web/CLI/TUI/API parity rows."
---

<objective>
Close Phase 6 deferred docs/search items: named document version tags, optional Meilisearch backend, and deterministic AI Q&A.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@.planning/phases/10-saas-hardening/10-RESEARCH-DEFERRED-CLOSURE.md
@.planning/phases/06-documents-memory-search/06-CONTEXT.md
@src/search
@src/server/trpc/routers
@src/web/src/routes
@src/cli/commands
@src/tui/screens
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add named document version tags</name>
  <files>src/db/entities/docs/DocumentVersionTag.ts, src/services/DocumentVersionTagService.ts, src/server/trpc/routers/docs.ts, tests/closure/docs-search.test.ts</files>
  <read_first>src/db/entities/docs, src/services/DocumentService.ts, src/server/trpc/routers/docs.ts, .planning/phases/06-documents-memory-search/06-CONTEXT.md</read_first>
  <action>
    Add tenant-scoped named tags for document versions, matching git tag UX: create tag, list tags, resolve tag to immutable version, move/delete tag with audit event.
    Tags must be unique per `org_id + document_id + name` and covered by Phase 10 tenant isolation audit.
  </action>
  <verify>bun test tests/closure/docs-search.test.ts tests/postgres/tenant-isolation.test.ts</verify>
  <acceptance_criteria>
    - New tag table/entity includes `org_id`.
    - Tag create/list/resolve/move/delete use shared service/router paths.
    - Tenant isolation test covers cross-org tag read and mutation denial.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add optional Meilisearch adapter</name>
  <files>package.json, src/search/meilisearch-adapter.ts, tests/search/meilisearch-adapter.test.ts</files>
  <read_first>src/search, .planning/phases/06-documents-memory-search/06-CONTEXT.md</read_first>
  <action>
    Add optional `meilisearch@0.58.0` adapter behind config and health checks.
    Keep PGlite/Orama default. Disabled mode must return deterministic capability status without network calls.
  </action>
  <verify>bun test tests/search/meilisearch-adapter.test.ts</verify>
  <acceptance_criteria>
    - Tests pass with Meilisearch disabled.
    - Adapter reports enabled only with URL/key config and health evidence.
    - No existing search path depends on Meilisearch.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add deterministic AI Q&A</name>
  <files>src/search/ai-qa.ts, src/server/trpc/routers/search.ts, tests/search/ai-qa.test.ts</files>
  <read_first>src/search, src/memory, src/server/trpc/routers/search.ts</read_first>
  <action>
    Add Q&A orchestration that retrieves deterministic source refs first and returns `insufficient_context` unless cited snippets support the answer.
    No embeddings/RAG dependency is allowed without explicit config. Disabled mode must be useful and non-hallucinating.
  </action>
  <verify>bun test tests/search/ai-qa.test.ts</verify>
  <acceptance_criteria>
    - Q&A output requires source refs or returns `insufficient_context`.
    - Tests cover disabled/unconfigured mode.
    - Router response schema has explicit Zod objects, no `z.any()`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add docs/search parity rows</name>
  <files>tests/closure/product-surface-parity.test.ts, src/web/src/routes, src/cli/commands, src/tui/screens</files>
  <read_first>.planning/phases/10-saas-hardening/10-UI-SPEC.md, src/web/src/routes, src/cli/commands, src/tui/screens</read_first>
  <action>
    Add Web/CLI/TUI/API parity rows for CLOSURE-06 and CLOSURE-07.
    CLI commands support `--json`; TUI screens expose `renderPlain`; Web routes expose loading/error/empty states.
  </action>
  <verify>bun test tests/closure/product-surface-parity.test.ts</verify>
  <acceptance_criteria>
    - Parity matrix enumerates CLOSURE-06 and CLOSURE-07 across Web/CLI/TUI/API.
    - No surface imports DB entities directly for these features.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Optional Meilisearch becomes required | Disabled-mode tests and config gate. |
| Q&A hallucinates from weak retrieval | Source-ref requirement and `insufficient_context` path. |
| Version tags leak across tenants | `org_id` uniqueness and PostgreSQL isolation tests. |
</threat_model>

<verification>
Run `bun test tests/closure/docs-search.test.ts tests/search/meilisearch-adapter.test.ts tests/search/ai-qa.test.ts tests/closure/product-surface-parity.test.ts`.
</verification>

<success_criteria>
CLOSURE-06 and CLOSURE-07 are implemented through shared services with tenant isolation and Web/CLI/TUI/API parity.
</success_criteria>
