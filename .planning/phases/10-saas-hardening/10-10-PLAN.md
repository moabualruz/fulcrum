---
phase: 10-saas-hardening
plan: 10
type: execute
wave: 5
depends_on: [10-09]
requirements: [SAS-01, SAS-06, CLOSURE-01, CLOSURE-02, CLOSURE-04, CLOSURE-05]
autonomous: true
files_modified:
  - src/cli/commands/auth.ts
  - src/context/context-bundle-service.ts
  - src/repos/status.ts
  - src/server/trpc/routers/inference.ts
  - scripts/static-build-proof.ts
  - tests/cli/auth-session.test.ts
  - tests/context/repo-state-bundle.test.ts
  - tests/trpc/app-router-scaffold.test.ts
  - tests/platform/static-proof.test.ts
  - .planning/phases/10-saas-hardening/10-UAT.md
must_haves:
  truths:
    - "Decisions covered: D-37, D-38, D-39, D-41, D-42, D-43."
    - "No closure item remains only as source-phase deferred prose."
    - "Do not push or mutate main; only report merge readiness."
---

<objective>
Close foundational deferred items: BUG-17 repo hygiene, auth login/logout stubs, Linux static proof, public schema `z.any()`, and MEM-09 repo-state context bundle.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@.planning/phases/10-saas-hardening/10-RESEARCH-DEFERRED-CLOSURE.md
@src/cli/commands/auth.ts
@src/context
@src/repos
@src/server/trpc/routers/inference.ts
@scripts/static-build-proof.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Close auth session stubs</name>
  <files>src/cli/commands/auth.ts, tests/cli/auth-session.test.ts</files>
  <read_first>src/cli/commands/auth.ts, src/auth/index.ts, tests/cli/auth.test.ts</read_first>
  <action>
    Replace public interactive `login` and `logout` stubs with real session lifecycle behavior or remove the commands from help output if unsupported.
    `logout` must invalidate local CLI session state and return `{"ok":true}` under `--json`.
    `login` must either start a supported browser/device flow or fail with a typed actionable error and no stub wording.
  </action>
  <verify>bun test tests/cli/auth-session.test.ts</verify>
  <acceptance_criteria>
    - `src/cli/commands/auth.ts` contains no `stub`.
    - `fulcrum auth logout --json` test asserts session invalidation.
    - Unsupported login path exits non-zero with next-step copy.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire repo state into context bundle</name>
  <files>src/context/context-bundle-service.ts, src/repos/status.ts, tests/context/repo-state-bundle.test.ts</files>
  <read_first>src/context, src/repos, .planning/phases/06-documents-memory-search/06-CONTEXT.md, .planning/phases/07-repos-artifacts-notifications/07-CONTEXT.md</read_first>
  <action>
    Replace MEM-09 empty/placeholder `repoState` with real repo status from Phase 7 repo services.
    Include dirty state, branch, divergence, stale sync, last commit, open tasks, and sync health where available.
  </action>
  <verify>bun test tests/context/repo-state-bundle.test.ts</verify>
  <acceptance_criteria>
    - Context bundle contains non-empty `repoState` for seeded repo.
    - Missing repo degrades to explicit empty state with reason.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close static proof and public schema gaps</name>
  <files>scripts/static-build-proof.ts, src/server/trpc/routers/inference.ts, tests/platform/static-proof.test.ts, tests/trpc/app-router-scaffold.test.ts</files>
  <read_first>scripts/static-build-proof.ts, src/server/trpc/routers/inference.ts, tests/trpc/app-router-scaffold.test.ts</read_first>
  <action>
    Make Linux static proof produce Docker/native Linux evidence or a precise environment blocker for UAT.
    Replace public `z.any()` schemas in inference router with explicit Zod object/union/record schemas.
  </action>
  <verify>bun test tests/platform/static-proof.test.ts tests/trpc/app-router-scaffold.test.ts</verify>
  <acceptance_criteria>
    - Public schema gate passes without `z.any()` in inference router.
    - Static proof records platform, command, status, and blocker if Docker/Linux unavailable.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Record BUG-17 merge-readiness report</name>
  <files>.planning/phases/10-saas-hardening/10-UAT.md</files>
  <read_first>.planning/STATE.md, .planning/phases/02-bug-fixes-foundation/02-CONTEXT.md</read_first>
  <action>
    Run non-mutating repo hygiene checks: branch, status, diff stat, log against main, and release merge readiness notes.
    Do not push or merge `main`.
  </action>
  <verify>git status --short</verify>
  <acceptance_criteria>
    - `10-UAT.md` contains `BUG-17`.
    - Report states whether `dev/v1.0` is merge-ready.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Stub commands survive milestone closure | Source grep and behavior tests fail on `stub`/unimplemented auth surface. |
| Context bundles omit repo state after repo phase exists | MEM-09 test seeds repo state and asserts non-empty bundle slice. |
| Main branch mutated during closure | Plan only permits report; merge/push requires explicit user request. |
</threat_model>

<verification>
Run `bun test tests/cli/auth-session.test.ts tests/context/repo-state-bundle.test.ts tests/platform/static-proof.test.ts tests/trpc/app-router-scaffold.test.ts`.
</verification>

<success_criteria>
CLOSURE-01, CLOSURE-02, CLOSURE-04, and CLOSURE-05 have passing evidence or explicit environment blocker in UAT.
</success_criteria>
