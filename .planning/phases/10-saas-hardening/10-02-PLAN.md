---
phase: 10-saas-hardening
plan: 2
type: execute
wave: 1
depends_on: [10-00]
requirements: [SAS-01, SAS-04]
autonomous: true
files_modified:
  - src/auth/index.ts
  - src/auth/adapter.ts
  - src/server/trpc/routers/orgs.ts
  - src/trpc/router.ts
  - tests/auth/better-auth-integration.test.ts
  - tests/postgres/auth-orgs.test.ts
must_haves:
  truths:
    - "Decisions covered: D-13, D-14, D-15, D-16, D-17."
    - "Better Auth remains the org/member provider."
    - "Roles are organization-scoped: owner, admin, member."
---

<objective>
Make Better Auth organization switching and member management functional through shared tRPC/API logic.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@src/auth/index.ts
@src/auth/adapter.ts
@src/trpc/router.ts
@tests/auth/better-auth-integration.test.ts
@tests/postgres/auth-orgs.test.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add org router around Better Auth organization APIs</name>
  <files>src/server/trpc/routers/orgs.ts, src/trpc/router.ts, tests/postgres/auth-orgs.test.ts</files>
  <read_first>src/auth/index.ts, src/auth/adapter.ts, src/trpc/router.ts, tests/postgres/auth-orgs.test.ts</read_first>
  <action>
    Extend the existing `src/server/trpc/routers/orgs.ts` if present; create it only if absent. Preserve any existing exports/tests and mount shape.
    The router must expose procedures:
    - `list`: returns organizations where current user has `OrgMember`.
    - `switch`: input `{ organizationId?: string, organizationSlug?: string }`, rejects non-members with `FORBIDDEN`, updates active organization using Better Auth session path.
    - `members.list`: returns members for active org.
    - `members.add`: owner/admin only, role `owner|admin|member`.
    - `members.remove`: owner/admin only, cannot remove last owner.
    - `members.updateRole`: owner/admin only, cannot demote last owner.
    - `invitations.create`, `invitations.cancel`, `invitations.accept`.
    Mount router as `orgs` in `src/trpc/router.ts`.
  </action>
  <verify>bun test tests/postgres/auth-orgs.test.ts tests/auth/better-auth-integration.test.ts</verify>
  <acceptance_criteria>
    - `src/server/trpc/routers/orgs.ts` contains `switch`.
    - `src/server/trpc/routers/orgs.ts` contains `members`.
    - `src/server/trpc/routers/orgs.ts` contains `last owner`.
    - `src/trpc/router.ts` contains `orgs`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Emit audit events for org/member mutations</name>
  <files>src/server/trpc/routers/orgs.ts, tests/postgres/auth-orgs.test.ts</files>
  <read_first>src/platform/audit-events.ts, src/server/trpc/routers/audit.ts, tests/postgres/auth-orgs.test.ts</read_first>
  <action>
    Emit typed audit events with verbs:
    - `org.switch`
    - `org.member.add`
    - `org.member.remove`
    - `org.member.role.update`
    - `org.invitation.create`
    - `org.invitation.cancel`
    - `org.invitation.accept`
    Payload must include `orgId`, `actorUserId`, `subjectUserId` where applicable, and `role`; payload must not include password, token, OAuth token, or secret values.
  </action>
  <verify>bun test tests/postgres/auth-orgs.test.ts</verify>
  <acceptance_criteria>
    - `src/server/trpc/routers/orgs.ts` contains `org.member.role.update`.
    - `tests/postgres/auth-orgs.test.ts` asserts an audit event for role update.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keep Web/CLI/TUI invitation parity</name>
  <files>src/server/trpc/routers/orgs.ts, tests/postgres/auth-orgs.test.ts</files>
  <read_first>src/cli/commands, src/tui/screens, tests/postgres/auth-orgs.test.ts</read_first>
  <action>
    Ensure invitation create/cancel/accept behavior is reachable by shared router/caller paths so Phase 10 Web, CLI, and TUI parity plans do not fork org membership logic.
    Tests must assert invite acceptance switches or exposes active org state consistently.
  </action>
  <verify>bun test tests/postgres/auth-orgs.test.ts</verify>
  <acceptance_criteria>
    - Invitation acceptance cannot bypass membership or role validation.
    - Tests cover `invitations.create`, `invitations.cancel`, and `invitations.accept`.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| User switches to org without membership | Router checks `OrgMember` before changing active org. |
| Last owner removed or demoted | Member mutations reject last-owner changes. |
| Audit event leaks invite token or auth secret | Payload schema excludes tokens/secrets. |
</threat_model>

<verification>
Run `bun test tests/postgres/auth-orgs.test.ts tests/auth/better-auth-integration.test.ts`.
</verification>

<success_criteria>
SAS-04 org switching and member management work through shared tRPC logic and enforce org-scoped roles.
</success_criteria>
