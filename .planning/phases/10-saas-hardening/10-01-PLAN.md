---
phase: 10-saas-hardening
plan: 1
type: execute
wave: 1
depends_on: [10-00]
requirements: [SAS-02, SAS-06]
autonomous: true
files_modified:
  - src/config/database.ts
  - src/db/mikro-orm.config.ts
  - src/cli/doctor.ts
  - src/cli/commands/db.ts
  - tests/db/mikro-orm-config.test.ts
  - tests/postgres/pool.test.ts
must_haves:
  truths:
    - "Decisions covered: D-18, D-19, D-20, D-21, D-22."
    - "Use existing pg@8.20.0 and MikroORM pool config; no new DB client."
    - "PgBouncer is deployment guidance only, not a bundled dependency."
---

<objective>
Configure and verify PostgreSQL pooling for SaaS mode.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@.planning/phases/10-saas-hardening/10-RESEARCH-DEPENDENCIES.md
@src/config/database.ts
@src/db/mikro-orm.config.ts
@src/cli/doctor.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Parse pool config and pass it to MikroORM</name>
  <files>src/config/database.ts, src/db/mikro-orm.config.ts, tests/db/mikro-orm-config.test.ts</files>
  <read_first>src/config/database.ts, src/db/mikro-orm.config.ts, tests/db/mikro-orm-config.test.ts</read_first>
  <action>
    Add parsing for:
    - `FULCRUM_DB_POOL_MIN` default `0`
    - `FULCRUM_DB_POOL_MAX` default `10`
    - `FULCRUM_DB_IDLE_TIMEOUT_MS` default `30000`
    - `FULCRUM_DB_CONNECTION_TIMEOUT_MS` default `2000`
    - `FULCRUM_DB_MAX_LIFETIME_SECONDS` optional informational field
    Validate values as non-negative integers and fail closed with messages containing the env var name.
    In PostgreSQL mode, `createOrmConfig()` must include `pool: { min, max, idleTimeoutMillis, connectionTimeoutMillis }`.
    In PGlite mode, do not pass pool config to the PGlite dialect.
  </action>
  <verify>bun test tests/db/mikro-orm-config.test.ts</verify>
  <acceptance_criteria>
    - `src/config/database.ts` contains `FULCRUM_DB_POOL_MAX`.
    - `src/db/mikro-orm.config.ts` contains `pool:`.
    - `tests/db/mikro-orm-config.test.ts` asserts PostgreSQL pool max `2` from env.
    - `tests/db/mikro-orm-config.test.ts` asserts PGlite config has no `pool`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Expose pool status in doctor/db commands</name>
  <files>src/cli/doctor.ts, src/cli/commands/db.ts, tests/postgres/pool.test.ts</files>
  <read_first>src/cli/doctor.ts, src/cli/commands/db.ts, tests/postgres/pool.test.ts</read_first>
  <action>
    Add `fulcrum db pool --json` or extend existing `fulcrum db status --json` to return:
    `{"backend":"postgres","pool":{"min":0,"max":10,"idleTimeoutMillis":30000,"connectionTimeoutMillis":2000,"totalCount":number|null,"idleCount":number|null,"waitingCount":number|null}}`.
    Add a doctor check named `postgres-pool` that warns if `waitingCount > 0` or if configured pool values are invalid.
    The PostgreSQL pool test must run concurrent queries with `FULCRUM_DB_POOL_MAX=2` and assert the configured max is honored.
  </action>
  <verify>bun test tests/postgres/pool.test.ts src/cli/doctor.test.ts</verify>
  <acceptance_criteria>
    - `src/cli/doctor.ts` contains `postgres-pool`.
    - `src/cli/commands/db.ts` contains `"pool"`.
    - `tests/postgres/pool.test.ts` asserts `max` equals `2`.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Connection storm exhausts PostgreSQL | Conservative defaults, explicit env config, doctor visibility, load test. |
| Session-level features break behind PgBouncer transaction pooling | Plan forbids session-level tenant state for protected paths. |
</threat_model>

<verification>
Run `bun test tests/db/mikro-orm-config.test.ts tests/postgres/pool.test.ts src/cli/doctor.test.ts`.
</verification>

<success_criteria>
SAS-02 has configured pool values, runtime status, and real PostgreSQL verification.
</success_criteria>
