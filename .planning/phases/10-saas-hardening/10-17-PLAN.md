---
phase: 10-saas-hardening
plan: 17
type: execute
wave: 9
depends_on: [10-01, 10-03, 10-04, 10-05, 10-09, 10-15]
requirements: [SAS-01, SAS-02, SAS-03, SAS-05, SAS-06, CLOSURE-17]
autonomous: true
files_modified:
  - docs/operations.md
  - scripts/postgres-export.ts
  - scripts/postgres-import.ts
  - scripts/postgres-backup.ts
  - scripts/postgres-restore.ts
  - scripts/deployment-smoke.ts
  - tests/postgres/operations.test.ts
  - tests/platform/deployment-smoke.test.ts
must_haves:
  truths:
    - "Decisions covered: D-04, D-08, D-11, D-22, D-26, D-32, D-33, D-34, D-35, D-36."
    - "Phase 10 must include production/deployment/operations proof, not only feature tests."
    - "PGlite-to-PostgreSQL migration and PostgreSQL backup/restore paths are explicit."
---

<objective>
Add operational closure proof: deployment runbook, PGlite-to-PostgreSQL migration, backup/restore, migration locking, secret rotation, and cross-process smoke.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@scripts/ci.ts
@src/db
@src/subscriptions/postgres-event-bus.ts
@src/queue/graphile-worker-adapter.ts
@src/security
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add PGlite to PostgreSQL export/import path</name>
  <files>scripts/postgres-export.ts, scripts/postgres-import.ts, tests/postgres/operations.test.ts</files>
  <read_first>src/db, tests/db/migrator-service.test.ts, .planning/phases/10-saas-hardening/10-RESEARCH-INTEGRATION.md</read_first>
  <action>
    Add deterministic export/import commands or scripts for moving local PGlite data into PostgreSQL.
    Include schema version check, tenant table ordering, dry-run, and idempotency/retry behavior.
  </action>
  <verify>bun test tests/postgres/operations.test.ts</verify>
  <acceptance_criteria>
    - Export/import checks schema version before data movement.
    - Dry-run emits planned table order.
    - Tests cover at least one tenant-owned table and one global table.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add backup/restore and migration locking proof</name>
  <files>scripts/postgres-backup.ts, scripts/postgres-restore.ts, tests/postgres/operations.test.ts</files>
  <read_first>src/db/migrations, tests/db/migration-downgrade.test.ts</read_first>
  <action>
    Add PostgreSQL backup/restore scripts or command wrappers with explicit environment requirements.
    Add migration locking/concurrency proof so two processes cannot run conflicting migrations.
  </action>
  <verify>bun test tests/postgres/operations.test.ts tests/db/migration-downgrade.test.ts</verify>
  <acceptance_criteria>
    - Backup/restore commands expose `--dry-run` and JSON status.
    - Migration locking test starts two contenders and proves one lock owner.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add deployment smoke and runbook</name>
  <files>docs/operations.md, scripts/deployment-smoke.ts, tests/platform/deployment-smoke.test.ts</files>
  <read_first>README.md, docs, scripts/ci.ts, src/server</read_first>
  <action>
    Add deployment runbook covering env vars, reverse proxy/DNS assumptions, health checks, PostgreSQL URL, pool sizing, event bus listener requirements, worker startup, backups, restore drill, secret rotation, and rollback.
    Add smoke test that starts two app/worker subprocesses against one PostgreSQL URL and verifies health, NOTIFY delivery, and graphile-worker coordination.
    If subprocess launch is impossible in the environment, record an explicit blocker; do not count same-process contexts as full deployment smoke.
  </action>
  <verify>bun test tests/platform/deployment-smoke.test.ts</verify>
  <acceptance_criteria>
    - Runbook includes env/DNS/reverse proxy/health check sections.
    - Smoke covers two-subprocess EventBus and worker behavior or records an explicit blocker.
    - Secret rotation procedure avoids printing secret values.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Feature-complete plan cannot deploy | Runbook plus deployment smoke and health checks. |
| PGlite users cannot migrate to SaaS Postgres | Export/import scripts with schema and dry-run checks. |
| Backup or migrations corrupt production data | Backup/restore proof and migration lock tests. |
</threat_model>

<verification>
Run `bun test tests/postgres/operations.test.ts tests/platform/deployment-smoke.test.ts tests/db/migration-downgrade.test.ts`.
</verification>

<success_criteria>
Phase 10 has operational proof for PostgreSQL deployment, migration, backup/restore, and cross-process SaaS behavior.
</success_criteria>
