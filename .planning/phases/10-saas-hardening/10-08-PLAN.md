---
phase: 10-saas-hardening
plan: 8
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-04, 10-05]
requirements: [SAS-02, SAS-03, SAS-04, SAS-05]
autonomous: true
files_modified:
  - src/tui/screens/organization.ts
  - src/tui/screens/saas-status.ts
  - src/tui/index.ts
  - tests/tui/organization.test.ts
  - tests/tui/saas-status.test.ts
must_haves:
  truths:
    - "Decisions covered: D-04, D-05, D-06, D-16, D-20, D-31."
    - "TUI is keyboard operational/admin UX for SaaS hardening."
---

<objective>
Add TUI parity for organization administration and SaaS operational status.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@src/tui/index.ts
@src/tui/screens/notifications.ts
@src/tui/screens/theme.ts
@tests/tui/smoke.test.ts
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Add organization TUI screen</name>
  <files>src/tui/screens/organization.ts, src/tui/index.ts, tests/tui/organization.test.ts</files>
  <read_first>src/tui/screens/settings.ts, src/tui/screens/notifications.ts, tests/tui/smoke.test.ts</read_first>
  <action>
    Add `OrganizationScreen` showing:
    - active org name/slug/role
    - list of orgs with active marker
    - member table with email and role
    - actions `switch`, `add member`, `remove member`, `set role`
    Use the existing TUI caller/tRPC path; do not import DB entities.
  </action>
  <verify>bun test tests/tui/organization.test.ts</verify>
  <acceptance_criteria>
    - `src/tui/screens/organization.ts` contains `OrganizationScreen`.
    - `tests/tui/organization.test.ts` asserts rendered role `owner`.
    - `src/tui/index.ts` registers organization screen route/menu entry.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add SaaS status TUI screen</name>
  <files>src/tui/screens/saas-status.ts, src/tui/index.ts, tests/tui/saas-status.test.ts</files>
  <read_first>src/tui/screens/doctor.ts, src/tui/screens/runs.ts, src/cli/commands/saas.ts</read_first>
  <action>
    Add `SaasStatusScreen` showing:
    - database backend and pool max/waiting count
    - EventBus adapter
    - worker adapter, queue depth, failed jobs
    - tenant isolation table counts and exception count
    - PostgreSQL integration test status if available
    Screen must be keyboard-only and render useful plain text under `renderPlain`.
  </action>
  <verify>bun test tests/tui/saas-status.test.ts</verify>
  <acceptance_criteria>
    - `src/tui/screens/saas-status.ts` contains `SaasStatusScreen`.
    - `tests/tui/saas-status.test.ts` asserts text `postgres-notify`.
    - `tests/tui/saas-status.test.ts` asserts text `graphile-worker`.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Operational SaaS state hidden outside Web | TUI status screen exposes backend, pool, queue, EventBus, tenant audit. |
| TUI surface becomes read-only while Web can mutate | Organization screen includes member and switch actions. |
</threat_model>

<verification>
Run `bun test tests/tui/organization.test.ts tests/tui/saas-status.test.ts`.
</verification>

<success_criteria>
TUI has keyboard-friendly SaaS org and operational parity.
</success_criteria>
