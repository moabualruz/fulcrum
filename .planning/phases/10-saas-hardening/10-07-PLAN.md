---
phase: 10-saas-hardening
plan: 7
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-04, 10-05]
requirements: [SAS-02, SAS-03, SAS-04, SAS-05]
autonomous: true
files_modified:
  - src/cli/commands/orgs.ts
  - src/cli/commands/saas.ts
  - src/cli/index.ts
  - tests/cli/orgs.test.ts
  - tests/cli/saas.test.ts
must_haves:
  truths:
    - "Decisions covered: D-04, D-05, D-06, D-07, D-16, D-20, D-31."
    - "Every new CLI command supports --json."
---

<objective>
Add CLI parity for SaaS org management and operational hardening status.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@src/cli/index.ts
@src/cli/commands/auth.ts
@src/cli/commands/db.ts
@src/server/trpc/routers/orgs.ts
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Add `fulcrum orgs` commands</name>
  <files>src/cli/commands/orgs.ts, src/cli/index.ts, tests/cli/orgs.test.ts</files>
  <read_first>src/cli/commands/auth.ts, src/cli/commands/docs.ts, src/server/trpc/routers/orgs.ts</read_first>
  <action>
    Add commands:
    - `fulcrum orgs list --json`
    - `fulcrum orgs switch <slug-or-id> --json`
    - `fulcrum orgs members list --json`
    - `fulcrum orgs members add --email <email> --role member --json`
    - `fulcrum orgs members remove <member-id> --json`
    - `fulcrum orgs members role set <member-id> --role admin --json`
    JSON list shape includes `id`, `slug`, `name`, `role`, `active`.
  </action>
  <verify>bun test tests/cli/orgs.test.ts</verify>
  <acceptance_criteria>
    - `src/cli/commands/orgs.ts` contains `members role set`.
    - `tests/cli/orgs.test.ts` asserts JSON field `active`.
    - `src/cli/index.ts` dispatches `orgs`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add `fulcrum saas status` commands</name>
  <files>src/cli/commands/saas.ts, src/cli/index.ts, tests/cli/saas.test.ts</files>
  <read_first>src/cli/commands/db.ts, src/cli/doctor.ts, src/queue/graphile-worker-adapter.ts, src/subscriptions/postgres-event-bus.ts</read_first>
  <action>
    Add `fulcrum saas status --json` returning:
    `{"database":{"backend":"postgres","pool":{...}},"eventBus":{"adapter":"postgres-notify"|"in-process"},"workers":{"adapter":"graphile-worker"|"in-process","queueDepth":number|null,"failedJobs":number|null},"tenantIsolation":{"rlsTables":number,"exceptions":number}}`.
    Add `fulcrum saas tenant-audit --json` returning per-table RLS/exception rows from `tenant-audit.ts`.
  </action>
  <verify>bun test tests/cli/saas.test.ts</verify>
  <acceptance_criteria>
    - `src/cli/commands/saas.ts` contains `tenant-audit`.
    - `tests/cli/saas.test.ts` asserts `eventBus.adapter`.
    - `tests/cli/saas.test.ts` asserts `workers.adapter`.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Scripted SaaS operations lack visibility | JSON status exposes pool, workers, EventBus, tenant audit. |
| CLI bypasses backend authorization | Commands call shared tRPC/local caller paths. |
</threat_model>

<verification>
Run `bun test tests/cli/orgs.test.ts tests/cli/saas.test.ts`.
</verification>

<success_criteria>
CLI has scriptable SaaS org and operational parity with JSON outputs.
</success_criteria>
