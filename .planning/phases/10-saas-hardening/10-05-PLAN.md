---
phase: 10-saas-hardening
plan: 5
type: execute
wave: 2
depends_on: [10-01]
requirements: [SAS-05, SAS-06]
autonomous: true
files_modified:
  - package.json
  - src/queue/graphile-worker-adapter.ts
  - src/queue/index.ts
  - src/workers/registry.ts
  - tests/postgres/graphile-worker.test.ts
  - tests/workers/registry.test.ts
must_haves:
  truths:
    - "Decisions covered: D-28, D-29, D-30, D-31, D-32."
    - "graphile-worker is an adapter; existing task names and payload assertions stay stable."
---

<objective>
Add PostgreSQL-backed graphile-worker coordination while preserving existing worker abstractions.
</objective>

<execution_context>
@.planning/phases/10-saas-hardening/10-CONTEXT.md
@src/queue/index.ts
@src/workers/registry.ts
@src/repos/workers/sync-remote.ts
@src/notifications/fanout-worker.ts
@tests/workers/registry.test.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add graphile-worker adapter</name>
  <files>package.json, src/queue/graphile-worker-adapter.ts, src/queue/index.ts, tests/postgres/graphile-worker.test.ts</files>
  <read_first>package.json, src/queue/index.ts, src/workers/registry.ts, tests/postgres/graphile-worker.test.ts</read_first>
  <action>
    Add dependency `graphile-worker` pinned to the current verified version.
    Create `src/queue/graphile-worker-adapter.ts` exporting `createGraphileWorkerAdapter`.
    Adapter must support:
    - `addJob(name, payload, { jobKey })`
    - task registration from `WorkerRegistry.listTasks()`
    - cron registration from `CronDefinition`
    - error propagation so graphile-worker retries failed jobs
    - JSON status with queue depth, failed jobs, delayed jobs, instance ID
  </action>
  <verify>bun test tests/postgres/graphile-worker.test.ts tests/workers/registry.test.ts</verify>
  <acceptance_criteria>
    - `package.json` contains `"graphile-worker"`.
    - `src/queue/graphile-worker-adapter.ts` contains `createGraphileWorkerAdapter`.
    - `src/queue/graphile-worker-adapter.ts` contains `jobKey`.
    - `src/queue/graphile-worker-adapter.ts` contains `queueDepth`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Prove two-worker dedupe and task parity</name>
  <files>tests/postgres/graphile-worker.test.ts, tests/workers/registry.test.ts</files>
  <read_first>src/repos/workers/sync-local.ts, src/repos/workers/sync-remote.ts, src/artifacts/worker.ts, src/notifications/fanout-worker.ts, src/workers/metrics-rollup.ts</read_first>
  <action>
    Extend tests so two graphile-worker instances are started against the same PostgreSQL DB.
    Enqueue the same logical task twice with identical `jobKey`.
    Assert handler invocation count is exactly `1`.
    Assert adapter registers existing task names without changing payload assertions.
  </action>
  <verify>bun test tests/postgres/graphile-worker.test.ts tests/workers/registry.test.ts</verify>
  <acceptance_criteria>
    - `tests/postgres/graphile-worker.test.ts` contains `handlerCalls`.
    - `tests/postgres/graphile-worker.test.ts` asserts `handlerCalls.length` equals `1`.
    - `tests/workers/registry.test.ts` still lists existing worker task names.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Duplicate background work across SaaS instances | graphile-worker adapter preserves job keys and proves single processing. |
| Worker rewrite breaks existing task contracts | Adapter consumes `WorkerRegistry` and existing payload assertions. |
</threat_model>

<verification>
Run `bun test tests/postgres/graphile-worker.test.ts tests/workers/registry.test.ts`.
</verification>

<success_criteria>
SAS-05 has PostgreSQL-backed multi-instance worker coordination with existing task parity.
</success_criteria>
