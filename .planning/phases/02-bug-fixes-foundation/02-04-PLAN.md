---
phase: 02-bug-fixes-foundation
plan: 04
type: execute
wave: 2
depends_on: [02-03]
files_modified:
  - src/utils/frontmatter.ts
  - src/utils/config-patcher.ts
  - src/cli/install.ts
  - src/cli/vendor-rules.ts
  - tests/docs/frontmatter-schemas.test.ts
  - src/cli/install.test.ts
  - src/cli/vendor-rules.test.ts
autonomous: true
requirements: [BUG-05, BUG-13]

must_haves:
  truths:
    - "D-05 D-06 frontmatter/config fixes require RED byte-stability tests before GREEN patchers"
    - "Config and frontmatter updates use targeted patchers, not whole-file parse/stringify rewrites"
  artifacts:
    - path: "src/utils/frontmatter.ts"
      provides: "Byte-stable frontmatter patching"
      contains: "patch"
---

<objective>
Replace whole-file config/frontmatter rewrites with targeted patchers that preserve unowned bytes.
</objective>

<tasks>
<task type="red">
  <name>Task 1: Add RED byte-stability tests</name>
  <files>tests/docs/frontmatter-schemas.test.ts, src/cli/install.test.ts, src/cli/vendor-rules.test.ts</files>
  <read_first>tests/docs/frontmatter-schemas.test.ts, src/cli/install.test.ts, src/cli/vendor-rules.test.ts, src/utils/io.ts</read_first>
  <action>
Add RED tests:
- Updating one frontmatter key preserves comments, key order, quoting style, blank lines, and body bytes
- Updating JSON/TOML config Fulcrum-owned key preserves unrelated keys and formatting
- Sentinel rules splice preserves content outside `<!-- BEGIN FULCRUM RULES -->` and `<!-- END FULCRUM RULES -->`
Commit RED tests before implementation.
  </action>
  <verify>
    <automated>bun test tests/docs/frontmatter-schemas.test.ts src/cli/install.test.ts src/cli/vendor-rules.test.ts</automated>
  </verify>
  <acceptance_criteria>
- RED output includes byte mismatch or missing targeted patch behavior
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 2: Implement targeted patch helpers</name>
  <files>src/utils/frontmatter.ts, src/utils/config-patcher.ts, src/cli/install.ts, src/cli/vendor-rules.ts</files>
  <read_first>src/utils/io.ts, src/cli/install.ts, src/cli/vendor-rules.ts, rules/AGENTS.md</read_first>
  <action>
Implement helpers:
- `patchFrontmatterKey(source, key, value)` changes only the target YAML scalar line or inserts it before closing frontmatter delimiter
- `patchJsonOwnedKey(source, path, value, ownershipMarker)` updates only owned keys and refuses unowned paths
- `patchTomlOwnedKey(source, dottedKey, value, ownershipMarker)` updates only owned keys and refuses unowned paths
Use these helpers in install/settings/rules code currently doing parse/stringify whole-file rewrites.
  </action>
  <verify>
    <automated>bun test tests/docs/frontmatter-schemas.test.ts src/cli/install.test.ts src/cli/vendor-rules.test.ts</automated>
  </verify>
  <acceptance_criteria>
- Tests compare exact unchanged prefix/suffix bytes around patched keys
- `rg "JSON\\.stringify\\([^\\n]+, null, 2\\)|yaml\\.stringify|YAML\\.stringify" src/cli src/utils` has no active matches in ownership-sensitive patch paths
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | Config/frontmatter files | mitigate | Patch exact owned keys and preserve unrelated bytes |
</threat_model>

<verification>
- `bun test tests/docs/frontmatter-schemas.test.ts src/cli/install.test.ts src/cli/vendor-rules.test.ts`
- `bun run ci`
</verification>

<success_criteria>
- Frontmatter YAML round-trips byte-stable outside edited key
- JSON/TOML config updates are targeted and ownership-safe
</success_criteria>

