---
phase: 02-bug-fixes-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci.ts
  - scripts/ci.test.ts
  - scripts/release.ts
  - package.json
  - src/web/package.json
  - src/web/bun.lock
  - src/web/tests/e2e/_smoke.spec.ts
autonomous: true
requirements: [BUG-03, BUG-04, BUG-16]

must_haves:
  truths:
    - "D-01 D-02 D-03 dependency-first planning starts with build/test blockers"
    - "D-05 D-06 every bug fix has RED evidence before GREEN"
    - "D-13 bun run ci runs all required stable gates"
    - "D-14 focused target recipes remain available"
    - "D-15 default CI includes smoke e2e; full e2e remains named recipe"
    - "D-16 skills lint and compression move to release gate"
  artifacts:
    - path: "scripts/ci.ts"
      provides: "Root CI with stable web gates and smoke e2e"
      contains: "web:e2e:smoke"
    - path: "scripts/release.ts"
      provides: "Release gate owns skills lint and compress check"
      contains: "compress"
---

<objective>
Make root CI the stable all-gates command for product code, with web typecheck/build/unit/smoke e2e included and vulnerable web lockfile dependency removed.
</objective>

<execution_context>
@.planning/phases/02-bug-fixes-foundation/02-CONTEXT.md
@.planning/phases/02-bug-fixes-foundation/02-RESEARCH.md
@.planning/phases/02-bug-fixes-foundation/02-VALIDATION.md
</execution_context>

<tasks>

<task type="red">
  <name>Task 1: Add RED CI contract tests</name>
  <files>scripts/ci.test.ts</files>
  <read_first>scripts/ci.ts, scripts/ci.test.ts, src/web/package.json, src/web/bun.lock</read_first>
  <action>
Add tests that fail on current CI policy:
- `STEPS` contains `web:check`, `web:build`, and `web:test`
- `STEPS` contains a default non-opt-in smoke step named `web:e2e:smoke`
- `STEPS` does not contain `skills:lint` or `compress:check`
- `src/web/bun.lock` does not contain exact string `"cookie": ["cookie@0.6.0"`
- `src/web/package.json` has scripts `web:e2e:smoke` and `web:e2e:full`
Commit this RED test separately before changing implementation.
  </action>
  <verify>
    <automated>bun test scripts/ci.test.ts</automated>
  </verify>
  <acceptance_criteria>
- Before implementation, `bun test scripts/ci.test.ts` fails with messages mentioning `web:e2e:smoke`, `skills:lint`, `compress:check`, or `cookie@0.6.0`
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 2: Restructure CI and web scripts</name>
  <files>scripts/ci.ts, scripts/release.ts, package.json, src/web/package.json, src/web/tests/e2e/_smoke.spec.ts</files>
  <read_first>scripts/ci.ts, scripts/release.ts, package.json, src/web/package.json, src/web/tests/e2e/_smoke.spec.ts</read_first>
  <action>
Update `scripts/ci.ts`:
- Keep `install`, `typecheck`, `symphony:lock`, `symphony:conformance`, `test`, `license-audit`, `ci:codegen`, `build:all`, `web:install`, `web:check`, `web:build`, `web:test`, and `ci:schemas`
- Remove default product CI steps named `skills:lint` and `compress:check`
- Add default smoke e2e step `{ name: "web:e2e:smoke", cmd: ["bun", "run", "web:e2e:smoke"], cwd: "src/web" }`
- Keep opt-in full e2e behind `FULCRUM_RUN_E2E=1`, but rename the step to `web:e2e:full`
Update `src/web/package.json`:
- Add `"web:e2e:smoke": "playwright test tests/e2e/_smoke.spec.ts"`
- Add `"web:e2e:full": "playwright test tests/e2e/"`
- Keep `"web:e2e"` as alias for full suite or update all callers to the new full name
Update release gate so `skills:lint` and `compress:check` still run during `bun run release`.
  </action>
  <verify>
    <automated>bun test scripts/ci.test.ts</automated>
    <automated>bun run ci</automated>
  </verify>
  <acceptance_criteria>
- `bun test scripts/ci.test.ts` exits 0
- `rg -n "web:e2e:smoke" scripts/ci.ts src/web/package.json` returns matches in both files
- `rg -n "skills:lint|compress:check" scripts/ci.ts` returns no matches
- `rg -n "skills.*lint|compress" scripts/release.ts` returns matches
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 3: Remove cookie 0.6.0 from web lockfile</name>
  <files>src/web/package.json, src/web/bun.lock</files>
  <read_first>src/web/package.json, src/web/bun.lock</read_first>
  <action>
Update web dependencies or overrides so `src/web/bun.lock` no longer resolves runtime `cookie@0.6.0`. Prefer upgrading `@sveltejs/kit` or adding Bun-supported resolution only if it is honored by `bun install --frozen-lockfile`. Do not hand-edit lockfile except as output from package manager resolution.
  </action>
  <verify>
    <automated>cd src/web && bun install --frozen-lockfile</automated>
    <automated>! rg '"cookie": \\["cookie@0\\.6\\.0"' src/web/bun.lock</automated>
  </verify>
  <acceptance_criteria>
- `cd src/web && bun install --frozen-lockfile` exits 0
- `rg '"cookie": \\["cookie@0\\.6\\.0"' src/web/bun.lock` returns no matches
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | CI gate selection | mitigate | Test `STEPS` so web and smoke gates cannot be dropped silently |
| T-02-02 | Elevation of Privilege | vulnerable cookie parser | mitigate | Remove `cookie@0.6.0` resolution from web lockfile |
</threat_model>

<verification>
- `bun test scripts/ci.test.ts`
- `bun run ci`
- `FULCRUM_RUN_E2E=1 bun run scripts/ci.ts` uses full e2e step name `web:e2e:full`
</verification>

<success_criteria>
- Root CI includes stable web gates and smoke e2e
- Full e2e remains named/opt-in
- Release gate still runs skills lint and compression
- `cookie@0.6.0` absent from web lockfile
</success_criteria>

