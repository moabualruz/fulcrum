---
phase: 02-bug-fixes-foundation
plan: 08
type: execute
wave: 3
depends_on: [02-02, 02-06, 02-07]
files_modified:
  - src/workers/registry.ts
  - src/artifacts/worker.ts
  - src/notifications/fanout-worker.ts
  - src/repos/workers/sync-local.ts
  - src/repos/workers/sync-remote.ts
  - src/cli/commands/auth.ts
  - src/tui/screens/auth.ts
  - src/server/trpc/routers/auth.ts
  - tests/workers/registry.test.ts
  - src/cli/commands/auth.test.ts
  - src/tui/screens/auth.test.ts
  - src/web/tests/e2e/auth-login.spec.ts
autonomous: true
requirements: [FND-04, FND-05, BUG-17]

must_haves:
  truths:
    - "D-04 BUG-17 local main sync/repo hygiene is deferred outside Phase 2 product/runtime execution"
    - "D-07 worker registry and auth parity receive unit and integration tests"
    - "D-09 D-10 worker/auth parity comes after DB, flags, and permission foundations"
    - "D-12 FND-05 includes Web login/auto-session, CLI fulcrum auth whoami, and TUI auth screen"
  artifacts:
    - path: "src/workers/registry.ts"
      provides: "Extensible worker registry"
      contains: "registerTask"
---

<objective>
Bootstrap an extensible worker registry and verify auth/init parity across Web, CLI, and TUI. Explicitly keep BUG-17 deferred as repo hygiene outside product execution.
</objective>

<tasks>
<task type="red">
  <name>Task 1: Add RED worker registry and auth parity tests</name>
  <files>tests/workers/registry.test.ts, src/cli/commands/auth.test.ts, src/tui/screens/auth.test.ts, src/web/tests/e2e/auth-login.spec.ts</files>
  <read_first>src/artifacts/worker.ts, src/notifications/fanout-worker.ts, src/repos/workers/sync-local.ts, src/repos/workers/sync-remote.ts, src/cli/commands/auth.ts, src/tui/screens/auth.ts, src/server/trpc/routers/auth.ts, .planning/phases/02-bug-fixes-foundation/02-UI-SPEC.md</read_first>
  <action>
Add RED tests:
- Worker registry can register task name, payload assertion, and async handler
- Registry rejects duplicate task names
- Registry awaits every handler promise and reports failure when handler rejects
- Existing notification/artifact/repo worker task names can be registered without changing payload contracts
- `fulcrum auth whoami --json` returns `{ userId, orgId, email, role }` when session exists and clear error when missing
- TUI auth screen renders `Settings › Auth`, `User Email`, `User ID`, `Org`, `Role`, `Passkeys`, and `Press [q] to go back`
- Web auth smoke verifies login/auto-session route still works or documents existing seeded auto-session behavior
Commit RED tests before implementation.
  </action>
  <verify>
    <automated>bun test tests/workers/registry.test.ts src/cli/commands/auth.test.ts src/tui/screens/auth.test.ts</automated>
    <automated>cd src/web && bun run web:e2e:smoke</automated>
  </verify>
  <acceptance_criteria>
- RED output names missing worker registry, missing whoami JSON, or missing TUI auth labels
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 2: Implement extensible worker registry</name>
  <files>src/workers/registry.ts, src/artifacts/worker.ts, src/notifications/fanout-worker.ts, src/repos/workers/sync-local.ts, src/repos/workers/sync-remote.ts</files>
  <read_first>src/notifications/fanout-worker.ts, src/artifacts/worker.ts, src/repos/workers/sync-local.ts, src/repos/workers/sync-remote.ts, tests/notifications/fanout-worker.test.ts, tests/artifacts/worker.test.ts</read_first>
  <action>
Implement `src/workers/registry.ts`:
- `registerTask(name, assertPayload, handler)`
- `getTask(name)`
- `listTasks()`
- Duplicate names throw `WorkerTaskAlreadyRegisteredError`
- Runner awaits handler and treats returned promise rejection as failed job
- Payload assertion receives unknown payload and must throw before handler runs
Bridge existing workers by exporting registration functions for notification fanout, artifact harvest/prune, repo sync local, and repo sync remote. Do not require Graphile Worker runtime for PGlite tests; keep adapter interface compatible with Graphile task `(payload, helpers)`.
  </action>
  <verify>
    <automated>bun test tests/workers/registry.test.ts tests/notifications/fanout-worker.test.ts tests/artifacts/worker.test.ts tests/repos/sync-local.test.ts tests/repos/sync-remote.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `rg "registerTask|assertPayload|WorkerTaskAlreadyRegisteredError" src/workers/registry.ts` returns matches
- Worker tests prove duplicate rejection and awaited handler failure
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 3: Complete auth/init parity</name>
  <files>src/cli/commands/auth.ts, src/tui/screens/auth.ts, src/server/trpc/routers/auth.ts, src/web/src/routes/auth/**</files>
  <read_first>src/cli/commands/auth.ts, src/tui/screens/auth.ts, src/server/trpc/routers/auth.ts, tests/trpc/auth.test.ts, tests/cli/auth.test.ts, src/web/src/routes/auth/login/+page.server.ts</read_first>
  <action>
Implement parity:
- CLI `fulcrum auth whoami --json` calls the same auth/session source as tRPC `auth.whoami`
- Missing CLI session prints `No active CLI session found. Run fulcrum init or fulcrum auth login before protected auth commands.`
- TUI auth screen renders resolved user/org/role/passkey values, not hardcoded placeholders unless data missing
- Web login/auto-session remains under `src/web/src/routes/auth/` and existing login/signup/invite tests pass
Do not redesign auth UI.
  </action>
  <verify>
    <automated>bun test src/cli/commands/auth.test.ts tests/cli/auth.test.ts src/tui/screens/auth.test.ts tests/trpc/auth.test.ts</automated>
    <automated>cd src/web && bun run web:e2e:smoke</automated>
  </verify>
  <acceptance_criteria>
- CLI whoami JSON contains keys `userId`, `orgId`, `email`, `role`
- TUI auth output contains exact labels from UI-SPEC
- Web auth smoke passes or existing fixture auto-session behavior is asserted
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Preserve BUG-17 deferral</name>
  <files>.planning/phases/02-bug-fixes-foundation/02-CONTEXT.md, .planning/phases/02-bug-fixes-foundation/02-08-SUMMARY.md</files>
  <read_first>.planning/phases/02-bug-fixes-foundation/02-CONTEXT.md</read_first>
  <action>
Do not sync local main or perform repo hygiene as part of Phase 2 execution. In SUMMARY, state `BUG-17 deferred outside Phase 2 product/runtime execution per D-04`.
  </action>
  <verify>
    <automated>rg "BUG-17.*defer|D-04" .planning/phases/02-bug-fixes-foundation/02-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
- No git branch sync, pull, push, or main hygiene command is performed by this plan
- SUMMARY contains exact string `BUG-17 deferred outside Phase 2 product/runtime execution per D-04`
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | worker payloads | mitigate | Runtime payload assertion before handler execution |
| T-02-14 | Information Disclosure | auth surfaces | mitigate | CLI/TUI/Web read same session contract and handle missing session explicitly |
</threat_model>

<verification>
- `bun test tests/workers/registry.test.ts tests/notifications/fanout-worker.test.ts tests/artifacts/worker.test.ts`
- `bun test src/cli/commands/auth.test.ts tests/cli/auth.test.ts src/tui/screens/auth.test.ts tests/trpc/auth.test.ts`
- `cd src/web && bun run web:e2e:smoke`
- `bun run ci`
</verification>

<success_criteria>
- Graphile-compatible worker registry exists and current workers register
- Web login/auto-session, CLI whoami, and TUI auth screen demonstrate parity
- BUG-17 remains intentionally deferred outside Phase 2 execution
</success_criteria>

