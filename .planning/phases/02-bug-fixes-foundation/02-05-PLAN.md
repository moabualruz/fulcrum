---
phase: 02-bug-fixes-foundation
plan: 05
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/cli/product.ts
  - src/cli/product.test.ts
  - src/cli/doctor.ts
  - src/cli/doctor.test.ts
  - src/tui/index.ts
  - src/web/src/routes/+layout.svelte
  - src/web/src/lib/components/command-palette/command-palette-handlers.ts
  - src/web/src/lib/components/command-palette/command-palette-handlers.test.ts
  - src/web/tests/e2e/_smoke.spec.ts
autonomous: true
requirements: [BUG-09, BUG-12, BUG-15, BUG-18]

must_haves:
  truths:
    - "D-05 D-06 parser, doctor, complexity, and Cmd+K fixes require RED evidence before GREEN"
    - "UI-SPEC Web Cmd+K contract uses existing CommandPalette and selectors"
  artifacts:
    - path: "src/web/src/routes/+layout.svelte"
      provides: "Global Cmd+K/Ctrl+K binding"
      contains: "svelte:window"
---

<objective>
Fix product CLI positional parsing, doctor warning counts, complexity hotspots, and web Cmd+K global palette shortcut.
</objective>

<tasks>
<task type="red">
  <name>Task 1: Add RED runtime bug tests</name>
  <files>src/cli/product.test.ts, src/cli/doctor.test.ts, src/web/src/lib/components/command-palette/command-palette-handlers.test.ts, src/web/tests/e2e/_smoke.spec.ts</files>
  <read_first>src/cli/product.ts, src/cli/doctor.ts, src/web/src/routes/+layout.svelte, src/web/src/lib/components/command-palette/command-palette-handlers.ts, .planning/phases/02-bug-fixes-foundation/02-UI-SPEC.md</read_first>
  <action>
Add RED tests:
- Product CLI parser handles positional args after flags, flags after positional args, `--json`, `--`, and unknown flag errors without swallowing positionals
- Doctor increments warning count when product-kernel DB check returns an error
- Command palette handler receives fresh open state and toggles on `Meta+k` and `Control+k`
- Smoke e2e opens `[data-command-palette][data-state='open']`, focuses `[data-command-palette-input]`, and closes with Escape
Commit RED tests before implementation.
  </action>
  <verify>
    <automated>bun test src/cli/product.test.ts src/cli/doctor.test.ts src/web/src/lib/components/command-palette/command-palette-handlers.test.ts</automated>
    <automated>cd src/web && bun run web:e2e:smoke</automated>
  </verify>
  <acceptance_criteria>
- RED output names parser positional failure, doctor warning count mismatch, or missing command palette open state
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 2: Fix parser, doctor, and Cmd+K shortcut</name>
  <files>src/cli/product.ts, src/cli/doctor.ts, src/web/src/routes/+layout.svelte, src/web/src/lib/components/command-palette/command-palette-handlers.ts</files>
  <read_first>src/cli/product.ts, src/cli/flags.ts, src/cli/doctor.ts, src/web/src/routes/+layout.svelte, src/web/src/lib/components/command-palette/command-palette-handlers.ts</read_first>
  <action>
Implement:
- Product parser returns `{ positionals, flags, passthrough }` with exact handling for `--json`, `--flag=value`, boolean flags, and `--`
- Doctor product-kernel DB error path increments `warnings` and includes a warning entry with subsystem `product-kernel-db`
- `+layout.svelte` adds `<svelte:window onkeydown={...}>` or Svelte 5 equivalent global keydown binding that calls existing `makeKeydownHandler(paletteOpen, next => paletteOpen = next)`
- Handler calls `preventDefault()` for Meta/Ctrl+K and Escape when open
  </action>
  <verify>
    <automated>bun test src/cli/product.test.ts src/cli/doctor.test.ts src/web/src/lib/components/command-palette/command-palette-handlers.test.ts</automated>
    <automated>cd src/web && bun run check</automated>
  </verify>
  <acceptance_criteria>
- `rg "svelte:window|onkeydown" src/web/src/routes/+layout.svelte` returns a match
- `rg "preventDefault" src/web/src/lib/components/command-palette/command-palette-handlers.ts` returns a match
- Doctor test asserts warnings increased from 0 to 1 on DB error
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 3: Refactor complexity hotspots under CCN 15</name>
  <files>src/tui/index.ts, src/cli/doctor.ts</files>
  <read_first>src/tui/index.ts, src/cli/doctor.ts, .planning/codebase/CONCERNS.md</read_first>
  <action>
Run `lizard src/tui/index.ts src/cli/doctor.ts` and refactor functions with CCN above 15 by extracting small pure helpers in the same file or nearest existing module. Do not change behavior. Add or update tests around extracted helpers only where existing coverage is missing.
  </action>
  <verify>
    <automated>lizard src/tui/index.ts src/cli/doctor.ts | tee /tmp/phase2-lizard.txt && ! rg "CCN.*(1[6-9]|[2-9][0-9])" /tmp/phase2-lizard.txt</automated>
    <automated>bun test src/cli/doctor.test.ts tests/tui/smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `lizard src/tui/index.ts src/cli/doctor.ts` reports no function CCN greater than 15
- Existing CLI doctor and TUI smoke tests pass
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Denial of Service | command palette shortcut | accept | Keyboard handler is client-only and constant-time |
</threat_model>

<verification>
- `bun test src/cli/product.test.ts src/cli/doctor.test.ts src/web/src/lib/components/command-palette/command-palette-handlers.test.ts`
- `cd src/web && bun run check && bun run web:e2e:smoke`
- `lizard src/tui/index.ts src/cli/doctor.ts`
- `bun run ci`
</verification>

<success_criteria>
- Product CLI parses flags/positionals correctly
- Doctor warning counts reflect DB errors
- Complexity hotspots under CCN 15
- Cmd+K/Ctrl+K opens palette and Escape closes it
</success_criteria>

