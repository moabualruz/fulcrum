---
phase: 02-bug-fixes-foundation
plan: 06
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - src/db/entities/TenantSetting.ts
  - src/db/repositories/TenantSettingRepository.ts
  - src/db/migrations/*
  - src/product-kernel/tenant-settings.ts
  - src/product-kernel/db/migrations/0004_tenant_settings.sql
  - src/flags/registry.ts
  - src/tui/feature-flags.ts
  - tests/db/migrations/composite-indexes.test.ts
  - tests/flags/registry.test.ts
  - src/product-kernel/tenant-settings.test.ts
autonomous: true
requirements: [FND-01, FND-03, FND-06, FND-07]

must_haves:
  truths:
    - "D-07 shared foundation infrastructure has unit and integration tests"
    - "D-09 D-10 DB/flags/tenant lane follows dependency order"
    - "D-11 tenant settings is standalone entity, repository, tests, and minimal surface"
  artifacts:
    - path: "src/db/entities/TenantSetting.ts"
      provides: "Canonical tenant_settings entity"
      contains: "TenantSetting"
---

<objective>
Install foundation DB pieces: tenant settings entity/repository, clean migrations on both backends, composite tenant indexes, and canonical feature-flag booleans.
</objective>

<tasks>
<task type="red">
  <name>Task 1: Add RED DB foundation tests</name>
  <files>src/product-kernel/tenant-settings.test.ts, tests/db/migrations/composite-indexes.test.ts, tests/flags/registry.test.ts</files>
  <read_first>src/product-kernel/tenant-settings.ts, src/product-kernel/db/migrations/0004_tenant_settings.sql, src/flags/registry.ts, src/tui/feature-flags.ts, tests/db/migrations/composite-indexes.test.ts</read_first>
  <action>
Add RED tests:
- `tenant_settings` migration includes `id` column or repository stops inserting `id`; test current mismatch by creating/upserting tenant setting
- MikroORM has `TenantSetting` entity and repository with `orgId`, `key`, `value`, `createdAt`, `updatedAt`
- Tenant-scoped tables have composite indexes beginning with `org_id`
- TUI feature flags read stable booleans through canonical `src/flags/registry.ts` or a bridge tested against it
Commit RED tests before implementation.
  </action>
  <verify>
    <automated>bun test src/product-kernel/tenant-settings.test.ts tests/db/migrations/composite-indexes.test.ts tests/flags/registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
- RED output names missing `id`, missing `TenantSetting`, missing composite index, or TUI flag drift
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 2: Implement tenant settings entity/repository and migration fix</name>
  <files>src/db/entities/TenantSetting.ts, src/db/repositories/TenantSettingRepository.ts, src/db/entities/index.ts, src/product-kernel/tenant-settings.ts, src/product-kernel/db/migrations/0004_tenant_settings.sql</files>
  <read_first>src/db/entities/auth/Org.ts, src/db/repositories/auth/FeatureFlagRepository.ts, src/product-kernel/tenant-settings.ts, src/product-kernel/db/migrations/0004_tenant_settings.sql</read_first>
  <action>
Implement canonical tenant settings:
- Entity `TenantSetting` mapped to table `tenant_settings`
- Fields: `id`, `orgId`, `key`, `value` JSON/text compatible with PGlite/PostgreSQL, `createdAt`, `updatedAt`
- Unique composite constraint `(org_id, key)`
- Repository methods `get(orgId, key)`, `set(orgId, key, value)`, `delete(orgId, key)`, `list(orgId)`
- Fix product-kernel migration/repository mismatch by either adding `id text primary key` to SQL or removing inserted `id`; choose one and make both paths agree
  </action>
  <verify>
    <automated>bun test src/product-kernel/tenant-settings.test.ts tests/db/migrations/composite-indexes.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `rg "class TenantSetting|tableName.*tenant_settings|tenant_settings" src/db/entities src/db/repositories` returns matches
- `src/product-kernel/tenant-settings.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 3: Verify composite indexes and canonical flag registry</name>
  <files>src/db/migrations/*, src/flags/registry.ts, src/tui/feature-flags.ts, tests/flags/registry.test.ts, tests/db/migrations/composite-indexes.test.ts</files>
  <read_first>src/flags/registry.ts, src/tui/feature-flags.ts, tests/flags/registry.test.ts, tests/db/migrations/composite-indexes.test.ts</read_first>
  <action>
Update migrations/tests so tenant-scoped tables have composite indexes with `org_id` first where queries scope by org. Update `src/tui/feature-flags.ts` to import or bridge canonical stable booleans from `src/flags/registry.ts` instead of maintaining an untested divergent list.
  </action>
  <verify>
    <automated>bun test tests/db/migrations/composite-indexes.test.ts tests/flags/registry.test.ts src/tui/feature-flags.test.ts</automated>
  </verify>
  <acceptance_criteria>
- Composite index test lists every tenant-scoped table checked
- TUI flag test proves one known flag returns same boolean as canonical registry
  </acceptance_criteria>
</task>

<task type="blocking">
  <name>Task 4: Explicit migration verification</name>
  <files>src/db/migrations/*, src/product-kernel/db/migrations/*</files>
  <read_first>src/cli/commands/db.ts, tests/db/migrator-service.test.ts</read_first>
  <action>
After schema edits, run `fulcrum db migrate` for PGlite and PostgreSQL if DSN available. Record exact commands and outputs in SUMMARY.
  </action>
  <verify>
    <automated>bun run src/index.ts db migrate --backend pglite --json</automated>
  </verify>
  <acceptance_criteria>
- PGlite migration command returns JSON with `"ok":true`
- PostgreSQL command is run or documented environment-blocked with missing DSN
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | tenant-scoped tables | mitigate | Composite `(org_id, ...)` indexes and repository org scoping |
| T-02-10 | Tampering | feature flags | mitigate | Stable canonical registry prevents surface drift |
</threat_model>

<verification>
- `bun test src/product-kernel/tenant-settings.test.ts tests/db/migrations/composite-indexes.test.ts tests/flags/registry.test.ts src/tui/feature-flags.test.ts`
- `bun run src/index.ts db migrate --backend pglite --json`
- `bun run ci`
</verification>

<success_criteria>
- Tenant settings entity/repository/tests exist
- Migrations clean on PGlite and PostgreSQL path
- Composite tenant indexes verified
- Feature flags return stable booleans from canonical registry
</success_criteria>

