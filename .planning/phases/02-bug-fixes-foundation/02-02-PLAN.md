---
phase: 02-bug-fixes-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/product-kernel/db/pglite.ts
  - src/product-kernel/db/postgres.ts
  - src/product-kernel/db/migrate.ts
  - src/db/mikro-orm.config.ts
  - src/cli/commands/db.ts
  - src/cli/product.ts
  - src/config/database.ts
  - tests/cli/build.test.ts
  - tests/db/migrator-service.test.ts
  - tests/db/mikro-orm-config.test.ts
autonomous: false
requirements: [BUG-01, FND-01]

must_haves:
  truths:
    - "D-08 compiled-binary evidence may use repro script plus captured output where normal tests cannot express runtime"
    - "D-17 compiled binary works in dev and shipped mode with PGlite and PostgreSQL"
    - "D-18 shipped default backend is PGlite"
    - "D-19 DB config surface includes CLI setting, persisted config, and env override"
    - "D-20 migrations are explicit via fulcrum db migrate for all backends"
  artifacts:
    - path: "src/config/database.ts"
      provides: "DB backend resolver with env/config/default precedence"
      contains: "pglite"
    - path: "src/cli/commands/db.ts"
      provides: "Explicit fulcrum db migrate command for every backend"
      contains: "migrate"
---

<objective>
Make compiled Fulcrum binaries launch with PGlite by default, support PostgreSQL override, and use explicit `fulcrum db migrate` for all database backends.
</objective>

<tasks>

<task type="red">
  <name>Task 1: Capture compiled binary and DB backend failures</name>
  <files>tests/cli/build.test.ts, tests/db/mikro-orm-config.test.ts, tests/db/migrator-service.test.ts</files>
  <read_first>package.json, scripts/build-all.ts, src/product-kernel/db/pglite.ts, src/product-kernel/db/postgres.ts, src/product-kernel/db/migrate.ts, src/cli/commands/db.ts, src/cli/product.ts</read_first>
  <action>
Add RED tests/repros:
- `tests/cli/build.test.ts` builds a local compiled binary target and asserts running `dist/fulcrum-* db status --json` does not throw `/$bunfs/root/pglite.data` or `Compiled Bun binary cannot use PGlite`
- DB config test asserts default backend is `pglite`, `DATABASE_URL=postgresql://...` resolves `postgres`, and persisted config can set `db.backend=postgres`
- Migration command test asserts `fulcrum db migrate --backend pglite --json` and `fulcrum db migrate --backend postgres --json` route through the same command surface
Commit RED evidence before implementation.
  </action>
  <verify>
    <automated>bun test tests/cli/build.test.ts tests/db/mikro-orm-config.test.ts tests/db/migrator-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
- RED output includes one of: `/$bunfs/root/pglite.data`, `Compiled Bun binary cannot use PGlite`, missing `db.backend`, or missing explicit migrate route
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 2: Implement DB backend resolver and compiled PGlite data path</name>
  <files>src/config/database.ts, src/product-kernel/db/pglite.ts, src/product-kernel/db/postgres.ts, src/db/mikro-orm.config.ts, src/cli/product.ts</files>
  <read_first>src/product-kernel/db/pglite.ts, src/product-kernel/db/postgres.ts, src/db/mikro-orm.config.ts, src/cli/product.ts, src/cli/flags.ts</read_first>
  <action>
Create or update `src/config/database.ts` with:
- `type DbBackend = "pglite" | "postgres"`
- `resolveDatabaseConfig({ env, config })` precedence: explicit CLI flag > persisted `db.backend`/`db.url` > `DATABASE_URL` > default `{ backend: "pglite" }`
- PGlite data directory defaults under `$FULCRUM_HOME/pglite.data` or existing Fulcrum home resolver, never `/$bunfs/root/pglite.data`
- PostgreSQL requires `DATABASE_URL` or persisted `db.url`
Update `openPglite` to remove the compiled-binary unsupported throw and resolve writable data dir outside Bun embedded filesystem.
Update MikroORM config to use the resolver instead of ad hoc `DATABASE_URL` branching.
Update product CLI open path to select PGlite/Postgres through resolver.
  </action>
  <verify>
    <automated>bun test tests/db/mikro-orm-config.test.ts src/product-kernel/db/migrate.test.ts src/cli/product.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `rg "\\$bunfs.*throw|Compiled Bun binary cannot use PGlite" src/product-kernel/db/pglite.ts` returns no matches
- `rg "backend.*pglite|DATABASE_URL|db.backend" src/config/database.ts src/db/mikro-orm.config.ts` returns matches
- PGlite path logic contains `FULCRUM_HOME` or existing Fulcrum home resolver
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 3: Make migration command explicit for PGlite and PostgreSQL</name>
  <files>src/cli/commands/db.ts, src/product-kernel/db/migrate.ts, src/db/migrator.ts</files>
  <read_first>src/cli/commands/db.ts, src/product-kernel/db/migrate.ts, tests/db/migrator-service.test.ts, src/web/src/routes/settings/database/migrations/+page.server.ts</read_first>
  <action>
Implement `fulcrum db migrate` as the explicit migration entrypoint:
- Accept `--backend pglite|postgres`
- Accept `--url <postgres-url>` for PostgreSQL or use `DATABASE_URL`
- Emit JSON with fields `backend`, `applied`, `pending`, `current`, `ok`
- Stop auto-running migrations from compiled startup/product init unless command explicitly requests initialization; if a first-run init needs schema, print actionable message `Run fulcrum db migrate`
- Ensure both product-kernel migrations and MikroORM migrations are handled or clearly separated by command flags if both still exist
  </action>
  <verify>
    <automated>bun run src/index.ts db migrate --backend pglite --json</automated>
    <automated>DATABASE_URL=postgresql://fulcrum:fulcrum@127.0.0.1:5432/fulcrum_test bun run src/index.ts db migrate --backend postgres --json</automated>
  </verify>
  <acceptance_criteria>
- `bun run src/index.ts db migrate --backend pglite --json` prints JSON containing `"backend":"pglite"` and `"ok":true`
- PostgreSQL command either migrates successfully when DB is available or exits with a clear connection error before mutating PGlite
- Product init no longer silently auto-migrates compiled mode by default
  </acceptance_criteria>
</task>

<task type="blocking">
  <name>Task 4: Schema push / migration verification</name>
  <files>src/db/migrations/**, src/product-kernel/db/migrations/**</files>
  <read_first>tests/db/migrations/composite-indexes.test.ts, tests/db/migrator-service.test.ts, src/product-kernel/db/migrate.test.ts</read_first>
  <action>
Run explicit migration verification after DB/schema edits and before final CI:
- `bun run src/index.ts db migrate --backend pglite --json`
- PostgreSQL: run `bun run src/index.ts db migrate --backend postgres --json` when a PostgreSQL DSN is available; otherwise record skipped status in SUMMARY with exact missing DSN reason
  </action>
  <verify>
    <automated>bun run src/index.ts db migrate --backend pglite --json</automated>
  </verify>
  <acceptance_criteria>
- PGlite migration JSON contains `"ok":true`
- PostgreSQL verification is either green or documented as environment-blocked with command and missing DSN
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | DB backend config | mitigate | Deterministic precedence CLI > persisted config > env > PGlite default |
| T-02-04 | Denial of Service | compiled PGlite path | mitigate | Always place data under writable Fulcrum home, never embedded Bun filesystem |
</threat_model>

<verification>
- `bun test tests/cli/build.test.ts tests/db/mikro-orm-config.test.ts tests/db/migrator-service.test.ts src/product-kernel/db/migrate.test.ts`
- `bun run src/index.ts db migrate --backend pglite --json`
- `bun run ci`
</verification>

<success_criteria>
- Compiled binary launches without ENOENT
- PGlite default and PostgreSQL override are configurable
- All migrations are explicit through `fulcrum db migrate`
</success_criteria>

