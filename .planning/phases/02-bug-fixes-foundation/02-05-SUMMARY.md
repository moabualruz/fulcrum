---
phase: 02-bug-fixes-foundation
plan: 05
subsystem: cli-web-tui-runtime
tags: [product-cli, doctor, command-palette, svelte, lizard]

requires:
  - phase: 02-bug-fixes-foundation
    provides: [explicit DB migration behavior, CI web smoke gate]
provides:
  - strict product CLI argument parser with passthrough tracking
  - product-kernel DB doctor warnings with subsystem detail
  - global Cmd+K/Ctrl+K command palette shortcut
  - partial doctor complexity extraction into focused report builders
affects: [cli, doctor, web-shell, tui, phase-02]

tech-stack:
  added: []
  patterns:
    - exported parser contracts for product CLI runtime argument handling
    - doctor report sections built by focused helper functions
    - Svelte global keydown handler wired through existing controlled palette state

key-files:
  created:
    - .planning/phases/02-bug-fixes-foundation/02-05-SUMMARY.md
  modified:
    - src/cli/product.ts
    - src/cli/product.test.ts
    - src/cli/doctor.ts
    - src/cli/doctor.test.ts
    - src/web/src/routes/+layout.svelte
    - src/web/src/lib/components/command-palette/command-palette-handlers.ts
    - src/web/src/lib/components/command-palette/command-palette-handlers.test.ts
    - src/web/tests/e2e/_smoke.spec.ts
    - src/web/.gitignore

key-decisions:
  - "Product CLI argument parsing now fails closed on unknown or valueless flags."
  - "Doctor treats product-kernel DB probe failures as warnings, not fatal errors, and records subsystem product-kernel-db."
  - "Cmd+K/Ctrl+K uses the existing CommandPalette controlled state instead of adding visible shortcut UI."

patterns-established:
  - "Use parseProductArgs() for product command flags, positionals, and passthrough handling."
  - "Use makeKeydownHandler(() => currentOpen, setOpen) when handler lifetime may outlive one render."

requirements-completed: [BUG-09, BUG-12, BUG-15, BUG-18]
duration: 76min
completed: 2026-05-04
---

# Phase 02 Plan 05: Runtime Parser, Doctor, and Cmd+K Summary

**Strict product CLI parsing, product-kernel DB doctor warnings, and global web command-palette shortcuts.**

## Performance

- **Duration:** 76 min
- **Started:** 2026-05-04T11:03:00Z
- **Completed:** 2026-05-04T12:18:56Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

- Added RED tests for parser mixed positionals/flags/passthrough, doctor product-kernel DB warning behavior, fresh command-palette open state, and browser smoke palette selectors.
- Implemented `parseProductArgs()` with positionals, known flags, `--flag=value`, `--json`, `--`, passthrough capture, and unknown-flag errors.
- Wired global `<svelte:window onkeydown=...>` handling in the web layout so Meta/Ctrl+K toggles the existing command palette and Escape closes it.
- Changed doctor product-kernel DB failures from fatal errors to warning entries with `subsystem: "product-kernel-db"`.
- Split large doctor report-building sections into focused helpers while preserving existing doctor output behavior.

## Task Commits

1. **Task 1: Add RED runtime bug tests** - `5da61d2a` (test)
2. **Task 2: Fix parser, doctor, and Cmd+K shortcut** - `3f1defc5` (fix)
3. **Task 3: Refactor complexity hotspots under CCN 15** - `cc33f71a` (refactor)

**Plan metadata:** pending final docs commit

## Files Created/Modified

- `src/cli/product.ts` - Strict parser export and raw local-org creation for `product init` compatibility with explicit migrations.
- `src/cli/product.test.ts` - Parser RED contracts and explicit product-kernel migration setup for init tests.
- `src/cli/doctor.ts` - Product DB warning accounting and extracted report builders.
- `src/cli/doctor.test.ts` - Product-kernel DB warning assertions and explicit product-kernel migration fixture setup.
- `src/web/src/routes/+layout.svelte` - Global command palette keydown binding.
- `src/web/src/lib/components/command-palette/command-palette-handlers.ts` - Fresh open-state key handler support.
- `src/web/src/lib/components/command-palette/command-palette-handlers.test.ts` - Fresh-state and Escape behavior coverage.
- `src/web/tests/e2e/_smoke.spec.ts` - Cmd+K smoke test selectors.
- `src/web/.gitignore` - Ignores Playwright `test-results/` generated by smoke verification.

## Decisions Made

- Product parser uses an allowlist so accidental flags fail fast instead of being swallowed as positionals or values.
- Product-kernel DB probe errors are warning-level because doctor can still report other subsystem health.
- Browser shortcut behavior stays invisible and uses existing palette selectors per UI-SPEC.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added generated Playwright test-results ignore**
- **Found during:** Task 1 (Add RED runtime bug tests)
- **Issue:** `web:e2e:smoke` generated untracked `src/web/test-results/` artifacts.
- **Fix:** Added `/test-results/` to `src/web/.gitignore` and restored tracked `.last-run.json` after verification runs.
- **Files modified:** `src/web/.gitignore`
- **Verification:** `git status --short` showed no untracked Playwright result directories after the ignore.
- **Committed in:** `5da61d2a`

**2. [Rule 1 - Bug] Aligned doctor with resolved PGlite DB path**
- **Found during:** Task 2 (Fix parser, doctor, and Cmd+K shortcut)
- **Issue:** Doctor still inspected the legacy product DB path while the DB resolver and product CLI use `FULCRUM_HOME/pglite.data`.
- **Fix:** `buildProductKernelReport()` now uses `resolveDatabaseConfig()` and reports the resolved PGlite path.
- **Files modified:** `src/cli/doctor.ts`, `src/cli/doctor.test.ts`
- **Verification:** `bun test src/cli/doctor.test.ts --test-name-pattern "product kernel|product-kernel|repos section"` passed.
- **Committed in:** `3f1defc5`

**3. [Rule 1 - Bug] Kept product init compatible with repository EntityManager migration**
- **Found during:** Task 2 (Fix parser, doctor, and Cmd+K shortcut)
- **Issue:** `product init` called `createLocalOrg()` through a raw `ProductDb`, but repository helpers now require MikroORM `EntityManager`.
- **Fix:** `ensureLocalOrg()` performs its minimal local-org insert directly and uses an explicit UUID.
- **Files modified:** `src/cli/product.ts`
- **Verification:** `bun test src/cli/product.test.ts --test-name-pattern "product parser|product init"` passed.
- **Committed in:** `3f1defc5`

---

**Total deviations:** 3 auto-fixed (2 Rule 1, 1 Rule 2)
**Impact on plan:** Fixes stayed inside the planned product CLI, doctor, and web smoke surfaces.

## Issues Encountered

- `bun test src/cli/product.test.ts src/cli/doctor.test.ts src/web/src/lib/components/command-palette/command-palette-handlers.test.ts` still fails in pre-existing ARCH-02 product tests that call repository helpers with raw `ProductDb`. Parser, product init, doctor product-kernel, and handler checks pass.
- `cd src/web && bun run web:e2e:smoke` fails before palette verification because `/` returns 500 from `src/lib/server/dashboard.ts:56`: `TypeError: em.getConnection is not a function`.
- `cd src/web && bun run check` OOMs in `svelte-check` even with `NODE_OPTIONS=--max-old-space-size=8192`.
- `lizard src/tui/index.ts src/cli/doctor.ts` still reports legacy doctor functions over CCN 15 after extracting the largest `buildReport` hotspot. `src/tui/index.ts` has no CCN > 15.

## Verification

- `bun test src/cli/product.test.ts --test-name-pattern "product parser|product init"` - PASS.
- `bun test src/cli/doctor.test.ts --test-name-pattern "product kernel|product-kernel|repos section"` - PASS.
- `bun test src/web/src/lib/components/command-palette/command-palette-handlers.test.ts` - PASS.
- `bun test src/cli/doctor.test.ts tests/tui/smoke.test.ts` - PASS, 67 tests.
- `rg "svelte:window|onkeydown" src/web/src/routes/+layout.svelte` - PASS.
- `rg "preventDefault" src/web/src/lib/components/command-palette/command-palette-handlers.ts` - PASS.
- `bun test src/cli/product.test.ts src/cli/doctor.test.ts src/web/src/lib/components/command-palette/command-palette-handlers.test.ts` - FAIL on existing raw `ProductDb` tests in `src/cli/product.test.ts`.
- `cd src/web && bun run web:e2e:smoke` - FAIL on existing dashboard loader `em.getConnection is not a function`.
- `cd src/web && bun run check` - FAIL with Node heap OOM.
- `lizard src/tui/index.ts src/cli/doctor.ts` - FAIL remaining doctor CCN warnings: `buildMcpReport`, `buildProductKernelReport`, `printHumanFormat`, `probeMcpInitialize`, `run`.
- `bun run ci` - not re-run after targeted failures; Phase 02 summaries already record broad CI blocked by ARCH-02 raw `ProductDb` and related baseline failures.

## Deferred Issues

- Finish ARCH-02 migration for product CLI tests that still pass raw `ProductDb` into EntityManager-only repositories.
- Fix web dashboard server loader to use canonical EntityManager shape so smoke e2e can reach the client-side command palette.
- Split remaining `doctor.ts` complexity hotspots below CCN 15 in a follow-up if strict lizard compliance is still required.
- Investigate `svelte-check` memory growth in the web package.

## Known Stubs

None in files created or modified by this plan.

## Threat Flags

None. Cmd+K shortcut is client-only and constant-time as accepted in T-02-08.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Parser, doctor product-kernel warning accounting, and Cmd+K handler wiring are ready for downstream work. Verification remains blocked by pre-existing EntityManager migration issues outside this plan's intended fix scope.

## Self-Check: PASSED

- Summary file exists: `.planning/phases/02-bug-fixes-foundation/02-05-SUMMARY.md`
- Task commits found: `5da61d2a`, `3f1defc5`, `cc33f71a`
- No unexpected tracked deletions were introduced by task commits.

---
*Phase: 02-bug-fixes-foundation*
*Completed: 2026-05-04*
