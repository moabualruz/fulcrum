---
phase: 02-bug-fixes-foundation
plan: 07
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/trpc/middleware.ts
  - src/trpc/permissions.ts
  - src/server/trpc/routers/**
  - src/trpc/routers/**
  - tests/trpc/app-router-scaffold.test.ts
  - tests/trpc/router.test.ts
autonomous: true
requirements: [FND-02]

must_haves:
  truths:
    - "D-21 planner matches existing tRPC middleware shape"
    - "D-22 permission lint is hard fail in default CI"
    - "D-23 hybrid permission model uses explicit resource/action constants backed by Casbin when enabled"
    - "D-24 local-dev bypass is env/feature gated and logged"
  artifacts:
    - path: "src/trpc/permissions.ts"
      provides: "Explicit permission constants and helpers"
      contains: "resource"
---

<objective>
Guarantee every protected tRPC procedure has explicit permission enforcement and a hard CI lint gate.
</objective>

<tasks>
<task type="red">
  <name>Task 1: Add RED permission lint fixtures</name>
  <files>tests/trpc/app-router-scaffold.test.ts, tests/trpc/router.test.ts</files>
  <read_first>src/trpc/middleware.ts, tests/trpc/app-router-scaffold.test.ts, tests/trpc/router.test.ts, src/server/trpc/routers/auth.ts</read_first>
  <action>
Add RED tests:
- A protected procedure without explicit permission metadata/check fails lint
- Allowlisted public auth flows are the only public mutation exceptions
- Derived path spoofing cannot make `resource/action` bypass Casbin
- Local-dev bypass only works when env/feature flag is enabled and emits a log entry containing `permission bypass`
Commit RED tests before implementation.
  </action>
  <verify>
    <automated>bun test tests/trpc/app-router-scaffold.test.ts tests/trpc/router.test.ts</automated>
  </verify>
  <acceptance_criteria>
- RED output names missing explicit permission or unauthorized bypass
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 2: Add explicit permission helper and metadata</name>
  <files>src/trpc/middleware.ts, src/trpc/permissions.ts, src/server/trpc/routers/**, src/trpc/routers/**</files>
  <read_first>src/trpc/middleware.ts, src/trpc/trpc.ts, src/server/trpc/routers/tasks.ts, src/server/trpc/routers/auth.ts, tests/permissions/casbin-adapter.test.ts</read_first>
  <action>
Implement:
- `src/trpc/permissions.ts` exports coarse resources/actions and `permission(resource, action)`
- `protectedProcedure` supports explicit metadata or wrapper like `permissionedProcedure(resource, action)`
- `assertPermission()` prefers explicit metadata; path-derived resource/action remains fallback only during migration if test allowlist permits it
- Casbin check still runs when `casbin-policies` flag enabled
- Local-dev bypass requires env/feature flag and logs `permission bypass resource=<resource> action=<action>`
Update routers so every protected procedure has explicit resource/action.
  </action>
  <verify>
    <automated>bun test tests/trpc/app-router-scaffold.test.ts tests/trpc/router.test.ts tests/permissions/casbin-adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `bun test tests/trpc/app-router-scaffold.test.ts` exits 0
- `rg "permission\\(|resource:|action:" src/server/trpc/routers src/trpc/routers` returns matches for protected routers
- Public mutation allowlist includes only auth accept/invite-style flows with comments
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 3: Wire permission lint into default CI</name>
  <files>scripts/ci.ts, scripts/ci.test.ts</files>
  <read_first>scripts/ci.ts, scripts/ci.test.ts, tests/trpc/app-router-scaffold.test.ts</read_first>
  <action>
Add a CI step named `trpc:permissions` running `bun test tests/trpc/app-router-scaffold.test.ts tests/trpc/router.test.ts`. This step must be hard fail and part of default `bun run ci`.
  </action>
  <verify>
    <automated>bun test scripts/ci.test.ts</automated>
    <automated>bun run ci</automated>
  </verify>
  <acceptance_criteria>
- `rg "trpc:permissions" scripts/ci.ts scripts/ci.test.ts` returns matches
- CI test asserts the step is present and not soft
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Elevation of Privilege | tRPC protected procedures | mitigate | Explicit resource/action metadata plus hard lint |
| T-02-12 | Spoofing | path-derived permissions | mitigate | Metadata wins; path derivation only fallback until removed |
</threat_model>

<verification>
- `bun test tests/trpc/app-router-scaffold.test.ts tests/trpc/router.test.ts tests/permissions/casbin-adapter.test.ts`
- `bun test scripts/ci.test.ts`
- `bun run ci`
</verification>

<success_criteria>
- `assertPermission()` enforced on every protected tRPC procedure
- Permission lint hard-fails default CI
- Casbin integration and local-dev bypass behavior tested
</success_criteria>

