---
phase: 02-bug-fixes-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/install.ts
  - src/cli/uninstall.ts
  - src/cli/component.ts
  - src/cli/package-parity.ts
  - src/cli/package-mirror.ts
  - src/cli/vendor-installs.ts
  - src/cli/install.test.ts
  - src/cli/uninstall.test.ts
  - src/cli/component.test.ts
  - src/cli/package-parity.test.ts
autonomous: true
requirements: [BUG-02, BUG-06, BUG-07, BUG-08, BUG-10, BUG-11, BUG-14]

must_haves:
  truths:
    - "D-01 D-02 installer safety blocks downstream shipped-surface reliability"
    - "D-03 installer ownership bugs are one dependency cluster"
    - "No Fulcrum command removes user-owned Claude plugins, settings, marketplaces, caches, skills, or commands"
  artifacts:
    - path: "src/cli/uninstall.ts"
      provides: "Marker-gated uninstall cleanup"
      contains: "fulcrum"
---

<objective>
Make install/uninstall/component/package lifecycle operations ownership-safe and filesystem-truthful.
</objective>

<tasks>
<task type="red">
  <name>Task 1: Add RED ownership and parity tests</name>
  <files>src/cli/install.test.ts, src/cli/uninstall.test.ts, src/cli/component.test.ts, src/cli/package-parity.test.ts</files>
  <read_first>src/cli/install.ts, src/cli/uninstall.ts, src/cli/component.ts, src/cli/package-parity.ts, src/cli/claude-plugin-markers.test.ts</read_first>
  <action>
Add tests that fail on current unsafe behavior:
- User-installed Claude plugin without Fulcrum ownership marker is not uninstalled
- Claude settings cleanup removes only Fulcrum-owned keys/blocks and preserves unrelated JSON/TOML keys byte-for-byte where possible
- Claude cache/marketplace removals require Fulcrum ownership marker
- Agent install dry-run does not execute `claude plugin marketplace add` or `claude plugin install`
- Non-dry-run install requires explicit confirmation before invoking Claude CLI plugin commands
- Component status checks filesystem presence and health, not ledger state alone
- Package parity validates native roots and fails when mirror ledger says installed but native root is missing
- Vendor mirrors never overwrite top-level skill/command names owned by another package
Commit RED tests separately.
  </action>
  <verify>
    <automated>bun test src/cli/install.test.ts src/cli/uninstall.test.ts src/cli/component.test.ts src/cli/package-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
- RED output names at least one ownership marker, confirmation, filesystem status, package parity, or top-level overwrite failure
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 2: Gate Claude plugin and settings cleanup by ownership</name>
  <files>src/cli/install.ts, src/cli/uninstall.ts, src/cli/vendor-installs.ts</files>
  <read_first>src/cli/install.ts, src/cli/uninstall.ts, src/cli/claude-plugin-markers.test.ts, src/agents/registry.ts</read_first>
  <action>
Implement ownership checks:
- Define explicit Fulcrum marker key/value for Claude plugin marketplace, plugin install metadata, settings keys, and cache directories
- Uninstall removes only entries with marker
- Cleanup of Claude settings patches only Fulcrum-owned keys; it must preserve user-defined keys and formatting as much as existing parser allows
- Any Claude CLI plugin command execution requires non-dry-run and explicit confirmation parameter; dry-run records intended command without executing
- Log or return skipped cleanup entries with reason `not-owned-by-fulcrum`
  </action>
  <verify>
    <automated>bun test src/cli/install.test.ts src/cli/uninstall.test.ts src/cli/claude-plugin-markers.test.ts</automated>
  </verify>
  <acceptance_criteria>
- Tests assert user-owned plugin/cache/settings remain after uninstall
- Dry-run install test observes zero spawned Claude CLI plugin commands
- Non-dry-run without confirmation returns an error containing `confirmation required`
  </acceptance_criteria>
</task>

<task type="green">
  <name>Task 3: Make component and package status use native filesystem truth</name>
  <files>src/cli/component.ts, src/cli/package-parity.ts, src/cli/package-mirror.ts</files>
  <read_first>src/cli/component.ts, src/cli/package-parity.ts, src/cli/package-mirror.ts, src/components/ledger.ts, src/cli/vendor-packages.ts</read_first>
  <action>
Update status/parity logic:
- Component status reports installed only when native root files exist, not when ledger row exists alone
- Package parity validates native roots for each agent/package mirror and reports missing root as warning/error
- Vendor mirror copy refuses to overwrite top-level skill/command names unless the existing file has the same Fulcrum ownership marker and package id
- Return machine-readable JSON fields `owned`, `nativeExists`, `ledgerExists`, `status`, and `reason`
  </action>
  <verify>
    <automated>bun test src/cli/component.test.ts src/cli/package-parity.test.ts src/cli/vendor-packages.test.ts</automated>
  </verify>
  <acceptance_criteria>
- Ledger-only status test returns `nativeExists: false`
- Missing native root parity test fails with reason `missing-native-root`
- Top-level collision test preserves existing non-Fulcrum file contents
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Agent config files | mitigate | Marker-gated targeted cleanup only |
| T-02-06 | Elevation of Privilege | Claude CLI invocation | mitigate | Explicit confirmation before plugin commands |
</threat_model>

<verification>
- `bun test src/cli/install.test.ts src/cli/uninstall.test.ts src/cli/component.test.ts src/cli/package-parity.test.ts src/cli/vendor-packages.test.ts`
- `bun run ci`
</verification>

<success_criteria>
- User-owned Claude assets preserved
- Installer does not auto-invoke Claude plugin commands without confirmation
- Component/package status reflects native filesystem truth
</success_criteria>

