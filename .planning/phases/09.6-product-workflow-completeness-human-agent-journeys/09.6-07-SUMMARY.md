# 09.6-07 Execution Summary

## Plan

Plan 07: CLI And TUI Workflow Parity.

Objective: keep CLI and TUI as dual-primary workflow clients with explicit scope, JSON trace, and thin invocation/visualization boundaries.

## Commits

- `7db5aefd feat(09.6-07): require explicit work scope`

## Delivered

- Added CLI guard for `fulcrum work create`:
  - requires `--project` for scoped mutation, or
  - requires explicit `--all-projects` for all-projects/global intent
- Added test coverage proving missing work scope fails before caller mutation.
- Preserved explicit all-projects mode as `scope: "all"` in the command payload.
- Re-ran existing CLI human workflow, agent workflow, TUI task hub, TUI run supervision, and client boundary tests.

## Verification

- RED phase:
  - `bun test tests/cli/phase96-human-workflow.test.ts --timeout 30000`
  - Failed first because `work create` accepted missing project scope and called the mutation.
- GREEN focused phase:
  - `bun test src/application/interface-parity.test.ts tests/cli/phase96-human-workflow.test.ts tests/cli/phase96-agent-workflow.test.ts apps/tui/src/__tests__/phase96-task-detail.test.ts apps/tui/src/__tests__/phase96-run-supervision.test.ts --timeout 30000 --reporter dots`
  - Passed: `10 pass`, `0 fail`, `54 expect()`.
- Additional check:
  - `git diff --check`
  - Passed.

## Notes

- This slice tightened the scope safety gate rather than rebuilding the full CLI/TUI shell.
- Existing tests already cover `work inspect`, `runs dispatch`, TUI task relationship hub, and TUI run supervision.
- Full `bun run ci` was not rerun for this slice.
