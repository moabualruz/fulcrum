# Phase 09.6: Product Workflow Completeness + Human/Agent Journey Repair - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-08
**Phase:** 09.6-product-workflow-completeness-human-agent-journeys
**Areas discussed:** active project strictness, vertical journey, task hub, run approval, CLI/TUI shape, completion proof, project hierarchy, templates, agent authority, UAT fixture, scope containment, Web IA, docs model, dashboards, migration, naming, API graph, testing architecture, trust UI, local reset, agent-native actions, density, trace UI, search, automation, destructive actions, built-in template, work taxonomy, memory, artifacts, CLI naming, safety gates, interaction mode, UAT wording

---

## Active Project Strictness

| Option | Description | Selected |
|--------|-------------|----------|
| Strict project-first | Work surfaces require project context; globals explicit as All projects | ✓ |
| Soft project-first | Prefer active project but allow implicit globals | |
| Hybrid by domain | Some domains global by default | |

**User's choice:** Strict project-first. Global becomes explicit portfolio/root context.
**Notes:** User added project hierarchy: workspaces/root projects, main projects, subprojects.

---

## Project Hierarchy And Inheritance

| Option | Description | Selected |
|--------|-------------|----------|
| Single projects tree | `projects.parent_id`, `kind`, `path`, `depth`, inheritance config | ✓ |
| Separate concepts | Workspace/project/subproject separate models | |
| Virtual roots | Mostly flat projects plus virtual aggregation nodes | |

**User's choice:** Single projects tree.
**Notes:** Arbitrary depth supported, but default UX is root/project/subproject. Parent policies can lock inherited settings fine-grained; child overrides win when not locked.

---

## First Vertical Journey

| Option | Description | Selected |
|--------|-------------|----------|
| H1 + H2 first | Startup/project/task/work planning first | |
| H1 + H4/A1 first | Startup/project/task/run loop first | |
| Full spine first | Setup through task/docs/context/run/artifact/memory/audit/CLI/TUI | ✓ |

**User's choice:** Full spine first, but full not thin.
**Notes:** First proof must be a complete representative product workflow, not a thin demo.

---

## Template Engine

| Option | Description | Selected |
|--------|-------------|----------|
| Code-defined templates | TypeScript registry only | |
| Local markdown/YAML templates | Repo/user-editable files only | |
| Both | Built-ins plus local files | ✓ |

**User's choice:** Both.
**Notes:** Build template engine first enough to avoid hardcoded first spine. Templates can include local automation/scripts/hooks with trust gates. Formats: `FULCRUM_TEMPLATE.md` and directory `fulcrum-template.yaml`.

---

## Task Detail Relationship Hub

| Option | Description | Selected |
|--------|-------------|----------|
| Dense hub | Show all panels at once | |
| Summary + drawer | Main view plus hidden relationship drawer | partial |
| Mode-based | Planning, Docs, Agent Run, Knowledge, Audit modes | ✓ |

**User's choice:** Mode-based primary UX plus real underlying docs/records behind scenes.
**Notes:** Mandatory first spine modes: Planning, Docs, Repo/Workspace, Agent Run, Knowledge, Audit/Activity. CLI/TUI expose both matching modes and direct domain commands.

---

## Context Preview And Run Approval

| Option | Description | Selected |
|--------|-------------|----------|
| Always preview | Human preview before every dispatch | |
| Policy-based auto-dispatch | Preview default; trusted policy can auto-dispatch | ✓ |
| Auto-dispatch default | Run immediately, inspect after | |

**User's choice:** Policy-based auto-dispatch.
**Notes:** Policy comes from template defaults plus project overrides. Approval gates are workflow/profile-defined, project-overridable, with loose defaults.

---

## Agent Authority

| Option | Description | Selected |
|--------|-------------|----------|
| Propose-only | Agents read/create proposals only | |
| Policy-controlled mutations | Agents mutate allowed classes by policy | ✓ |
| Full autonomy | Broad project graph autonomy | configurable mode |

**User's choice:** Policy-controlled by default; Full autonomy configurable.
**Notes:** Effective permissions derive from agent profile identity, workflow/template defaults, project policy, and task/run overrides. User later selected admin/project actions as agent-executable when explicit policy/trust permits.

---

## CLI And TUI Shape

| Option | Description | Selected |
|--------|-------------|----------|
| `work` primary | Workflow commands default; domains advanced | |
| Domain primary | Keep existing domain commands primary | |
| Dual primary | Workflow commands plus domain commands | ✓ |

**User's choice:** Dual primary.
**Notes:** Canonical CLI domains are plural: `projects`, `tasks`, `docs`, `runs`, `artifacts`, `templates`; also `work`, `trace`, `memory`, `context`, `automations`, `audit`, `doctor`. TUI resumes last context, fallback active project workbench.

---

## API Graph Contract

| Option | Description | Selected |
|--------|-------------|----------|
| Entity + summaries | Adjacent IDs/counts only | |
| Entity + mode payloads | Mode-ready payloads in one call | |
| Selectable includes | `include=docs,runs,...` graph slices | ✓ |

**User's choice:** Selectable includes with default adjacent summaries.
**Notes:** Schema is typed trace spine plus extension links. CLI/API/tRPC use Zod schemas.

---

## Completion Proof And UAT

| Option | Description | Selected |
|--------|-------------|----------|
| Journey tests + proof doc | H1-H6/A1-A5 and proof | |
| Full gate package | Proof, startup, Web, CLI, TUI, graph, Symphony, docs | |
| User UAT required | Full gate package plus explicit user approval | ✓ |

**User's choice:** User UAT required, but code must verify all feasible cycles programmatically first.
**Notes:** Add broad E2E suites for Web/CLI/TUI/API/use cases, deterministic fake run gate, optional real local agent run. UAT output should include both checklist and scenario scripts.

---

## Scope Containment And Migration

| Option | Description | Selected |
|--------|-------------|----------|
| Preserve compatibility | Keep old routes/commands/behavior | |
| Project-first cutover | Canonical project routes; explicit portfolio only | ✓ |
| Hard remove all old surfaces | Remove anything old | as needed |

**User's choice:** No need to maintain any old anything if it does not fit product vision/workflows.
**Notes:** Product is not in use yet. Clean rebuild allowed for IA, routes, commands, screens, migrations, schema, seeds, local init.

---

## Web IA And Density

| Option | Description | Selected |
|--------|-------------|----------|
| Project tree in sidebar | Full hierarchy in sidebar | |
| Picker owns tree | Topbar picker only | |
| Both | Picker full tree; sidebar compact current scope/child shortcuts | ✓ |

**User's choice:** Both.
**Notes:** Default visual mode is minimal/simple plus balanced progressive disclosure; top advanced mode switch reveals dense operational console. Project default plus user override control density.

---

## Docs, Memory, Artifacts

| Option | Description | Selected |
|--------|-------------|----------|
| Docs first-class only | Task modes link docs | |
| Mode pages generated docs | Modes backed by generated doc records | |
| Hybrid | First-class docs plus generated mode views that can snapshot/promote | ✓ |

**User's choice:** Hybrid.
**Notes:** Full doc template set required. Memory scopes include workspace/project/subproject/task/run/doc/artifact/repo. Memory promotion policy-based, Full auto can grant direct accepted writes. Artifact lifecycle full: created, pending review, accepted, rejected, linked, promoted, archived, expired.

---

## Safety And Trust

| Option | Description | Selected |
|--------|-------------|----------|
| Destructive only | Gate delete/reset/rebuild/schema wipe | |
| Destructive + executable | Also gate scripts/hooks/shell/network | |
| Destructive + executable + escalation | Also gate Full auto, policy loosening, admin permissions, trust changes | ✓ |

**User's choice:** Gate all high-risk categories.
**Notes:** Trust labels: Manual, Assisted, Trusted, Full auto. Destructive actions are trust-mode aware with dry-run/snapshot safeguards and extra confirmation for schema/data reset unless explicitly configured.

---

## the agent's Discretion

- Exact implementation plan split and sequencing after context is planner discretion, provided full spine first and startup/doctor repair remain hard gates.
- Exact schema names and route/component file splits are implementation discretion if they preserve hierarchy, trace spine, typed links, and no-orphan constraints.
- Exact package additions are implementation discretion within researched package options and reuse-first policy.

## Deferred Ideas

- SaaS/multi-tenant hardening, RLS, billing, hosted gateway, PostgreSQL SaaS load tests.
- External Slack/Discord/Jira/Linear/GitHub full dispatch and cloud sync unless already needed for local/Symphony flow.
- Public template marketplace, remote template registry, enterprise trust infrastructure.
- Marketing/landing redesign.
