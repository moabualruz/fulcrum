---
phase: 08-surface-delivery
plan: 7
type: execute
wave: 5
depends_on: [08-02, 08-03, 08-05, 08-06]
requirements: [CLI-01, CLI-05, CLI-06, CLI-07, TUI-02, TUI-07, WEB-08, WEB-10, API-03]
autonomous: true
files_modified:
  - src/cli/__tests__/phase08-parity-smoke.test.ts
  - src/api/__tests__/phase08-api-parity.test.ts
  - src/tui/__tests__/phase08-tui-parity.test.ts
  - src/web/tests/e2e/phase08-surface-delivery.spec.ts
  - .planning/phases/08-surface-delivery/08-UAT.md
must_haves:
  truths:
    - "Final cross-surface smoke covers representative domains."
    - "`bun run ci` passes or blockers are documented with exact failing commands."
    - "Phase 08 UAT file records requirement-level pass/fail."
---

<objective>
Run final hardening across CLI/TUI/Web/API and record Phase 08 UAT status.
</objective>

<execution_context>
@.planning/phases/08-surface-delivery/08-CONTEXT.md
@.planning/phases/08-surface-delivery/08-VALIDATION.md
@package.json
@scripts/ci.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Cross-surface parity smoke</name>
  <files>src/cli/__tests__/phase08-parity-smoke.test.ts</files>
  <read_first>src/cli/__tests__/phase07-parity-smoke.test.ts, src/surfaces/parity.ts, src/trpc/router.ts</read_first>
  <action>
    Add smoke test that verifies representative flow for:
    - task list/create JSON through CLI.
    - docs list JSON through CLI.
    - repos list/sync status JSON through CLI.
    - artifacts list/download metadata JSON through CLI.
    - notifications unread/list JSON through CLI.
    - REST OpenAPI contains same domains.
    - TUI parity inventory contains same domains.
  </action>
  <verify>bun test src/cli/__tests__/phase08-parity-smoke.test.ts</verify>
  <acceptance_criteria>
    - Smoke test imports `REQUIRED_SURFACE_DOMAINS`.
    - Smoke test includes strings `tasks`, `docs`, `repos`, `artifacts`, `notifications`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove stale generated/stub paths and update graph</name>
  <files>src/cli/generated/, src/api/routes/, src/tui/app.ts, graphify-out/GRAPH_REPORT.md</files>
  <read_first>src/cli/generated-domains.ts, src/api/hono.ts, src/tui/app.ts, CLAUDE.md</read_first>
  <action>
    Remove or quarantine generated command modules still reachable only as "not wired yet".
    Remove API route stubs replaced by real handlers.
    Confirm `src/tui/app.ts` absent.
    Run `graphify update .` after code changes per project rule.
  </action>
  <verify>rg -n "not wired yet|stub store|In-memory stub|replaced by Pillar" src/cli src/api src/tui || true</verify>
  <acceptance_criteria>
    - Required runtime paths contain no `not wired yet`.
    - `test ! -f src/tui/app.ts` exits 0.
    - `graphify update .` executed after code edits.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Final CI and UAT record</name>
  <files>.planning/phases/08-surface-delivery/08-UAT.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/08-surface-delivery/08-VALIDATION.md, package.json</read_first>
  <action>
    Run:
    - `bun test src/surfaces/parity.test.ts`
    - `bun test src/cli/__tests__/phase08-cli-parity.test.ts src/cli/__tests__/phase08-parity-smoke.test.ts`
    - `bun test src/api/__tests__/phase08-api-parity.test.ts src/trpc/__tests__/schema-validation.test.ts`
    - `bun test src/tui/__tests__/phase08-opentui-gate.test.ts src/tui/__tests__/phase08-tui-parity.test.ts`
    - `cd src/web && bun run web:test`
    - Huashu gate evidence exists in `08-UI-SPEC.md`
    - `bun run ci`
    Create `08-UAT.md` listing CLI-01..07, TUI-01..08, WEB-01..11, API-01..05 with PASS/FAIL and evidence command.
  </action>
  <verify>bun run ci</verify>
  <acceptance_criteria>
    - `08-UAT.md` contains every requirement ID from CLI-01 through API-05.
    - Each requirement line contains `PASS` or `FAIL`.
    - `08-UAT.md` includes a Huashu gate note for TUI and Web.
    - Final command output for `bun run ci` is recorded in UAT notes.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| False complete from isolated tests | Cross-surface smoke and full CI gate. |
| Stale graph after code edits | Mandatory `graphify update .`. |
</threat_model>

<verification>
`bun run ci`
</verification>

<success_criteria>
Phase 08 is ready for execution completion: parity smoke, web checks, OpenAPI checks, TUI gate, and CI are green or exact blockers are recorded.
</success_criteria>
