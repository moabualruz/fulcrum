---
phase: 08-surface-delivery
plan: 3
type: execute
wave: 2
depends_on: [08-01]
requirements: [API-01, API-02, API-03, API-04, API-05]
autonomous: true
files_modified:
  - src/api/hono.ts
  - src/api/rate-limit.ts
  - src/api/routes/docs.ts
  - src/api/routes/search.ts
  - src/api/routes/artifacts.ts
  - src/api/routes/runs.ts
  - src/api/routes/memory.ts
  - src/api/__tests__/phase08-api-parity.test.ts
  - src/trpc/__tests__/schema-validation.test.ts
must_haves:
  truths:
    - "Public REST routes delegate to real tRPC/service-backed paths, not in-memory stores."
    - "`/api/v1/openapi.json` serves valid OpenAPI 3.1 JSON."
    - "Rate limit headers are emitted and keyed by caller identity/org/API key."
---

<objective>
Complete and validate the public REST/API surface using existing Hono + zod-openapi stack.
</objective>

<execution_context>
@.planning/phases/08-surface-delivery/08-CONTEXT.md
@src/api/hono.ts
@src/api/auth.ts
@src/api/routes/kernel-tasks.ts
@src/trpc/router.ts
@src/trpc/schemas/
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED API route/OpenAPI/rate-limit tests</name>
  <files>src/api/__tests__/phase08-api-parity.test.ts, src/trpc/__tests__/schema-validation.test.ts</files>
  <read_first>src/api/__tests__/repos.api.test.ts, src/trpc/__tests__/root-wiring.test.ts, src/api/hono.ts</read_first>
  <action>
    Add tests that assert:
    - `GET /api/v1/openapi.json` returns JSON with `openapi: "3.1.0"`.
    - OpenAPI paths include `/tasks`, `/docs`, `/search`, `/runs`, `/repos`, `/artifacts`, `/memory`, `/notifications`.
    - API response includes rate-limit headers for authenticated route.
    - Schema validation rejects malformed payload for at least tasks, docs, search, notifications, artifacts, repos, runs.
  </action>
  <verify>bun test src/api/__tests__/phase08-api-parity.test.ts src/trpc/__tests__/schema-validation.test.ts</verify>
  <acceptance_criteria>
    - Test file contains literal `/api/v1/openapi.json`.
    - Test file asserts `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace REST stubs with real handlers</name>
  <files>src/api/hono.ts, src/api/routes/docs.ts, src/api/routes/search.ts, src/api/routes/artifacts.ts, src/api/routes/runs.ts, src/api/routes/memory.ts</files>
  <read_first>src/api/hono.ts, src/trpc/router.ts, src/trpc/schemas/documents.ts, src/trpc/schemas/search.ts, src/trpc/schemas/artifacts.ts, src/trpc/schemas/runs.ts, src/trpc/schemas/memories.ts</read_first>
  <action>
    Update route modules so handlers call real service/tRPC/local caller paths.
    Remove in-memory arrays and comments stating "stub store" or "replaced by Pillar".
    Keep `@hono/zod-openapi` `createRoute` definitions and Zod schemas.
    Ensure auth stays on all routes except OpenAPI JSON.
  </action>
  <verify>rg -n "stub store|In-memory|replaced by Pillar|hardcoded" src/api/routes src/api/hono.ts</verify>
  <acceptance_criteria>
    - Required route modules no longer define mutable in-memory stores.
    - `src/api/hono.ts` keeps `OpenAPIHono`.
    - Handlers return data from tRPC/service path.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add Fulcrum-owned rate limiter</name>
  <files>src/api/rate-limit.ts, src/api/hono.ts, src/api/__tests__/phase08-api-parity.test.ts</files>
  <read_first>src/api/auth.ts, src/api/hono.ts, .planning/phases/08-surface-delivery/08-RESEARCH-DEPENDENCIES.md</read_first>
  <action>
    Implement fixed-window rate limiter middleware in `src/api/rate-limit.ts`.
    Key order: authenticated user id, org id, API key hash, remote address fallback.
    Default headers: `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`.
    Status 429 body shape: `{ "error": { "code": "RATE_LIMITED", "message": "rate limit exceeded" } }`.
    Mount on `/api/v1/*` after feature flag and before routes.
  </action>
  <verify>bun test src/api/__tests__/phase08-api-parity.test.ts</verify>
  <acceptance_criteria>
    - `src/api/rate-limit.ts` exports `rateLimit`.
    - 429 response includes code `RATE_LIMITED`.
    - Tests prove same identity consumes same bucket.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| API bypass via duplicate route | Single `src/api/hono.ts` route factory remains source of truth. |
| Brute force/API abuse | Identity-keyed rate limiter and 429 tests. |
| Schema confusion | Zod validation tests reject bad payloads. |
</threat_model>

<verification>
`bun test src/api/__tests__/phase08-api-parity.test.ts src/trpc/__tests__/schema-validation.test.ts`
</verification>

<success_criteria>
API-01..05 satisfied for real routes, OpenAPI, validation, webhook route continuity, and rate limiting.
</success_criteria>
