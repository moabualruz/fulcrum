---
phase: 08-surface-delivery
plan: 2
type: execute
wave: 2
depends_on: [08-01]
requirements: [CLI-01, CLI-02, CLI-03, CLI-04, CLI-05, CLI-06, CLI-07]
autonomous: true
files_modified:
  - src/cli/index.ts
  - src/cli/completion.ts
  - src/cli/commands/tasks.ts
  - src/cli/commands/sprints.ts
  - src/cli/commands/memory.ts
  - src/cli/commands/search.ts
  - src/cli/commands/artifacts.ts
  - src/cli/commands/doctor.ts
  - src/cli/__tests__/phase08-cli-parity.test.ts
must_haves:
  truths:
    - "Every required CLI domain dispatches through tRPC/local caller."
    - "`--json` emits only parseable JSON for machine mode."
    - "`fulcrum completion --shell bash|zsh|fish|powershell` emits non-empty shell scripts."
---

<objective>
Wire CLI parity for all required domains, stable JSON output, completion, and binary build smoke.
</objective>

<execution_context>
@.planning/phases/08-surface-delivery/08-CONTEXT.md
@.planning/phases/08-surface-delivery/08-RESEARCH.md
@src/cli/index.ts
@src/cli/local-caller.ts
@src/cli/arg-parser.ts
@src/cli/commands/pillar14-generated.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED CLI dispatch and JSON tests</name>
  <files>src/cli/__tests__/phase08-cli-parity.test.ts</files>
  <read_first>src/cli/product.test.ts, src/cli/commands/report.test.ts, src/cli/index.ts</read_first>
  <action>
    Extend `phase08-cli-parity.test.ts` with JSON parse tests for exact commands:
    `fulcrum tasks list --json`, `fulcrum sprints list --json`, `fulcrum docs list --json`, `fulcrum memory list --json`, `fulcrum runs list --json`, `fulcrum repos list --json`, `fulcrum artifacts list --json`, `fulcrum search query test --json`, `fulcrum notify list --json`, `fulcrum routing rules list --json`, `fulcrum inference status --json`, `fulcrum components status --json`, `fulcrum doctor --json`.
  </action>
  <verify>bun test src/cli/__tests__/phase08-cli-parity.test.ts</verify>
  <acceptance_criteria>
    - Test file contains every command string listed above.
    - Each JSON test calls `JSON.parse(stdout)` and asserts object or array.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire missing CLI domains through local caller</name>
  <files>src/cli/index.ts, src/cli/commands/tasks.ts, src/cli/commands/sprints.ts, src/cli/commands/memory.ts, src/cli/commands/search.ts, src/cli/commands/artifacts.ts, src/cli/commands/doctor.ts</files>
  <read_first>src/cli/index.ts, src/cli/commands/repos.ts, src/cli/commands/docs.ts, src/cli/commands/pillar14-generated.ts, src/cli/local-caller.ts</read_first>
  <action>
    Add top-level dispatch cases for `projects`, `tasks`, `sprints`, `memory`, `search`, `artifacts`, `components`, and `doctor` where absent.
    Each non-help path must build DB container, create local tRPC caller using existing helper, call router procedure, and cleanup in `finally`.
    Remove or bypass runtime "not wired yet" throws for required domains.
  </action>
  <verify>rg -n "case \"tasks\"|case \"sprints\"|case \"memory\"|case \"search\"|case \"artifacts\"|case \"doctor\"|case \"components\"" src/cli/index.ts</verify>
  <acceptance_criteria>
    - `src/cli/index.ts` contains dispatch cases for all required domains.
    - `rg -n "not wired yet" src/cli src/cli/commands` returns no required-domain runtime command path.
    - Commands use local tRPC caller or existing real domain command, not product-kernel stubs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add completion command and binary smoke</name>
  <files>src/cli/index.ts, src/cli/completion.ts, src/cli/__tests__/phase08-cli-parity.test.ts</files>
  <read_first>src/cli/completion.ts, src/cli/index.ts, package.json</read_first>
  <action>
    Add command `fulcrum completion --shell <shell>`.
    Supported shells exactly: `bash`, `zsh`, `fish`, `powershell`.
    Output non-empty completion script and exit 2 for unsupported shell.
    Add test asserting each supported shell output length > 50.
    Add build smoke invoking `bun run build` or `bun run build:all` according to existing CI plan.
  </action>
  <verify>bun test src/cli/__tests__/phase08-cli-parity.test.ts && bun run build</verify>
  <acceptance_criteria>
    - `fulcrum completion --shell bash` output includes `fulcrum`.
    - unsupported shell emits a non-zero exit path.
    - `bun run build` exits 0 on macOS.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| JSON mode polluted by human text | Tests parse stdout as JSON for every command. |
| Destructive command ambiguity | Mutations keep explicit ids/flags and existing confirmation behavior. |
</threat_model>

<verification>
`bun test src/cli/__tests__/phase08-cli-parity.test.ts && bun run build`
</verification>

<success_criteria>
CLI-01..07 satisfied with real command wiring, JSON, completion, and build smoke.
</success_criteria>
