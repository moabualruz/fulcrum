---
phase: 08-surface-delivery
plan: 1
type: execute
wave: 1
depends_on: []
requirements: [CLI-01, CLI-04, CLI-06, TUI-07, WEB-07, WEB-10, API-01, API-02]
autonomous: true
files_modified:
  - src/surfaces/parity.ts
  - src/surfaces/parity.test.ts
  - src/cli/__tests__/phase08-cli-parity.test.ts
  - src/api/__tests__/phase08-api-parity.test.ts
  - src/tui/__tests__/phase08-tui-parity.test.ts
must_haves:
  truths:
    - "A parity matrix exists for Web, CLI, TUI, REST/API across Phase 08 domains."
    - "Tests fail when a required domain is missing from CLI dispatch, TUI navigation, REST routes, or Web routes."
    - "No later plan starts from guesswork; parity inventory is source of truth."
---

<objective>
Create the Phase 08 parity inventory and failing gates that define "maximum feature parity" before implementation.
</objective>

<execution_context>
@.planning/phases/08-surface-delivery/08-CONTEXT.md
@.planning/phases/08-surface-delivery/08-RESEARCH.md
@.planning/phases/08-surface-delivery/08-PATTERNS.md
@src/trpc/router.ts
@src/cli/generated-domains.ts
@src/cli/index.ts
@src/api/hono.ts
@src/tui/index.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define canonical parity matrix</name>
  <files>src/surfaces/parity.ts, src/surfaces/parity.test.ts</files>
  <read_first>src/trpc/router.ts, src/cli/generated-domains.ts, src/tui/index.ts, src/api/hono.ts, src/web/src/routes/+layout.svelte</read_first>
  <action>
    Create `src/surfaces/parity.ts` exporting `REQUIRED_SURFACE_DOMAINS` with exact domains:
    `projects`, `tasks`, `sprints`, `docs`, `memory`, `runs`, `repos`, `artifacts`, `search`, `notifications`, `skills`, `routing`, `inference`, `components`, `doctor`, `auth`.
    For each domain define expected surfaces: `web`, `cli`, `tui`, and `api` where external API applies.
    Add helpers `listMissingCliDomains`, `listMissingTuiDomains`, `listMissingApiDomains`, and `listMissingWebRoutes`.
  </action>
  <verify>bun test src/surfaces/parity.test.ts</verify>
  <acceptance_criteria>
    - `src/surfaces/parity.ts` contains `REQUIRED_SURFACE_DOMAINS`.
    - `src/surfaces/parity.test.ts` imports `appRouter` and `GENERATED_DOMAIN_COMMANDS`.
    - Test asserts `tasks`, `docs`, `memory`, `runs`, `repos`, `artifacts`, `search`, and `notifications` are present in parity matrix.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CLI/API/TUI/Web inventory tests</name>
  <files>src/cli/__tests__/phase08-cli-parity.test.ts, src/api/__tests__/phase08-api-parity.test.ts, src/tui/__tests__/phase08-tui-parity.test.ts</files>
  <read_first>src/cli/index.ts, src/api/hono.ts, src/tui/index.ts, src/web/src/routes/</read_first>
  <action>
    Add tests that inspect source strings and exported inventories:
    - CLI test asserts top-level dispatch covers all required CLI domains or compatibility wrappers.
    - API test asserts `createPublicApi` registers real route groups and serves `/api/v1/openapi.json`.
    - TUI test asserts nav/screen inventory contains required domain labels.
    - Web test coverage can be in `src/surfaces/parity.test.ts` and assert route files exist for required domains.
  </action>
  <verify>bun test src/surfaces/parity.test.ts src/cli/__tests__/phase08-cli-parity.test.ts src/api/__tests__/phase08-api-parity.test.ts src/tui/__tests__/phase08-tui-parity.test.ts</verify>
  <acceptance_criteria>
    - CLI parity test fails if `src/cli/index.ts` lacks `case "tasks"` or equivalent alias.
    - API parity test fails if `src/api/hono.ts` has only stub routes for docs/search/artifacts.
    - TUI parity test fails if a required nav label is absent.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| False parity from route existence only | Tests inspect dispatch + real handler markers + JSON behavior in later plans. |
| Hidden API stubs | Inventory classifies stubs separately from real service-backed routes. |
</threat_model>

<verification>
Run `bun test src/surfaces/parity.test.ts src/cli/__tests__/phase08-cli-parity.test.ts src/api/__tests__/phase08-api-parity.test.ts src/tui/__tests__/phase08-tui-parity.test.ts`.
</verification>

<success_criteria>
Parity gaps are explicit, test-backed, and consumed by subsequent plans.
</success_criteria>
