---
phase: 08-surface-delivery
plan: 6
type: execute
wave: 4
depends_on: [08-01]
requirements: [WEB-01, WEB-02, WEB-03, WEB-04, WEB-05, WEB-06, WEB-07, WEB-08, WEB-09, WEB-10, WEB-11]
autonomous: true
files_modified:
  - src/web/tests/e2e/phase08-surface-delivery.spec.ts
  - src/web/tests/a11y/phase08-routes.test.ts
  - src/web/tests/vitest/phase08-route-render.test.ts
  - .planning/phases/08-surface-delivery/08-UI-SPEC.md
  - src/web/src/routes/settings/api/+page.server.ts
  - src/web/src/routes/settings/api/+page.svelte
must_haves:
  truths:
    - "14 required Playwright journeys are present."
    - "All Phase 5-7 routes render without server errors."
    - "Dark mode persists and collab flag UI is verified."
---

<objective>
Complete Web surface verification and UAT without redesigning existing pages.
</objective>

<execution_context>
@.planning/phases/08-surface-delivery/08-UI-SPEC.md
@src/web/tests/e2e/
@src/web/tests/a11y/
@src/web/src/routes/
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 0: Huashu expert review gate for Web completion</name>
  <files>.planning/phases/08-surface-delivery/08-UI-SPEC.md, src/web/tests/vitest/phase08-route-render.test.ts</files>
  <read_first>/Users/mkh/.agents/skills/huashu-design/references/design-context.md, /Users/mkh/.agents/skills/huashu-design/references/critique-guide.md, /Users/mkh/.agents/skills/huashu-design/references/verification.md, src/web/src/lib/components/ui/, src/web/src/routes/projects/[id]/board/+page.svelte, src/web/src/routes/search/+page.svelte, src/web/src/routes/repos/+page.svelte, src/web/src/routes/artifacts/+page.svelte, src/web/src/routes/inbox/+page.svelte</read_first>
  <action>
    Before finalizing Web UAT, apply `$huashu-design` as expert review:
    - Extract existing Web design context from shadcn-svelte/Bits UI components and Phase 5-7 routes.
    - Review route family consistency for visual hierarchy, craft, functionality, and anti-AI-slop.
    - Blocking fixes: overlapping text, illegible density, decorative filler content, generic gradient/orb decoration, inconsistent spacing system, missing operational affordances.
    - Non-blocking: purely aesthetic redesign preferences.
    - Add route-render assertions only for concrete blockers found.
  </action>
  <verify>rg -n "Huashu|visual hierarchy|craft|functionality|anti-AI-slop|route family consistency" .planning/phases/08-surface-delivery/08-UI-SPEC.md src/web/tests/vitest/phase08-route-render.test.ts</verify>
  <acceptance_criteria>
    - Web completion uses existing design context rather than a new visual direction.
    - Blocking Huashu findings become route-render or e2e assertions.
    - No new decorative landing/hero composition is introduced.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add 14 journey Playwright spec</name>
  <files>src/web/tests/e2e/phase08-surface-delivery.spec.ts</files>
  <read_first>src/web/tests/e2e/fixtures.ts, src/web/tests/e2e/phase05-task-views.spec.ts, src/web/tests/e2e/phase06-docs-memory-search.spec.ts, src/web/tests/e2e/phase07-repos-artifacts-notifications.spec.ts</read_first>
  <action>
    Create `phase08-surface-delivery.spec.ts` with tests named exactly:
    `first-time setup`, `project CRUD`, `task CRUD`, `kanban move`, `sprint management`, `doc CRUD`, `doc editing`, `search and facets`, `memory browse`, `repo management`, `artifact download`, `notification rules`, `agent dispatch`, `theme customization`.
    Reuse existing fixture and conditional skip pattern for services unavailable in isolated environment.
  </action>
  <verify>cd src/web && bun run web:e2e -- tests/e2e/phase08-surface-delivery.spec.ts</verify>
  <acceptance_criteria>
    - Spec file contains all 14 exact test names.
    - Tests visit real route paths, not placeholder pages.
    - Conditional skips include concrete route/error reason.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route render and a11y sweeps</name>
  <files>src/web/tests/vitest/phase08-route-render.test.ts, src/web/tests/a11y/phase08-routes.test.ts</files>
  <read_first>src/web/src/routes/, src/web/tests/a11y/icon-button-sweep.test.ts, src/web/tests/vitest/_smoke.test.ts</read_first>
  <action>
    Add route render tests for:
    `/projects`, `/projects/[id]/board`, `/projects/[id]/list`, `/projects/[id]/calendar`, `/projects/[id]/gantt`, `/projects/[id]/reports`, `/docs`, `/docs/[id]/edit`, `/memory`, `/search`, `/repos`, `/artifacts`, `/inbox`, `/runs`, `/settings/api`.
    Add a11y route sweep for same stable routes where fixtures can supply data.
  </action>
  <verify>cd src/web && bun run web:test -- tests/vitest/phase08-route-render.test.ts && bun run web:a11y -- tests/a11y/phase08-routes.test.ts</verify>
  <acceptance_criteria>
    - Render test includes every route string above.
    - A11y test fails on axe violations and includes icon-button sweep compatibility.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: API settings route completion and theme/collab checks</name>
  <files>src/web/src/routes/settings/api/+page.server.ts, src/web/src/routes/settings/api/+page.svelte, src/web/tests/e2e/phase08-surface-delivery.spec.ts</files>
  <read_first>src/web/src/routes/settings/api/+page.server.ts, src/web/src/routes/settings/api/+page.svelte, src/web/src/routes/+layout.svelte</read_first>
  <action>
    Ensure settings API page shows OpenAPI URL `/api/v1/openapi.json`, API key status, and rate-limit status if available.
    Add dark-mode persistence check through local storage/cookie existing mechanism.
    Add collab flag verification that presence/cursor UI appears only when collab feature flag enabled.
  </action>
  <verify>cd src/web && bun run web:test && bun run web:e2e -- tests/e2e/phase08-surface-delivery.spec.ts</verify>
  <acceptance_criteria>
    - Settings API page contains `/api/v1/openapi.json`.
    - E2E asserts theme persists after reload.
    - Collab test toggles feature flag and asserts flag-gated UI.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Route crashes hidden by unit tests | Playwright route journeys and route render sweep. |
| Accessibility regression | axe route sweep. |
</threat_model>

<verification>
`cd src/web && bun run web:test && bun run web:e2e -- tests/e2e/phase08-surface-delivery.spec.ts && bun run web:a11y -- tests/a11y/phase08-routes.test.ts`
</verification>

<success_criteria>
WEB-01..11 satisfied by route render, components, libraries, 14 journeys, dark mode, and collab flag verification.
</success_criteria>
