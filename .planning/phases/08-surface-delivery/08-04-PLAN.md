---
phase: 08-surface-delivery
plan: 4
type: execute
wave: 3
depends_on: [08-01]
requirements: [TUI-01, TUI-02, TUI-06]
autonomous: true
files_modified:
  - package.json
  - bun.lock
  - src/tui/opentui/adapter.ts
  - src/tui/opentui/adapter.test.ts
  - src/tui/__tests__/phase08-opentui-gate.test.ts
must_haves:
  truths:
    - "OpenTUI is gated before any full rewrite."
    - "`@opentui/core@0.2.2` and `@opentui/solid@0.2.2` install and build."
    - "TUI screen logic remains headless-testable."
---

<objective>
Install and prove the OpenTUI renderer path before rewriting Fulcrum's TUI.
</objective>

<execution_context>
@.planning/phases/08-surface-delivery/08-CONTEXT.md
@.planning/phases/08-surface-delivery/08-RESEARCH-DEPENDENCIES.md
@.planning/phases/08-surface-delivery/08-UI-SPEC.md
@src/tui/index.ts
@src/tui/renderer.ts
@src/tui/testing/fake-tty.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED OpenTUI adapter tests</name>
  <files>src/tui/opentui/adapter.test.ts, src/tui/__tests__/phase08-opentui-gate.test.ts</files>
  <read_first>src/tui/renderer.ts, src/tui/testing/fake-tty.ts, src/tui/index.ts</read_first>
  <action>
    Add tests for an adapter API:
    - `createFulcrumTuiRenderer({ testMode: true })` returns object with `render`, `dispose`, `writeStatus`.
    - test mode does not require real TTY.
    - dependency versions in `package.json` exactly include `@opentui/core` and `@opentui/solid`.
  </action>
  <verify>bun test src/tui/opentui/adapter.test.ts src/tui/__tests__/phase08-opentui-gate.test.ts</verify>
  <acceptance_criteria>
    - Tests fail before adapter/dependencies exist.
    - Tests do not open an interactive terminal.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install OpenTUI packages and create adapter</name>
  <files>package.json, bun.lock, src/tui/opentui/adapter.ts</files>
  <read_first>package.json, src/tui/renderer.ts, src/tui/testing/fake-tty.ts</read_first>
  <action>
    Run `bun add @opentui/core@0.2.2 @opentui/solid@0.2.2`.
    Create `src/tui/opentui/adapter.ts`.
    In non-test mode, import `createCliRenderer` from `@opentui/core`.
    In test mode, return a fake renderer matching the adapter API.
    Do not rewrite screens in this task.
  </action>
  <verify>bun test src/tui/opentui/adapter.test.ts</verify>
  <acceptance_criteria>
    - `package.json` contains `"@opentui/core": "0.2.2"` and `"@opentui/solid": "0.2.2"`.
    - `src/tui/opentui/adapter.ts` contains `createCliRenderer`.
    - `bun test src/tui/opentui/adapter.test.ts` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Prove launch/build gate</name>
  <files>src/tui/__tests__/phase08-opentui-gate.test.ts</files>
  <read_first>src/cli/index.ts, src/cli/commands/tui.ts, scripts/build-all.ts</read_first>
  <action>
    Add test or script smoke that runs `fulcrum tui --no-tui` and verifies TUI command still has non-interactive help path.
    Run `bun run build` to ensure Bun compile path still works with OpenTUI dependency present.
    If native package fails install/build, stop and document blocker in `08-04-SUMMARY.md`; do not continue to rewrite plans.
  </action>
  <verify>bun test src/tui/__tests__/phase08-opentui-gate.test.ts && bun run build</verify>
  <acceptance_criteria>
    - Non-interactive TUI smoke exits 0.
    - `bun run build` exits 0.
    - Failure path is explicit blocker, not silent fallback.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Native renderer breaks binary | Gate includes install/build tests before rewrite. |
| Interactive test hangs | Adapter supports test mode and non-TTY command path. |
</threat_model>

<verification>
`bun test src/tui/opentui/adapter.test.ts src/tui/__tests__/phase08-opentui-gate.test.ts && bun run build`
</verification>

<success_criteria>
TUI-01/TUI-02 can proceed with proven OpenTUI dependency and adapter.
</success_criteria>
