---
phase: 08-surface-delivery
plan: 5
type: execute
wave: 4
depends_on: [08-02, 08-04]
requirements: [TUI-01, TUI-02, TUI-03, TUI-04, TUI-05, TUI-06, TUI-07, TUI-08]
autonomous: true
files_modified:
  - src/tui/index.ts
  - src/tui/app.ts
  - .planning/phases/08-surface-delivery/08-UI-SPEC.md
  - src/tui/screens/index.ts
  - src/tui/screens/runs.ts
  - src/tui/screens/task-board.ts
  - src/tui/screens/docs-tree-screen.ts
  - src/tui/screens/search-screen.ts
  - src/tui/__tests__/phase08-tui-parity.test.ts
must_haves:
  truths:
    - "TUI uses OpenTUI adapter for runtime rendering."
    - "TUI screens call tRPC caller methods, not direct DB/entity imports."
    - "Live run monitor streams through subscription/EventBus path."
    - "`src/tui/app.ts` is removed after replacement."
---

<objective>
Rewrite/complete TUI surface with OpenTUI adapter and all-domain parity.
</objective>

<execution_context>
@.planning/phases/08-surface-delivery/08-UI-SPEC.md
@src/tui/index.ts
@src/tui/opentui/adapter.ts
@src/tui/screens/
@src/trpc/router.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 0: Huashu design gate for TUI composition</name>
  <files>.planning/phases/08-surface-delivery/08-UI-SPEC.md, src/tui/__tests__/phase08-tui-parity.test.ts</files>
  <read_first>/Users/mkh/.agents/skills/huashu-design/references/workflow.md, /Users/mkh/.agents/skills/huashu-design/references/design-context.md, /Users/mkh/.agents/skills/huashu-design/references/critique-guide.md, /Users/mkh/.agents/skills/huashu-design/references/verification.md, src/tui/index.ts, src/tui/screens/runs.ts, src/tui/screens/repos.ts</read_first>
  <action>
    Before rewriting screens, apply `$huashu-design` as a design-direction gate:
    - Extract exact existing TUI vocabulary from `src/tui/index.ts`, `src/tui/renderer.ts`, `src/tui/screens/`.
    - State assumptions for TUI audience: repeated operator use, keyboard-first, high information density, no marketing/hero layout.
    - Define the target layout: domain nav, list/table pane, detail/log pane, persistent status footer.
    - Score the proposed TUI composition using Huashu critique dimensions: functionality and visual hierarchy must be at least 8/10; craft at least 7/10.
    - Feed resulting concrete layout rules into implementation tests.
  </action>
  <verify>rg -n "Huashu|domain nav|detail/log pane|status footer|functionality.*8|visual hierarchy.*8" .planning/phases/08-surface-delivery/08-UI-SPEC.md src/tui/__tests__/phase08-tui-parity.test.ts</verify>
  <acceptance_criteria>
    - `08-UI-SPEC.md` contains `Huashu-Design Gate`.
    - TUI parity tests assert domain nav, detail/log pane, and status footer.
    - No task introduces decorative hero/card-heavy TUI layout.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: RED all-domain TUI navigation tests</name>
  <files>src/tui/__tests__/phase08-tui-parity.test.ts</files>
  <read_first>src/tui/index.ts, src/tui/screens/, src/tui/testing/fake-tty.ts</read_first>
  <action>
    Add tests asserting nav labels exactly include Projects, Tasks, Docs, Memory, Runs, Repos, Artifacts, Search, Notifications, Routing/Skills, Doctor/Settings.
    Add tests for keyboard keys: `j`, `k`, arrow up/down, Enter, Escape, `/`, `q`.
  </action>
  <verify>bun test src/tui/__tests__/phase08-tui-parity.test.ts</verify>
  <acceptance_criteria>
    - Test contains every nav label string.
    - Test uses FakeTTY/test renderer, not real stdin raw mode.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Connect screens to caller and OpenTUI adapter</name>
  <files>src/tui/index.ts, src/tui/screens/index.ts, src/tui/screens/task-board.ts, src/tui/screens/docs-tree-screen.ts, src/tui/screens/search-screen.ts</files>
  <read_first>src/tui/index.ts, src/tui/opentui/adapter.ts, src/tui/screens/repos.ts, src/tui/screens/notifications.ts, src/tui/screens/artifacts.ts</read_first>
  <action>
    Refactor TUI root to render through OpenTUI adapter in runtime mode.
    Keep screen state/caller methods separate from renderer specifics.
    Ensure tasks, sprints, docs, memory, search, repos, artifacts, notifications screens call `TuiCaller` methods and render loading/empty/error states.
  </action>
  <verify>bun test src/tui/__tests__/phase08-tui-parity.test.ts src/tui/screens/repos.test.ts src/tui/screens/notifications-audit.test.ts</verify>
  <acceptance_criteria>
    - `src/tui/index.ts` imports `createFulcrumTuiRenderer`.
    - `rg -n "from \"../db|from \"../../db|EntityManager|MikroORM" src/tui/screens` returns no direct screen data access.
    - Screens expose empty/error states in tests.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Live run monitor and command palette</name>
  <files>src/tui/screens/runs.ts, src/tui/index.ts, src/tui/__tests__/phase08-tui-parity.test.ts</files>
  <read_first>src/trpc/router.ts, src/subscriptions/procedures.ts, src/tui/screens/runs.ts</read_first>
  <action>
    Implement live run monitor layout:
    - run list left.
    - transcript/log pane right.
    - status footer with state, duration, agent.
    Subscribe via `runsSubscriptions` when caller exposes it; fallback to refresh button only in tests without subscription.
    Add command palette action list matching Web Cmd+K categories: create task, create doc, search, dispatch run, open settings.
  </action>
  <verify>bun test src/tui/__tests__/phase08-tui-parity.test.ts</verify>
  <acceptance_criteria>
    - `src/tui/screens/runs.ts` contains `runsSubscriptions` or caller subscription path.
    - Tests assert transcript/log text updates after synthetic event.
    - Command palette includes `Create task`, `Create doc`, `Search`, `Dispatch run`, `Settings`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Remove dead TUI root</name>
  <files>src/tui/app.ts, src/tui/__tests__/phase08-tui-parity.test.ts</files>
  <read_first>src/tui/app.ts, src/tui/index.ts, src/tui/desktop-app.ts</read_first>
  <action>
    Delete `src/tui/app.ts`.
    Update imports/tests if any still reference it.
    Add assertion that `src/tui/app.ts` does not exist.
  </action>
  <verify>test ! -f src/tui/app.ts && bun test src/tui/__tests__/phase08-tui-parity.test.ts</verify>
  <acceptance_criteria>
    - `test ! -f src/tui/app.ts` exits 0.
    - `rg -n "from \"./app|from \"../app|tui/app" src tests` returns no references.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| TUI leaks data cross-org | Caller procedures enforce auth/org; screens do not import DB directly. |
| Terminal hang | Non-TTY and FakeTTY tests retained. |
</threat_model>

<verification>
`bun test src/tui/__tests__/phase08-tui-parity.test.ts src/tui/screens/repos.test.ts src/tui/screens/notifications-audit.test.ts`
</verification>

<success_criteria>
TUI-01..08 satisfied with OpenTUI path, functional screens, live run monitor, command palette, keyboard navigation, and no dead app root.
</success_criteria>
