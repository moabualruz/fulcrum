---
phase: 09-cross-cutting-testing
plan: 1
type: execute
wave: 1
depends_on: [09-00]
requirements: [XCT-01, XCT-02]
autonomous: true
files_modified:
  - src/i18n/index.ts
  - src/i18n/locales/en.json
  - src/i18n/locales/fr.json
  - src/i18n/locales/ar.json
  - scripts/i18n-extract.ts
  - src/server/trpc/routers/theme.ts
  - src/web/src/lib/theme.ts
  - src/web/src/routes/settings/i18n/+page.server.ts
  - src/web/src/routes/settings/i18n/+page.svelte
  - src/web/src/routes/settings/theme/+page.server.ts
  - src/web/src/routes/settings/theme/+page.svelte
  - src/cli/i18n.ts
  - src/cli/theme.ts
  - src/tui/screens/i18n-screen.ts
  - src/tui/screens/theme.ts
must_haves:
  truths:
    - "Decisions covered: D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15."
    - "i18n uses stable src/i18n/index.ts adapter and supports en, fr, ar."
    - "Theme customization uses mode-watcher plus sanitized CSS variables, not a new theming package."
    - "i18n and theme are available through Web, CLI, TUI, and tRPC/API paths."
---

<objective>
Complete i18n and custom theme parity without replacing existing adapter/theme primitives.
</objective>

<execution_context>
@.planning/phases/09-cross-cutting-testing/09-CONTEXT.md
@.planning/phases/09-cross-cutting-testing/09-RESEARCH-DEPENDENCIES.md
@src/i18n/index.ts
@src/server/trpc/routers/theme.ts
@src/web/src/lib/theme.ts
@src/tui/screens/i18n-screen.ts
@tests/tui/theme.test.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Harden i18n adapter and extraction gate</name>
  <files>src/i18n/index.ts, src/i18n/locales/en.json, src/i18n/locales/fr.json, src/i18n/locales/ar.json, scripts/i18n-extract.ts, src/i18n/i18n.test.ts, scripts/i18n-extract.test.ts</files>
  <read_first>src/i18n/index.ts, src/i18n/README.md, scripts/i18n-extract.ts, scripts/i18n-extract.test.ts</read_first>
  <action>
    Keep adapter exports exactly: `t`, `setLocale`, `getLocale`, `dirForLocale`, `formatDate`, `isI18nEnabled`, `normalizeLocale`.
    Ensure catalogs contain keys for settings labels: `settings.i18n.title`, `settings.theme.title`, `settings.telemetry.title`, `settings.errors.title`, `settings.backups.title`, `settings.data.title`, `settings.secrets.title`, `settings.audit.title`.
    Ensure `dirForLocale("ar", true)` returns `rtl` and `dirForLocale("fr", true)` returns `ltr`.
    Extend extraction gate so missing keys in `fr` or `ar` fail tests.
  </action>
  <verify>bun test src/i18n/i18n.test.ts scripts/i18n-extract.test.ts</verify>
  <acceptance_criteria>
    - `src/i18n/index.ts` exports `normalizeLocale`.
    - `src/i18n/locales/ar.json` contains `settings.i18n.title`.
    - `scripts/i18n-extract.test.ts` contains an assertion for missing locale keys.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire i18n and theme across Web/CLI/TUI</name>
  <files>src/web/src/routes/settings/i18n/+page.server.ts, src/web/src/routes/settings/i18n/+page.svelte, src/web/src/routes/settings/theme/+page.server.ts, src/web/src/routes/settings/theme/+page.svelte, src/cli/i18n.ts, src/cli/theme.ts, src/tui/screens/i18n-screen.ts, src/tui/screens/theme.ts</files>
  <read_first>src/server/trpc/routers/theme.ts, src/web/src/routes/settings/theme/theme.ts, src/cli/generated/theme.ts, tests/tui/i18n-screen.test.ts, tests/tui/theme.test.ts</read_first>
  <action>
    Add or harden commands:
    - `fulcrum i18n list --json` returns `{"locales":["en","fr","ar"],"defaultLocale":"en"}`.
    - `fulcrum i18n set --locale fr --json` returns `{"locale":"fr","dir":"ltr"}`.
    - `fulcrum theme list --json` returns array entries with `key`, `value`, `defaultValue`.
    - `fulcrum theme set --key theme.accent --value #2563EB --json` returns updated setting.
    Web settings pages must call existing server/tRPC paths and apply locale/theme without route reload errors.
    TUI screens must render locale/theme values as plain text labels.
  </action>
  <verify>bun test tests/cli/cross-cutting-platform.test.ts tests/tui/i18n-screen.test.ts tests/tui/theme.test.ts tests/trpc/theme.test.ts</verify>
  <acceptance_criteria>
    - `src/cli/i18n.ts` contains `list` and `set` command handling.
    - `src/cli/theme.ts` contains `theme.accent`.
    - `src/tui/screens/i18n-screen.ts` renders `en`, `fr`, and `ar`.
    - `src/web/src/lib/theme.ts` still contains `SAFE_VAR` and `SAFE_VALUE`.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| CSS injection through custom theme values | Preserve `SAFE_VAR` and `SAFE_VALUE`; validate router inputs. |
| SSR locale race from global mutable state | Keep adapter scoped and test route reload/persist behavior. |
</threat_model>

<verification>
Run `bun test src/i18n/i18n.test.ts scripts/i18n-extract.test.ts tests/cli/cross-cutting-platform.test.ts tests/tui/i18n-screen.test.ts tests/tui/theme.test.ts tests/trpc/theme.test.ts`.
</verification>

<success_criteria>
XCT-01 and XCT-02 are available and tested across Web, CLI, TUI, and tRPC paths.
</success_criteria>
