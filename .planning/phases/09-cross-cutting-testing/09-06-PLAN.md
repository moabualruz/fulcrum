---
phase: 09-cross-cutting-testing
plan: 6
type: execute
wave: 2
depends_on: [09-00]
requirements: [XCT-10, XCT-12, TST-01]
autonomous: true
files_modified:
  - src/db/migrator-service.ts
  - src/db/migrations/
  - tests/db/migration-downgrade.test.ts
  - src/platform/graceful-shutdown.ts
  - tests/platform/graceful-shutdown.test.ts
  - scripts/ci.ts
must_haves:
  truths:
    - "Decisions covered: D-35, D-36, D-37, D-38."
    - "Migration downgrade is documented by executable tests, not prose only."
    - "Graceful shutdown handles PGlite integrity, jobs, subscriptions, and workspace cleanup."
    - "Infrastructure tests run in local CI."
---

<objective>
Add migration downgrade and graceful shutdown gates for infrastructure safety.
</objective>

<execution_context>
@.planning/phases/09-cross-cutting-testing/09-CONTEXT.md
@src/db/migrator-service.ts
@src/db/migrations/
@scripts/ci.ts
@src/subscriptions/event-bus.ts
@src/orchestration/symphony/workspace.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add migration downgrade smoke</name>
  <files>tests/db/migration-downgrade.test.ts, src/db/migrator-service.ts</files>
  <read_first>src/db/migrator-service.ts, tests/db/migrator-service.test.ts, src/db/migrations/Migration20260507001.ts, src/db/migration-checksums.ts</read_first>
  <action>
    Create `tests/db/migration-downgrade.test.ts` that:
    - creates in-memory PGlite test DB.
    - migrates up to latest.
    - records latest migration name from `SchemaMigration`.
    - migrates down one step using MikroORM migrator API.
    - migrates up again.
    - asserts migration checksum ledger is valid and schema queries still work.
    Ensure every new Phase 09 migration includes a `down()` method or the test fails with migration filename.
  </action>
  <verify>bun test tests/db/migration-downgrade.test.ts</verify>
  <acceptance_criteria>
    - Test file contains `migrateDown`.
    - Test file asserts `down()` exists.
    - Test file imports `SchemaMigration`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement graceful shutdown coordinator</name>
  <files>src/platform/graceful-shutdown.ts, tests/platform/graceful-shutdown.test.ts</files>
  <read_first>src/subscriptions/event-bus.ts, src/db/context.ts, src/orchestration/symphony/workspace.ts, tests/workers/registry.test.ts</read_first>
  <action>
    Create `src/platform/graceful-shutdown.ts` exporting `createGracefulShutdown`.
    It must accept hooks:
    `closeHttpServer`, `closeDatabase`, `stopWorkers`, `closeSubscriptions`, `cleanupWorkspaces`, `log`.
    It must expose `shutdown(signal)` and return `{ ok: true, signal, completed: [...] }`.
    It must be idempotent: repeated `shutdown("SIGTERM")` returns same completion result without running hooks twice.
    It must run hooks in order: `stopWorkers`, `closeSubscriptions`, `closeHttpServer`, `closeDatabase`, `cleanupWorkspaces`.
  </action>
  <verify>bun test tests/platform/graceful-shutdown.test.ts</verify>
  <acceptance_criteria>
    - `src/platform/graceful-shutdown.ts` exports `createGracefulShutdown`.
    - Test asserts hook order.
    - Test asserts idempotency for repeated signal.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add infrastructure gates to CI</name>
  <files>scripts/ci.ts</files>
  <read_first>scripts/ci.ts, scripts/ci.test.ts, tests/db/migration-downgrade.test.ts, tests/platform/graceful-shutdown.test.ts</read_first>
  <action>
    Add CI steps:
    - name `migration:downgrade`, command `bun test tests/db/migration-downgrade.test.ts`.
    - name `graceful:shutdown`, command `bun test tests/platform/graceful-shutdown.test.ts`.
    Keep these before `build:all`.
  </action>
  <verify>bun test scripts/ci.test.ts tests/db/migration-downgrade.test.ts tests/platform/graceful-shutdown.test.ts</verify>
  <acceptance_criteria>
    - `scripts/ci.ts` contains `migration:downgrade`.
    - `scripts/ci.ts` contains `graceful:shutdown`.
    - `scripts/ci.test.ts` passes.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Failed downgrade corrupts production schema | Up/down smoke and checksum validation. |
| Shutdown interrupts DB/jobs mid-write | Ordered hooks, idempotent shutdown, tests for completion result. |
</threat_model>

<verification>
Run `bun test tests/db/migration-downgrade.test.ts tests/platform/graceful-shutdown.test.ts scripts/ci.test.ts`.
</verification>

<success_criteria>
XCT-10, XCT-12, and TST-01 are covered by executable local infrastructure gates.
</success_criteria>
