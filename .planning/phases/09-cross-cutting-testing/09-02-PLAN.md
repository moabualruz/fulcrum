---
phase: 09-cross-cutting-testing
plan: 2
type: execute
wave: 1
depends_on: [09-00]
requirements: [XCT-08, XCT-09, TST-03, TST-04]
autonomous: true
files_modified:
  - src/web/tests/a11y/phase09-cross-cutting.test.ts
  - src/web/tests/e2e/phase09-accessibility.spec.ts
  - tests/tui/accessibility.test.ts
  - src/tui/screens/
must_haves:
  truths:
    - "Web core cross-cutting settings pass WCAG 2.1 AA axe checks."
    - "TUI screens are keyboard navigable and do not rely on color-only state."
    - "Accessibility gates run in local CI."
---

<objective>
Add Web WCAG 2.1 AA and TUI accessibility gates for cross-cutting surfaces.
</objective>

<execution_context>
@.planning/phases/09-cross-cutting-testing/09-CONTEXT.md
@src/web/tests/a11y/phase08-routes.test.ts
@src/web/tests/a11y/icon-button-sweep.test.ts
@src/tui/testing/fake-tty.ts
@tests/tui/settings-screens.test.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add Web WCAG 2.1 AA route sweep</name>
  <files>src/web/tests/a11y/phase09-cross-cutting.test.ts</files>
  <read_first>src/web/tests/a11y/phase08-routes.test.ts, src/web/playwright.config.ts, src/web/src/routes/settings/theme/+page.svelte, src/web/src/routes/audit/+page.svelte</read_first>
  <action>
    Create Playwright axe test covering exact routes:
    `/settings/i18n`, `/settings/theme`, `/settings/telemetry`, `/settings/errors`, `/settings/backups`, `/settings/data`, `/settings/secrets`, `/audit`, `/settings/database/migrations`.
    Use `new AxeBuilder({ page }).withTags(["wcag2a", "wcag2aa", "wcag21a", "wcag21aa"])`.
    Assert zero violations per route.
  </action>
  <verify>cd src/web && bun run web:a11y</verify>
  <acceptance_criteria>
    - `src/web/tests/a11y/phase09-cross-cutting.test.ts` contains `wcag21aa`.
    - Test file contains `/settings/theme` and `/settings/secrets`.
    - Test uses `AxeBuilder`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add keyboard/focus E2E accessibility checks</name>
  <files>src/web/tests/e2e/phase09-accessibility.spec.ts</files>
  <read_first>src/web/tests/e2e/phase08-surface-delivery.spec.ts, src/web/tests/e2e/fixtures.ts, src/web/src/routes/+layout.svelte</read_first>
  <action>
    Add tests that use keyboard only:
    - Tab to settings navigation.
    - Open theme settings, change dark/light mode, assert focus remains visible.
    - Open audit filters, apply one filter, close menu with Escape.
    - Verify icon-only buttons on cross-cutting settings have accessible names through `getByRole`.
  </action>
  <verify>cd src/web && bun run web:e2e:full -- phase09-accessibility.spec.ts</verify>
  <acceptance_criteria>
    - Test contains `page.keyboard.press("Tab")`.
    - Test contains `page.keyboard.press("Escape")`.
    - Test uses `getByRole`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add TUI accessibility contract tests</name>
  <files>tests/tui/accessibility.test.ts, src/tui/screens/</files>
  <read_first>src/tui/testing/fake-tty.ts, tests/tui/theme.test.ts, tests/tui/settings-screens.test.ts, src/tui/screens/index.ts</read_first>
  <action>
    Create `tests/tui/accessibility.test.ts` asserting:
    - every settings screen is reachable with keyboard input through FakeTTY.
    - selected/focused row includes visible text marker, not only ANSI color.
    - high contrast theme renders labels `High contrast` and status words `enabled` or `disabled`.
    - icon/status cells include plain text labels like `error`, `warning`, `success`, `muted`, `active`.
  </action>
  <verify>bun test tests/tui/accessibility.test.ts</verify>
  <acceptance_criteria>
    - Test imports `FakeTTY`.
    - Test asserts `High contrast`.
    - Test asserts at least one non-color status label.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Automated axe misses keyboard traps | Add explicit Playwright keyboard tests. |
| TUI color-only statuses exclude users | FakeTTY tests assert text labels and high contrast. |
</threat_model>

<verification>
Run `cd src/web && bun run web:a11y`; run `bun test tests/tui/accessibility.test.ts`.
</verification>

<success_criteria>
XCT-08, XCT-09, TST-03, and TST-04 have executable accessibility gates.
</success_criteria>
