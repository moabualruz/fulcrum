---
phase: 09-cross-cutting-testing
plan: 0
type: tdd
wave: 0
depends_on: []
requirements: [XCT-01, XCT-02, XCT-03, XCT-04, XCT-05, XCT-06, XCT-07, XCT-08, XCT-09, XCT-10, XCT-11, XCT-12, TST-01, TST-02, TST-03, TST-04, TST-05, TST-06, TST-07, TST-08, TST-09, TST-10]
autonomous: true
files_modified:
  - src/platform/cross-cutting-parity.ts
  - tests/platform/cross-cutting-parity.test.ts
  - tests/platform/phase09-red-gates.test.ts
must_haves:
  truths:
    - "Decisions covered: D-01, D-02, D-03, D-04, D-05, D-06, D-07."
    - "A Phase 09 parity matrix exists for i18n, theme, telemetry, errors, backup, data, secrets, audit, migrations, shutdown, and coverage."
    - "Tests fail when a required cross-cutting capability lacks Web, CLI, TUI, or API/tRPC coverage."
    - "Every XCT-01..12 and TST-01..10 requirement is represented before implementation plans start."
---

<objective>
Create Phase 09 RED gates and parity inventory before feature hardening.
</objective>

<execution_context>
@.planning/phases/09-cross-cutting-testing/09-CONTEXT.md
@.planning/phases/09-cross-cutting-testing/09-RESEARCH.md
@.planning/phases/09-cross-cutting-testing/09-PATTERNS.md
@src/trpc/router.ts
@src/cli/index.ts
@src/tui/index.ts
@src/web/src/routes/
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define cross-cutting parity matrix</name>
  <files>src/platform/cross-cutting-parity.ts, tests/platform/cross-cutting-parity.test.ts</files>
  <read_first>src/surfaces/parity.ts, .planning/phases/09-cross-cutting-testing/09-CONTEXT.md, src/trpc/router.ts, src/cli/index.ts, src/tui/index.ts</read_first>
  <action>
    Create `src/platform/cross-cutting-parity.ts` exporting `CROSS_CUTTING_CAPABILITIES`.
    Include exact capability ids: `i18n`, `theme`, `telemetry`, `errors`, `backup`, `data-import-export`, `secrets`, `audit`, `migration-downgrade`, `graceful-shutdown`, `coverage`.
    For each capability define required surfaces: `web`, `cli`, `tui`, `trpc`, and `rest` where file/download endpoints apply.
    Add helpers `missingCrossCuttingSurfaces`, `capabilitiesForRequirement`, and `requirementsWithoutCapabilities`.
  </action>
  <verify>bun test tests/platform/cross-cutting-parity.test.ts</verify>
  <acceptance_criteria>
    - `src/platform/cross-cutting-parity.ts` contains `CROSS_CUTTING_CAPABILITIES`.
    - `CROSS_CUTTING_CAPABILITIES` contains `i18n`, `theme`, `telemetry`, `errors`, `backup`, `data-import-export`, `secrets`, `audit`, `migration-downgrade`, `graceful-shutdown`, `coverage`.
    - `tests/platform/cross-cutting-parity.test.ts` asserts all XCT-01 through XCT-12 are mapped.
    - `tests/platform/cross-cutting-parity.test.ts` asserts all TST-01 through TST-10 are mapped.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED gates for missing parity and coverage</name>
  <files>tests/platform/phase09-red-gates.test.ts</files>
  <read_first>scripts/ci.ts, bunfig.toml, src/web/vitest.config.ts, src/web/playwright.config.ts, src/web/tests/a11y/phase08-routes.test.ts</read_first>
  <action>
    Create tests that assert:
    - root CI has coverage gate stage named `coverage`.
    - web Vitest config has `coverage.thresholds.lines` set to `80`.
    - `scripts/ci.ts` includes `web:a11y`.
    - `scripts/ci.ts` includes a migration downgrade test command.
    - `scripts/ci.ts` includes a graceful shutdown test command.
    These tests are expected to start RED if implementation is missing.
  </action>
  <verify>bun test tests/platform/phase09-red-gates.test.ts</verify>
  <acceptance_criteria>
    - Test file contains literal `coverage.thresholds.lines`.
    - Test file contains literal `web:a11y`.
    - Test file contains literal `migration downgrade`.
    - Test file contains literal `graceful shutdown`.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| False parity from docs only | Static tests inspect source files and route/command inventories. |
| Phase 09 scope drops requirements | Requirement-capability mapping covers every XCT/TST ID. |
</threat_model>

<verification>
Run `bun test tests/platform/cross-cutting-parity.test.ts tests/platform/phase09-red-gates.test.ts`.
</verification>

<success_criteria>
Phase 09 has failing gates that force implementation plans to close all cross-cutting and testing requirements.
</success_criteria>
