---
phase: 09-cross-cutting-testing
plan: 3
type: execute
wave: 1
depends_on: [09-00]
requirements: [XCT-03, XCT-04, TST-06]
autonomous: true
files_modified:
  - src/server/trpc/routers/telemetry.ts
  - src/platform/remote-telemetry.ts
  - src/errors/reporter.ts
  - src/server/trpc/routers/error-logs.ts
  - src/cli/telemetry.ts
  - src/cli/generated/errorLogs.ts
  - src/tui/screens/settings-telemetry.ts
  - src/tui/screens/settings-errors.ts
  - tests/orchestration/otel-telemetry.test.ts
must_haves:
  truths:
    - "Decisions covered: D-20, D-21, D-22, D-23."
    - "Telemetry is opt-in only and local-first."
    - "Remote telemetry and error reporting never send when feature flag or opt-in is off."
    - "Error reporting scrubs paths and PII before signed delivery."
---

<objective>
Complete telemetry opt-in and local error reporting parity with optional, signed remote delivery.
</objective>

<execution_context>
@.planning/phases/09-cross-cutting-testing/09-CONTEXT.md
@src/server/trpc/routers/telemetry.ts
@src/platform/remote-telemetry.ts
@src/errors/reporter.ts
@src/orchestration/symphony/telemetry.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Persist and audit telemetry opt-in state</name>
  <files>src/server/trpc/routers/telemetry.ts, tests/trpc/telemetry.test.ts</files>
  <read_first>src/server/trpc/routers/telemetry.ts, src/db/entities/platform/TelemetryEvent.ts, src/db/entities/TenantSetting.ts, tests/trpc/telemetry.test.ts</read_first>
  <action>
    Replace in-memory `optedInByOrg` behavior with persisted org-scoped setting through existing settings/entity path.
    `telemetry.status` must return `opted_in` and `row_count`.
    `telemetry.optIn`, `telemetry.optOut`, and `telemetry.purge` must emit audit events with verbs `opted_in`, `opted_out`, `purged`.
    Preserve `scrubTelemetryPayload` so all string values become `null`.
  </action>
  <verify>bun test tests/trpc/telemetry.test.ts src/platform/remote-telemetry.test.ts</verify>
  <acceptance_criteria>
    - `src/server/trpc/routers/telemetry.ts` no longer stores opt-in only in a request-local `Map`.
    - `tests/trpc/telemetry.test.ts` asserts opt-in persists across router/store instances.
    - `tests/trpc/telemetry.test.ts` asserts string payload values are scrubbed.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden error reporting and remote worker contract</name>
  <files>src/errors/reporter.ts, src/server/trpc/routers/error-logs.ts, src/errors/reporter.test.ts, tests/trpc/errorLogs.test.ts</files>
  <read_first>src/errors/reporter.ts, src/server/trpc/routers/error-logs.ts, src/db/entities/platform/ErrorLog.ts</read_first>
  <action>
    Ensure `buildReportPayload` omits `user_id`, `org_id`, email, token, secret, password, api_key, and file contents.
    Ensure stack traces scrub `/Users/<name>/`, `/home/<name>/`, and `C:\Users\<name>\`.
    Ensure `enqueueErrorReport` no-ops when `featureEnabled=false` or endpoint is missing.
    Ensure signed remote payload uses `X-Fulcrum-Signature` HMAC-SHA256.
  </action>
  <verify>bun test src/errors/reporter.test.ts tests/trpc/errorLogs.test.ts</verify>
  <acceptance_criteria>
    - `src/errors/reporter.test.ts` asserts `<homedir>` replacement.
    - `src/errors/reporter.test.ts` asserts no enqueue when feature disabled.
    - `src/errors/reporter.ts` contains `X-Fulcrum-Signature`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire telemetry/error surfaces</name>
  <files>src/cli/telemetry.ts, src/cli/generated/errorLogs.ts, src/tui/screens/settings-telemetry.ts, src/tui/screens/settings-errors.ts, src/web/src/routes/settings/telemetry/+page.svelte, src/web/src/routes/settings/errors/+page.svelte</files>
  <read_first>tests/cli/cross-cutting-platform.test.ts, tests/tui/settings-screens.test.ts, src/web/tests/vitest/settings-telemetry-route.test.ts, src/web/tests/vitest/settings-errors-route.test.ts</read_first>
  <action>
    Ensure:
    - `fulcrum telemetry status --json`, `opt-in --json`, `opt-out --json`, `purge --json`.
    - `fulcrum error-logs list --json`, `get <id> --json`, `purge --json`.
    - TUI telemetry screen shows `opted in`, `opted out`, `row count`.
    - TUI errors screen shows error message, occurred_at, scrubbed stack.
    - Web settings pages expose same actions and render server data.
  </action>
  <verify>bun test tests/cli/cross-cutting-platform.test.ts tests/tui/settings-screens.test.ts && cd src/web && bun run web:test -- settings-telemetry-route.test.ts settings-errors-route.test.ts</verify>
  <acceptance_criteria>
    - CLI tests parse JSON from telemetry status.
    - TUI tests assert `opted` and `errors`.
    - Web Vitest tests assert telemetry and errors routes render without throwing.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| PII leaks through telemetry or errors | Scrub payloads; tests assert omitted keys/path replacement. |
| Remote reporting violates local-first default | Feature flag and opt-in gates; no-op tests when off. |
</threat_model>

<verification>
Run `bun test tests/trpc/telemetry.test.ts src/platform/remote-telemetry.test.ts src/errors/reporter.test.ts tests/trpc/errorLogs.test.ts tests/orchestration/otel-telemetry.test.ts`.
</verification>

<success_criteria>
XCT-03, XCT-04, and TST-06 are covered by local-first observability tests and parity surfaces.
</success_criteria>
