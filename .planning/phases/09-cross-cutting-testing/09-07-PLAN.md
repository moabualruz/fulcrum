---
phase: 09-cross-cutting-testing
plan: 7
type: execute
wave: 3
depends_on: [09-00, 09-01, 09-02, 09-03, 09-04, 09-05, 09-06]
requirements: [TST-02, TST-05, TST-07, TST-09]
autonomous: true
files_modified:
  - tests/trpc/all-routers-contract.test.ts
  - tests/cli/phase09-all-domains.test.ts
  - tests/symphony/spec-lock.test.ts
  - tests/platform/gate-regressions.test.ts
must_haves:
  truths:
    - "Every tRPC router has integration coverage."
    - "Every CLI domain has command coverage with JSON output."
    - "Symphony conformance remains green."
    - "Gate review regression tests cover known CF/F bugs."
---

<objective>
Create comprehensive tRPC, CLI, Symphony, and gate regression coverage.
</objective>

<execution_context>
@.planning/phases/09-cross-cutting-testing/09-CONTEXT.md
@src/trpc/router.ts
@src/cli/generated-domains.ts
@scripts/gen-conformance-trace.ts
@tests/infrastructure/p1-coverage-matrix.test.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add all-router tRPC contract gate</name>
  <files>tests/trpc/all-routers-contract.test.ts</files>
  <read_first>src/trpc/router.ts, tests/trpc/router.test.ts, tests/trpc/schemas.test.ts, src/test-utils/trpc.ts</read_first>
  <action>
    Create test that introspects `appRouter._def.procedures` and asserts every router namespace has at least one integration test file in `tests/trpc/`.
    Required namespaces include: `tasks`, `sprints`, `docs`, `memory`, `repos`, `artifacts`, `search`, `notifications`, `audit`, `backup`, `telemetry`, `theme`, `errorLogs`, `credentials`, `routing`, `skills`, `inference`.
    For each required namespace, assert matching test filename exists or explicit allowlist entry exists with reason.
  </action>
  <verify>bun test tests/trpc/all-routers-contract.test.ts</verify>
  <acceptance_criteria>
    - Test contains `appRouter`.
    - Test contains `audit`, `backup`, `telemetry`, `theme`, `errorLogs`.
    - Test fails when required namespace has no test file and no reason.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add all-domain CLI JSON coverage gate</name>
  <files>tests/cli/phase09-all-domains.test.ts</files>
  <read_first>src/cli/generated-domains.ts, src/cli/index.ts, tests/cli/codegen-gate.test.ts, tests/cli/completion.test.ts</read_first>
  <action>
    Create test covering 15 CLI domains from REQUIREMENTS.md CLI-04:
    `projects`, `tasks`, `docs`, `memory`, `runs`, `repos`, `artifacts`, `search`, `notifications`, `skills`, `router`, `symphony`, `inference`, `components`, `doctor`.
    Add Phase 09 domains: `i18n`, `theme`, `telemetry`, `backup`, `data`, `audit`, `secrets`.
    Assert each domain has at least one `--json` smoke or generated test.
  </action>
  <verify>bun test tests/cli/phase09-all-domains.test.ts</verify>
  <acceptance_criteria>
    - Test contains all 15 domain strings.
    - Test contains `--json`.
    - Test contains Phase 09 domains `i18n`, `theme`, `telemetry`, `backup`, `data`, `audit`, `secrets`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add gate regression suite</name>
  <files>tests/platform/gate-regressions.test.ts</files>
  <read_first>tests/infrastructure/p1-coverage-matrix.test.ts, tests/trpc/app-router-scaffold.test.ts, scripts/ci.ts, scripts/ci-schemas.ts</read_first>
  <action>
    Create regression tests for prior gate failure classes:
    - permission gate: every protected procedure has permission metadata.
    - schema gate: public Zod schemas do not use `z.any()` fields.
    - stub gate: no `not wired yet`, `stub store`, `In-memory stub` in runtime CLI/API/TUI paths.
    - router alias gate: no duplicate root router aliases for same domain.
    - compression/skill gate remains in CI if present.
  </action>
  <verify>bun test tests/platform/gate-regressions.test.ts tests/symphony/spec-lock.test.ts src/orchestration/__tests__/symphony-conformance.test.ts</verify>
  <acceptance_criteria>
    - Test contains `not wired yet`.
    - Test contains `z.any`.
    - Test imports or runs permission gate helper.
    - Symphony spec lock and conformance commands pass.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Router/CLI gaps hidden by broad CI | Dedicated inventory gates fail on missing domains. |
| Past bugs regress | Regression suite codifies CF/F gate classes. |
</threat_model>

<verification>
Run `bun test tests/trpc/all-routers-contract.test.ts tests/cli/phase09-all-domains.test.ts tests/platform/gate-regressions.test.ts tests/symphony/spec-lock.test.ts src/orchestration/__tests__/symphony-conformance.test.ts`.
</verification>

<success_criteria>
TST-02, TST-05, TST-07, and TST-09 are represented by hard local tests.
</success_criteria>
