---
phase: 09-cross-cutting-testing
plan: 5
type: execute
wave: 2
depends_on: [09-00, 09-03, 09-04]
requirements: [XCT-07, XCT-11]
autonomous: true
files_modified:
  - src/secrets/vault.ts
  - src/secrets/keyring.ts
  - src/secrets/vault-adapter.ts
  - src/secrets/credentials-router.ts
  - src/server/trpc/routers/audit.ts
  - src/platform/audit-events.ts
  - src/cli/audit.ts
  - src/cli/secrets.ts
  - src/tui/screens/audit.ts
  - src/tui/screens/settings-secrets.ts
must_haves:
  truths:
    - "Decisions covered: D-28, D-29, D-30, D-31, D-32, D-33, D-34."
    - "All secret kinds are encrypted at rest and never exported plaintext."
    - "Every cross-cutting mutation emits an audit event with no plaintext secrets."
    - "Audit log is queryable/exportable with retention policy."
---

<objective>
Verify secret encryption-at-rest and complete audit logging coverage across Phase 09 mutations.
</objective>

<execution_context>
@.planning/phases/09-cross-cutting-testing/09-CONTEXT.md
@src/secrets/vault.ts
@src/secrets/keyring.ts
@src/secrets/vault-adapter.ts
@src/server/trpc/routers/audit.ts
@src/platform/audit-events.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Verify secret encryption and provider fallback</name>
  <files>src/secrets/vault.ts, src/secrets/keyring.ts, src/secrets/vault-adapter.ts, tests/secrets/vault.test.ts, tests/secrets/keyring.test.ts, tests/secrets/vault-adapter.test.ts</files>
  <read_first>src/secrets/vault.ts, src/secrets/keyring.ts, src/secrets/vault-adapter.ts, tests/secrets/vault.test.ts, tests/secrets/keyring.test.ts</read_first>
  <action>
    Ensure tests cover:
    - `ALGO_LABEL === "nacl-secretbox"`.
    - `KDF_LABEL === "pbkdf2-sha256"`.
    - `KDF_ITERATIONS === 100_000`.
    - encrypted envelope differs for same plaintext due to random nonce.
    - tampered envelope throws `DecryptionFailedError`.
    - fallback key file mode is `0600`.
    - Vault/AWS provider resolves only when `FULCRUM_FEATURES` contains `vault-integration`.
  </action>
  <verify>bun test tests/secrets/vault.test.ts tests/secrets/keyring.test.ts tests/secrets/vault-adapter.test.ts</verify>
  <acceptance_criteria>
    - Tests assert `nacl-secretbox`.
    - Tests assert `pbkdf2-sha256`.
    - Tests assert fallback key mode `0600`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add audit event coverage for cross-cutting mutations</name>
  <files>src/platform/audit-events.ts, src/server/trpc/routers/audit.ts, tests/trpc/audit.test.ts, tests/e2e/notifications-audit-pipeline.test.ts</files>
  <read_first>src/platform/audit-events.ts, src/server/trpc/routers/audit.ts, src/db/entities/core/Event.ts, tests/trpc/audit.test.ts</read_first>
  <action>
    Register payload schemas for exact audit keys:
    `user_setting.updated`, `theme.updated`, `telemetry_event.opted_in`, `telemetry_event.opted_out`, `telemetry_event.purged`, `error_log.created`, `backup.created`, `backup.restored`, `backup.exported`, `backup.imported`, `credential.created`, `credential.updated`, `credential.rotated`, `credential.deleted`, `migration.downgraded`, `system.shutdown.completed`.
    Ensure each schema rejects fields named `value`, `secret`, `token`, `password`, `apiKey`, `api_key`, `encrypted_value`.
  </action>
  <verify>bun test tests/trpc/audit.test.ts tests/e2e/notifications-audit-pipeline.test.ts</verify>
  <acceptance_criteria>
    - `src/platform/audit-events.ts` contains `credential.rotated`.
    - Tests assert secret-like payload keys are rejected.
    - Tests assert audit export supports `csv` and `json`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire secrets and audit surfaces</name>
  <files>src/cli/secrets.ts, src/cli/audit.ts, src/tui/screens/settings-secrets.ts, src/tui/screens/audit.ts, src/web/src/routes/settings/secrets/+page.svelte, src/web/src/routes/audit/+page.svelte</files>
  <read_first>tests/trpc/credentials.test.ts, tests/cli/runs-notify-audit-webhooks.test.ts, tests/tui/notifications-audit.test.ts, src/web/src/routes/settings/secrets/page.server.test.ts, src/web/src/routes/audit/page.server.test.ts</read_first>
  <action>
    Ensure:
    - `fulcrum secrets set --name <name> --value <value> --json` returns metadata only, not plaintext.
    - `fulcrum secrets rotate --name <name> --json` returns provider/status metadata only.
    - `fulcrum audit query --json`, `export --format csv`, `export --format json`, `retention set --days <n> --json`.
    - TUI secrets screen never renders secret value.
    - Web audit filters include actor, subject kind, verb, date range, project.
  </action>
  <verify>bun test tests/trpc/credentials.test.ts tests/cli/runs-notify-audit-webhooks.test.ts tests/tui/notifications-audit.test.ts</verify>
  <acceptance_criteria>
    - CLI secrets test asserts stdout does not contain original secret value.
    - TUI secrets test asserts masked value contains `••••` or `redacted`.
    - Web audit route test asserts filter fields render.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Plaintext secrets leak through audit/export/UI | Redaction schema tests, masked UI tests, CLI stdout tests. |
| Audit logs are incomplete | Mutation-specific event keys and parity tests. |
</threat_model>

<verification>
Run `bun test tests/secrets/vault.test.ts tests/secrets/keyring.test.ts tests/secrets/vault-adapter.test.ts tests/trpc/audit.test.ts tests/trpc/credentials.test.ts`.
</verification>

<success_criteria>
XCT-07 and XCT-11 are complete with encrypted secret storage and full audit coverage.
</success_criteria>
