---
phase: 04-inference-router-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase-04-static-build-proof.ts
  - evals/phase-04-router.promptfooconfig.yaml
  - src/inference/embedding-dimension.test.ts
  - src/inference/backend-real-calls.test.ts
  - src/router/learned-drafts.test.ts
  - src/skills/mcp-virtual-skills.test.ts
  - src/skills/lock-enforcement.test.ts
autonomous: true
requirements: [INF-01, INF-02, INF-05, INF-06, RTR-02, RTR-03, RTR-05, RTR-07]
must_haves:
  truths:
    - "Static macOS and Linux proof gates exist before implementation closes INF-02 per D-03"
    - "Router LLM regression corpus checks abstain, disabled draft, and conflict evidence per D-13, D-14"
    - "Embedding dimension tests fail when vector length differs from configured model metadata per D-05, D-06, D-07, D-08"
    - "Skill lock and MCP descriptor tests prove fail-closed and descriptor-only semantics per D-17, D-18, D-19, D-21"
  artifacts:
    - path: scripts/phase-04-static-build-proof.ts
      provides: "repeatable macOS/Linux static build proof"
      contains: "linux-x64"
    - path: evals/phase-04-router.promptfooconfig.yaml
      provides: "router LLM fallback eval corpus"
      contains: "abstained"
    - path: src/inference/embedding-dimension.test.ts
      provides: "dimension fail-closed tests"
      contains: "expected=384"
  key_links:
    - from: scripts/phase-04-static-build-proof.ts
      to: scripts/build-all.ts
      via: "imports or child process invocation"
      pattern: "build-all"
    - from: evals/phase-04-router.promptfooconfig.yaml
      to: "router structured outputs"
      via: "promptfoo assertions"
      pattern: "review_needed|conflict|abstained"
---

<objective>
Create Wave 0 validation scaffolds that make Phase 4 executable without missing-test gaps.

Purpose: lock the Linux static proof path, fastembed major decision gate, embedding dimension gate, router LLM eval gate, MCP descriptor gate, and skill lock gate before production code changes.
Output: focused tests/evals/scripts consumed by later plans.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-inference-router-skills/04-CONTEXT.md
@.planning/phases/04-inference-router-skills/04-AI-SPEC.md
@.planning/phases/04-inference-router-skills/04-RESEARCH.md
@.planning/phases/04-inference-router-skills/04-VALIDATION.md
@.planning/phases/04-inference-router-skills/04-PATTERNS.md
@scripts/build-all.ts
@src/inference/protocol.ts
@src/router/types.ts
@src/skills/lock.ts
</context>

<tasks>
<task id="04-01-01" type="auto" tdd="true">
  <name>Task 1: Add static build and embedding proof gates</name>
  <files>scripts/phase-04-static-build-proof.ts, src/inference/embedding-dimension.test.ts, src/inference/backend-real-calls.test.ts</files>
  <read_first>scripts/build-all.ts, inference/Cargo.toml, inference/inference-embed/Cargo.toml, inference/inference-embed/src/lib.rs, src/inference/protocol.ts, src/inference/backends/types.ts, .planning/phases/04-inference-router-skills/04-RESEARCH.md</read_first>
  <action>Create `scripts/phase-04-static-build-proof.ts` that runs `bun run scripts/build-all.ts`, verifies artifact metadata contains `darwin-arm64` and `linux-x64`, prints JSON with keys `ok`, `targets`, `artifacts`, `linuxProof`, and exits 1 unless both targets are proven. If Docker/cross/Linux builder is unavailable, exit 1 with JSON field `linuxProof:"missing"` and `reason:"linux static proof unavailable"`; do not mark skipped as success. Add `src/inference/embedding-dimension.test.ts` with failing-first tests for `expected=384`, vector length mismatch throwing `embedding dimension mismatch expected=384 actual=3`, and no stale non-comment `vector(1536)` hits under `src/` or `inference/`. Add `src/inference/backend-real-calls.test.ts` that enumerates configured backends from `FULCRUM_FEATURES`, requires embedded fastembed real embed/generate probes when configured, and treats unconfigured Ollama/LM Studio/OpenAI-compatible as non-blocking with explicit `unconfigured` state per D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08.</action>
  <verify>
    <automated>bun test src/inference/embedding-dimension.test.ts src/inference/backend-real-calls.test.ts</automated>
    <automated>bun run scripts/phase-04-static-build-proof.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "linuxProof|linux-x64|darwin-arm64" scripts/phase-04-static-build-proof.ts` returns all three tokens.
    - `rg -n "expected=384|vector\\(1536\\)|embedding dimension mismatch expected=384 actual=3" src/inference/embedding-dimension.test.ts` returns all three patterns.
    - `rg -n "unconfigured|embedded|ollama|lm-studio|openai-compatible" src/inference/backend-real-calls.test.ts` returns all five backend/state tokens.
  </acceptance_criteria>
  <done>INF-01/02/05/06 validation gates exist and fail closed when proof or dimension guarantees are missing.</done>
</task>

<task id="04-01-02" type="auto" tdd="true">
  <name>Task 2: Add router eval and supply-chain safety gates</name>
  <files>evals/phase-04-router.promptfooconfig.yaml, src/router/learned-drafts.test.ts, src/skills/mcp-virtual-skills.test.ts, src/skills/lock-enforcement.test.ts</files>
  <read_first>src/router/types.ts, src/router/llm-fallback.ts, src/router/auto-assign.ts, src/skills/lock.ts, src/skills/loader.ts, src/cli/mcp-builtins.ts, src/components/catalog.ts, .planning/phases/04-inference-router-skills/04-AI-SPEC.md</read_first>
  <action>Create Promptfoo config `evals/phase-04-router.promptfooconfig.yaml` with labeled cases named `deterministic-match`, `low-confidence-abstain`, `disabled-draft-review-needed`, and `overlap-conflict`; expected outputs must include `matched`, `abstained`, `review_needed`, `conflict`, `confidence`, `backend`, and `evidence`. Add tests asserting no-match learning creates disabled draft rows with fields `taskFacts`, `noMatchReason`, `proposedConditions`, `proposedActions`, `source`, `confidence`, `backend`, `model`, and `matchingActiveRuleIds`; low confidence records `abstained` evidence and does not create an active rule. Add MCP virtual skill tests for `source:"mcp"`, `invokableByFulcrum:false`, descriptor SHA-256, and tool manifest hash. Add lock tests proving SHA mismatch returns `status:"sha_mismatch"`, `available:false`, exact `expectedSha256`, exact `actualSha256`, and override audit payload per D-09 through D-24.</action>
  <verify>
    <automated>bun test src/router/learned-drafts.test.ts src/skills/mcp-virtual-skills.test.ts src/skills/lock-enforcement.test.ts</automated>
    <automated>bunx promptfoo eval --config evals/phase-04-router.promptfooconfig.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "deterministic-match|low-confidence-abstain|disabled-draft-review-needed|overlap-conflict" evals/phase-04-router.promptfooconfig.yaml` returns all four cases.
    - `rg -n "review_needed|conflict|matchingActiveRuleIds|abstained" src/router/learned-drafts.test.ts` returns all four tokens.
    - `rg -n "source.*mcp|invokableByFulcrum.*false|descriptorSha256|toolManifestHash" src/skills/mcp-virtual-skills.test.ts` returns all four patterns.
    - `rg -n "sha_mismatch|available.*false|expectedSha256|actualSha256" src/skills/lock-enforcement.test.ts` returns all four patterns.
  </acceptance_criteria>
  <done>RTR-02/03/05/07 validation gates exist for draft safety, LLM fallback, MCP descriptors, and lock fail-closed behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| build host -> artifact proof | local host claims static binary support |
| model/backend -> inference service | configured backend returns untrusted probe/model output |
| MCP descriptor -> skill registry | descriptor text and tool manifests may be malicious |
| upstream skill -> installed skill | upstream content may be tampered or conflict with local edits |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-STATIC | Spoofing | scripts/phase-04-static-build-proof.ts | mitigate | require explicit `linux-x64` and `darwin-arm64` artifact proof; missing Linux proof exits 1 |
| T-04-01-EMBED | Tampering | src/inference/embedding-dimension.test.ts | mitigate | assert model/schema/vector length `384`; reject `vector(1536)` storage references |
| T-04-01-MCP | Tampering | src/skills/mcp-virtual-skills.test.ts | mitigate | descriptor-only `invokableByFulcrum:false` plus SHA-256 hashes |
| T-04-01-SHA | Tampering | src/skills/lock-enforcement.test.ts | mitigate | `sha_mismatch` makes affected skill unavailable and exposes expected/actual SHA |
</threat_model>

<verification>
Run `bun test src/inference/embedding-dimension.test.ts src/inference/backend-real-calls.test.ts src/router/learned-drafts.test.ts src/skills/mcp-virtual-skills.test.ts src/skills/lock-enforcement.test.ts`.
Run `bunx promptfoo eval --config evals/phase-04-router.promptfooconfig.yaml`.
Run `bun run scripts/phase-04-static-build-proof.ts`; Linux proof missing is a valid RED gate until INF-02 implementation closes it.
</verification>

<success_criteria>
Wave 0 no longer has missing validation files for INF-01, INF-02, INF-05, INF-06, RTR-02, RTR-03, RTR-05, or RTR-07.
</success_criteria>

<output>
After completion, create `.planning/phases/04-inference-router-skills/04-01-SUMMARY.md`.
</output>
