---
phase: 04-inference-router-skills
plan: 07
type: execute
wave: 3
depends_on: [04-03, 04-04, 04-05]
files_modified:
  - src/web/src/routes/settings/routing/routing.types.ts
  - src/web/src/routes/settings/routing/routing.server.ts
  - src/web/src/routes/settings/routing/RoutingPage.svelte
  - src/web/src/routes/settings/routing/+page.server.ts
  - src/web/src/routes/settings/routing/page.server.test.ts
  - src/web/src/routes/settings/routing/page.svelte.test.ts
  - src/web/src/routes/inference/+page.svelte
  - src/web/src/routes/settings/inference/+page.server.ts
autonomous: true
requirements: [RTR-06, RTR-08, INF-03, INF-04, INF-05]
must_haves:
  truths:
    - "Web routing editor is table-first with tabs `Rules`, `Drafts`, `Test`, `LLM Gate`, and `Evidence` per UI-SPEC"
    - "Web rule authoring defaults to Builder and has `Raw JSON` escape hatch per D-27"
    - "Web save requires strict validation plus `Test routing` dry-run per D-28"
    - "Web learned drafts show Review needed, Conflict, Abstained, evidence, and conflict IDs per D-09 through D-12"
    - "Web inference backend status shows embedded/Ollama/LM Studio/OpenAI-compatible states with reasons and probes per D-01 through D-04"
  artifacts:
    - path: src/web/src/routes/settings/routing/RoutingPage.svelte
      provides: "routing editor UI"
      contains: "data-routing-tabs"
    - path: src/web/src/routes/settings/routing/routing.server.ts
      provides: "server actions calling tRPC"
      contains: "drafts.approve"
    - path: src/web/src/routes/inference/+page.svelte
      provides: "backend status UI"
      contains: "Degraded"
  key_links:
    - from: src/web/src/routes/settings/routing/routing.server.ts
      to: src/server/trpc/routers/routing.ts
      via: "trpcPost/trpcGet"
      pattern: "routing\\.test|routing\\.drafts"
    - from: src/web/src/routes/inference/+page.svelte
      to: src/server/trpc/routers/inference.ts
      via: "server load data"
      pattern: "backends"
---

<objective>
Implement Web routing/inference operational UX per UI-SPEC.

Purpose: give operators table-first Web controls for routing rules, learned drafts, LLM gate, evidence, and backend status without bypassing shared tRPC behavior.
Output: Svelte routing editor and inference status updates with tests.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-inference-router-skills/04-CONTEXT.md
@.planning/phases/04-inference-router-skills/04-UI-SPEC.md
@.planning/phases/04-inference-router-skills/04-PATTERNS.md
@src/web/src/routes/settings/routing/RoutingPage.svelte
@src/web/src/routes/settings/routing/routing.server.ts
@src/web/src/routes/inference/+page.svelte
</context>

<tasks>
<task id="04-07-01" type="auto" tdd="true">
  <name>Task 1: Upgrade Web routing editor for rules, drafts, tests, LLM gate, and evidence</name>
  <files>src/web/src/routes/settings/routing/routing.types.ts, src/web/src/routes/settings/routing/routing.server.ts, src/web/src/routes/settings/routing/RoutingPage.svelte, src/web/src/routes/settings/routing/+page.server.ts</files>
  <read_first>src/web/src/routes/settings/routing/RoutingPage.svelte, src/web/src/routes/settings/routing/routing.server.ts, src/web/src/routes/settings/routing/routing.types.ts, src/web/src/routes/settings/routing/+page.server.ts, src/server/trpc/routers/routing.ts, .planning/phases/04-inference-router-skills/04-UI-SPEC.md</read_first>
  <action>Render one dense settings route with tabs exactly `Rules`, `Drafts`, `Test`, `LLM Gate`, `Evidence`. Rules table columns must be `Priority`, `Name`, `Scope`, `Source`, `Conditions`, `Agent`, `Skill set`, `Status`, `Updated`, `Actions`. Draft table columns must be `Draft ID`, `Proposed rule`, `Source`, `Confidence`, `Conflict state`, `Matching active rules`, `Created`, `Actions`. Add builder/raw toggle labels `Builder` and `Raw JSON`; default to builder. Add actions `Create rule`, `Save rule`, `Test routing`, `Approve draft`, and delete confirmations with exact UI-SPEC copy. Server actions must call `routing.test`, `routing.create`, `routing.update`, `routing.delete`, `routing.drafts.approve`, `routing.drafts.update`, and `routing.drafts.delete`. Do not store business logic in Svelte; use tRPC action responses per D-11, D-25, D-26, D-27, D-28.</action>
  <verify>
    <automated>cd src/web && bun test --conditions=svelte ./src/routes/settings/routing/page.server.test.ts ./src/routes/settings/routing/page.svelte.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Rules|Drafts|Test|LLM Gate|Evidence" src/web/src/routes/settings/routing/RoutingPage.svelte` returns all five tab labels.
    - `rg -n "Builder|Raw JSON|Save rule|Test routing|Approve draft" src/web/src/routes/settings/routing/RoutingPage.svelte` returns all five action/control labels.
    - `rg -n "routing\\.drafts\\.approve|routing\\.drafts\\.delete|routing\\.test" src/web/src/routes/settings/routing/routing.server.ts` returns all three tRPC actions.
    - `rg -n "data-routing-tabs|data-routing-drafts-table|data-routing-evidence" src/web/src/routes/settings/routing/RoutingPage.svelte` returns all three test selectors.
  </acceptance_criteria>
  <done>Web routing editor satisfies UI-SPEC structure and uses shared tRPC behavior for all rule/draft/test actions.</done>
</task>

<task id="04-07-02" type="auto" tdd="true">
  <name>Task 2: Add backend status and dimension state to Web inference UI</name>
  <files>src/web/src/routes/inference/+page.svelte, src/web/src/routes/settings/inference/+page.server.ts, src/web/src/routes/settings/routing/page.server.test.ts, src/web/src/routes/settings/routing/page.svelte.test.ts</files>
  <read_first>src/web/src/routes/inference/+page.svelte, src/web/src/routes/settings/inference/+page.server.ts, src/server/trpc/routers/inference.ts, src/inference/service.ts, .planning/phases/04-inference-router-skills/04-UI-SPEC.md</read_first>
  <action>Render backend status rows for `embedded`, `Ollama`, `LM Studio`, and `OpenAI-compatible` with fields `configured`, `enabled`, `status`, `reason`, `model`, `embed probe`, `generate probe`, `dimensions`, `last checked`, `action`. Show `Start` only for embedded; external rows show `Probe`. Render status labels `running`, `stopped`, `degraded`, `unavailable`, `unconfigured`; include reason text for degraded/unavailable. Add dimension banner copy exactly `Embedding dimensions do not match. Writes and search are blocked until migration or reindex completes.` when configured model dimension differs from schema dimension. Tests must assert `Degraded`, `Unavailable`, `MCP virtual`, `SHA mismatch`, `Review needed`, `Conflict`, and `Abstained` labels appear when provided by load data per D-01 through D-08 and UI-SPEC.</action>
  <verify>
    <automated>cd src/web && bun test --conditions=svelte ./src/routes/inference/page.server.test.ts ./src/routes/settings/inference/page.server.test.ts ./src/routes/settings/routing/page.svelte.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "embedded|Ollama|LM Studio|OpenAI-compatible|Probe" src/web/src/routes/inference/+page.svelte` returns all backend/action labels.
    - `rg -n "Embedding dimensions do not match\\. Writes and search are blocked until migration or reindex completes\\." src/web/src/routes/inference/+page.svelte` returns exact mismatch copy.
    - `rg -n "Degraded|Unavailable|Review needed|Conflict|Abstained|SHA mismatch|MCP virtual" src/web/src/routes/settings/routing/page.svelte.test.ts src/web/src/routes/inference/+page.svelte` returns all required state labels.
  </acceptance_criteria>
  <done>Web inference and routing screens expose backend/dimension/draft/lock states with exact labels and no color-only semantics.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser form -> server action | untrusted rule JSON and destructive actions |
| tRPC result -> operator UI | security state must be visible |
| backend health -> Web display | degraded/unavailable reasons guide operator actions |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-FORM | Tampering | routing.server.ts | mitigate | server actions call tRPC validation/dry-run; no client-only validation trust |
| T-04-07-STATE | Information Disclosure | RoutingPage.svelte | mitigate | display exact lock/draft/backend labels and SHA values only where returned by API |
| T-04-07-DESTRUCT | Elevation of Privilege | destructive dialogs | mitigate | confirmations require object name or SHA values per UI-SPEC |
| T-04-07-DIM | Denial of Service | inference UI | mitigate | dimension mismatch banner states writes/search blocked; no override control |
</threat_model>

<verification>
Run `cd src/web && bun test --conditions=svelte ./src/routes/settings/routing/page.server.test.ts ./src/routes/settings/routing/page.svelte.test.ts ./src/routes/inference/page.server.test.ts ./src/routes/settings/inference/page.server.test.ts`.
</verification>

<success_criteria>
Web satisfies RTR-06/RTR-08 and inference status UI-SPEC expectations using table-first operational surfaces and shared tRPC action paths.
</success_criteria>

<output>
After completion, create `.planning/phases/04-inference-router-skills/04-07-SUMMARY.md`.
</output>
