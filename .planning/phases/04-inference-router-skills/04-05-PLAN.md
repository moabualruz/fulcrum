---
phase: 04-inference-router-skills
plan: 05
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/db/entities/skills/McpVirtualSkill.ts
  - src/db/entities/skills/SkillConflict.ts
  - src/db/entities/skills/index.ts
  - src/db/migrations/Migration20260505042000_skill_supply_chain.ts
  - src/skills/mcp-virtual-skills.ts
  - src/skills/registry-service.ts
  - src/skills/lock.ts
  - src/skills/upstream-sync.ts
  - src/skills/loader.ts
autonomous: true
requirements: [RTR-04, RTR-05, RTR-07]
must_haves:
  truths:
    - "MCP servers appear as globally visible virtual skills with source `mcp` and descriptor metadata per D-17 and D-20"
    - "MCP virtual skills are descriptor-only and never invokable by Fulcrum surfaces per D-18"
    - "MCP descriptors are pinned by server name, command/package/version/env hints, descriptor hash, and tool manifest hash per D-19"
    - "Skill lock SHA mismatch fails closed with expected/actual SHA per D-21"
    - "Upstream sync auto-merges only clean local files and creates structured conflict artifacts for local edits per D-22 and D-23"
    - "Override writes audit-ready records per D-24"
  artifacts:
    - path: src/skills/mcp-virtual-skills.ts
      provides: "MCP descriptor to virtual skill mapping"
      exports: ["buildMcpVirtualSkillDescriptors"]
    - path: src/skills/registry-service.ts
      provides: "merged local/upstream/MCP skill listing"
      exports: ["SkillRegistryService"]
    - path: src/db/entities/skills/SkillConflict.ts
      provides: "structured sync/lock conflict persistence"
      contains: "baseHash"
  key_links:
    - from: src/skills/registry-service.ts
      to: src/skills/mcp-virtual-skills.ts
      via: "includes MCP descriptor rows"
      pattern: "source.*mcp"
    - from: src/skills/loader.ts
      to: src/skills/lock.ts
      via: "fail closed on SHA mismatch"
      pattern: "sha_mismatch"
---

<objective>
Implement skill registry supply-chain safety and MCP virtual skill descriptors.

Purpose: make local/upstream/MCP skills visible through one registry while lock mismatches and sync conflicts fail closed per skill.
Output: MCP virtual skill mapping, registry service, conflict entity, and lock/sync enforcement.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-inference-router-skills/04-CONTEXT.md
@.planning/phases/04-inference-router-skills/04-RESEARCH.md
@.planning/phases/04-inference-router-skills/04-PATTERNS.md
@src/skills/lock.ts
@src/skills/upstream-sync.ts
@src/skills/loader.ts
@src/cli/mcp-builtins.ts
@src/components/catalog.ts
</context>

<tasks>
<task id="04-05-01" type="auto" tdd="true">
  <name>Task 1: Add MCP virtual skill descriptors and registry service</name>
  <files>src/db/entities/skills/McpVirtualSkill.ts, src/db/entities/skills/index.ts, src/db/migrations/Migration20260505042000_skill_supply_chain.ts, src/skills/mcp-virtual-skills.ts, src/skills/registry-service.ts</files>
  <read_first>src/db/entities/skills/FulcrumSkill.ts, src/db/entities/skills/SkillVersion.ts, src/cli/mcp-builtins.ts, src/components/catalog.ts, src/skills/loader.ts, src/skills/mcp-virtual-skills.test.ts</read_first>
  <action>Create `McpVirtualSkill` entity/table `mcp_virtual_skills` with fields `id`, `slug`, `serverName`, `commandOrUrl`, `packageName`, `version`, `envHintsJson`, `toolNamesJson`, `descriptorSha256`, `toolManifestHash`, `source` fixed to `mcp`, `invokableByFulcrum` fixed false, `createdAt`, `updatedAt`. Add `buildMcpVirtualSkillDescriptors()` that reads built-in MCP catalog and registered descriptors, sorts tool names by name before hashing, computes SHA-256 with `node:crypto`, and returns rows with `MCP virtual` display label. Create `SkillRegistryService.list()` returning merged source values `local | upstream | mcp`, with MCP rows globally visible and no per-agent support fields per D-17, D-18, D-19, D-20.</action>
  <verify>
    <automated>bun test src/skills/mcp-virtual-skills.test.ts src/server/trpc/routers/__tests__/skills.test.ts src/cli/mcp-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "invokableByFulcrum.*false|source.*mcp|descriptorSha256|toolManifestHash" src/db/entities/skills/McpVirtualSkill.ts src/skills/mcp-virtual-skills.ts` returns all four patterns.
    - `rg -n "local|upstream|mcp" src/skills/registry-service.ts` returns all three source values.
    - `rg -n "perAgent|agentSupport|supportedAgents" src/skills/mcp-virtual-skills.ts src/skills/registry-service.ts` returns no MCP-specific per-agent support field.
  </acceptance_criteria>
  <done>MCP servers are searchable descriptor-only virtual skills in shared registry output.</done>
</task>

<task id="04-05-02" type="auto" tdd="true">
  <name>Task 2: Enforce lock fail-closed and structured sync conflicts</name>
  <files>src/db/entities/skills/SkillConflict.ts, src/db/entities/skills/index.ts, src/skills/lock.ts, src/skills/upstream-sync.ts, src/skills/loader.ts</files>
  <read_first>src/skills/lock.ts, src/skills/upstream-sync.ts, src/skills/loader.ts, src/skills/conflict-resolver.ts, src/skills/lock-enforcement.test.ts, src/db/entities/skills/SkillVersion.ts</read_first>
  <action>Add `SkillConflict` entity/table `skill_conflicts` with `id`, `slug`, `kind` enum `upstream_conflict | sha_mismatch`, `status` enum `open | overridden | resolved`, `localHash`, `upstreamHash`, `baseHash`, `expectedSha256`, `actualSha256`, `suggestedResolution`, `auditNote`, `createdAt`, `updatedAt`. Update lock validation to return per-skill state `ok | sha_mismatch`, `available:false` on mismatch, and exact expected/actual SHA fields. Update upstream sync so clean local files auto-merge when local hash equals base hash; local edits create `SkillConflict` artifact and do not write inline conflict markers to `SKILL.md`. Add override function requiring `auditNote` and recording `overridden` state per D-21, D-22, D-23, D-24.</action>
  <verify>
    <automated>bun test src/skills/lock-enforcement.test.ts src/skills/upstream-sync* src/skills/loader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "upstream_conflict|sha_mismatch|overridden|auditNote" src/db/entities/skills/SkillConflict.ts src/skills/lock.ts src/skills/upstream-sync.ts` returns all four values.
    - `rg -n "expectedSha256|actualSha256|available.*false" src/skills/lock.ts src/skills/loader.ts` returns all three fields/states.
    - `rg -n "<<<<<<<|=======|>>>>>>>" src/skills/upstream-sync.ts src/skills/lock.ts src/skills/loader.ts` returns no inline conflict-marker writers.
  </acceptance_criteria>
  <done>Lock mismatch and upstream conflicts fail closed with structured conflict records and override audit notes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| MCP registry/catalog -> skill registry | untrusted descriptor and tool metadata enters UI/search |
| upstream source -> local skill install | upstream content may differ from pinned local content |
| operator override -> failed-closed skill | user re-enables affected capability |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-MCP | Tampering | src/skills/mcp-virtual-skills.ts | mitigate | SHA-256 descriptor/tool manifest hashes; descriptor-only `invokableByFulcrum:false` |
| T-04-05-SHA | Tampering | src/skills/lock.ts | mitigate | mismatch state `sha_mismatch`, `available:false`, exact expected/actual SHA |
| T-04-05-CONFLICT | Repudiation | src/db/entities/skills/SkillConflict.ts | mitigate | structured conflict artifact with base/local/upstream hashes |
| T-04-05-OVERRIDE | Elevation of Privilege | src/skills/upstream-sync.ts | mitigate | override requires audit note and records `overridden` status |
</threat_model>

<verification>
Run `bun test src/skills/mcp-virtual-skills.test.ts src/skills/lock-enforcement.test.ts src/skills/upstream-sync* src/skills/loader.test.ts src/server/trpc/routers/__tests__/skills.test.ts src/cli/mcp-registry.test.ts`.
</verification>

<success_criteria>
RTR-04, RTR-05, and RTR-07 pass with MCP descriptor-only rows, safe sync auto-merge/conflicts, and lock mismatch fail-closed state.
</success_criteria>

<output>
After completion, create `.planning/phases/04-inference-router-skills/04-05-SUMMARY.md`.
</output>
