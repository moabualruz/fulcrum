---
phase: 04-inference-router-skills
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/inference/model-metadata.ts
  - src/inference/protocol.ts
  - src/inference/client.ts
  - src/product-kernel/embeddings.ts
  - src/memory/retrieval/sidecar.ts
  - src/memory/retrieval/scoring.ts
  - src/product-kernel/db/migrations/0004_embeddings.sql
autonomous: true
requirements: [INF-01, INF-06]
must_haves:
  truths:
    - "Default fastembed model metadata reports dimension 384 per D-05 and D-06"
    - "Embedding writes/search reject mismatched vectors instead of padding, truncating, or coercing per D-07"
    - "Schema, product-kernel embedding writes, memory retrieval, and search scoring share the same dimension guard per D-08"
  artifacts:
    - path: src/inference/model-metadata.ts
      provides: "model dimension metadata and fail-closed validation"
      exports: ["DEFAULT_EMBEDDING_DIMENSION", "assertEmbeddingDimension"]
    - path: src/product-kernel/db/migrations/0004_embeddings.sql
      provides: "pgvector vector(384) schema"
      contains: "vector(384)"
  key_links:
    - from: src/product-kernel/embeddings.ts
      to: src/inference/model-metadata.ts
      via: "dimension assertion before DB write/search"
      pattern: "assertEmbeddingDimension"
    - from: src/memory/retrieval/sidecar.ts
      to: src/inference/model-metadata.ts
      via: "dimension assertion on sidecar response"
      pattern: "DEFAULT_EMBEDDING_DIMENSION"
---

<objective>
Implement model-metadata-derived embedding dimension enforcement.

Purpose: make `vector(384)` the default fastembed contract and fail closed if configured model output, schema, or stored vectors disagree.
Output: shared dimension metadata module wired into embedding write/read/search paths.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-inference-router-skills/04-CONTEXT.md
@.planning/phases/04-inference-router-skills/04-RESEARCH.md
@.planning/phases/04-inference-router-skills/04-PATTERNS.md
@src/inference/protocol.ts
@src/product-kernel/embeddings.ts
@src/memory/retrieval/sidecar.ts
</context>

<tasks>
<task id="04-02-01" type="auto" tdd="true">
  <name>Task 1: Add embedding model metadata contract</name>
  <files>src/inference/model-metadata.ts, src/inference/protocol.ts, src/inference/client.ts</files>
  <read_first>src/inference/protocol.ts, src/inference/client.ts, inference/inference-embed/src/lib.rs, src/inference/embedding-dimension.test.ts, .planning/phases/04-inference-router-skills/04-CONTEXT.md</read_first>
  <action>Create `src/inference/model-metadata.ts` exporting `DEFAULT_EMBEDDING_MODEL = "BAAI/bge-small-en-v1.5"`, `DEFAULT_EMBEDDING_DIMENSION = 384`, `EmbeddingModelMetadataSchema`, `getEmbeddingModelMetadata(modelId?: string)`, `assertEmbeddingDimension(vector: readonly number[], expected = DEFAULT_EMBEDDING_DIMENSION): void`, and `assertSchemaSupportsEmbeddingDimension(schemaDimension: number, modelDimension: number): void`. Add protocol fields `dimensions: number` to `EmbedResultSchema` and model kind `embed` metadata where missing. Update `src/inference/client.ts` so `embed()` includes and validates `dimensions` before returning. If a non-384 configured model is requested and no schema support is explicit, throw `InferenceError` message `embedding model dimension unsupported model=<modelId> expectedSchema=384 actualModel=<dimension>` per D-05, D-06, D-07.</action>
  <verify>
    <automated>bun test src/inference/embedding-dimension.test.ts src/server/trpc/routers/__tests__/inference.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "DEFAULT_EMBEDDING_MODEL|DEFAULT_EMBEDDING_DIMENSION = 384|assertEmbeddingDimension|assertSchemaSupportsEmbeddingDimension" src/inference/model-metadata.ts` returns all four exports.
    - `rg -n "dimensions" src/inference/protocol.ts src/inference/client.ts` returns matches in both files.
    - `rg -n "embedding model dimension unsupported" src/inference/model-metadata.ts src/inference/client.ts` returns at least one match.
  </acceptance_criteria>
  <done>Embedding model metadata is centralized and used by inference client responses.</done>
</task>

<task id="04-02-02" type="auto" tdd="true">
  <name>Task 2: Wire dimension guards into write/read/search paths</name>
  <files>src/product-kernel/embeddings.ts, src/memory/retrieval/sidecar.ts, src/memory/retrieval/scoring.ts, src/product-kernel/db/migrations/0004_embeddings.sql</files>
  <read_first>src/product-kernel/embeddings.ts, src/memory/retrieval/sidecar.ts, src/memory/retrieval/scoring.ts, src/product-kernel/db/migrations/0004_embeddings.sql, src/product-kernel/db/embeddings.test.ts, src/product-kernel/embeddings.test.ts, src/inference/model-metadata.ts</read_first>
  <action>Import `assertEmbeddingDimension` and `DEFAULT_EMBEDDING_DIMENSION` into every embedding write/search path touched here. Before writing task/doc/memory embeddings, call `assertEmbeddingDimension(embedding, DEFAULT_EMBEDDING_DIMENSION)`. Before cosine scoring or hybrid search, reject query and stored vectors whose length is not 384; return FTS fallback only when embeddings are disabled, not when dimensions mismatch. Ensure `0004_embeddings.sql` contains only `vector(384)` for pgvector tables and no `vector(1536)`. Preserve existing product-kernel behavior except dimension mismatch now throws per D-05 through D-08.</action>
  <verify>
    <automated>bun test src/product-kernel/db/embeddings.test.ts src/product-kernel/embeddings.test.ts src/inference/embedding-dimension.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "assertEmbeddingDimension" src/product-kernel/embeddings.ts src/memory/retrieval/sidecar.ts src/memory/retrieval/scoring.ts` returns matches in all three files.
    - `rg -n "vector\\(384\\)" src/product-kernel/db/migrations/0004_embeddings.sql` returns at least two matches.
    - `rg -n "vector\\(1536\\)" src product-kernel inference 2>/dev/null | grep -v '^#'` returns no source-code matches.
  </acceptance_criteria>
  <done>Embedding storage and retrieval reject mixed dimensions and retain `vector(384)` schema.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| inference backend -> TS client | backend returns vector length and model metadata |
| TS service -> DB/search | vectors cross into storage and similarity code |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-DIM | Tampering | src/inference/model-metadata.ts | mitigate | hard fail on model/schema/vector dimension mismatch with exact expected/actual values |
| T-04-02-DOS | Denial of Service | src/memory/retrieval/scoring.ts | mitigate | reject mismatched vectors before cosine loop to avoid corrupt scoring |
| T-04-02-INFO | Information Disclosure | src/memory/retrieval/sidecar.ts | accept | no new secret data emitted; errors include only dimensions and model id |
</threat_model>

<verification>
Run `bun test src/inference/embedding-dimension.test.ts src/product-kernel/db/embeddings.test.ts src/product-kernel/embeddings.test.ts src/server/trpc/routers/__tests__/inference.test.ts`.
</verification>

<success_criteria>
No source storage reference to `vector(1536)` remains, default model dimension is `384`, and every embedding write/read/search path touched by Phase 4 validates vector length before use.
</success_criteria>

<output>
After completion, create `.planning/phases/04-inference-router-skills/04-02-SUMMARY.md`.
</output>
