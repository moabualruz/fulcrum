---
phase: 04-inference-router-skills
plan: 06
type: execute
wave: 4
depends_on: [04-04, 04-05]
files_modified:
  - src/server/trpc/routers/routing.ts
  - src/server/trpc/routers/skills.ts
  - src/cli/commands/routing.ts
  - src/index.ts
  - src/tui/screens/routing-rules.ts
  - src/tui/index.ts
autonomous: true
requirements: [RTR-06, RTR-08, RTR-02, RTR-03, RTR-04, RTR-05, RTR-07]
must_haves:
  truths:
    - "Web/CLI/TUI can list, test, create, update, delete routing rules and learned drafts via shared tRPC/service per D-11 and D-25"
    - "Route tests return matched rule/draft, facts used, confidence, backend, and unmatched reason per D-26"
    - "Rule saves use strict validation and dry-run support; invalid JSON/conditions are rejected per D-28"
    - "CLI JSON includes labels `review_needed`, `conflict`, `abstained`, `degraded`, `unavailable`, and `sha_mismatch` per UI-SPEC"
    - "Skill conflict/lock override APIs require audit note and exact SHA values per D-24"
  artifacts:
    - path: src/server/trpc/routers/routing.ts
      provides: "shared routing CRUD/test/draft procedures"
      contains: "drafts"
    - path: src/cli/commands/routing.ts
      provides: "CLI routing parity"
      contains: "--json"
    - path: src/tui/screens/routing-rules.ts
      provides: "TUI routing parity screen"
      contains: "Drafts"
  key_links:
    - from: src/server/trpc/routers/routing.ts
      to: src/router/service.ts
      via: "procedure delegates"
      pattern: "RoutingService"
    - from: src/cli/commands/routing.ts
      to: src/server/trpc/routers/routing.ts
      via: "in-process tRPC caller"
      pattern: "routing"
---

<objective>
Expose routing and skill supply-chain behavior through shared tRPC, CLI, and TUI paths.

Purpose: prevent surface-specific business logic and make operator controls consistent outside the Web UI.
Output: tRPC procedures plus CLI/TUI parity for routing rules, learned drafts, tests, and overrides.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-inference-router-skills/04-CONTEXT.md
@.planning/phases/04-inference-router-skills/04-UI-SPEC.md
@.planning/phases/04-inference-router-skills/04-PATTERNS.md
@src/server/trpc/routers/routing.ts
@src/server/trpc/routers/skills.ts
@src/cli/commands/routing.ts
@src/tui/screens/routing-rules.ts
</context>

<tasks>
<task id="04-06-01" type="auto" tdd="true">
  <name>Task 1: Add tRPC routing and skills procedures</name>
  <files>src/server/trpc/routers/routing.ts, src/server/trpc/routers/skills.ts</files>
  <read_first>src/server/trpc/routers/routing.ts, src/server/trpc/routers/skills.ts, src/router/service.ts, src/skills/registry-service.ts, src/skills/lock.ts, src/trpc/middleware.ts</read_first>
  <action>Extend `routingRouter` with procedures `list`, `create`, `update`, `delete`, `test`, `drafts.list`, `drafts.approve`, `drafts.delete`, `drafts.update`, and `config.updateLlmGate`. Inputs must use Zod and permissioned procedures. `test` response must include `status`, `matchedRuleId`, `draftId`, `factsUsed`, `confidence`, `backend`, `model`, `whyUnmatched`, and `evidence`. `create/update` must validate `conditionsJson` with `json-rules-engine` and require successful dry-run token `dryRunId` before save. Extend `skillsRouter` with `registry.list`, `conflicts.list`, `conflicts.override`, and `lock.override`; overrides require `auditNote`, `expectedSha256`, and `actualSha256` where applicable per D-11, D-24, D-25, D-26, D-28.</action>
  <verify>
    <automated>bun test src/server/trpc/routers/routing* src/server/trpc/routers/__tests__/skills.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "drafts|approve|whyUnmatched|dryRunId|config\\.updateLlmGate" src/server/trpc/routers/routing.ts` returns all five API tokens.
    - `rg -n "registry\\.list|conflicts\\.override|lock\\.override|auditNote" src/server/trpc/routers/skills.ts` returns all four API tokens.
    - `rg -n "permissionedProcedure" src/server/trpc/routers/routing.ts src/server/trpc/routers/skills.ts` returns matches for mutation procedures.
  </acceptance_criteria>
  <done>Shared API exposes routing drafts/tests/config and skill conflict/lock overrides with validation.</done>
</task>

<task id="04-06-02" type="auto" tdd="true">
  <name>Task 2: Wire CLI and TUI parity</name>
  <files>src/cli/commands/routing.ts, src/index.ts, src/tui/screens/routing-rules.ts, src/tui/index.ts</files>
  <read_first>src/cli/commands/routing.ts, src/index.ts, src/tui/screens/routing-rules.ts, src/tui/index.ts, src/server/trpc/routers/routing.ts, .planning/phases/04-inference-router-skills/04-UI-SPEC.md</read_first>
  <action>Update CLI help and implementation to support `fulcrum routing rules list|create|update|delete|test --json`, `fulcrum routing drafts list|approve|update|delete --json`, and `fulcrum routing llm-gate get|set --input-mode task_facts|task_plus_history|full_context --enabled true|false --json`. JSON outputs must include labels `review_needed`, `conflict`, `abstained`, `degraded`, `unavailable`, and `sha_mismatch` when returned by service. Update TUI routing screen with panes/tabs `Rules`, `Drafts`, `Test`, `Backends`, raw JSON editor path, visible status labels, and actions approve/delete/test. TUI may use raw JSON only, but must call shared tRPC and show validation errors/dry-run results per D-15, D-16, D-25, D-26, D-27, D-28.</action>
  <verify>
    <automated>bun test src/cli src/tui/screens/routing-rules.ts src/tui</automated>
    <automated>bun run src/index.ts routing rules list --json</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "drafts|llm-gate|task_facts|task_plus_history|full_context" src/cli/commands/routing.ts src/index.ts` returns all CLI command/input-mode tokens.
    - `rg -n "Rules|Drafts|Test|Backends|Raw JSON" src/tui/screens/routing-rules.ts src/tui/index.ts` returns all TUI panes/labels.
    - `bun run src/index.ts routing rules list --json | jq -e 'type == "array" or type == "object"'` succeeds.
  </acceptance_criteria>
  <done>CLI and TUI expose the same routing/draft/config operations through shared service semantics.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI/TUI input -> tRPC router | raw JSON and command flags are untrusted |
| operator override -> skill availability | override changes failed-closed state |
| service output -> terminal UI | status labels must not hide security states |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-JSON | Tampering | src/server/trpc/routers/routing.ts | mitigate | Zod input plus `json-rules-engine` condition validation and dry-run token |
| T-04-06-AUTHZ | Elevation of Privilege | routing/skills mutations | mitigate | use `permissionedProcedure` for create/update/delete/approve/override |
| T-04-06-OVERRIDE | Repudiation | src/server/trpc/routers/skills.ts | mitigate | override requires audit note plus expected/actual SHA |
| T-04-06-UX | Information Disclosure | CLI/TUI status output | mitigate | status labels include exact safe states, not color-only output |
</threat_model>

<verification>
Run `bun test src/server/trpc/routers/routing* src/server/trpc/routers/__tests__/skills.test.ts src/cli src/tui/screens/routing-rules.ts src/tui`.
Run `bun run src/index.ts routing rules list --json`.
</verification>

<success_criteria>
RTR-06 and RTR-08 backend/CLI/TUI parity is functional for routing rules, learned drafts, route tests, LLM gate config, and skill lock/conflict overrides.
</success_criteria>

<output>
After completion, create `.planning/phases/04-inference-router-skills/04-06-SUMMARY.md`.
</output>
