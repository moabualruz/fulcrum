---
phase: 04-inference-router-skills
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - src/db/entities/router/RoutingRule.ts
  - src/db/entities/router/RoutingDraft.ts
  - src/db/entities/router/RoutingAudit.ts
  - src/db/entities/router/index.ts
  - src/db/migrations/Migration20260505041000_routing_drafts.ts
  - src/router/decision-schema.ts
  - src/router/learned-drafts.ts
  - src/router/conflict-detector.ts
  - src/router/service.ts
  - src/router/llm-fallback.ts
  - src/router/auto-assign.ts
autonomous: true
requirements: [RTR-01, RTR-02, RTR-03]
must_haves:
  truths:
    - "Deterministic rules match before any LLM fallback per RTR-01"
    - "No-match learning stores disabled review-needed drafts, never active rules, per D-09"
    - "Draft evidence includes task facts, no-match reason, proposed conditions/actions, source, confidence, and backend/model per D-10"
    - "Overlapping learned drafts are `conflict`, disabled, and list matching active rule IDs per D-12"
    - "LLM fallback is off by default; when enabled, it recommends, abstains, or creates disabled drafts only per D-13, D-14, D-15, D-16"
  artifacts:
    - path: src/router/decision-schema.ts
      provides: "explainable routing output schemas"
      exports: ["RoutingDecisionResultSchema", "LearnedDraftSchema"]
    - path: src/router/service.ts
      provides: "shared routing service for tRPC/CLI/TUI/Web"
      exports: ["RoutingService"]
    - path: src/db/entities/router/RoutingDraft.ts
      provides: "disabled learned draft persistence"
      contains: "review_needed"
  key_links:
    - from: src/router/auto-assign.ts
      to: src/router/service.ts
      via: "delegates decision and draft persistence"
      pattern: "RoutingService"
    - from: src/router/service.ts
      to: src/db/entities/router/RoutingDraft.ts
      via: "MikroORM persistence"
      pattern: "RoutingDraft"
---

<objective>
Implement safe router decision, learned draft, conflict, and LLM fallback service behavior.

Purpose: convert routing from direct learned activation into explainable, auditable, disabled-draft workflow.
Output: routing draft entities, decision schemas, conflict detector, and shared service.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-inference-router-skills/04-CONTEXT.md
@.planning/phases/04-inference-router-skills/04-AI-SPEC.md
@.planning/phases/04-inference-router-skills/04-RESEARCH.md
@.planning/phases/04-inference-router-skills/04-PATTERNS.md
@src/router/auto-assign.ts
@src/router/rules-engine.ts
@src/router/llm-fallback.ts
@src/db/entities/router/RoutingRule.ts
</context>

<tasks>
<task id="04-04-01" type="auto" tdd="true">
  <name>Task 1: Add routing draft/audit entities and schemas</name>
  <files>src/db/entities/router/RoutingDraft.ts, src/db/entities/router/RoutingAudit.ts, src/db/entities/router/index.ts, src/db/migrations/Migration20260505041000_routing_drafts.ts, src/router/decision-schema.ts</files>
  <read_first>src/db/entities/router/RoutingRule.ts, src/db/entities/router/index.ts, src/db/migrations/Migration20260502050000_routing_rules.ts, src/router/types.ts, src/router/learned-drafts.test.ts</read_first>
  <action>Create `RoutingDraft` entity/table `routing_drafts` with fields `id`, `org`, `projectId`, `status` enum `review_needed | conflict | abstained`, `enabled` default false, `taskFactsJson`, `noMatchReason`, `proposedConditionsJson`, `proposedActionsJson`, `source` enum `no_match | llm`, `confidence`, `backend`, `model`, `matchingActiveRuleIdsJson`, `createdAt`, `updatedAt`. Create `RoutingAudit` entity/table `routing_audit_events` with `id`, `org`, `eventType`, `subjectType`, `subjectId`, `payloadJson`, `createdAt`. Export both from router entity barrel. Add migration with indexes `idx_routing_drafts_org_status`, `idx_routing_drafts_org_created`, and `idx_routing_audit_org_created`. Add `src/router/decision-schema.ts` exporting Zod schemas with exact statuses `matched | no_match | recommended | draft_created | conflict | abstained` and input modes `task_facts | task_plus_history | full_context` per D-09 through D-16.</action>
  <verify>
    <automated>bun test src/router/learned-drafts.test.ts src/db</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "review_needed|conflict|abstained|enabled.*false" src/db/entities/router/RoutingDraft.ts` returns all draft state/disabled tokens.
    - `rg -n "idx_routing_drafts_org_status|idx_routing_audit_org_created" src/db/migrations/Migration20260505041000_routing_drafts.ts` returns both index names.
    - `rg -n "task_facts|task_plus_history|full_context|draft_created" src/router/decision-schema.ts` returns all four schema values.
  </acceptance_criteria>
  <done>Routing draft and audit persistence contracts exist with disabled states and evidence fields.</done>
</task>

<task id="04-04-02" type="auto" tdd="true">
  <name>Task 2: Implement service, conflict detector, and LLM fallback safety</name>
  <files>src/router/learned-drafts.ts, src/router/conflict-detector.ts, src/router/service.ts, src/router/llm-fallback.ts, src/router/auto-assign.ts</files>
  <read_first>src/router/rules-engine.ts, src/router/auto-assign.ts, src/router/llm-fallback.ts, src/router/no-match-prompt.ts, src/router/telemetry.ts, src/router/decision-schema.ts, src/router/learned-drafts.test.ts, evals/phase-04-router.promptfooconfig.yaml</read_first>
  <action>Implement `RoutingService` with methods `testRoute`, `createDraftFromNoMatch`, `approveDraft`, `deleteDraft`, and `dryRunRule`. Deterministic `json-rules-engine` matches must return `status:"matched"` with `confidence:1`, `matchedRuleId`, `factsUsed`, `evidence`, and no LLM call. No match with `router-llm` disabled returns `status:"no_match"` plus creates `review_needed` disabled draft only when requested by learning path. LLM fallback must parse structured output, retry 3 times on schema failure, use confidence threshold `0.75`, abstain below `0.55`, never activate rules, and create disabled draft or conflict draft only. Conflict detector must compare proposed conditions/actions against active rules and store `matchingActiveRuleIds`. Preserve `full_context` as default LLM input mode and selectable `task_facts` / `task_plus_history`, with guardrail config fields recorded in evidence per D-09 through D-16 and AI-SPEC boundary.</action>
  <verify>
    <automated>bun test src/router/rules-engine.test.ts src/router/auto-assign.test.ts src/router/llm-fallback.test.ts src/router/learned-drafts.test.ts</automated>
    <automated>bunx promptfoo eval --config evals/phase-04-router.promptfooconfig.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "confidenceThreshold.*0\\.75|abstainThreshold.*0\\.55|full_context" src/router/service.ts src/router/llm-fallback.ts` returns threshold/default mode values.
    - `rg -n "approveDraft|deleteDraft|createDraftFromNoMatch|matchingActiveRuleIds" src/router/service.ts src/router/learned-drafts.ts src/router/conflict-detector.ts` returns all service APIs/fields.
    - `rg -n "enabled.*true" src/router/llm-fallback.ts src/router/learned-drafts.ts` returns no code path that creates active learned drafts.
  </acceptance_criteria>
  <done>Router service returns explainable decisions and persists only disabled learned drafts until explicit approval.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| task facts/context -> router service | untrusted work item facts drive routing |
| LLM backend -> router service | model output may be malformed or unsafe |
| draft approval -> active routing rule | disabled behavior becomes active behavior |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-DRAFT | Elevation of Privilege | src/router/learned-drafts.ts | mitigate | drafts created with `enabled=false` and `review_needed/conflict/abstained` states |
| T-04-04-LLM | Tampering | src/router/llm-fallback.ts | mitigate | Zod parse, 3 retries, thresholds 0.75/0.55, abstain on invalid/low confidence |
| T-04-04-CONFLICT | Tampering | src/router/conflict-detector.ts | mitigate | overlapping active rule IDs stored and approval hidden until conflict resolved |
| T-04-04-AUDIT | Repudiation | src/db/entities/router/RoutingAudit.ts | mitigate | every draft/approval/delete records payload JSON with subject id |
</threat_model>

<verification>
Run `bun test src/router/rules-engine.test.ts src/router/auto-assign.test.ts src/router/llm-fallback.test.ts src/router/learned-drafts.test.ts`.
Run `bunx promptfoo eval --config evals/phase-04-router.promptfooconfig.yaml`.
</verification>

<success_criteria>
RTR-01, RTR-02, and RTR-03 pass through deterministic-first, disabled-draft, conflict, and LLM abstention behavior with persisted evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/04-inference-router-skills/04-04-SUMMARY.md`.
</output>
