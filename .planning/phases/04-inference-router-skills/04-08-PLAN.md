---
phase: 04-inference-router-skills
plan: 08
type: execute
wave: 5
depends_on: [04-06, 04-07]
files_modified:
  - src/trpc/router.ts
  - src/cli/doctor.ts
  - src/tui/__tests__/inference-screen.test.ts
  - src/tui/__tests__/routing-rules-screen.test.ts
  - src/server/trpc/routers/__tests__/inference.test.ts
  - src/server/trpc/routers/__tests__/skills.test.ts
  - src/server/trpc/routers/routing.test.ts
autonomous: true
requirements: [INF-01, INF-02, INF-03, INF-04, INF-05, INF-06, INF-07, RTR-01, RTR-02, RTR-03, RTR-04, RTR-05, RTR-06, RTR-07, RTR-08]
must_haves:
  truths:
    - "All INF-01..07 and RTR-01..08 automated gates run from one Phase 4 verification path"
    - "CLI doctor reports inference sidecar and backend states with typed reasons"
    - "tRPC root exposes inference, routing, and skills routers without duplicate aliases"
    - "Web/CLI/TUI parity labels and JSON states match UI-SPEC"
    - "LangGraph, if installed, remains internal router-service implementation detail and no agent profile/Symphony/Sandcastle path imports it"
  artifacts:
    - path: src/trpc/router.ts
      provides: "root router wiring"
      contains: "routing"
    - path: src/cli/doctor.ts
      provides: "doctor JSON output for inference"
      contains: "inference-backends"
    - path: src/tui/__tests__/routing-rules-screen.test.ts
      provides: "TUI routing parity tests"
      contains: "Drafts"
  key_links:
    - from: src/trpc/router.ts
      to: src/server/trpc/routers/routing.ts
      via: "router mount"
      pattern: "routing"
    - from: src/cli/doctor.ts
      to: src/doctor/checks/inference.ts
      via: "doctor runAll/check registration"
      pattern: "inference"
---

<objective>
Close Phase 4 with root wiring, parity tests, and final verification gates.

Purpose: prove all requirement IDs are reachable through shipped surfaces and no AI framework/internal router choice leaks into CLI agent usability or Phase 3 orchestration paths.
Output: final wiring and regression tests for whole Phase 4.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-inference-router-skills/04-CONTEXT.md
@.planning/phases/04-inference-router-skills/04-AI-SPEC.md
@.planning/phases/04-inference-router-skills/04-VALIDATION.md
@src/trpc/router.ts
@src/cli/doctor.ts
@src/tui/__tests__/inference-screen.test.ts
</context>

<tasks>
<task id="04-08-01" type="auto" tdd="true">
  <name>Task 1: Verify root wiring and no LangGraph boundary leaks</name>
  <files>src/trpc/router.ts, src/cli/doctor.ts, src/server/trpc/routers/__tests__/inference.test.ts, src/server/trpc/routers/__tests__/skills.test.ts, src/server/trpc/routers/routing.test.ts</files>
  <read_first>src/trpc/router.ts, src/cli/doctor.ts, src/server/trpc/routers/inference.ts, src/server/trpc/routers/routing.ts, src/server/trpc/routers/skills.ts, src/agents/registry.ts, src/orchestration/symphony/dispatch.ts, src/orchestration/sandbox-runner.ts, .planning/phases/04-inference-router-skills/04-AI-SPEC.md</read_first>
  <action>Ensure root `appRouter` mounts canonical `inference`, `routing`, and `skills` routers once, with no duplicate aliases. Ensure CLI doctor includes inference checks from `src/doctor/checks/inference.ts` and JSON names `inference-sidecar` and `inference-backends`. Add tests that import `src/agents/`, `src/orchestration/symphony/`, and `src/orchestration/sandbox-runner.ts` and assert they do not import `@langchain/langgraph`, `@langchain/core`, or router graph modules. If LangGraph is installed, only files under `src/router/` may import it. This enforces AI-SPEC boundary: internal router-service detail only, no CLI agent usability/Profile/Symphony/Sandcastle impact.</action>
  <verify>
    <automated>bun test src/server/trpc/routers/__tests__/inference.test.ts src/server/trpc/routers/__tests__/skills.test.ts src/server/trpc/routers/routing.test.ts</automated>
    <automated>rg -n "@langchain/langgraph|@langchain/core" src/agents src/orchestration src/cli | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "inference|routing|skills" src/trpc/router.ts` returns all three canonical mounts.
    - `rg -n "inference-sidecar|inference-backends" src/cli/doctor.ts src/doctor/checks/inference.ts` returns both doctor check names.
    - `rg -n "@langchain/langgraph|@langchain/core" src/agents src/orchestration src/cli` returns zero matches.
  </acceptance_criteria>
  <done>Root API and doctor are wired, and LangGraph/LangChain do not leak into agent/orchestration/CLI runtime boundaries.</done>
</task>

<task id="04-08-02" type="auto" tdd="true">
  <name>Task 2: Add parity and final phase verification tests</name>
  <files>src/tui/__tests__/inference-screen.test.ts, src/tui/__tests__/routing-rules-screen.test.ts, src/server/trpc/routers/__tests__/inference.test.ts, src/server/trpc/routers/__tests__/skills.test.ts, src/server/trpc/routers/routing.test.ts</files>
  <read_first>src/tui/__tests__/inference-screen.test.ts, src/tui/screens/routing-rules.ts, src/tui/index.ts, src/cli/commands/routing.ts, src/web/src/routes/settings/routing/page.svelte.test.ts, src/web/src/routes/inference/+page.svelte, .planning/phases/04-inference-router-skills/04-VALIDATION.md</read_first>
  <action>Add/extend parity tests so TUI output includes `Rules`, `Drafts`, `Test`, `Backends`, `Review needed`, `Conflict`, `Abstained`, `Degraded`, `Unavailable`, and `SHA mismatch` when service fixtures return those states. Add server tests that execute one route test, one disabled draft approval, one conflict delete, one MCP virtual skill list, one SHA mismatch override, and one inference backend probe. Include a final test command comment block in SUMMARY requiring exact commands: `bun test src/inference src/router src/skills src/server/trpc/routers`, `cd src/web && bun test --conditions=svelte ./src/routes/settings/routing ./src/routes/inference`, `cargo test --manifest-path inference/Cargo.toml`, `bun run scripts/static-build-proof.ts`, and `bun run ci`. The static proof command must pass and record macOS + Linux artifact paths, versions, and target triples; blocked or missing Linux proof is not accepted for INF-02 closure.</action>
  <verify>
    <automated>bun test src/tui/__tests__/inference-screen.test.ts src/tui/__tests__/routing-rules-screen.test.ts src/server/trpc/routers/__tests__/inference.test.ts src/server/trpc/routers/__tests__/skills.test.ts src/server/trpc/routers/routing.test.ts</automated>
    <automated>bun test src/inference src/router src/skills src/server/trpc/routers</automated>
    <automated>cargo test --manifest-path inference/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Rules|Drafts|Test|Backends|Review needed|Conflict|Abstained|Degraded|Unavailable|SHA mismatch" src/tui/__tests__/routing-rules-screen.test.ts src/tui/__tests__/inference-screen.test.ts` returns all required labels.
    - `rg -n "mcp|sha_mismatch|approve|conflict|backend" src/server/trpc/routers/__tests__/skills.test.ts src/server/trpc/routers/routing.test.ts src/server/trpc/routers/__tests__/inference.test.ts` returns all scenario tokens.
    - `bun test src/inference src/router src/skills src/server/trpc/routers` exits 0.
  </acceptance_criteria>
  <done>Final Phase 4 tests prove three-surface parity, inference backend evidence, router draft safety, and skill supply-chain controls.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| root router -> surfaces | incorrect mounts can bypass canonical service behavior |
| AI framework -> agent runtime | internal router implementation could leak into CLI agent dispatch |
| final verification -> phase completion | incomplete gates could falsely mark phase done |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-ALIAS | Elevation of Privilege | src/trpc/router.ts | mitigate | canonical single mounts for inference/routing/skills; no duplicate aliases |
| T-04-08-LANG | Tampering | src/agents, src/orchestration, src/cli | mitigate | grep gate forbids LangGraph/LangChain imports outside router service boundary |
| T-04-08-PARITY | Repudiation | final tests | mitigate | parity tests assert exact labels and state tokens across TUI/API |
| T-04-08-GATE | Spoofing | SUMMARY verification | mitigate | final commands include root CI, Rust tests, static proof, promptfoo eval |
</threat_model>

<verification>
Run `bun test src/inference src/router src/skills src/server/trpc/routers`.
Run `cd src/web && bun test --conditions=svelte ./src/routes/settings/routing ./src/routes/inference`.
Run `cargo test --manifest-path inference/Cargo.toml`.
Run `bun run scripts/static-build-proof.ts`.
Run `bun run ci`.
</verification>

<success_criteria>
Every requirement INF-01..07 and RTR-01..08 has passing automated evidence, including a passing macOS + Linux static proof with artifact path/version/target output for INF-02, with no source audit gaps.
</success_criteria>

<output>
After completion, create `.planning/phases/04-inference-router-skills/04-08-SUMMARY.md`.
</output>
