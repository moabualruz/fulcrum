---
phase: 05-task-management-metrics
plan: 09
type: execute
wave: 3
depends_on: [05]
files_modified:
  - src/web/src/lib/components/charts/BurndownChart.svelte
  - src/web/src/lib/components/charts/BurnupChart.svelte
  - src/web/src/lib/components/charts/VelocityChart.svelte
  - src/web/src/lib/components/charts/CfdChart.svelte
  - src/web/src/lib/components/charts/CycleTimeChart.svelte
  - src/web/src/lib/components/charts/ThroughputChart.svelte
  - src/web/src/lib/components/charts/WipChart.svelte
  - src/web/src/lib/components/charts/MonteCarloChart.svelte
  - src/web/src/lib/components/reports/ReportCard.svelte
  - src/web/src/lib/components/reports/DateRangePicker.svelte
  - src/web/src/routes/projects/[id]/reports/+page.svelte
  - src/web/src/routes/projects/[id]/reports/+page.server.ts
  - src/web/src/app.css
autonomous: true
requirements:
  - TSK-03
  - TSK-04
  - TSK-05

must_haves:
  truths:
    - "LayerChart renders burndown, velocity, CFD, cycle time from real tRPC data"
    - "All charts load client-only via dynamic import (no SSR crash)"
    - "Reports page uses tRPC reportsRouter (not openProductDb)"
    - "Date range picker allows preset + custom range selection"
    - "Chart color tokens chart-6 through chart-8 added to app.css"
  artifacts:
    - path: "src/web/src/lib/components/charts/BurndownChart.svelte"
      provides: "LayerChart line chart for burndown"
      contains: "layerchart"
    - path: "src/web/src/routes/projects/[id]/reports/+page.server.ts"
      provides: "Reports page load via tRPC (not openProductDb)"
      contains: "projectId"
  key_links:
    - from: "src/web/src/routes/projects/[id]/reports/+page.svelte"
      to: "src/server/trpc/routers/reports.ts"
      via: "tRPC client"
      pattern: "trpc.*reports"
    - from: "src/web/src/lib/components/charts/BurndownChart.svelte"
      to: "layerchart"
      via: "dynamic import"
      pattern: "import.*layerchart"
---

<objective>
Build 8 primary chart components using LayerChart, migrate reports page from openProductDb to tRPC, and add report infrastructure (ReportCard shell, DateRangePicker).

Purpose: Delivers TSK-03 (burndown), TSK-04 (velocity), TSK-05 (cycle time + throughput + WIP + CFD) as visual charts. Eliminates the ARCH-01 violation in reports page. LayerChart provides D3-powered composable charts per D-56..D-59.

Output: 8 chart components, report infrastructure, migrated reports page.
</objective>

<execution_context>
@.planning/phases/05-task-management-metrics/05-UI-SPEC.md
@.planning/phases/05-task-management-metrics/05-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From reportsRouter (Plan 05):
- reports.burndown({ sprintId, dateRange? }) → BurndownPoint[]
- reports.velocity({ projectId, lastN? }) → VelocityPoint[]
- reports.cycleTime({ projectId, dateRange }) → CycleTimePoint[]
- reports.cfd({ projectId, dateRange }) → CfdPoint[]
- reports.throughput({ projectId, dateRange }) → ThroughputPoint[]
- reports.wipOverTime({ projectId, dateRange }) → WipPoint[]

From 05-UI-SPEC.md LayerChart composition table:
- BurndownChart: Chart, Svg, Axis, Line, Area (reference), Highlight, Tooltip, Legend, ReferenceLines
- VelocityChart: Chart, Svg, Axis, Bars, Line (overlay), Tooltip, Legend
- CfdChart: Chart, Svg, Axis, Area (stacked), Tooltip, Legend
- CycleTimeChart: Chart, Svg, Axis, Points (scatter), ReferenceLines (percentiles), Tooltip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate reports page to tRPC and create report infrastructure components</name>
  <files>src/web/src/routes/projects/[id]/reports/+page.server.ts, src/web/src/routes/projects/[id]/reports/+page.svelte, src/web/src/lib/components/reports/ReportCard.svelte, src/web/src/lib/components/reports/DateRangePicker.svelte, src/web/src/app.css</files>
  <read_first>
    - src/web/src/routes/projects/[id]/reports/+page.server.ts (current — find openProductDb usage to remove)
    - src/web/src/routes/projects/[id]/reports/+page.svelte (current — understand tab structure)
    - src/web/src/app.css (find --chart-1 through --chart-5 vars to extend)
    - .planning/phases/05-task-management-metrics/05-UI-SPEC.md (lines 1046-1095 — Screen 8 reports dashboard)
  </read_first>
  <action>
**+page.server.ts** — REWRITE to eliminate openProductDb ARCH violation:
```typescript
// Replace openProductDb with thin pass-through — data fetched client-side via tRPC
export async function load({ params }) {
  return { projectId: params.id };
}
```
Remove ALL imports of `openProductDb`, `loadBurndown`, `loadVelocity`, `loadWip`, etc. The page now fetches data client-side via tRPC client.

**+page.svelte** — REWRITE the reports page:
- Keep existing tab structure (Sprint Reports, Flow Metrics, Project, Team, Quality)
- Replace data tables with chart component placeholders
- Each tab loads relevant chart components
- Add DateRangePicker and scope selector at top
- Charts loaded via tRPC client calls in `$effect` blocks (client-side, not server load)
- Tab structure per D-53 (project + workspace scope toggle)

**ReportCard.svelte** — Chart container shell:
- Props: `{ title: string; description?: string; loading?: boolean }`
- Layout: shadcn Card with header (title + description), skeleton loader when loading, slot for chart content.
- `data-report-card` attribute.

**DateRangePicker.svelte** (per D-55):
- Props: `{ value: { from: Date; to: Date }; onChange: (range) => void }`
- Preset buttons: Last 7d, 14d, 30d, 90d, Current Sprint
- Custom range: use shadcn-svelte Calendar component for date selection
- `data-date-range-picker` attribute.

**app.css** — Add chart color tokens (per D-58):
```css
/* After existing --chart-5 */
--chart-6: oklch(0.65 0.15 200);
--chart-7: oklch(0.70 0.12 280);
--chart-8: oklch(0.60 0.18 340);
```
Add both to `:root` and `.dark` themes.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && grep -vc "openProductDb" src/web/src/routes/projects/[id]/reports/+page.server.ts && grep -c "chart-6\|chart-7\|chart-8" src/web/src/app.css && ls src/web/src/lib/components/reports/ReportCard.svelte src/web/src/lib/components/reports/DateRangePicker.svelte 2>&1 | grep -c "svelte"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "openProductDb" src/web/src/routes/projects/[id]/reports/+page.server.ts` returns 0 (REMOVED)
    - `grep "projectId\|params.id" src/web/src/routes/projects/[id]/reports/+page.server.ts` matches
    - `grep -c "chart-6" src/web/src/app.css` returns >= 1
    - `grep -c "chart-7" src/web/src/app.css` returns >= 1
    - `grep -c "chart-8" src/web/src/app.css` returns >= 1
    - ReportCard.svelte and DateRangePicker.svelte exist
    - `grep "data-report-card" src/web/src/lib/components/reports/ReportCard.svelte` matches
    - `grep "data-date-range-picker" src/web/src/lib/components/reports/DateRangePicker.svelte` matches
  </acceptance_criteria>
  <done>Reports page migrated to tRPC (openProductDb ARCH violation eliminated). Report infrastructure components created. Chart color tokens extended.</done>
</task>

<task type="auto">
  <name>Task 2: Create 8 LayerChart chart components with client-only dynamic import</name>
  <files>src/web/src/lib/components/charts/BurndownChart.svelte, src/web/src/lib/components/charts/BurnupChart.svelte, src/web/src/lib/components/charts/VelocityChart.svelte, src/web/src/lib/components/charts/CfdChart.svelte, src/web/src/lib/components/charts/CycleTimeChart.svelte, src/web/src/lib/components/charts/ThroughputChart.svelte, src/web/src/lib/components/charts/WipChart.svelte, src/web/src/lib/components/charts/MonteCarloChart.svelte</files>
  <read_first>
    - .planning/phases/05-task-management-metrics/05-PATTERNS.md (lines 1146-1178 — chart SSR guard pattern)
    - .planning/phases/05-task-management-metrics/05-UI-SPEC.md (lines 1096-1340 — all 18 chart type specs)
  </read_first>
  <action>
All chart components MUST use the SSR guard pattern (per D-57, Pitfall 4):
```svelte
<script lang="ts">
  import { browser } from "$app/environment";
  let Chart = $state<any>(null);
  $effect(() => {
    if (browser) {
      import("layerchart").then((mod) => { Chart = mod; });
    }
  });
</script>
{#if browser && Chart}
  <!-- LayerChart composition -->
{:else}
  <!-- Skeleton loader -->
{/if}
```

**BurndownChart.svelte** (D-35):
- Props: `{ data: Array<{ date: Date; pointsRemaining: number; scopeTotal: number }> }`
- LayerChart: Chart + Svg + Axis (x=date, y=points) + Line (actual remaining) + Line (ideal burndown, dashed) + Area (below actual) + Tooltip + Legend
- Ideal line: straight from scopeTotal at day 0 to 0 at end date.
- Color: chart-1 for actual, chart-2 (dashed) for ideal.

**BurnupChart.svelte** (D-36):
- Props: `{ data: Array<{ date: Date; pointsCompleted: number; scopeTotal: number }> }`
- Line for completed (ascending), Line for scope total (shows creep).
- Color: chart-1 for completed, chart-3 for scope.

**VelocityChart.svelte** (D-37):
- Props: `{ data: Array<{ sprintName: string; pointsCompleted: number; rollingAvg: number }> }`
- Bars for points per sprint + Line overlay for rolling average.
- Bar color: chart-1 (on-target green), chart-5 (below amber).
- Color coding: if pointsCompleted >= rollingAvg → chart-1, else chart-5.

**CfdChart.svelte** (D-39):
- Props: `{ data: Array<{ date: Date; backlog: number; unstarted: number; started: number; completed: number }> }`
- Stacked Area chart with one band per status category.
- Colors: chart-1 (completed/green), chart-2 (started/blue), chart-3 (unstarted/gray), chart-4 (backlog/light).

**CycleTimeChart.svelte** (D-40):
- Props: `{ data: Array<{ taskId: string; completionDate: Date; cycleTimeHours: number }>; percentiles: { p50: number; p75: number; p95: number } }`
- Scatter plot: x=completionDate, y=cycleTimeHours.
- ReferenceLines at p50, p75, p95 with labels.
- Percentile bands as semi-transparent Area fills.

**ThroughputChart.svelte** (D-42):
- Props: `{ data: Array<{ week: string; completedCount: number; rollingAvg: number }> }`
- Bars for weekly count + Line for rolling average.

**WipChart.svelte** (D-43):
- Props: `{ data: Array<{ date: Date; wipCount: number }>; wipLimit?: number }`
- Area chart for WIP over time. ReferenceLine at wipLimit (red, dashed).
- Highlight area above limit in destructive color.

**MonteCarloChart.svelte** (D-105..D-108):
- Props: `{ data: { remaining: number; throughputHistory: number[] }; targetDate?: Date }`
- Client-side Monte Carlo: run 1000 iterations sampling from throughputHistory.
- Fan chart: layered Area bands at p50 (dark), p75, p85, p95 (lightest).
- ReferenceLine at targetDate (vertical).
- Computation: `monteCarloSimulation(remaining, throughputHistory, 1000)` — pure function, ~10ms.

All charts: interactive Tooltip on hover showing exact values per D-59. Use CSS variables `hsl(var(--chart-N))` for colors.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && ls src/web/src/lib/components/charts/*.svelte 2>&1 | grep -c "svelte" && grep -c "browser" src/web/src/lib/components/charts/BurndownChart.svelte && grep -c "layerchart" src/web/src/lib/components/charts/BurndownChart.svelte</automated>
  </verify>
  <acceptance_criteria>
    - 8 chart component files exist in `src/web/src/lib/components/charts/`
    - `grep "browser" src/web/src/lib/components/charts/BurndownChart.svelte` matches (SSR guard)
    - `grep "import.*layerchart" src/web/src/lib/components/charts/BurndownChart.svelte` matches (dynamic import)
    - `grep "browser" src/web/src/lib/components/charts/CfdChart.svelte` matches
    - `grep "browser" src/web/src/lib/components/charts/MonteCarloChart.svelte` matches
    - `grep "monteCarloSimulation\|1000.*iterations" src/web/src/lib/components/charts/MonteCarloChart.svelte` matches
    - All charts have Props interface with typed data input
    - No chart imports layerchart at module top level (all dynamic)
  </acceptance_criteria>
  <done>8 chart components render via LayerChart with client-only dynamic import. Monte Carlo runs 1000 iterations client-side. All interactive with tooltips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tRPC → client charts | Report data flows to browser for rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-22 | Information Disclosure | Reports data | mitigate | reportsRouter uses permissionedProcedure; data scoped to org |
| T-05-23 | Denial of Service | Monte Carlo | accept | Client-side computation; 1000 iterations ~10ms; no server impact |
</threat_model>

<verification>
- Reports page loads without openProductDb
- Chart components don't crash on SSR (browser guard present)
- LayerChart dynamic import resolves
</verification>

<success_criteria>
All 8 primary charts render real data from tRPC. Reports page ARCH-01 violation eliminated. Monte Carlo forecast runs client-side. TSK-03, TSK-04, TSK-05 visual requirements met.
</success_criteria>

<output>
After completion, create `.planning/phases/05-task-management-metrics/05-09-SUMMARY.md`
</output>
