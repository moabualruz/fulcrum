---
phase: 05-task-management-metrics
plan: 06
type: execute
wave: 3
depends_on: [03, 04, 05]
files_modified:
  - src/trpc/router.ts
  - src/services/TaskService.ts
  - src/services/SprintService.ts
  - src/services/AutomationService.ts
  - src/services/AutomationService.test.ts
  - src/server/trpc/routers/automations.ts
autonomous: true
requirements: [TSK-07, TSK-08]

must_haves:
  truths:
    - "All new routers (comments, workflows, relationships, reports, automations) mounted in AppRouter"
    - "TaskService extended with transition validation call and watcher auto-subscribe on assign"
    - "SprintService extended with capacity math, retro notes, close with disposition"
    - "AutomationService evaluates rules on EventBus events with cycle detection"
  artifacts:
    - path: "src/trpc/router.ts"
      provides: "Updated AppRouter with all Phase 5 routers"
      contains: "commentsRouter"
    - path: "src/services/AutomationService.ts"
      provides: "Rule-based automation engine"
      exports: ["AutomationService"]
    - path: "src/services/SprintService.ts"
      provides: "Extended sprint operations"
      contains: "getCapacityPreview"
  key_links:
    - from: "src/trpc/router.ts"
      to: "all Phase 5 routers"
      via: "import + mount"
      pattern: "commentsRouter|workflowsRouter|relationshipsRouter|reportsRouter|automationsRouter"
---

<objective>
Integration plan: mount all Phase 5 routers in AppRouter, extend TaskService/SprintService with new capabilities, create AutomationService. This plan exclusively owns the shared files (HIGH-06 fix).

Purpose: Eliminates Wave 2 same-file conflicts by making this the single plan that touches router.ts and TaskService.ts.
Output: Updated AppRouter, extended services, AutomationService.
</objective>

<execution_context>
@.planning/phases/05-task-management-metrics/05-RESEARCH.md
@.planning/phases/05-task-management-metrics/05-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-task-management-metrics/05-03-SUMMARY.md
@.planning/phases/05-task-management-metrics/05-04-SUMMARY.md
@.planning/phases/05-task-management-metrics/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend TaskService + SprintService + mount existing routers (NOT automations)</name>
  <files>src/trpc/router.ts, src/services/TaskService.ts, src/services/SprintService.ts</files>
  <read_first>
    - src/trpc/router.ts (current router mounts -- full file)
    - src/services/TaskService.ts (current methods, emitTaskEvent, bulkUpdate)
    - src/services/CommentService.ts (from Plan 03 -- for watcher subscribe call)
    - src/services/WorkflowService.ts (from Plan 04 -- for validateTransition)
    - src/services/RelationshipService.ts (from Plan 04)
    - src/server/trpc/routers/comments.ts (export name)
    - src/server/trpc/routers/workflows.ts (export name)
    - src/server/trpc/routers/relationships.ts (export name)
    - src/services/SprintService.ts (current close(), get(), methods)
    - src/db/entities/tasks/FieldDependencyRule.ts (entity from Plan 02)
  </read_first>
  <action>
**src/trpc/router.ts** -- Add imports and mounts for Phase 5 routers EXCEPT automations (automationsRouter does not exist yet -- created in Task 2):
```typescript
import { commentsRouter } from "../server/trpc/routers/comments.ts";
import { workflowsRouter } from "../server/trpc/routers/workflows.ts";
import { relationshipsRouter } from "../server/trpc/routers/relationships.ts";
```
Note: reportsRouter import likely already exists (check file). If stub, leave import as-is since Plan 05 overwrote the file.

Mount in appRouter:
```typescript
comments: commentsRouter,
workflows: workflowsRouter,
relationships: relationshipsRouter,
// reports: reportsRouter -- likely already mounted, verify
// automations: automationsRouter -- mounted in Task 2 after file creation
```

**src/services/TaskService.ts** -- Extend with:

1. **Transition validation** (D-24): In `updateStatus` / task update methods, before changing status, call `WorkflowService.validateTransition(orgId, projectId, fromStatus, toStatus)`. If `!allowed`, throw TRPCError with FORBIDDEN code and reason message.

2. **Watcher auto-subscribe on assign** (D-08): When assigneeId changes, call `CommentService.subscribe(orgId, taskId, newAssigneeId, 'assign')`.

3. **Started timestamp** (D-25): When task moves to 'started' category status, set `task.startedAt = new Date()` if not already set.

4. **Field-change events** (D-34): Update emitTaskEvent to populate `fieldName`, `fromValue`, `toValue` on the Event entity (use the new columns from Plan 01).

5. **Server-side field dependency validation** (HIGH-03, D-111): In create/update methods, after all field changes, query FieldDependencyRule entities INLINE using `em.find(FieldDependencyRule, { project: projectId, action: 'require' })`. For each matching rule: check if source field matches source_value AND target field is empty -> reject with validation error listing missing required fields. DO NOT import FieldDependencyService (it does not exist yet -- Plan 12 creates it and can refactor this inline logic into the service later).

6. **Remove ensureTaskProjectColumn** DDL call if still present (anti-pattern from RESEARCH.md). project_id now exists via migration.

**src/services/SprintService.ts** -- Extend (DO NOT rewrite, only add methods):
- `getCapacityPreview(orgId, sprintId)`: query tasks where sprintId matches, sum points -> assigned. Get sprint.capacityPoints -> capacity. Return `{ assigned, capacity, percentage: assigned/capacity*100 }`. (D-27, MEDIUM-02 fix: this is the method Plan 08 depends on)
- `close(orgId, sprintId, disposition: 'rollover'|'backlog'|'keep')`: existing close logic + for 'rollover': move incomplete tasks to next sprint; for 'backlog': clear sprintId on incomplete tasks; for 'keep': leave as-is. Persist closedSummary with counts (D-28).
- `saveRetrospective(orgId, sprintId, notes, summary)`: update sprint.retrospectiveNotes and sprint.closedSummary (D-29).
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg "commentsRouter|workflowsRouter|relationshipsRouter" src/trpc/router.ts && rg "validateTransition\|FieldDependencyRule\|subscribe.*assign" src/services/TaskService.ts && rg "getCapacityPreview\|saveRetrospective" src/services/SprintService.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/trpc/router.ts imports and mounts commentsRouter, workflowsRouter, relationshipsRouter (NOT automationsRouter yet)
    - TaskService calls WorkflowService.validateTransition before status changes (D-24)
    - TaskService auto-subscribes assignee as watcher on assign (D-08)
    - TaskService sets startedAt when entering started category (D-25)
    - TaskService emits field_name/from_value/to_value in events (D-34)
    - TaskService validates field dependency rules INLINE using em.find(FieldDependencyRule) -- no FieldDependencyService import (HIGH-03)
    - No ensureTaskProjectColumn DDL call remains
    - SprintService has getCapacityPreview returning {assigned, capacity, percentage}
    - SprintService.close handles disposition parameter (D-28)
    - SprintService.saveRetrospective persists notes + summary (D-29)
    - `bun run build` passes (no missing imports since automationsRouter not referenced yet)
  </acceptance_criteria>
  <done>AppRouter has comments/workflows/relationships routers. TaskService extended with transition validation, watcher subscribe, inline field dependency validation (HIGH-03/HIGH-06). SprintService extended with capacity/retro/close.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create AutomationService + automations router + mount in AppRouter</name>
  <files>src/services/AutomationService.ts, src/services/AutomationService.test.ts, src/server/trpc/routers/automations.ts, src/trpc/router.ts</files>
  <read_first>
    - src/subscriptions/event-bus.ts (EventBus subscribe/publish)
    - src/db/entities/tasks/ProjectAutomation.ts (entity from Plan 02)
    - .planning/phases/05-task-management-metrics/05-CONTEXT.md (D-26..D-30, D-89..D-92)
  </read_first>
  <behavior>
    - AutomationService.evaluate(event, orgId, projectId) finds matching automations, executes actions, respects cycle limit of 5
    - AutomationService.create/update/delete/list CRUD for ProjectAutomation
    - AutomationService.getTemplates() returns 4 predefined templates (D-92)
  </behavior>
  <action>
**AutomationService.ts** (D-89..D-92):
- Constructor(em, eventBus)
- `evaluate(event, orgId, projectId)`: query enabled automations for project matching trigger_type to event.verb. For each match: evaluate condition (if present) against event payload. If condition passes, execute action. Track execution depth -- if chain exceeds 5, log warning and halt (D-91 cycle detection).
- Actions dispatch: `set_status` -> update task, `set_assignee` -> update task, `add_label`/`remove_label` -> update task.labels, `add_comment` -> CommentService.createComment (MEDIUM-01 fix: uses CommentService from Plan 03 -- dependency declared), `subscribe_watcher` -> CommentService.subscribe, `move_to_sprint` -> update task.sprintId.
- Increment `executionCount` on automation after successful execution.
- `getTemplates()` returns 4 hardcoded templates per D-92.
- CRUD methods: create/update/delete/list standard pattern.
- `setupAutomationListener(eventBus)`: subscribe to all task.* events, call evaluate.

**src/server/trpc/routers/automations.ts** -- Create file:
```typescript
export const automationsRouter = t.router({
  list: /* permissioned, input: projectId, delegate to service */,
  create: /* permissioned, input: full automation config, delegate */,
  update: /* permissioned, input: id + partial config, delegate */,
  delete: /* permissioned, input: id, delegate */,
  templates: /* permissioned, query returning predefined templates */,
});
```

**src/trpc/router.ts** -- NOW add automationsRouter (file exists from this task):
```typescript
import { automationsRouter } from "../server/trpc/routers/automations.ts";
// Add to appRouter:
automations: automationsRouter,
```

Tests for AutomationService:
- Test evaluate matches trigger and executes action
- Test cycle detection halts at depth 5
- Test condition evaluation (field equals check)
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/services/AutomationService.test.ts && rg "automationsRouter" src/trpc/router.ts</automated>
  </verify>
  <acceptance_criteria>
    - AutomationService.ts exports class with evaluate, setupAutomationListener, CRUD, getTemplates
    - Cycle detection at depth 5 (D-91)
    - automationsRouter exports with list, create, update, delete, templates procedures
    - automationsRouter mounted in AppRouter (import + mount added in this task AFTER file creation)
    - AutomationService tests pass
    - `bun run build` passes
  </acceptance_criteria>
  <done>AutomationService with cycle detection, automations router created AND mounted. MEDIUM-01 dependency resolved. Build passes because automationsRouter file exists before mount.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EventBus -> AutomationService | Internal events trigger automated mutations |
| client -> automationsRouter | User-defined rules execute with system privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Elevation | automation actions | mitigate | Actions execute within same org scope; cannot cross org boundary |
| T-05-14 | Denial of Service | automation chains | mitigate | Max 5 chain depth with halt + warning log (D-91) |
| T-05-15 | Tampering | automation config | mitigate | Only project members can create automations (permissionedProcedure) |
</threat_model>

<verification>
- All tests pass
- AppRouter compiles with new mounts (automationsRouter mounted AFTER file creation in Task 2)
- `bun run build` passes
- No same-file conflicts with Plans 03-05 (they don't touch router.ts or TaskService.ts)
</verification>

<success_criteria>
HIGH-06 resolved: this plan exclusively owns router.ts and TaskService.ts. All Phase 5 routers mounted (automations last, after file creation). TaskService has transition validation (D-24), inline field dependency validation using em.find(FieldDependencyRule) without importing FieldDependencyService (HIGH-03), watcher subscribe (D-08). AutomationService operational with cycle detection (D-91). SprintService has capacity (D-27) and retro (D-29). MEDIUM-01/02 dependency fixes applied.
</success_criteria>

<output>
After completion, create `.planning/phases/05-task-management-metrics/05-06-SUMMARY.md`
</output>
