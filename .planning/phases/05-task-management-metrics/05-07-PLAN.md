---
phase: 05-task-management-metrics
plan: 07
type: execute
wave: 4
depends_on: [06]
files_modified:
  - src/web/src/lib/components/tasks/TaskDetailPanel.svelte
  - src/web/src/lib/components/tasks/TaskComments.svelte
  - src/web/src/lib/components/tasks/ActivityFeed.svelte
  - src/web/src/lib/components/tasks/WatcherList.svelte
  - src/web/src/lib/components/tasks/MentionSuggestion.svelte
autonomous: true
requirements: [TSK-01, TSK-02]

must_haves:
  truths:
    - "Task detail panel opens as right side panel preserving list context"
    - "Comments with TipTap editor, threading, resolve/unresolve render"
    - "Activity feed shows field-change diffs"
    - "Watcher list with subscribe/unsubscribe"
    - "Mention suggestion popup shows both users and teams (D-101)"
    - "Custom fields visible in task detail panel (D-78 partial fix)"
  artifacts:
    - path: "src/web/src/lib/components/tasks/TaskDetailPanel.svelte"
      provides: "Right side panel with all sections"
      contains: "TaskComments"
    - path: "src/web/src/lib/components/tasks/TaskComments.svelte"
      provides: "Comment list + TipTap editor"
      contains: "tiptap"
    - path: "src/web/src/lib/components/tasks/MentionSuggestion.svelte"
      provides: "Dual source mention popup (users + teams)"
      contains: "team"
  key_links:
    - from: "TaskDetailPanel.svelte"
      to: "tRPC comments/watchers"
      via: "trpc.comments.list.query"
      pattern: "trpc.comments"
---

<objective>
Build the task detail side panel with comments, activity feed, watchers, and mention suggestions. Implements D-16..D-18 (panel UX) and D-101 (dual mention sources).

Purpose: Central task interaction surface — where users manage comments, view activity, control watchers.
Output: 5 Svelte components for task detail experience.
</objective>

<execution_context>
@.planning/phases/05-task-management-metrics/05-UI-SPEC.md
@.planning/phases/05-task-management-metrics/05-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-task-management-metrics/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TaskDetailPanel with sections</name>
  <files>src/web/src/lib/components/tasks/TaskDetailPanel.svelte, src/web/src/lib/components/tasks/ActivityFeed.svelte, src/web/src/lib/components/tasks/WatcherList.svelte</files>
  <read_first>
    - src/web/src/lib/components/tasks/TaskTable.svelte (existing component pattern — imports, tRPC usage)
    - src/web/src/lib/components/tasks/TaskList.svelte (component structure)
    - .planning/phases/05-task-management-metrics/05-UI-SPEC.md (TaskDetailPanel section, spacing)
    - .planning/phases/05-task-management-metrics/05-CONTEXT.md (D-16..D-18)
  </read_first>
  <action>
**TaskDetailPanel.svelte** (D-16, D-17, D-18):
- Props: `taskId: string`, `onClose: () => void`, `onNavigate: (direction: 'prev'|'next') => void`
- Layout: fixed right panel (width ~480px), slide-in animation from right
- Sections (top to bottom per D-17):
  1. Task type badge + Title (inline edit via contenteditable or input). Badge shows icon: Epic=purple diamond, Task=blue circle, Subtask=gray nested icon, Bug=red bug icon. Type changeable via dropdown if project has multiple types enabled.
  2. Status/Priority/Assignee/Labels bar (horizontal row of pickers)
  3. Rich text description (TipTap editor placeholder — full wiring in Plan 13 collab)
  4. Custom fields section (D-78 fix: render custom field values from task data, editable)
  5. Dependencies section (show blockers/blocked-by from trpc.relationships.listForTask)
  6. Subtasks list (child tasks where parent_id = this task). "Add subtask" button creates task with taskType='subtask' + parent_id set. Shows progress (N/M complete).
  7. Parent link: if task has parent_id, show breadcrumb "Epic Name > This Task" at top
  8. Tabbed area: Comments tab + Activity tab
  9. Watchers section (bottom)
- Keyboard: listen for J/K (navigate tasks via onNavigate), Esc (close via onClose) — per D-18
- Fetch task data via `trpc.tasks.get.query({ taskId })`

**ActivityFeed.svelte** (D-05):
- Props: `taskId: string`
- Fetch events via `trpc.audit.list.query` filtered by subjectId=taskId (or new procedure if needed)
- Render field-change diffs: "User changed status from X to Y", "User changed assignee from A to B"
- Use Event.fieldName, fromValue, toValue columns
- Format timestamps relatively (2h ago, yesterday)

**WatcherList.svelte** (D-07, D-09):
- Props: `taskId: string`
- Fetch via `trpc.comments.watchers.query({ taskId })`
- Render avatar stack (max 5 visible + overflow count per UI spec: 24px avatar, 8px overlap)
- Subscribe/unsubscribe button for current user
- Show source badge on hover (manual, mention, assign, create)
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && ls src/web/src/lib/components/tasks/TaskDetailPanel.svelte src/web/src/lib/components/tasks/ActivityFeed.svelte src/web/src/lib/components/tasks/WatcherList.svelte</automated>
  </verify>
  <acceptance_criteria>
    - TaskDetailPanel.svelte exists with all 8 sections per D-17
    - Has J/K/Esc keyboard handlers (D-18)
    - Custom fields section present (D-78 partial fix)
    - Dependencies section shows relationships (D-19/D-21 partial)
    - ActivityFeed.svelte renders field-change diffs (D-05)
    - WatcherList.svelte shows avatar stack with subscribe/unsubscribe
    - All use tRPC queries (not raw fetch)
  </acceptance_criteria>
  <done>Task detail panel with all sections, keyboard nav, activity feed, watchers</done>
</task>

<task type="auto">
  <name>Task 2: Create TaskComments + MentionSuggestion (dual source)</name>
  <files>src/web/src/lib/components/tasks/TaskComments.svelte, src/web/src/lib/components/tasks/MentionSuggestion.svelte</files>
  <read_first>
    - src/web/src/lib/tiptap/ (existing TipTap setup if any)
    - src/web/package.json (verify TipTap extensions installed)
    - .planning/phases/05-task-management-metrics/05-CONTEXT.md (D-01..D-04, D-100, D-101)
    - .planning/phases/05-task-management-metrics/05-UI-SPEC.md (comment component spec)
  </read_first>
  <action>
**TaskComments.svelte** (D-01, D-02, D-03, D-04):
- Props: `taskId: string`
- Fetch comments via `trpc.comments.threaded.query({ taskId })` (returns tree structure)
- Render threaded: top-level comments rendered full-width, replies indented 24px with vertical thread line (border-left: 2px solid border-muted). Nested replies indent further (max 3 levels, then flatten). Matches GitHub Issues threading UX.
- Each reply shows smaller avatar (20px vs 28px for top-level), "replying to @name" context
- Reply button on each comment opens inline sub-editor below that comment with parentCommentId pre-set
- Resolved comments collapse (show "Resolved by @name" header only, expand on click) per D-03
- Each comment shows: author avatar, name, timestamp, body rendered from TipTap JSON, reaction bar
- Reaction bar: 6 emoji buttons (D-04), clicking adds/removes via trpc.comments.addReaction/removeReaction
- Resolve/Unresolve button on each comment (calls trpc.comments.resolve/unresolve)
- New comment editor at bottom: TipTap with extensions:
  - @tiptap/extension-mention (configured with MentionSuggestion)
  - @tiptap/extension-task-list (checklists)
  - @tiptap/extension-placeholder ("Write a comment...")
- Submit sends TipTap JSON body to trpc.comments.create

**MentionSuggestion.svelte** (D-100, D-101 — MEDIUM-03 fix):
- TipTap mention extension suggestion plugin component
- Two data sources: users (from org members) and teams (from org teams)
- Query both on keystroke after '@': fetch users via existing org members endpoint, fetch teams
- Display: show type indicator icon (person icon for user, group icon for team)
- On select: insert mention node with `attrs: { id, type: 'user'|'team', label: displayName }`
- The `type` attribute is what CommentService.extractMentions uses to discriminate (D-100)
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && ls src/web/src/lib/components/tasks/TaskComments.svelte src/web/src/lib/components/tasks/MentionSuggestion.svelte && rg "team" src/web/src/lib/components/tasks/MentionSuggestion.svelte</automated>
  </verify>
  <acceptance_criteria>
    - TaskComments.svelte fetches via trpc.comments.threaded (tree structure)
    - Renders replies indented with vertical thread line (D-01 nested threading)
    - Reply button opens inline editor with parentCommentId
    - Max 3 indent levels then flatten
    - Has resolve/unresolve UI (D-03)
    - Has reaction emoji bar with 6 emojis (D-04)
    - TipTap editor configured with mention, task-list, placeholder extensions
    - MentionSuggestion.svelte has dual sources: users AND teams (D-101, MEDIUM-03)
    - Mention node includes type:'user'|'team' attribute (D-100 discriminator)
  </acceptance_criteria>
  <done>Comments UI with TipTap + dual mention sources (users+teams). D-100/D-101/MEDIUM-03 addressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TipTap JSON → server | Comment body is user-controlled rich text JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Tampering | comment body XSS | mitigate | TipTap renders sanitized HTML from JSON schema; no raw HTML injection |
| T-05-17 | Info Disclosure | mention suggestions | mitigate | Only show org members/teams for current org |
</threat_model>

<verification>
- All 5 component files exist
- MentionSuggestion has team support
- `bun run build` passes (Svelte compilation)
</verification>

<success_criteria>
Task detail panel fully functional with comments (threading, resolve, reactions), activity feed (field diffs), watchers (avatar stack), and dual-source mentions (users + teams per D-100/D-101). Custom fields visible in panel (D-78). Keyboard nav J/K/Esc (D-18).
</success_criteria>

<output>
After completion, create `.planning/phases/05-task-management-metrics/05-07-SUMMARY.md`
</output>
