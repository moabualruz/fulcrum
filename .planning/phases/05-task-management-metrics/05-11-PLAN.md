---
phase: 05-task-management-metrics
plan: 11
type: execute
wave: 5
depends_on: [06, 07, 08]
files_modified:
  - src/web/src/lib/components/tasks/FilterBuilder.svelte
  - src/web/src/lib/components/tasks/QuickFilters.svelte
  - src/web/src/lib/components/tasks/BulkActionBar.svelte
  - src/web/src/lib/components/tasks/BulkCustomFieldEdit.svelte
  - src/services/TaskService.test.ts
  - src/filters/ast.test.ts
autonomous: true
requirements: [TSK-11, TSK-12, TSK-13]

must_haves:
  truths:
    - "Filter builder creates AST from visual chip selections"
    - "Saved views persist filter + view config round-trip"
    - "Bulk ops handle 50+ tasks in single transaction with max 200 cap (D-75)"
    - "Custom fields all 9 types verified end-to-end"
    - "Bulk edit includes custom fields (D-78)"
    - "Filter builder supports custom field references (D-72)"
    - "Labels with groups model backed by data (D-79)"
    - "Priority levels as proper enum (D-80)"
  artifacts:
    - path: "src/web/src/lib/components/tasks/FilterBuilder.svelte"
      provides: "Visual filter chip builder"
      contains: "Select"
    - path: "src/web/src/lib/components/tasks/BulkActionBar.svelte"
      provides: "Bulk action toolbar"
      contains: "transaction"
    - path: "src/filters/ast.test.ts"
      provides: "Extended filter AST tests including custom fields"
  key_links:
    - from: "FilterBuilder.svelte"
      to: "src/filters/ast.ts"
      via: "builds AST nodes from UI selections"
      pattern: "ast"
---

<objective>
Filter builder UI (chip-based, Linear-style), saved view round-trip verification, bulk operations with 50+ task testing, and custom field verification across all 9 types.

Purpose: Power-user productivity features — filter, bulk edit, custom fields.
Output: Filter components, bulk action bar, verification tests.
</objective>

<execution_context>
@.planning/phases/05-task-management-metrics/05-UI-SPEC.md
@.planning/phases/05-task-management-metrics/05-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-task-management-metrics/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FilterBuilder + QuickFilters + SavedView verification</name>
  <files>src/web/src/lib/components/tasks/FilterBuilder.svelte, src/web/src/lib/components/tasks/QuickFilters.svelte, src/filters/ast.test.ts</files>
  <read_first>
    - src/filters/ast.ts (existing filter AST implementation)
    - src/filters/ast.test.ts (existing tests — extend, don't replace)
    - src/db/entities/tasks/SavedView.ts (existing entity — check fields)
    - .planning/phases/05-task-management-metrics/05-CONTEXT.md (D-69..D-72)
  </read_first>
  <action>
**FilterBuilder.svelte** (D-69, D-72):
- No external filter library — use shadcn-svelte primitives (Select, Popover, Badge, Button) per D-69
- UX: click "+ Filter" → Popover opens with field picker (Select) → pick field → pick operator (equals/contains/in/not-in/gt/lt) → pick value (type-aware: date picker for dates, multi-select for labels, user picker for assignee) → chip appears
- Chips: each active filter rendered as Badge with field name + operator + value. X button removes filter.
- AND/OR combinator toggle between filter chips
- Custom field references (D-72): field picker includes custom field definitions from project. Operators are type-aware (text→contains/equals, number→gt/lt/equals, date→before/after, select→in/not-in).
- Output: AST node (compatible with existing src/filters/ast.ts format). Emit `onFilterChange(ast)` event.
- Labels filter shows label groups (D-79): group labels by group name in picker
- Priority filter shows 5 levels (D-80): Urgent/High/Medium/Low/No Priority
- "Include archived" toggle (D-114): checkbox in filter toolbar. When checked, removes `archived_at IS NULL` default filter. Shows archived tasks with "Archived" badge.
- "Export" button (D-120): in filter/saved view toolbar. Triggers `trpc.reports.exportCsv` with current filter AST + visible columns. For >1000 rows, background job with download link.

**QuickFilters.svelte** (D-71):
- Sidebar component with preset buttons:
  - "My tasks" (assignee = currentUser)
  - "Recently updated" (updatedAt > 24h ago)
  - "Unassigned" (assignee is null)
  - "Blocked" (has blocking relationship)
  - "Overdue" (dueDate < today)
  - "My Work" (D-119: assignee = currentUser, cross-project, grouped by due date urgency)
- Each click sets filter AST and emits to parent

**ast.test.ts** — Extend existing tests (D-72, TSK-13):
- Test round-trip: create filter AST → save to SavedView → reload → apply → verify structure matches
- Test AND/OR combinators
- Test custom field reference filters (field_id based)
- Test all operator types per field type
- Verify filter compiles to valid query conditions
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/filters/ast.test.ts && ls src/web/src/lib/components/tasks/FilterBuilder.svelte src/web/src/lib/components/tasks/QuickFilters.svelte</automated>
  </verify>
  <acceptance_criteria>
    - FilterBuilder uses shadcn-svelte primitives only (D-69)
    - FilterBuilder supports custom field references in picker (D-72)
    - FilterBuilder outputs AST compatible with existing ast.ts
    - QuickFilters has 5 presets (D-71)
    - ast.test.ts verifies round-trip: create→save→reload→apply (TSK-13)
    - ast.test.ts tests AND/OR combinators
    - Tests pass
  </acceptance_criteria>
  <done>Filter builder with custom fields, quick filters, AST round-trip verified (TSK-13)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BulkActionBar with custom fields + 50-task verification + label/priority model</name>
  <files>src/web/src/lib/components/tasks/BulkActionBar.svelte, src/web/src/lib/components/tasks/BulkCustomFieldEdit.svelte, src/services/TaskService.test.ts</files>
  <read_first>
    - src/services/TaskService.ts (bulkUpdate, bulkDelete methods)
    - src/db/entities/tasks/CustomFieldDef.ts (field definitions)
    - src/db/entities/tasks/schemas.ts (CUSTOM_FIELD_TYPES — now 9 types)
    - .planning/phases/05-task-management-metrics/05-CONTEXT.md (D-73..D-80)
  </read_first>
  <behavior>
    - BulkActionBar dispatches bulk mutations for up to 200 tasks (D-75)
    - TaskService.bulkUpdate runs in single transaction with em.flush() (D-75)
    - Bulk events create one Event per field with affected_task_ids array (D-76)
    - Custom field bulk edit works for all 9 types (D-78, MEDIUM-05 fix)
    - 50+ task test exercises full pipeline (TSK-11)
  </behavior>
  <action>
**BulkActionBar.svelte** (D-73, D-74, D-78):
- Renders when `selectedTaskIds.length > 0`
- Shows count "N tasks selected"
- Action buttons (each opens picker Popover): Set Status, Set Assignee, Set Priority, Add Label, Remove Label, Move to Sprint, Set Due Date, Edit Custom Fields (D-78 MEDIUM-05 fix), Archive (D-114: calls trpc.tasks.archive with selected IDs), Delete
- On action confirm: calls `trpc.tasks.bulkUpdate.mutate({ taskIds, patch })` or `trpc.tasks.bulkDelete.mutate({ taskIds })`
- Max 200 enforcement (D-75): if selectedTaskIds.length > 200, disable actions with tooltip "Max 200 tasks per operation"
- Multi-select UX (D-73): parent component handles checkbox + shift-click + cmd-click + ctrl-a logic

**BulkCustomFieldEdit.svelte** (D-78 fix for MEDIUM-05):
- Picker that lists all custom fields for project
- Select field → show type-appropriate input (text input, number input, date picker, select dropdown, multi-select, checkbox, url input, user picker)
- On confirm: patch includes `customFields: { [fieldId]: value }`

**TaskService.test.ts** — Add bulk operation tests (TSK-11, D-75, D-76):
- Test bulk update 50+ tasks in single transaction: create 55 fixture tasks, call bulkUpdate with status change, verify all 55 updated
- Test max 200 cap: attempt 201 tasks → rejects with error
- Test bulk events: single Event created per changed field with affected_task_ids array (D-76)
- Test custom field types round-trip (TSK-12): for each of 9 types (text, number, date, select, multi_select, checkbox, url, user, json), create custom field def → set value on task → read back → verify. This is the verification-only test per D-77.
- Test label group model (MEDIUM-04, D-79): verify the task.labels JSON field supports structured arrays with {id, name, color, group} shape. No separate label_groups table exists — labels are stored as structured JSON array on the task entity. Test that saving labels with group property round-trips correctly.
- Test priority ordering: verify priority values map to Urgent(0)/High(1)/Medium(2)/Low(3)/None(4) (D-80)
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/services/TaskService.test.ts -t "bulk\|custom.field\|label\|priority"</automated>
  </verify>
  <acceptance_criteria>
    - BulkActionBar shows all action buttons per D-74
    - BulkCustomFieldEdit handles all 9 custom field types (D-78, MEDIUM-05)
    - Max 200 enforcement (D-75)
    - TaskService.test.ts: 50+ task bulk test passes (TSK-11)
    - TaskService.test.ts: bulk events use affected_task_ids (D-76)
    - TaskService.test.ts: all 9 custom field types round-trip (TSK-12)
    - Labels and priority model verified (D-79, D-80, MEDIUM-04)
    - Tests pass
  </acceptance_criteria>
  <done>Bulk actions with custom fields, 50+ task verification, label/priority model tested. TSK-11/TSK-12 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bulk mutation → DB | Large write in single transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24 | Denial of Service | bulk 200 tasks | mitigate | Hard cap at 200 (D-75), single transaction prevents partial state |
| T-05-25 | Tampering | bulk cross-project | mitigate | Service validates all taskIds belong to caller's org/project |
</threat_model>

<verification>
- Filter and bulk components exist
- Tests pass for AST round-trip, bulk 50+, custom fields
- `bun run build` passes
</verification>

<success_criteria>
Filter builder with custom field support (D-69..D-72). Saved view round-trip verified (TSK-13). Bulk ops tested with 50+ tasks, max 200 cap (TSK-11, D-75). Custom fields all 9 types verified (TSK-12). Bulk custom field edit (D-78, MEDIUM-05). Labels/priority model (D-79/D-80, MEDIUM-04).
</success_criteria>

<output>
After completion, create `.planning/phases/05-task-management-metrics/05-11-SUMMARY.md`
</output>
