---
phase: 05-task-management-metrics
plan: 08
type: execute
wave: 3
depends_on: [03, 04, 05, 06]
files_modified:
  - src/web/src/lib/components/board/TaskCard.svelte
  - src/web/src/lib/components/board/WipLimitIndicator.svelte
  - src/web/src/lib/components/board/SwimlaneHeader.svelte
  - src/web/src/lib/components/board/DensityToggle.svelte
  - src/web/src/lib/components/tasks/TaskListView.svelte
  - src/web/src/lib/components/sprints/BacklogTray.svelte
  - src/web/src/lib/components/sprints/SprintCapacityBar.svelte
autonomous: true
requirements:
  - TSK-07
  - TSK-11

must_haves:
  truths:
    - "Board cards drag between status columns via svelte-dnd-action"
    - "Board supports grouping by status, assignee, priority, label, sprint"
    - "WIP limit shows red warning when column exceeds limit"
    - "Card density toggles between compact and comfortable"
    - "List view uses TanStack Table with column resize, sort, virtual scroll"
    - "Sprint backlog tray allows drag-to-sprint planning"
  artifacts:
    - path: "src/web/src/lib/components/board/TaskCard.svelte"
      provides: "Kanban card with density variants"
      contains: "data-task-card"
    - path: "src/web/src/lib/components/tasks/TaskListView.svelte"
      provides: "TanStack Table powered list view"
      contains: "tanstack"
  key_links:
    - from: "src/web/src/lib/components/board/TaskCard.svelte"
      to: "src/web/src/lib/components/tasks/TaskDetailPanel.svelte"
      via: "click opens panel"
      pattern: "onclick.*TaskDetailPanel\|onSelect"
---

<objective>
Build board enhancements (DnD swimlanes, WIP limits, card density) and TanStack Table list view with virtual scrolling. Add sprint planning backlog tray.

Purpose: The board and list are the two primary task views. Board needs competitive DnD with grouping (D-10..D-13). List needs headless table with inline edit, virtual scroll for 10k+ tasks (D-14..D-15). Sprint planning UX needs drag-from-backlog (D-26).

Output: Board components (card, swimlane, WIP, density), TanStack list view, backlog tray, capacity bar.
</objective>

<execution_context>
@.planning/phases/05-task-management-metrics/05-UI-SPEC.md
@.planning/phases/05-task-management-metrics/05-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From svelte-dnd-action (installed):
```typescript
import { dndzone } from "svelte-dnd-action";
// use:dndzone={{ items, type, ... }} on container
```

From @tanstack/svelte-table (installed in Plan 01):
```typescript
import { createSvelteTable, getCoreRowModel, getSortedRowModel } from "@tanstack/svelte-table";
```

From @tanstack/svelte-virtual (installed in Plan 01):
```typescript
import { createVirtualizer } from "@tanstack/svelte-virtual";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create board components (TaskCard, WipLimit, Swimlane, Density) with DnD</name>
  <files>src/web/src/lib/components/board/TaskCard.svelte, src/web/src/lib/components/board/WipLimitIndicator.svelte, src/web/src/lib/components/board/SwimlaneHeader.svelte, src/web/src/lib/components/board/DensityToggle.svelte</files>
  <read_first>
    - src/web/src/lib/components/tasks/TaskTable.svelte (existing board/table component for pattern)
    - .planning/phases/05-task-management-metrics/05-UI-SPEC.md (lines 459-543 — Screen 1 board spec)
  </read_first>
  <action>
**TaskCard.svelte** (per D-13):
- Props: `{ task: TaskOutput; density: "compact" | "comfortable"; onSelect: (taskId: string) => void }`
- Compact mode: title + assignee avatar (24px circle) + priority icon (16px). Max height ~48px.
- Comfortable mode: title + 2-line description preview + labels (Badge component, max 3) + points badge + assignee avatar + priority icon. Max height ~96px.
- Blocked badge: if task has blocking relationships, show red "Blocked" Badge per D-20.
- Click → calls onSelect(task.id) to open TaskDetailPanel.
- `data-task-card` and `data-task-id={task.id}` attributes.
- Draggable styling: on drag start, reduce opacity to 0.5, add shadow-lg (per UI-SPEC animation 150ms ease-out).

**WipLimitIndicator.svelte** (per D-12):
- Props: `{ current: number; limit: number | null }`
- If limit is null, don't render.
- If current <= limit: render `{current}/{limit}` in muted text.
- If current > limit: render in destructive color (red), column header gets `border-destructive` class.
- Badge style: `bg-destructive/10 text-destructive` when exceeded.

**SwimlaneHeader.svelte** (per D-11):
- Props: `{ label: string; count: number; collapsed: boolean; onToggle: () => void }`
- Collapsible row header using shadcn-svelte Collapsible.
- Shows group label (e.g., assignee name, priority level), task count badge.
- Chevron icon rotates on collapse.

**DensityToggle.svelte** (per D-13):
- Props: `{ value: "compact" | "comfortable"; onChange: (v) => void }`
- Use shadcn-svelte toggle-group with two options: compact icon (rows), comfortable icon (cards).
- Persist preference in localStorage.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && ls src/web/src/lib/components/board/TaskCard.svelte src/web/src/lib/components/board/WipLimitIndicator.svelte src/web/src/lib/components/board/SwimlaneHeader.svelte src/web/src/lib/components/board/DensityToggle.svelte 2>&1 | grep -c "svelte"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist
    - `grep "data-task-card" src/web/src/lib/components/board/TaskCard.svelte` matches
    - `grep "compact.*comfortable" src/web/src/lib/components/board/TaskCard.svelte` matches (density variants)
    - `grep "destructive\|red\|exceeded" src/web/src/lib/components/board/WipLimitIndicator.svelte` matches
    - `grep "Collapsible\|collapsed" src/web/src/lib/components/board/SwimlaneHeader.svelte` matches
    - `grep "toggle-group\|localStorage" src/web/src/lib/components/board/DensityToggle.svelte` matches
    - All use `interface Props` + `$props()` (Svelte 5)
  </acceptance_criteria>
  <done>Board components deliver card density toggle, WIP limit warnings, collapsible swimlanes, and DnD-ready task cards.</done>
</task>

<task type="auto">
  <name>Task 2: Create TanStack Table list view and sprint planning components</name>
  <files>src/web/src/lib/components/tasks/TaskListView.svelte, src/web/src/lib/components/sprints/BacklogTray.svelte, src/web/src/lib/components/sprints/SprintCapacityBar.svelte</files>
  <read_first>
    - src/web/src/lib/components/tasks/TaskTable.svelte (existing table — understand current approach)
    - .planning/phases/05-task-management-metrics/05-UI-SPEC.md (lines 544-627 — Screen 2 list view spec)
    - .planning/phases/05-task-management-metrics/05-UI-SPEC.md (lines 807-850 — Screen 4 sprint board spec)
  </read_first>
  <action>
**TaskListView.svelte** (per D-14, D-15):
- Props: `{ tasks: TaskOutput[]; projectId: string; columns?: string[]; onTaskSelect: (id: string) => void }`
- Use `createSvelteTable` from `@tanstack/svelte-table`:
  - Define column defs: title, status, priority, assignee, labels, dueDate, points, sprint, createdAt + custom field columns from project
  - Enable: sorting (getSortedRowModel), column visibility, column resizing, multi-field grouping
- Use `createVirtualizer` from `@tanstack/svelte-virtual`:
  - Virtualize rows for 10k+ performance
  - Estimated row height: 40px (compact) or 52px (comfortable)
  - Overscan: 5 rows
- Inline editing: click cell → contenteditable or select dropdown for enum fields (status, priority, assignee)
- Column customization: right-click header → show/hide/reorder columns. Persist via SavedView entity.
- Row click → calls onTaskSelect(taskId) to open detail panel.
- `data-task-list-view` attribute.

**BacklogTray.svelte** (per D-26):
- Props: `{ projectId: string; currentSprintId: string }`
- Sliding panel from left side (300px wide, fixed) showing backlog tasks (no sprint assigned).
- Tasks draggable FROM backlog TO sprint board using `svelte-dnd-action` cross-container.
- On drop into sprint: call `trpc.tasks.update({ id, sprintId: currentSprintId })`.
- Filter/search input at top of tray.
- `data-backlog-tray` attribute.

**SprintCapacityBar.svelte** (per D-27):
- Props: `{ assignedPoints: number; capacityPoints: number }`
- Use shadcn-svelte Progress component.
- Color: green (< 80%), yellow (80-100%), red (> 100%).
- Label: `{assignedPoints} / {capacityPoints} pts ({utilization}%)`.
- Utilization = Math.round((assignedPoints / capacityPoints) * 100).
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && ls src/web/src/lib/components/tasks/TaskListView.svelte src/web/src/lib/components/sprints/BacklogTray.svelte src/web/src/lib/components/sprints/SprintCapacityBar.svelte 2>&1 | grep -c "svelte"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist
    - `grep "tanstack\|createSvelteTable\|svelte-table" src/web/src/lib/components/tasks/TaskListView.svelte` matches
    - `grep "createVirtualizer\|svelte-virtual" src/web/src/lib/components/tasks/TaskListView.svelte` matches
    - `grep "data-task-list-view" src/web/src/lib/components/tasks/TaskListView.svelte` matches
    - `grep "dndzone\|svelte-dnd-action" src/web/src/lib/components/sprints/BacklogTray.svelte` matches
    - `grep "data-backlog-tray" src/web/src/lib/components/sprints/BacklogTray.svelte` matches
    - `grep "Progress\|capacityPoints\|utilization" src/web/src/lib/components/sprints/SprintCapacityBar.svelte` matches
  </acceptance_criteria>
  <done>List view with virtual scrolling + inline edit handles 10k tasks. Backlog tray enables drag-to-sprint planning. Capacity bar visualizes sprint load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DnD drop → tRPC | Status/sprint changes from drag actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-20 | Tampering | DnD status change | mitigate | Drop triggers tRPC mutation → WorkflowService validates transition server-side (Plan 04) |
| T-05-21 | Denial of Service | Virtual scroll | accept | TanStack Virtual handles DOM recycling; server pagination if needed in future |
</threat_model>

<verification>
- All 7 component files exist
- TanStack Table and Virtual imports resolve
- svelte-dnd-action imports resolve
</verification>

<success_criteria>
Board and list views competitive with Linear: DnD with swimlanes and WIP limits on board, headless table with virtual scroll on list. Sprint planning via backlog tray functional.
</success_criteria>

<output>
After completion, create `.planning/phases/05-task-management-metrics/05-08-SUMMARY.md`
</output>
