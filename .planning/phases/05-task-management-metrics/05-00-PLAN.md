---
phase: 05-task-management-metrics
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/services/CommentService.test.ts
  - src/services/TaskService.test.ts
  - src/services/ReportService.test.ts
  - src/workers/metrics-rollup.test.ts
  - src/services/SprintService.test.ts
  - tests/db/custom-fields.test.ts
  - src/web/tests/vitest/BurndownChart.test.ts
  - src/web/tests/vitest/VelocityChart.test.ts
  - src/web/tests/vitest/GanttView.test.ts
  - src/web/tests/vitest/CalendarView.test.ts
autonomous: true
requirements: [TSK-01, TSK-02, TSK-03, TSK-04, TSK-05, TSK-06, TSK-07, TSK-08, TSK-09, TSK-10, TSK-11, TSK-12]

must_haves:
  truths:
    - "All 10 test files exist with RED failing stubs before Wave 1 begins"
    - "Each test file has at least one expect(true).toBe(false) that will go GREEN when implementation lands"
    - "Backend tests use bun:test, frontend tests use vitest"
  artifacts:
    - path: "src/services/CommentService.test.ts"
      provides: "RED stub for comment CRUD + watchers"
      contains: "expect"
    - path: "src/web/tests/vitest/BurndownChart.test.ts"
      provides: "RED stub for BurndownChart render"
      contains: "expect"
    - path: "src/web/tests/vitest/GanttView.test.ts"
      provides: "RED stub for GanttView render"
      contains: "expect"
  key_links: []
---

<objective>
Create RED failing test stubs for all 9 VALIDATION.md test files. These stubs fail immediately (expect(true).toBe(false)) and go GREEN only when real implementation lands in later waves.

Purpose: Nyquist compliance — RED tests exist before implementation, proving RED-to-GREEN transition.
Output: 9 test files with failing assertions.
</objective>

<execution_context>
@.planning/phases/05-task-management-metrics/05-VALIDATION.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RED backend test stubs (6 files)</name>
  <files>src/services/CommentService.test.ts, src/services/TaskService.test.ts, src/services/ReportService.test.ts, src/workers/metrics-rollup.test.ts, src/services/SprintService.test.ts, tests/db/custom-fields.test.ts</files>
  <action>
Create 6 backend test files using bun:test. Each file has descriptive test names matching the requirement they validate, with a single `expect(true).toBe(false)` body that will be replaced by real assertions when implementation lands.

**src/services/CommentService.test.ts** (TSK-01, TSK-02):
```typescript
import { describe, it, expect } from "bun:test";

describe("CommentService", () => {
  it("creates comment with TipTap JSON body", () => {
    expect(true).toBe(false); // RED — goes GREEN in Plan 03
  });
  it("resolves and unresolves comments", () => {
    expect(true).toBe(false);
  });
  it("extracts mentions from TipTap doc (user + team)", () => {
    expect(true).toBe(false);
  });
  it("auto-subscribes mentioned users as watchers", () => {
    expect(true).toBe(false);
  });
  it("subscribes assignee as watcher on assign", () => {
    expect(true).toBe(false);
  });
});
```

**src/services/TaskService.test.ts** (TSK-02, TSK-11):
```typescript
import { describe, it, expect } from "bun:test";

describe("TaskService - watchers", () => {
  it("auto-subscribes task creator as watcher", () => {
    expect(true).toBe(false); // RED — goes GREEN in Plan 06
  });
});

describe("TaskService - bulk operations", () => {
  it("bulk updates 50+ tasks in single transaction", () => {
    expect(true).toBe(false); // RED — goes GREEN in Plan 11
  });
  it("rejects bulk operations exceeding 200 tasks", () => {
    expect(true).toBe(false);
  });
  it("creates single event per field with affected_task_ids", () => {
    expect(true).toBe(false);
  });
});
```

**src/services/ReportService.test.ts** (TSK-05):
```typescript
import { describe, it, expect } from "bun:test";

describe("ReportService", () => {
  it("computes burndown data from sprint tasks", () => {
    expect(true).toBe(false); // RED — goes GREEN in Plan 05
  });
  it("computes velocity across sprints", () => {
    expect(true).toBe(false);
  });
  it("generates CFD data by status category", () => {
    expect(true).toBe(false);
  });
  it("calculates cycle time percentiles (p50, p75, p95)", () => {
    expect(true).toBe(false);
  });
});
```

**src/workers/metrics-rollup.test.ts** (TSK-06):
```typescript
import { describe, it, expect } from "bun:test";

describe("metrics-rollup worker", () => {
  it("rolls up daily snapshot from task state", () => {
    expect(true).toBe(false); // RED — goes GREEN in Plan 05
  });
  it("handles empty project gracefully", () => {
    expect(true).toBe(false);
  });
});
```

**src/services/SprintService.test.ts** (TSK-07, TSK-08):
```typescript
import { describe, it, expect } from "bun:test";

describe("SprintService - capacity", () => {
  it("computes capacity preview with assigned vs total", () => {
    expect(true).toBe(false); // RED — goes GREEN in Plan 06
  });
});

describe("SprintService - retrospective", () => {
  it("saves retrospective notes and summary", () => {
    expect(true).toBe(false); // RED — goes GREEN in Plan 06
  });
  it("closes sprint with rollover disposition", () => {
    expect(true).toBe(false);
  });
});
```

**tests/db/custom-fields.test.ts** (TSK-12):
```typescript
import { describe, it, expect } from "bun:test";

describe("Custom fields - all 9 types round-trip", () => {
  it("text field round-trips", () => { expect(true).toBe(false); });
  it("number field round-trips", () => { expect(true).toBe(false); });
  it("date field round-trips", () => { expect(true).toBe(false); });
  it("select field round-trips", () => { expect(true).toBe(false); });
  it("multi_select field round-trips", () => { expect(true).toBe(false); });
  it("checkbox field round-trips", () => { expect(true).toBe(false); });
  it("url field round-trips", () => { expect(true).toBe(false); });
  it("user field round-trips", () => { expect(true).toBe(false); });
  it("json field round-trips", () => { expect(true).toBe(false); });
});
```
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && ls src/services/CommentService.test.ts src/services/TaskService.test.ts src/services/ReportService.test.ts src/workers/metrics-rollup.test.ts src/services/SprintService.test.ts tests/db/custom-fields.test.ts 2>&1 | grep -c "No such" | grep -q "^0$" && echo "ALL EXIST"</automated>
  </verify>
  <done>6 backend RED test stubs exist. All fail immediately. Will go GREEN as Plans 03-11 implement features.</done>
</task>

<task type="auto">
  <name>Task 2: Create RED frontend vitest stubs (4 files)</name>
  <files>src/web/tests/vitest/BurndownChart.test.ts, src/web/tests/vitest/VelocityChart.test.ts, src/web/tests/vitest/GanttView.test.ts, src/web/tests/vitest/CalendarView.test.ts</files>
  <read_first>
    - src/web/vitest.config.ts (test config — understand setup and test file globs)
    - src/web/tests/ (existing test directory structure)
  </read_first>
  <action>
Create 3 frontend vitest test files with render assertions that fail until components exist.

**src/web/tests/vitest/BurndownChart.test.ts** (TSK-03):
```typescript
import { describe, it, expect } from "vitest";

describe("BurndownChart", () => {
  it("renders with line chart showing actual vs ideal", () => {
    // RED — goes GREEN when Plan 09 creates BurndownChart.svelte
    expect(true).toBe(false);
  });
  it("renders tooltip on hover with exact values", () => {
    expect(true).toBe(false);
  });
  it("uses SSR guard to prevent server-side render", () => {
    expect(true).toBe(false);
  });
});
```

**src/web/tests/vitest/VelocityChart.test.ts** (TSK-04):
```typescript
import { describe, it, expect } from "vitest";

describe("VelocityChart", () => {
  it("renders bar chart with completed points per sprint", () => {
    // RED — goes GREEN when Plan 09 creates VelocityChart.svelte
    expect(true).toBe(false);
  });
  it("renders rolling average line overlay", () => {
    expect(true).toBe(false);
  });
  it("colors bars green when >= average, amber when below", () => {
    expect(true).toBe(false);
  });
});
```

**src/web/tests/vitest/GanttView.test.ts** (TSK-09):
```typescript
import { describe, it, expect } from "vitest";

describe("GanttView", () => {
  it("renders SVAR Gantt component with task bars", () => {
    // RED — goes GREEN when Plan 10 creates GanttView.svelte
    expect(true).toBe(false);
  });
  it("renders dependency arrows between tasks", () => {
    expect(true).toBe(false);
  });
  it("highlights critical path tasks in red", () => {
    expect(true).toBe(false);
  });
});
```

**src/web/tests/vitest/CalendarView.test.ts** (TSK-10):
```typescript
import { describe, it, expect } from "vitest";

describe("CalendarView", () => {
  it("renders @event-calendar/core with task due dates", () => {
    // RED — goes GREEN when Plan 10 Task 2 creates CalendarView.svelte
    expect(true).toBe(false);
  });
  it("renders task cards in calendar day cells", () => {
    expect(true).toBe(false);
  });
  it("supports month/week/day view switching", () => {
    expect(true).toBe(false);
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && ls src/web/tests/vitest/BurndownChart.test.ts src/web/tests/vitest/VelocityChart.test.ts src/web/tests/vitest/GanttView.test.ts src/web/tests/vitest/CalendarView.test.ts 2>&1 | grep -c "No such" | grep -q "^0$" && echo "ALL EXIST"</automated>
  </verify>
  <done>4 frontend RED vitest stubs exist. All fail. Will go GREEN when Plans 09-10 implement chart/calendar components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test stubs only — no runtime trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No threats — test-only plan |
</threat_model>

<verification>
- All 10 test files exist on disk
- Backend tests fail when run: `bun test src/services/CommentService.test.ts` exits non-zero
- Frontend tests fail when run: `cd src/web && bun run web:test -- BurndownChart` exits non-zero
- CalendarView test fails: `cd src/web && bun run web:test -- CalendarView` exits non-zero
</verification>

<success_criteria>
10 RED test stubs created matching VALIDATION.md Wave 0 Gaps list. Each file has descriptive test names and `expect(true).toBe(false)` bodies. Nyquist compliance satisfied: RED exists before GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/05-task-management-metrics/05-00-SUMMARY.md`
</output>
