---
phase: 05-task-management-metrics
plan: 13
type: execute
wave: 5
depends_on: [06, 09]
files_modified:
  - src/server/yjs-server.ts
  - src/server/yjs-server.test.ts
  - src/web/src/lib/components/tasks/CollaborativeEditor.svelte
  - src/web/src/lib/components/tasks/PresenceIndicators.svelte
  - src/web/src/routes/workspace/portfolio/+page.svelte
  - src/web/src/lib/components/reports/PortfolioTable.svelte
  - src/web/src/lib/components/reports/AgeChart.svelte
  - src/web/src/lib/components/reports/ScopeChart.svelte
  - src/web/src/lib/components/reports/WorkloadChart.svelte
  - src/web/src/lib/components/reports/ResourceAllocation.svelte
autonomous: true
requirements: [TSK-05, TSK-06]

must_haves:
  truths:
    - "Yjs WebSocket server runs alongside Hono with session auth"
    - "Yjs server supports both in-process and standalone modes via FULCRUM_YJS_STANDALONE env var"
    - "WebSocket URL configurable via environment variable (not hardcoded localhost)"
    - "Yjs snapshots persist to PostgreSQL via YjsSnapshot entity"
    - "TipTap collaboration cursors show other users"
    - "Presence indicators on task detail panel"
    - "Portfolio table renders all projects with progress"
    - "Remaining analytics charts (age, scope, workload, resource) render"
    - "Workspace-scope report aggregation works end-to-end (D-94, D-95)"
  artifacts:
    - path: "src/server/yjs-server.ts"
      provides: "y-websocket server with auth + persistence + dual-mode (in-process/standalone)"
      exports: ["createYjsServer", "startYjsServer"]
    - path: "src/web/src/lib/components/tasks/CollaborativeEditor.svelte"
      provides: "TipTap + Yjs collaborative editor"
      contains: "collaboration"
    - path: "src/web/src/routes/workspace/portfolio/+page.svelte"
      provides: "Portfolio dashboard route"
      contains: "PortfolioTable"
  key_links:
    - from: "yjs-server.ts"
      to: "src/db/entities/tasks/YjsSnapshot.ts"
      via: "persist doc state to DB"
      pattern: "YjsSnapshot"
    - from: "CollaborativeEditor.svelte"
      to: "yjs-server.ts"
      via: "WebSocket connection"
      pattern: "WebsocketProvider"
---

<objective>
Real-time collaboration (Yjs WebSocket server with auth + persistence, TipTap collab cursors, presence), portfolio dashboard, and remaining analytics charts (age, scope, workload, resource allocation).

Purpose: Collaboration enables multi-user editing. Portfolio provides workspace-level visibility.
Output: Yjs server (dual-mode), collaborative editor, portfolio page, 4 additional chart components.
</objective>

<execution_context>
@.planning/phases/05-task-management-metrics/05-CONTEXT.md
@.planning/phases/05-task-management-metrics/05-UI-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-task-management-metrics/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1a: Create Yjs WebSocket server with auth + persistence + dual-mode</name>
  <files>src/server/yjs-server.ts, src/server/yjs-server.test.ts</files>
  <read_first>
    - src/server/hono.ts (Hono server setup -- understand how to run alongside)
    - src/db/entities/tasks/YjsSnapshot.ts (entity from Plan 02)
    - .planning/phases/05-task-management-metrics/05-CONTEXT.md (D-97..D-99)
  </read_first>
  <behavior>
    - Yjs server authenticates WebSocket upgrade requests against session
    - Yjs server persists document snapshots to PostgreSQL via YjsSnapshot entity
    - WebSocket URL comes from environment variable FULCRUM_YJS_URL (not hardcoded localhost)
    - Yjs server supports both in-process and standalone modes via FULCRUM_YJS_STANDALONE env var
  </behavior>
  <action>
**src/server/yjs-server.ts** (D-97, D-98, MEDIUM-08 fix):
- Import `WebSocketServer` from 'ws' (comes with y-websocket)
- Import y-websocket server utilities
- `createYjsServer(options: { port?: number, em: EntityManager })`:
  - Create WebSocket server on configurable port (default from `process.env.FULCRUM_YJS_PORT || 1234`)
  - On WebSocket upgrade: validate session cookie/token from request headers. Reject with 401 if invalid (auth check per D-98).
  - On document update: debounced persist (every 5s of inactivity) -- encode doc state as `Y.encodeStateAsUpdate(doc)`, upsert into YjsSnapshot entity by doc_name.
  - On document load: read YjsSnapshot by doc_name, apply state if exists.
- `startYjsServer(em)`: creates and starts the server. Export for Hono startup integration.
- Export URL helper: `getYjsUrl()` returns `process.env.FULCRUM_YJS_URL || 'ws://localhost:1234'`

**Dual-mode support (GO condition #2 from peer review):**
```typescript
const standalone = process.env.FULCRUM_YJS_STANDALONE === 'true';

if (standalone) {
  // Production/SaaS: runs as its own process on FULCRUM_YJS_PORT
  const port = Number(process.env.FULCRUM_YJS_PORT) || 4444;
  const server = createYjsServer({ port, em });
  server.listen(port, () => console.log(`Yjs standalone on :${port}`));
} else {
  // Dev/local: export handler for Hono to mount in-process
  export const yjsUpgradeHandler = createYjsServer({ em });
}
```

**MEDIUM-08 fix**: Client connects to `getYjsUrl()` (environment-configurable), NOT hardcoded `ws://localhost:1234`.

**src/server/yjs-server.test.ts:**
- Test auth rejection: WebSocket upgrade without valid session -> 401
- Test persistence: update doc -> wait debounce -> verify YjsSnapshot row exists
- Test load: pre-seed YjsSnapshot row -> connect -> verify doc has state
- Test standalone mode: FULCRUM_YJS_STANDALONE=true starts own HTTP server
  </action>
  <deployment_note>
**Yjs Deployment Strategy (GO condition #2 from peer review):**

| Environment | Mode | How |
|-------------|------|-----|
| Dev/local | In-process | Yjs WebSocket server starts alongside Hono. Single process, zero config. |
| Production/SaaS | Standalone | Set `FULCRUM_YJS_STANDALONE=true`. Yjs server runs as its own process on `FULCRUM_YJS_PORT` (default 4444). Load balanced independently. |

**Scaling strategy for standalone mode:**
- Horizontal scaling via sticky sessions (WebSocket connections route to same instance per document)
- Each Yjs instance handles documents in memory; persistence layer (PostgreSQL) is shared
- Document state syncs through DB on reconnect (no cross-instance pub/sub needed for MVP)
- Future: Redis pub/sub for cross-instance awareness if needed beyond ~10 concurrent editors per doc

**The code supports both modes via:**
```
if (standalone) { listen(port) } else { export handler for Hono mount }
```
Same auth, persistence, and document handling logic in both paths.
  </deployment_note>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/server/yjs-server.test.ts && rg "FULCRUM_YJS_URL\|FULCRUM_YJS_STANDALONE\|process.env" src/server/yjs-server.ts</automated>
  </verify>
  <acceptance_criteria>
    - yjs-server.ts exports createYjsServer and startYjsServer
    - Uses environment variable for URL (MEDIUM-08 fix -- no hardcoded localhost)
    - Auth validates session on WebSocket upgrade (D-98)
    - Persists to YjsSnapshot entity (D-98, using HIGH-05 entity from Wave 1)
    - Yjs server supports both in-process and standalone modes via FULCRUM_YJS_STANDALONE env var
    - Standalone mode listens on FULCRUM_YJS_PORT (default 4444)
    - Tests verify auth rejection, persistence, and standalone boot
    - LOW-04 addressed: startYjsServer exported and callable from Hono startup
  </acceptance_criteria>
  <done>Yjs server with auth, persistence, env-configurable URL, dual-mode (in-process + standalone). MEDIUM-08/LOW-04 resolved. GO condition #2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 1b: Create CollaborativeEditor + PresenceIndicators UI</name>
  <files>src/web/src/lib/components/tasks/CollaborativeEditor.svelte, src/web/src/lib/components/tasks/PresenceIndicators.svelte</files>
  <read_first>
    - src/web/src/lib/tiptap/ (existing TipTap setup if any)
    - src/web/package.json (verify collaboration extensions installed)
    - .planning/phases/05-task-management-metrics/05-CONTEXT.md (D-97, D-99)
  </read_first>
  <action>
**CollaborativeEditor.svelte** (D-97):
- TipTap editor with extensions:
  - `@tiptap/extension-collaboration` configured with Yjs doc
  - `@tiptap/extension-collaboration-cursor` for cursor presence
- WebSocket provider: `new WebsocketProvider(getYjsUrl(), docName, ydoc)`
  - docName = `task-${taskId}` for task descriptions
- Show cursor labels with user name + color
- Fallback: if WebSocket fails to connect, editor works in non-collab mode (graceful degradation)

**PresenceIndicators.svelte** (D-99):
- Props: `taskId: string`
- Uses awareness from Yjs provider to show who's viewing
- Render avatar dots for active users (heartbeat-based presence)
- Max 5 visible + overflow count
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg "collaboration\|WebsocketProvider" src/web/src/lib/components/tasks/CollaborativeEditor.svelte && ls src/web/src/lib/components/tasks/PresenceIndicators.svelte</automated>
  </verify>
  <acceptance_criteria>
    - CollaborativeEditor.svelte uses collaboration + collaboration-cursor extensions (D-97)
    - Connects via getYjsUrl() (not hardcoded)
    - Graceful fallback if WebSocket unavailable
    - PresenceIndicators.svelte shows user avatars (D-99)
  </acceptance_criteria>
  <done>Collaborative editor + presence indicators. D-97/D-99 UI complete.</done>
</task>

<task type="auto">
  <name>Task 2: Portfolio dashboard + remaining analytics charts</name>
  <files>src/web/src/routes/workspace/portfolio/+page.svelte, src/web/src/lib/components/reports/PortfolioTable.svelte, src/web/src/lib/components/reports/AgeChart.svelte, src/web/src/lib/components/reports/ScopeChart.svelte, src/web/src/lib/components/reports/WorkloadChart.svelte, src/web/src/lib/components/reports/ResourceAllocation.svelte</files>
  <read_first>
    - src/web/src/routes/workspace/ (existing workspace routes)
    - .planning/phases/05-task-management-metrics/05-CONTEXT.md (D-44..D-49, D-93..D-96)
    - .planning/phases/05-task-management-metrics/05-UI-SPEC.md (portfolio section)
  </read_first>
  <action>
**PortfolioTable.svelte** (D-93):
- Table showing all projects: name, progress (% done donut), active sprint, overdue count, health status
- Health = green/amber/red based on velocity trend (green = on track, amber = slowing, red = behind)
- Progress computed from metrics_snapshots workspace aggregation (D-94)
- Fetch via `trpc.reports.progressRollup.query({ scopeType: 'workspace' })` (D-95)

**AgeChart.svelte** (D-44):
- Horizontal bar chart: current items grouped by status, bar length = days in current status
- Items > 14 days highlighted red
- Props: `data: {taskTitle: string, status: string, daysInStatus: number}[]`

**ScopeChart.svelte** (D-46):
- Line chart: original scope vs current scope vs completed over time
- Shows scope creep visually
- Props: `data: {date: string, original: number, current: number, completed: number}[]`

**WorkloadChart.svelte** (D-48):
- Stacked bar per assignee: task count by status category
- Shows who's overloaded vs underutilized
- Props: `data: {assignee: string, backlog: number, started: number, completed: number, ...}[]`

**ResourceAllocation.svelte** (D-96):
- Stacked bar per team member showing task distribution across projects
- >100% line indicates over-allocation
- Props: `data: {member: string, projects: {name: string, points: number}[]}[]`

**+page.svelte** (portfolio route):
- Renders PortfolioTable at top
- Below: workspace-scope charts (workload, resource allocation)
- Navigation link from workspace nav
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && ls src/web/src/routes/workspace/portfolio/+page.svelte src/web/src/lib/components/reports/PortfolioTable.svelte src/web/src/lib/components/reports/AgeChart.svelte src/web/src/lib/components/reports/WorkloadChart.svelte src/web/src/lib/components/reports/ResourceAllocation.svelte</automated>
  </verify>
  <acceptance_criteria>
    - Portfolio route exists at workspace/portfolio/
    - PortfolioTable shows all projects with progress/health (D-93)
    - PortfolioTable uses workspace scope from reportsRouter (D-94, D-95)
    - AgeChart renders horizontal bars with stale highlight (D-44)
    - ScopeChart shows scope creep lines (D-46)
    - WorkloadChart shows per-assignee stacked bars (D-48)
    - ResourceAllocation shows cross-project distribution (D-96)
    - All charts use LayerChart with SSR guards
  </acceptance_criteria>
  <done>Portfolio dashboard with project table + workspace analytics. D-93..D-96 complete. Remaining charts delivered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket upgrade | Untrusted connection attempts to Yjs server |
| portfolio data | Cross-project visibility requires workspace membership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-28 | Spoofing | Yjs WebSocket auth | mitigate | Session validation on upgrade, reject 401 |
| T-05-29 | Info Disclosure | cross-project portfolio | mitigate | Workspace scope queries filtered by org membership |
| T-05-30 | Tampering | collaborative doc | accept | Yjs CRDT handles concurrent edits correctly by design |
</threat_model>

<verification>
- Yjs server tests pass
- Yjs standalone mode boots correctly with FULCRUM_YJS_STANDALONE=true
- Portfolio route renders
- `bun run build` passes
</verification>

<success_criteria>
Real-time collaboration: Yjs server with auth (D-97..D-99), env-configurable URL (MEDIUM-08), persistence to DB (D-98), startup export (LOW-04), dual-mode in-process/standalone (GO condition #2). Portfolio: table with progress (D-93), workspace aggregation (D-94/D-95), resource allocation (D-96). Remaining charts: age (D-44), scope (D-46), workload (D-48).
</success_criteria>

<output>
After completion, create `.planning/phases/05-task-management-metrics/05-13-SUMMARY.md`
</output>
