---
phase: 05-task-management-metrics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/entities/tasks/TaskComment.ts
  - src/db/entities/tasks/TaskWatcher.ts
  - src/db/entities/tasks/CommentReaction.ts
  - src/db/entities/tasks/TaskRelationship.ts
  - src/db/entities/tasks/ProjectAutomation.ts
  - src/db/entities/tasks/FieldDependencyRule.ts
  - src/db/entities/tasks/index.ts
autonomous: true
requirements:
  - TSK-01
  - TSK-02

must_haves:
  truths:
    - "All 6 new entities compile and export correctly"
    - "Each entity uses Stage-3 decorators with explicit type on every @Property"
    - "Each entity has org FK with cascade delete for tenant isolation"
    - "Barrel export includes all new entities"
  artifacts:
    - path: "src/db/entities/tasks/TaskComment.ts"
      provides: "Comment entity with threading + resolve"
      exports: ["TaskComment"]
    - path: "src/db/entities/tasks/TaskWatcher.ts"
      provides: "Watcher join entity with source enum"
      exports: ["TaskWatcher"]
    - path: "src/db/entities/tasks/TaskRelationship.ts"
      provides: "Task dependency relationships"
      exports: ["TaskRelationship"]
    - path: "src/db/entities/tasks/index.ts"
      provides: "Barrel export for all task entities"
      contains: "TaskComment"
  key_links:
    - from: "src/db/entities/tasks/index.ts"
      to: "src/db/entities/tasks/TaskComment.ts"
      via: "export"
      pattern: "export.*TaskComment"
---

<objective>
Create all 6 new MikroORM entities for Phase 5: TaskComment, TaskWatcher, CommentReaction, TaskRelationship, ProjectAutomation, FieldDependencyRule. Update barrel export.

Purpose: These entities back comments (D-01..D-04), watchers (D-07..D-09), task dependencies (D-19..D-21), automation rules (D-89..D-92), and field dependency rules (D-109..D-111). Services in Wave 2 depend on these entities existing.

Output: 6 new entity files + updated barrel export.
</objective>

<execution_context>
@.planning/phases/05-task-management-metrics/05-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From src/db/entities/tasks/Sprint.ts (entity analog):
```typescript
import { Entity, Index, ManyToOne, PrimaryKey, Property } from "@mikro-orm/decorators/es";
import { OptionalProps } from "@mikro-orm/core";
import { Org } from "../auth/Org.ts";
```
- Stage-3 decorators: explicit `type` on every @Property (no reflect-metadata)
- @ManyToOne(() => Org, { fieldName: "org_id", nullable: false, deleteRule: "cascade" })
- @PrimaryKey({ type: "uuid", defaultRaw: "gen_random_uuid()" })
- jsonb columns: type: "json", defaultRaw: "'{}'::jsonb", returning: false
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TaskComment, TaskWatcher, CommentReaction entities</name>
  <files>src/db/entities/tasks/TaskComment.ts, src/db/entities/tasks/TaskWatcher.ts, src/db/entities/tasks/CommentReaction.ts</files>
  <read_first>
    - src/db/entities/tasks/Sprint.ts (analog pattern — imports, decorators, [OptionalProps])
    - src/db/entities/tasks/MetricsCache.ts (analog for join-table entities)
    - .planning/phases/05-task-management-metrics/05-PATTERNS.md (lines 48-192 — exact entity code)
  </read_first>
  <action>
Create three entity files using the exact patterns from 05-PATTERNS.md:

**TaskComment.ts** (per D-01, D-02, D-03):
- Table: `task_comments`
- Indexes: `task_comments_task_id` on taskId, `task_comments_org_task` on (org, taskId)
- Properties: id (uuid PK), org (ManyToOne Org cascade), taskId (uuid), authorId (uuid), body (json/jsonb — TipTap JSON), parentCommentId (uuid nullable — threading per D-01), resolved (boolean default false), resolvedBy (uuid nullable), resolvedAt (datetime nullable), createdAt, updatedAt
- [OptionalProps]: "createdAt" | "updatedAt" | "resolved" | "resolvedAt" | "resolvedBy" | "parentCommentId"

**TaskWatcher.ts** (per D-07):
- Table: `task_watchers`
- Unique constraint: `task_watchers_task_user_unique` on (taskId, userId)
- Index: `task_watchers_task_id` on taskId
- Properties: id (uuid PK), org (ManyToOne Org cascade), taskId (uuid), userId (uuid), source (string with check: 'manual','mention','assign','create' per D-07), createdAt
- [OptionalProps]: "createdAt"
- Import `Unique` from `@mikro-orm/decorators/es` (needed for unique constraint decorator)

**CommentReaction.ts** (per D-04):
- Table: `comment_reactions`
- Unique constraint: `comment_reactions_unique` on (commentId, userId, emoji)
- Index: `comment_reactions_comment_id` on commentId
- Properties: id (uuid PK), org (ManyToOne Org cascade), commentId (uuid), userId (uuid), emoji (string — one of 6 standard reactions), createdAt
- [OptionalProps]: "createdAt"
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && grep -c "@Entity" src/db/entities/tasks/TaskComment.ts && grep -c "@Entity" src/db/entities/tasks/TaskWatcher.ts && grep -c "@Entity" src/db/entities/tasks/CommentReaction.ts && grep "task_comments" src/db/entities/tasks/TaskComment.ts && grep "task_watchers" src/db/entities/tasks/TaskWatcher.ts && grep "comment_reactions" src/db/entities/tasks/CommentReaction.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "parentCommentId" src/db/entities/tasks/TaskComment.ts` returns >= 1
    - `grep -c "resolved" src/db/entities/tasks/TaskComment.ts` returns >= 1
    - `grep "deleteRule.*cascade" src/db/entities/tasks/TaskComment.ts` matches
    - `grep "source.*manual.*mention.*assign.*create" src/db/entities/tasks/TaskWatcher.ts` matches
    - `grep "Unique" src/db/entities/tasks/TaskWatcher.ts` matches
    - `grep "emoji" src/db/entities/tasks/CommentReaction.ts` matches
    - All three files import from `@mikro-orm/decorators/es`
  </acceptance_criteria>
  <done>TaskComment supports flat + threaded comments with resolve. TaskWatcher tracks subscription source. CommentReaction supports emoji reactions.</done>
</task>

<task type="auto">
  <name>Task 2: Create TaskRelationship, ProjectAutomation, FieldDependencyRule entities + update barrel</name>
  <files>src/db/entities/tasks/TaskRelationship.ts, src/db/entities/tasks/ProjectAutomation.ts, src/db/entities/tasks/FieldDependencyRule.ts, src/db/entities/tasks/index.ts</files>
  <read_first>
    - src/db/entities/tasks/Task.ts (analog for TaskRelationship)
    - src/db/entities/tasks/index.ts (current barrel exports)
    - .planning/phases/05-task-management-metrics/05-PATTERNS.md (lines 196-359 — exact entity code)
  </read_first>
  <action>
Create three entity files using exact patterns from 05-PATTERNS.md:

**TaskRelationship.ts** (per D-19):
- Table: `task_relationships`
- Unique: `task_relationships_unique` on (fromTaskId, toTaskId, type)
- Indexes: `task_relationships_from_task` on fromTaskId, `task_relationships_to_task` on toTaskId
- Properties: id (uuid PK), org (ManyToOne Org cascade), fromTaskId (uuid), toTaskId (uuid), type (string with check: 'blocks','blocked_by','relates_to','duplicate_of'), createdBy (uuid nullable), createdAt
- [OptionalProps]: "createdAt"

**ProjectAutomation.ts** (per D-89, D-90):
- Table: `project_automations`
- Index: `project_automations_project_id` on projectId
- Properties: id (uuid PK), org (ManyToOne Org cascade), projectId (uuid), name (string), triggerType (string), triggerConfig (json/jsonb), condition (json nullable), actionType (string), actionConfig (json/jsonb), enabled (boolean default true), executionCount (integer default 0), createdAt, updatedAt
- [OptionalProps]: "createdAt" | "updatedAt" | "enabled" | "executionCount" | "condition"

**FieldDependencyRule.ts** (per D-109):
- Table: `field_dependency_rules`
- Index: `field_dependency_rules_project_id` on projectId
- Properties: id (uuid PK), org (ManyToOne Org cascade), projectId (uuid), sourceFieldId (uuid), sourceValue (string), targetFieldId (uuid), action (string with check: 'show','hide','require'), createdAt
- [OptionalProps]: "createdAt"

**index.ts** — Add export lines for all 6 new entities:
```typescript
export { TaskComment } from "./TaskComment.ts";
export { TaskWatcher } from "./TaskWatcher.ts";
export { CommentReaction } from "./CommentReaction.ts";
export { TaskRelationship } from "./TaskRelationship.ts";
export { ProjectAutomation } from "./ProjectAutomation.ts";
export { FieldDependencyRule } from "./FieldDependencyRule.ts";
```
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && grep -c "@Entity" src/db/entities/tasks/TaskRelationship.ts && grep -c "@Entity" src/db/entities/tasks/ProjectAutomation.ts && grep -c "@Entity" src/db/entities/tasks/FieldDependencyRule.ts && grep -c "TaskComment\|TaskWatcher\|CommentReaction\|TaskRelationship\|ProjectAutomation\|FieldDependencyRule" src/db/entities/tasks/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "blocks.*blocked_by.*relates_to.*duplicate_of" src/db/entities/tasks/TaskRelationship.ts` matches
    - `grep -c "triggerType" src/db/entities/tasks/ProjectAutomation.ts` returns >= 1
    - `grep -c "executionCount" src/db/entities/tasks/ProjectAutomation.ts` returns >= 1
    - `grep "show.*hide.*require" src/db/entities/tasks/FieldDependencyRule.ts` matches
    - `grep -c "sourceFieldId" src/db/entities/tasks/FieldDependencyRule.ts` returns >= 1
    - `grep -c "TaskComment" src/db/entities/tasks/index.ts` returns >= 1
    - `grep -c "TaskRelationship" src/db/entities/tasks/index.ts` returns >= 1
    - `grep -c "ProjectAutomation" src/db/entities/tasks/index.ts` returns >= 1
    - `grep -c "FieldDependencyRule" src/db/entities/tasks/index.ts` returns >= 1
    - All entity files import from `@mikro-orm/decorators/es` (not `@mikro-orm/core` for decorators)
  </acceptance_criteria>
  <done>All 6 new entities created and exported via barrel. TaskRelationship supports 4 relationship types. ProjectAutomation stores rule engine config. FieldDependencyRule stores conditional field visibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Entity → DB | ORM entities define data shape for all CRUD |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Elevation of Privilege | TaskComment entity | mitigate | org FK with cascade delete — all queries must filter by org_id (enforced in service layer, Plan 03) |
| T-05-04 | Elevation of Privilege | TaskWatcher entity | mitigate | Same org_id FK cascade pattern; unique constraint prevents duplicate subscriptions |
| T-05-05 | Tampering | ProjectAutomation entity | mitigate | triggerConfig/actionConfig are jsonb — validated at service layer (Plan 06) before execution |
</threat_model>

<verification>
- All 6 new entity files exist and use `@mikro-orm/decorators/es` imports
- Barrel export includes all 6 new entities
- TypeScript compilation succeeds for entity directory
</verification>

<success_criteria>
6 new MikroORM entities created with correct Stage-3 decorator patterns, org-scoped FK cascades, and barrel-exported. Ready for service layer consumption in Wave 2.
</success_criteria>

<output>
After completion, create `.planning/phases/05-task-management-metrics/05-02-SUMMARY.md`
</output>
