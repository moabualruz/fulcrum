---
phase: 05-task-management-metrics
plan: 05
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/services/ReportService.ts
  - src/services/ReportService.test.ts
  - src/workers/metrics-rollup.ts
  - src/workers/metrics-rollup.test.ts
  - src/server/trpc/routers/reports.ts
autonomous: true
requirements: [TSK-03, TSK-04, TSK-05, TSK-06]

must_haves:
  truths:
    - "ReportService computes burndown, velocity, CFD, cycle time, throughput, WIP, lead time from snapshots/events"
    - "Metrics rollup worker updates MetricsCache snapshots on event"
    - "Reports support project AND workspace scope (D-53, HIGH-01)"
    - "CSV export available for all report types (D-54)"
    - "Daily snapshot rows include workspace scope_type"
  artifacts:
    - path: "src/services/ReportService.ts"
      provides: "Analytics queries against two-layer model"
      exports: ["ReportService"]
    - path: "src/workers/metrics-rollup.ts"
      provides: "Worker job for snapshot generation"
      exports: ["metricsRollupJob"]
    - path: "src/server/trpc/routers/reports.ts"
      provides: "Reports tRPC router (replaces legacy raw SQL)"
      exports: ["reportsRouter"]
  key_links:
    - from: "src/workers/metrics-rollup.ts"
      to: "src/db/entities/tasks/MetricsCache.ts"
      via: "upsert snapshot rows"
      pattern: "MetricsCache"
    - from: "src/services/ReportService.ts"
      to: "MetricsCache entity"
      via: "query snapshots by scope_type"
      pattern: "scopeType.*workspace"
---

<objective>
Create ReportService (two-layer analytics queries), metrics rollup worker, and reportsRouter. Replaces legacy raw SQL reports page with tRPC-backed service.

Purpose: Powers all charts (burndown, velocity, CFD, cycle time, etc.) and provides workspace-scope reports (D-53, D-95).
Output: ReportService, metrics-rollup worker, reportsRouter, tests.
</objective>

<execution_context>
@.planning/phases/05-task-management-metrics/05-RESEARCH.md
@.planning/phases/05-task-management-metrics/05-RESEARCH-REPORTS.md
@.planning/phases/05-task-management-metrics/05-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-task-management-metrics/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ReportService with all analytics queries + CSV export</name>
  <files>src/services/ReportService.ts, src/services/ReportService.test.ts</files>
  <read_first>
    - src/services/SprintService.ts (service pattern)
    - src/db/entities/tasks/MetricsCache.ts (extended entity with scope_type, points_total, etc.)
    - src/db/entities/core/Event.ts (extended with field_name, from_value, to_value)
    - src/web/src/routes/projects/[id]/reports/+page.server.ts (legacy reports to replace — DO NOT copy raw SQL pattern)
    - .planning/phases/05-task-management-metrics/05-RESEARCH-REPORTS.md (report type definitions)
    - .planning/phases/05-task-management-metrics/05-CONTEXT.md (D-35..D-55)
  </read_first>
  <behavior>
    - getBurndown(orgId, scopeType, scopeId, dateRange) returns daily {date, remaining, ideal} from MetricsCache
    - getBurnup(orgId, scopeType, scopeId, dateRange) returns daily {date, completed, total}
    - getVelocity(orgId, scopeType, scopeId, lastN) returns per-sprint {sprintName, completed, average}
    - getCfd(orgId, scopeType, scopeId, dateRange) returns daily status_counts from MetricsCache
    - getCycleTime(orgId, scopeType, scopeId, dateRange) returns per-task {taskId, completedAt, cycleTimeHours} from Events
    - getLeadTime(orgId, scopeType, scopeId, dateRange) same but createdAt→completedAt
    - getThroughput(orgId, scopeType, scopeId, dateRange) returns weekly completion counts
    - getWipOverTime(orgId, scopeType, scopeId, dateRange) returns daily WIP from MetricsCache.wipCount
    - getWorkload(orgId, scopeType, scopeId) returns per-assignee task distribution
    - getBlockedItems(orgId, scopeType, scopeId) returns blocked tasks with days-blocked
    - getStaleIssues(orgId, scopeType, scopeId, thresholdDays=14) returns no-activity tasks
    - getProgressRollup(orgId, scopeType, scopeId) returns % complete by points + count
    - exportCsv(reportType, data) serializes any report data to CSV string (D-54)
    - All queries accept scopeType='workspace' which aggregates across all projects in org (D-53, D-95, HIGH-01)
  </behavior>
  <action>
Create `src/services/ReportService.ts`:
- Constructor(em: EntityManager)
- Layer 2 queries (MetricsCache): burndown, burnup, velocity, CFD, WIP, progress rollup use `em.find(MetricsCache, { org, scopeType, scopeId, date: { $gte, $lte } })`. For workspace scope: omit scopeId filter, just filter by `scopeType: 'workspace'` OR aggregate all project-scoped rows.
- Layer 1 queries (Event): cycle time, lead time, throughput query Event entities with `verb: 'task.status_changed'` and compute deltas. Cycle time = diff between first `started` event and `completed` event per task. Lead time = diff between task.createdAt and completed event.
- Workspace scope (D-53, D-95): when scopeType='workspace', query MetricsCache rows with scope_type='workspace' if they exist, OR aggregate across all project rows in org. This handles HIGH-01 — workspace is now a valid scope_type.
- CSV export (D-54): `exportCsv` takes report type identifier and data array, maps to column headers, joins as CSV string. Return as string (router streams it with content-type text/csv).
- Date range (D-55): all queries accept `dateRange: { start: Date, end: Date }` parameter.

Create tests verifying:
- Burndown returns decreasing ideal line + actual from snapshots
- Workspace scope aggregates across projects
- CSV export produces valid comma-separated output with headers
- Cycle time computes correctly from event timestamps
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/services/ReportService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - ReportService exports with all 13+ query methods
    - Each method accepts scopeType parameter including 'workspace' (HIGH-01/D-53/D-95)
    - exportCsv method returns valid CSV string (D-54)
    - All queries use MikroORM (no raw SQL, no openProductDb)
    - Tests pass
  </acceptance_criteria>
  <done>ReportService with full analytics suite, workspace scope, CSV export. Tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create metrics rollup worker + reportsRouter</name>
  <files>src/workers/metrics-rollup.ts, src/workers/metrics-rollup.test.ts, src/server/trpc/routers/reports.ts</files>
  <read_first>
    - src/workers/registry.ts (WorkerRegistry pattern — createWorkerRegistry, registerTask)
    - src/subscriptions/event-bus.ts (EventBus subscribe pattern)
    - src/server/trpc/routers/tasks.ts (router pattern)
    - src/trpc/routers/reports.ts (existing reports router — may be stub, check content)
  </read_first>
  <behavior>
    - metricsRollupJob receives {scope_type, scope_id, org_id}, computes current snapshot, upserts MetricsCache row for today
    - Worker triggered by EventBus on task mutations (status_change, points_change, sprint_change)
    - Also supports nightly catchup mode (fills gaps)
    - reportsRouter exposes all ReportService methods as tRPC queries + CSV mutation
  </behavior>
  <action>
**src/workers/metrics-rollup.ts:**
- Import WorkerRegistry pattern from `src/workers/registry.ts`
- Register `metrics_rollup` task with payload validation (scope_type, scope_id, org_id required)
- Handler: instantiate ReportService-like logic to compute current metrics for scope:
  - Query all tasks in scope (by project_id or sprint_id)
  - Count by status category → status_counts
  - Sum points → points_total, points_completed, points_remaining
  - Count tasks → tasks_total, tasks_completed
  - Count WIP (status='started') → wip_count
  - Upsert MetricsCache row for today's date + scope
- Export `setupMetricsRollupListener(eventBus)` function that subscribes to task events and enqueues rollup jobs
- For workspace scope: also compute workspace-level aggregation row

**src/workers/metrics-rollup.test.ts:**
- Test that handler computes correct counts from mock tasks
- Test that upsert updates existing row for same date/scope

**src/server/trpc/routers/reports.ts** (overwrite existing stub if present):
```typescript
export const reportsRouter = t.router({
  burndown: permissionedProcedure({ resource: "reports", action: "list" })
    .input(z.object({ scopeType: z.enum(["sprint","project","epic","workspace"]), scopeId: z.string().uuid().optional(), dateRange: DateRangeSchema }))
    .query(/* ReportService.getBurndown */),
  burnup: /* same pattern */,
  velocity: /* ... */,
  cfd: /* ... */,
  cycleTime: /* ... */,
  leadTime: /* ... */,
  throughput: /* ... */,
  wipOverTime: /* ... */,
  workload: /* ... */,
  blockedItems: /* ... */,
  staleIssues: /* ... */,
  progressRollup: /* ... */,
  exportCsv: permissionedProcedure({ resource: "reports", action: "list" })
    .input(z.object({ reportType: z.string(), scopeType: z.enum([...]), scopeId: z.string().uuid().optional(), dateRange: DateRangeSchema }))
    .mutation(/* ReportService.exportCsv — returns CSV string */),
});
```

DateRangeSchema = `z.object({ start: z.string().datetime(), end: z.string().datetime() })`

Do NOT modify src/trpc/router.ts — Plan 06 owns it (HIGH-06).
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/workers/metrics-rollup.test.ts && rg "export.*reportsRouter" src/server/trpc/routers/reports.ts</automated>
  </verify>
  <acceptance_criteria>
    - metrics-rollup.ts exports metricsRollupJob and setupMetricsRollupListener
    - Worker computes and upserts MetricsCache rows
    - Worker handles workspace scope aggregation
    - reportsRouter exports with all report type procedures
    - reportsRouter scopeType enum includes "workspace" (HIGH-01)
    - reportsRouter has exportCsv procedure (D-54)
    - Tests pass
  </acceptance_criteria>
  <done>Metrics rollup worker + reportsRouter complete. Two-layer model operational.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → reportsRouter | scopeId must be validated against org membership |
| EventBus → worker | Internal trust boundary — events are system-generated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Info Disclosure | reports cross-org | mitigate | All queries scoped by orgId from context |
| T-05-12 | Denial of Service | metrics worker flood | mitigate | Debounce: one rollup per scope per 5s via dedup in WorkerRegistry |
</threat_model>

<verification>
- All tests pass
- reportsRouter compiles
- `bun run build` passes
</verification>

<success_criteria>
Two-layer analytics model fully operational: events feed worker, worker updates snapshots, ReportService queries both layers. Workspace scope supported end-to-end (HIGH-01). CSV export (D-54). Daily snapshot worker with EventBus trigger (D-33). Tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-task-management-metrics/05-05-SUMMARY.md`
</output>
