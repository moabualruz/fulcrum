---
phase: 01-architecture-convergence-security
plan: 05
type: execute
wave: 2
depends_on: [01-04]
files_modified:
  - src/product-kernel/store/repositories.ts
  - src/db/repositories/tasks/TaskRepository.ts
  - src/db/repositories/core/SprintRepository.ts
  - src/db/repositories/core/EventRepository.ts
  - src/db/repositories/core/OrgRepository.ts
autonomous: true
requirements: [ARCH-02]

must_haves:
  truths:
    - "product-kernel/store/repositories.ts has zero raw SQL for task/sprint/org/event CRUD"
    - "All functions previously in repositories.ts are available via MikroORM repositories"
    - "appendEvent function replaced with EventRepository.append()"
  artifacts:
    - path: "src/db/repositories/tasks/TaskRepository.ts"
      provides: "MikroORM task CRUD replacing raw SQL"
    - path: "src/db/repositories/core/EventRepository.ts"
      provides: "MikroORM event persistence replacing appendEvent()"
  key_links:
    - from: "src/product-kernel/api/router.ts"
      to: "src/db/repositories/"
      via: "imports from MikroORM repos instead of raw SQL functions"
      pattern: "import.*Repository"
---

<objective>
Migrate product-kernel/store/repositories.ts (713 lines, 36 raw SQL calls) to MikroORM repositories.

Purpose: This file is the largest concentration of raw SQL in the codebase. It contains task CRUD, sprint CRUD, org setup, event persistence, and API key lookup — all bypassing the ORM. Migrating this eliminates the primary source of dual data layer inconsistency.

Output: repositories.ts reduced to re-exports or deleted. All operations through MikroORM.
</objective>

<execution_context>
@.planning/phases/01-architecture-convergence-security/01-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
From src/product-kernel/store/repositories.ts (713 lines, key exports):
```typescript
export async function createTask(db: ProductDb, orgId: string, input: {...}): Promise<TaskRow>
export async function listTasks(db: ProductDb, orgId: string): Promise<TaskRow[]>
export async function createSprint(db: ProductDb, orgId: string, input: {...}): Promise<SprintRow>
export async function updateSprint(db: ProductDb, orgId: string, id: string, input: {...}): Promise<SprintRow>
export async function listSprints(db: ProductDb, orgId: string): Promise<SprintRow[]>
export async function appendEvent(db: ProductDb, evt: {...}): Promise<{id: string}>
export async function createLocalOrg(db: ProductDb, slug: string, name: string): Promise<string>
export async function findApiKeyByHash(db: ProductDb, hash: string): Promise<{...} | null>
```

Existing MikroORM entities (from src/db/entities/):
- tasks/ — Task entity exists
- core/ — Event entity exists (UUID PKs)
- repos/ — Sprint likely exists

Existing MikroORM repositories (from src/db/repositories/):
- tasks/ — likely has TaskRepository
- core/ — likely has base repos

Callers of repositories.ts:
- src/product-kernel/api/router.ts — imports createTask, listTasks, createSprint, etc.
- scripts/seed-web-shell.ts — imports appendEvent, createLocalOrg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create/extend MikroORM repositories for task, sprint, org, event, API key</name>
  <files>src/db/repositories/tasks/TaskRepository.ts, src/db/repositories/core/SprintRepository.ts, src/db/repositories/core/EventRepository.ts, src/db/repositories/core/OrgRepository.ts, src/db/repositories/auth/ApiKeyRepository.ts</files>
  <read_first>src/product-kernel/store/repositories.ts, src/db/entities/tasks/, src/db/entities/core/Event.ts, src/db/repositories/tasks/, src/db/repositories/core/</read_first>
  <action>
For each raw SQL function in repositories.ts, create or extend a MikroORM repository method:

1. **TaskRepository** (extend if exists, create if not):
   - `create(orgId, input)` — replaces `createTask()` raw SQL INSERT
   - `listByOrg(orgId)` — replaces `listTasks()` raw SQL SELECT
   - Match the return types exactly (TaskRow shape)

2. **SprintRepository**:
   - `create(orgId, input)` — replaces `createSprint()`
   - `update(orgId, id, input)` — replaces `updateSprint()`
   - `listByOrg(orgId)` — replaces `listSprints()`

3. **EventRepository**:
   - `append(evt)` — replaces `appendEvent()` raw SQL INSERT
   - Use UUID PKs (matching Event entity), NOT ULIDs
   - This is critical: appendEvent() uses newUlid() but Event entity uses UUID
   - The repository MUST use UUID to match the entity PK format

4. **OrgRepository**:
   - `createLocal(slug, name)` — replaces `createLocalOrg()`

5. **ApiKeyRepository**:
   - `findByHash(hash)` — replaces `findApiKeyByHash()`

Each repository:
- Extends `EntityRepository<Entity>` from MikroORM
- Uses `this.em` (EntityManager) not raw SQL
- Uses `this.findOne()`, `this.find()`, `this.create()`, `this.em.persistAndFlush()`
- Matches existing repository patterns in src/db/repositories/

Read existing repositories first to match the project's repository pattern.
  </action>
  <verify>
    <automated>rg -l "EntityRepository\|extends.*Repository" src/db/repositories/tasks/TaskRepository.ts src/db/repositories/core/EventRepository.ts 2>/dev/null | wc -l | xargs test 1 -le</automated>
  </verify>
  <acceptance_criteria>
- TaskRepository has `create` and `listByOrg` methods
- SprintRepository has `create`, `update`, `listByOrg` methods
- EventRepository has `append` method using UUID (not ULID)
- OrgRepository has `createLocal` method
- All repositories use MikroORM EntityManager, zero raw SQL
  </acceptance_criteria>
  <done>MikroORM repositories cover all functions from product-kernel/store/repositories.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire callers and deprecate repositories.ts raw SQL</name>
  <files>src/product-kernel/store/repositories.ts, src/product-kernel/api/router.ts, scripts/seed-web-shell.ts</files>
  <read_first>src/product-kernel/api/router.ts, scripts/seed-web-shell.ts</read_first>
  <action>
1. In `src/product-kernel/api/router.ts`:
   - Replace imports from `../store/repositories.ts` with MikroORM repository usage
   - The API router has access to `db: ProductDb` in its env — it needs EntityManager instead
   - Add EntityManager to ApiEnv (or get it from the MikroORM instance)
   - Replace: `createTask(db, orgId, input)` → `em.getRepository(Task).create(orgId, input)`
   - Replace: `listTasks(db, orgId)` → `em.getRepository(Task).listByOrg(orgId)`
   - Same for sprint, event functions

2. In `scripts/seed-web-shell.ts`:
   - Replace `appendEvent(db, ...)` with EventRepository usage
   - Replace `createLocalOrg(db, ...)` with OrgRepository usage
   - Seed script may need MikroORM init — check how other scripts get EntityManager

3. In `src/product-kernel/store/repositories.ts`:
   - Add `@deprecated` JSDoc to each exported function
   - Have each function log a warning: `console.warn("deprecated: use MikroORM repository")`
   - OR if all callers are updated, delete the raw SQL function bodies and re-export from MikroORM repos
   - Keep the file as a re-export facade until all callers are confirmed migrated

4. Verify: `rg "from.*repositories" --type ts` shows no direct raw SQL usage from app code
  </action>
  <verify>
    <automated>rg "import.*from.*store/repositories" src/product-kernel/api/router.ts 2>/dev/null | grep -v "deprecated\|//" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
- `src/product-kernel/api/router.ts` imports from MikroORM repositories, not raw SQL
- `scripts/seed-web-shell.ts` uses MikroORM repositories
- Each function in repositories.ts either deleted or marked @deprecated
- `rg "db\.query" src/product-kernel/store/repositories.ts` returns 0 active (non-deprecated) calls
  </acceptance_criteria>
  <done>product-kernel/store/repositories.ts raw SQL eliminated. All callers use MikroORM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> data layer | Ensuring ORM parameterization prevents SQL injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | Raw SQL in repositories.ts | mitigate | Replace with MikroORM parameterized queries; ORM handles escaping |
| T-01-11 | Repudiation | Event PK format inconsistency | mitigate | EventRepository uses UUID only (matching entity), eliminating ULID/UUID mix |
</threat_model>

<verification>
- `rg "db\.query" src/product-kernel/store/repositories.ts` returns 0 active calls
- Product-kernel API router uses MikroORM
- Event persistence uses UUID (not ULID)
</verification>

<success_criteria>
- Zero raw SQL in product-kernel/store/repositories.ts (or file deprecated)
- All callers migrated to MikroORM repositories
- Event PKs consistently UUID
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-convergence-security/01-05-SUMMARY.md`
</output>
