---
phase: 01-architecture-convergence-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trpc/router.ts
autonomous: true
requirements: [ARCH-07, ARCH-08]

must_haves:
  truths:
    - "AppRouter contains zero inline stub routers (no crudRouter, listProcedure, getProcedure, mutationProcedure, idMutationProcedure helpers)"
    - "No duplicate mount aliases exist — each domain router mounted once under canonical name"
    - "tRPC type inference still works (AppRouter type exports unchanged)"
  artifacts:
    - path: "src/trpc/router.ts"
      provides: "Clean AppRouter with no stubs, no duplicate mounts"
      contains: "export const appRouter"
  key_links:
    - from: "src/trpc/router.ts"
      to: "src/server/trpc/routers/*.ts"
      via: "import statements for real domain routers"
      pattern: "import.*Router.*from"
---

<objective>
Remove ~200 lines of stub routers and duplicate mount aliases from AppRouter.

Purpose: Stubs return empty arrays/null for CRUD operations, masking missing implementations. Duplicate mounts (skills/fulcrum_skills, memory/memories, runs/agent_runs, notify/notifications) create Casbin authorization bypass via aliased resource strings.

Output: Clean AppRouter with only real domain routers, each mounted once.
</objective>

<execution_context>
@.planning/phases/01-architecture-convergence-security/01-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/trpc/router.ts

<interfaces>
From src/trpc/router.ts (current state — to be cleaned):
- Lines 60-121: Stub infrastructure (StubRowSchema, StubOperationOutputSchema, op(), listProcedure(), getProcedure(), mutationProcedure(), idMutationProcedure(), crudProcedures(), crudRouter())
- Lines 134-140: projectsRouter stub (uses crudProcedures)
- Lines 142-148: customFieldsRouter stub
- Lines 150: savedViewsRouter stub
- Lines 152-156: docVersionsRouter stub
- Lines 158-163: docCommentsRouter stub
- Lines 165-169: docLinksRouter stub
- Lines 171-180: contextRouter stub
- Lines 182-188: agentRunsRouter stub
- Lines 248-251: repoBranchesRouter stub
- Lines 253-256: repoCommitsRouter stub
- Lines 258-283: searchRouter (partial stub — savedList/savedCreate/savedUpdate/savedDelete are REAL, query/suggest are stubs)
- Lines 287-297: connectorsRouter stub
- Lines 299-316: doctorRouter stub
- Lines 318-323: invitationsRouter stub

Duplicate mounts at lines 325-381:
- `fulcrum_skills: skillsRouter` (line 359) — duplicate of `skills: skillsRouter` (line 358)
- `memory: memoryRouter` (line 378) — duplicate of `memories: memoryRouter` (line 340)
- `runs: agentRunsRouter` (line 379) — duplicate of `agent_runs: agentRunsRouter` (line 344)
- `notifications: notificationsRouter` (line 380) — duplicate of `notify: notificationsRouter` (line 349)

Real routers that MUST be preserved:
- agentsRouter (lines 190-246) — has real testProfile mutation with DB access
- searchRouter saved search procedures (lines 267-283) — real implementations
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate mount aliases from AppRouter</name>
  <files>src/trpc/router.ts</files>
  <read_first>src/trpc/router.ts</read_first>
  <action>
Remove these duplicate mount entries from the appRouter definition (per ARCH-08):
- `fulcrum_skills: skillsRouter` — keep only `skills: skillsRouter`
- `memory: memoryRouter` — keep only `memories: memoryRouter` (memories is the canonical name used by CLI codegen)
- `runs: agentRunsRouter` — keep only `agent_runs: agentRunsRouter`
- `notifications: notificationsRouter` — keep only `notify: notificationsRouter`

Also remove the comment "// Backward-compatible aliases for pre-P13 root names." and the three lines below it.

Do NOT remove the canonical mounts. Only remove the alias duplicates.

After removing, grep the codebase for any caller using the removed alias names:
- `trpc.fulcrum_skills.` — if found, update to `trpc.skills.`
- `trpc.memory.` — if found, update to `trpc.memories.`
- `trpc.runs.` — if found, update to `trpc.agent_runs.`
- `trpc.notifications.` — if found, update to `trpc.notify.`

Check CLI generated files, TUI files, and web route files for these alias usages.
  </action>
  <verify>
    <automated>rg -c "fulcrum_skills:|memory: memoryRouter|runs: agentRunsRouter|notifications: notificationsRouter" src/trpc/router.ts | grep -q "^0$" || echo "FAIL: aliases still present"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "fulcrum_skills" src/trpc/router.ts` returns 0
- `grep -c "Backward-compatible aliases" src/trpc/router.ts` returns 0
- No callers in codebase reference removed alias names (or callers updated to canonical names)
  </acceptance_criteria>
  <done>Zero duplicate mounts in AppRouter. All callers use canonical names.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stub routers and stub infrastructure from AppRouter</name>
  <files>src/trpc/router.ts</files>
  <read_first>src/trpc/router.ts</read_first>
  <action>
Remove all stub infrastructure and inline stub routers (per ARCH-07):

1. Remove stub helper functions (lines 57-121): EmptyInputSchema, IdInputSchema, OptionalRecordInputSchema, StubRowSchema, StubOperationOutputSchema, op(), listProcedure(), getProcedure(), mutationProcedure(), idMutationProcedure(), crudProcedures(), crudRouter()

2. Remove inline stub routers that use these helpers:
   - projectsRouter (lines 134-140) — replace with `crudRouter("projects")` placeholder comment `// TODO: real projectsRouter needed`
   - customFieldsRouter (lines 142-148)
   - savedViewsRouter (line 150)
   - docVersionsRouter (lines 152-156)
   - docCommentsRouter (lines 158-163)
   - docLinksRouter (lines 165-169)
   - contextRouter (lines 171-180)
   - agentRunsRouter (lines 182-188)
   - repoBranchesRouter (lines 248-251)
   - repoCommitsRouter (lines 253-256)
   - connectorsRouter (lines 287-297)
   - doctorRouter (lines 299-316)
   - invitationsRouter (lines 318-323)

3. For searchRouter (lines 258-283): Keep the real saved search procedures (savedList, savedCreate, savedUpdate, savedDelete). Remove the stub query/suggest procedures. The result is a searchRouter with only saved search operations.

4. For each removed stub router, comment out its mount in appRouter with: `// STUB REMOVED — real router needed: {domain}`

5. Keep these REAL routers untouched: agentsRouter (has testProfile), dbRouter, healthRouter, and all imported routers.

6. Verify the file still compiles: `bun build --no-bundle src/trpc/router.ts --outdir /tmp/check-router 2>&1`
  </action>
  <verify>
    <automated>grep -v '^//' src/trpc/router.ts | grep -v '^\s*//' | grep -c "crudRouter\|crudProcedures\|listProcedure()\|getProcedure()\|mutationProcedure\|idMutationProcedure\|StubRowSchema\|StubOperationOutputSchema" | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "StubRowSchema" src/trpc/router.ts` returns 0
- `grep -c "crudRouter" src/trpc/router.ts` returns 0
- `grep -c "listProcedure()" src/trpc/router.ts` returns 0
- File still type-checks (no import errors on removed routers)
- Real routers (agentsRouter, searchRouter saved searches) preserved
  </acceptance_criteria>
  <done>AppRouter has zero stub routers. Only real domain routers remain mounted. Stub mount points commented with "STUB REMOVED" markers for future implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> tRPC | Removing stubs means clients calling stub endpoints get errors instead of empty data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | Duplicate router mounts | mitigate | Remove alias mounts that bypass Casbin resource-string checks |
| T-01-02 | Information Disclosure | Stub routers | mitigate | Remove stubs that return empty arrays instead of proper 404/not-implemented |
</threat_model>

<verification>
- `rg "crudRouter\|StubRowSchema\|fulcrum_skills" src/trpc/router.ts` returns no matches
- `bun run check` passes (or type-check equivalent)
- AppRouter type still exports correctly
</verification>

<success_criteria>
- Zero stub routers in AppRouter
- Zero duplicate mount aliases
- All real routers preserved and functional
- Type-check passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-convergence-security/01-01-SUMMARY.md`
</output>
