---
phase: 01-architecture-convergence-security
plan: 10
type: execute
wave: 4
depends_on: [01-05, 01-07, 01-08, 01-09]
files_modified:
  - src/product-kernel/api/router.ts
  - src/api/hono.ts
  - src/trpc/context.ts
autonomous: false
requirements: [ARCH-09, ARCH-12]

must_haves:
  truths:
    - "Single Hono API at src/api/ with unified auth middleware"
    - "product-kernel/api/router.ts deleted or merged into src/api/"
    - "TrpcContext no longer has db?: ProductDb field — only em: EntityManager"
    - "OpenAPI spec serves from unified API"
  artifacts:
    - path: "src/api/hono.ts"
      provides: "Unified REST API with merged routes from product-kernel"
      contains: "OpenAPIHono"
    - path: "src/trpc/context.ts"
      provides: "Clean TrpcContext with em only"
  key_links:
    - from: "src/api/hono.ts"
      to: "src/services/"
      via: "route handlers delegate to services"
      pattern: "import.*Service"
---

<objective>
Merge two Hono APIs into one and remove ProductDb from TrpcContext.

Purpose: Two API implementations (product-kernel/api/router.ts + src/api/hono.ts) with different auth, different routes, different OpenAPI specs. TrpcContext carries both em and db fields, allowing silent fallback to empty results when em is null.

Output: Single API, single auth, single OpenAPI spec. TrpcContext is em-only.
</objective>

<execution_context>
@.planning/phases/01-architecture-convergence-security/01-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
From src/product-kernel/api/router.ts (674 lines):
- OpenAPIHono with routes for tasks, sprints, reports, notifications, audit
- Own Bearer token auth via SHA-256 hash lookup
- Imports from web layer (fixed in plan 09) and repositories.ts (migrated in plan 05)

From src/api/hono.ts (to be checked):
- May have separate route definitions and auth

From src/trpc/context.ts:
- TrpcContext likely has: { em?: EntityManager, db?: ProductDb, orgId, userId, requestId }
- After ARCH-02 complete (plans 05+06), db field is unused
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge product-kernel API routes into src/api/hono.ts</name>
  <files>src/api/hono.ts, src/product-kernel/api/router.ts</files>
  <read_first>src/api/hono.ts, src/product-kernel/api/router.ts, src/product-kernel/api/schemas.ts</read_first>
  <action>
1. Read both API files to understand route overlap and auth differences.

2. In `src/api/hono.ts`:
   - Import route definitions from product-kernel/api/ that don't already exist
   - Product-kernel API has: task CRUD, sprint CRUD, reports (velocity/burndown), notification rules CRUD, audit query
   - Merge these routes under the same `/api/v1/` prefix

3. Unify auth middleware:
   - product-kernel uses: Bearer token -> SHA-256 hash lookup in api_keys table
   - src/api/ likely uses: session cookie or similar
   - Create single auth middleware that supports BOTH: session cookie OR Bearer token
   - Pattern: check Authorization header first, fall back to session cookie

4. Move OpenAPI schema definitions (from product-kernel/api/schemas.ts) into src/api/schemas.ts or colocate with routes.

5. After merge: delete product-kernel/api/router.ts (or empty it to a re-export)
   - Check all imports of this file: `rg "product-kernel/api/router" --type ts`
   - Update or remove all import references

6. Verify OpenAPI spec is complete:
   - All routes from both APIs appear in the merged spec
   - `GET /api/v1/openapi.json` serves valid spec
  </action>
  <verify>
    <automated>! test -s src/product-kernel/api/router.ts 2>/dev/null && rg "OpenAPIHono" src/api/hono.ts 2>/dev/null | head -1 && echo "OK" || rg -c "route" src/api/hono.ts</automated>
  </verify>
  <acceptance_criteria>
- `src/product-kernel/api/router.ts` deleted or empty
- `src/api/hono.ts` contains all routes from both APIs
- Single auth middleware handling both Bearer token and session cookie
- `rg "product-kernel/api/router" src/ --type ts` returns 0 active imports
  </acceptance_criteria>
  <done>Single unified Hono API with merged routes and auth.</done>
</task>

<task type="auto">
  <name>Task 2: Remove ProductDb from TrpcContext</name>
  <files>src/trpc/context.ts</files>
  <read_first>src/trpc/context.ts</read_first>
  <action>
1. Read `src/trpc/context.ts` to find the `db?: ProductDb` field.

2. Remove the `db?: ProductDb` field from TrpcContext type and creation.

3. Search for all uses of `ctx.db` in the codebase:
   ```bash
   rg "ctx\.db\b" src/ --type ts --glob '!*.test.*'
   ```
   All should be 0 after plans 05-06 migrated everything to MikroORM.

4. If any `ctx.db` references remain, update them to use `ctx.em` (EntityManager).

5. Remove ProductDb import from context.ts.

6. Remove any ProductDb-related context creation (openProductDb calls in context factory).

7. Verify type-check passes: `bun run check` or `tsc --noEmit`
  </action>
  <verify>
    <automated>rg -c "ProductDb" src/trpc/context.ts 2>/dev/null | xargs test 0 -eq && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "ProductDb" src/trpc/context.ts` returns 0
- `grep -c "db?:" src/trpc/context.ts` returns 0 (no optional db field)
- `rg "ctx\.db\b" src/ --type ts --glob '!*.test.*'` returns 0
- Type-check passes
  </acceptance_criteria>
  <done>TrpcContext carries em only. No ProductDb in tRPC layer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All 12 ARCH requirements and 4 SEC requirements addressed:
- Stub routers removed, duplicate mounts eliminated (ARCH-07, ARCH-08)
- Security vulnerabilities patched (SEC-01..04)
- DDL removed from handlers (ARCH-11)
- PGlite connection pooled (ARCH-10)
- Raw SQL migrated to MikroORM (ARCH-01, ARCH-02)
- Service layer extracted (ARCH-03)
- Events unified (ARCH-04)
- Module boundaries enforced (ARCH-05, ARCH-06)
- APIs consolidated (ARCH-12)
- TrpcContext cleaned (ARCH-09)
  </what-built>
  <how-to-verify>
1. Run `bun run check` — should pass with zero type errors
2. Run `bun test` — existing tests should pass
3. Start dev server: `bun run dev` — should start without errors
4. Verify zero raw SQL: `rg "db\.query" src/web/src/lib/server/*.ts --glob '!*.test.*'` — should return 0
5. Verify zero stubs: `rg "StubRowSchema\|crudRouter" src/trpc/router.ts` — should return 0
6. Verify no layering violations: `rg "from.*web/src/lib/server" src/product-kernel/ src/cli/` — should return 0
7. Load a page in browser — verify data renders (not empty arrays from stubs)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API auth | Unified auth must handle both token and session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-18 | Spoofing | Dual auth merge | mitigate | Single middleware checking Bearer token first, session cookie second; both validated |
| T-01-19 | Elevation of Privilege | ProductDb bypass | mitigate | Remove db field from context; all access through em with permission checks |
</threat_model>

<verification>
- `bun run check` passes
- Single API serves all routes
- TrpcContext has no ProductDb
- Dev server starts and pages render data
</verification>

<success_criteria>
- Single Hono API with unified auth
- TrpcContext is em-only (no ProductDb)
- All ARCH and SEC requirements verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-convergence-security/01-10-SUMMARY.md`
</output>
