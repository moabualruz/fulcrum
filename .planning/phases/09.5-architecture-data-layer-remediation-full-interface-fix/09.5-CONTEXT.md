# Phase 9.5: Full Architecture Overhaul + Integration Preservation - Context

**Gathered:** 2026-05-06
**Updated:** 2026-05-06 (consolidated from gray area resolutions + new discussion)
**Status:** Ready for planning

<domain>
## Phase Boundary

Establish one unified data manipulation layer (`src/application/**`) that every interface (Web, CLI, TUI, API/tRPC, workers) calls for all data operations. Eliminate all direct DB access from interfaces, consolidate dual migration systems, repair navigation/routing, and prove integration with real E2E tests. Preserve every existing feature/logic/benefit — no regressions.

</domain>

<decisions>
## Implementation Decisions

### Framework Stack (LOCKED)
- **D-01:** Keep Hono + `@hono/zod-openapi` as REST/OpenAPI adapter. No framework switch.
- **D-02:** Keep tRPC as first-party typed adapter for CLI/TUI/web.
- **D-03:** Keep `needle-di` for dependency injection. Add application-service bindings to existing container.
- **D-04:** MikroORM is sole ORM and sole migration authority.
- **D-05:** Bun runtime, SvelteKit web framework — no changes.
- **D-06:** No adoption of NestJS, Elysia, Fastify, Express, Koa, hapi, GraphQL, Effect Platform, or Encore.ts.

### Application Layer Shape
- **D-07:** Move + reshape `src/services/` into `src/application/` domain modules. Structure: `src/application/{domain}/commands.ts`, `queries.ts`, `types.ts`.
- **D-08:** Existing service classes (TaskService, DocService, SprintService, etc.) decompose into domain module exported functions. Class methods → named exports.
- **D-09:** Legacy function-based files (`tasks.ts`, `runs.ts`, `artifacts.ts`) fold into their domain modules. One place per domain.
- **D-10:** Application commands/queries are the ONLY callable boundary from interfaces. Dependency direction: `interfaces → application → ORM repositories → database`.

### Application Layer Internals
- **D-11:** Application commands open write transactions. Queries do not open write transactions.
- **D-12:** Create `src/application/errors.ts` with typed error classes: `AppValidationError`, `AppUnauthorizedError`, `AppForbiddenError`, `AppNotFoundError`, `AppConflictError`, `AppInvariantError`, `AppExternalDependencyError`. Adapters map to transport-specific codes.
- **D-13:** Shared DTO types live in application `types.ts` files. Allows tRPC/OpenAPI/CLI output to share schemas.
- **D-14:** Continue per-request/per-command EntityManager forks via needle-di container.

### Database Access Rules
- **D-15:** NO raw SQL anywhere — all DB access through MikroORM QueryBuilder or repository methods. Application query modules may use QueryBuilder for complex aggregations but NOT `em.execute()` or `.query()` raw SQL.
- **D-16:** Dashboard loader and all `+page.server.ts` files must call application query modules, not direct EM/raw SQL.
- **D-17:** Web `OrmProductDb.query()` compatibility shim must be eliminated. All callers move to application services.
- **D-18:** Deprecated `openProductDb()` must be removed. `getProductDb()` stays only as startup bootstrap, not as data access.

### Migration Consolidation
- **D-19:** MikroORM migrations are the only runtime migration authority.
- **D-20:** Product-kernel SQL migrations become legacy compatibility input only.
- **D-21:** Tests must seed through MikroORM migrations unless explicitly testing legacy upgrade path.
- **D-22:** Keep existing MikroORM migration files; do not collapse into one giant file.

### Product-Kernel Entity Coverage
- **D-23:** Create MikroORM entities for ALL product-kernel domains that currently only have raw SQL store functions: bitbucket (PRs, issues, auth), gitlab (MRs, issues, auth), repo-files (tree, blame, content), audit (events, exports).
- **D-24:** Product-kernel store functions become legacy — replaced by application domain modules backed by new MikroORM entities.

### Interface Boundaries
- **D-25:** SvelteKit `+page.server.ts` / `+server.ts` / form actions parse input, call application services, render/serialize result, map errors. No business logic, no direct DB.
- **D-26:** tRPC routers become thin adapters over application commands/queries. No business logic in procedures.
- **D-27:** Hono REST routes become thin adapters over application commands/queries.
- **D-28:** CLI keeps `createLocalCaller()` in-process tRPC path. Convert direct PGlite CLI files (`import.ts`, `sprints.ts`, `artifact.ts`) to caller/application adapters. Exception: `fulcrum db migrate` may call DB/migrator layer directly.
- **D-29:** TUI keeps `buildCaller()` in-process tRPC path. Remove direct DB enrichment from `enrichTuiCaller()` — expose required data through application/tRPC queries.
- **D-30:** Workers call application commands/queries. No direct DB from worker code.

### Authorization & Tenant Enforcement
- **D-31:** Authorization and tenant enforcement move into application command/query entrypoints.
- **D-32:** tRPC middleware, REST auth middleware, web sessions, CLI auth, TUI auth become transport authentication/context builders only.

### Validation & Schema Ownership
- **D-33:** Zod schemas owned by application domain modules. Interface adapters reference them, not own separate schemas.

### Event System
- **D-34:** Full transactional outbox pattern: MikroORM outbox entity + polling/pg-notify worker. Events persisted in same transaction as domain write, dispatched async.
- **D-35:** Application commands emit domain events once through the outbox. No interface-specific event emission.
- **D-36:** Existing `EventDispatcher` logic moves from ProductDb/product-kernel into application layer.
- **D-37:** Search indexing triggered from application events/outbox, not interface-specific writes.

### Collab/Realtime System (audited 2026-05-06)
- **D-38:** Full audit + rewire of collab system through application events.
- **D-39:** PGlite bridge (`src/subscriptions/pglite-bridge.ts`) takes direct PGlite import — must be rewired to use MikroORM/application events. `emitNotify()` calls `pglite.query()` directly — must route through application event outbox.
- **D-40:** Bell notification broadcaster's `unreadCountForUser()` must come from application query, not direct DB.
- **D-41:** Collab server room manager (`src/docs/collab/bun-ws-collab-server.ts`) is in-memory, feature-flagged — preserve existing behavior but ensure doc persistence writes go through application layer.
- **D-42:** `src/subscriptions/event-bus.ts` (in-process EventBus) is transport — stays as-is but receives events from application outbox, not from direct PGlite LISTEN/NOTIFY.

### Search & Indexing
- **D-43:** Search indexing triggered from application events/outbox. Feature writes cannot bypass search indexing.

### REST/OpenAPI
- **D-44:** Hono + `@hono/zod-openapi` remains external/stable REST/OpenAPI adapter. Thin adapter over application commands/queries.

### UI/UX
- **D-45:** Fix stale navigation and command palette. Map route/action ownership.
- **D-46:** Keep operational-density design, not marketing layouts.

### Test Architecture
- **D-47:** Playwright E2E is real and mandatory using Tiered Mandatory policy: normal CI hard-fails smoke/critical journeys; full Playwright workflows gate Phase 9.5 acceptance.
- **D-48:** Module-boundary tests that fail on raw DB access from interfaces.
- **D-49:** Service/ORM integration tests through application layer.
- **D-58:** CLI integration tests: all 20+ test files in `src/cli/*.test.ts` must pass after migration. Exit codes (0/1/2), stdout format, `--json` output stability verified.
- **D-59:** TUI integration tests: all 12+ test files in `src/tui/__tests__/` and `src/tui/screens/*.test.ts` must pass. Parity test (`phase08-tui-parity.test.ts`) and OpenTUI gate must pass.
- **D-60:** Cross-interface parity tests: for each domain module (tasks, sprints, runs, notifications), verify that Web, CLI, TUI, and API produce equivalent results when calling the same application command/query.
- **D-61:** CLI command signature regression tests: verify every existing CLI flag/argument/subcommand still works identically after migration from PGlite to application services.
- **D-62:** TUI screen rendering tests: each TUI screen must produce expected output for given application data after `enrichTuiCaller()` removal.

### Feature Preservation
- **D-50:** Every move/delete has replacement mapping, test proof, and migration note.
- **D-51:** Feature preservation inventory required before any destructive changes.

### Performance & Startup
- **D-52:** Application layer must not add measurable startup penalty vs current direct-access pattern.

### Security
- **D-53:** Application layer enforces org/project/user permissions so all interfaces share authorization behavior.

### Rollout Strategy
- **D-54:** Vertical slice first: pick one domain (e.g., tasks), build full application module, wire all interfaces, prove E2E. Then expand domain by domain.

### Policies (User-Solidified)
- **D-55:** Backend Spike Policy: Paper First. No runnable NestJS/Elysia spikes unless application-layer design fails a concrete check.
- **D-56:** ProductDb/Product-Kernel Shim Policy: Strict Allowlist. Product-kernel SQL stores are legacy-only, not runtime data API.
- **D-57:** Playwright Gate Policy: Tiered Mandatory. Smoke/critical in CI; full journeys gate acceptance.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Architecture & Audit
- `.planning/INTEGRATION-FAILURE-AUDIT.md` — Root cause analysis of why Phase 10 was blocked. F-001 through F-006 findings.
- `.planning/audit/` — Detailed per-finding audit documents.
- `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-GRAY-AREA-RESOLUTIONS.md` — Full rationale for all 25 gray area decisions.
- `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-RESEARCH.md` — Framework research (Hono, Elysia, Fastify, Express, Koa, hapi comparison).
- `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CODEBASE-REUSE-RESEARCH.md` — Existing reusable assets inventory.

### Existing Code (must understand before modifying)
- `src/services/` — Current service layer (13+ classes) being reshaped into `src/application/`.
- `src/db/db.module.ts` — needle-di container bindings for all repositories.
- `src/db/mikro-orm.config.ts` — ORM configuration and initialization.
- `src/subscriptions/` — EventBus + PGlite bridge + polling fallback (realtime transport).
- `src/docs/collab/` — Collab server, provider factory, flag guard.
- `src/notifications/realtime-bell.ts` — Notification broadcaster via awareness server.
- `src/product-kernel/store/` — Legacy SQL store functions to be replaced.
- `src/web/src/lib/server/db.ts` — Web DB singleton (OrmProductDb shim to eliminate).
- `src/web/src/lib/server/dashboard.ts` — Dashboard loader (raw SQL via EM to convert).
- `src/web/src/lib/product-queries.ts` — Product queries (to route through application).
- `src/web/src/lib/components/app/nav-items.ts` — Navigation inventory (19 items).

### Roadmap
- `.planning/ROADMAP.md` §Phase 9.5 — Success criteria (9 items), TDD policy, requirements codes.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **13 service classes** in `src/services/`: TaskService, DocService, SprintService, CommentService, WorkflowService, AutomationService, FieldDependencyService, RecurrenceService, RelationshipService, ReportService, TemplateService, plus function-based tasks.ts, runs.ts, artifacts.ts. These ARE the business logic — reshape, don't rewrite.
- **needle-di container** in `src/db/db.module.ts`: 30+ repository bindings already registered. Add application-service bindings alongside.
- **tRPC caller paths**: CLI `createLocalCaller()` and TUI `buildCaller()` already create shared context with forked EntityManager. Pattern works — just need application layer beneath.
- **EventBus** in `src/subscriptions/event-bus.ts`: Process-singleton pub/sub, 4 topic channels. Reuse as transport; feed from outbox instead of PGlite bridge.
- **Collab system**: Feature-flagged (`real-time-collab-server`), self-contained. No actual Yjs dependency — custom Bun WS room manager + provider factory. Needs 2 PGlite violation fixes but otherwise clean.

### Established Patterns
- **Singleton DB** via `initProductDb()` / `getProductDb()` in web — will be replaced by application layer but startup pattern stays.
- **Feature flags** via `FULCRUM_FEATURES` env var comma-separated — collab system uses this consistently.
- **MikroORM migrations** in `src/db/migrations/` — 30+ TypeScript migration files. This is the winning migration path.
- **Product-kernel SQL migrations** in `src/product-kernel/db/migrations/` — 40+ SQL files. Legacy path to deprecate.

### Integration Points
- **Web hooks.server.ts** — Entry point for DB initialization. Must set up application layer here.
- **tRPC routers** — Already exist but contain business logic. Must become thin adapters.
- **Hono REST routes** in `src/web/src/routes/api/` — Direct DB access in CSV import/export, etc.
- **CLI commands** — 20+ files with direct PGlite/DB access to convert.
- **TUI desktop-app.ts** — Entry point for TUI with enrichment that bypasses services.

### MikroORM Entity Coverage (75 entities exist)
Domains WITH entities: auth (User, Org, Session, Account, etc.), tasks (Task, Project, Sprint, etc.), docs (Document, DocVersion, DocLink, DocComment, DocTemplate), orchestration (AgentRun, WorkflowDefinition), repos (Repo, RepoBranch, RepoCommit, RepoFilesIndex), notifications (Notification, NotificationDelivery, PushSubscription, Webhook, etc.), search (SearchDocument, SavedSearch), skills (FulcrumSkill, McpVirtualSkill, SkillConflict, SkillVersion), memory (Memory, MemoryLink, ContextSnapshot), jobs (Job), router (RoutingRule, RoutingDraft, RoutingAudit), inference (ModelCache, ProviderCredential), platform (Credential, ErrorLog, TelemetryEvent, TelemetryOutbox, etc.), sandbox (AgentProfile, Artifact, Edge), flags (CasbinRule, WebhookSubscription, NotificationRule), connectors (ConnectorSyncLog), artifacts (ArtifactRetentionPolicy).

Domains WITHOUT entities (need creation — D-23):
- **Bitbucket**: PRs, issues, app passwords, OAuth tokens — currently `src/product-kernel/store/bitbucket.ts`
- **GitLab**: MRs, issues, PATs, OAuth tokens — currently `src/product-kernel/store/gitlab.ts`
- **GitHub**: connector state — currently `src/product-kernel/store/github.ts`
- **Repo files**: tree, blame, content — currently `src/product-kernel/store/repo-files.ts` (Note: `RepoFilesIndex` entity exists but covers indexing, not the full tree/blame/content domain)
- **Audit**: events, exports, export status — currently `src/product-kernel/store/audit.ts`
- **Settings**: tenant settings, connectors, credentials — currently `src/product-kernel/store/settings.ts` + `settings-connectors-credentials.ts`
- **Storage backends**: S3, GCS, Azure — currently `src/product-kernel/store/storage-factory.ts` + backend files (these are adapter config, may not need entities)

</code_context>

<violation_inventory>
## Violation Inventory (exact files to fix)

### Web Routes Importing `openProductDb` / `getProductDb` (30+ files)
Every file below calls `openProductDb()` or `getProductDb()` and runs raw SQL. Must convert to application query/command calls.

**Dashboard + Home:**
- `src/web/src/routes/+page.server.ts` → imports `openProductDb` + `loadDashboard`

**Runs:**
- `src/web/src/routes/runs/+page.server.ts` → `openProductDb`
- `src/web/src/routes/runs/[id]/+page.server.ts` → `openProductDb`
- `src/web/src/routes/runs/[id]/artifacts/+page.server.ts` — (test file references)

**Docs:**
- `src/web/src/routes/docs/[id]/+page.server.ts` → `openProductDb`
- `src/web/src/routes/docs/[id]/history/+page.server.ts` → `openProductDb`
- `src/web/src/routes/docs/[id]/edit/+page.server.ts` — (test file references)
- `src/web/src/routes/docs/global/+page.server.ts` → `openProductDb`
- `src/web/src/routes/docs/new/+page.server.ts` — (test file references)

**Projects:**
- `src/web/src/routes/projects/+page.server.ts` → `openProductDb`
- `src/web/src/routes/projects/[id]/backlog/+page.server.ts` → `openProductDb`
- `src/web/src/routes/projects/[id]/gantt/+page.server.ts` → `openProductDb`
- `src/web/src/routes/projects/[id]/artifacts/+page.server.ts` → `openProductDb`
- `src/web/src/routes/projects/[id]/sprints/+page.server.ts` → `openProductDb`
- `src/web/src/routes/projects/[id]/repos/+page.server.ts` → product-kernel store import
- `src/web/src/routes/projects/[id]/activity/+page.server.ts` → product-kernel `listEventsFiltered`
- `src/web/src/routes/projects/[id]/reports/+page.server.ts` → `openProductDb`

**Memory, Context, Orchestration:**
- `src/web/src/routes/memory/+page.server.ts` → `openProductDb`
- `src/web/src/routes/memory/[id]/+page.server.ts` → `openProductDb`
- `src/web/src/routes/context/preview/+page.server.ts` → `openProductDb`
- `src/web/src/routes/orchestration/+page.server.ts` → `openProductDb`

**Artifacts, Audit, Tasks:**
- `src/web/src/routes/artifacts/+page.server.ts` → `openProductDb`
- `src/web/src/routes/artifacts/[id]/+page.server.ts` → `openProductDb`
- `src/web/src/routes/artifacts/[id]/download/+server.ts` → `openProductDb`
- `src/web/src/routes/audit/+page.server.ts` → `openProductDb` + product-kernel `EventRow`
- `src/web/src/routes/audit/export/+server.ts` → `openProductDb`
- `src/web/src/routes/tasks/[id]/+page.server.ts` → `openProductDb`
- `src/web/src/routes/boards/+page.server.ts` → `openProductDb`

**API Routes:**
- `src/web/src/routes/api/data/export-csv/+server.ts` → `openProductDb` + product-kernel `TaskRow`
- `src/web/src/routes/api/data/import-csv/+server.ts` → `openProductDb` + product-kernel `createTask`
- `src/web/src/routes/api/skills/+server.ts` → `openProductDb`
- `src/web/src/routes/api/bell/+server.ts` → `openProductDb`
- `src/web/src/routes/api/repos/[id]/tree/+server.ts` → `openProductDb` + product-kernel repo-files

**Settings:**
- `src/web/src/routes/settings/integrations/linear/+page.server.ts` → product-kernel `settings-connectors-credentials`

**Web server helpers (raw SQL):**
- `src/web/src/lib/server/dashboard.ts` — 9 raw `conn.execute()` SQL queries
- `src/web/src/lib/product-queries.ts` — 11 raw `db.query()` SQL calls
- `src/web/src/lib/server/projects.ts` → product-kernel `createProject`
- `src/web/src/lib/server/sprints.ts` → product-kernel store
- `src/web/src/lib/server/settings.ts` → product-kernel store
- `src/web/src/lib/server/repo-files.ts` → product-kernel store

### CLI Files with Direct PGlite (3 files + callers)
- `src/cli/import.ts:14` — `import { openPglite }`, line 142: `await openPglite(join(dbDir, "main"))`
- `src/cli/sprints.ts:3` — `import { openPglite }`, line 58: `await openPglite(join(dir, "main"))`
- `src/cli/artifact.ts:3` — `import { openPglite }`, line 15-18: `openProductDb()` wrapper calling `openPglite`

### CLI Files Importing Product-Kernel Stores
- `src/cli/import.ts` → product-kernel `repositories`
- `src/cli/product.ts` → product-kernel `repositories`
- `src/cli/sprints.ts` → product-kernel `repositories`
- `src/cli/export.ts` → product-kernel `TaskRow` from repositories
- `src/cli/connectors.ts` → product-kernel `settings-connectors-credentials`
- `src/cli/notify.ts` → product-kernel `notifications`

### TUI Direct DB Access
- `src/tui/screens/settings-screens.ts` → imports product-kernel `settings-connectors-credentials` types

### Hono/API Routes with Product-Kernel Imports
- `src/api/auth.ts` → product-kernel `findApiKeyByHash`
- `src/api/routes/kernel-audit.ts` → product-kernel `queryAuditEvents`
- `src/api/routes/kernel-tasks.ts` → product-kernel `repositories`
- `src/api/routes/kernel-notifications.ts` → product-kernel `notifications`
- `src/api/routes/kernel-sprints.ts` → product-kernel `repositories`

### tRPC Router Business Logic (direct EM access)
- `src/router/telemetry.ts` — direct `em.persist()` + `em.flush()` via EventRepository
- `src/router/conflict-detector.ts` — direct RoutingRuleRepository queries
- `src/router/no-match-prompt.ts` — direct `em.persist()` + `repository.save()` for rule creation
- `src/router/service.ts` — stub routing rule persistence

### Worker/Orchestration Direct EM (acceptable but should route through application)
- `src/orchestration/symphony/stall.ts` — `em.fork()`
- `src/orchestration/symphony/hooks.ts` — `em.fork()`
- `src/orchestration/symphony/tracker.ts` — 5× `em.fork()` calls
- `src/orchestration/symphony/prompt.ts` — `em.fork()`
- `src/orchestration/symphony/workspace.ts` — 5× `em.fork()`, `em.findOneOrFail()`
- `src/orchestration/symphony/retry.ts` — 2× `em.fork()`
- `src/orchestration/symphony/orchestrator.ts` — `em.fork()`
- `src/notifications/defaults.ts` — raw `em.getConnection().execute()` SQL (violation)

### Event/Data Flow Files
- `src/events/handlers/sprint-closed.ts` → product-kernel `EventRow`, `MetricsSnapshot`
- `src/data/csv-export.ts` → product-kernel `TaskRow`

### Subscriptions PGlite Violations
- `src/subscriptions/pglite-bridge.ts` — imports `PGlite` type directly, `emitNotify()` calls `pglite.query()`
- `src/notifications/realtime-bell.ts` — `unreadCountForUser()` needs application query source

</violation_inventory>

<domain_module_mapping>
## Domain Module Mapping (src/services/ → src/application/)

### tasks/
**Source:** `src/services/TaskService.ts` + `src/services/tasks.ts`
**Methods → exports:** create, update, delete, get, list, assign, move, archive, findByIdentifier, bulkUpdate, reorder
**Also absorbs:** `src/services/FieldDependencyService.ts` (field dependency rules are task-domain)
**Callers:** web routes (tasks, boards, backlog, table, list, sprint), tRPC, CLI import/export, API kernel-tasks

### docs/
**Source:** `src/services/DocService.ts`
**Methods → exports:** create, update, delete, get, list, getVersion, listVersions, search
**Also absorbs:** doc collab persistence writes
**Callers:** web routes (docs/, docs/[id], docs/new, docs/global), tRPC

### sprints/
**Source:** `src/services/SprintService.ts`
**Methods → exports:** create, update, delete, get, list, start, close, getCapacityPreview, removeTask
**Callers:** web routes (sprints, sprint/[id]), CLI sprints, API kernel-sprints

### comments/
**Source:** `src/services/CommentService.ts`
**Methods → exports:** create, update, delete, listForTask
**Callers:** web routes, tRPC

### workflows/
**Source:** `src/services/WorkflowService.ts`
**Methods → exports:** getTransitionGraph, updateTransitions, getMethodology, updateMethodology, getEnabledTaskTypes, updateEnabledTaskTypes
**Callers:** web settings routes, tRPC

### automations/
**Source:** `src/services/AutomationService.ts`
**Methods → exports:** create, update, delete, list, evaluate, matchAndExecute
**Callers:** web settings routes, tRPC, event handlers

### relationships/
**Source:** `src/services/RelationshipService.ts`
**Methods → exports:** create, delete, listForTask, listBlockers, listDependents, checkCyclic
**Callers:** web routes, tRPC

### reports/
**Source:** `src/services/ReportService.ts`
**Callers:** web project reports route

### templates/
**Source:** `src/services/TemplateService.ts`
**Methods → exports:** create, list, delete
**Callers:** web settings routes

### recurrence/
**Source:** `src/services/RecurrenceService.ts`
**Methods → exports:** create, delete, list, evaluateDue
**Callers:** worker/scheduler, tRPC

### runs/
**Source:** `src/services/runs.ts` + `src/services/artifacts.ts`
**Absorbs:** agent run CRUD, artifact CRUD
**Callers:** web routes (runs, artifacts), CLI agent/artifact, tRPC, orchestration

### dashboard/
**Source:** `src/web/src/lib/server/dashboard.ts` (9 raw SQL queries to convert)
**New:** `src/application/dashboard/queries.ts` — loadDashboard via QueryBuilder
**Callers:** web home page `+page.server.ts`

### repos/
**Source:** product-kernel `repos.ts` + `repo-files.ts` + web `repo-files.ts`
**New entities needed:** repo tree, blame, content (beyond existing RepoFilesIndex)
**Callers:** web repos routes, CLI, API tree endpoint

### audit/
**Source:** product-kernel `audit.ts`
**New entities needed:** audit events, exports
**Callers:** web audit route, API kernel-audit, audit export

### connectors/
**Source:** product-kernel `bitbucket.ts`, `gitlab.ts`, `github.ts`, `settings-connectors-credentials.ts`
**New entities needed:** bitbucket PRs/issues/auth, gitlab MRs/issues/auth, github state, connector credentials
**Callers:** CLI connectors, web settings integrations, sync workers

### memory/
**Source:** web memory routes (currently direct DB)
**Entities exist:** Memory, MemoryLink, ContextSnapshot
**Callers:** web memory routes, CLI

### notifications/
**Source:** product-kernel `notifications.ts` + `src/notifications/`
**Entities exist:** Notification, NotificationDelivery, etc.
**Callers:** CLI notify, API kernel-notifications, web bell, fanout worker

### search/
**Source:** web search routes (currently direct DB)
**Entities exist:** SearchDocument, SavedSearch
**Callers:** web search route, indexing from outbox events

### orchestration/
**Source:** `src/orchestration/symphony/` (tracker, stall, retry, orchestrator, etc.)
**Entities exist:** AgentRun, WorkflowDefinition
**Note:** Heavy em.fork() usage — route through application commands for state mutations, keep em.fork() for read-side orchestration queries

### skills/
**Source:** web skills route + entities
**Entities exist:** FulcrumSkill, McpVirtualSkill, SkillConflict, SkillVersion
**Callers:** web skills settings, API skills endpoint

</domain_module_mapping>

<specifics>
## Specific Ideas

- Vertical slice: start with **tasks** domain as first application module. It has the most interface coverage (web, CLI, tRPC, REST) and existing TaskService to reshape.
- Domain module structure: `src/application/tasks/{commands.ts, queries.ts, types.ts}` — exported functions, not classes.
- The collab system is feature-flagged and self-contained — fix PGlite violations but don't restructure the collab architecture itself.

</specifics>

<deferred>
## Deferred Ideas

- **Yjs/Hocuspocus upgrade**: Collab server currently uses custom Bun WS room manager. Evaluating Hocuspocus v4 or native Yjs integration is a separate phase.
- **Effect Platform adoption**: May be future internal service pattern but paradigm shift — not Phase 9.5.
- **GraphQL API surface**: Not needed for first-party TypeScript adapters. Evaluate post-v1.0 if third-party integrations require it.
- **Multi-node event delivery**: Full outbox covers local-first; distributed pub/sub (Redis, NATS) is SaaS-phase work.

</deferred>

---

*Phase: 9.5-architecture-data-layer-remediation-full-interface-fix*
*Context gathered: 2026-05-06*
