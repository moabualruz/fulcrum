---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 13
subsystem: closure-proof
tags: [architecture-boundary, ci, e2e, phase-gate, blocker]

requires:
  - plan: 09.5-09
    provides: application correctness blocker fixes
  - plan: 09.5-10
    provides: CI and Symphony conformance fixes
  - plan: 09.5-11
    provides: web and REST boundary cleanup
  - plan: 09.5-12
    provides: CLI/TUI/router boundary cleanup
provides:
  - Phase 9.5 gap-closure command evidence
  - Phase 10 unblock decision
affects: [phase-09.5, phase-10, architecture-tests, ci]

tech-stack:
  added: []
  patterns:
    - proof-only closure gate
    - stop-on-first-blocker evidence capture

key-files:
  created:
    - .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-13-SUMMARY.md
  modified: []

key-decisions:
  - "Phase 10 remains blocked because Plan 13 architecture grep gates still report direct data-access patterns outside product-kernel."
  - "CI proof commands after the architecture grep gate were not run, per Plan 13 rollback instructions."

requirements-completed: []

duration: proof run completed 2026-05-06
completed: 2026-05-06
---

# Phase 09.5 Plan 13: Architecture And Full E2E Closure Proof Summary

**Result: BLOCKED. Phase 10 is not unblocked.**

Plan 13 is proof-only. Targeted blocker regressions passed, but architecture grep gates failed before full CI/E2E proof. No source or gate changes were made during Plan 13 after the failure.

## Verification Evidence

### Targeted Blocker Regressions

- `cd src/web && bun run web:test -- tests/vitest/cmdk-palette.test.ts` passed: 5 tests, 0 failures.
- `bun test src/architecture && test ! -f src/product-kernel/api/router.ts` passed: 22 architecture tests, 0 failures; removed router file absent.
- `bun test src/application/outbox.test.ts src/application/tasks/tasks.integration.test.ts src/api/routes/kernel-tasks.test.ts src/orchestration/__tests__/symphony-conformance.test.ts scripts/ci.test.ts src/web/src/routes/application-adapters.test.ts src/api/__tests__/hono-single-api.test.ts src/cli/application-parity.test.ts src/tui/e2e/tui-screen-smoke.test.ts src/tui/e2e/tui-navigation.test.ts src/tui/e2e/tui-data-display.test.ts` passed: 174 tests, 0 failures.

### Architecture Grep Gates

Failed command:

```sh
rg 'openPglite|openProductDb' src/ --type ts | grep -v product-kernel/ | grep -v node_modules
```

Result: failed, 302 matching lines. Representative runtime matches include:

- `src/infrastructure/doctor/legacy-db.ts`
- `src/config/database.ts`
- `src/web/src/routes/api/repos/[id]/tree/+server.ts`
- `src/web/src/routes/orchestration/+page.server.ts`
- `src/web/src/routes/audit/export/+server.ts`
- `src/web/src/lib/server/db.ts`
- `src/web/src/routes/docs/global/+page.server.ts`
- `src/web/src/routes/projects/[id]/runs/[runId]/+page.server.ts`
- `src/web/src/routes/settings/secrets/+page.server.ts`

Failed command:

```sh
rg 'em\.(persist|flush|execute|getConnection)' src/web/ src/cli/ src/tui/ src/router/ src/api/ --type ts | grep -v test
```

Result: failed, 73 matching lines. Representative runtime matches include:

- `src/web/src/routes/projects/[id]/reports/+page.server.ts`
- `src/web/src/routes/docs/[id]/history/+page.server.ts`
- `src/web/src/routes/docs/[id]/edit/+page.server.ts`
- `src/web/src/routes/docs/[id]/+page.server.ts`
- `src/web/src/lib/server/orm-helpers.ts`
- `src/web/src/lib/server/audit.ts`
- `src/web/src/lib/server/reports.ts`
- `src/web/src/lib/server/orchestration.ts`
- `src/web/src/lib/server/project-connectors.ts`

Failed command:

```sh
rg 'ProductDb' src/ --type ts | grep -v product-kernel/
```

Result: failed, 405 matching lines. Representative runtime matches include:

- `src/services/tasks.ts`
- `src/services/artifacts.ts`
- `src/connectors/framework.ts`
- `src/services/runs.ts`
- `src/search/backend.ts`
- `src/search/query-service.ts`
- `src/orchestration/symphony/http-server.ts`
- `src/web/src/lib/server/settings.ts`
- `src/collab/server.ts`

Passed command:

```sh
rg 'PGlite' src/subscriptions/ --type ts
```

Result: no matches.

## CI Closure

Not run after the architecture grep failures:

- `bun run ci --tier=unit`
- `bun run ci --tier=integration`
- `FULCRUM_RUN_E2E=1 bun run ci --tier=e2e`
- `FULCRUM_RUN_E2E=1 bun run ci`

Reason: Plan 13 rollback says to stop and record the failing command when any targeted regression, architecture gate, or full CI command fails. Running later CI would not unblock Phase 10 while architecture gates are red.

## Phase 10 Status

Phase 10 remains blocked.

Required follow-up: create another Phase 9.5 gap-closure plan or route work back through Plans 09.5-09 through 09.5-12 to remove or formally narrow the remaining direct data-access patterns. Do not mark Phase 9.5 closed until architecture grep gates and `FULCRUM_RUN_E2E=1 bun run ci` both exit 0.

## Issues Encountered

- The executable architecture tests pass, but the Plan 13 raw grep acceptance gates are stricter than current allowlisted architecture tests.
- Existing dirty files were left unstaged: `AGENTS.md` and `src/db/migrations/.snapshot-postgres.json`.

## Self-Check: BLOCKED

- Summary file created.
- Targeted blocker regressions passed.
- Architecture grep gates failed.
- Full E2E CI closure was not reached.

---
*Phase: 09.5-architecture-data-layer-remediation-full-interface-fix*
*Completed: 2026-05-06*
