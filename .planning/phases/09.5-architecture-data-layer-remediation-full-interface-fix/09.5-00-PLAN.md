---
phase: 9.5
plan: 00
title: Validation Harness And Boundary RED Tests
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/architecture/boundary.test.ts
  - src/architecture/no-raw-sql.test.ts
  - src/architecture/migrations.test.ts
  - src/architecture/trpc-completeness.test.ts
  - src/architecture/security-regression.test.ts
  - scripts/ci.ts
autonomous: true
requirements:
  - ARCH-REMED-01..12
  - DATA-01..08
  - API-DEC-01..06
  - IFACE-01..08
  - E2E-01..08
requirements_addressed:
  - R-1
  - R-7
  - R-9
  - R-11
  - R-14
  - R-15
  - R-16
  - R-17
  - R-18
  - R-19
  - R-20
  - R-21
  - R-23
  - R-24
must_haves:
  truths:
    - D-47
    - D-48
    - D-49
    - D-58
    - D-59
    - D-60
    - D-61
    - D-62
    - D-72
    - D-73
    - D-74
    - D-75
    - D-76
    - D-77
    - D-78
---

<objective>
Create the RED validation harness that proves Phase 9.5 starts by failing on existing architectural violations and gives every later plan deterministic gates.
</objective>

<threat_model>
- T-00-01: False-positive verification lets direct DB/product-kernel access survive. Mitigation: grep-based tests fail until forbidden imports/calls are gone.
- T-00-02: Tier flags silently skip required tests. Mitigation: tier tests assert accepted flags and full `bun run ci` remains the closure gate.
- Block on: high.
</threat_model>

<tasks>
<task id="09.5-00-01" type="tdd">
  <read_first>
    <file>09.5-UAT-BASELINE.md</file>
    <file>09.5-SPEC.md</file>
    <file>scripts/ci.ts</file>
    <file>package.json</file>
  </read_first>
  <action>Write `src/architecture/boundary.test.ts` with failing tests that scan `apps/web/src/routes`, `apps/cli/src`, `apps/tui/src`, `apps/server/src/api`, `apps/server/src/router`, and `apps/server/src/runtime/trpc` for `openPglite`, `openProductDb`, `getProductDb`, `ProductDb`, and `from .*product-kernel` in non-test `.ts` files.</action>
  <acceptance_criteria>
    <criterion>`src/architecture/boundary.test.ts` contains `openPglite|openProductDb|getProductDb|ProductDb`.</criterion>
    <criterion>`bun test src/architecture/boundary.test.ts` fails before implementation removes current violations.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-00-02" type="tdd">
  <read_first>
    <file>09.5-SPEC.md</file>
    <file>apps/web/src/lib/server/db.ts</file>
    <file>src/product-kernel/db/migrate.ts</file>
  </read_first>
  <action>Write `src/architecture/no-raw-sql.test.ts` and `src/architecture/migrations.test.ts`. The raw-SQL test rejects `.execute(` and `.query(` outside tests and `src/product-kernel/db/migrations`. The migration test rejects runtime imports/calls of product-kernel `runMigrations()` outside explicit legacy upgrade tests.</action>
  <acceptance_criteria>
    <criterion>`src/architecture/no-raw-sql.test.ts` contains `.execute(` and `.query(` patterns.</criterion>
    <criterion>`src/architecture/migrations.test.ts` contains `runMigrations` and `product-kernel`.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-00-03" type="tdd">
  <read_first>
    <file>09.5-CROSS-PHASE-AUDIT.md</file>
    <file>apps/server/src/router/index.ts</file>
    <file>apps/server/src/api/hono.ts</file>
  </read_first>
  <action>Write `src/architecture/trpc-completeness.test.ts` requiring router modules for `notifications`, `search`, `orchestration`, `repos`, `connectors`, and `webhooks`; write `src/architecture/security-regression.test.ts` with RED cases named `CR-01 path traversal`, `CR-02 XSS`, `CR-03 deterministic IDs`, `CR-04 approval race`, and `WR-05 terminal workspace path validation`. Additionally write the following RED boundary/architecture tests covering missing requirements:
  - R-11: boundary test scanning `src/subscriptions/` for PGlite imports (must fail while direct PGlite usage exists)
  - R-14: boundary test for `getConnection().execute()` in `src/workers/` and `src/notifications/` (must fail while raw SQL exists)
  - R-16: architecture test asserting rate-limit middleware exists in `apps/server/src/runtime/` or `apps/server/src/api/`
  - R-17: architecture test asserting encrypt/decrypt functions exist in `src/application/crypto.ts` or equivalent
  - R-19: architecture test asserting `src/product-kernel/api/router.ts` should not exist (file must be removed)
  - R-21: architecture test asserting `fulcrum settings` CLI command exists in `apps/cli/src/`</action>
  <acceptance_criteria>
    <criterion>`src/architecture/trpc-completeness.test.ts` contains all six domain names.</criterion>
    <criterion>`src/architecture/security-regression.test.ts` contains `CR-01`, `CR-02`, `CR-03`, `CR-04`, and `WR-05`.</criterion>
    <criterion>R-11 boundary test scans `src/subscriptions/` for `PGlite` or `pglite` imports.</criterion>
    <criterion>R-14 boundary test scans `src/workers/` and `src/notifications/` for `getConnection().execute()`.</criterion>
    <criterion>R-16 architecture test asserts rate-limit middleware file exists.</criterion>
    <criterion>R-17 architecture test asserts encrypt/decrypt module exists.</criterion>
    <criterion>R-19 architecture test asserts `src/product-kernel/api/router.ts` does not exist.</criterion>
    <criterion>R-21 architecture test asserts `fulcrum settings` command registration exists.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-00-04" type="execute">
  <read_first>
    <file>scripts/ci.ts</file>
    <file>package.json</file>
  </read_first>
  <action>Extend CI argument parsing in `scripts/ci.ts` for `--tier=quick|unit|integration|e2e|full` and `--domain=application|web|cli|tui|api`. Note: R-24 tiered CI is ALREADY PARTIALLY IMPLEMENTED in `scripts/ci.ts` — this task should extend the existing implementation rather than creating from scratch. Quick runs typecheck only. Unit runs quick plus non-integration Bun tests. Integration runs unit plus DB/application integration. E2E runs Playwright/CLI/TUI E2E only when `FULCRUM_RUN_E2E=1`. Full remains current `bun run ci` behavior plus Phase 9.5 gates.</action>
  <acceptance_criteria>
    <criterion>`bun run ci --tier=quick` exits 0 and prints a quick/typecheck stage.</criterion>
    <criterion>`bun run ci --domain=application --tier=unit` exits 0 or fails only because Phase 9.5 RED tests are still red.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts src/architecture/migrations.test.ts src/architecture/trpc-completeness.test.ts src/architecture/security-regression.test.ts`
- `bun run ci --tier=quick`
</verification>

<success_criteria>
- Boundary, raw SQL, migration, router completeness, and security regression tests exist and fail for known current violations.
- CI tier/domain flags are accepted without breaking default `bun run ci`.
</success_criteria>
