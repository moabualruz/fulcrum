---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 11
title: Web And REST Boundary Cleanup Gap Closure
type: tdd
wave: 1
depends_on:
  - 09.5-03
  - 09.5-04
  - 09.5-06
files_modified:
  - src/web/src/routes/runs/[id]/+page.server.ts
  - src/web/src/routes/runs/[id]/artifacts/+page.server.ts
  - src/web/src/routes/api/data/export-csv/+server.ts
  - src/web/src/routes/api/data/import-csv/+server.ts
  - src/web/src/routes/api/skills/+server.ts
  - src/web/src/routes/api/bell/+server.ts
  - src/web/src/routes/search/+page.server.ts
  - src/web/src/routes/application-adapters.test.ts
  - src/api/__tests__/hono-single-api.test.ts
  - src/api/hono.ts
  - src/web/src/lib/server/**/*.ts
  - src/web/src/routes/**/*.ts
  - src/web/tests/**/*.ts
  - src/trpc/**/*.ts
  - src/api/routes/{saved-views,sprints,tasks}.ts
  - src/data/csv-export.ts
autonomous: true
gap_closure: true
requirements:
  - ARCH-REMED-01..12
  - DATA-01..08
  - API-DEC-01..06
  - IFACE-01..08
requirements_addressed:
  - R-1
  - R-2
  - R-6
  - R-9
  - R-11
  - R-13
  - R-15
  - R-19
must_haves:
  truths:
    - "Web loaders and SvelteKit API routes do not import or call `openProductDb`, `ProductDb`, or product-kernel runtime stores."
    - "Legacy `src/product-kernel/api/router.ts` remains absent and Hono is the sole public REST/OpenAPI entrypoint per D-01/D-66."
    - "Web route replacement preserves runs, artifacts, CSV import/export, skills, bell, and search behavior through application modules or tRPC/local caller adapters."
  artifacts:
    - path: "src/web/src/routes/application-adapters.test.ts"
      provides: "Boundary assertions for web routes."
      contains: "openProductDb"
    - path: "src/api/__tests__/hono-single-api.test.ts"
      provides: "R-19 single Hono API guard."
      contains: "product-kernel/api/router"
    - path: "src/api/hono.ts"
      provides: "Canonical REST/OpenAPI adapter registration."
      contains: "createPublicApiRouter"
  key_links:
    - from: "src/web/src/routes/**"
      to: "src/application/**"
      via: "application query/command imports or tRPC local caller"
      pattern: "src/application|createLocalCaller"
    - from: "src/api/hono.ts"
      to: "src/api/routes/kernel-*.ts"
      via: "single Hono app registration"
      pattern: "registerKernel"
---

<objective>
Remove residual ProductDb/product-kernel runtime usage from web and REST boundary files and lock R-19 single-Hono API closure.

Purpose: Phase 9.5 architecture gates still fail because web/runtime routes retain direct ProductDb paths and dual API proof is red/stale.
Output: TDD web/REST boundary cleanup with behavior-preserving adapter rewires.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-PATTERNS.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-VERIFICATION.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-03-SUMMARY.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-04-SUMMARY.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-06-SUMMARY.md

<interfaces>
Application modules available from prior plans:
```ts
src/application/runs/{queries,commands,types}.ts
src/application/artifacts/{queries,commands,types}.ts
src/application/search/{queries,types}.ts
src/application/notifications/{queries,types}.ts
src/application/settings/{queries,commands,types}.ts
```

Canonical API entrypoint:
```ts
export function createPublicApiRouter(deps?: PublicApiDeps): Hono;
export function createPublicApi(deps?: PublicApiDeps): Hono;
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 09.5-11-01: RED tests for web runtime ProductDb violations</name>
  <files>src/web/src/routes/application-adapters.test.ts</files>
  <read_first>
    <file>src/web/src/routes/application-adapters.test.ts</file>
    <file>src/web/src/routes/runs/[id]/+page.server.ts</file>
    <file>src/web/src/routes/runs/[id]/artifacts/+page.server.ts</file>
    <file>src/web/src/routes/api/data/export-csv/+server.ts</file>
    <file>src/web/src/routes/api/data/import-csv/+server.ts</file>
    <file>src/web/src/routes/api/skills/+server.ts</file>
    <file>src/web/src/routes/api/bell/+server.ts</file>
    <file>src/web/src/routes/search/+page.server.ts</file>
  </read_first>
  <action>RED: Extend `application-adapters.test.ts` to scan the exact runtime files listed in this plan and fail on `openProductDb`, `getProductDb`, `OrmProductDb`, `ProductDb`, `from .*product-kernel`, `.query(`, and `getConnection().execute`. Exclude test files only. Include a feature preservation table in test names: runs detail, run artifacts, CSV export/import, skills API, bell API, and search loader.</action>
  <verify>
    <automated>bun test src/web/src/routes/application-adapters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Test names mention all six runtime surfaces.</criterion>
    <criterion>Test fails before cleanup on at least `openProductDb` matches.</criterion>
    <criterion>No test fixture files are added to plan ownership.</criterion>
  </acceptance_criteria>
  <done>Web boundary violations have deterministic RED coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-11-02: GREEN web route rewires through application boundary</name>
  <files>src/web/src/routes/runs/[id]/+page.server.ts, src/web/src/routes/runs/[id]/artifacts/+page.server.ts, src/web/src/routes/api/data/export-csv/+server.ts, src/web/src/routes/api/data/import-csv/+server.ts, src/web/src/routes/api/skills/+server.ts, src/web/src/routes/api/bell/+server.ts, src/web/src/routes/search/+page.server.ts, src/web/src/routes/application-adapters.test.ts</files>
  <read_first>
    <file>src/application/runs/queries.ts</file>
    <file>src/application/runs/commands.ts</file>
    <file>src/application/artifacts/queries.ts</file>
    <file>src/application/search/queries.ts</file>
    <file>src/application/notifications/queries.ts</file>
    <file>src/web/src/lib/server/dashboard.ts</file>
    <file>src/web/src/lib/product-queries.ts</file>
  </read_first>
  <action>GREEN: Rewire the owned web runtime files to application query/command modules or existing tRPC/local caller adapters. Preserve response shapes and status codes. For CSV import/export, use application commands/queries for data access and keep serialization/parsing inside route adapter only. For bell API, call application notification count query, not ProductDb. For search loader, call application search query/read model, not raw ProductDb. Do not modify product-kernel stores.</action>
  <verify>
    <automated>bun test src/web/src/routes/application-adapters.test.ts</automated>
    <automated>! rg 'openProductDb|getProductDb|OrmProductDb|ProductDb|from .*product-kernel' src/web/src/routes/runs/[id]/+page.server.ts src/web/src/routes/runs/[id]/artifacts/+page.server.ts src/web/src/routes/api/data/export-csv/+server.ts src/web/src/routes/api/data/import-csv/+server.ts src/web/src/routes/api/skills/+server.ts src/web/src/routes/api/bell/+server.ts src/web/src/routes/search/+page.server.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Owned runtime files contain zero `openProductDb`, `ProductDb`, and product-kernel imports.</criterion>
    <criterion>Owned runtime files contain zero `.query(` and `getConnection().execute` data access.</criterion>
    <criterion>Existing route tests for affected surfaces pass or are updated only to use application-backed fixtures.</criterion>
  </acceptance_criteria>
  <done>Owned web runtime routes are thin adapters over application data operations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-11-03: RED/GREEN R-19 single Hono API proof</name>
  <files>src/api/__tests__/hono-single-api.test.ts, src/api/hono.ts</files>
  <read_first>
    <file>src/api/__tests__/hono-single-api.test.ts</file>
    <file>src/api/hono.ts</file>
    <file>src/api/routes/kernel-tasks.ts</file>
    <file>src/api/routes/kernel-sprints.ts</file>
    <file>src/api/routes/kernel-notifications.ts</file>
    <file>src/api/routes/kernel-audit.ts</file>
  </read_first>
  <action>RED/GREEN: Ensure `hono-single-api.test.ts` asserts `src/product-kernel/api/router.ts` does not exist, `src/api/hono.ts` does not import `product-kernel/api/router`, and canonical kernel routes are registered through application-backed `src/api/routes/kernel-*.ts`. If `src/product-kernel/api/router.ts` is already absent, keep the test green and update stale comments in `src/api/hono.ts` that say runtime deps are backed by ProductDb/services so they describe application deps instead. Do not recreate the deleted router.</action>
  <verify>
    <automated>bun test src/api/__tests__/hono-single-api.test.ts</automated>
    <automated>test ! -f src/product-kernel/api/router.ts</automated>
    <automated>! rg 'ProductDb routes are mounted|backed by ProductDb' src/api/hono.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`test ! -f src/product-kernel/api/router.ts` exits 0.</criterion>
    <criterion>`src/api/hono.ts` comments and imports reference application-backed routes, not ProductDb-backed runtime routes.</criterion>
    <criterion>`bun test src/api/__tests__/hono-single-api.test.ts` exits 0.</criterion>
  </acceptance_criteria>
  <done>R-19 dual API merge remains closed by an executable guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-11-04: RED/GREEN complete web, REST, tRPC, and test-fixture boundary inventory</name>
  <files>src/web/src/lib/server/**/*.ts, src/web/src/routes/**/*.ts, src/web/tests/**/*.ts, src/trpc/**/*.ts, src/api/routes/{saved-views,sprints,tasks}.ts, src/data/csv-export.ts, src/web/src/routes/application-adapters.test.ts, src/api/__tests__/hono-single-api.test.ts</files>
  <read_first>
    <file>.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-VERIFICATION.md</file>
    <file>src/architecture/boundary.test.ts</file>
    <file>src/architecture/no-raw-sql.test.ts</file>
    <file>src/trpc/context.ts</file>
    <file>src/trpc/routers/orchestration.ts</file>
    <file>src/web/tests/e2e/fixtures.ts</file>
  </read_first>
  <action>RED: Generate the current web/REST/tRPC/test-fixture violation inventory with `rg -l 'openPglite|openProductDb|getProductDb|ProductDb|from\\s+[\"'\\'''][^\"'\\''']*product-kernel[^\"'\\''']*[\"'\\''']' src --glob '*.ts' | rg -v 'src/product-kernel/'` and `rg -l 'em\\.(persist|flush|execute|getConnection)|getConnection\\(\\)\\.execute' src/web src/api src/trpc --glob '*.ts' | rg -v '\\.test\\.ts$'`. Copy every web, REST, tRPC, and web-test-fixture path from that inventory into the boundary test table before GREEN work. GREEN: migrate runtime paths to application modules/local callers; migrate fixtures to MikroORM application fixtures or explicit `src/test-support/product-db-legacy.ts` compatibility wrappers; remove `ProductDb` types from tRPC public adapters by using application deps; and update tests so any remaining legacy helper is named, centralized, and excluded only by an exact allowlist with rationale. Do not leave per-route/per-test ad hoc ProductDb opens.</action>
  <verify>
    <automated>bun test src/web/src/routes/application-adapters.test.ts src/api/__tests__/hono-single-api.test.ts src/trpc/routers/orchestration.test.ts src/trpc/rest-api.test.ts</automated>
    <automated>! rg 'openPglite|openProductDb|getProductDb|ProductDb|from .*product-kernel' src/web src/trpc src/api/routes src/data --type ts | rg -v 'src/test-support/product-db-legacy.ts'</automated>
    <automated>! rg 'em\\.(persist|flush|execute|getConnection)|getConnection\\(\\)\\.execute' src/web src/api src/trpc --type ts | rg -v '\\.test\\.ts$' | rg -v 'src/web/src/lib/server/em.ts'</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Every current web/REST/tRPC/test-fixture match from the verification grep has a migrated path or a centralized exact allowlist entry with rationale.</criterion>
    <criterion>No SvelteKit route, web server helper, tRPC router/context, REST route, or web E2E fixture opens ProductDb/PGlite directly.</criterion>
    <criterion>Boundary tests fail on newly introduced ad hoc `ProductDb`, `openPglite`, `openProductDb`, product-kernel imports, or raw connection execution in interface paths.</criterion>
  </acceptance_criteria>
  <done>All web, REST, tRPC, and web-test-fixture categories called out by verification are owned and either migrated or explicitly centralized as legacy compatibility.</done>
</task>
</tasks>

<rollback>
If a route/helper rewire breaks response shape, revert that route file and keep the expanded boundary test failing with the exact path rather than deleting the guard. If centralized legacy fixture support blocks test migration, keep the wrapper in `src/test-support/product-db-legacy.ts` only and remove per-file opens before proceeding. If tRPC ProductDb removal changes public procedure contracts, rollback the adapter internals only; exported schemas and client-visible response fields must stay unchanged.
</rollback>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> SvelteKit server routes | User/session input reaches web adapters. |
| REST client -> Hono router | Public API requests reach canonical API adapter. |
| Web route -> application module | Adapter boundary must not bypass application auth/data rules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Elevation of Privilege | web runtime routes | mitigate | Remove direct ProductDb/product-kernel access so application auth scopes apply. |
| T-11-02 | Tampering | CSV import route | mitigate | Parse CSV in adapter, write through application commands only. |
| T-11-03 | Information Disclosure | bell/search/routes | mitigate | Read via application queries with org/project context. |
| T-11-04 | Spoofing | REST API entrypoints | mitigate | Keep one Hono app and no legacy product-kernel router. |
</threat_model>

<verification>
- `bun test src/web/src/routes/application-adapters.test.ts`
- `bun test src/api/__tests__/hono-single-api.test.ts`
- `test ! -f src/product-kernel/api/router.ts`
- `rg 'openProductDb|openPglite|ProductDb' src/web/src/routes src/api --type ts | grep -v test | grep -v product-kernel/` returns zero matches for owned runtime files.
- `bun run ci --tier=integration --domain=web`
- `bun run ci --tier=integration --domain=api`
</verification>

<success_criteria>
Owned web/runtime and REST files no longer participate in ProductDb/product-kernel boundary violations, and R-19 single-Hono API proof is green.
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-11-SUMMARY.md`.
</output>
