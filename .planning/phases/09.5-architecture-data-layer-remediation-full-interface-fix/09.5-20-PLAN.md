---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 20
type: execute
gap_closure: true
wave: 2
depends_on: ["09.5-19"]
files_modified:
  - src/application/auth/session-context.ts
  - src/application/audit/commands.ts
  - src/application/audit/queries.ts
  - src/application/audit/types.ts
  - src/application/memory/commands.ts
  - src/application/memory/queries.ts
  - src/application/memory/types.ts
  - src/application/routing.ts
  - src/server/trpc/routers/auth.ts
  - src/server/trpc/routers/audit.ts
  - src/server/trpc/routers/memory.ts
  - src/server/trpc/routers/routing.ts
autonomous: true
requirements: [R-1, R-5, R-9, R-13, R-20, R-24]
must_haves:
  truths:
    - "Auth, audit, memory, and routing tRPC/server router procedures delegate to application commands/queries."
    - "Routers do not import entities, repositories, db modules, or call raw EntityManager methods."
    - "Authorization/session context used by routers is application-owned, not duplicated per interface."
  artifacts:
    - path: "src/application/auth/session-context.ts"
      provides: "Shared session/app-context lookup for interface callers"
      exports: ["resolveApplicationSessionContext"]
    - path: "src/server/trpc/routers/audit.ts"
      provides: "Thin audit adapter over application audit commands/queries"
    - path: "src/server/trpc/routers/routing.ts"
      provides: "Thin routing adapter over application routing commands/queries"
  key_links:
    - from: "src/server/trpc/routers/audit.ts"
      to: "src/application/audit/queries.ts"
      via: "procedure delegate"
      pattern: "audit.*queries"
    - from: "src/server/trpc/routers/auth.ts"
      to: "src/application/auth/session-context.ts"
      via: "shared session context"
      pattern: "resolveApplicationSessionContext"
---

<objective>
Consolidate high-impact server tRPC routers for auth, audit, memory, and routing behind application modules.

Purpose: close R-5/R-20 residual router business logic while preserving procedure names, permissions, input/output schemas, and tRPC error behavior.
Output: application-backed router procedures matching the `src/server/trpc/routers/tasks.ts` pattern.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-INTERFACE-ENCAPSULATION-INTEGRATION-RESEARCH.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-ENCAPSULATION-DEPENDENCIES-RESEARCH.md
@src/server/trpc/routers/tasks.ts
@src/application/tasks/commands.ts
@src/application/tasks/queries.ts
@src/application/error-mapping.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move auth session-context lookup into application</name>
  <files>src/application/auth/session-context.ts, src/server/trpc/routers/auth.ts</files>
  <read_first>
    - src/server/trpc/routers/auth.ts
    - src/cli/local-caller.ts
    - src/tui/index.ts
    - src/application/error-mapping.ts
  </read_first>
  <behavior>
    - Existing `auth.whoami` output remains unchanged, including reduced-metadata fallback from Plan 18.
    - Session/org/user lookup logic is exported from application and can be reused by CLI/TUI in Plan 24.
  </behavior>
  <action>Create `src/application/auth/session-context.ts` with typed functions for resolving active session/app context from EntityManager and existing context values. Update `src/server/trpc/routers/auth.ts` so procedure body validates/authenticates context, delegates lookup/enrichment to application, and maps `AppError` through existing error mapping. Keep direct `ctx.em` access limited to passing the EntityManager into application; no entity imports in router.</action>
  <verify>
    <automated>bun test tests/trpc/auth.test.ts src/cli/commands/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>`auth.ts` has no `em.find`, `em.findOne`, `em.create`, `em.persist`, `em.flush`, entity imports, repository imports, or business branching beyond adapter mapping.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move audit and memory router behavior into application modules</name>
  <files>src/application/audit/commands.ts, src/application/audit/queries.ts, src/application/audit/types.ts, src/application/memory/commands.ts, src/application/memory/queries.ts, src/application/memory/types.ts, src/server/trpc/routers/audit.ts, src/server/trpc/routers/memory.ts</files>
  <read_first>
    - src/server/trpc/routers/audit.ts
    - src/server/trpc/routers/memory.ts
    - src/application/audit/queries.ts
    - src/application/memory/queries.ts
  </read_first>
  <behavior>
    - Audit query/export/retention semantics match current tests and CSV/export limits.
    - Memory list/search/create/update/delete procedures share application DTOs and enforce org/user context in application.
  </behavior>
  <action>Move filter construction, export formatting inputs, retention policy lookup, memory CRUD/query logic, and tenant checks into `src/application/audit/*` and `src/application/memory/*`. Routers must become adapters: `permissionedProcedure`, input schema, output schema, call application, map error. Do not introduce raw SQL; use repository/QueryBuilder inside application queries where aggregation is needed per D-15.</action>
  <verify>
    <automated>bun test src/application/audit/audit.integration.test.ts src/application/memory src/server/trpc/routers/__tests__ tests/trpc/router.test.ts</automated>
  </verify>
  <acceptance_criteria>`rg -n "ctx\\.em|em\\.(find|findOne|create|persist|flush|transactional)|getRepository\\(" src/server/trpc/routers/audit.ts src/server/trpc/routers/memory.ts` returns zero matches except passing `ctx.em` once into an application call if the architecture gate allows that exact adapter handoff.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Move routing router/repository work behind application routing commands</name>
  <files>src/application/routing.ts, src/server/trpc/routers/routing.ts</files>
  <read_first>
    - src/application/routing.ts
    - src/server/trpc/routers/routing.ts
    - src/router/conflict-detector.ts
    - src/router/no-match-prompt.ts
    - src/router/telemetry.ts
  </read_first>
  <behavior>
    - Routing rules, conflict detection, learned no-match prompt behavior, and telemetry persistence are invoked through application functions.
    - Application routing no longer self-opens PGlite/MikroORM fallback for interface paths; it accepts injected EntityManager/repositories.
  </behavior>
  <action>Reshape `src/application/routing.ts` into command/query exports consumed by `src/server/trpc/routers/routing.ts`. Remove router-side repository/entity work and application-layer self-opening DB fallback from runtime invocation path. Keep any legacy fallback behind an explicit test-only or bootstrap-only function with exact architecture allowlist rationale.</action>
  <verify>
    <automated>bun test src/server/trpc/routers/__tests__/routing.test.ts src/router/routing-service.test.ts src/architecture/boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>`src/server/trpc/routers/routing.ts` matches tasks-router adapter posture; no `src/router/**` runtime file imports DB entities/repositories or directly persists routing data.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
| --- | --- |
| tRPC -> application | Authenticated request context invokes domain behavior. |
| router -> database | Residual direct DB access can bypass app-layer auth/tenant rules. |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-09.5-20-01 | Elevation of privilege | auth/audit/memory/routing routers | mitigate | Application entrypoints enforce org/user context; routers only build transport context. |
| T-09.5-20-02 | Information disclosure | audit/memory queries | mitigate | Move filters and tenant scoping into application queries with integration tests. |
</threat_model>

<verification>
Run `bun test src/architecture/boundary.test.ts src/server/trpc/routers/__tests__ tests/trpc/auth.test.ts tests/trpc/router.test.ts`.
</verification>

<success_criteria>
Auth, audit, memory, and routing router bodies are thin adapters and pass new Plan 19 gates.
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-20-SUMMARY.md`.
</output>
