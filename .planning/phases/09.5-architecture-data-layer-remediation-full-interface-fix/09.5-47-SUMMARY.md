# Phase 09.5 Plan 47 Summary

## Outcome

Moved webhook tRPC persistence into `src/application/webhooks` and rewired `src/trpc/routers/webhooks.ts` as a thin adapter over application commands/queries.

## Changes

- Added application webhook DTO/input types in `src/application/webhooks/types.ts`.
- Added ORM-backed webhook read functions in `src/application/webhooks/queries.ts`:
  - `listWebhooks`
  - `getWebhook`
  - `listWebhookDeliveries`
  - `getWebhookDelivery`
- Extended `src/application/webhooks/commands.ts` with ORM-backed persistence:
  - `createWebhook`
  - `updateWebhook`
  - `deleteWebhook`
- Replaced direct router entity access with application calls plus `requireTrpcEntityManager(ctx)`.
- Preserved feature-gate behavior through `outbound-webhooks`.
- Preserved redacted webhook DTO output: `secret: "****"`.
- Added integration coverage for application CRUD/delivery reads and router adapter behavior.

## Verification

```sh
bun test src/application/webhooks/webhooks.integration.test.ts src/trpc/routers/webhooks.test.ts -- -x
! rg -n "ctx\\.em|em\\.find|em\\.findOne|em\\.create|em\\.persist|em\\.flush|em\\.transactional|getRepository\\(" src/trpc/routers/webhooks.ts
bun run lint
```

All commands passed before summary creation.
