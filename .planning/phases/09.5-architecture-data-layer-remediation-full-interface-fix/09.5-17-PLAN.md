---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 17
title: Raw EntityManager Sql Closure
type: tdd
wave: 3
depends_on:
  - 09.5-14
  - 09.5-16
files_modified:
  - apps/web/src/lib/server/**/*.ts
  - apps/web/src/routes/**/*.ts
  - apps/server/src/api/**/*.ts
  - apps/server/src/trpc/**/*.ts
  - apps/server/src/router/**/*.ts
  - src/application/**/*.ts
  - src/db/entities/**/*.ts
  - src/db/repositories/**/*.ts
  - src/architecture/no-raw-sql.test.ts
autonomous: true
gap_closure: true
requirements:
  - R-1
  - R-2
  - R-5
  - R-9
  - R-10
must_haves:
  truths:
    - "Interface/application paths do not call `em.getConnection()`, `.execute(`, or `.query(` for business data access."
    - "Complex reads use MikroORM QueryBuilder, entity mappings, repositories, or narrowly-scoped application read-model modules."
    - "Architecture tests enforce the same raw SQL contract as Plan 13 grep gates."
---

<objective>
Remove raw EntityManager connection and SQL execution from web/API/tRPC/router/application paths so the second Plan 13 grep gate can pass.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-PATTERNS.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-GAP-CLOSURE-RESEARCH.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-13-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 09.5-17-01: RED raw EM inventory gate</name>
  <files>src/architecture/no-raw-sql.test.ts</files>
  <read_first>
    <file>src/architecture/no-raw-sql.test.ts</file>
    <file>apps/web/src/lib/server/orm-helpers.ts</file>
    <file>apps/web/src/lib/server/em.ts</file>
    <file>apps/web/src/routes/docs/[id]/+page.server.ts</file>
    <file>apps/web/src/routes/projects/[id]/reports/+page.server.ts</file>
  </read_first>
  <action>Replace the current raw SQL allowlist with a RED gate matching Plan 13: `em\\.(persist|flush|execute|getConnection)`, `getConnection().execute`, `.execute(`, and `.query(` under `apps/web`, `apps/cli/src`, `apps/tui/src`, `apps/server/src/router`, and `apps/server/src/api`, excluding only test/spec files and migration files. The failure must name `apps/web/src/lib/server/orm-helpers.ts`, `apps/web/src/lib/server/em.ts`, `apps/web/src/lib/server/reports.ts`, and docs route files.</action>
  <verify>
    <automated>bun test src/architecture/no-raw-sql.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/architecture/no-raw-sql.test.ts` contains no permanent allowlist for web server helpers.</criterion>
    <criterion>RED failure names at least four current raw EM files.</criterion>
    <criterion>Test ignores migrations and tests only, not runtime adapter directories.</criterion>
  </acceptance_criteria>
  <done>Raw SQL violations are gate-enforced with Plan 13 semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-17-02: GREEN entity and repository coverage for raw helpers</name>
  <files>src/db/entities/**/*.ts, src/db/repositories/**/*.ts, src/application/**/*.ts, apps/web/src/lib/server/orm-helpers.ts</files>
  <read_first>
    <file>apps/web/src/lib/server/orm-helpers.ts</file>
    <file>src/db/entities/platform/DomainEventOutbox.ts</file>
    <file>src/db/entities/tasks/Task.ts</file>
    <file>src/db/db.module.ts</file>
    <file>src/application/outbox.ts</file>
  </read_first>
  <action>Add missing entity/repository/read-model methods needed to replace `appendEventOrm`, `indexSearchDocumentOrm`, `enqueueJobOrm`, report aggregations, docs version/history reads, audit reads, and custom-field/saved-view/project-connector operations. Use `em.createQueryBuilder(Entity, alias)`, `em.find`, `em.findOne`, `em.persist`, and repository methods. Delete or rewrite helper functions that call `em.getConnection().execute()`.</action>
  <verify>
    <automated>bun test src/application/outbox.test.ts apps/web/src/lib/server/reports.test.ts apps/web/src/lib/server/doc-versions.test.ts apps/web/src/lib/server/audit.test.ts</automated>
    <automated>! rg 'em\\.getConnection\\(|getConnection\\(\\)\\.execute|\\.query\\(|\\.execute\\(' apps/web/src/lib/server src/application src/db/repositories --type ts | rg -v '\\.(test|spec)\\.ts$'</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`apps/web/src/lib/server/orm-helpers.ts` has no `getConnection` or `.execute(` calls, or the file is deleted.</criterion>
    <criterion>Application/repository raw execution grep returns no matches outside migrations/tests.</criterion>
    <criterion>Outbox/report/docs/audit representative tests pass.</criterion>
  </acceptance_criteria>
  <done>Shared raw helper functionality is replaced by entities, repositories, and QueryBuilder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-17-03: GREEN route-level raw EM cleanup</name>
  <files>apps/web/src/routes/**/*.ts, apps/server/src/api/**/*.ts, apps/server/src/trpc/**/*.ts, apps/server/src/router/**/*.ts, src/architecture/no-raw-sql.test.ts</files>
  <read_first>
    <file>apps/web/src/routes/docs/[id]/+page.server.ts</file>
    <file>apps/web/src/routes/docs/[id]/edit/+page.server.ts</file>
    <file>apps/web/src/routes/docs/[id]/history/+page.server.ts</file>
    <file>apps/web/src/routes/projects/[id]/reports/+page.server.ts</file>
    <file>apps/server/src/router/telemetry.ts</file>
  </read_first>
  <action>Replace remaining route/router raw EM calls with application commands/queries or QueryBuilder-backed repository calls. Preserve docs page/edit/history response keys, project reports rows, router telemetry behavior, and API error mapping. Do not introduce raw SQL through helper functions with different names.</action>
  <verify>
    <automated>bun test apps/web/src/routes/application-adapters.test.ts apps/server/src/router/adapter-application.test.ts src/architecture/no-raw-sql.test.ts</automated>
    <automated>! rg 'em\\.(persist|flush|execute|getConnection)|getConnection\\(\\)\\.execute|\\.query\\(|\\.execute\\(' apps/web apps/cli/src apps/tui/src apps/server/src/router apps/server/src/api --type ts | rg -v '\\.(test|spec)\\.ts$' | rg -v 'src/db/migrations|src/product-kernel/'</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Plan 13 raw EM grep returns no matches.</criterion>
    <criterion>No new helper wraps raw `.execute(` or `.query(` in interface/application paths.</criterion>
    <criterion>Route adapter and router application tests pass.</criterion>
  </acceptance_criteria>
  <done>Runtime interface/application raw SQL gate is green.</done>
</task>
</tasks>

<rollback>
If a QueryBuilder replacement cannot express an existing query safely, add the missing entity mapping first. Do not hide raw SQL in a renamed helper or architecture allowlist.
</rollback>

<threat_model>
| Threat ID | Category | Component | Mitigation |
| --- | --- | --- | --- |
| T-17-01 | Tampering | raw SQL rewrites | Use tests around each query result shape before replacing SQL. |
| T-17-02 | Information Disclosure | report/docs queries | Preserve org/project filters in QueryBuilder conditions. |
| T-17-03 | Denial of Service | aggregations | Keep indexes and avoid N+1 loops when replacing aggregate SQL. |
</threat_model>

<verification>
- `bun test src/architecture/no-raw-sql.test.ts`
- `bun test apps/web/src/routes/application-adapters.test.ts apps/server/src/router/adapter-application.test.ts`
- `! rg 'em\\.(persist|flush|execute|getConnection)|getConnection\\(\\)\\.execute|\\.query\\(|\\.execute\\(' apps/web apps/cli/src apps/tui/src apps/server/src/router apps/server/src/api --type ts | rg -v '\\.(test|spec)\\.ts$' | rg -v 'src/db/migrations|src/product-kernel/'`
</verification>

<success_criteria>
Plan 13 raw EntityManager/SQL grep gate returns no matches and representative behavior tests pass.
</success_criteria>
