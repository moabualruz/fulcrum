---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 10
subsystem: testing
tags: [ci, e2e, vitest, symphony, web]

requires:
  - phase: 09.5-08
    provides: Phase 9.5 verification blockers for SYM-18, cmdk vitest, and WR-03 CI semantics
provides:
  - SYM-18 startup sweep conformance proof using valid org-root workspaces
  - Portable command palette route test path resolution under Vitest
  - Explicit web:e2e:full CI gate metadata and skip output
affects:
  - src/orchestration/__tests__/symphony-conformance.test.ts
  - apps/web/tests/vitest/cmdk-palette.test.ts
  - scripts/ci.ts
  - scripts/ci.test.ts

tech-stack:
  added: []
  patterns:
    - TDD gate tests for CI step metadata
    - import.meta.url plus fileURLToPath path resolution for Vitest portability
    - Explicit skipReason metadata for gated local CI stages

key-files:
  created:
    - .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-10-SUMMARY.md
  modified:
    - src/orchestration/__tests__/symphony-conformance.test.ts
    - apps/web/tests/vitest/cmdk-palette.test.ts
    - scripts/ci.ts
    - scripts/ci.test.ts

key-decisions:
  - "Keep FULCRUM_RUN_E2E=1 as the Phase 9.5 full-E2E closure switch, but always expose web:e2e:full in CI step metadata."
  - "Use fileURLToPath(import.meta.url) for web Vitest filesystem roots instead of Bun-only import.meta.dir."

patterns-established:
  - "CI gated steps may remain skipped locally only when the step name and skipReason are visible and test-covered."

requirements-completed:
  - ARCH-REMED-01..12
  - E2E-01..08

duration: 7m03s
completed: 2026-05-06
---

# Phase 09.5 Plan 10: CI Conformance And Web Test Gate Gap Closure Summary

SYM-18 conformance, command-palette Vitest portability, and full-E2E CI gate visibility are covered by focused tests.

## Performance

- **Duration:** 7m03s
- **Started:** 2026-05-06T20:38:27Z
- **Completed:** 2026-05-06T20:45:30Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- Tightened the SYM-18 startup sweep test to use valid org-root scratch workspaces and assert dry-run counts, `before_remove`, deletion, and running-run preservation.
- Replaced Bun-only `import.meta.dir` in the web command-palette route test with `fileURLToPath(import.meta.url)`.
- Added WR-03 CI tests and `buildAllSteps()` so `web:e2e:full` is always visible, with explicit `skipReason` unless `FULCRUM_RUN_E2E=1`.

## Task Commits

1. **Task 09.5-10-01: SYM-18 startup cleanup sweep** - `5faa2a08` (test)
2. **Task 09.5-10-02: Portable command palette Vitest path** - `45ace729` (fix)
3. **Task 09.5-10-03: Explicit full E2E CI semantics** - `dcf2cba1` (fix)

## Files Created/Modified

- `src/orchestration/__tests__/symphony-conformance.test.ts` - Uses real org-root scratch workspace paths for SYM-18 cleanup proof.
- `apps/web/tests/vitest/cmdk-palette.test.ts` - Uses `fileURLToPath(import.meta.url)` for portable route-root resolution.
- `scripts/ci.ts` - Exports `buildAllSteps()` and explicit `skipReason` handling for `web:e2e:full`.
- `scripts/ci.test.ts` - Adds WR-03 tests for full-E2E inclusion and explicit skip metadata.
- `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-10-SUMMARY.md` - Execution record.

## Verification Evidence

| Command | Result | Notes |
| --- | --- | --- |
| `bun test src/orchestration/__tests__/symphony-conformance.test.ts --test-name-pattern "Startup cleanup sweep"` | PASS | RED before edit failed on invalid `/tmp` paths; tightened test passes with valid org root. |
| `bun test src/orchestration/__tests__/symphony-conformance.test.ts` | PASS | 85 passed, 0 failed. |
| `cd apps/web && bun run web:test -- tests/vitest/cmdk-palette.test.ts` | PASS | 5 passed, 0 failed. |
| `bun test scripts/ci.test.ts` | PASS | 21 passed, 0 failed. |
| `rg 'import\.meta\.dir' apps/web/tests/vitest/cmdk-palette.test.ts` | PASS | Zero matches. |
| `FULCRUM_RUN_E2E=1 bun run ci --tier=e2e --domain=web` | BLOCKED | Current worktree typecheck fails in concurrent out-of-scope edits before web steps run. Earlier Task 02 run reached and passed `web:test`, then failed in existing Playwright smoke startup. |
| `bun run ci --tier=unit` | BLOCKED | Reached and passed `symphony:conformance`, then failed on application task-scope tests owned by another gap plan. |

## Decisions Made

- Full E2E remains controlled by `FULCRUM_RUN_E2E=1`; without it, CI prints and exposes a skip reason instead of silently omitting the gate.
- No workspace runtime change was needed for SYM-18 after the conformance test used valid workspace-root paths; existing `sweepTerminalWorkspaces()` already validates paths and preserves running workspaces.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Test Fixture Bug] Fixed SYM-18 conformance fixture paths**
- **Found during:** Task 09.5-10-01
- **Issue:** Existing test seeded `/tmp/sym-sweep-*` paths outside the org workspace root, causing security validation to fail before testing cleanup behavior.
- **Fix:** Seeded scratch paths under `<scratchRoot>/<orgId>/...` and asserted dry-run, deletion, hook calls, and running workspace preservation.
- **Files modified:** `src/orchestration/__tests__/symphony-conformance.test.ts`
- **Verification:** `bun test src/orchestration/__tests__/symphony-conformance.test.ts`
- **Committed in:** `5faa2a08`

**2. [Rule 3 - Blocking] Documented concurrent out-of-scope CI blockers**
- **Found during:** Task 09.5-10-01 and Task 09.5-10-03
- **Issue:** Full tier commands became blocked by concurrent dirty files outside this plan, including task-scope application tests and typecheck failures in search/doctor files.
- **Fix:** Did not modify out-of-scope files; verified owned files with focused commands and recorded blockers.
- **Files modified:** None for the blocker.
- **Verification:** Focused commands above pass.
- **Committed in:** Summary only.

**Total deviations:** 2 auto-handled.

## Issues Encountered

- `bun run ci --tier=unit` now reaches and passes `symphony:conformance`, but fails later in `application:unit` on CR-02/CR-03/WR-02 tests outside this plan.
- `FULCRUM_RUN_E2E=1 bun run ci --tier=e2e --domain=web` passed `web:test` after the path fix in the Task 02 run, then failed in existing Playwright smoke startup. A later rerun was blocked earlier by concurrent typecheck edits outside this plan.

## User Setup Required

None.

## Known Stubs

None found in plan-owned files.

## Threat Flags

None. Changes add test filesystem coverage and CI metadata only; no new network endpoint, auth path, schema boundary, or privileged file access path was introduced.

## Next Phase Readiness

Plan-owned blockers are closed. Phase 9.5 full closure still depends on other gap plans finishing their concurrent application/search/CLI changes and resolving the remaining Playwright smoke startup failure.

## Self-Check: PASSED

- Summary file exists at `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-10-SUMMARY.md`.
- Task commits exist: `5faa2a08`, `45ace729`, `dcf2cba1`.
- Focused verification commands for conformance, cmdk Vitest, and CI tests pass.
- `AGENTS.md` remains unstaged and untouched by this plan.

---
*Phase: 09.5-architecture-data-layer-remediation-full-interface-fix*
*Completed: 2026-05-06*
