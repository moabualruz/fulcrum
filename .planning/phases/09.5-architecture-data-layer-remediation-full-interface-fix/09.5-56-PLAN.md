---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 56
type: execute
gap_closure: true
wave: 7
depends_on: ["09.5-52"]
files_modified:
  - src/orchestration/symphony/workflow-runtime.ts
  - src/orchestration/symphony/dispatch.ts
  - src/orchestration/__tests__/symphony-conformance.test.ts
autonomous: true
requirements: [ARCH-REMED-01, IFACE-01, IFACE-03, E2E-08]
---

# Plan 56: Symphony Config and Preflight Conformance

## Objective

Close the latest OpenAI Symphony SPEC config/preflight slice identified by subagents and local `vendor/openai-symphony/SPEC.md`: sections 5.3, 6.1, and 6.3.

## Research Inputs

- `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-SYMPHONY-LATEST-SPEC-RESEARCH.md`
- `vendor/openai-symphony/SPEC.md`
- `src/orchestration/symphony/workflow-runtime.ts`
- `src/orchestration/symphony/dispatch.ts`
- `src/orchestration/__tests__/symphony-conformance.test.ts`

## Tasks

### Task 1: Add failing conformance assertions

Add tests for:

- Linear tracker defaults: endpoint `https://api.linear.app/graphql`, active states `["Todo", "In Progress"]`, terminal states `["Closed", "Cancelled", "Canceled", "Duplicate", "Done"]`.
- Workspace root default: `<system-temp>/symphony_workspaces`.
- Relative workspace root resolves relative to the `WORKFLOW.md` directory.
- `hooks.timeout_ms` default: `60000`.
- Invalid `agent.max_concurrent_agents_by_state` entries are ignored; valid state keys normalize lowercase.
- Dispatch preflight validates `tracker.kind`, `tracker.api_key`, Linear `tracker.project_slug`, and non-empty `codex.command`.

### Task 2: Implement runtime config normalization

Update `loadWorkflowRuntime` normalization to apply the tested defaults and sanitize per-state concurrency without weakening existing env and `~` expansion behavior.

### Task 3: Implement preflight checks

Update `validateRuntimeConfig` in `dispatch.ts` so legacy numeric checks stay compatible, while Symphony runtime config preflight enforces SPEC-required tracker/Codex fields when those sections are present.

## Verification

Run:

```bash
bun test src/orchestration/__tests__/symphony-conformance.test.ts -- -x
bun run lint
bun run ci --tier=unit
```

## Success Criteria

Conformance tests prove the latest SPEC config/preflight behavior; no interface/business-data boundary code changes are mixed into this slice.

