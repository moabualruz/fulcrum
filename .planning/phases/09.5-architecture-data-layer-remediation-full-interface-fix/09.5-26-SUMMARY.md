---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 26
status: completed
gap_closure: true
completed_at: 2026-05-07
---

# Plan 09.5-26 Summary

## Outcome

Project activity, calendar, Gantt, repos, and runs routes now call application-owned queries and commands. The route files no longer open databases, call raw SQL, import product-kernel compatibility helpers, or import web server data helpers for these workflows.

## Changes

- Added request-to-application scope glue in `src/web/src/lib/server/application-scope.ts`.
- Added default org resolution in `src/application/auth/org-context.ts`.
- Added project read/action boundaries:
  - `src/application/projects/queries.ts`
  - `src/application/projects/commands.ts`
- Extended repo and run application modules for project route needs:
  - project repo cards, add repo, link repo
  - project runs list/detail, cancel, retry
- Converted these route files to adapter-only delegates:
  - `src/web/src/routes/projects/[id]/activity/+page.server.ts`
  - `src/web/src/routes/projects/[id]/calendar/+page.server.ts`
  - `src/web/src/routes/projects/[id]/gantt/+page.server.ts`
  - `src/web/src/routes/projects/[id]/repos/+page.server.ts`
  - `src/web/src/routes/projects/[id]/runs/+page.server.ts`
  - `src/web/src/routes/projects/[id]/runs/[runId]/+page.server.ts`
- Removed remediated Plan 26 files from `src/architecture/no-raw-sql.test.ts` expected violation baselines.

## Verification

- `bun run lint` passed.
- `bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts` passed.
- `bun test src/application/repos src/application/runs` passed.
- Direct-access grep across remediated Plan 26 route files returned no matches.

## Known Test Runner Gap

`bun test 'src/web/src/routes/projects/[id]/repos/page.server.test.ts'` currently fails before route assertions because `src/test-support/product-fixtures.ts` does not export `createRepo`, which that test imports. This is an existing fixture export mismatch, not a residual direct-access failure in the remediated route.
