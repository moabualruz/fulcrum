---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 14
title: Web Api Trpc Runtime Direct Access Closure
type: tdd
wave: 1
depends_on:
  - 09.5-13
files_modified:
  - apps/web/src/routes/**/*.ts
  - apps/web/src/lib/server/**/*.ts
  - apps/server/src/api/routes/**/*.ts
  - apps/server/src/trpc/**/*.ts
  - src/application/**/*.ts
  - src/architecture/boundary.test.ts
  - src/architecture/no-raw-sql.test.ts
autonomous: true
gap_closure: true
requirements:
  - R-1
  - R-2
  - R-5
  - R-6
  - R-9
  - R-19
must_haves:
  truths:
    - "No SvelteKit runtime route/helper, REST route, or tRPC runtime adapter imports or calls `openProductDb`, `ProductDb`, `openPglite`, `getProductDb`, or product-kernel stores."
    - "Web/API/tRPC adapters parse input, call `src/application/**`, and preserve existing response shapes/status codes."
    - "Raw `em.getConnection()` runtime usage in web/API/tRPC is removed or deferred only to Plan 17 with an exact failing test."
  artifacts:
    - path: "src/architecture/boundary.test.ts"
      provides: "Runtime web/API/tRPC direct-access gate."
      contains: "apps/web/src/routes"
    - path: "apps/web/src/routes/application-adapters.test.ts"
      provides: "Route adapter preservation coverage."
      contains: "application query module"
    - path: "apps/server/src/api/__tests__/hono-single-api.test.ts"
      provides: "Hono-only REST proof."
      contains: "product-kernel/api/router"
  key_links:
    - from: "apps/web/src/routes/**/*.ts"
      to: "src/application/**"
      via: "load/action adapters call application query/command functions"
      pattern: "from \"../../application|from \"$lib/server/application"
    - from: "apps/server/src/trpc/**/*.ts"
      to: "src/application/**"
      via: "procedure resolver delegates to application boundary"
      pattern: "AppContext"
---

<objective>
Close the Plan 13 runtime web/API/tRPC direct-access blocker by removing ProductDb/PGlite/product-kernel usage from runtime adapter paths.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-PATTERNS.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-GAP-CLOSURE-RESEARCH.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-13-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 09.5-14-01: RED runtime adapter inventory tests</name>
  <files>src/architecture/boundary.test.ts, apps/web/src/routes/application-adapters.test.ts</files>
  <read_first>
    <file>src/architecture/boundary.test.ts</file>
    <file>apps/web/src/routes/application-adapters.test.ts</file>
    <file>.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-13-SUMMARY.md</file>
  </read_first>
  <action>Extend architecture and adapter tests so runtime files under `apps/web/src/routes`, `apps/web/src/lib/server`, `apps/server/src/api/routes`, and `apps/server/src/trpc` fail on `openProductDb`, `openPglite`, `getProductDb`, `ProductDb`, and `from .*product-kernel`. Exclude only `*.test.ts`, `*.spec.ts`, and files under `src/product-kernel`. Add an assertion that the runtime inventory starts non-empty before GREEN work by naming at least these current files: `apps/web/src/routes/api/repos/[id]/tree/+server.ts`, `apps/web/src/routes/orchestration/+page.server.ts`, `apps/web/src/routes/audit/export/+server.ts`, `apps/web/src/lib/server/db.ts`, `apps/server/src/trpc/context.ts`, and `apps/server/src/trpc/routers/orchestration.ts`.</action>
  <verify>
    <automated>bun test src/architecture/boundary.test.ts apps/web/src/routes/application-adapters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/architecture/boundary.test.ts` contains `apps/web/src/routes`, `apps/web/src/lib/server`, `apps/server/src/api/routes`, and `apps/server/src/trpc` runtime roots.</criterion>
    <criterion>Test failure output before GREEN names at least one current runtime direct-access file.</criterion>
    <criterion>No broad allowlist is added for web/API/tRPC runtime paths.</criterion>
  </acceptance_criteria>
  <done>Runtime direct-access violations have executable RED coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-14-02: GREEN web route and helper rewires</name>
  <files>apps/web/src/routes/**/*.ts, apps/web/src/lib/server/**/*.ts, src/application/**/*.ts, apps/web/src/routes/application-adapters.test.ts</files>
  <read_first>
    <file>apps/web/src/lib/server/em.ts</file>
    <file>apps/web/src/lib/server/orm-helpers.ts</file>
    <file>src/application/tasks/queries.ts</file>
    <file>src/application/runs/queries.ts</file>
    <file>src/application/artifacts/queries.ts</file>
    <file>src/application/search/queries.ts</file>
    <file>src/application/notifications/queries.ts</file>
  </read_first>
  <action>For every runtime match from `rg -l 'openPglite|openProductDb|getProductDb|ProductDb|from\\s+[\"'\\'''][^\"'\\''']*product-kernel' apps/web/src/routes apps/web/src/lib/server --glob '*.ts' | rg -v '\\.(test|spec)\\.ts$'`, replace direct DB usage with application query/command calls or `getEm()` plus repository/QueryBuilder where an application module already exists. Preserve loader return keys, action status codes, redirect targets, cookies, and JSON shapes. Remove `apps/web/src/lib/server/db.ts` as a runtime data API; if bootstrap helpers remain, move them behind neutral names that do not contain `ProductDb` or `openProductDb` and do not expose `.query()`.</action>
  <verify>
    <automated>bun test apps/web/src/routes/application-adapters.test.ts</automated>
    <automated>! rg 'openPglite|openProductDb|getProductDb|ProductDb|from .*product-kernel' apps/web/src/routes apps/web/src/lib/server --type ts | rg -v '\\.(test|spec)\\.ts$'</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Runtime web route/helper grep for `openPglite|openProductDb|getProductDb|ProductDb|from .*product-kernel` returns no matches.</criterion>
    <criterion>`apps/web/src/routes/application-adapters.test.ts` passes and still checks route response key preservation.</criterion>
    <criterion>No route changes its exported `load` or `actions` names.</criterion>
  </acceptance_criteria>
  <done>Web runtime adapters are free of ProductDb/PGlite/product-kernel direct access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-14-03: GREEN REST and tRPC runtime rewires</name>
  <files>apps/server/src/api/routes/**/*.ts, apps/server/src/trpc/**/*.ts, src/application/**/*.ts, apps/server/src/api/__tests__/hono-single-api.test.ts</files>
  <read_first>
    <file>apps/server/src/api/hono.ts</file>
    <file>apps/server/src/api/__tests__/hono-single-api.test.ts</file>
    <file>apps/server/src/trpc/context.ts</file>
    <file>apps/server/src/trpc/routers/orchestration.ts</file>
    <file>src/application/error-mapping.ts</file>
  </read_first>
  <action>Replace runtime `ProductDb` dependencies in `apps/server/src/api/routes/**/*.ts` and `apps/server/src/trpc/**/*.ts` with application command/query dependencies. `TrpcContext` must not expose `db?: ProductDb`; use `em` plus `AppContext` only. Hono route handlers must resolve an EntityManager and AppContext, then call application modules. Keep `apps/server/src/api/hono.ts` the sole REST entry point and do not recreate `src/product-kernel/api/router.ts`.</action>
  <verify>
    <automated>bun test apps/server/src/api/__tests__/hono-single-api.test.ts apps/server/src/api/routes/kernel-tasks.test.ts apps/server/src/trpc/routers/orchestration.test.ts apps/server/src/trpc/rest-api.test.ts</automated>
    <automated>! rg 'openPglite|openProductDb|getProductDb|ProductDb|from .*product-kernel' apps/server/src/api/routes apps/server/src/trpc --type ts | rg -v '\\.(test|spec)\\.ts$'</automated>
    <automated>test ! -f src/product-kernel/api/router.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`apps/server/src/trpc/context.ts` contains no `ProductDb` type or `db?:` product-kernel field.</criterion>
    <criterion>Runtime REST/tRPC grep for direct ProductDb/product-kernel access returns no matches.</criterion>
    <criterion>`test ! -f src/product-kernel/api/router.ts` exits 0.</criterion>
  </acceptance_criteria>
  <done>REST and tRPC runtime adapters are thin application-boundary adapters.</done>
</task>
</tasks>

<rollback>
If a web/API/tRPC rewire breaks a public response shape, keep the RED boundary test and revert only that adapter internals, not the gate. Preserve exported schemas, status codes, and JSON keys. Do not reintroduce `ProductDb` naming outside `src/product-kernel`.
</rollback>

<threat_model>
| Threat ID | Category | Component | Mitigation |
| --- | --- | --- | --- |
| T-14-01 | Tampering | architecture gates | Add stricter tests before implementation and do not loosen Plan 13 grep commands. |
| T-14-02 | Information Disclosure | web/API adapters | Preserve AppContext auth/project scope while moving data access. |
| T-14-03 | Repudiation | route rewires | Keep route-specific adapter tests proving response shapes. |
</threat_model>

<verification>
- `bun test src/architecture/boundary.test.ts apps/web/src/routes/application-adapters.test.ts`
- `bun test apps/server/src/api/__tests__/hono-single-api.test.ts apps/server/src/api/routes/kernel-tasks.test.ts apps/server/src/trpc/routers/orchestration.test.ts apps/server/src/trpc/rest-api.test.ts`
- `! rg 'openPglite|openProductDb|getProductDb|ProductDb|from .*product-kernel' apps/web/src/routes apps/web/src/lib/server apps/server/src/api/routes apps/server/src/trpc --type ts | rg -v '\\.(test|spec)\\.ts$'`
- `test ! -f src/product-kernel/api/router.ts`
</verification>

<success_criteria>
All runtime web/API/tRPC direct ProductDb/PGlite/product-kernel matches are gone, public adapter tests pass, and R-19 remains closed.
</success_criteria>
