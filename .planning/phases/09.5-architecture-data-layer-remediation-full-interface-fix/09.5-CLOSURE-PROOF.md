---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
artifact: closure-proof
plan: 49
status: failed
updated: 2026-05-07
---

# Phase 09.5 Plan 49 Closure Proof

**Closure failed. Phase 10 remains blocked.**

Plan 49 closed the targeted CLI init and web EM helper residuals, but the exact Plan 27 Task 1 closure chain still exited 1. The failing point is `src/architecture/boundary.test.ts`, whose residual direct-access expected list is now stale versus the current codebase.

## Result

| Gate | Result | Exit | Closure Impact |
| --- | --- | --- | --- |
| Focused Plan 49 tests | PASS | 0 | Targeted behavior preserved. |
| Focused Plan 49 CLI/web grep | PASS | 0 | `src/cli/commands/init.ts` and `src/web/src/lib/server/em.ts` are grep-clean. |
| Exact Plan 27 Task 1 architecture/research closure chain | FAIL | 1 | Stop. Do not claim closure. |
| Exact Plan 27 Task 2 official CI closure chain | NOT RUN | n/a | Blocked by Task 1 failure. |

## Focused Checks

```sh
bun test src/application/init/init.integration.test.ts src/cli/commands/init.test.ts src/web/src/lib/server/em.test.ts -- -x
```

Exit code: `0`

```text
3 pass
0 fail
6 expect() calls
Ran 3 tests across 3 files.
```

```sh
! rg -n "ctx\\.em|em\\.find|em\\.findOne|em\\.create|em\\.persist|em\\.flush|em\\.transactional|getRepository\\(" src/cli/commands/init.ts src/web/src/lib/server/em.ts
```

Exit code: `0`

## Exact Plan 27 Task 1 Command

```sh
bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts src/architecture/migrations.test.ts && ! rg -n "ctx\\.em|em\\.find|em\\.findOne|em\\.create|em\\.persist|em\\.flush|em\\.transactional|getRepository\\(" src/web/src/routes src/web/src/lib src/cli src/tui src/api src/router src/trpc src/server/trpc -g '*.ts' -g '*.tsx' | rg -v "\\.test\\.ts|\\.spec\\.ts|/__tests__/|src/web/src/lib/server/db\\.ts|src/trpc/context\\.ts|src/cli/index\\.ts" && ! rg -n "openDatabase|getDatabase|getEm|getDefaultOrgIdOrm|ormSqlConnection|WebDatabaseHandle|LegacyDatabaseHandle|application-compat" src/web/src/routes src/web/src/lib -g '*.ts' -g '*.tsx' | rg -v "\\.test\\.ts|\\.spec\\.ts|src/web/src/lib/server/db\\.ts" && ! rg -n "from ['\\\"][^'\\\"]*(db/entities|db/repositories|db/db.module|db/mikro-orm.config)" src/web/src/routes src/web/src/lib src/cli src/tui src/api src/router src/trpc src/server/trpc -g '*.ts' -g '*.tsx' | rg -v "\\.test\\.ts|\\.spec\\.ts|/__tests__/|src/web/src/lib/server/db\\.ts|src/cli/index\\.ts"
```

Exit code: `1`

Failure evidence:

```text
src/architecture/boundary.test.ts:
12 pass
1 fail
16 expect() calls
Ran 13 tests across 3 files.

Expected residual list included:
src/server/trpc/routers/audit.ts
src/server/trpc/routers/auth.ts
src/server/trpc/routers/memory.ts
src/server/trpc/routers/routing.ts
src/server/trpc/routers/tasks.ts
src/trpc/routers/repos.ts
src/trpc/routers/webhooks.ts
src/web/src/lib/components/docs/frontmatter-ui.ts
src/web/src/lib/server/em.ts

Actual residual list was:
src/server/trpc/routers/memory.ts
src/server/trpc/routers/routing.ts
src/web/src/lib/components/docs/frontmatter-ui.ts
```

The command stopped at the failing architecture test before running the three Plan 27 grep segments.

## Post-Failure Diagnostics

These were run separately after the exact chain failed; they are diagnostic only, not a replacement for the exact Plan 27 command.

| Diagnostic | Result | Evidence |
| --- | --- | --- |
| Direct EntityManager/repository grep segment | PASS | No matches after exclusions. |
| Legacy DB handle alias grep segment | FAIL | Matches remain in `src/web/src/routes/agents/[name]/+page.server.ts`, `src/web/src/routes/agents/+page.server.ts`, `src/web/src/routes/memory/[id]/+page.server.ts`, `src/web/src/routes/projects/new/+page.server.ts`, `src/web/src/routes/settings/notifications/+page.server.ts`, and `src/web/src/routes/settings/skills/+page.server.ts`. |
| Entity/repository/config import grep segment | FAIL | Matches remain in `src/server/trpc/routers/memory.ts`, `src/server/trpc/routers/routing.ts`, and `src/web/src/lib/components/docs/frontmatter-ui.ts`. |

## Exact Plan 27 Task 2 Command

```sh
bun run ci --tier=unit && bun run ci --tier=integration && FULCRUM_RUN_E2E=1 bun run ci --tier=e2e && FULCRUM_RUN_E2E=1 bun run ci
```

Result: `NOT RUN`

Reason: exact Plan 27 Task 1 failed, and Plan 49 requires stopping before Task 2 unless Task 1 passes.

## Closure Decision

No closure claim. No Phase 10 unblock claim. Failure evidence only.

---

# Plan 50-52 Gap Closure Proof

Date: `2026-05-07 07:12:58 CEST`

## Exact Plan 27 Task 1 Command

```sh
bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts src/architecture/migrations.test.ts && ! rg -n "ctx\\.em|em\\.find|em\\.findOne|em\\.create|em\\.persist|em\\.flush|em\\.transactional|getRepository\\(" src/web/src/routes src/web/src/lib src/cli src/tui src/api src/router src/trpc src/server/trpc -g '*.ts' -g '*.tsx' | rg -v "\\.test\\.ts|\\.spec\\.ts|/__tests__/|src/web/src/lib/server/db\\.ts|src/trpc/context\\.ts|src/cli/index\\.ts" && ! rg -n "openDatabase|getDatabase|getEm|getDefaultOrgIdOrm|ormSqlConnection|WebDatabaseHandle|LegacyDatabaseHandle|application-compat" src/web/src/routes src/web/src/lib -g '*.ts' -g '*.tsx' | rg -v "\\.test\\.ts|\\.spec\\.ts|src/web/src/lib/server/db\\.ts" && ! rg -n "from ['\\\"][^'\\\"]*(db/entities|db/repositories|db/db.module|db/mikro-orm.config)" src/web/src/routes src/web/src/lib src/cli src/tui src/api src/router src/trpc src/server/trpc -g '*.ts' -g '*.tsx' | rg -v "\\.test\\.ts|\\.spec\\.ts|/__tests__/|src/web/src/lib/server/db\\.ts|src/cli/index\\.ts"
```

Exit code: `0`

Evidence:

```text
src/architecture/boundary.test.ts: 9 pass, 0 fail
src/architecture/no-raw-sql.test.ts: 3 pass, 0 fail
src/architecture/migrations.test.ts: 1 pass, 0 fail

13 pass
0 fail
16 expect() calls
Ran 13 tests across 3 files.
```

All three grep segments exited `0` under negated `rg` gates:

- Direct EntityManager/repository grep: no matches after allowed composition-root/test exclusions.
- Legacy web data-handle alias grep: no route/helper matches after allowed `src/web/src/lib/server/db.ts` exclusion.
- DB entity/repository/config import grep: no interface matches after allowed composition-root/test exclusions.

## Supplemental Focused Checks

```sh
bun test src/web/src/routes/agents/page.server.test.ts src/web/src/routes/settings/notifications/page.server.test.ts src/web/src/routes/settings/skills/page.server.test.ts -- -x
```

Exit code: `0`

```text
11 pass
0 fail
29 expect() calls
Ran 11 tests across 3 files.
```

```sh
bun run lint
```

Exit code: `0`

## Remaining Closure Status

Exact Plan 27 Task 1 is now closed. Exact Plan 27 Task 2 CI chain remains to run before Phase 10 can be unblocked.
