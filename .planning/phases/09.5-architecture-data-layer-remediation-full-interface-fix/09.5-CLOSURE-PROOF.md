---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
artifact: closure-proof
plan: 27
status: failed
created: 2026-05-07
---

# Phase 09.5 Plan 27 Closure Proof

**Closure failed. Phase 10 remains blocked.**

## Result

| Gate | Result | Exit | Closure Impact |
| --- | --- | --- | --- |
| Task 1 architecture/research closure chain | FAIL | 1 | Stop. Do not claim closure. |
| Task 2 official CI closure chain | NOT RUN | n/a | Blocked by Task 1 failure. |

## Task 1 Command

```sh
bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts src/architecture/migrations.test.ts && ! rg -n "ctx\.em|em\.find|em\.findOne|em\.create|em\.persist|em\.flush|em\.transactional|getRepository\(" src/web/src/routes src/web/src/lib src/cli src/tui src/api src/router src/trpc src/server/trpc -g '*.ts' -g '*.tsx' | rg -v "\.test\.ts|\.spec\.ts|/__tests__/|src/web/src/lib/server/db\.ts|src/trpc/context\.ts|src/cli/index\.ts" && ! rg -n "openDatabase|getDatabase|getEm|getDefaultOrgIdOrm|ormSqlConnection|WebDatabaseHandle|LegacyDatabaseHandle|application-compat" src/web/src/routes src/web/src/lib -g '*.ts' -g '*.tsx' | rg -v "\.test\.ts|\.spec\.ts|src/web/src/lib/server/db\.ts" && ! rg -n "from ['\"][^'\"]*(db/entities|db/repositories|db/db.module|db/mikro-orm.config)" src/web/src/routes src/web/src/lib src/cli src/tui src/api src/router src/trpc src/server/trpc -g '*.ts' -g '*.tsx' | rg -v "\.test\.ts|\.spec\.ts|/__tests__/|src/web/src/lib/server/db\.ts|src/cli/index\.ts"
```

Exit code: `1`

## Task 1 Output

Architecture tests passed:

```text
13 pass
0 fail
16 expect() calls
Ran 13 tests across 3 files.
```

The first forbidden-pattern grep failed with matches:

```text
src/server/trpc/routers/auth.ts:77:    return mapAppError(() => authApplication.resolveApplicationSessionContext(ctx.em, appContext(ctx)));
src/server/trpc/routers/auth.ts:88:        authApplication.createInvitation(requireAuthEntityManager(ctx.em), appContext(ctx), input)
src/server/trpc/routers/auth.ts:100:        authApplication.acceptInvitation(requireAuthEntityManager(ctx.em as EntityManager | null), {
src/server/trpc/routers/tasks.ts:57:  if (ctx.em) return ctx.em;
src/server/trpc/routers/audit.ts:97:  if (ctx.em) return ctx.em;
src/server/trpc/routers/memory.ts:107:  if (ctx.em) return ctx.em;
src/server/trpc/routers/memory.ts:143:      ctx.em
src/server/trpc/routers/memory.ts:166:      ctx.em
src/server/trpc/routers/routing.ts:163:  if (ctx.em) return ctx.em;
src/trpc/routers/webhooks.ts:159:      const rows = await ctx.em.find(
src/trpc/routers/webhooks.ts:175:      const row = await ctx.em.findOne(WebhookDelivery, { id: input.id, org: { id: ctx.orgId } });
src/trpc/routers/webhooks.ts:191:      const rows = await ctx.em.find(Webhook, { org: { id: ctx.orgId } }, { orderBy: { createdAt: "DESC" } });
src/trpc/routers/webhooks.ts:203:      const row = await ctx.em.findOne(Webhook, { id: input.id, org: { id: ctx.orgId } });
src/trpc/routers/webhooks.ts:220:      const webhook = ctx.em.create(Webhook, {
src/trpc/routers/webhooks.ts:229:      ctx.em.persist(webhook);
src/trpc/routers/webhooks.ts:230:      await ctx.em.flush();
src/trpc/routers/webhooks.ts:242:      const webhook = await ctx.em.findOne(Webhook, { id: input.id, org: { id: ctx.orgId } });
src/trpc/routers/webhooks.ts:251:      await ctx.em.flush();
src/trpc/routers/webhooks.ts:263:      const webhook = await ctx.em.findOne(Webhook, { id: input.id, org: { id: ctx.orgId } });
src/trpc/routers/webhooks.ts:267:      await ctx.em.flush();
src/trpc/routers/repos.ts:66:    return ctx.em.getRepository(Repo) as RepoRepository;
src/trpc/routers/repos.ts:111:  const event = ctx.em.create(Event, {
src/trpc/routers/repos.ts:119:  ctx.em.persist(event);
src/cli/commands/init.ts:58:    const orgRepo = orm.em.fork().getRepository(Org);
src/web/src/lib/server/em.ts:11:  const org = await em.findOne(Org, { slug: "default" } as never);
```

## Task 2 Command

```sh
bun run ci --tier=unit && bun run ci --tier=integration && FULCRUM_RUN_E2E=1 bun run ci --tier=e2e && FULCRUM_RUN_E2E=1 bun run ci
```

Result: `NOT RUN`

Reason: Task 1 failed, and Plan 27 requires stopping on first failure.

## Closure Decision

No closure claim. No Phase 10 unblock claim. Failure evidence only.
