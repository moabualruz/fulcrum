---
phase: 9.5
plan: 02
title: MikroORM Schema Authority And Transactional Outbox
type: tdd
wave: 1
depends_on:
  - 09.5-00
files_modified:
  - src/db/entities/platform/DomainEventOutbox.ts
  - src/db/entities/connectors/BitbucketPullRequest.ts
  - src/db/entities/connectors/BitbucketIssue.ts
  - src/db/entities/connectors/GitlabMergeRequest.ts
  - src/db/entities/connectors/GitlabIssue.ts
  - src/db/entities/connectors/GithubConnectorState.ts
  - src/db/entities/repos/RepoTreeEntry.ts
  - src/db/entities/repos/RepoBlameLine.ts
  - src/db/entities/audit/AuditEvent.ts
  - src/db/entities/audit/AuditExport.ts
  - src/db/entities/settings/ConnectorCredential.ts
  - src/db/migrations/Migration20260506095000_phase95_application_authority.ts
  - src/db/migrator-service.ts
  - src/product-kernel/db/migrate.ts
autonomous: true
requirements:
  - DATA-01..08
  - ARCH-REMED-01..12
requirements_addressed:
  - R-7
  - R-8
  - R-10
  - R-11
  - R-14
  - R-15
  - R-23
must_haves:
  truths:
    - D-15
    - D-19
    - D-20
    - D-21
    - D-22
    - D-23
    - D-24
    - D-34
    - D-35
    - D-36
    - D-37
    - D-38
    - D-39
    - D-40
    - D-41
    - D-42
    - D-43
---

<objective>
Make MikroORM the only runtime schema/migration authority and add entity coverage plus transactional outbox needed by application commands.
</objective>

<threat_model>
- T-02-01: Schema drift between product-kernel SQL and MikroORM reappears. Mitigation: architecture test rejects runtime product-kernel migrations and tests seed through MikroORM only.
- T-02-02: Events dispatch without transaction atomicity. Mitigation: outbox row written inside same `em.transactional` as domain mutation.
- Block on: high.
</threat_model>

<schema_push_requirement>
**[BLOCKING] Schema Push Required**

This phase modifies schema-relevant files in `src/db/entities/**` and `src/db/migrations/**`.
- ORM detected: MikroORM
- Push command: `bun test src/db/migrator-service.test.ts src/db/migrations/*.test.ts`
- Non-TTY workaround: Bun tests run non-interactively
- Task 09.5-02-04 must run after entity/migration edits and before verification.
</schema_push_requirement>

<tasks>
<task id="09.5-02-01" type="tdd">
  <read_first>
    <file>src/db/entities/connectors/index.ts</file>
    <file>src/db/entities/repos/index.ts</file>
    <file>src/db/entities/flags/WebhookSubscription.ts</file>
    <file>09.5-SPEC.md</file>
  </read_first>
  <action>Write failing integration tests for new MikroORM entities: bitbucket PR/issue/auth, gitlab MR/issue/auth, github connector state, repo tree/blame/content, audit event/export, connector credential/settings. Each test must create, persist, reload, and assert org/project foreign keys.</action>
  <acceptance_criteria>
    <criterion>`src/db/entities/connectors/bitbucket.integration.test.ts` contains `BitbucketPullRequest` and `BitbucketIssue`.</criterion>
    <criterion>`src/db/entities/repos/repo-files.integration.test.ts` contains `RepoTreeEntry` and `RepoBlameLine`.</criterion>
    <criterion>`src/db/entities/audit/audit-entity.integration.test.ts` contains `AuditEvent` and `AuditExport`.</criterion>
    <criterion>`src/db/entities/settings/settings-entity.integration.test.ts` contains `ConnectorCredential` or `Settings` entity (D-23 lists settings/credentials entity).</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-02-02" type="execute">
  <read_first>
    <file>src/db/entities/index.ts</file>
    <file>src/db/mikro-orm.config.ts</file>
    <file>src/product-kernel/store/bitbucket.ts</file>
    <file>src/product-kernel/store/gitlab.ts</file>
    <file>src/product-kernel/store/repo-files.ts</file>
    <file>src/product-kernel/store/audit.ts</file>
  </read_first>
  <action>Add MikroORM entity classes and barrel exports for all missing product-kernel domains named in D-23, including the settings/credentials entity (`ConnectorCredential` or equivalent). Add one migration `Migration20260506095000_phase95_application_authority.ts` creating the required tables and indexes. Do not collapse existing migrations. Do not use raw SQL from application code; migration files may use MikroORM migration APIs.</action>
  <acceptance_criteria>
    <criterion>`src/db/entities/connectors/index.ts` exports Bitbucket, GitLab, and GitHub connector entities.</criterion>
    <criterion>`src/db/entities/repos/index.ts` exports repo tree/blame/content entities.</criterion>
    <criterion>`src/db/entities/audit/index.ts` exports audit event/export entities.</criterion>
    <criterion>`src/db/entities/settings/ConnectorCredential.ts` (or equivalent) exists and is registered in MikroORM config (D-23 settings/credentials entity).</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-02-03" type="tdd">
  <read_first>
    <file>src/db/entities/core/Event.ts</file>
    <file>src/subscriptions/event-bus.ts</file>
    <file>src/subscriptions/pglite-bridge.ts</file>
    <file>src/notifications/realtime-bell.ts</file>
  </read_first>
  <action>Write `src/application/outbox.test.ts` and `src/application/outbox.integration.test.ts` for event serialization, same-transaction outbox creation, idempotent worker dispatch, search-index handoff, notification fanout handoff, and bell count query through application notification query.</action>
  <acceptance_criteria>
    <criterion>`src/application/outbox.integration.test.ts` contains `same transaction`, `idempotent`, and `dispatch` assertions.</criterion>
    <criterion>Tests fail while `src/subscriptions/pglite-bridge.ts` still calls `pglite.query()` directly.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-02-04" type="execute">
  <read_first>
    <file>src/db/migrator-service.ts</file>
    <file>src/product-kernel/db/migrate.ts</file>
    <file>src/subscriptions/pglite-bridge.ts</file>
    <file>src/notifications/realtime-bell.ts</file>
  </read_first>
  <action>Implement the outbox entity, application outbox writer/worker, and MikroORM-only migrator path. Outbox worker design: polling with configurable interval (default 1s) + pg-notify fast path for immediate dispatch when available. Product-kernel SQL migrations become legacy compatibility input and are not called from runtime startup, web, CLI, TUI, API, or tests except explicit legacy-upgrade tests. Rewire `pglite-bridge.ts` and `realtime-bell.ts` to consume application outbox/notification queries instead of direct PGlite. Additionally fix R-14: migrate `notifications/defaults.ts` raw `em.getConnection().execute()` SQL to use MikroORM query builder or application queries.</action>
  <acceptance_criteria>
    <criterion>`rg 'runMigrations' src/ --type ts | grep -v product-kernel/db/migrations | grep -v test` returns zero runtime product-kernel migration matches.</criterion>
    <criterion>`rg 'PGlite|pglite\\.query' src/subscriptions/ src/notifications/realtime-bell.ts --type ts` returns zero matches.</criterion>
    <criterion>`rg 'getConnection\\(\\)\\.execute' src/notifications/ src/workers/ --type ts` returns zero matches (R-14).</criterion>
    <criterion>Outbox worker uses configurable polling interval with pg-notify fast path.</criterion>
    <criterion>`bun test src/db/migrator-service.test.ts src/application/outbox.integration.test.ts` exits 0.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `bun test src/db/entities/connectors src/db/entities/repos src/db/entities/audit src/db/entities/settings`
- `bun test src/application/outbox.test.ts src/application/outbox.integration.test.ts src/architecture/migrations.test.ts`
- `bun run ci --tier=integration --domain=application`
</verification>

<success_criteria>
- New product-kernel-only domains have MikroORM entities/repository coverage.
- Runtime migration authority is MikroORM only.
- Outbox feeds events/search/notifications/collab without PGlite bridge violations.
</success_criteria>
