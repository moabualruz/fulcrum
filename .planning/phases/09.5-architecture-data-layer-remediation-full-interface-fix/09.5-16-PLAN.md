---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 16
title: Non Web Legacy ProductDb Closure
type: tdd
wave: 2
depends_on:
  - 09.5-14
files_modified:
  - src/config/database.ts
  - src/db/product-migrations.ts
  - src/search/**/*.ts
  - src/services/**/*.ts
  - src/connectors/**/*.ts
  - src/docs/**/*.ts
  - src/collab/**/*.ts
  - src/doctor/checks/**/*.ts
  - src/orchestration/symphony/**/*.ts
  - src/application/**/*.ts
  - src/product-kernel/**/*.ts
  - src/architecture/boundary.test.ts
autonomous: true
gap_closure: true
requirements:
  - R-1
  - R-7
  - R-10
  - R-11
  - R-14
  - R-15
must_haves:
  truths:
    - "`ProductDb`, `openPglite`, and `openProductDb` names do not appear outside `src/product-kernel/`."
    - "Legacy compatibility code that truly needs PGlite/product-kernel stays under `src/product-kernel/` and exposes neutral protocol interfaces to the rest of the app."
    - "Search, connector, docs, collab, doctor, service, and orchestration callers keep behavior through application or neutral repository protocols."
---

<objective>
Remove the remaining non-web `ProductDb` and PGlite compatibility leaks that keep global architecture grep gates red.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-PATTERNS.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-GAP-CLOSURE-RESEARCH.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-13-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 09.5-16-01: RED non-web legacy leak inventory</name>
  <files>src/architecture/boundary.test.ts</files>
  <read_first>
    <file>src/architecture/boundary.test.ts</file>
    <file>src/search/query-service.ts</file>
    <file>src/services/tasks.ts</file>
    <file>src/connectors/framework.ts</file>
    <file>src/orchestration/symphony/http-server.ts</file>
  </read_first>
  <action>Replace broad non-web allowlists in `src/architecture/boundary.test.ts` with a RED inventory that fails on every `ProductDb`, `openPglite`, `openProductDb`, `getProductDb`, and product-kernel import outside `src/product-kernel/`. The failure must name representative categories: `src/config/database.ts`, `src/search/query-service.ts`, `src/services/tasks.ts`, `src/connectors/framework.ts`, `src/docs/doc-embedder.ts`, `src/collab/server.ts`, `src/doctor/checks/api.ts`, and `src/orchestration/symphony/http-server.ts`.</action>
  <verify>
    <automated>bun test src/architecture/boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Boundary test no longer has `EXACT_LEGACY_INFRASTRUCTURE_ALLOWLIST` entries that permit `ProductDb` outside `src/product-kernel/`.</criterion>
    <criterion>RED failure covers search, services, connectors, docs, collab, doctor, config/db, and orchestration categories.</criterion>
    <criterion>No category is hidden by a regex exclusion other than `src/product-kernel/` and vendor paths.</criterion>
  </acceptance_criteria>
  <done>Non-web legacy leaks are exact and cannot be silently allowlisted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-16-02: GREEN neutral protocol extraction</name>
  <files>src/product-kernel/**/*.ts, src/config/database.ts, src/db/product-migrations.ts, src/search/**/*.ts, src/docs/**/*.ts, src/collab/**/*.ts, src/connectors/**/*.ts, src/doctor/checks/**/*.ts</files>
  <read_first>
    <file>src/config/database.ts</file>
    <file>src/db/product-migrations.ts</file>
    <file>src/search/backend.ts</file>
    <file>src/search/indexers/base.ts</file>
    <file>src/docs/doc-embedder.ts</file>
    <file>src/connectors/framework.ts</file>
  </read_first>
  <action>Move low-level PGlite/product-kernel compatibility code that truly must remain to `src/product-kernel/legacy/**` or `src/product-kernel/testing/**`. Outside `src/product-kernel/`, replace exported types and parameters named `ProductDb` with neutral protocols such as `SqlExecutor`, `SearchIndexStore`, `LegacySqlStore`, or `CollabPersistence`. Replace functions named `openProductDb`/`openPglite` with neutral factory names outside the kernel. Search indexers, docs embedder, connector framework, collab server, and doctor checks must depend on those neutral interfaces or application modules, not product-kernel concrete types.</action>
  <verify>
    <automated>bun test src/search/query-service.test.ts src/docs/doc-embedder.test.ts src/connectors/framework.test.ts src/cli/doctor.test.ts</automated>
    <automated>! rg 'ProductDb|openProductDb|openPglite|getProductDb|from .*product-kernel' src/config src/db src/search src/docs src/collab src/connectors src/doctor src/orchestration/symphony --type ts | rg -v '^src/product-kernel/'</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Non-web infrastructure grep for `ProductDb|openProductDb|openPglite|getProductDb|from .*product-kernel` returns no matches outside `src/product-kernel/`.</criterion>
    <criterion>Search indexer/query/doc/connector/doctor representative tests pass.</criterion>
    <criterion>Neutral protocol files outside `src/product-kernel/` contain no product-kernel import.</criterion>
  </acceptance_criteria>
  <done>Legacy concrete DB naming is contained to product-kernel-owned implementation files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-16-03: GREEN legacy service and orchestration callers</name>
  <files>src/services/**/*.ts, src/orchestration/symphony/**/*.ts, src/application/**/*.ts, src/trpc/**/*.ts</files>
  <read_first>
    <file>src/services/tasks.ts</file>
    <file>src/services/runs.ts</file>
    <file>src/services/artifacts.ts</file>
    <file>src/orchestration/symphony/http-server.ts</file>
    <file>src/application/tasks/commands.ts</file>
    <file>src/application/runs/queries.ts</file>
    <file>src/application/artifacts/queries.ts</file>
  </read_first>
  <action>Retire or rewrite `src/services/tasks.ts`, `src/services/runs.ts`, and `src/services/artifacts.ts` so callers use application modules or neutral protocols and no `ProductDb` compatibility branch remains outside `src/product-kernel/`. Rewire `src/orchestration/symphony/http-server.ts` to accept application query/command dependencies or neutral status readers instead of `ProductDb`. Preserve HTTP response contracts and Symphony conformance behavior.</action>
  <verify>
    <automated>bun test src/orchestration/__tests__/symphony-conformance.test.ts src/application/tasks/tasks.integration.test.ts src/api/routes/kernel-tasks.test.ts</automated>
    <automated>! rg 'ProductDb|openProductDb|openPglite|getProductDb|from .*product-kernel' src/services src/orchestration/symphony --type ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/services` contains no `ProductDb`, `openProductDb`, `openPglite`, or product-kernel import matches.</criterion>
    <criterion>`src/orchestration/symphony/http-server.ts` contains no `ProductDb` field/comment/type.</criterion>
    <criterion>Symphony conformance passes, including SYM-18 startup cleanup sweep.</criterion>
  </acceptance_criteria>
  <done>Legacy service wrappers and Symphony HTTP compatibility no longer leak ProductDb names.</done>
</task>
</tasks>

<rollback>
If moving compatibility implementation under `src/product-kernel/` causes import cycles, keep implementation in place temporarily but rename only after extracting a neutral protocol and add a failing architecture test naming the cycle. Do not add a new allowlist that preserves global grep failures.
</rollback>

<threat_model>
| Threat ID | Category | Component | Mitigation |
| --- | --- | --- | --- |
| T-16-01 | Tampering | legacy compatibility | Keep concrete DB implementation in product-kernel-owned paths only. |
| T-16-02 | Denial of Service | search/indexing | Preserve indexer tests and outbox dispatch behavior during protocol extraction. |
| T-16-03 | Spoofing | doctor/orchestration | Preserve injected dependency contracts and response auth behavior. |
</threat_model>

<verification>
- `bun test src/architecture/boundary.test.ts`
- `bun test src/search/query-service.test.ts src/docs/doc-embedder.test.ts src/connectors/framework.test.ts src/cli/doctor.test.ts src/orchestration/__tests__/symphony-conformance.test.ts`
- `! rg 'ProductDb|openProductDb|openPglite|getProductDb|from .*product-kernel' src --type ts | rg -v '^src/product-kernel/'`
</verification>

<success_criteria>
The global ProductDb/PGlite/product-kernel grep gate returns no matches outside `src/product-kernel/`, and representative non-web behavior tests pass.
</success_criteria>
