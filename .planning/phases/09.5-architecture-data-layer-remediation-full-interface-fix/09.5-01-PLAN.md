---
phase: 9.5
plan: 01
title: Application Kernel And Tasks Vertical Slice
type: tdd
wave: 1
depends_on:
  - 09.5-00
files_modified:
  - src/application/errors.ts
  - src/application/error-mapping.ts
  - src/application/tasks/commands.ts
  - src/application/tasks/queries.ts
  - src/application/tasks/types.ts
  - src/application/tasks/schema.ts
  - src/db/db.module.ts
  - src/server/trpc/routers/tasks.ts
  - src/api/routes/kernel-tasks.ts
autonomous: true
requirements:
  - ARCH-REMED-01..12
  - DATA-01..08
  - API-DEC-01..06
  - IFACE-01..08
  - E2E-01..08
requirements_addressed:
  - R-1
  - R-3
  - R-4
  - R-5
  - R-6
  - R-9
  - R-13
  - R-23
must_haves:
  truths:
    - D-01
    - D-02
    - D-03
    - D-04
    - D-05
    - D-06
    - D-07
    - D-08
    - D-09
    - D-10
    - D-11
    - D-12
    - D-13
    - D-14
    - D-31
    - D-32
    - D-33
    - D-53
    - D-54
    - D-55
---

<objective>
Create the canonical `src/application/**` boundary and prove it end-to-end with the tasks domain before expanding to other domains.
</objective>

<threat_model>
- T-01-01: Application services skip tenant/permission checks because adapters already authenticate. Mitigation: commands/queries require an application context with org/user/project and test forbidden/cross-org cases.
- T-01-02: Moving `TaskService` changes behavior. Mitigation: git-move service first, then wrap with commands/queries and preserve existing service tests.
- Block on: high.
</threat_model>

<tasks>
<task id="09.5-01-01" type="tdd">
  <read_first>
    <file>src/services/TaskService.ts</file>
    <file>src/services/TaskService.test.ts</file>
    <file>src/db/db.module.ts</file>
    <file>src/db/repositories/tasks/TaskRepository.ts</file>
  </read_first>
  <action>Write failing tests `src/application/errors.test.ts`, `src/application/error-mapping.test.ts`, and `src/application/tasks/tasks.integration.test.ts`. Tests must assert the seven AppError classes, tRPC/HTTP/CLI mappings, task create/list/get/update/delete through application commands/queries, validation failure via `AppValidationError`, not-found via `AppNotFoundError`, and cross-org access via `AppForbiddenError`.</action>
  <acceptance_criteria>
    <criterion>`src/application/errors.test.ts` contains all seven class names from D-12.</criterion>
    <criterion>`src/application/tasks/tasks.integration.test.ts` contains `createTask`, `listTasks`, `getTask`, `updateTask`, and `deleteTask` assertions.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-01-02" type="execute">
  <read_first>
    <file>src/services/TaskService.ts</file>
    <file>src/services/tasks.ts</file>
    <file>src/server/trpc/routers/tasks.ts</file>
  </read_first>
  <action>Create `src/application/errors.ts`, `src/application/error-mapping.ts`, and `src/application/tasks/{commands.ts,queries.ts,types.ts,schema.ts}`. Fold task functions from `src/services/tasks.ts` and `src/services/TaskService.ts` into task commands/queries as exported functions (D-08: decompose classes into exported functions — NO `task.service.ts` class wrapper). Export DTOs, not ORM entities. Commands use `em.transactional`. Queries do not open write transactions.</action>
  <acceptance_criteria>
    <criterion>`src/application/tasks/commands.ts` contains `export async function createTask` and `em.transactional`.</criterion>
    <criterion>`src/application/tasks/queries.ts` contains `export async function listTasks` and does not contain `em.transactional`.</criterion>
    <criterion>`src/application/tasks/types.ts` exports DTO types and does not export MikroORM entity classes.</criterion>
    <criterion>No `task.service.ts` file exists in `src/application/tasks/` — D-08 requires exported functions, not service classes.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-01-02b" type="tdd">
  <read_first>
    <file>src/server/trpc/routers/tasks.ts</file>
    <file>src/api/routes/kernel-tasks.ts</file>
    <file>src/application/tasks/commands.ts</file>
  </read_first>
  <action>Write RED adapter tests for tRPC and Hono task routes (D-74 step 6: write adapter test that fails before step 7 rewire). Tests mock application task commands/queries and assert adapters call application layer, not product-kernel directly. Tests must fail before 01-03 rewires adapters.</action>
  <acceptance_criteria>
    <criterion>Adapter test for tRPC tasks router exists and imports from `src/application/tasks/`.</criterion>
    <criterion>Adapter test for Hono kernel-tasks route exists and imports from `src/application/tasks/`.</criterion>
    <criterion>Both tests fail (RED) before task 01-03 rewires the adapters.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-01-03" type="execute">
  <read_first>
    <file>src/db/db.module.ts</file>
    <file>src/server/trpc/routers/tasks.ts</file>
    <file>src/api/routes/kernel-tasks.ts</file>
  </read_first>
  <action>Add application task bindings to `src/db/db.module.ts`. Rewire `src/server/trpc/routers/tasks.ts` and `src/api/routes/kernel-tasks.ts` so each procedure/handler only validates input, calls `src/application/tasks/*`, serializes DTOs, and maps AppError. Remove product-kernel repository imports from `kernel-tasks.ts`.</action>
  <acceptance_criteria>
    <criterion>`rg 'from .*product-kernel' src/api/routes/kernel-tasks.ts` returns no matches.</criterion>
    <criterion>`rg 'em\\.(persist|flush|find|fork|getConnection)' src/server/trpc/routers/tasks.ts src/api/routes/kernel-tasks.ts` returns no matches.</criterion>
    <criterion>Adapter RED tests from 01-02b now pass (GREEN).</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-01-03b" type="execute">
  <read_first>
    <file>src/cli/import.ts</file>
    <file>src/cli/local-caller.ts</file>
    <file>src/cli/commands/tasks.ts</file>
  </read_first>
  <action>Migrate at least one CLI command (e.g., task-related paths in `src/cli/import.ts` or `src/cli/commands/tasks.ts`) to use `createLocalCaller()` → application tasks module. D-54 vertical slice must prove ALL interfaces including CLI.</action>
  <acceptance_criteria>
    <criterion>At least one CLI task command calls `createLocalCaller()` then application tasks commands/queries.</criterion>
    <criterion>`rg 'from .*product-kernel' src/cli/commands/tasks.ts` returns no matches after migration.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-01-03c" type="execute">
  <read_first>
    <file>src/tui/index.ts</file>
    <file>src/tui/screens/tasks.ts</file>
  </read_first>
  <action>Migrate at least one TUI screen to get task data via `buildCaller()` → application tasks module. D-54 vertical slice must prove ALL interfaces including TUI.</action>
  <acceptance_criteria>
    <criterion>At least one TUI task screen calls `buildCaller()` then application tasks queries.</criterion>
    <criterion>`rg 'from .*product-kernel' src/tui/screens/tasks.ts` returns no matches after migration.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-01-04" type="tdd">
  <read_first>
    <file>src/cli/local-caller.ts</file>
    <file>src/tui/index.ts</file>
    <file>09.5-UAT-BASELINE.md</file>
  </read_first>
  <action>Add cross-interface parity test for tasks: create via application command, read via tRPC caller, serialize through CLI JSON formatter, and assert the TUI caller receives identical id/title/status/project/org fields.</action>
  <acceptance_criteria>
    <criterion>Parity test contains `createLocalCaller` and `buildCaller` or their test helpers.</criterion>
    <criterion>Parity test asserts identical `id`, `title`, `status`, `projectId`, and `orgId` fields.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `bun test src/application/errors.test.ts src/application/error-mapping.test.ts src/application/tasks/tasks.integration.test.ts`
- `bun test src/server/trpc/routers/tasks.test.ts src/api/routes/kernel-tasks.test.ts`
- `bun run ci --tier=unit --domain=application`
</verification>

<success_criteria>
- Tasks domain proves `interfaces -> application -> repository -> database`.
- tRPC and Hono task adapters contain no business logic or product-kernel imports.
</success_criteria>
