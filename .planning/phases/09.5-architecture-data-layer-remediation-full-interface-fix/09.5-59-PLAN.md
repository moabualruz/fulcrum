# Phase 09.5 Plan 59: Repo Structure Hygiene

Status: active
Research: `09.5-59-REPO-STRUCTURE-RESEARCH.md`

## Goal

Reduce repo topology noise without breaking Phase 09.5 boundaries. Establish enforceable structure rules before larger package or folder moves.

## Tasks

1. Add architecture guard tests for:
   - no empty source directories outside explicit allowlist
   - no duplicate template-only context ADR files under `src/*/docs/adr/`
2. Run the new test and confirm RED.
3. Remove empty source route folder `apps/web/src/routes/projects/[id]/sprint/[sid]/`.
4. Remove duplicate context ADR template copies, preserving `docs/adr/0000-template.md`.
5. Update `src/*/CONTEXT.md` wording to say context ADR dirs are created lazily when decisions exist.
6. Run the new test and confirm GREEN.
7. Run focused architecture tests and boundary lint.
8. Refresh graphify.
9. Update Phase 09.5 validation/eval notes with structure-cleanup evidence.

## Execution Notes

- 2026-05-07: TDD RED confirmed `src/architecture/repo-structure.test.ts` failed on the empty SvelteKit route folder and six duplicate context ADR template files.
- 2026-05-07: Cleanup kept canonical ADR template at `docs/adr/0000-template.md`; context ADR directories now remain absent until a real context decision exists.
- 2026-05-07: `ast-grep` scan recorded remaining service/tRPC structural debt as deferred, not Plan 59 scope.

## Non-goals

- No workspace split in this plan.
- No mass test moves.
- No large import-tree rewrites.
- No deletion of legacy runtime layers until existing Phase 09.5 boundary replacement maps prove coverage.
