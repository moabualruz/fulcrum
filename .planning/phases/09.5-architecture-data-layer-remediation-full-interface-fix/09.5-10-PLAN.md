---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 10
title: CI Conformance And Web Test Gate Gap Closure
type: tdd
wave: 1
depends_on:
  - 09.5-00
  - 09.5-07
  - 09.5-08
files_modified:
  - src/orchestration/__tests__/symphony-conformance.test.ts
  - src/orchestration/symphony/workspace.ts
  - apps/web/tests/vitest/cmdk-palette.test.ts
  - scripts/ci.ts
  - scripts/ci.test.ts
autonomous: true
gap_closure: true
requirements:
  - ARCH-REMED-01..12
  - E2E-01..08
requirements_addressed:
  - R-12
  - R-18
  - R-22
  - R-24
  - SYM-18
  - WR-03
must_haves:
  truths:
    - "`bun run ci --tier=unit` reaches and passes `symphony:conformance` including SYM-18 startup cleanup."
    - "`FULCRUM_RUN_E2E=1 bun run ci --tier=e2e --domain=web` does not fail in `cmdk-palette.test.ts` due to undefined `import.meta.dir`."
    - "`--tier=e2e` and full CI semantics explicitly include full E2E when requested and cannot silently hide Phase 9.5 closure gates."
  artifacts:
    - path: "src/orchestration/symphony/workspace.ts"
      provides: "Startup terminal workspace sweep implementation."
      contains: "sweepTerminalWorkspaces"
    - path: "apps/web/tests/vitest/cmdk-palette.test.ts"
      provides: "Portable route-root path resolution for Bun/Vitest web CI."
      contains: "import.meta.url"
    - path: "scripts/ci.ts"
      provides: "Explicit e2e/full gate behavior."
      contains: "web:e2e:full"
  key_links:
    - from: "scripts/ci.ts"
      to: "src/orchestration/__tests__/symphony-conformance.test.ts"
      via: "symphony:conformance CI step"
      pattern: "symphony:conformance"
    - from: "scripts/ci.ts"
      to: "apps/web/tests/vitest/cmdk-palette.test.ts"
      via: "web:test CI step under --domain=web"
      pattern: "web:test"
---

<objective>
Close the CI blockers that prevent every tier from reaching Phase 9.5 acceptance: SYM-18 startup cleanup, web command palette vitest portability, and hidden full-E2E semantics.

Purpose: Phase 10 remains blocked until CI tiers and full E2E closure gate are deterministic.
Output: TDD fixes for conformance, web test pathing, and explicit CI E2E behavior.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-VERIFICATION.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-REVIEW.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-08-SUMMARY.md

<interfaces>
From `scripts/ci.ts`:
```ts
export interface Step { name: string; cmd: string[]; soft?: boolean; cwd?: string; env?: NodeJS.ProcessEnv; }
export interface TieredStep extends Step { tier: CiTier; domain: CiDomain | "all"; always?: boolean; }
export const ALL_STEPS: TieredStep[];
```

From `src/orchestration/symphony/workspace.ts`:
```ts
export async function sweepTerminalWorkspaces(
  em: EntityManager,
  orgId: string,
  opts?: { beforeRemove?: (run: AgentRun) => Promise<void>; dryRun?: boolean },
): Promise<number>;
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 09.5-10-01: RED/GREEN SYM-18 startup cleanup sweep</name>
  <files>src/orchestration/__tests__/symphony-conformance.test.ts, src/orchestration/symphony/workspace.ts</files>
  <read_first>
    <file>src/orchestration/__tests__/symphony-conformance.test.ts</file>
    <file>src/orchestration/symphony/workspace.ts</file>
    <file>src/orchestration/symphony/hooks.ts</file>
  </read_first>
  <action>Reproduce the current SYM-18 failure with `bun test src/orchestration/__tests__/symphony-conformance.test.ts --test-name-pattern 'Startup cleanup sweep'`. If the existing test does not expose the precise failure, tighten it first: seed terminal and running runs with valid temp workspace paths inside a scratch root, assert `beforeRemove` is called for terminal runs, assert terminal workspaces are removed when not dry-run, assert running workspace remains, and assert dry-run counts without deletion. GREEN: fix `sweepTerminalWorkspaces()` to use the same terminal state list as reconciliation, fork/flush EntityManager safely, call `beforeRemove` before removal, validate paths per Plan 07 security rules, and clear or preserve `workspacePath` consistently with existing conformance expectations.</action>
  <verify>
    <automated>bun test src/orchestration/__tests__/symphony-conformance.test.ts --test-name-pattern "Startup cleanup sweep"</automated>
    <automated>bun run ci --tier=unit</automated>
  </verify>
  <acceptance_criteria>
    <criterion>SYM-18 startup sweep test fails before implementation and passes after.</criterion>
    <criterion>`bun run ci --tier=unit` exits 0 through `symphony:conformance`.</criterion>
    <criterion>`sweepTerminalWorkspaces()` validates removal paths and does not delete running workspaces.</criterion>
  </acceptance_criteria>
  <done>SYM-18 no longer blocks unit/integration/e2e tiers before later gates run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-10-02: RED/GREEN portable command palette vitest path</name>
  <files>apps/web/tests/vitest/cmdk-palette.test.ts</files>
  <read_first>
    <file>apps/web/tests/vitest/cmdk-palette.test.ts</file>
    <file>apps/web/package.json</file>
    <file>scripts/ci.ts</file>
  </read_first>
  <action>RED: Run the web-domain CI command from verification and confirm `import.meta.dir` is undefined in `cmdk-palette.test.ts`. GREEN: replace `join(import.meta.dir, ...)` with portable path derivation from `fileURLToPath(new URL(..., import.meta.url))` or `dirname(fileURLToPath(import.meta.url))`. Keep test semantics unchanged: it still scans real route files and command palette ownership.</action>
  <verify>
    <automated>cd apps/web && bun run web:test -- tests/vitest/cmdk-palette.test.ts</automated>
    <automated>FULCRUM_RUN_E2E=1 bun run ci --tier=e2e --domain=web</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg 'import\\.meta\\.dir' apps/web/tests/vitest/cmdk-palette.test.ts` returns zero matches.</criterion>
    <criterion>`cmdk-palette.test.ts` uses `import.meta.url` plus `fileURLToPath` or `new URL()` path resolution.</criterion>
    <criterion>`FULCRUM_RUN_E2E=1 bun run ci --tier=e2e --domain=web` gets past `web:test`; remaining failures, if any, are E2E/browser behavior and must be fixed in the same task if caused by this edit.</criterion>
  </acceptance_criteria>
  <done>Web-domain CI no longer fails before Playwright because of path API drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-10-03: RED/GREEN explicit full E2E CI semantics</name>
  <files>scripts/ci.ts, scripts/ci.test.ts</files>
  <read_first>
    <file>scripts/ci.ts</file>
    <file>scripts/ci.test.ts</file>
    <file>.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-REVIEW.md</file>
  </read_first>
  <action>RED: Add or update `scripts/ci.test.ts` tests for WR-03. Tests must assert: `--tier=e2e` includes smoke/a11y plus an explicit full-E2E stage when `FULCRUM_RUN_E2E=1`; no-flag full CI with `FULCRUM_RUN_E2E=1` includes `web:e2e:full`; no-flag full CI without the env prints or exposes a clear skip reason rather than silently pretending full E2E ran; and `--tier=e2e --domain=web` includes web full E2E when env is set. GREEN: implement explicit behavior in `scripts/ci.ts` without adding GitHub Actions or npm. Keep D-76/D-78: Phase closure still requires `FULCRUM_RUN_E2E=1 bun run ci`.</action>
  <verify>
    <automated>bun test scripts/ci.test.ts</automated>
    <automated>FULCRUM_RUN_E2E=1 bun run ci --tier=e2e --domain=web</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`scripts/ci.test.ts` has tests named `WR-03` or `full E2E`.</criterion>
    <criterion>`scripts/ci.ts` still has `web:e2e:full` tier `e2e` and domain `web`.</criterion>
    <criterion>Full E2E omission without `FULCRUM_RUN_E2E=1` is explicit in output or step metadata, not hidden.</criterion>
  </acceptance_criteria>
  <done>Tiered CI semantics are explicit, and the Phase 9.5 closure command is hard-gated by full E2E.</done>
</task>
</tasks>

<rollback>
If SYM-18 cleanup removes unsafe paths or running workspaces, revert `src/orchestration/symphony/workspace.ts` and keep the tightened conformance test to document the unsafe behavior. If CI full-E2E semantics cause unacceptable local runtime cost, rollback must keep an explicit skip/requirement message and Phase 9.5 closure must remain `FULCRUM_RUN_E2E=1 bun run ci`; do not silently omit `web:e2e:full`. If command-palette path resolution fails in either Bun or Vitest, revert to a helper using `fileURLToPath(import.meta.url)` covered by the single-file test.
</rollback>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> local workspace | CI executes tests and cleanup code against generated temp paths. |
| Web test runner -> filesystem | Test scans SvelteKit route files using local path APIs. |
| Developer/release command -> CI tier selection | CLI flags decide which gates block closure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `sweepTerminalWorkspaces()` | mitigate | Validate paths before removal and keep running workspaces untouched. |
| T-10-02 | Denial of Service | `cmdk-palette.test.ts` | mitigate | Use portable `import.meta.url` path resolution so web CI does not fail before E2E. |
| T-10-03 | Repudiation | `scripts/ci.ts` | mitigate | Make full-E2E inclusion/skip explicit and testable. |
</threat_model>

<verification>
- `bun test src/orchestration/__tests__/symphony-conformance.test.ts`
- `bun test scripts/ci.test.ts`
- `cd apps/web && bun run web:test -- tests/vitest/cmdk-palette.test.ts`
- `bun run ci --tier=unit`
- `FULCRUM_RUN_E2E=1 bun run ci --tier=e2e --domain=web`
</verification>

<success_criteria>
SYM-18 conformance and web command palette vitest are green. CI tier/full-E2E behavior is explicit and covered by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-10-SUMMARY.md`.
</output>
