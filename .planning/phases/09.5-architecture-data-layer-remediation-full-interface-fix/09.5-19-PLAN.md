---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 19
type: execute
gap_closure: true
wave: 1
depends_on: ["09.5-18"]
files_modified:
  - src/architecture/boundary.test.ts
  - src/architecture/no-raw-sql.test.ts
autonomous: true
requirements: [R-1, R-2, R-3, R-4, R-5, R-6, R-9, R-13, R-20, R-24]
must_haves:
  truths:
    - "Architecture tests fail when web, CLI, TUI, API, router, tRPC, or server tRPC code performs direct ORM/database/entity work."
    - "Temporary exceptions are exact composition/test paths with one-line rationale, not broad directory excludes."
    - "Existing ProductDb/PGlite/product-kernel and raw EntityManager gates remain active."
  artifacts:
    - path: "src/architecture/boundary.test.ts"
      provides: "Broad interface direct-access import and ORM call boundary"
      contains: "ctx\\.em|em\\.find|em\\.findOne|em\\.create|em\\.persist|em\\.flush|em\\.transactional|getRepository"
    - path: "src/architecture/no-raw-sql.test.ts"
      provides: "No raw SQL or direct data-handle gate"
      contains: "openDatabase|getDatabase|getEm|getDefaultOrgIdOrm|ormSqlConnection|application-compat"
  key_links:
    - from: "src/architecture/boundary.test.ts"
      to: "src/web/src/routes src/web/src/lib src/cli src/tui src/api src/router src/trpc src/server/trpc"
      via: "runtime source scan"
      pattern: "src/server/trpc"
    - from: "src/architecture/no-raw-sql.test.ts"
      to: "research grep gates"
      via: "exact regex implementation"
      pattern: "rg -n"
---

<objective>
Harden Phase 09.5 architecture gates so residual interface-layer direct access from the research is executable failure, not prose.

Purpose: force later plans to move business logic/data manipulation/data providing behind `src/application/**` per D-10, D-15, D-25 through D-30, D-48, D-60, and D-72.
Output: failing-first boundary tests with exact temporary composition/test allowlist only.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-SPEC.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-INTERFACE-ENCAPSULATION-INTEGRATION-RESEARCH.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-ENCAPSULATION-DEPENDENCIES-RESEARCH.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-INTERFACE-PARITY-PLATFORM-RESEARCH.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-18-SUMMARY.md

<interfaces>
Use current `src/application/tasks/*`, `src/server/trpc/routers/tasks.ts`, `src/api/routes/kernel-tasks.ts`, `src/cli/local-caller.ts`, and `src/tui/index.ts` as target examples. Do not weaken existing gates from Plans 14-18.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add broad interface direct-access boundary gate</name>
  <files>src/architecture/boundary.test.ts</files>
  <read_first>
    - src/architecture/boundary.test.ts
    - .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-INTERFACE-ENCAPSULATION-INTEGRATION-RESEARCH.md
  </read_first>
  <behavior>
    - Test fails on `ctx.em`, `em.find`, `em.findOne`, `em.create`, `em.persist`, `em.flush`, `em.transactional`, or `getRepository(` in `src/web/src/routes`, `src/web/src/lib`, `src/cli`, `src/tui`, `src/api`, `src/router`, `src/trpc`, `src/server/trpc`.
    - Test excludes only tests/specs and exact composition files: `src/web/src/lib/server/db.ts`, `src/trpc/context.ts`, `src/cli/index.ts` when each has a local rationale string in the allowlist entry.
  </behavior>
  <action>Add a RED Bun test to `src/architecture/boundary.test.ts` implementing the exact research grep gate. Filter file paths before matching content so comments in test/spec files cannot self-trigger. Allowlist entries must be exact file paths with rationale; no directory-wide allowlist for interface/runtime paths. Use D-10, D-25 through D-30, D-48, and D-60 in the test name or message.</action>
  <verify>
    <automated>bun test src/architecture/boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>Gate reports every current direct access violation by exact file path and pattern; old ProductDb/PGlite/product-kernel gate still runs.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add web data-handle and application-compat gate</name>
  <files>src/architecture/no-raw-sql.test.ts</files>
  <read_first>
    - src/architecture/no-raw-sql.test.ts
    - .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-INTERFACE-ENCAPSULATION-INTEGRATION-RESEARCH.md
  </read_first>
  <behavior>
    - Test fails on `openDatabase`, `getDatabase`, `getEm`, `getDefaultOrgIdOrm`, `ormSqlConnection`, `WebDatabaseHandle`, `LegacyDatabaseHandle`, and `application-compat` in web routes/helpers after exact temporary exclusions.
    - Test fails on raw `.execute(` and `.query(` outside tests and legacy product-kernel DB files.
  </behavior>
  <action>Extend `src/architecture/no-raw-sql.test.ts` with the two research gates. Keep Plan 17 narrow `em.(persist|flush|execute|getConnection)` semantics, then add separate gates for web data-handle aliases and generic raw SQL. Exclude `src/web/src/lib/server/db.ts` only as composition root while later plans remove callers; do not exclude `src/web/src/routes/**` or domain helper files.</action>
  <verify>
    <automated>bun test src/architecture/no-raw-sql.test.ts</automated>
  </verify>
  <acceptance_criteria>Gate output matches research grep intent and contains no broad allowlist hiding route/helper/router violations.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

<task type="auto">
  <name>Task 3: Record exact proof commands in tests</name>
  <files>src/architecture/boundary.test.ts, src/architecture/no-raw-sql.test.ts</files>
  <read_first>
    - src/architecture/boundary.test.ts
    - src/architecture/no-raw-sql.test.ts
  </read_first>
  <action>Add short comments beside allowlist arrays explaining each exact temporary exception. Do not add comments that contain forbidden regex tokens unless tests filter comments or comments live outside scanned fixtures. Keep command strings in test failure messages so executors can copy exact grep gates from failing output.</action>
  <verify>
    <automated>bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts</automated>
  </verify>
  <acceptance_criteria>Architecture gate suite runs and fails only on real source violations or passes after later plans close them; no self-invalidating grep gate from test prose.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
| --- | --- |
| interface -> application | Untrusted user/API/CLI/TUI input crosses into domain behavior. |
| architecture test -> source scan | False allowlists can hide security and tenancy bypasses. |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-09.5-19-01 | Elevation of privilege | interface adapters | mitigate | Gate direct ORM/entity access so auth/tenant logic cannot bypass application entrypoints. |
| T-09.5-19-02 | Tampering | architecture allowlists | mitigate | Exact file-path allowlist with rationale; no broad directory exclusions. |
</threat_model>

<verification>
Run `bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts src/architecture/migrations.test.ts`.
</verification>

<success_criteria>
New gates cover all direct-access patterns from research and create RED failures that Plans 20-27 must close without weakening gate scope.
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-19-SUMMARY.md`.
</output>
