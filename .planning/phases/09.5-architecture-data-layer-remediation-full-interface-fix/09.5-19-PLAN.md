---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 19
type: execute
gap_closure: true
wave: 1
depends_on: ["09.5-18"]
files_modified:
  - src/architecture/boundary.test.ts
  - src/architecture/no-raw-sql.test.ts
autonomous: true
requirements: [ARCH-REMED-01, ARCH-REMED-02, ARCH-REMED-05, ARCH-REMED-06, DATA-01, DATA-02, IFACE-01, IFACE-02, IFACE-03, IFACE-04, IFACE-05, E2E-01]
must_haves:
  truths: ["Architecture tests fail on residual direct ORM/data-provider access in interface layers.", "Allowlist entries are exact files with rationale.", "Existing ProductDb/PGlite/product-kernel gates stay active."]
  artifacts:
    - path: "src/architecture/boundary.test.ts"
      provides: "Direct ORM/entity/repository import boundary"
    - path: "src/architecture/no-raw-sql.test.ts"
      provides: "Raw SQL and web data-handle boundary"
  key_links:
    - from: "src/architecture/boundary.test.ts"
      to: "src/web/src/routes src/web/src/lib src/cli src/tui src/api src/router src/trpc src/server/trpc"
      via: "source scan"
      pattern: "ctx\\.em|em\\.find|getRepository"
---

<objective>
Harden executable architecture gates for residual interface encapsulation gaps.
Purpose: make every implementation plan close real research inventory paths per D-10, D-15, D-25 through D-30, D-48, and D-72.
Output: RED-first boundary tests with exact allowlists only.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-INTERFACE-ENCAPSULATION-INTEGRATION-RESEARCH.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-ENCAPSULATION-DEPENDENCIES-RESEARCH.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add broad direct-access source gate</name>
  <files>src/architecture/boundary.test.ts</files>
  <behavior>- Fails on `ctx.em`, `em.find`, `em.findOne`, `em.create`, `em.persist`, `em.flush`, `em.transactional`, `getRepository(`, and interface imports of `db/entities`, `db/repositories`, `db.module`, or `mikro-orm.config` outside exact composition/test exceptions.</behavior>
  <action>Add RED Bun tests using research grep patterns. Filter file paths before matching content. Exclude tests/specs and exact composition files only: `src/web/src/lib/server/db.ts`, `src/trpc/context.ts`, `src/cli/index.ts`, each with rationale. No directory-wide interface/runtime allowlists.</action>
  <verify><automated>bun test src/architecture/boundary.test.ts</automated></verify>
  <done>Gate reports residual interface direct-access violations by exact path and preserves prior legacy gates.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add web data-handle and raw SQL gates</name>
  <files>src/architecture/no-raw-sql.test.ts</files>
  <behavior>- Fails on `openDatabase`, `getDatabase`, `getEm`, `getDefaultOrgIdOrm`, `ormSqlConnection`, `WebDatabaseHandle`, `LegacyDatabaseHandle`, `application-compat`, `.query(`, and raw `.execute(` in web routes/helpers and interface roots.</behavior>
  <action>Extend raw SQL architecture tests with separate gates for web data-handle aliases and generic SQL. Keep `src/web/src/lib/server/db.ts` as composition-root exception only until callers are gone. Do not exclude `src/web/src/routes/**` or domain helper files.</action>
  <verify><automated>bun test src/architecture/no-raw-sql.test.ts</automated></verify>
  <done>Gate output matches research grep intent and contains no broad allowlist hiding route/helper/router violations.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
| --- | --- |
| interface -> application | Untrusted interface input crosses into domain behavior. |
| tests -> source scan | Weak allowlists can hide privilege and tenancy bypasses. |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-09.5-19-01 | Elevation of privilege | interface adapters | mitigate | Block direct persistence/repository access outside application. |
| T-09.5-19-02 | Tampering | architecture tests | mitigate | Exact path allowlists with local rationale only. |
</threat_model>

<verification>bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts src/architecture/migrations.test.ts</verification>
<success_criteria>Plans 20-39 have executable RED gates they must close without weakening scope.</success_criteria>
<output>After completion, create `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-19-SUMMARY.md`.</output>
