---
phase: 09.5
artifact: eval-review
created: 2026-05-07
updated: 2026-05-07
state: A
overall_score: 100
verdict: PASS
critical_gap_count: 0
---

# Phase 09.5 Eval Review

## Scope

Phase 09.5 is architecture/data-boundary remediation. Evaluation substrate is deterministic:

- architecture boundary tests
- direct-access grep gates
- web route fake-provider prevention
- interface parity and focused regression tests
- Symphony SPEC lock and conformance tests
- CI tier closure evidence

## Covered

| Dimension | Status | Evidence |
| --- | --- | --- |
| Boundary/data access | COVERED | `src/architecture/boundary.test.ts`, `src/architecture/no-raw-sql.test.ts`, direct-access grep gate. |
| Interface fake-provider prevention | COVERED | Importers/connectors route-local stores removed; boundary test blocks web route fake stores. |
| Review feedback | COVERED | Unsafe FormData casts, CSV parsing, connector save coverage, and boundary regex fixed. |
| Symphony latest SPEC | COVERED | `just sync-symphony`, `.symphony-spec.lock`, `tests/symphony/spec-lock.test.ts`, `src/orchestration/__tests__/symphony-conformance.test.ts`. |
| Unit/integration CI | COVERED | `bun run ci --tier=unit` and `bun run ci --tier=integration` passed. |
| Web E2E tier | COVERED | `bun run ci --tier=e2e --domain=web` passed build, web unit, coverage, a11y, smoke, and full E2E. |

## Critical Gap

None open.

### EV-01: Full E2E Gate Repaired

`bun run ci --tier=e2e --domain=web` now includes full web E2E by default and exits 0.

Latest result:

- `web:a11y`: 19 passed, 3 skipped
- `web:e2e:smoke`: 3 passed
- `web:e2e:full`: 98 passed, 8 skipped

The run still logs non-failing 500s for older route fixture gaps; tests and CI gate pass.

## Verdict

Architecture remediation and product E2E gate pass.

## 2026-05-07 Follow-Up: Plan 58 Eval Fixes

- Interface-boundary eval improved: web/CLI/TUI scan now reports no direct ORM/business-logic hits except web composition-root type references.
- Symphony eval current: `just sync-symphony` against latest upstream SPEC produced no lock drift; spec lock and conformance tests pass.
- EV-01 fixed: full web E2E passes through the normal CI e2e tier.

## 2026-05-07 Follow-Up: Code Review Fix Eval

- Post-review boundary fix verified: `listChildren()` now applies `ctx.projectId` to child queries, with a regression test proving project-scoped callers do not receive cross-project children.
- Web CSV import/export route orchestration moved behind `src/application/tasks/csv.ts`; route files now only validate request shape, acquire application scope, call application functions, and serialize responses.
- Route-boundary eval tightened: `apps/web/src/routes/application-adapters.test.ts` now blocks CSV parser imports, route-local parsed-record loops, temp-file writes, and `Bun.file()` orchestration in runtime route surfaces.
- Code review rerun status: PASS, 0 critical, 0 warning, 0 info in `09.5-REVIEW.md`.
- Focused validation passed: task integration, web adapter, architecture boundary, raw SQL, TypeScript, boundary lint, and Symphony SPEC lock/conformance tests.

## 2026-05-07 Follow-Up: Plan 59 Repo Structure Eval

- Repo-structure research persisted to `09.5-59-REPO-STRUCTURE-RESEARCH.md`; implementation plan persisted to `09.5-59-PLAN.md`.
- State-of-the-art structure review chose incremental cleanup over an immediate `apps/`/`packages/` split because `src/services`, `src/product-kernel`, and dual tRPC roots still require domain-by-domain convergence.
- New structure guard `src/architecture/repo-structure.test.ts` protects against empty source directories and duplicate context ADR template trees.
- TDD loop verified RED on the empty SvelteKit route folder plus duplicate ADR templates, then GREEN after cleanup.
- Context docs now point to lazy context ADR directory creation from the canonical `docs/adr/0000-template.md`.
- AST-grep scans recorded deferred structural debt instead of hiding it: application-layer imports from legacy service wrappers and dual tRPC roots remain future cleanup items.
- Focused validation passed: repo-structure test, architecture boundary/raw-SQL tests, TypeScript, boundary lint, and graphify refresh.

## 2026-05-07 Follow-Up: Plan 60 Structure Standardization Eval

- State-of-art app/core layout now exists: `apps/web`, `apps/cli`, `apps/tui`, `apps/server`, with root `src` reserved for shared/core modules.
- Local-first constraint preserved: CLI/TUI use local application callers; web dev runs from `apps/web`; `fulcrum web` serves built SvelteKit output from local disk with local DB/container setup.
- Interface/server aliases make app boundaries explicit and remove path ambiguity from `src` surface folders.
- Structure regression guard now fails if runnable surfaces reappear under root `src`.
- Focused validation passed: root frozen install, web frozen install, TypeScript, Svelte check, repo-structure tests, and boundary lint.
