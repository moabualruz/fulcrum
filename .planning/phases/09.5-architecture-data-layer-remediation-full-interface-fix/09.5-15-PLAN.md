---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 15
title: Test Fixture Direct Access Closure
type: tdd
wave: 2
depends_on:
  - 09.5-14
files_modified:
  - src/**/*test.ts
  - src/web/tests/**/*.ts
  - src/test-support/**/*.ts
  - src/architecture/boundary.test.ts
autonomous: true
gap_closure: true
requirements:
  - R-1
  - R-15
  - R-22
  - R-23
must_haves:
  truths:
    - "Plan 13 global grep gates include test files; ad hoc test `openPglite` and `ProductDb` usage must disappear too."
    - "Tests use shared application/ORM fixtures, not direct product-kernel DB handles."
    - "Any remaining PGlite bootstrap for tests lives under `src/product-kernel/` or uses neutral helper names outside grep patterns."
---

<objective>
Remove direct `openPglite`, `openProductDb`, and `ProductDb` naming from tests and fixtures so Plan 13 global grep gates can pass.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-GAP-CLOSURE-RESEARCH.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-13-SUMMARY.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-14-PLAN.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 09.5-15-01: RED exact test fixture inventory</name>
  <files>src/architecture/boundary.test.ts, src/test-support/**/*.ts</files>
  <read_first>
    <file>src/architecture/boundary.test.ts</file>
    <file>src/web/tests/e2e/fixtures.ts</file>
    <file>src/web/src/routes/page.server.test.ts</file>
    <file>src/cli/interactive/init.test.ts</file>
  </read_first>
  <action>Add a test-fixture inventory test that scans `src/**/*.test.ts`, `src/**/*.spec.ts`, and `src/web/tests/**/*.ts` for `openPglite`, `openProductDb`, `getProductDb`, `ProductDb`, and product-kernel imports. The RED test must report representative current files including `src/web/tests/e2e/fixtures.ts`, `src/web/src/routes/page.server.test.ts`, `src/cli/interactive/init.test.ts`, and `src/search/query-service.test.ts`.</action>
  <verify>
    <automated>bun test src/architecture/boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Boundary test scans test/spec/e2e files, not runtime files only.</criterion>
    <criterion>RED failure names at least one web E2E fixture and one CLI/search test fixture.</criterion>
    <criterion>No allowlist entry accepts ad hoc per-file PGlite/ProductDb opens.</criterion>
  </acceptance_criteria>
  <done>Test direct-access violations are visible and exact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-15-02: GREEN shared application/ORM fixtures</name>
  <files>src/test-support/**/*.ts, src/web/tests/**/*.ts, src/**/*test.ts</files>
  <read_first>
    <file>src/db/mikro-orm.config.ts</file>
    <file>src/db/migrator-service.ts</file>
    <file>src/product-kernel/db/migrate.ts</file>
    <file>src/web/tests/e2e/fixtures.ts</file>
  </read_first>
  <action>Create or extend shared test-support helpers with neutral names such as `createIsolatedOrmFixture`, `seedDefaultOrg`, and `createApplicationCallerFixture`. Migrate test files currently importing or naming `openPglite`, `openProductDb`, `getProductDb`, or `ProductDb` to these helpers. If low-level PGlite setup is unavoidable, keep it in a product-kernel-owned helper path under `src/product-kernel/testing/` and export neutral app/ORM fixtures to tests.</action>
  <verify>
    <automated>bun test src/web/tests/vitest/cmdk-palette.test.ts src/cli/interactive/init.test.ts src/search/query-service.test.ts src/web/src/routes/page.server.test.ts</automated>
    <automated>! rg 'openPglite|openProductDb|getProductDb|ProductDb|from .*product-kernel' src --type ts | rg '\\.(test|spec)\\.ts|src/web/tests/'</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Global test grep for `openPglite|openProductDb|getProductDb|ProductDb|from .*product-kernel` returns no test/spec/e2e matches.</criterion>
    <criterion>Shared fixture helper names contain no `ProductDb`, `openProductDb`, or `openPglite` substrings outside `src/product-kernel/`.</criterion>
    <criterion>Representative migrated tests pass.</criterion>
  </acceptance_criteria>
  <done>Test fixtures no longer keep global grep gates red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-15-03: GREEN web E2E and generated tests</name>
  <files>src/web/tests/**/*.ts, tests/**/*.ts, src/cli/generated/**/*.ts</files>
  <read_first>
    <file>src/web/tests/e2e/fixtures.ts</file>
    <file>tests/cli/codegen-gate.test.ts</file>
    <file>src/cli/generated/tasks.ts</file>
  </read_first>
  <action>Migrate web E2E fixtures and generated-code assertions so they depend on application/ORM fixtures and current generated CLI snapshots. Preserve Playwright journey semantics and do not reintroduce `gotoOrSkip`. Regenerate CLI snapshots with `bun run scripts/cli/codegen.ts` if generated command fixtures change.</action>
  <verify>
    <automated>bun run scripts/ci/codegen.ts</automated>
    <automated>cd src/web && bun run web:test -- tests/vitest/cmdk-palette.test.ts</automated>
    <automated>cd src/web && FULCRUM_RUN_E2E=1 bunx playwright test tests/e2e/phase95-dashboard.spec.ts tests/e2e/phase95-doc-crud.spec.ts tests/e2e/phase95-parity.spec.ts tests/e2e/phase95-settings.spec.ts tests/e2e/phase95-sprint-workflow.spec.ts tests/e2e/phase95-task-crud.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg 'gotoOrSkip' src/web/tests/e2e/phase95-*.spec.ts src/web/tests/e2e/_smoke.spec.ts` returns no matches.</criterion>
    <criterion>Web Phase 9.5 Playwright specs pass with `FULCRUM_RUN_E2E=1`.</criterion>
    <criterion>Codegen gate passes after any generated snapshot updates.</criterion>
  </acceptance_criteria>
  <done>Web E2E/test fixture layer remains real and grep-clean.</done>
</task>
</tasks>

<rollback>
If a fixture migration makes a test depend on runtime singleton state, revert that fixture change and build an isolated ORM fixture instead. Do not mark a test as skipped to pass grep gates.
</rollback>

<threat_model>
| Threat ID | Category | Component | Mitigation |
| --- | --- | --- | --- |
| T-15-01 | Repudiation | tests | Keep tests real; no skip conversion or `gotoOrSkip`. |
| T-15-02 | Tampering | fixture helpers | Centralize low-level setup and enforce grep-clean test files. |
</threat_model>

<verification>
- `bun test src/architecture/boundary.test.ts`
- `! rg 'openPglite|openProductDb|getProductDb|ProductDb|from .*product-kernel' src --type ts | rg '\\.(test|spec)\\.ts|src/web/tests/'`
- `cd src/web && FULCRUM_RUN_E2E=1 bunx playwright test tests/e2e/phase95-dashboard.spec.ts tests/e2e/phase95-doc-crud.spec.ts tests/e2e/phase95-parity.spec.ts tests/e2e/phase95-settings.spec.ts tests/e2e/phase95-sprint-workflow.spec.ts tests/e2e/phase95-task-crud.spec.ts`
</verification>

<success_criteria>
No test/spec/E2E file contributes to Plan 13 direct-access grep failures, and key test suites still run without skips.
</success_criteria>
