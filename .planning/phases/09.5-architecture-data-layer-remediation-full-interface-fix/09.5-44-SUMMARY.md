---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 44
subsystem: cli
tags: [cli, application-layer, architecture-boundary, symphony]
requires:
  - phase: 09.5-28
    provides: admin/status application-backed routers
  - phase: 09.5-36
    provides: shared CLI local caller context
provides:
  - residual CLI command files routed through caller/application boundaries
  - documented bootstrap-only CLI init exception
  - jobs dispatch schema repair required by Symphony dispatch verification
affects: [cli, orchestration, db, architecture-boundary, phase-09.5]
tech-stack:
  added: []
  patterns:
    - CLI commands use createLocalCaller/application modules for data access
    - Bootstrap-only direct DB access remains isolated to init command rationale
key-files:
  created:
    - src/db/migrations/Migration20260507002_jobs_dispatch_project_scope.ts
  modified:
    - src/architecture/boundary.test.ts
    - src/cli/docs-templates.ts
    - src/cli/commands/auth.ts
    - src/cli/commands/flags.ts
    - src/cli/commands/project-config.ts
    - src/cli/commands/report.ts
    - src/cli/commands/task-hierarchy.ts
    - src/cli/commands/task-relate.ts
    - src/cli/commands/symphony.ts
    - src/cli/commands/pillar14-generated.ts
    - src/db/doctor-checks.ts
    - src/db/entities/jobs/Job.ts
requirements-completed: [ARCH-REMED-01, ARCH-REMED-02, DATA-01, IFACE-03, IFACE-04, IFACE-06, E2E-05]
completed: 2026-05-07
---

# Phase 09.5 Plan 44 Summary

Residual CLI direct DB/entity imports now route through the local caller/application boundary. `src/cli/commands/init.ts` remains the exact bootstrap exception because it initializes PGlite/MikroORM, runs migrations, and seeds local org state before callers exist.

## Commits

- `1f20ff0b` - `test(09.5-44): add CLI direct access boundary gate`
- `f52d4584` - `fix(09.5-44): route residual CLI commands through caller boundary`
- Orchestrator follow-up - TUI baseline removal plus jobs/events dispatch schema repair.

## Verification

- `bun run lint` - PASS
- `bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts` - PASS
- `bun test src/orchestration/__tests__/symphony-conformance.test.ts` - PASS, 85 pass
- Plan 44 owned-file direct DB/MikroORM scan - PASS per worker checkpoint

## Follow-up Fix Applied

Plan 44 uncovered schema drift in fresh migrations: `dispatchRun` wrote `jobs.project_id` and event `actor`, but the migrated schema did not provide those columns. Added `Migration20260507002_jobs_dispatch_project_scope.ts`, updated `Job` entity metadata, and bumped `MAX_KNOWN_MIGRATION_VERSION`.

## Remaining Exceptions

- `src/cli/commands/init.ts` only: bootstrap initialization boundary.

## Threat Flags

None. The migration aligns existing local dispatch persistence with the application contract; it does not add a new external surface.
