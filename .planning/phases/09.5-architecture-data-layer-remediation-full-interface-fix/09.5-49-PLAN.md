---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 49
type: execute
gap_closure: true
wave: 3
depends_on: ["09.5-46", "09.5-47", "09.5-48"]
files_modified:
  - src/application/init/queries.ts
  - src/application/init/init.integration.test.ts
  - src/application/auth/org-context.ts
  - src/cli/commands/init.ts
  - src/cli/commands/init.test.ts
  - src/web/src/lib/server/em.ts
  - src/web/src/lib/server/em.test.ts
  - .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CLOSURE-PROOF.md
  - .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-VERIFICATION.md
  - .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-UAT.md
autonomous: true
requirements: [ARCH-REMED-01, ARCH-REMED-02, ARCH-REMED-03, DATA-01, DATA-02, IFACE-01, IFACE-03, E2E-01, E2E-08]
must_haves:
  truths:
    - "CLI init and web EM helper have no Plan 27 forbidden EntityManager/repository matches"
    - "exact Plan 27 architecture grep chain passes after Plans 46-48"
    - "closure proof documents architecture chain and official CI chain outcomes"
  artifacts:
    - path: "src/application/init/queries.ts"
      provides: "application-owned init/default-org existence query"
      exports: ["hasAnyOrg"]
    - path: "src/application/auth/org-context.ts"
      provides: "application-owned default org resolver"
      exports: ["resolveOrgId"]
    - path: "src/cli/commands/init.ts"
      provides: "CLI composition bootstrap that invokes application/service helpers"
    - path: "src/web/src/lib/server/em.ts"
      provides: "web compatibility helper without direct org lookup"
    - path: ".planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CLOSURE-PROOF.md"
      provides: "updated pass/fail closure evidence"
  key_links:
    - from: "src/cli/commands/init.ts"
      to: "src/application/init/queries.ts"
      via: "hasAnyOrg(orm.em.fork())"
      pattern: "hasAnyOrg"
    - from: "src/web/src/lib/server/em.ts"
      to: "src/application/auth/org-context.ts"
      via: "resolveOrgId(manager, 'default')"
      pattern: "resolveOrgId"
---

<objective>
Close remaining CLI/web helper grep failures and rerun the exact Plan 27 closure gates.

Purpose: Plan 27 failed after architecture tests passed because `src/cli/commands/init.ts` and `src/web/src/lib/server/em.ts` still contained raw repository/entity-manager query text. This final gap plan moves that data lookup into application helpers and records closure proof.
Output: CLI/web residual cleanup plus updated proof documents.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-PATTERNS.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CLOSURE-PROOF.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-46-SUMMARY.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-47-SUMMARY.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-48-SUMMARY.md

<interfaces>
Existing contracts:
```ts
// src/application/auth/org-context.ts
export async function resolveOrgId(em: EntityManager, candidate: string | null | undefined): Promise<string>;

// src/cli/commands/init.ts
export async function run(argv?: readonly string[]): Promise<void>;

// src/web/src/lib/server/em.ts
export async function requestEntityManager(): Promise<EntityManager>;
export async function resolveDefaultOrgId(em: EntityManager): Promise<string>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move init/default-org lookups into application helpers</name>
  <files>src/application/init/queries.ts, src/application/init/init.integration.test.ts, src/application/auth/org-context.ts, src/cli/commands/init.ts, src/cli/commands/init.test.ts, src/web/src/lib/server/em.ts, src/web/src/lib/server/em.test.ts</files>
  <behavior>
    - Test 1: `hasAnyOrg(em)` returns false before seed and true after an `Org` row exists.
    - Test 2: `run()` in CLI init still prints `Local org bootstrapped` when no org existed and `Already initialized` when org existed.
    - Test 3: `resolveDefaultOrgId(manager)` in web helper delegates to application `resolveOrgId` and returns the default org id.
    - Test 4: grep finds no Plan 27 forbidden matches in `src/cli/commands/init.ts` or `src/web/src/lib/server/em.ts`.
  </behavior>
  <action>Create `src/application/init/queries.ts` with `hasAnyOrg(em: EntityManager): Promise<boolean>` using application-owned MikroORM data access. Update `src/cli/commands/init.ts` to call `hasAnyOrg(orm.em.fork())` instead of `orm.em.fork().getRepository(Org)`. Keep CLI bootstrap/migration wiring in `init.ts`; this is composition/startup, not business logic. Update `src/web/src/lib/server/em.ts` so `resolveDefaultOrgId(manager)` delegates to `resolveOrgId(manager, "default")` from `src/application/auth/org-context.ts` and removes direct `Org` import and `em.findOne`. Do not touch `src/web/src/lib/server/db.ts`, which is a Plan 27 exact composition exception.</action>
  <verify>
    <automated>bun test src/application/init/init.integration.test.ts src/cli/commands/init.test.ts src/web/src/lib/server/em.test.ts -x</automated>
    <automated>! rg -n "ctx\\.em|em\\.find|em\\.findOne|em\\.create|em\\.persist|em\\.flush|em\\.transactional|getRepository\\(" src/cli/commands/init.ts src/web/src/lib/server/em.ts</automated>
  </verify>
  <done>CLI init and web EM helper no longer contain forbidden direct EntityManager/repository text, while bootstrap/default-org behavior is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Run exact Plan 27 closure gates and update proof docs</name>
  <files>.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CLOSURE-PROOF.md, .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-VERIFICATION.md, .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-UAT.md</files>
  <action>After Tasks 46-49 implementation passes focused tests, rerun the exact Plan 27 Task 1 architecture/research closure command below without weakening grep or adding new allowlists. If it passes, run the exact Plan 27 Task 2 CI chain. Update `09.5-CLOSURE-PROOF.md`, `09.5-VERIFICATION.md`, and `09.5-UAT.md` with command, exit code, pass/fail result, and concise output. If any command fails, record failure evidence and keep Phase 10 blocked; do not claim closure.</action>
  <verify>
    <automated>bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts src/architecture/migrations.test.ts &amp;&amp; ! rg -n "ctx\\.em|em\\.find|em\\.findOne|em\\.create|em\\.persist|em\\.flush|em\\.transactional|getRepository\\(" src/web/src/routes src/web/src/lib src/cli src/tui src/api src/router src/trpc src/server/trpc -g '*.ts' -g '*.tsx' | rg -v "\\.test\\.ts|\\.spec\\.ts|/__tests__/|src/web/src/lib/server/db\\.ts|src/trpc/context\\.ts|src/cli/index\\.ts" &amp;&amp; ! rg -n "openDatabase|getDatabase|getEm|getDefaultOrgIdOrm|ormSqlConnection|WebDatabaseHandle|LegacyDatabaseHandle|application-compat" src/web/src/routes src/web/src/lib -g '*.ts' -g '*.tsx' | rg -v "\\.test\\.ts|\\.spec\\.ts|src/web/src/lib/server/db\\.ts" &amp;&amp; ! rg -n "from ['\\\"][^'\\\"]*(db/entities|db/repositories|db/db.module|db/mikro-orm.config)" src/web/src/routes src/web/src/lib src/cli src/tui src/api src/router src/trpc src/server/trpc -g '*.ts' -g '*.tsx' | rg -v "\\.test\\.ts|\\.spec\\.ts|/__tests__/|src/web/src/lib/server/db\\.ts|src/cli/index\\.ts"</automated>
    <automated>bun run ci --tier=unit &amp;&amp; bun run ci --tier=integration &amp;&amp; FULCRUM_RUN_E2E=1 bun run ci --tier=e2e &amp;&amp; FULCRUM_RUN_E2E=1 bun run ci</automated>
  </verify>
  <done>Proof docs reflect exact closure gate results. Phase 10 is unblocked only if both automated command chains pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI init -> database bootstrap | Local CLI command initializes persistent local database. |
| web server helper -> application auth | Web server helper resolves default org context for server routes. |
| proof docs -> phase status | Verification evidence drives Phase 10 blocked/unblocked status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.5-49-01 | Tampering | CLI init org detection | mitigate | Test `hasAnyOrg` before/after seed; preserve MigratorService then SeedService order. |
| T-09.5-49-02 | Information disclosure | closure proof logs | accept | Proof records command output only; no secrets expected in architecture/CI gates. |
| T-09.5-49-03 | Repudiation | Phase closure status | mitigate | Record exact command, exit code, and pass/fail evidence in proof, verification, and UAT docs. |
</threat_model>

<verification>
Focused residual checks:

```sh
bun test src/application/init/init.integration.test.ts src/cli/commands/init.test.ts src/web/src/lib/server/em.test.ts -x
! rg -n "ctx\\.em|em\\.find|em\\.findOne|em\\.create|em\\.persist|em\\.flush|em\\.transactional|getRepository\\(" src/cli/commands/init.ts src/web/src/lib/server/em.ts
```

Exact Plan 27 closure checks:

```sh
bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts src/architecture/migrations.test.ts && ! rg -n "ctx\\.em|em\\.find|em\\.findOne|em\\.create|em\\.persist|em\\.flush|em\\.transactional|getRepository\\(" src/web/src/routes src/web/src/lib src/cli src/tui src/api src/router src/trpc src/server/trpc -g '*.ts' -g '*.tsx' | rg -v "\\.test\\.ts|\\.spec\\.ts|/__tests__/|src/web/src/lib/server/db\\.ts|src/trpc/context\\.ts|src/cli/index\\.ts" && ! rg -n "openDatabase|getDatabase|getEm|getDefaultOrgIdOrm|ormSqlConnection|WebDatabaseHandle|LegacyDatabaseHandle|application-compat" src/web/src/routes src/web/src/lib -g '*.ts' -g '*.tsx' | rg -v "\\.test\\.ts|\\.spec\\.ts|src/web/src/lib/server/db\\.ts" && ! rg -n "from ['\\\"][^'\\\"]*(db/entities|db/repositories|db/db.module|db/mikro-orm.config)" src/web/src/routes src/web/src/lib src/cli src/tui src/api src/router src/trpc src/server/trpc -g '*.ts' -g '*.tsx' | rg -v "\\.test\\.ts|\\.spec\\.ts|/__tests__/|src/web/src/lib/server/db\\.ts|src/cli/index\\.ts"
bun run ci --tier=unit && bun run ci --tier=integration && FULCRUM_RUN_E2E=1 bun run ci --tier=e2e && FULCRUM_RUN_E2E=1 bun run ci
```
</verification>

<success_criteria>
All Plan 27 failed files are grep-clean, exact Plan 27 Task 1 passes, exact Plan 27 Task 2 result is recorded, and proof docs do not claim closure unless both chains pass.
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-49-SUMMARY.md`.
</output>
