---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 52
type: execute
gap_closure: true
wave: 6
depends_on: ["09.5-51"]
files_modified:
  - src/shared/memory/enums.ts
  - src/shared/routing/rule-source.ts
  - src/shared/docs/enums.ts
  - src/db/entities/memory/enums.ts
  - src/db/entities/router/RoutingRule.ts
  - src/db/entities/docs/enums.ts
  - src/application/memory/types.ts
  - src/application/routing.ts
  - src/application/docs/types.ts
  - apps/server/src/runtime/trpc/routers/memory.ts
  - apps/server/src/runtime/trpc/routers/routing.ts
  - apps/web/src/lib/components/docs/frontmatter-ui.ts
  - apps/server/src/runtime/trpc/routers/memory.test.ts
  - apps/server/src/runtime/trpc/routers/routing-adapter.test.ts
  - apps/web/src/lib/components/docs/frontmatter-ui.test.ts
  - src/architecture/boundary.test.ts
  - .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CLOSURE-PROOF.md
  - .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-VERIFICATION.md
  - .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-UAT.md
autonomous: true
requirements: [ARCH-REMED-01, ARCH-REMED-02, ARCH-REMED-03, DATA-01, DATA-02, IFACE-01, IFACE-02, IFACE-03, IFACE-04, IFACE-06, E2E-08]
must_haves:
  truths:
    - "Interface adapters and visualization helpers do not import `src/db/entities/**` for public enum/type contracts."
    - "Memory tRPC schemas get memory enum arrays from application/shared contract modules, not DB entity files."
    - "Routing tRPC schemas get `RoutingRuleSource` values from application/shared contract modules, not `RoutingRule.ts`."
    - "Docs frontmatter UI gets `DocType` from application/shared contract modules, not docs entity enums."
    - "Exact Plan 27 Task 1 final forbidden entity-import grep segment exits 0."
  artifacts:
    - path: "src/shared/memory/enums.ts"
      provides: "runtime-safe memory enum arrays and public memory enum types"
    - path: "src/shared/routing/rule-source.ts"
      provides: "runtime-safe routing rule source enum/object and union type"
    - path: "src/shared/docs/enums.ts"
      provides: "runtime-safe docs enum arrays, Zod enums, and public docs enum types"
    - path: "apps/server/src/runtime/trpc/routers/memory.ts"
      provides: "memory tRPC adapter with zero db/entities imports"
    - path: "apps/server/src/runtime/trpc/routers/routing.ts"
      provides: "routing tRPC adapter with zero db/entities imports"
    - path: "apps/web/src/lib/components/docs/frontmatter-ui.ts"
      provides: "docs visualization helper with zero db/entities imports"
  key_links:
    - from: "src/db/entities/memory/enums.ts"
      to: "src/shared/memory/enums.ts"
      via: "re-export or import-forward compatibility"
      pattern: "from .*shared/memory/enums"
    - from: "src/db/entities/router/RoutingRule.ts"
      to: "src/shared/routing/rule-source.ts"
      via: "entity enum source constant import"
      pattern: "from .*shared/routing/rule-source"
    - from: "src/db/entities/docs/enums.ts"
      to: "src/shared/docs/enums.ts"
      via: "re-export or import-forward compatibility"
      pattern: "from .*shared/docs/enums"
    - from: "Plan 27 Task 1"
      to: "forbidden entity-import grep segment"
      via: "no interface imports from db/entities"
      pattern: "db/entities"
---

<objective>
Close the current Plan 27 Task 1 final forbidden entity-import grep failure.

Purpose: the user's architecture boundary is strict: interfaces are invocation/visualization only. Public enum/type contracts used by interface adapters must be routed through application or shared modules, not imported from `src/db/entities/**`.
Output: a focused gap-closure implementation plan that moves the three public enum/type imports behind shared/application contract modules, updates boundary tests, and records proof without touching protected dirty files.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-INTERFACE-ENCAPSULATION-INTEGRATION-RESEARCH.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-51-PLAN.md
@src/architecture/boundary.test.ts
@apps/server/src/runtime/trpc/routers/memory.ts
@apps/server/src/runtime/trpc/routers/routing.ts
@apps/web/src/lib/components/docs/frontmatter-ui.ts
@src/db/entities/memory/enums.ts
@src/db/entities/router/RoutingRule.ts
@src/db/entities/docs/enums.ts

<interfaces>
Exact failing Plan 27 Task 1 final grep matches:
```text
apps/server/src/runtime/trpc/routers/memory.ts importing ../../../db/entities/memory/enums.ts for MEMORY_IMPORTANCE, MEMORY_KINDS, MEMORY_SOURCES
apps/server/src/runtime/trpc/routers/routing.ts importing ../../../db/entities/router/RoutingRule.ts for RoutingRuleSource
apps/web/src/lib/components/docs/frontmatter-ui.ts importing ../../../../../db/entities/docs/enums.ts for DocType
```

Current public contracts to preserve exactly:
```ts
// memory
export const MEMORY_KINDS = ["note", "decision", "blocker", "file_ref", "section_anchor", "link", "fact"] as const;
export const MEMORY_IMPORTANCE = ["low", "medium", "high"] as const;
export const MEMORY_SOURCES = ["heuristic", "llm", "manual"] as const;
export type MemoryKind = (typeof MEMORY_KINDS)[number];
export type MemoryImportance = (typeof MEMORY_IMPORTANCE)[number];
export type MemorySource = (typeof MEMORY_SOURCES)[number];

// routing
export enum RoutingRuleSource {
  Manual = "manual",
  Learned = "learned",
  Imported = "imported",
}
export const ROUTING_RULE_SOURCES = [
  RoutingRuleSource.Manual,
  RoutingRuleSource.Learned,
  RoutingRuleSource.Imported,
] as const;

// docs
export const DOC_TYPES = ["spec", "adr", "wiki", "runbook", "meeting", "postmortem", "rfc", "note", "scratch"] as const;
export const SCOPES = ["project", "global"] as const;
export const LINK_KINDS = ["wikilink", "task_ref", "run_ref", "mention"] as const;
export const DocTypeEnum = z.enum(DOC_TYPES);
export type DocType = (typeof DOC_TYPES)[number];
```

Forbidden final grep segment from Plan 27 Task 1:
```bash
! rg -n "from ['\"][^'\"]*(db/entities|db/repositories|db/db.module|db/mikro-orm.config)" apps/web/src/routes apps/web/src/lib apps/cli/src apps/tui/src apps/server/src/api apps/server/src/router apps/server/src/trpc apps/server/src/runtime/trpc -g '*.ts' -g '*.tsx' | rg -v "\.test\.ts|\.spec\.ts|/__tests__/|apps/web/src/lib/server/db\.ts|apps/cli/src/index\.ts"
```
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
| --- | --- | --- |
| GOAL | Interfaces are invocation/visualization only and must not import `db/entities`. | Covered by all tasks and final grep proof. |
| REQ IFACE-01/03 | Interface adapters call application/shared contracts, not database/entity modules. | Covered by Tasks 1-3. |
| REQ DATA-01/02 | Data/entity layer remains behind application/shared contracts. | Covered by shared contract extraction and entity import-forwarding. |
| RESEARCH | Hono/tRPC/SvelteKit remain thin adapters over application layer. | Covered by tRPC and docs visualization rewires. |
| CONTEXT D-10/D-13/D-25/D-26 | Application commands/queries and DTO/types are callable interface boundary; tRPC routers are thin adapters. | Covered by replacing router/UI entity imports with shared/application contract imports. |
| CONTEXT Deferred Ideas | Yjs/Hocuspocus, Effect adoption, GraphQL, multi-node delivery. | Excluded. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract public enum contracts into shared modules</name>
  <files>src/shared/memory/enums.ts, src/shared/routing/rule-source.ts, src/shared/docs/enums.ts, src/db/entities/memory/enums.ts, src/db/entities/router/RoutingRule.ts, src/db/entities/docs/enums.ts, src/application/memory/types.ts, src/application/routing.ts, src/application/docs/types.ts, src/architecture/boundary.test.ts</files>
  <behavior>
    - Shared modules export the same memory, routing, and docs enum values/types currently used by DB entities, application DTOs, tRPC routers, and web visualization helpers.
    - DB entity files continue to receive the exact same runtime values and TypeScript types from shared modules without duplicating enum literals.
    - Application modules expose public enum/type contracts without requiring interface code to import `src/db/entities/**`.
  </behavior>
  <action>Add failing boundary test coverage first by changing `EXPECTED_RESIDUAL_DIRECT_ACCESS_FILES` in `src/architecture/boundary.test.ts` from the three current files to `[]`; this must fail before implementation. Create `src/shared/memory/enums.ts`, `src/shared/routing/rule-source.ts`, and `src/shared/docs/enums.ts` with the exact current literal values. Move ownership of public enum arrays and Zod enum schemas into shared modules; update DB entity enum files to import or re-export from shared modules for backward-compatible internal DB imports. Update `src/application/memory/types.ts`, `src/application/routing.ts`, and `src/application/docs/types.ts` to import public types/constants from shared modules instead of DB entity enum files where possible. Keep DB entity classes inside `src/db/entities/**`; only public enum/type contracts move. Do not edit `AGENTS.md` or `src/db/migrations/.snapshot-postgres.json`.</action>
  <verify>
    <automated>bun test src/architecture/boundary.test.ts -- -x</automated>
  </verify>
  <done>Shared contract modules exist, entity/application code uses them, and architecture boundary expected residual list no longer contains the three interface files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire memory and routing tRPC schemas through contracts</name>
  <files>apps/server/src/runtime/trpc/routers/memory.ts, apps/server/src/runtime/trpc/routers/routing.ts, apps/server/src/runtime/trpc/routers/memory.test.ts, apps/server/src/runtime/trpc/routers/routing-adapter.test.ts</files>
  <behavior>
    - `memoryRouter` imports `MEMORY_KINDS`, `MEMORY_IMPORTANCE`, and `MEMORY_SOURCES` from an application/shared contract module, not `../../../db/entities/memory/enums.ts`.
    - `routingRouter` imports `RoutingRuleSource` or equivalent runtime values from an application/shared contract module, not `../../../db/entities/router/RoutingRule.ts`.
    - Existing tRPC input/output validation and defaults remain byte-for-byte equivalent at the API boundary: memory kinds/importances/sources and routing rule source values stay unchanged.
  </behavior>
  <action>Add tests first in `memory.test.ts` and `routing-adapter.test.ts` that import the router source text or use existing module behavior to assert no `db/entities` import remains while enum validation still accepts valid values and rejects invalid values. Update `apps/server/src/runtime/trpc/routers/memory.ts` to import memory contract constants from `src/shared/memory/enums.ts` or an application re-export. Update `apps/server/src/runtime/trpc/routers/routing.ts` to import routing source constants from `src/shared/routing/rule-source.ts` or an application re-export. Preserve all permission metadata, app error mapping, `requireTrpcEntityManager(ctx)`, and procedure shapes; this task changes contract imports only.</action>
  <verify>
    <automated>bun test apps/server/src/runtime/trpc/routers/memory.test.ts apps/server/src/runtime/trpc/routers/routing-adapter.test.ts src/architecture/boundary.test.ts -- -x &amp;&amp; ! rg -n "from ['\"][^'\"]*db/entities" apps/server/src/runtime/trpc/routers/memory.ts apps/server/src/runtime/trpc/routers/routing.ts</automated>
  </verify>
  <done>Memory and routing tRPC routers have zero `db/entities` imports and their enum schema behavior remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewire docs frontmatter UI and record final grep proof</name>
  <files>apps/web/src/lib/components/docs/frontmatter-ui.ts, apps/web/src/lib/components/docs/frontmatter-ui.test.ts, .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CLOSURE-PROOF.md, .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-VERIFICATION.md, .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-UAT.md</files>
  <behavior>
    - `frontmatter-ui.ts` imports `DocType` from a shared/application docs contract module, not `../../../../../db/entities/docs/enums.ts`.
    - Frontmatter field derivation, validation, YAML parsing, and unknown-key preservation keep current behavior.
    - Exact Plan 27 Task 1 final forbidden entity-import grep segment passes with no allowlist weakening.
  </behavior>
  <action>Add a failing assertion to `frontmatter-ui.test.ts` that reads `frontmatter-ui.ts` and rejects any `db/entities` import, while keeping existing frontmatter behavior tests. Update `frontmatter-ui.ts` to import `DocType` from `src/shared/docs/enums.ts` or `src/application/docs/types.ts`. Run architecture tests and the exact Plan 27 Task 1 final grep segment. Append command, exit code, and concise output to `09.5-CLOSURE-PROOF.md`; update `09.5-VERIFICATION.md` and `09.5-UAT.md` only with passing evidence. Do not edit `AGENTS.md`, `src/db/migrations/.snapshot-postgres.json`, or any migration snapshot.</action>
  <verify>
    <automated>bun test apps/web/src/lib/components/docs/frontmatter-ui.test.ts src/architecture/boundary.test.ts -- -x &amp;&amp; ! rg -n "from ['\"][^'\"]*(db/entities|db/repositories|db/db.module|db/mikro-orm.config)" apps/web/src/routes apps/web/src/lib apps/cli/src apps/tui/src apps/server/src/api apps/server/src/router apps/server/src/trpc apps/server/src/runtime/trpc -g '*.ts' -g '*.tsx' | rg -v "\.test\.ts|\.spec\.ts|/__tests__/|apps/web/src/lib/server/db\.ts|apps/cli/src/index\.ts"</automated>
  </verify>
  <done>Docs frontmatter UI has zero `db/entities` imports, existing frontmatter tests pass, final forbidden entity-import grep returns no matches, and proof docs record passing evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| interface adapter -> public contracts | tRPC/web visualization code consumes runtime enum values and TypeScript types without crossing into DB entities. |
| DB entity -> shared contracts | Entity decorators consume shared enum values while preserving database mapping. |
| command evidence -> closure docs | Test/grep output determines Phase 09.5 closure status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-09.5-52-01 | Tampering | enum contract extraction | mitigate | Single shared module per domain owns literals; DB files import/re-export instead of duplicating values. |
| T-09.5-52-02 | Information Disclosure | interface imports | mitigate | Boundary test and final grep reject `db/entities` imports from interface roots. |
| T-09.5-52-03 | Denial of Service | schema behavior drift | mitigate | Router/frontmatter tests prove enum validation and UI behavior remain unchanged after import rewires. |
| T-09.5-52-04 | Repudiation | proof docs | mitigate | Record exact commands, exit codes, and concise output before updating verification/UAT status. |
</threat_model>

<verification>
Run all task-level automated commands, then rerun exact Plan 27 Task 1. Success requires the final forbidden entity-import grep segment to exit 0 without changing allowlists to hide the three current offenders.
</verification>

<success_criteria>
`apps/server/src/runtime/trpc/routers/memory.ts`, `apps/server/src/runtime/trpc/routers/routing.ts`, and `apps/web/src/lib/components/docs/frontmatter-ui.ts` have no `db/entities` imports; shared/application contracts own public enum/type access; architecture tests pass with no expected residual entity-import files; exact Plan 27 Task 1 final forbidden entity-import grep segment passes; proof docs record passing evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-52-SUMMARY.md`.
</output>
