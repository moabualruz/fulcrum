---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 21
type: execute
gap_closure: true
wave: 2
depends_on: ["09.5-19"]
files_modified:
  - src/application/docs/commands.ts
  - src/application/docs/queries.ts
  - src/application/docs/types.ts
  - src/application/notifications/commands.ts
  - src/application/notifications/queries.ts
  - src/application/notifications/types.ts
  - src/application/reports/commands.ts
  - src/application/reports/queries.ts
  - src/application/reports/types.ts
  - src/application/skills/commands.ts
  - src/application/skills/queries.ts
  - src/application/skills/types.ts
  - src/application/telemetry/commands.ts
  - src/server/trpc/routers/docs.ts
  - src/server/trpc/routers/reports.ts
  - src/server/trpc/routers/skills.ts
  - src/server/trpc/routers/telemetry.ts
  - src/trpc/routers/notifications.ts
  - src/trpc/routers/reports.ts
autonomous: true
requirements: [R-1, R-5, R-9, R-10, R-13, R-20, R-24]
must_haves:
  truths:
    - "Telemetry, skills, reports, notifications, and docs router operations delegate to application modules."
    - "Report aggregate reads use QueryBuilder/repositories in application, not `em.getConnection().execute` in routers."
    - "Notification and doc mutations emit events through application/outbox path, not interface code."
  artifacts:
    - path: "src/application/skills/queries.ts"
      provides: "Skill read/query behavior for routers and interfaces"
    - path: "src/application/reports/queries.ts"
      provides: "Report aggregation without raw SQL in routers"
    - path: "src/trpc/routers/notifications.ts"
      provides: "Thin notifications tRPC adapter"
  key_links:
    - from: "src/trpc/routers/reports.ts"
      to: "src/application/reports/queries.ts"
      via: "report procedures delegate"
      pattern: "reports.*queries"
    - from: "src/trpc/routers/notifications.ts"
      to: "src/application/notifications"
      via: "notification procedures delegate"
      pattern: "notifications"
---

<objective>
Consolidate remaining high-impact tRPC/server router domains: telemetry, skills, reports, notifications, and docs.

Purpose: remove business logic/data providing from router layers and close R-5/R-9/R-20 for domains explicitly named in research.
Output: thin router adapters over application commands/queries, with raw report SQL moved to application QueryBuilder/repository code.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-INTERFACE-PARITY-PLATFORM-RESEARCH.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-ENCAPSULATION-DEPENDENCIES-RESEARCH.md
@src/server/trpc/routers/tasks.ts
@src/api/routes/kernel-tasks.ts
@src/application/outbox.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move reports and telemetry router logic into application</name>
  <files>src/application/reports/commands.ts, src/application/reports/queries.ts, src/application/reports/types.ts, src/application/telemetry/commands.ts, src/server/trpc/routers/reports.ts, src/server/trpc/routers/telemetry.ts, src/trpc/routers/reports.ts</files>
  <read_first>
    - src/server/trpc/routers/reports.ts
    - src/server/trpc/routers/telemetry.ts
    - src/trpc/routers/reports.ts
    - src/application/reports/queries.ts
    - src/application/telemetry/commands.ts
  </read_first>
  <behavior>
    - Reports procedures preserve current response fields and chart DTOs.
    - Telemetry write/read paths use application telemetry command/query functions.
    - `src/trpc/routers/reports.ts` contains no `em.getConnection().execute`, raw SQL string assembly, or report business branching.
  </behavior>
  <action>Move report aggregate semantics from tRPC routers into `src/application/reports/queries.ts` using MikroORM QueryBuilder/repository APIs only. Keep DTO types in `types.ts`. Update both server and canonical tRPC reports routers to delegate. Ensure telemetry router uses `src/application/telemetry/commands.ts` or new query exports instead of direct persistence.</action>
  <verify>
    <automated>bun test src/application/reports/reports.integration.test.ts src/server/trpc/routers tests/trpc/router.test.ts src/architecture/no-raw-sql.test.ts</automated>
  </verify>
  <acceptance_criteria>`rg -n "getConnection\\(\\)\\.execute|\\.query\\(|em\\.(find|findOne|create|persist|flush|transactional)" src/server/trpc/routers/reports.ts src/trpc/routers/reports.ts src/server/trpc/routers/telemetry.ts` returns zero matches except exact application delegate handoff allowed by gate.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move skills and docs router behavior into application modules</name>
  <files>src/application/docs/commands.ts, src/application/docs/queries.ts, src/application/docs/types.ts, src/application/skills/commands.ts, src/application/skills/queries.ts, src/application/skills/types.ts, src/server/trpc/routers/docs.ts, src/server/trpc/routers/skills.ts</files>
  <read_first>
    - src/server/trpc/routers/docs.ts
    - src/server/trpc/routers/skills.ts
    - src/application/docs/commands.ts
    - src/application/docs/queries.ts
    - src/server/trpc/routers/__tests__/skills.test.ts
    - src/server/trpc/routers/__tests__/skills-registry.test.ts
  </read_first>
  <behavior>
    - Docs CRUD/version/template behavior remains equivalent and returns application DTOs.
    - Skills registry/list/install/conflict queries run through application `skills` commands/queries.
  </behavior>
  <action>Create `src/application/skills/{commands,queries,types}.ts` if absent and move router-local skill business logic there. Refactor docs and skills routers to adapter-only code: permission metadata, schema validation, application call, error mapping. Do not move UI formatting into application; DTOs stay transport-neutral.</action>
  <verify>
    <automated>bun test src/application/docs/docs.integration.test.ts src/server/trpc/routers/__tests__/skills.test.ts src/server/trpc/routers/__tests__/skills-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>Docs/skills routers contain no DB entity/repository imports and no direct EntityManager operations beyond app delegate handoff if allowed.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Move notifications router behavior behind application notifications</name>
  <files>src/application/notifications/commands.ts, src/application/notifications/queries.ts, src/application/notifications/types.ts, src/trpc/routers/notifications.ts</files>
  <read_first>
    - src/trpc/routers/notifications.ts
    - src/application/notifications/commands.ts
    - src/application/notifications/queries.ts
    - src/application/outbox.ts
  </read_first>
  <behavior>
    - Notification list/count/mark-read/rule/channel operations share application query/command path.
    - Mutations use outbox/event path when domain events are produced.
  </behavior>
  <action>Refactor `src/trpc/routers/notifications.ts` so every procedure delegates to `src/application/notifications/*`. Put unread count, rules, channels, deliveries, and mark-read behavior in application. Preserve response shapes expected by web bell, CLI, TUI, and existing tests.</action>
  <verify>
    <automated>bun test src/application/notifications/notifications.integration.test.ts src/tui/screens/notifications-audit.test.ts tests/trpc/router.test.ts src/architecture/boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>`src/trpc/routers/notifications.ts` is adapter-only and has no entity/repository/db imports or direct ORM calls.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
| --- | --- |
| tRPC -> reports/notifications/docs/skills | Authenticated interface calls sensitive project/org data. |
| application -> outbox | Mutations must emit events once. |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-09.5-21-01 | Information disclosure | reports/audit-like queries | mitigate | Tenant filters live in application queries and are covered by integration tests. |
| T-09.5-21-02 | Repudiation | telemetry/notifications/docs mutations | mitigate | Application commands produce event/outbox records where domain events exist. |
</threat_model>

<verification>
Run `bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts src/application/reports/reports.integration.test.ts src/application/notifications/notifications.integration.test.ts`.
</verification>

<success_criteria>
Telemetry, skills, reports, notifications, and docs routers are thin adapters and Plan 19 gates no longer report these files.
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-21-SUMMARY.md`.
</output>
