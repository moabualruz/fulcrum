---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 09
subsystem: application
tags: [tasks, outbox, hono, authorization, tdd]

requires:
  - phase: 09.5-01
    provides: task application command/query boundary
  - phase: 09.5-02
    provides: transactional domain outbox entity and writer
provides:
  - Per-event outbox identity preventing legitimate repeated task events from being dropped
  - Project-scoped task list/get/update/delete/parent/dependency authorization at application boundary
  - REST task create field preservation for accepted application fields
affects: [tasks, api, application-layer, outbox, phase-09.5]

tech-stack:
  added: []
  patterns:
    - TDD red/green commits per blocker
    - Project-scoped not-found behavior for hidden same-org tasks
    - Per-event outbox keys with persisted eventKey dispatch

key-files:
  created:
    - .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/deferred-items.md
  modified:
    - src/application/outbox.ts
    - src/application/outbox.test.ts
    - src/application/tasks/commands.ts
    - src/application/tasks/queries.ts
    - src/application/tasks/tasks.integration.test.ts
    - src/api/routes/kernel-tasks.ts
    - src/api/routes/kernel-tasks.test.ts

key-decisions:
  - "Outbox eventKey now defaults to crypto.randomUUID() per event; producer idempotency must be explicit in a future key if needed."
  - "Project-scoped application contexts hide same-org tasks outside the active project with AppNotFoundError."
  - "REST task create forwards all fields accepted by the application create schema instead of silently dropping them."

patterns-established:
  - "Task dependency replacement removes existing reverse edges to the target before applying new blocks/blocked_by edges."
  - "Project-scoped task mutations query only tasks visible to ctx.projectId."

requirements-completed:
  - ARCH-REMED-01..12
  - DATA-01..08
  - API-DEC-01..06
  - IFACE-01..08

duration: 6min
completed: 2026-05-06
---

# Phase 09.5 Plan 09: Application Correctness And Task Authorization Gap Closure Summary

**Outbox repeat events, project-scoped task authorization, dependency removal, and REST create field preservation closed with failing-first tests**

## Performance

- **Duration:** 6min
- **Started:** 2026-05-06T20:38:24Z
- **Completed:** 2026-05-06T20:44:03Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments

- Closed CR-01 by generating per-event outbox keys and preserving persisted event keys during dispatch.
- Closed CR-02, CR-03, and WR-02 by enforcing project scope in task visibility/list/mutation paths and removing stale reverse dependency edges.
- Closed WR-01 by forwarding `descriptionText`, `tiptapContent`, `points`, and `assigneeId` from REST create to the application command.

## Task Commits

1. **Task 09.5-09-01 RED: outbox repeat-event test** - `ecb0d5ef` (test)
2. **Task 09.5-09-01 GREEN: outbox repeat events** - `27f64849` (feat)
3. **Task 09.5-09-02 RED: task scope/dependency tests** - `e6a03c89` (test)
4. **Task 09.5-09-02 GREEN: project scope and dependency removal** - `dcf0c3d8` (feat)
5. **Task 09.5-09-03 RED: REST field preservation test** - `2284b15e` (test)
6. **Task 09.5-09-03 GREEN: REST create forwarding** - `9b9fe02a` (feat)

**Plan metadata:** pending summary commit.

## Files Created/Modified

- `src/application/outbox.ts` - Uses `crypto.randomUUID()` for per-event outbox keys and dispatches rows with persisted event keys.
- `src/application/outbox.test.ts` - CR-01 proof that repeated same-subject events produce and dispatch two rows.
- `src/application/tasks/commands.ts` - Project-scoped dependency/bulk mutation visibility and reverse-edge replacement fix.
- `src/application/tasks/queries.ts` - Project-scoped list/get visibility with hidden tasks returned as not found.
- `src/application/tasks/tasks.integration.test.ts` - CR-02, CR-03, and WR-02 integration tests.
- `src/api/routes/kernel-tasks.ts` - REST create forwards all accepted task fields.
- `src/api/routes/kernel-tasks.test.ts` - WR-01 adapter field preservation test.
- `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/deferred-items.md` - Out-of-scope concurrent CI typecheck failure.

## Decisions Made

- Used generated per-event UUID keys instead of an implicit subject-derived idempotency policy because subject-derived keys caused event loss.
- Returned `AppNotFoundError` for hidden same-org project tasks to avoid cross-project data disclosure.
- Kept REST request schema broad and forwarded accepted fields rather than shrinking the public request shape.

## TDD Gate Compliance

- RED commits present before GREEN commits for all three blockers: `ecb0d5ef`, `e6a03c89`, `2284b15e`.
- GREEN commits follow their RED commits: `27f64849`, `dcf0c3d8`, `9b9fe02a`.
- No refactor commit needed; cleanup was contained in GREEN changes.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Seeded project rows through test PGlite fixture**
- **Found during:** Task 09.5-09-02
- **Issue:** `Project` entity exists but is not registered in the test ORM metadata; project-scoped task tests hit FK setup failures before exercising CR-03/WR-02.
- **Fix:** Seeded the required `projects` rows through the existing `TestOrm.pglite` fixture in the RED test setup.
- **Files modified:** `src/application/tasks/tasks.integration.test.ts`
- **Verification:** `bun test src/application/tasks/tasks.integration.test.ts` failed for intended CR-02/CR-03/WR-02 assertions before GREEN and passed after implementation.
- **Committed in:** `e6a03c89`

**2. [Rule 2 - Missing Critical] Scoped bulk task mutation helper**
- **Found during:** Task 09.5-09-02
- **Issue:** The same project-scoped mutation boundary applied to bulk task helpers in `commands.ts`; leaving it unscoped would preserve a parallel bypass.
- **Fix:** Added `ctx.projectId` filtering to `findBulkTasksOrThrow()`.
- **Files modified:** `src/application/tasks/commands.ts`
- **Verification:** `bun test src/application/tasks/tasks.integration.test.ts`
- **Committed in:** `dcf0c3d8`

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 missing critical).
**Impact on plan:** Both were required to make the planned tests meaningful and keep task mutation authorization consistent.

## Issues Encountered

- `bun run ci --tier=unit --domain=application` and `bun run ci --tier=integration --domain=api` failed during shared typecheck on concurrent, out-of-scope edits in `src/application/search/queries.ts`. Documented in `deferred-items.md`; 09.5-09 files were not changed to address another agent's ownership.

## Verification

- `bun test src/application/outbox.test.ts` - passed, 4 tests.
- `bun test src/application/tasks/tasks.integration.test.ts` - passed, 7 tests.
- `bun test src/api/routes/kernel-tasks.test.ts` - passed, 3 tests.
- `bun test src/api/routes/kernel-tasks.test.ts src/application/tasks/tasks.integration.test.ts src/application/outbox.test.ts` - passed, 14 tests.
- `rg 'crypto\\.randomUUID|idempotencyKey' src/application/outbox.ts` - matched `crypto.randomUUID`.
- `rg 'eventKey: `\\$\\{input\\.verb\\}:\\$\\{input\\.subjectKind\\}' src/application/outbox.ts` - no matches.
- `rg 'descriptionText|tiptapContent|points|assigneeId' src/api/routes/kernel-tasks.ts` - matched forwarded fields.
- `rg 'ctx\\.projectId' src/application/tasks/queries.ts src/application/tasks/commands.ts` - matched project-scope guards.
- `bun run ci --tier=unit --domain=application` - failed before domain tests due out-of-scope `src/application/search/queries.ts` type errors.
- `bun run ci --tier=integration --domain=api` - failed before domain tests due same out-of-scope type errors.

## Known Stubs

None. Stub scan found only normal test/setup nulls and empty arrays, not user-facing placeholders.

## Threat Flags

None. Changes stayed inside planned task/outbox/API trust boundaries from T-09-01 through T-09-04.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

CR-01, CR-02, CR-03, WR-01, and WR-02 are closed in 09.5-09 ownership. Remaining CI progress depends on concurrent plan cleanup for `src/application/search/queries.ts` type errors.

## Self-Check: PASSED

- Summary file exists at `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-09-SUMMARY.md`.
- Deferred item file exists at `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/deferred-items.md`.
- Task commits found: `ecb0d5ef`, `27f64849`, `e6a03c89`, `dcf0c3d8`, `2284b15e`, `9b9fe02a`.
- Key changed files exist on disk.
- Focused tests passed; domain CI typecheck failure is documented as out-of-scope concurrent work.

---
*Phase: 09.5-architecture-data-layer-remediation-full-interface-fix*
*Completed: 2026-05-06*
