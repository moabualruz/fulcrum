---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 36
subsystem: cli
tags: [cli, trpc, application-layer, architecture-boundary]
requires:
  - phase: 09.5-20
    provides: application session-context service used as the basis for CLI/TUI caller context
  - phase: 09.5-35
    provides: preceding interface boundary remediation baseline
provides:
  - shared CLI/TUI local caller context in application layer
  - CLI command-local DB/session lookup removal for docs, memory, routing, search, and skills
  - product init database readiness moved behind application helper
affects: [cli, tui, trpc, architecture-boundary, phase-09.5]
tech-stack:
  added: []
  patterns:
    - CLI commands call createLocalCaller or application helpers and keep formatting only
    - active local session lookup centralized under src/application/cli-tui
key-files:
  created:
    - src/application/cli-tui/caller-context.ts
  modified:
    - src/cli/local-caller.ts
    - src/cli/product.ts
    - src/cli/commands/routing.ts
    - src/cli/commands/search.ts
    - src/cli/commands/skills.ts
    - src/cli/commands/docs.ts
    - src/cli/commands/memory.ts
key-decisions:
  - "Keep createLocalCaller as the CLI public bridge while moving EntityManager/session construction into src/application/cli-tui/caller-context.ts."
  - "Leave architecture boundary baseline updates to the orchestrator per concurrent-wave ownership rules."
patterns-established:
  - "Command files resolve their domain caller through src/cli/local-caller.ts instead of constructing tRPC context locally."
  - "Local product readiness is application-owned, with CLI product.ts preserving output and flag behavior."
requirements-completed: [ARCH-REMED-01, ARCH-REMED-02, DATA-01, IFACE-03, IFACE-04, IFACE-06, E2E-05]
duration: 6m04s
completed: 2026-05-07T03:21:29Z
---

# Phase 09.5 Plan 36: CLI Data Access Remediation Summary

**CLI residual data/session access moved behind a shared application local-caller context while command output contracts stayed stable.**

## Performance

- **Duration:** 6m04s
- **Started:** 2026-05-07T03:15:25Z
- **Completed:** 2026-05-07T03:21:29Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments

- Added `src/application/cli-tui/caller-context.ts` as the shared application-owned context/session/caller factory for local CLI/TUI-style invocation.
- Simplified `src/cli/local-caller.ts` to only delegate to the application caller factory.
- Moved `fulcrum product init` PGlite/product migration readiness behind the application helper while preserving JSON shape.
- Removed command-local EntityManager/session/tRPC context construction from docs, memory, routing, search, and skills CLI command files.

## Task Commits

1. **Task 1: Share caller context and remove legacy CLI PGlite/store paths** - `af5a383b` (`feat`)
2. **Task 2: Route CLI domain commands through caller/application** - `33f299b6` (`feat`)

## Files Created/Modified

- `src/application/cli-tui/caller-context.ts` - Shared application local-caller context, active session lookup, and product readiness helper.
- `src/cli/local-caller.ts` - Thin CLI bridge delegating to application caller context.
- `src/cli/product.ts` - Product init delegates database readiness to application helper.
- `src/cli/commands/routing.ts` - Domain command resolver delegates through `createLocalCaller`.
- `src/cli/commands/search.ts` - Domain command resolver delegates through `createLocalCaller`.
- `src/cli/commands/skills.ts` - Domain command resolver delegates through `createLocalCaller`.
- `src/cli/commands/docs.ts` - Domain command resolver delegates through `createLocalCaller`.
- `src/cli/commands/memory.ts` - Domain command resolver delegates through `createLocalCaller`; digest session/EM context comes from application helper.

## Decisions Made

- Kept `createLocalCaller()` as the public CLI API so existing command modules and tests did not need call-site churn.
- Centralized CLI active session lookup in `src/application/cli-tui/caller-context.ts`, not in command files.
- Did not edit architecture boundary baseline tests. Baseline removals are listed below for orchestrator integration.

## Deviations from Plan

None - plan executed as scoped. The only red verification is the intentionally deferred architecture baseline update.

**Total deviations:** 0 auto-fixed.
**Impact on plan:** CLI code is remediated; boundary baseline needs orchestrator-owned update.

## Verification

- `bun run lint` - PASS
- `bun test src/cli/e2e/cli-smoke.test.ts src/cli/e2e/cli-workflows.test.ts src/cli/e2e/cli-errors.test.ts src/cli/e2e/cli-settings.test.ts src/cli/__tests__/phase08-cli-parity.test.ts` - PASS, 26 passed
- `bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts` - FAIL, 9 passed / 1 failed due expected residual baseline entries no longer present

## Architecture Baseline Removals Needed

`src/architecture/boundary.test.ts` `EXPECTED_RESIDUAL_DIRECT_ACCESS_FILES` should remove:

- `src/cli/commands/docs.ts`
- `src/cli/commands/memory.ts`
- `src/cli/commands/routing.ts`
- `src/cli/commands/search.ts`
- `src/cli/commands/skills.ts`
- `src/cli/local-caller.ts`
- `src/router/conflict-detector.ts`
- `src/router/no-match-prompt.ts`
- `src/router/rules-engine.ts`
- `src/router/telemetry.ts`

The first six are from this plan. The four `src/router/*` removals appear from concurrent worker changes and were not edited or staged by this plan.

## Known Stubs

None.

## Threat Flags

None. No new network endpoint, auth path, file access boundary, or schema boundary was introduced. Existing local CLI session lookup moved from CLI files into application code.

## Issues Encountered

- Architecture boundary test is now stale relative to remediated files. Per prompt, baseline tests were not edited.
- Concurrent dirty files existed outside this plan scope and were not staged or modified by this plan.

## User Setup Required

None.

## Next Phase Readiness

CLI residual direct data access in the owned files is moved behind application/tRPC caller paths. Orchestrator should update the architecture baseline list, then rerun the architecture suite.

## Self-Check: PASSED

- Created summary file exists.
- Task commits found: `af5a383b`, `33f299b6`.
- Owned changed files exist.

---
*Phase: 09.5-architecture-data-layer-remediation-full-interface-fix*
*Completed: 2026-05-07*
