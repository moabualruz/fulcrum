---
phase: 9.5
plan: 05
title: CLI And TUI Application Parity
type: tdd
wave: 3
depends_on:
  - 09.5-04
files_modified:
  - src/cli/import.ts
  - src/cli/export.ts
  - src/cli/sprints.ts
  - src/cli/artifact.ts
  - src/cli/connectors.ts
  - src/cli/notify.ts
  - src/cli/settings.ts
  - src/cli/product.ts
  - src/tui/index.ts
  - src/tui/screens/settings-screens.ts
  - src/tui/screens/sprints.test.ts
  - src/tui/e2e/tui-screen-smoke.test.ts
autonomous: true
requirements:
  - IFACE-01..08
  - E2E-01..08
  - ARCH-REMED-01..12
requirements_addressed:
  - R-3
  - R-4
  - R-21
  - R-23
must_haves:
  truths:
    - D-28
    - D-29
    - D-58
    - D-59
    - D-60
    - D-61
    - D-62
    - D-68
    - D-70
    - D-71
---

<objective>
Move CLI and TUI data workflows onto `createLocalCaller()` / `buildCaller()` backed by application services while preserving command signatures, stdout/stderr, exit codes, `--json`, and screen behavior.
</objective>

<threat_model>
- T-05-01: CLI output changes break scripts. Mitigation: snapshot and JSON-shape regression tests for every migrated command.
- T-05-02: TUI screens render stale/fake data after direct enrichment removal. Mitigation: per-screen tests inject application-backed caller fixtures.
- Block on: high.
</threat_model>

<tasks>
<task id="09.5-05-01" type="tdd">
  <read_first>
    <file>09.5-UI-SPEC.md</file>
    <file>src/cli/import.ts</file>
    <file>src/cli/sprints.ts</file>
    <file>src/cli/artifact.ts</file>
    <file>src/cli/local-caller.ts</file>
  </read_first>
  <action>Add RED CLI integration tests for `import`, `export`, `sprints`, `artifact`, `connectors`, `notify`, and new `settings` command. Each test asserts no PGlite/ProductDb path, stable exit codes 0/1/2, stable stdout, and JSON object/array shape under `--json`.</action>
  <acceptance_criteria>
    <criterion>`src/cli/settings.test.ts` contains `settings list --json`, `settings get`, and `settings set` cases.</criterion>
    <criterion>CLI tests assert exit code 2 for invalid arguments and exit code 1 for missing resources.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-05-02" type="execute">
  <read_first>
    <file>src/cli/local-caller.ts</file>
    <file>src/cli/import.ts</file>
    <file>src/cli/export.ts</file>
    <file>src/cli/sprints.ts</file>
    <file>src/cli/artifact.ts</file>
    <file>src/cli/connectors.ts</file>
    <file>src/cli/notify.ts</file>
  </read_first>
  <action>Rewire migrated CLI commands to use `createLocalCaller()` and tRPC/application procedures. Add `src/cli/settings.ts` and register `fulcrum settings list|get|set` through the same caller path. Remove `openPglite`, product-kernel `runMigrations`, and product-kernel store imports from these command files. Preserve all existing flags and output names.</action>
  <acceptance_criteria>
    <criterion>`rg 'openPglite|runMigrations|ProductDb|from .*product-kernel' src/cli/import.ts src/cli/export.ts src/cli/sprints.ts src/cli/artifact.ts src/cli/connectors.ts src/cli/notify.ts src/cli/settings.ts` returns zero matches.</criterion>
    <criterion>`bun run src/index.ts settings --help` prints usage containing `list`, `get`, and `set`.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-05-03" type="tdd">
  <read_first>
    <file>src/tui/index.ts</file>
    <file>src/tui/screen-registry.ts</file>
    <file>src/tui/screens/settings-screens.ts</file>
    <file>09.5-UAT-BASELINE.md</file>
  </read_first>
  <action>Add TUI tests for all 9 TUI screens: auth, sprints, connectors, audit, task-calendar, i18n-screen, theme, skills, settings-screens. Tests must provide application/tRPC caller fixtures and fail if screens import product-kernel or use direct EntityManager reads for display data.</action>
  <acceptance_criteria>
    <criterion>`src/tui/e2e/tui-screen-smoke.test.ts` enumerates all 9 screens: auth, sprints, connectors, audit, task-calendar, i18n-screen, theme, skills, settings-screens.</criterion>
    <criterion>`src/tui/screens/settings-screens.test.ts` asserts data comes from caller fixture.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-05-04" type="execute">
  <read_first>
    <file>src/tui/index.ts</file>
    <file>src/tui/screens/settings-screens.ts</file>
    <file>src/tui/screens/search.ts</file>
  </read_first>
  <action>Remove direct DB enrichment for TUI data display. `buildCaller()` remains the entry point; `enrichTuiCaller()` direct `em.findOne` paths move to application/tRPC queries for auth/status/profile data. Settings screens use connectors/settings application routers, not product-kernel types or stores. Also migrate `src/cli/product.ts` to application-backed caller path — it imports product-kernel repositories per CONTEXT violation inventory.</action>
  <acceptance_criteria>
    <criterion>`rg 'from .*product-kernel|em\\.find|em\\.persist|em\\.flush' src/tui --type ts | grep -v test` returns zero matches except explicitly documented telemetry adapter if still allowed by architecture test.</criterion>
    <criterion>`bun test src/tui` exits 0.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-05-05" type="tdd">
  <read_first>
    <file>src/cli/index.ts</file>
    <file>src/cli/import.ts</file>
    <file>src/cli/export.ts</file>
    <file>src/cli/sprints.ts</file>
    <file>src/cli/artifact.ts</file>
    <file>src/cli/connectors.ts</file>
    <file>src/cli/notify.ts</file>
    <file>src/cli/settings.ts</file>
    <file>src/cli/product.ts</file>
  </read_first>
  <action>Add CLI command signature regression tests (D-61). For every existing CLI flag, argument, and subcommand, write a test asserting `--help` output contains the expected flags and that invalid flags produce exit code 2. This prevents silent signature drift during migration.</action>
  <acceptance_criteria>
    <criterion>`src/cli/e2e/cli-signature-regression.test.ts` covers every CLI subcommand help output.</criterion>
    <criterion>All 20 CLI tests pass: `bun test src/cli --reporter=verbose 2>&1 | grep -c 'pass'` returns at least 20 (D-58).</criterion>
    <criterion>All 12 TUI tests pass: `bun test src/tui --reporter=verbose 2>&1 | grep -c 'pass'` returns at least 12 (D-59).</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `bun test src/cli src/tui`
- `bun run ci --tier=unit --domain=cli`
- `bun run ci --tier=unit --domain=tui`
</verification>

<success_criteria>
- CLI and TUI use application-backed tRPC paths.
- Existing command/screen UX remains stable and covered by functional tests.
</success_criteria>
