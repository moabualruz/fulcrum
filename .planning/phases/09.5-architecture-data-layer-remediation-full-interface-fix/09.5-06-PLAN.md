---
phase: 9.5
plan: 06
title: Domain Expansion And Product-Kernel Runtime Retirement
type: tdd
wave: 3
depends_on:
  - 09.5-01
  - 09.5-02
  - 09.5-03
  - 09.5-04
  - 09.5-05
files_modified:
  - src/application/docs
  - src/application/sprints
  - src/application/reports
  - src/application/runs
  - src/application/artifacts
  - src/application/repos
  - src/application/audit
  - src/application/connectors
  - src/application/memory
  - src/application/notifications
  - src/application/search
  - src/application/orchestration
  - src/application/settings
  - src/product-kernel/store
autonomous: true
requirements:
  - ARCH-REMED-01..12
  - DATA-01..08
  - IFACE-01..08
requirements_addressed:
  - R-1
  - R-7
  - R-8
  - R-9
  - R-14
  - R-23
must_haves:
  truths:
    - D-07
    - D-08
    - D-09
    - D-10
    - D-15
    - D-23
    - D-24
    - D-30
    - D-43
    - D-50
    - D-51
    - D-52
    - D-56
---

<objective>
Expand the proven tasks slice across all remaining shipped domains and retire ProductDb/product-kernel store functions from runtime use without losing behavior.
</objective>

<threat_model>
- T-06-01: Feature behavior disappears during broad migration. Mitigation: every domain has replacement mapping, integration test, and before/after adapter assertion before deleting legacy runtime path.
- T-06-02: Raw SQL sneaks into new application modules. Mitigation: no-raw-SQL architecture test gates each domain before moving to next.
- Block on: high.
</threat_model>

<tasks>
<task id="09.5-06-00" type="execute">
  <read_first>
    <file>09.5-CONTEXT.md</file>
    <file>src/product-kernel/store/repos.ts</file>
    <file>src/product-kernel/store/audit.ts</file>
    <file>src/product-kernel/store/notifications.ts</file>
    <file>src/product-kernel/store/settings.ts</file>
  </read_first>
  <action>Create feature-preservation replacement mapping (D-50/D-51). For each product-kernel store function being retired, document: function name, current callers, replacement application module path, and behavioral equivalence assertion. Cover all 7 entity domains from D-23: bitbucket, gitlab, github, repo-files, audit, settings/credentials, notifications. Write to `09.5-REPLACEMENT-MAP.md`.</action>
  <acceptance_criteria>
    <criterion>`09.5-REPLACEMENT-MAP.md` contains rows for all 7 entity domains: bitbucket, gitlab, github, repo-files, audit, settings/credentials, notifications.</criterion>
    <criterion>Each row names: legacy function, caller files, replacement path.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-06-01" type="tdd">
  <read_first>
    <file>09.5-CONTEXT.md</file>
    <file>09.5-UAT-BASELINE.md</file>
    <file>src/services/DocService.ts</file>
    <file>src/services/SprintService.ts</file>
    <file>src/services/ReportService.ts</file>
  </read_first>
  <action>RED: Write failing application command/query tests for core domains: docs, sprints, reports, runs, artifacts. Each domain test asserts create/list/get where applicable, not-found AppError, validation AppError, and org/project scoping.</action>
  <acceptance_criteria>
    <criterion>`find src/application/{docs,sprints,reports,runs,artifacts} -name '*integration.test.ts' | wc -l` is at least 5.</criterion>
    <criterion>Each test imports from `src/application/<domain>` and not `src/product-kernel/store`.</criterion>
    <criterion>All tests fail (RED) because application modules don't exist yet.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-06-02" type="execute">
  <read_first>
    <file>09.5-PATTERNS.md</file>
    <file>src/services/DocService.ts</file>
    <file>src/services/SprintService.ts</file>
    <file>src/services/ReportService.ts</file>
    <file>src/services/runs.ts</file>
    <file>src/services/artifacts.ts</file>
  </read_first>
  <action>GREEN: Move/extract existing services into `src/application/{docs,sprints,reports,runs,artifacts}/`. Use command/query/type/schema files. Preserve existing service behavior first, then eliminate direct ProductDb/raw SQL callers. Add application bindings in `src/db/db.module.ts`. Tests from 06-01 must pass.</action>
  <acceptance_criteria>
    <criterion>`ls src/application/{docs,sprints,reports,runs,artifacts}` returns directories.</criterion>
    <criterion>`rg 'from .*src/services|from .*\\.\\./services' apps/server/src/runtime/trpc apps/server/src/api apps/web/src/routes apps/cli/src apps/tui/src --type ts | grep -v test` returns no migrated-domain service imports.</criterion>
    <criterion>`bun test src/application/{docs,sprints,reports,runs,artifacts}` exits 0 (GREEN).</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-06-03" type="tdd">
  <read_first>
    <file>src/product-kernel/store/repos.ts</file>
    <file>src/product-kernel/store/repo-files.ts</file>
    <file>src/product-kernel/store/audit.ts</file>
    <file>src/product-kernel/store/notifications.ts</file>
    <file>src/product-kernel/store/settings.ts</file>
    <file>src/product-kernel/store/settings-connectors-credentials.ts</file>
  </read_first>
  <action>RED: Write failing tests for product-kernel extraction domains. Cover all 7 entity domains from D-23: bitbucket, gitlab, github, repo-files, audit, settings/credentials, notifications. Tests assert application modules exist and handle CRUD, not-found, and scoping for each domain.</action>
  <acceptance_criteria>
    <criterion>Tests exist for all 7 entity domains: bitbucket, gitlab, github, repo-files, audit, settings/credentials, notifications.</criterion>
    <criterion>All tests fail (RED) because extraction hasn't happened yet.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-06-04" type="execute">
  <read_first>
    <file>09.5-REPLACEMENT-MAP.md</file>
    <file>src/product-kernel/store/repos.ts</file>
    <file>src/product-kernel/store/repo-files.ts</file>
    <file>src/product-kernel/store/audit.ts</file>
    <file>src/product-kernel/store/notifications.ts</file>
    <file>src/product-kernel/store/settings.ts</file>
    <file>src/product-kernel/store/settings-connectors-credentials.ts</file>
  </read_first>
  <action>GREEN: Extract runtime logic from product-kernel stores into `src/application/{repos,audit,notifications,settings,connectors,search,memory,orchestration}/` backed by MikroORM entities/repositories. Follow replacement mapping from 06-00. Leave product-kernel store files only as legacy compatibility shims or test fixtures. Tests from 06-03 must pass.</action>
  <acceptance_criteria>
    <criterion>`rg 'from .*product-kernel/store' apps/web apps/cli/src apps/tui/src apps/server/src/api apps/server/src/router apps/server/src/runtime/trpc --type ts | grep -v test` returns zero matches.</criterion>
    <criterion>`rg 'ProductDb' src/application --type ts` returns zero matches.</criterion>
    <criterion>`bun test src/application/{repos,audit,notifications,settings,connectors,search,memory,orchestration}` exits 0 (GREEN).</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-06-05" type="tdd">
  <read_first>
    <file>src/orchestration/symphony/tracker.ts</file>
    <file>src/orchestration/symphony/orchestrator.ts</file>
    <file>src/orchestration/symphony/retry.ts</file>
    <file>src/notifications/defaults.ts</file>
  </read_first>
  <action>RED: Write failing tests for worker/orchestration state mutations via application commands. Assert that orchestration state changes go through application commands, not direct EM. Assert `getConnection().execute()` is absent from notification defaults.</action>
  <acceptance_criteria>
    <criterion>Worker mutation tests exist and fail (RED).</criterion>
    <criterion>Tests assert application command calls for orchestration state changes.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-06-06" type="execute">
  <read_first>
    <file>src/orchestration/symphony/tracker.ts</file>
    <file>src/orchestration/symphony/orchestrator.ts</file>
    <file>src/orchestration/symphony/retry.ts</file>
    <file>src/notifications/defaults.ts</file>
  </read_first>
  <action>GREEN: Move worker state mutations through application commands/queries. For performance-sensitive orchestration reads, keep direct EM only behind application query functions and document the allowed read path in the module comment. Replace `src/notifications/defaults.ts` raw `em.getConnection().execute()` with repository/query-builder code. Tests from 06-05 must pass.</action>
  <acceptance_criteria>
    <criterion>`rg 'getConnection\\(\\)\\.execute' src/ --type ts | grep -v test` returns zero matches.</criterion>
    <criterion>`bun test` for worker mutation tests exits 0 (GREEN).</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-06-07" type="execute">
  <read_first>
    <file>src/product-kernel/store/index.ts</file>
    <file>src/db/mikro-orm.config.ts</file>
  </read_first>
  <action>R-7 migration consolidation: Remove runtime `runMigrations()` calls from product-kernel. MikroORM is sole migration authority. Verify no product-kernel code calls `runMigrations()` or `ProductDb.migrate()` at runtime.</action>
  <acceptance_criteria>
    <criterion>`rg 'runMigrations|ProductDb\\.migrate' src/product-kernel --type ts | grep -v test` returns zero matches.</criterion>
    <criterion>MikroORM config in `src/db/mikro-orm.config.ts` is the single migration entry point.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `bun test src/application`
- `bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts`
- `bun run ci --tier=integration --domain=application`
</verification>

<success_criteria>
- Runtime product-kernel store imports are gone from interfaces.
- All shipped domains have application command/query modules and integration coverage.
</success_criteria>
