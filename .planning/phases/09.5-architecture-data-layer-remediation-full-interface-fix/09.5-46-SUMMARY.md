---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 46
subsystem: trpc-adapters
tags: [architecture-remediation, trpc, entity-manager-boundary]
requires:
  - phase: 09.5-27
    provides: forbidden EntityManager grep closure target
provides:
  - centralized tRPC EntityManager resolver
  - server router adapter cleanup for auth/tasks/audit/memory/routing
affects: [src/trpc/context.ts, src/server/trpc/routers]
tech-stack:
  added: []
  patterns: [composition-root dependency resolver, thin tRPC adapter]
key-files:
  created:
    - src/trpc/context.test.ts
    - .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-46-SUMMARY.md
  modified:
    - src/trpc/context.ts
    - src/server/trpc/routers/auth.ts
    - src/server/trpc/routers/tasks.ts
    - src/server/trpc/routers/audit.ts
    - src/server/trpc/routers/memory.ts
    - src/server/trpc/routers/routing.ts
key-decisions:
  - "Keep EntityManager extraction in src/trpc/context.ts via requireTrpcEntityManager(ctx)."
  - "Keep server tRPC routers as app adapters that validate input, build app context, and call src/application modules."
  - "Remove memory list/search missing-EntityManager empty-array fallbacks so missing persistence fails deterministically."
patterns-established:
  - "Routers obtain persistence dependency through requireTrpcEntityManager(ctx), not local helper functions."
requirements-completed: [ARCH-REMED-01, ARCH-REMED-02, ARCH-REMED-03, DATA-01, API-DEC-01, IFACE-04, E2E-01]
completed: 2026-05-07
status: completed
---

# Phase 09.5 Plan 46 Summary

## Accomplishments

- Added `requireTrpcEntityManager(ctx)` to `src/trpc/context.ts`.
- Added context tests for resolved and missing persistence dependency behavior.
- Replaced local router persistence guards in `auth`, `tasks`, `audit`, `memory`, and `routing` tRPC routers.
- Removed Plan 27 forbidden `ctx.em` text and direct `em.*` comments from owned server router files.
- Removed memory list/search silent fallback arrays when persistence dependency is absent.

## Verification

- `bun test src/trpc/context.test.ts src/server/trpc/routers/auth.test.ts src/server/trpc/routers/tasks.test.ts src/server/trpc/routers/audit.test.ts src/server/trpc/routers/memory.test.ts src/server/trpc/routers/routing-adapter.test.ts -x`
  - Result: FAIL, exit 1.
  - Evidence: installed `bun test` rejects `-x` as `Invalid Argument '-x'`.
- `bun test src/trpc/context.test.ts src/server/trpc/routers/auth.test.ts src/server/trpc/routers/tasks.test.ts src/server/trpc/routers/audit.test.ts src/server/trpc/routers/memory.test.ts src/server/trpc/routers/routing-adapter.test.ts -- -x`
  - Result: PASS, 11 pass, 0 fail.
- `! rg -n "ctx\\.em|em\\.find|em\\.findOne|em\\.create|em\\.persist|em\\.flush|em\\.transactional|getRepository\\(" src/server/trpc/routers/auth.ts src/server/trpc/routers/tasks.ts src/server/trpc/routers/audit.ts src/server/trpc/routers/memory.ts src/server/trpc/routers/routing.ts`
  - Result: PASS, no matches.
- `bun run lint`
  - Result: PASS.

## Deviations

- Exact required `bun test ... -x` command cannot pass on installed Bun because `-x` is not a supported flag. Same file set passed with `-- -x`, which treats `-x` as a non-option argument after Bun's parser.

## Self-Check

- Owned router files contain no Plan 27 forbidden grep matches.
- Routers remain adapter-only: Zod input/output, app context construction, application module call, and `AppError` mapping.
- Unrelated dirty files `AGENTS.md` and `src/db/migrations/.snapshot-postgres.json` were not touched.
