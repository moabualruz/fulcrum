# 09.5 API, tRPC, Router Boundary Gap Research

Source: Ampere API-contract subagent, 2026-05-07.

## Executive Finding

REST, tRPC, and router surfaces still contain fallback stores, direct ORM resolution, and persistence-selection logic. They should call application facades only; missing application dependencies should fail through canonical app errors.

## REST Gaps

- `apps/server/src/api/routes/tasks.ts`: fallback task store around line 78.
- `apps/server/src/api/routes/docs.ts`: fallback document store around line 34.
- `apps/server/src/api/routes/memory.ts`: `fallbackMemories` around line 183.
- `apps/server/src/api/routes/sprints.ts`: fallback sprint store around line 49.
- `apps/server/src/api/routes/saved-views.ts`: fallback saved-view store around line 48.
- `apps/server/src/api/routes/notifications.ts`: notification fallback store around line 34.
- `apps/server/src/api/routes/search.ts`: stub search result path around line 37.
- `apps/server/src/api/routes/kernel-reports.ts`: `FIXED_ORG` and fallback report data around line 56.

Replace these with injected public API/application facades. If a facade is unavailable, return canonical application error mapping, not empty or stub data.

## REST ORM Resolution Gaps

- `apps/server/src/api/routes/repos.ts`
- `apps/server/src/api/routes/search.ts`
- `apps/server/src/api/routes/kernel-tasks.ts`
- `apps/server/src/api/routes/kernel-sprints.ts`
- `apps/server/src/api/routes/kernel-audit.ts`
- `apps/server/src/api/routes/kernel-notifications.ts`

Move EntityManager resolution to API composition/application adapter modules, then pass application callers into routes.

## tRPC Gaps

- `apps/server/src/runtime/trpc/routers/orgs.ts`: persistence selection belongs in application org service/facade.
- `apps/server/src/trpc/routers/artifacts.ts`: repository/storage/events fallback wiring belongs in application artifact service.
- `apps/server/src/trpc/context.ts`: direct `ctx.em` should stay composition-only; routers should use application caller/facade helpers.

## Router Runtime Gap

- `apps/server/src/router/rules-engine.ts`: repository initialization and malformed-rule mutation are business/data operations. Move disable/mutation behavior into application routing service. Router should evaluate a supplied ruleset only.

## Guardrail Patterns

Ban in route/runtime adapter files:

- `@mikro-orm/postgresql`, `src/db/entities`, `src/db/repositories`
- `requireTrpcEntityManager`, `optionalTrpcEntityManager`, `ctx.em`
- `em.find`, `em.findOne`, `em.create`, `em.persist`, `em.flush`, `em.transactional`, `em.getRepository`
- `getKysely`, `.execute(`, `.query(`
- `new Map<`, `createFallbackStore`, `fallbackMemories`, `StubSearchResults`, `makeNotifStore`, `FIXED_ORG`

## Verification

- `bun run scripts/check-module-boundaries.ts`
- `bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts -- -x`
- `bun test apps/server/src/api/__tests__/phase95-interface-parity.test.ts apps/server/src/api/__tests__/phase08-api-parity.test.ts -- -x`
- Focused tRPC tests for routers moved to application facades.

