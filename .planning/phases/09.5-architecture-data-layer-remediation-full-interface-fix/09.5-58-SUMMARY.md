---
plan: 58
phase: 09.5
status: complete
gap_closure: true
created: 2026-05-07
---

# Plan 58 Summary

## Completed

- CLI import/export now invoke application/API through `createLocalCaller` instead of constructing ORM/runtime state.
- Web docs/search surfaces now import domain/application types, not DB/product-kernel modules.
- Task detail SQL/data mutation moved into `src/application/tasks/task-detail.ts`; web file is only a re-export.
- Passkey auth context loading moved into `src/application/auth/passkey-context.ts`.
- CLI db migration/status logic moved into `src/application/db/commands.ts`.
- Interactive init seed logic and interactive error type moved into `src/application/init/**`.
- Boundary tests now scan `.svelte` sources and catch generic `execute<T>()` raw SQL calls.
- WR-03 fixed: full web E2E no longer requires `FULCRUM_RUN_E2E=1` to appear in CI.

## Evidence

- Interface ORM/business-logic scan: no web/CLI/TUI hits except `src/web/src/lib/server/application-scope.ts` composition-root type references.
- `bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts src/architecture/migrations.test.ts src/web/src/lib/server/task-detail.test.ts 'src/web/src/routes/tasks/[id]/page.server.test.ts'`
- `bun run lint:boundaries`
- `bun test scripts/ci.test.ts`
- `bun run --bun tsc --noEmit`
- `just sync-symphony`
- `bun test tests/symphony/spec-lock.test.ts src/orchestration/__tests__/symphony-conformance.test.ts`
- `bun run ci --tier=unit`
- `bun run ci --tier=e2e --domain=web`

## Remaining Risk

- Full web E2E gate is no longer hidden and now passes through `bun run ci --tier=e2e --domain=web`.
