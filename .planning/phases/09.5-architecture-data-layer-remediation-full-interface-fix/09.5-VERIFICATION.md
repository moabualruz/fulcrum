---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
artifact: verification
created: 2026-05-06
plan: 08
status: blocked
---

# Phase 9.5 Verification

**Phase 10 remains blocked.** Plan 09.5-08 added real Web/CLI/TUI E2E gates and collected closure evidence, but Phase 9.5 cannot close because architecture acceptance and full CI are still red.

## Command Evidence

| Command | Result | Evidence |
| --- | --- | --- |
| `rg 'gotoOrSkip' src/web/tests/e2e/phase95-*.spec.ts src/web/tests/e2e/_smoke.spec.ts` | PASS | Zero matches. |
| `cd src/web && FULCRUM_RUN_E2E=1 bunx playwright test tests/e2e/phase95-dashboard.spec.ts tests/e2e/phase95-doc-crud.spec.ts tests/e2e/phase95-parity.spec.ts tests/e2e/phase95-settings.spec.ts tests/e2e/phase95-sprint-workflow.spec.ts tests/e2e/phase95-task-crud.spec.ts` | PASS | 15 passed, 0 failed. Critical journey count >= 10. |
| `bun test src/cli/e2e/cli-smoke.test.ts src/cli/e2e/cli-workflows.test.ts src/cli/e2e/cli-errors.test.ts src/cli/e2e/cli-settings.test.ts` | PASS | 9 passed, 0 failed. |
| `bun test src/tui/e2e/tui-screen-smoke.test.ts src/tui/e2e/tui-navigation.test.ts src/tui/e2e/tui-data-display.test.ts` | PASS | 16 passed, 0 failed. |
| `ls src/cli/e2e/{cli-smoke.test.ts,cli-workflows.test.ts,cli-errors.test.ts,cli-settings.test.ts}` | PASS | All four files exist. |
| `ls src/tui/e2e/{tui-screen-smoke.test.ts,tui-navigation.test.ts,tui-data-display.test.ts}` | PASS | All three files exist. |
| `bun test src/web/src/lib/components/app/nav-items.test.ts` | PASS | 6 passed, 0 failed. |
| `rg 'openPglite\|openProductDb' src/ --type ts \| grep -v product-kernel/ \| grep -v node_modules` | FAIL | Many matches remain in web routes, CLI doctor/agent paths, tests, and fixtures. |
| `rg 'em\.(persist\|flush\|execute\|getConnection)' src/web/ src/cli/ src/tui/ src/router/ src/api/ --type ts \| grep -v test` | FAIL | Matches remain in `src/tui/telemetry.ts`, `src/cli/commands/routing.ts`, web route/server helper files, and ORM helper files. |
| `rg 'ProductDb' src/ --type ts \| grep -v product-kernel/` | FAIL | Many matches remain across search, web routes/helpers/tests, CLI, tRPC, docs, and connector paths. |
| `rg 'PGlite' src/subscriptions/ --type ts` | PASS | Zero matches in subscriptions. |
| `test ! -f src/product-kernel/api/router.ts` | FAIL | `src/product-kernel/api/router.ts` still exists. |
| `bun run ci --tier=unit` | FAIL | Stops at `symphony:conformance`: `Startup cleanup sweep (SYM-18)` failed; 84 pass, 1 fail. |
| `bun run ci --tier=integration` | FAIL | Same `symphony:conformance` SYM-18 failure before integration stages. |
| `FULCRUM_RUN_E2E=1 bun run ci --tier=e2e` | FAIL | Same `symphony:conformance` SYM-18 failure before E2E stages. |
| `FULCRUM_RUN_E2E=1 bun run ci --tier=e2e --domain=web` | FAIL | Stops at `web:test`: `tests/vitest/cmdk-palette.test.ts` fails with `TypeError: The "path" argument must be of type string. Received undefined`. |
| `FULCRUM_RUN_E2E=1 bun run ci` | FAIL | Same `symphony:conformance` SYM-18 failure before later full-CI stages. |

## Skipped-Test Count

- Phase 9.5 critical Playwright specs: 0 skipped.
- Phase 9.5 CLI/TUI E2E specs: 0 skipped.
- Existing non-Phase-9.5 suites may still contain skips; this verification did not change or audit non-owned specs beyond the requested `gotoOrSkip` guard check.

## Parity Proof

| Surface | Proof | Result |
| --- | --- | --- |
| Web | 15 Phase 9.5 Playwright checks cover dashboard, project navigation, task board, sprint affordances, docs, command palette, settings, and parity routes. | PASS |
| CLI | Four E2E files exercise smoke, CRUD workflows, error handling, and settings through injected tRPC/application caller contracts. | PASS |
| TUI | Three E2E files exercise screen smoke, navigation flow, and data display through `TuiApp` + `FakeTTY` + injected caller contracts. | PASS |
| Cross-surface | Web/CLI/TUI now have explicit phase-owned E2E gates, but full cross-interface create-via-one-surface/verify-via-other remains blocked by architecture/data-layer failures. | BLOCKED |

## Security Proof

| Requirement | Evidence | Result |
| --- | --- | --- |
| R-16 rate limiting | Plan 09.5-07 implemented REST/tRPC rate-limit tests and middleware per summary commit `3d40c09d`; full CI still blocked by unrelated conformance failure. | PARTIAL |
| R-17 webhook encryption | Plan 09.5-07 implemented AES-GCM webhook secret encryption per summary commit `4001ba0e`; full CI still blocked. | PARTIAL |
| R-18 security findings | Plan 09.5-07 closed CR-01..CR-04/WR-05 with `SECURITY.md` evidence; full CI still blocked. | PARTIAL |
| Boundary security | Direct ProductDb/openProductDb/raw EM access remains in interface and test paths. | FAIL |

## Feature Preservation Mapping

| Feature | Preserved By | Result |
| --- | --- | --- |
| Web dashboard/navigation/task/doc/settings journeys | New `src/web/tests/e2e/phase95-*.spec.ts` files. | PASS for phase-owned specs |
| CLI task/settings/sprint workflows | New `src/cli/e2e/*.test.ts` files with output and exit-code assertions. | PASS |
| TUI task/project/run display and navigation | New `src/tui/e2e/*.test.ts` files using FakeTTY. | PASS |
| Existing web command palette | Existing `tests/vitest/cmdk-palette.test.ts` currently fails in web CI. | FAIL |
| Existing Symphony startup cleanup | Existing `src/orchestration/__tests__/symphony-conformance.test.ts` currently fails SYM-18. | FAIL |

## Requirement Matrix

| Requirement | Status | Evidence |
| --- | --- | --- |
| R-1 Unified app layer | FAIL | `openPglite/openProductDb/ProductDb` grep still returns many non-product-kernel matches. |
| R-2 Web route migration | FAIL | Many web routes still import/call `openProductDb`; direct Phase 9.5 specs pass but route logs show residual 500s behind error pages. |
| R-3 CLI migration | PARTIAL | Phase-owned CLI E2E specs pass, but `src/cli/doctor.ts` and `src/cli/agent.ts` still open PGlite/ProductDb paths. |
| R-4 TUI migration | PARTIAL | Phase-owned TUI E2E specs pass; `src/tui/telemetry.ts` still performs direct `em.persist`/`em.flush`. |
| R-5 tRPC thin adapters | FAIL | `ProductDb` and direct context DB references remain in tRPC orchestration paths. |
| R-6 Hono thin adapters | PARTIAL | `src/api/hono.ts` exists and Plan 09.5-04/07 hardened adapters; product-kernel API router still exists. |
| R-7 Migration consolidation | FAIL | Product-kernel migration compatibility and PGlite test fixtures still appear outside legacy-only boundary. |
| R-8 New MikroORM entities | PARTIAL | Plan 09.5-02 added entities, but `ProductDb` grep still finds non-product-kernel runtime/test paths. |
| R-9 No raw SQL | FAIL | `em.getConnection().execute` remains in web helpers/routes and CLI routing. |
| R-10 Event outbox | PARTIAL | Plan 09.5-02 added outbox, but full CI and architecture gates do not pass. |
| R-11 Collab PGlite fix | PASS | `rg 'PGlite' src/subscriptions/ --type ts` returned zero matches. |
| R-12 Nav repair | PASS | `bun test src/web/src/lib/components/app/nav-items.test.ts` passed 6/6. |
| R-13 Cross-interface parity | PARTIAL | Web/CLI/TUI phase-owned E2E gates pass; cross-surface data verification remains blocked by architecture failures. |
| R-14 Worker migration | PARTIAL | Worker mutation application boundaries landed in Plan 09.5-06, but full CI blocked and direct DB patterns remain elsewhere. |
| R-15 Test infra migration | FAIL | `openPglite/ProductDb` remains in many test fixtures, including web E2E fixtures. |
| R-16 Rate limiting | PARTIAL | Plan 09.5-07 tests and implementation exist; full CI blocked. |
| R-17 Webhook encryption | PARTIAL | Plan 09.5-07 encryption tests and implementation exist; full CI blocked. |
| R-18 Security findings | PARTIAL | Plan 09.5-07 security regression passed locally then; full CI blocked now. |
| R-19 Dual API merge | FAIL | `src/product-kernel/api/router.ts` still exists. |
| R-20 Missing tRPC routers | PARTIAL | Prior plans added router coverage, but ProductDb tRPC orchestration references remain. |
| R-21 CLI settings | PASS | `src/cli/settings.ts` exists; `src/cli/e2e/cli-settings.test.ts` passes. |
| R-22 Playwright real | PARTIAL | Phase 9.5 critical specs pass 15/15 with zero `gotoOrSkip`; full web-domain CI is blocked before E2E by cmdk vitest failure. |
| R-23 Strict TDD | PARTIAL | Plan 09.5-08 added test-only and verification commits; prior implementation TDD compliance must be evaluated from earlier plan summaries. |
| R-24 Tiered CI | FAIL | Tier flags exist, but `--tier=unit`, `--tier=integration`, `--tier=e2e`, and full CI all fail at SYM-18. |

## Phase 10 Unblock Statement

Phase 10 remains blocked. Required unblock conditions:

1. `FULCRUM_RUN_E2E=1 bun run ci` exits 0.
2. Architecture grep gates remove residual `openPglite/openProductDb/ProductDb` and direct `em.getConnection()` interface paths.
3. `src/product-kernel/api/router.ts` is removed or formally exempted by an updated Phase 9.5 decision.
4. `tests/vitest/cmdk-palette.test.ts` and `src/orchestration/__tests__/symphony-conformance.test.ts` are green.

## Residual Blockers

| Blocker | Scope | Owner |
| --- | --- | --- |
| SYM-18 startup cleanup sweep fails in Symphony conformance. | Existing orchestration test/runtime path, outside Plan 09.5-08 ownership. | Future fix plan/orchestrator |
| Web command palette vitest uses `import.meta.dir` and fails in web-domain CI. | Existing web vitest, outside Plan 09.5-08 ownership. | Future fix plan/orchestrator |
| ProductDb/openProductDb and raw EM access remain across many web/CLI/test files. | Architecture remediation incomplete outside Plan 09.5-08 ownership. | Future architecture cleanup |
| `src/product-kernel/api/router.ts` remains. | Dual API merge incomplete outside Plan 09.5-08 ownership. | Future architecture cleanup |
