---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
artifact: verification
created: 2026-05-06
updated: 2026-05-07
plan: 57
status: partial
---

# Phase 9.5 Verification

## Status

Architecture/data-layer remediation is verified. Full E2E remains red, so Phase 10 remains blocked for release-level closure.

## Evidence

| Gate | Command | Result |
| --- | --- | --- |
| Exact architecture closure | `bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts src/architecture/migrations.test.ts && ! rg ...` | PASS: 14 pass, direct-access grep clean. |
| Importer/connector boundary | `bun test src/web/src/routes/settings/importers/page.server.test.ts src/web/src/routes/settings/connectors/page.server.test.ts src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts -- -x` | PASS. |
| Expanded focused conformance | `bun test src/web/src/routes/settings/importers/page.server.test.ts src/web/src/routes/settings/connectors/page.server.test.ts src/application/outbox.test.ts src/application/tasks/tasks.integration.test.ts src/api/routes/kernel-tasks.test.ts src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts src/architecture/migrations.test.ts src/orchestration/__tests__/symphony-conformance.test.ts tests/symphony/spec-lock.test.ts -- -x` | PASS: 143 pass in prior run; latest focused subset PASS: 132 pass. |
| Module boundaries | `bun run lint:boundaries` | PASS: 0 violations. |
| Web typecheck | `cd src/web && bun run check` | PASS: 0 errors, 150 warnings. |
| Unit CI | `bun run ci --tier=unit` | PASS. |
| Integration CI | `bun run ci --tier=integration` | PASS. |
| Web smoke E2E | `cd src/web && bun run web:e2e:smoke` | PASS: 3 passed. |
| Web full E2E | `cd src/web && bun run web:e2e:full` | FAIL: 51 passed, 7 skipped, 48 failed. |

## Requirement Matrix

| Requirement | Status | Evidence |
| --- | --- | --- |
| ARCH-REMED-01 | PASS | Architecture boundary and direct-access grep gates clean. |
| ARCH-REMED-02 | PASS | Interfaces no longer import product-kernel/open legacy DB handles directly outside allowed composition/test scopes. |
| ARCH-REMED-03 | PASS | `bun run lint:boundaries` reports 0 violations. |
| IFACE-01 | PASS | Importers/connectors now delegate to application modules; route-local fake data stores removed. |
| IFACE-03 | PASS | Web routes, CLI/TUI/API/router/tRPC/server tRPC checked by boundary tests. |
| IFACE-09 | PASS | Boundary test fails on web route stub providers and fake persistence stores. |
| SYMPHONY-01 | PASS | Latest SPEC lock and conformance tests pass. |
| E2E-01 | FAIL | Full web E2E still has 48 failures. |
| E2E-08 | FAIL | `FULCRUM_RUN_E2E=1 bun run ci --tier=e2e` cannot be claimed green while full web E2E is red. |

## Remaining Blocker

Full E2E failures are now product/runtime issues, not hidden architecture-boundary drift:

- artifact/search schema drift in web ORM vs product-kernel/E2E assumptions
- PGlite prepared-statement parameter errors on several project routes
- legacy UI assertions for command palette, report date picker, project slug fill, TipTap, and journey task duplication

## Phase 10 Unblock

Phase 10 remains blocked until full E2E exits 0 or the project explicitly scopes those older product failures outside the release gate.
