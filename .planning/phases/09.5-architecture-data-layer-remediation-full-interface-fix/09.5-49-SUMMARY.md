---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 49
status: blocked
created: 2026-05-07
---

# Plan 49 Summary

## Changes

- Added `src/application/init/queries.ts` with `hasAnyOrg(em)` and local init orchestration behind application-owned helpers.
- Updated `apps/cli/src/commands/init.ts` to call application init orchestration instead of importing ORM config, entities, DB module wiring, or repositories directly.
- Updated `apps/web/src/lib/server/em.ts` so `resolveDefaultOrgId` delegates to `resolveOrgId(em, "default")`.
- Updated `resolveOrgId` to resolve the seeded default org by `DEFAULT_ORG_ID`.
- Added focused tests for init org detection, CLI init output, and web helper delegation.
- Updated closure proof, verification, and UAT docs with current pass/fail evidence.

## Verification

| Command | Exit | Result |
| --- | --- | --- |
| `bun test src/application/init/init.integration.test.ts apps/cli/src/commands/init.test.ts apps/web/src/lib/server/em.test.ts -- -x` | 0 | PASS |
| `! rg -n "ctx\\.em|em\\.find|em\\.findOne|em\\.create|em\\.persist|em\\.flush|em\\.transactional|getRepository\\(" apps/cli/src/commands/init.ts apps/web/src/lib/server/em.ts` | 0 | PASS |
| exact Plan 27 Task 1 architecture/research closure chain | 1 | FAIL |
| exact Plan 27 Task 2 CI closure chain | n/a | NOT RUN |

## Failure

Exact Plan 27 Task 1 failed in `src/architecture/boundary.test.ts`. Current actual residual list:

```text
apps/server/src/runtime/trpc/routers/memory.ts
apps/server/src/runtime/trpc/routers/routing.ts
apps/web/src/lib/components/docs/frontmatter-ui.ts
```

The test still expected additional stale residuals, including `apps/web/src/lib/server/em.ts`, so the command exited 1 before grep segments ran.

Separate diagnostics after the exact-chain failure:

- Direct EntityManager/repository grep segment: PASS.
- Legacy DB handle alias grep segment: FAIL in web route files outside Plan 49 ownership.
- Entity/repository/config import grep segment: FAIL in `apps/server/src/runtime/trpc/routers/memory.ts`, `apps/server/src/runtime/trpc/routers/routing.ts`, and `apps/web/src/lib/components/docs/frontmatter-ui.ts`.

## Closure

Phase 10 remains blocked. No closure claim.
