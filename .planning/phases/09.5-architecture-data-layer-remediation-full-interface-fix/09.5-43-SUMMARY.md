# Phase 09.5 Plan 43 Summary

## Outcome

Plan 43 follow-up retired the residual web compatibility/data-handle helper surface covered by the expanded grep gate.

## Changes

- Deleted `src/web/src/lib/server/application-compat.ts`.
- Renamed web EM helper exports to composition-neutral names in `src/web/src/lib/server/em.ts`, while preserving escaped compatibility exports for existing composition-root wiring outside the gate.
- Renamed `ormSqlConnection` helper to `sqlAccess`; `src/web/src/lib/server/db.ts` remains the exact web database composition exception.
- Replaced route-level EM/default-org helper usage with `requestAppScope(locals)` in:
  - `src/web/src/routes/runs/[id]/artifacts/+page.server.ts`
  - `src/web/src/routes/api/data/export-csv/+server.ts`
  - `src/web/src/routes/api/data/import-csv/+server.ts`
  - `src/web/src/routes/api/skills/+server.ts`
  - `src/web/src/routes/api/bell/+server.ts`
  - `src/web/src/routes/search/+page.server.ts`
  - `src/web/src/routes/docs/[id]/edit/+page.server.ts`
  - `src/web/src/routes/docs/+page.server.ts`
- Replaced web helper imports of the deleted compatibility shim with direct application runtime imports in `projects.ts`, `sprints.ts`, `settings.ts`, `feature-flags.ts`, and `task-detail.ts`.
- Updated Linear settings route to avoid the deleted compatibility shim and avoid legacy data-handle names in route code.
- Tightened `src/architecture/no-raw-sql.test.ts` expected data-handle inventory to remove all files remediated by this follow-up.

## Integration Map

- Request scope producer: `src/web/src/hooks.server.ts` populates `locals.em`, `locals.orgId`, `locals.userId`, and `locals.activeProjectId`.
- Request scope consumer: `src/web/src/lib/server/application-scope.ts` resolves `{ em, ctx }` for routes and API handlers.
- Application consumers: remediated routes call `application/*` queries/commands with `ctx` instead of calling web EM/default-org helpers.
- Composition root exception: `src/web/src/lib/server/db.ts` remains the exact allowed singleton database bootstrap and SQL-handle adapter.
- Must not break: existing dirty files `AGENTS.md` and `src/db/migrations/.snapshot-postgres.json` were not edited by this work.

## Verification

- Exact grep gate: no matches outside `src/web/src/lib/server/db.ts`.
- `bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts src/cli/commands/skills.test.ts`
- `bun run lint`
