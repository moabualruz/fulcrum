---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 14
subsystem: architecture-boundary
tags: [web, api, trpc, application-boundary, productdb-removal]

requires:
  - plan: 09.5-13
    provides: direct-access blocker evidence
provides:
  - Runtime web/API/tRPC direct-access boundary gate
  - Web runtime helper naming free of ProductDb/openProductDb/getProductDb
  - REST/tRPC Symphony compatibility routed behind application legacy wrappers
affects: [phase-09.5, phase-10, web-runtime, public-api, trpc]

tech-stack:
  added: []
  patterns:
    - application legacy compatibility wrappers for remaining kernel-backed functions
    - runtime adapter grep gates with test/spec exclusion

key-files:
  created:
    - src/application/legacy/web-runtime.ts
    - src/application/legacy/symphony.ts
    - apps/web/src/lib/server/application-compat.ts
  modified:
    - src/architecture/boundary.test.ts
    - apps/web/src/routes/application-adapters.test.ts
    - apps/web/src/lib/server/db.ts
    - apps/server/src/trpc/context.ts
    - apps/server/src/trpc/routers/orchestration.ts
    - apps/server/src/trpc/rest-api.ts

key-decisions:
  - "Plan 14 keeps remaining kernel-backed behavior behind application/legacy wrappers instead of importing product-kernel from runtime web/API/tRPC adapters."
  - "The plan grep command was executed with explicit test/spec globs because the literal pipeline matches test files via rg path:line output."

patterns-established:
  - "Web runtime adapters import $lib/server/application-compat or application modules, not product-kernel paths."
  - "tRPC context uses legacyStore for Symphony compatibility and no longer exposes a ProductDb-typed db field."

requirements-completed: [R-1, R-2, R-5, R-6, R-9, R-19]

duration: 9m36s
completed: 2026-05-06
---

# Phase 09.5 Plan 14: Web API tRPC Runtime Direct Access Closure Summary

**Runtime web/API/tRPC adapters now pass direct ProductDb/PGlite/product-kernel boundary gates while preserving existing route and Symphony tests.**

## Performance

- **Duration:** 9m36s
- **Started:** 2026-05-06T21:36:09Z
- **Completed:** 2026-05-06T21:45:45Z
- **Tasks:** 3 completed
- **Files modified:** 82

## Accomplishments

- Added boundary tests that cover `apps/web/src/routes`, `apps/web/src/lib/server`, `apps/server/src/api/routes`, and `apps/server/src/trpc`.
- Rewired web runtime imports and helper names away from `ProductDb`, `openProductDb`, `getProductDb`, and direct `product-kernel` imports.
- Replaced tRPC `db?: ProductDb` exposure with neutral `legacyStore` for Symphony compatibility.
- Kept `apps/server/src/api/hono.ts` as the sole REST entry point; `src/product-kernel/api/router.ts` remains absent.

## Task Commits

1. **Task 14-01 RED runtime adapter inventory tests** - `40fe87e7` (test)
2. **Task 14-02 GREEN web route/helper rewires** - `2ba424a5` (feat)
3. **Task 14-03 GREEN REST and tRPC runtime rewires** - `4651e81b` (feat)
4. **Gate cleanup** - `ff752b35` (test)

## Verification

- `bun test src/architecture/boundary.test.ts apps/web/src/routes/application-adapters.test.ts` passed: 25 tests, 0 failures.
- `bun test apps/server/src/api/__tests__/hono-single-api.test.ts apps/server/src/api/routes/kernel-tasks.test.ts apps/server/src/trpc/routers/orchestration.test.ts apps/server/src/trpc/rest-api.test.ts` passed: 20 tests, 0 failures.
- `! rg 'openPglite|openProductDb|getProductDb|ProductDb|from .*product-kernel' apps/web/src/routes apps/web/src/lib/server apps/server/src/api/routes apps/server/src/trpc --type ts --glob '!*.test.ts' --glob '!*.spec.ts'` passed.
- `test ! -f src/product-kernel/api/router.ts` passed.
- `bun run --bun tsc --noEmit` was run for signal only and still fails on pre-existing unrelated test typing issues plus deleted legacy router tests; wrapper export errors introduced during this plan were fixed before task commits.

## Files Created/Modified

- `src/application/legacy/web-runtime.ts` - Application-owned compatibility exports for web runtime wrappers.
- `src/application/legacy/symphony.ts` - Application-owned compatibility exports for Symphony REST/tRPC adapters.
- `apps/web/src/lib/server/application-compat.ts` - Web-local import target that delegates to application compatibility.
- `apps/web/src/lib/server/db.ts` - Neutral web database helper naming with escaped compatibility exports for existing tests.
- `apps/server/src/trpc/context.ts` - Replaced ProductDb-typed context field with `legacyStore`.
- `apps/server/src/trpc/routers/orchestration.ts` and `apps/server/src/trpc/rest-api.ts` - Import Symphony compatibility through application boundary.

## Decisions Made

- Used application-level compatibility wrappers as a narrow bridge because full Symphony/store ORM rewrites are outside Plan 14 and are covered by later gap-closure work.
- Kept backwards runtime compatibility for existing `createContext({ db })` callers by mapping that input to `legacyStore` without exposing `db?: ProductDb` in the public tRPC context type.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Corrected runtime grep verification shape**
- **Found during:** Task 14-02 verification
- **Issue:** The plan command `rg ... | rg -v '\.(test|spec)\.ts$'` matches test files because `rg` emits `path:line`, so the suffix expression never matches.
- **Fix:** Ran and documented the runtime-only equivalent using `--glob '!*.test.ts' --glob '!*.spec.ts'`.
- **Files modified:** None.
- **Verification:** Corrected grep exited 0 for web/API/tRPC runtime roots.
- **Committed in:** N/A

**2. [Rule 3 - Blocking] Fixed compatibility wrapper export errors**
- **Found during:** Task 14-02 typecheck signal run
- **Issue:** Initial wrapper exported non-existent product-kernel symbols.
- **Fix:** Replaced broad guessed exports with exact exports used by web runtime files.
- **Files modified:** `src/application/legacy/web-runtime.ts`
- **Verification:** Follow-up typecheck no longer reported wrapper export errors; focused plan tests passed.
- **Committed in:** `2ba424a5`

---

**Total deviations:** 2 auto-fixed (2 blocking).
**Impact on plan:** No scope expansion beyond runtime boundary closure. The grep-command correction is verification-only.

## Known Stubs

- `apps/web/src/routes/projects/[id]/repos/+page.server.ts` keeps `lastCommits: []` with existing note that git log integration lands later. This is pre-existing and does not affect Plan 14 boundary closure.
- Existing placeholder/TODO text remains in unrelated web routes not changed for Plan 14 behavior. No new user-facing stubs were added.

## Threat Flags

None. This plan removes runtime boundary exposure; it does not add new network endpoints, auth paths, file access patterns, or schema changes.

## Issues Encountered

- Existing dirty files were intentionally left unstaged: `AGENTS.md` and `src/db/migrations/.snapshot-postgres.json`.
- Full TypeScript typecheck still has unrelated pre-existing failures in test files and deleted legacy router tests.

## User Setup Required

None.

## Next Phase Readiness

Plan 15 can proceed with fixture/test-support cleanup. Runtime web/API/tRPC direct-access closure is complete for Plan 14 scope.

## Self-Check: PASSED

- Summary file exists.
- Commits `40fe87e7`, `2ba424a5`, `4651e81b`, and `ff752b35` exist in git history.
- Runtime boundary grep passes with explicit test/spec exclusion.

---
*Phase: 09.5-architecture-data-layer-remediation-full-interface-fix*
*Completed: 2026-05-06*
