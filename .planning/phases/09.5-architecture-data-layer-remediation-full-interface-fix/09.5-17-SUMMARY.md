---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 17
subsystem: architecture-boundary
tags: [raw-sql, entity-manager, querybuilder, blocked]

requires:
  - plan: 09.5-14
    provides: web/API/tRPC runtime direct-access closure
  - plan: 09.5-16
    provides: non-web ProductDb/PGlite closure
provides:
  - RED raw EntityManager/SQL architecture gate
  - blocker evidence for over-broad `.query(` / `.execute(` grep semantics
affects: [phase-09.5, plan-09.5-18, architecture-tests]

tech-stack:
  added: []
  patterns:
    - Plan 13-style architecture gate without runtime allowlist
    - blocker-first handling for architectural API rename risk

key-files:
  created:
    - .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-17-SUMMARY.md
  modified:
    - src/architecture/no-raw-sql.test.ts

key-decisions:
  - "Task 2 stopped before implementation because satisfying the plan grep requires renaming non-SQL public adapter methods such as search.query, audit.query, and application.execute, which is an architectural/API contract change."
  - "The plan verification grep also fails to exclude test files because `rg -v '\\.(test|spec)\\.ts$'` runs against ripgrep output lines that end with source text, not filenames."

patterns-established:
  - "Architecture boundary violations report exact file:line evidence with tests and migrations excluded."

requirements-completed: []

duration: 22min
completed: 2026-05-06
---

# Phase 09.5 Plan 17: Raw EntityManager SQL Closure Summary

**RED raw EntityManager/SQL architecture gate landed; GREEN implementation blocked by over-broad grep semantics requiring public adapter API renames.**

## Performance

- **Duration:** 22min
- **Started:** 2026-05-06T22:38:33Z
- **Completed:** 2026-05-06T23:00:00Z
- **Tasks:** 1/3 complete
- **Files modified:** 2

## Accomplishments

- Replaced the prior raw SQL allowlist in `src/architecture/no-raw-sql.test.ts` with a Plan 13-style runtime scan.
- RED test failure names current raw EM files including `src/web/src/lib/server/orm-helpers.ts`, `src/web/src/lib/server/em.ts`, `src/web/src/lib/server/reports.ts`, and docs routes.
- Exclusions are limited to test/spec/`__tests__`/`tests` sources, migrations, `node_modules`, and SvelteKit output.

## Task Commits

1. **Task 09.5-17-01: RED raw EM inventory gate** - `8365f306` (test)

## Files Created/Modified

- `src/architecture/no-raw-sql.test.ts` - no-allowlist architecture gate reporting file:line raw EM/SQL matches.
- `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-17-SUMMARY.md` - blocker summary and evidence.

## Decisions Made

- Stopped at Rule 4 instead of renaming adapter APIs. The verification regex treats non-SQL methods as violations:
  - `src/api/routes/search.ts: const output = await svc.query(orgId, {`
  - `src/cli/commands/search.ts: caller.search.query(input)`
  - `src/tui/screens/audit.ts: this.opts.caller.audit.query(...)`
  - `src/router/domain-adapter.ts: config.application.execute(input, context)`
- These names are public/internal adapter contracts, not raw SQL. Renaming them would affect CLI/TUI/router/search/audit APIs beyond Plan 17's intended raw SQL replacement scope.

## Verification Evidence

- `bun test src/architecture/no-raw-sql.test.ts` failed as expected for RED.
  - Failure listed 148 runtime matches after test-directory exclusion.
  - Target files named: `src/web/src/lib/server/orm-helpers.ts`, `src/web/src/lib/server/em.ts`, `src/web/src/lib/server/reports.ts`, `src/web/src/routes/docs/[id]/+page.server.ts`, `src/web/src/routes/docs/[id]/edit/+page.server.ts`, `src/web/src/routes/docs/[id]/history/+page.server.ts`.
- `bun test src/application/outbox.test.ts src/web/src/lib/server/reports.test.ts src/web/src/lib/server/doc-versions.test.ts src/web/src/lib/server/audit.test.ts` failed before implementation.
  - `src/application/outbox.test.ts` passed.
  - `reports`, `doc-versions`, and `audit` tests failed because they pass `TestStore` objects without `em.getConnection`, while current source expects `EntityManager`.
- Plan verification grep currently fails on non-runtime test files because the exclusion is anchored to end-of-line:
  - Command run: `rg 'em\\.(persist|flush|execute|getConnection)|getConnection\\(\\)\\.execute|\\.query\\(|\\.execute\\(' src/web src/cli src/tui src/router src/api --type ts | rg -v '\\.(test|spec)\\.ts$' | rg -v 'src/db/migrations|src/product-kernel/' | head -80`
  - Evidence includes `src/web/src/routes/page.server.test.ts: await db.query(` and other `*.test.ts` files because ripgrep output lines end with matched source text, not the filename.

## Deviations from Plan

### Auto-fixed Issues

None.

---

**Total deviations:** 0 auto-fixed.
**Impact on plan:** Task 2 requires user/architectural decision before GREEN work can proceed safely.

## Issues Encountered

- **Rule 4 - Architectural Change Required:** Passing the plan grep exactly requires renaming non-SQL `.query(` and `.execute(` method calls across search, audit, router domain adapter, CLI, and TUI contracts.
- **Verification Command Bug:** `rg -v '\\.(test|spec)\\.ts$'` does not exclude ripgrep result lines for `*.test.ts`; it should match filename delimiters, e.g. `rg -v '\\.(test|spec)\\.ts:'`, or use an architecture test that filters file paths before scanning content.
- Existing dirty files were preserved and not staged: `AGENTS.md`, `src/db/migrations/.snapshot-postgres.json`.

## User Setup Required

None.

## Next Phase Readiness

Not ready for Plan 18 proof. Needed decision:

1. Narrow the grep/test semantics to raw SQL/EntityManager execution only and fix the test exclusion pattern.
2. Or explicitly authorize a broad API rename away from `.query(` / `.execute(` names across search/audit/router/CLI/TUI.

## Self-Check: BLOCKED

- Summary file exists.
- Task 1 commit exists: `8365f306`.
- RED gate exists and fails with target file evidence.
- Task 2/3 not executed because Rule 4 blocker requires decision.

---
*Phase: 09.5-architecture-data-layer-remediation-full-interface-fix*
*Completed: 2026-05-06*
