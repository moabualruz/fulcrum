---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 26
type: execute
gap_closure: true
wave: 3
depends_on: ["09.5-20", "09.5-21", "09.5-22", "09.5-23"]
files_modified:
  - src/api/hono.ts
  - src/api/routes/audit.ts
  - src/api/routes/docs.ts
  - src/api/routes/memory.ts
  - src/api/routes/notifications.ts
  - src/api/routes/repos.ts
  - src/api/routes/runs.ts
  - src/api/routes/search.ts
  - src/api/routes/sprints.ts
  - src/api/routes/artifacts.ts
  - src/api/__tests__/hono-single-api.test.ts
  - src/api/routes/kernel-application.test.ts
  - src/trpc/router.ts
  - src/trpc/routers/runs.ts
  - src/trpc/routers/sprints.ts
  - src/trpc/routers/search.ts
  - src/trpc/routers/repos.ts
  - src/trpc/routers/artifacts.ts
autonomous: true
requirements: [R-1, R-5, R-6, R-9, R-13, R-19, R-20, R-24]
must_haves:
  truths:
    - "Hono REST routes are thin adapters over application modules and one public API entry point remains."
    - "Canonical tRPC routers for runs, sprints, search, repos, and artifacts are application-backed, not stubs."
    - "OpenAPI and router mount tests prove no duplicate/stub API path for scoped domains."
  artifacts:
    - path: "src/api/hono.ts"
      provides: "Single Hono API mount over application-backed routes"
    - path: "src/trpc/router.ts"
      provides: "Canonical AppRouter mounts application-backed routers"
    - path: "src/api/__tests__/hono-single-api.test.ts"
      provides: "Single API and OpenAPI proof"
  key_links:
    - from: "src/api/routes/sprints.ts"
      to: "src/application/sprints"
      via: "Hono route delegate"
      pattern: "application/sprints"
    - from: "src/trpc/routers/runs.ts"
      to: "src/application/runs"
      via: "tRPC route delegate"
      pattern: "application/runs"
---

<objective>
Close Hono/API and canonical tRPC residual adapter gaps after application modules exist.

Purpose: satisfy R-6/R-19/R-20 while ensuring REST/tRPC expose the same application data as Web/CLI/TUI.
Output: Hono routes and canonical tRPC routers are thin adapters; stub/empty routers are replaced.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-INTERFACE-PARITY-PLATFORM-RESEARCH.md
@src/api/hono.ts
@src/api/routes/kernel-tasks.ts
@src/server/trpc/routers/tasks.ts
@src/trpc/router.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make Hono routes application-backed thin adapters</name>
  <files>src/api/hono.ts, src/api/routes/audit.ts, src/api/routes/docs.ts, src/api/routes/memory.ts, src/api/routes/notifications.ts, src/api/routes/repos.ts, src/api/routes/runs.ts, src/api/routes/search.ts, src/api/routes/sprints.ts, src/api/routes/artifacts.ts, src/api/routes/kernel-application.test.ts</files>
  <read_first>
    - src/api/hono.ts
    - src/api/routes/kernel-tasks.ts
    - src/api/routes/audit.ts
    - src/api/routes/docs.ts
    - src/api/routes/memory.ts
    - src/api/routes/notifications.ts
    - src/api/routes/repos.ts
    - src/api/routes/runs.ts
    - src/api/routes/search.ts
    - src/api/routes/sprints.ts
    - src/api/routes/artifacts.ts
  </read_first>
  <behavior>
    - Hono routes validate request, resolve EntityManager/AppContext, call application, map AppError to HTTP.
    - No Hono route imports product-kernel stores, db entities/repositories, or contains business logic.
  </behavior>
  <action>Refactor listed Hono route modules using `src/api/routes/kernel-tasks.ts` as template. Preserve existing route paths, API-key auth behavior, rate limit middleware from prior plans, response codes, and OpenAPI schema registration. Static/spec fallback may remain only for no-deps OpenAPI generation if it does not serve runtime business data.</action>
  <verify>
    <automated>bun test src/api/routes/kernel-application.test.ts src/api/routes/kernel-tasks.test.ts src/api/__tests__/phase08-api-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>`rg -n "product-kernel|db/entities|db/repositories|em\\.(find|findOne|create|persist|flush|transactional)|\\.query\\(|getConnection\\(\\)\\.execute" src/api/routes src/api/hono.ts -g '*.ts' -g '!*.test.ts'` returns zero matches except exact app delegate handoff allowed by Plan 19 gates.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace canonical tRPC stubs with application-backed routers</name>
  <files>src/trpc/router.ts, src/trpc/routers/runs.ts, src/trpc/routers/sprints.ts, src/trpc/routers/search.ts, src/trpc/routers/repos.ts, src/trpc/routers/artifacts.ts</files>
  <read_first>
    - src/trpc/router.ts
    - src/trpc/routers/runs.ts
    - src/trpc/routers/sprints.ts
    - src/trpc/routers/search.ts
    - src/trpc/routers/repos.ts
    - src/trpc/routers/artifacts.ts
    - src/server/trpc/routers/tasks.ts
  </read_first>
  <behavior>
    - `runs`, `sprints`, `search`, `repos`, and `artifacts` procedures return application data, never empty stubs.
    - Router mount names stay canonical with no duplicate alias bypass.
  </behavior>
  <action>Replace stubbed or direct-ORM canonical tRPC routers with procedures that delegate to application modules from Plans 20-23. Update `src/trpc/router.ts` mounts only as needed to keep one canonical domain name. Keep procedure names and input/output shapes stable for CLI/TUI/web callers.</action>
  <verify>
    <automated>bun test tests/trpc/app-router-scaffold.test.ts tests/trpc/router.test.ts src/trpc/routers/artifacts.test.ts src/application/tasks/interface-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>No router returns hardcoded `[]`, `{ok: true}` for a mutation with no application call, or direct `ctx.em` business logic; parity tests from Plan 25 can read application-created rows through these routers.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove single API and adapter boundary closure</name>
  <files>src/api/__tests__/hono-single-api.test.ts, src/architecture/boundary.test.ts, src/architecture/no-raw-sql.test.ts</files>
  <read_first>
    - src/api/__tests__/hono-single-api.test.ts
    - src/architecture/boundary.test.ts
    - src/architecture/no-raw-sql.test.ts
  </read_first>
  <behavior>
    - `src/product-kernel/api/router.ts` remains absent.
    - `/api/v1/openapi.json` is produced by `src/api/hono.ts` only.
    - Architecture gates have no API/tRPC violations for this plan's files.
  </behavior>
  <action>Extend Hono single-API tests to assert application-backed route registration for scoped domains and no legacy product-kernel router dependency. Remove obsolete API/tRPC files from Plan 19 allowlists when closed; do not add new exceptions.</action>
  <verify>
    <automated>bun test src/api/__tests__/hono-single-api.test.ts src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts</automated>
  </verify>
  <acceptance_criteria>`test ! -f src/product-kernel/api/router.ts` passes; API/tRPC files in this plan do not appear in direct-access gate output.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
| --- | --- |
| public REST/tRPC -> application | External/API-key and first-party callers invoke domain operations. |
| OpenAPI/router mounts -> clients | Duplicate or stale routes can bypass authZ. |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-09.5-26-01 | Elevation of privilege | duplicate API mounts | mitigate | Single Hono and canonical tRPC mount tests; no duplicate aliases. |
| T-09.5-26-02 | Tampering | REST/tRPC mutations | mitigate | Adapters delegate to application commands with shared validation/auth. |
</threat_model>

<verification>
Run `bun test src/api/__tests__/hono-single-api.test.ts src/api/routes/kernel-application.test.ts tests/trpc/router.test.ts src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts`.
</verification>

<success_criteria>
Hono and canonical tRPC are thin adapters over application modules; no stub or legacy API route remains in scoped domains.
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-26-SUMMARY.md`.
</output>
