---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 40
subsystem: settings
tags: [sveltekit, mikroorm, settings, application-layer, architecture-boundary]
requires:
  - phase: 09.5-architecture-data-layer-remediation-full-interface-fix
    provides: requestAppScope and existing settings application module
provides:
  - application-backed settings backup/data/errors/feature-flags/secrets/telemetry adapters
  - settings DTO/query/command surface for current MikroORM schema columns
affects: [settings-routes, architecture-boundary, data-layer-remediation]
tech-stack:
  added: []
  patterns: [SvelteKit route as FormData/session/response adapter, settings application command/query ownership]
key-files:
  created:
    - .planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-40-SUMMARY.md
  modified:
    - src/application/settings/commands.ts
    - src/application/settings/queries.ts
    - src/web/src/routes/settings/backups/+page.server.ts
    - src/web/src/routes/settings/data/+page.server.ts
    - src/web/src/routes/settings/errors/+page.server.ts
    - src/web/src/routes/settings/feature-flags/+page.server.ts
    - src/web/src/routes/settings/secrets/+page.server.ts
    - src/web/src/routes/settings/telemetry/+page.server.ts
key-decisions:
  - "Plan 40 executed only for user-owned backup/data/errors/feature-flags/secrets/telemetry routes; integrations, notifications, and skills routes were left untouched."
  - "Backup history now uses tenant_settings metadata because no current backups table/entity exists in the MikroORM schema."
  - "Data export delegates current-schema table discovery/redaction to the existing application import-export manifest helper."
patterns-established:
  - "Settings web routes parse FormData and call requestAppScope plus application settings commands/queries."
  - "Settings DTOs preserve existing page field names while sourcing from current MikroORM entities."
requirements-completed: [ARCH-REMED-01, ARCH-REMED-02, DATA-01, DATA-05, API-DEC-01, IFACE-01, IFACE-02, E2E-02, E2E-03]
duration: 7min
completed: 2026-05-07
---

# Phase 09.5 Plan 40: Settings Route Application Boundary Summary

**Settings backup/data/errors/feature-flags/secrets/telemetry pages now delegate reads and writes through `src/application/settings` with route files acting as SvelteKit adapters.**

## Performance

- **Duration:** 7 min
- **Started:** 2026-05-07T02:52:03Z
- **Completed:** 2026-05-07T02:58:53Z
- **Tasks:** 1 owned subset completed
- **Files modified:** 9

## Accomplishments

- Moved six global settings route groups off `openDatabase`, raw `db.query`, and route-owned table creation.
- Added settings application queries/commands for backup metadata, data export/import preflight, errors, feature flags/rollouts, secrets, and telemetry.
- Preserved existing page DTO field names such as `rollout_percent`, `cohort_rules`, `stack_trace`, `created_at`, `optIn`, and `rowCount`.

## Task Commits

1. **Task 1 owned subset: settings backup/data/errors/feature-flags/secrets/telemetry routes** - `1d8c2223` (feat)

## Files Created/Modified

- `src/application/settings/commands.ts` - settings write operations for backup metadata, imports, errors, feature flags, secrets, and telemetry.
- `src/application/settings/queries.ts` - settings read DTOs for backups, exports, errors, feature flags, secrets, and telemetry.
- `src/web/src/routes/settings/backups/+page.server.ts` - thin backup adapter using `requestAppScope`.
- `src/web/src/routes/settings/data/+page.server.ts` - thin data import/export adapter using `requestAppScope`.
- `src/web/src/routes/settings/errors/+page.server.ts` - thin error log adapter using `requestAppScope`.
- `src/web/src/routes/settings/feature-flags/+page.server.ts` - thin feature flag adapter using `requestAppScope`.
- `src/web/src/routes/settings/secrets/+page.server.ts` - thin secrets adapter using `requestAppScope`.
- `src/web/src/routes/settings/telemetry/+page.server.ts` - thin telemetry adapter using `requestAppScope`.
- `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-40-SUMMARY.md` - execution record.

## Decisions Made

- Scope followed user ownership, not the full plan frontmatter. Integrations, notifications, and skills route files were not edited.
- Backup list/create no longer creates a guessed `backups` table. It records backup summaries in `tenant_settings` under `settings.backups.history`.
- Feature flag page DTOs use `FeatureFlag` plus `FeatureFlagRollout`; fields missing from current schema, such as `docs_url`, remain `null` to preserve shape.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Used current schema instead of route-created guessed tables**
- **Found during:** Task 1
- **Issue:** Legacy route code created ad hoc `backups`, `credentials`, `telemetry_settings`, and simplified `feature_flags` tables with columns that do not match current MikroORM entities.
- **Fix:** Routed data through `TenantSetting`, `Credential`, `FeatureFlag`, `FeatureFlagRollout`, `ErrorLog`, and `TelemetryEvent`.
- **Files modified:** settings commands/queries and six settings routes.
- **Verification:** `rg` found no data-handle/raw SQL calls in the six owned route files.

**Total deviations:** 1 auto-fixed.
**Impact on plan:** Required to satisfy "use current schema columns, not legacy guessed columns."

## Issues Encountered

- `bun run lint` failed in out-of-scope `src/application/repos/queries.ts:147` due `TS2347: Untyped function calls may not accept type arguments`. That file already had concurrent modifications and was outside ownership.
- Architecture tests failed only because `src/architecture/no-raw-sql.test.ts` expected the six remediated route files to remain in residual debt baselines.
- Existing settings route unit tests still contain legacy `openDatabase`/raw-SQL mocks and call actions without SvelteKit `locals`; they are stale after application-boundary migration.

## Known Stubs

None found in owned modified files.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: settings-secret-storage | src/application/settings/commands.ts | Secrets writes moved into application layer and continue to persist encoded values in current `Credential.encryptedValue`; no plaintext route storage remains. |

## User Setup Required

None.

## Next Phase Readiness

Remove remediated settings route paths from `src/architecture/no-raw-sql.test.ts` residual baselines, then update stale route unit tests to mock `requestAppScope` and application settings commands/queries.

## Self-Check: PASSED

- Verified all owned modified files and summary file exist.
- `git diff --check` passed for owned modified files.
- Task commit exists: `1d8c2223`.

---
*Phase: 09.5-architecture-data-layer-remediation-full-interface-fix*
*Completed: 2026-05-07*
