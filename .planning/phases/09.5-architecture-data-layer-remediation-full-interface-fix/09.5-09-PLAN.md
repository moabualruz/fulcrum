---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 09
title: Application Correctness And Task Authorization Gap Closure
type: tdd
wave: 1
depends_on:
  - 09.5-01
  - 09.5-02
files_modified:
  - src/application/outbox.ts
  - src/application/outbox.test.ts
  - src/application/tasks/commands.ts
  - src/application/tasks/queries.ts
  - src/application/tasks/tasks.integration.test.ts
  - src/api/routes/kernel-tasks.ts
  - src/api/routes/kernel-tasks.test.ts
autonomous: true
gap_closure: true
requirements:
  - ARCH-REMED-01..12
  - DATA-01..08
  - API-DEC-01..06
  - IFACE-01..08
requirements_addressed:
  - R-1
  - R-5
  - R-6
  - R-9
  - R-10
  - R-13
  - R-18
  - CR-01
  - CR-02
  - CR-03
  - WR-01
  - WR-02
must_haves:
  truths:
    - "Two updates for the same task/subject create two dispatchable outbox rows instead of deduping away the later event."
    - "Project-scoped application task contexts cannot list, read, update, delete, parent, or dependency-edit tasks from another project."
    - "REST task create either rejects unsupported fields or forwards every accepted application field without silent data loss."
  artifacts:
    - path: "src/application/outbox.ts"
      provides: "Per-event outbox identity with optional explicit idempotency key."
      contains: "crypto.randomUUID"
    - path: "src/application/tasks/queries.ts"
      provides: "Project-scoped task list/get visibility enforcement."
      contains: "ctx.projectId"
    - path: "src/application/tasks/commands.ts"
      provides: "Correct dependency edge replacement and project-scoped mutation guards."
      contains: "replaceTaskDependencyEdges"
    - path: "src/api/routes/kernel-tasks.ts"
      provides: "REST task create forwards accepted application fields."
      contains: "descriptionText"
  key_links:
    - from: "src/api/routes/kernel-tasks.ts"
      to: "src/application/tasks/commands.ts"
      via: "taskApplication.createTask(resolveEntityManager(c), appContext(c), input)"
      pattern: "descriptionText|tiptapContent|points|assigneeId"
    - from: "src/application/tasks/commands.ts"
      to: "src/application/tasks/queries.ts"
      via: "findVisibleTask(txEm, ctx, taskId)"
      pattern: "findVisibleTask"
    - from: "src/application/outbox.ts"
      to: "src/db/entities/platform/DomainEventOutbox.ts"
      via: "writeOutboxEvent persists unique eventKey rows"
      pattern: "eventKey"
---

<objective>
Close application-layer correctness/security review blockers: outbox event loss, stale task dependency reverse edges, project-scope bypass, task list scope leak, and REST create field loss.

Purpose: Phase 9.5 cannot unblock Phase 10 while application commands lose events or bypass project boundaries.
Output: Failing-first tests plus green application/REST fixes for CR-01, CR-02, CR-03, WR-01, and WR-02.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-PATTERNS.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-REVIEW.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-01-SUMMARY.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-02-SUMMARY.md

<interfaces>
From `src/application/outbox.ts`:
```ts
export interface OutboxEventInput {
  orgId: string;
  projectId?: string | null;
  verb: string;
  subjectKind: string;
  subjectId?: string | null;
  payload?: Record<string, unknown>;
}
export function serializeOutboxEvent(input: OutboxEventInput): SerializedOutboxEvent;
export async function writeOutboxEvent(em: EntityManager, input: OutboxEventInput): Promise<DomainEventOutbox>;
export async function dispatchPendingOutboxEvents(em: EntityManager, dispatcher: OutboxDispatcher): Promise<{ dispatched: number }>;
```

From `src/application/tasks/queries.ts`:
```ts
export async function listTasks(em: EntityManager, ctx: AppContext, input?: ListTasksInput): Promise<TaskDto[]>;
export async function findVisibleTask(em: EntityManager, ctx: AppContext, taskId: string, options?: { includeDeleted?: boolean }): Promise<Task>;
```

From `src/application/tasks/commands.ts`:
```ts
export async function updateTask(em: EntityManager, ctx: AppContext, taskId: string, input: UpdateTaskInput): Promise<TaskDto>;
export async function deleteTask(em: EntityManager, ctx: AppContext, taskId: string): Promise<TaskDto>;
export async function setDependencies(em: EntityManager, ctx: AppContext, taskId: string, dependencies: TaskDependencies): Promise<TaskDto>;
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 09.5-09-01: RED tests for outbox event uniqueness</name>
  <files>src/application/outbox.test.ts, src/application/outbox.ts</files>
  <read_first>
    <file>src/application/outbox.ts</file>
    <file>src/db/entities/platform/DomainEventOutbox.ts</file>
    <file>.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-REVIEW.md</file>
  </read_first>
  <action>RED: Add tests proving two `writeOutboxEvent()` calls with the same `verb`, `subjectKind`, and `subjectId` but different payloads create two unprocessed rows and `dispatchPendingOutboxEvents()` dispatches both. Add a separate explicit idempotency test only if you add `idempotencyKey` to `OutboxEventInput`; otherwise assert generated per-event UUID keys. Then GREEN: change `serializeOutboxEvent()` so `eventKey` is per event via `crypto.randomUUID()` or scoped to an explicit `idempotencyKey`, never derived solely from subject identity. Preserve D-34/D-35 transactional outbox behavior.</action>
  <verify>
    <automated>bun test src/application/outbox.test.ts</automated>
    <automated>rg 'crypto\\.randomUUID|idempotencyKey' src/application/outbox.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`bun test src/application/outbox.test.ts` exits 0 and includes a test named for CR-01.</criterion>
    <criterion>Test asserts two later events for one subject both dispatch, not one.</criterion>
    <criterion>`rg 'eventKey: `\\$\\{input\\.verb\\}:\\$\\{input\\.subjectKind\\}' src/application/outbox.ts` returns no matches.</criterion>
  </acceptance_criteria>
  <done>CR-01 event loss is closed with repeat-event dispatch proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-09-02: RED tests for task dependency removal and project scoping</name>
  <files>src/application/tasks/tasks.integration.test.ts, src/application/tasks/commands.ts, src/application/tasks/queries.ts</files>
  <read_first>
    <file>src/application/tasks/tasks.integration.test.ts</file>
    <file>src/application/tasks/commands.ts</file>
    <file>src/application/tasks/queries.ts</file>
    <file>src/application/tasks/types.ts</file>
  </read_first>
  <action>RED: Add integration tests for CR-02, CR-03, and WR-02. CR-02 test must create A and B where B blocks A, call `setDependencies(A, { blocks: [], blocked_by: [] })`, then assert A has no `blocked_by` and B no longer has `blocks: [A]`. CR-03 tests must use `ctx.projectId` to prove `getTask`, `updateTask`, `deleteTask`, `setParent`, and `setDependencies` cannot access another project task in the same org. WR-02 test must prove `listTasks()` with `ctx.projectId` returns only that project. GREEN: update `replaceTaskDependencyEdges()` so it removes existing reverse edges pointing at the target before applying new edges, filter `listTasks()` by project scope, and enforce project scope in `findVisibleTask()` with `AppNotFoundError` for hidden tasks.</action>
  <verify>
    <automated>bun test src/application/tasks/tasks.integration.test.ts</automated>
    <automated>rg 'ctx\\.projectId' src/application/tasks/queries.ts src/application/tasks/commands.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Tests include names `CR-02`, `CR-03`, and `WR-02`.</criterion>
    <criterion>`replaceTaskDependencyEdges()` deletes old reverse edges before writing new target edges.</criterion>
    <criterion>`findVisibleTask()` checks `ctx.projectId` and throws not-found for same-org hidden project tasks.</criterion>
    <criterion>`listTasks()` applies `ctx.projectId` scope before serialization.</criterion>
  </acceptance_criteria>
  <done>Task dependency removal works, and all application task reads/mutations honor project scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-09-03: RED tests for REST task create field preservation</name>
  <files>src/api/routes/kernel-tasks.test.ts, src/api/routes/kernel-tasks.ts</files>
  <read_first>
    <file>src/api/routes/kernel-tasks.ts</file>
    <file>src/api/routes/kernel-tasks.test.ts</file>
    <file>src/application/tasks/schema.ts</file>
  </read_first>
  <action>RED: Extend Hono task route tests for WR-01 so POST `/tasks` sends accepted fields `descriptionText`, `tiptapContent`, `points`, and `assigneeId`; assert the injected `createTask` receives every field. GREEN: either forward all fields accepted by `CreateTaskBody` to application create, or shrink `CreateTaskBody` so unsupported fields are rejected with 400. Prefer forwarding because `CreateTaskBody` already extends application schema. Preserve `project_id` to `projectId` mapping.</action>
  <verify>
    <automated>bun test src/api/routes/kernel-tasks.test.ts</automated>
    <automated>rg 'descriptionText|tiptapContent|points|assigneeId' src/api/routes/kernel-tasks.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>WR-01 test fails before implementation and passes after.</criterion>
    <criterion>`createTask` call forwards `descriptionText`, `tiptapContent`, `points`, and `assigneeId`, or schema rejects them explicitly.</criterion>
    <criterion>`bun test src/api/routes/kernel-tasks.test.ts src/application/tasks/tasks.integration.test.ts src/application/outbox.test.ts` exits 0.</criterion>
  </acceptance_criteria>
  <done>REST accepted task fields are not silently dropped.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST client -> Hono task route | Untrusted JSON enters application task create. |
| Web/CLI/TUI/API context -> application task boundary | Authenticated context may be project-scoped and must not cross project data. |
| Application write -> outbox consumers | Domain events feed search, notifications, audit, and event bus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `src/application/outbox.ts` | mitigate | Use per-event UUID or explicit idempotency key so later legitimate events are persisted and dispatched. |
| T-09-02 | Information Disclosure | `src/application/tasks/queries.ts` | mitigate | Enforce `ctx.projectId` in list/get helpers and return not-found for hidden project tasks. |
| T-09-03 | Elevation of Privilege | `src/application/tasks/commands.ts` | mitigate | Route mutations through `findVisibleTask()` and project-scoped referenced-task checks. |
| T-09-04 | Tampering | `src/api/routes/kernel-tasks.ts` | mitigate | Make accepted REST fields match application input exactly or fail validation. |
</threat_model>

<verification>
- `bun test src/application/outbox.test.ts`
- `bun test src/application/tasks/tasks.integration.test.ts`
- `bun test src/api/routes/kernel-tasks.test.ts`
- `bun run ci --tier=unit --domain=application`
- `bun run ci --tier=integration --domain=api`
</verification>

<success_criteria>
CR-01, CR-02, CR-03, WR-01, and WR-02 are closed by failing-first tests and green implementation. Application task boundary owns project authorization per D-31/D-53.
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-09-SUMMARY.md`.
</output>
