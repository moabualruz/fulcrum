---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 37
subsystem: tui
tags: [tui, trpc, application-layer, interface-encapsulation, telemetry]

requires:
  - phase: 09.5-20
    provides: application/data-layer remediation foundations
  - phase: 09.5-22
    provides: application telemetry command path
  - phase: 09.5-35
    provides: interface boundary tightening
  - phase: 09.5-36
    provides: shared CLI/TUI caller-context factory
provides:
  - TUI buildCaller delegates session/context setup to application cli-tui caller context
  - TUI telemetry sink no longer imports ORM/entity types from TUI runtime code
  - Search screen delegates query interpretation to caller.search.query
  - Phase 09.5 TUI interface parity gate
affects: [phase-09.5, tui, architecture-boundary-baseline]

tech-stack:
  added: []
  patterns:
    - TUI runtime uses createApplicationLocalCaller and requireCliTuiSessionContext
    - TUI screens remain DTO-only renderers over caller-provided data

key-files:
  created:
    - apps/tui/src/__tests__/phase95-interface-parity.test.ts
  modified:
    - apps/tui/src/index.ts
    - apps/tui/src/telemetry.ts
    - apps/tui/src/screens/search.ts

key-decisions:
  - "TUI buildCaller now reuses src/application/cli-tui/caller-context.ts instead of duplicating EntityManager/session lookup."
  - "TUI search no longer imports src/search/nl-filter.ts; query interpretation remains behind caller.search.query."
  - "Architecture baseline updates were intentionally left to the orchestrator per plan instruction."

patterns-established:
  - "TUI runtime boundary tests scan for persistence imports without embedding forbidden fixture literals."
  - "TUI telemetry keeps DbTelemetrySink compatibility while hiding DB/entity type imports behind application command types."

requirements-completed: [ARCH-REMED-01, ARCH-REMED-02, DATA-01, IFACE-03, IFACE-04, IFACE-07, E2E-06]

duration: 3 min
completed: 2026-05-07
---

# Phase 09.5 Plan 37: TUI Interface Encapsulation Summary

**TUI caller, telemetry, and search rendering now route through application/tRPC caller paths instead of runtime DB/session side channels.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-05-07T03:25:16Z
- **Completed:** 2026-05-07T03:28:56Z
- **Tasks:** 2 completed
- **Files modified:** 4

## Accomplishments

- Removed duplicated TUI EntityManager/session lookup from `buildCaller()` and delegated to `createApplicationLocalCaller()`.
- Reworked TUI telemetry typing so `apps/tui/src/telemetry.ts` no longer imports MikroORM or auth entities.
- Removed direct `src/search/nl-filter.ts` import from the search screen; query handling stays behind `caller.search.query`.
- Added `phase95-interface-parity.test.ts` to lock TUI runtime/screen interface boundaries.

## Task Commits

1. **RED gate:** `baee8d39` (`test`) - added failing TUI interface parity checks.
2. **Task 1:** `afd49650` (`feat`) - routed TUI caller/session/telemetry setup through application layer.
3. **Task 2:** `2475e2c2` (`fix`) - kept search screen query interpretation behind caller path.
4. **Type fix:** `b679a8d6` (`fix`) - aligned TUI caller cast with app router shape.

## Files Created/Modified

- `apps/tui/src/__tests__/phase95-interface-parity.test.ts` - TUI boundary parity checks for runtime and listed screens.
- `apps/tui/src/index.ts` - uses shared application caller/session context and removes TUI-local DB context builder.
- `apps/tui/src/telemetry.ts` - preserves `DbTelemetrySink` API without DB/entity imports in TUI runtime code.
- `apps/tui/src/screens/search.ts` - delegates raw query to caller path with no local NL filter import.

## Decisions Made

- Reused Plan 36 `src/application/cli-tui/caller-context.ts` rather than introducing a TUI-specific context abstraction.
- Kept `DbTelemetrySink` public constructor compatible for existing tests while moving its direct type surface to application telemetry command types.
- Left architecture baseline changes unedited; orchestrator owns baseline removals.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Avoided architecture fixture self-match**
- **Found during:** Task 1 verification
- **Issue:** New parity test source initially contained forbidden boundary literals and caused `boundary.test.ts` fixture scan to fail.
- **Fix:** Built regex patterns without embedding forbidden fixture literals.
- **Files modified:** `apps/tui/src/__tests__/phase95-interface-parity.test.ts`
- **Verification:** `bun test apps/tui/src/__tests__/phase95-interface-parity.test.ts src/architecture/boundary.test.ts` no longer reports the new test file as a fixture violation.
- **Committed in:** `afd49650`

**2. [Rule 3 - Blocking] Fixed TUI caller cast typecheck issue**
- **Found during:** Required `bun run lint`
- **Issue:** tRPC app caller shape is broader than `TuiCaller`; direct cast produced TS2352.
- **Fix:** Narrowed via `unknown` before applying TUI facade enrichment.
- **Files modified:** `apps/tui/src/index.ts`
- **Verification:** Follow-up `bun run lint` no longer reports TUI errors.
- **Committed in:** `b679a8d6`

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both fixes were necessary to preserve verification quality. No extra product scope added.

## Issues Encountered

- `bun run lint` still fails in unrelated concurrently edited files:
  - `apps/cli/src/commands/flags.ts(244,5)` - `missingSessionMessage` not accepted by current `createLocalCaller` type.
  - `apps/cli/src/docs-templates.ts(123,5)` - same type mismatch.
- `bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts` fails only because the architecture baseline still expects removed residual TUI files.

## Verification

- `bun test tests/tui/foundation.test.ts apps/tui/src/__tests__/phase08-tui-parity.test.ts apps/tui/src/e2e/tui-screen-smoke.test.ts apps/tui/src/e2e/tui-navigation.test.ts apps/tui/src/e2e/tui-data-display.test.ts apps/tui/src/screens/settings-screens.test.ts apps/tui/src/screens/skills.test.ts apps/tui/src/__tests__/phase95-interface-parity.test.ts` - PASS, 61 pass / 0 fail.
- `bun run lint` - FAIL due unrelated concurrent CLI files listed above; no TUI errors after `b679a8d6`.
- `bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts` - FAIL due expected baseline removals below; no raw SQL failures.

## Architecture Baseline Changes Needed

Orchestrator should update `src/architecture/boundary.test.ts` baseline by removing:

- `apps/tui/src/index.ts`
- `apps/tui/src/telemetry.ts`

Current architecture command result after this plan: 10 pass / 1 fail. The failing assertion is `interface adapters do not use ORM/entity/repository access outside exact composition roots`, where received no longer includes those two TUI files.

## Known Stubs

None introduced by this plan. Existing TUI empty arrays/fallback text in `apps/tui/src/index.ts` were not changed.

## Threat Flags

None. No new network endpoint, auth path, file access pattern, or schema trust boundary introduced.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

TUI residual runtime/session/telemetry access is caller/application-backed. Phase can proceed after orchestrator applies architecture baseline removals and concurrent CLI lint edits settle.

## Self-Check: PASSED

- Summary file exists.
- Commits exist: `baee8d39`, `afd49650`, `2475e2c2`, `b679a8d6`.
- Owned source files are committed.

---
*Phase: 09.5-architecture-data-layer-remediation-full-interface-fix*
*Completed: 2026-05-07*
