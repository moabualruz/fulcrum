---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 24
type: execute
gap_closure: true
wave: 3
depends_on: ["09.5-20", "09.5-21"]
files_modified:
  - src/application/auth/session-context.ts
  - src/application/cli-tui/caller-context.ts
  - src/cli/local-caller.ts
  - src/cli/commands/routing.ts
  - src/cli/commands/search.ts
  - src/cli/commands/skills.ts
  - src/cli/commands/sprints.ts
  - src/cli/commands/docs.ts
  - src/cli/commands/memory.ts
  - src/cli/artifacts.ts
  - src/cli/settings.ts
  - src/tui/index.ts
  - src/tui/telemetry.ts
  - src/tui/screens/routing-rules.ts
  - src/tui/screens/search.ts
  - src/tui/screens/skills.ts
  - src/tui/screens/sprints.ts
  - src/tui/screens/settings.ts
autonomous: true
requirements: [R-1, R-3, R-4, R-9, R-13, R-21, R-24]
must_haves:
  truths:
    - "CLI commands invoke `createLocalCaller()` or application services; no command owns persistence/business logic."
    - "TUI uses `buildCaller()` and application-backed queries; no direct session/context/telemetry data enrichment in TUI."
    - "CLI/TUI command signatures, JSON output, exit codes, and screen render tests remain stable."
  artifacts:
    - path: "src/application/cli-tui/caller-context.ts"
      provides: "Shared caller context factory for CLI and TUI"
      exports: ["buildApplicationCallerContext"]
    - path: "src/cli/local-caller.ts"
      provides: "CLI in-process tRPC caller without direct session DB logic"
    - path: "src/tui/index.ts"
      provides: "TUI caller factory without direct DB enrichment"
  key_links:
    - from: "src/cli/local-caller.ts"
      to: "src/application/auth/session-context.ts"
      via: "session context delegate"
      pattern: "resolveApplicationSessionContext"
    - from: "src/tui/index.ts"
      to: "src/application/cli-tui/caller-context.ts"
      via: "shared caller context"
      pattern: "buildApplicationCallerContext"
---

<objective>
Move CLI/TUI session/context, telemetry, and data enrichment behind shared application caller services.

Purpose: close R-3/R-4 residual direct interface logic while preserving CLI UX and TUI screen behavior.
Output: CLI/TUI are invocation/visualization layers over createLocalCaller/buildCaller/application only.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-UI-SPEC.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-INTERFACE-PARITY-PLATFORM-RESEARCH.md
@src/cli/local-caller.ts
@src/tui/index.ts
@src/server/trpc/routers/tasks.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared CLI/TUI caller context service</name>
  <files>src/application/auth/session-context.ts, src/application/cli-tui/caller-context.ts, src/cli/local-caller.ts, src/tui/index.ts</files>
  <read_first>
    - src/application/auth/session-context.ts
    - src/cli/local-caller.ts
    - src/tui/index.ts
    - src/trpc/context.ts
  </read_first>
  <behavior>
    - `createLocalCaller()` signature and `requireSession` behavior remain stable.
    - `buildCaller()` signature and TUI caller shape remain stable.
    - Active session/org/user lookup lives in application code, not CLI/TUI files.
  </behavior>
  <action>Create `src/application/cli-tui/caller-context.ts` using Plan 20 session-context exports to construct the shared tRPC/application context object for CLI and TUI. Update `src/cli/local-caller.ts` and `src/tui/index.ts` to call this service. Keep DI/container composition in CLI/TUI where runtime bootstrapping requires it, but move session lookup and app-context derivation to application.</action>
  <verify>
    <automated>bun test src/cli/application-parity.test.ts tests/tui/foundation.test.ts src/tui/__tests__/phase08-tui-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>`rg -n "Session|Org|User|em\\.(find|findOne)|getRepository\\(" src/cli/local-caller.ts src/tui/index.ts` returns zero matches except type imports from application DTOs.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route CLI domain commands through caller/application only</name>
  <files>src/cli/commands/routing.ts, src/cli/commands/search.ts, src/cli/commands/skills.ts, src/cli/commands/sprints.ts, src/cli/commands/docs.ts, src/cli/commands/memory.ts, src/cli/artifacts.ts, src/cli/settings.ts</files>
  <read_first>
    - src/cli/commands/routing.ts
    - src/cli/commands/search.ts
    - src/cli/commands/skills.ts
    - src/cli/commands/sprints.ts
    - src/cli/commands/docs.ts
    - src/cli/commands/memory.ts
    - src/cli/artifacts.ts
    - src/cli/settings.ts
    - src/cli/trpc-client.ts
  </read_first>
  <behavior>
    - CLI commands keep existing argv, help, stdout/stderr, exit code, and `--json` contracts.
    - Commands call `createLocalCaller()`/`callProcedure()`/application facade and do not import db/entities/repositories/db.module/mikro config.
  </behavior>
  <action>Refactor listed CLI commands to delegate all read/write behavior through tRPC caller or application services introduced by Plans 20-21. Keep formatting in CLI files. Preserve `fulcrum settings --help` and `fulcrum settings list --json` from R-21. Do not touch `fulcrum db migrate` direct migrator exception.</action>
  <verify>
    <automated>bun test src/cli/e2e/cli-smoke.test.ts src/cli/e2e/cli-workflows.test.ts src/cli/e2e/cli-errors.test.ts src/cli/e2e/cli-settings.test.ts src/cli/__tests__/phase08-cli-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>`rg -n "ENTITY_MANAGER_TOKEN|registerDbBindings|MikroORM|createOrmConfig|orm\\.em|em\\.|db/entities|db/repositories" src/cli/commands src/cli/artifacts.ts src/cli/settings.ts` returns zero matches outside tests and explicit DB migration command paths.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove TUI direct telemetry/data enrichment</name>
  <files>src/tui/index.ts, src/tui/telemetry.ts, src/tui/screens/routing-rules.ts, src/tui/screens/search.ts, src/tui/screens/skills.ts, src/tui/screens/sprints.ts, src/tui/screens/settings.ts</files>
  <read_first>
    - src/tui/index.ts
    - src/tui/telemetry.ts
    - src/tui/screens/routing-rules.ts
    - src/tui/screens/search.ts
    - src/tui/screens/skills.ts
    - src/tui/screens/sprints.ts
    - src/tui/screens/settings.ts
  </read_first>
  <behavior>
    - TUI screens render same data from caller/application DTOs.
    - Telemetry persistence uses application telemetry commands and no direct EntityManager work in TUI files.
  </behavior>
  <action>Remove DB-backed behavior from `enrichTuiCaller()` and TUI telemetry setup. Expose missing data through tRPC/application queries from Plans 20-21. TUI screen files may map DTOs to widgets/keybindings only. Preserve OpenTUI gate and existing snapshots/functional expectations.</action>
  <verify>
    <automated>bun test src/tui/e2e/tui-screen-smoke.test.ts src/tui/e2e/tui-navigation.test.ts src/tui/e2e/tui-data-display.test.ts src/tui/screens/settings-screens.test.ts src/tui/screens/skills.test.ts src/tui/__tests__/routing-rules-screen.test.ts src/architecture/boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>`rg -n "ENTITY_MANAGER_TOKEN|registerDbBindings|MikroORM|em\\.|db/entities|db/repositories" src/tui -g '*.ts' -g '!*.test.ts'` returns zero matches.</acceptance_criteria>
  <done>Acceptance criteria above pass.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
| --- | --- |
| shell/TUI input -> caller context | Local user actions invoke app operations. |
| CLI/TUI -> tRPC/application | In-process caller can bypass HTTP middleware if context is wrong. |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-09.5-24-01 | Spoofing | caller context | mitigate | Shared application session-context service resolves session/org/user consistently. |
| T-09.5-24-02 | Elevation of privilege | CLI/TUI direct DB bypass | mitigate | Architecture gate bans DB/entity imports and direct ORM calls in CLI/TUI. |
</threat_model>

<verification>
Run `bun test src/cli/e2e src/tui/e2e src/cli/__tests__/phase08-cli-parity.test.ts src/tui/__tests__/phase08-tui-parity.test.ts src/architecture/boundary.test.ts`.
</verification>

<success_criteria>
CLI/TUI contain no direct persistence/data enrichment and all user-facing output/screen tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-24-SUMMARY.md`.
</output>
