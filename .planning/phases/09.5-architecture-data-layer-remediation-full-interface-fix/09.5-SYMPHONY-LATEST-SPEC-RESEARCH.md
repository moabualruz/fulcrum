# 09.5 Symphony Latest Spec Research

Source: subagent latest-spec audit, 2026-05-07.

## Latest Spec

- URL: `https://raw.githubusercontent.com/openai/symphony/refs/heads/main/SPEC.md`
- Latest fetched SHA-256: `36153a9ae3340b280cba30f3397631e466268386218dd0db8f7101a2306f3c42`
- Local `.symphony-spec.lock` hash matched the fetched latest spec.
- Existing focused conformance test passed: `bun test src/orchestration/__tests__/symphony-conformance.test.ts`.

## Remaining Conformance Gaps

- Config defaults and preflight behavior incomplete: SPEC sections 5.3, 6.1, 6.3, 6.4.
- Dynamic reload incomplete: SPEC section 6.2.
- Daemon runtime state, timers, and terminal cleanup incomplete: SPEC sections 4.1.8 and 16.
- Linear tracker contract incomplete: SPEC sections 11 and 17.3.
- Workspace shell hook semantics incomplete: SPEC sections 5.3.4 and 15.4.
- Worker app-server repeated turns/session id incomplete: SPEC sections 7.1, 16.5, and 4.2.
- Observability and HTTP refresh incomplete: SPEC sections 13.3 and 13.7.2.
- Conformance trace is coverage-light: SPEC sections 17 and 18.

## Required Integration Map

- Primary files: `src/orchestration/symphony/workflow-runtime.ts`, `src/orchestration/symphony/prompt.ts`, `src/orchestration/symphony/orchestrator.ts`, `src/orchestration/symphony/dispatch.ts`, `src/orchestration/symphony/linear-tracker.ts`, `src/orchestration/symphony/workspace.ts`, `src/orchestration/symphony/hooks.ts`, `src/orchestration/symphony/app-server-client.ts`, `src/product-kernel/symphony/http-api.ts`, `scripts/gen-conformance-trace.ts`.
- Tests to extend: `src/orchestration/__tests__/symphony-conformance.test.ts` plus focused app-server/workspace/runtime tests.
- Conformance target cannot rely on hash match alone; tests must assert runtime behavior for spec sections, not only document pinning.

## Config and Preflight Slice

Source: Bernoulli Symphony-config subagent, 2026-05-07.

Immediate high-value slice:

- `src/orchestration/symphony/workflow-runtime.ts`
  - Default Linear tracker config should include `tracker.endpoint`, `active_states`, and `terminal_states`.
  - Default `workspace.root` should be `<system-temp>/symphony_workspaces` when omitted.
  - Relative workspace paths should resolve relative to `WORKFLOW.md`; absolute paths should normalize unchanged.
  - Default `hooks.timeout_ms` should be `60000`.
  - Invalid `agent.max_concurrent_agents_by_state` entries should be ignored, not hard-failed; valid state keys should normalize lowercase.
- `src/orchestration/symphony/dispatch.ts`
  - `validateRuntimeConfig` should enforce SPEC 6.3 preflight: `tracker.kind`, `tracker.api_key`, `tracker.project_slug`, and non-empty `codex.command`.

Focused tests:

- Missing workspace root falls back to OS temp `symphony_workspaces`.
- Relative workspace root resolves from workflow file directory.
- Linear tracker defaults populate endpoint, active states, and terminal states.
- Hook timeout default is 60000 ms.
- Invalid per-state concurrency entries are ignored while valid entries survive lowercasing.
- Preflight passes with valid Linear+Codex config and fails each required missing field.
