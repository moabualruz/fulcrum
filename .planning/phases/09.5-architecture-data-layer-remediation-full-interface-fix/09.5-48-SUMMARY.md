---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 48
subsystem: api
tags: [trpc, repos, mikroorm, events, queue]

requires:
  - phase: 09.5-46
    provides: requireTrpcEntityManager tRPC EntityManager boundary helper
provides:
  - tRPC repo data manipulation moved into src/application/repos/trpc-adapter.ts
  - Thin src/trpc/routers/repos.ts adapter with no direct EntityManager/repository access
  - Application-level repo lifecycle event recording and sync queue DTO construction
affects: [repos, trpc, application-data-layer, plan-27-grep]

tech-stack:
  added: []
  patterns: [application-owned tRPC facade, thin router adapter, org-scoped RepoRepository operations]

key-files:
  created:
    - src/application/repos/trpc-adapter.ts
    - src/trpc/routers/repos.test.ts
  modified:
    - src/application/repos/types.ts
    - src/application/repos/queries.ts
    - src/application/repos/repos.integration.test.ts
    - src/trpc/routers/repos.ts

key-decisions:
  - "Repo tRPC router resolves only EntityManager, AppContext, and queue transport dependency."
  - "RepoRepository, Repo/Event/Org entities, status derivation, slug derivation, and event writes live under src/application/repos."
  - "Existing tRPC DTO shapes remain source-compatible through src/trpc/schemas/repos.ts types."

patterns-established:
  - "Router procedures call repoApplication.* functions and contain no direct DB mutation/read logic."
  - "Application facade emits repo.registered, repo.sync.requested, and repo.unregistered events after org-scoped repo mutations."
  - "Sync queue result construction uses deterministic `${taskName}:${repoId}` jobKey in application code."

requirements-completed: [ARCH-REMED-01, ARCH-REMED-02, ARCH-REMED-03, DATA-01, DATA-04, API-DEC-01, IFACE-04, E2E-01]

duration: unknown
completed: 2026-05-07
---

# Phase 09.5 Plan 48 Summary

**Repo tRPC lifecycle writes, events, sync status mapping, and queue DTO construction now live in application repos.**

## Performance

- **Duration:** unknown
- **Completed:** 2026-05-07
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments

- Added `src/application/repos/trpc-adapter.ts` facade exporting `listTrpcRepos`, `getTrpcRepo`, `registerTrpcRepo`, `syncTrpcRepo`, `enqueueRepoSync`, `getTrpcRepoStatus`, and `unregisterTrpcRepo`.
- Rewired `src/trpc/routers/repos.ts` to delegate all repo reads/writes/events/status mapping to `repoApplication.*` calls.
- Preserved repo tRPC DTO shape, including `lastTouchedAt`, `archived`, sync status buckets, task name, and deterministic job key.
- Added integration coverage for local/remote registration derivation, sync events, unregister events, status mapping, queue calls, and router source cleanliness.

## Files Created/Modified

- `src/application/repos/trpc-adapter.ts` - Application-owned repo tRPC facade, event emitter, status mapper, queue result builder.
- `src/application/repos/types.ts` - Extends `RepoDto` with `lastTouchedAt` and `archived`.
- `src/application/repos/queries.ts` - Serializes `lastTouchedAt` and `archived`.
- `src/application/repos/repos.integration.test.ts` - Adds facade integration tests.
- `src/trpc/routers/repos.ts` - Thin adapter over application facade.
- `src/trpc/routers/repos.test.ts` - Adds router lifecycle and grep-clean source tests.

## Decisions Made

- Kept queue resolution in router as transport dependency only; all repo lookup, mutation, event recording, status mapping, and queue DTO construction moved to application code.
- Used existing `RepoRepository` in `src/application/repos/trpc-adapter.ts` to preserve current org-scoped behavior and default repo fields.
- Used valid persisted `sync_status` values (`idle`, `syncing`, `error`) in integration tests while asserting public status buckets (`stale`, `synced`, `running`, `failed`).

## Deviations from Plan

None - plan executed within requested ownership. `src/application/repos/commands.ts` remained unchanged because existing command exports did not require edits for the tRPC facade.

## Verification

- `bun test src/application/repos/repos.integration.test.ts src/trpc/routers/repos.test.ts -- -x`
- `! rg -n "ctx\\.em|em\\.find|em\\.findOne|em\\.create|em\\.persist|em\\.flush|em\\.transactional|getRepository\\(" src/trpc/routers/repos.ts`
- `bun run lint`

## User Setup Required

None.

## Next Phase Readiness

Plan 27 repo router grep closure is satisfied for `src/trpc/routers/repos.ts`. Future repo routes should keep data access in `src/application/repos/**` and router code limited to schema, permission, context, and transport dependency resolution.

---
*Phase: 09.5-architecture-data-layer-remediation-full-interface-fix*
*Completed: 2026-05-07*
