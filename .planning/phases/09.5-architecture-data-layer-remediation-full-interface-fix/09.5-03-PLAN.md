---
phase: 9.5
plan: 03
title: Web Adapter Migration Navigation And SSR Preservation
type: tdd
wave: 2
depends_on:
  - 09.5-01
  - 09.5-02
files_modified:
  - src/web/src/routes/+page.server.ts
  - src/web/src/routes/runs/+page.server.ts
  - src/web/src/routes/docs
  - src/web/src/routes/projects
  - src/web/src/routes/memory
  - src/web/src/routes/artifacts
  - src/web/src/routes/audit
  - src/web/src/routes/tasks
  - src/web/src/routes/boards
  - src/web/src/lib/product-queries.ts
  - src/web/src/lib/server/dashboard.ts
  - src/web/src/lib/components/app/nav-items.ts
  - src/web/tests/e2e/_smoke.spec.ts
autonomous: true
requirements:
  - ARCH-REMED-01..12
  - IFACE-01..08
  - E2E-01..08
requirements_addressed:
  - R-2
  - R-9
  - R-12
  - R-22
  - R-23
must_haves:
  truths:
    - D-16
    - D-17
    - D-18
    - D-25
    - D-45
    - D-46
    - D-50
    - D-51
    - D-52
    - D-57
    - D-69
---

<objective>
Migrate SvelteKit loaders/actions from `openProductDb` and raw ProductDb shims to application commands/queries while preserving existing data shapes and repairing nav/command-palette dead links.
</objective>

<threat_model>
- T-03-01: Web loader migration drops fields and silently breaks UI. Mitigation: adapter tests assert old/new data shape keys before route rewires.
- T-03-02: Dead routes remain hidden by skipped Playwright tests. Mitigation: nav smoke visits every nav item and fails on SSR error page.
- Block on: high.
</threat_model>

<tasks>
<task id="09.5-03-01" type="tdd">
  <read_first>
    <file>09.5-UI-SPEC.md</file>
    <file>src/web/src/lib/components/app/nav-items.ts</file>
    <file>src/web/tests/vitest/cmdk-palette.test.ts</file>
    <file>src/web/tests/e2e/_smoke.spec.ts</file>
  </read_first>
  <action>Update or add RED tests for sidebar grouping, removed `/repos` primary nav item, Settings single-entry grouping, command palette route validity, and Playwright nav smoke that visits every remaining nav item and asserts HTTP 200/no SvelteKit error page.</action>
  <acceptance_criteria>
    <criterion>`src/web/src/lib/components/app/nav-items.test.ts` asserts Work, Agent OS, System, and Settings groups.</criterion>
    <criterion>`src/web/tests/e2e/_smoke.spec.ts` fails if a nav route returns an error page or skipped status.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-03-02" type="tdd">
  <read_first>
    <file>src/web/src/routes/+page.server.ts</file>
    <file>src/web/src/lib/server/dashboard.ts</file>
    <file>src/web/src/lib/product-queries.ts</file>
  </read_first>
  <action>Write web adapter tests for dashboard, projects, runs, docs, artifacts, audit, memory, tasks, and boards loaders. Each test mocks application query modules and asserts the loader calls application, not `openProductDb`, and returns the same top-level data keys currently consumed by `.svelte` pages.</action>
  <acceptance_criteria>
    <criterion>At least one test file under `src/web/src/routes` asserts application query mocks for each migrated surface.</criterion>
    <criterion>Tests contain no `openProductDb` import.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-03-03" type="execute">
  <read_first>
    <file>09.5-CONTEXT.md</file>
    <file>src/web/src/routes/+page.server.ts</file>
    <file>src/web/src/routes/runs/+page.server.ts</file>
    <file>src/web/src/routes/projects/+page.server.ts</file>
    <file>src/web/src/routes/docs/[id]/+page.server.ts</file>
    <file>src/web/src/lib/server/dashboard.ts</file>
    <file>src/web/src/lib/product-queries.ts</file>
  </read_first>
  <action>Replace web `openProductDb`/`getProductDb`/ProductDb helper usage with `src/application/{dashboard,projects,runs,docs,artifacts,audit,memory,tasks,sprints,repos}/queries.ts` and commands. Delete or reduce `OrmProductDb.query()` shim after all callers are gone. Preserve page data property names; do not redesign pages.</action>
  <acceptance_criteria>
    <criterion>`rg 'openProductDb|getProductDb|OrmProductDb|ProductDb' src/web/src/routes src/web/src/lib/product-queries.ts src/web/src/lib/server/dashboard.ts --type ts` returns zero matches.</criterion>
    <criterion>`rg '\\.query\\(|\\.execute\\(' src/web/src/routes src/web/src/lib/product-queries.ts src/web/src/lib/server/dashboard.ts --type ts | grep -v test` returns zero matches.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-03-04" type="execute">
  <read_first>
    <file>src/web/src/lib/components/app/nav-items.ts</file>
    <file>src/web/src/lib/components/app/command-palette.svelte</file>
    <file>09.5-UI-SPEC.md</file>
  </read_first>
  <action>Implement navigation contract: remove top-level `/repos`, group nav as Work, Agent OS, System, Settings, keep 36px nav items and 16px Lucide icons, and remove command palette entries that point to missing routes or dead actions. Settings primary item points to `/settings/inference`.</action>
  <acceptance_criteria>
    <criterion>`rg 'href: .*/repos' src/web/src/lib/components/app/nav-items.ts` returns no top-level `/repos` entry.</criterion>
    <criterion>`src/web/src/lib/components/app/nav-items.ts` contains `Work`, `Agent OS`, `System`, and `Settings` group labels or equivalent exported group keys.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-03-05" type="tdd">
  <read_first>
    <file>src/web/tests/e2e/_smoke.spec.ts</file>
    <file>src/web/tests/e2e/phase08-surface-delivery.spec.ts</file>
    <file>09.5-UI-SPEC.md</file>
  </read_first>
  <action>Create Playwright journey test specs for R-22 critical user journeys beyond smoke: E-02 dashboard journey, E-03 task CRUD journey, E-04 sprint workflow journey, E-05 doc CRUD journey, E-06 command palette journey, E-07 settings journey. Each spec navigates the full happy path and asserts key data/state transitions. Plan 03 claims R-22 but only has smoke — these journey specs close the gap.</action>
  <acceptance_criteria>
    <criterion>`src/web/tests/e2e/journey-dashboard.spec.ts` exists with at least one navigation + data assertion.</criterion>
    <criterion>`src/web/tests/e2e/journey-task-crud.spec.ts` exists with create/read/update/delete flow.</criterion>
    <criterion>`src/web/tests/e2e/journey-sprint.spec.ts` exists with sprint workflow assertions.</criterion>
    <criterion>`src/web/tests/e2e/journey-docs.spec.ts` exists with doc CRUD flow.</criterion>
    <criterion>`src/web/tests/e2e/journey-command-palette.spec.ts` exists with palette open/search/navigate.</criterion>
    <criterion>`src/web/tests/e2e/journey-settings.spec.ts` exists with settings navigation.</criterion>
  </acceptance_criteria>
</task>
<task id="09.5-03-06" type="execute">
  <read_first>
    <file>src/web/tests/e2e/phase08-surface-delivery.spec.ts</file>
  </read_first>
  <action>Fix D-69 contradiction: remove all `gotoOrSkip` guards from `phase08-surface-delivery.spec.ts`. These guards silently skip tests when routes fail, hiding regressions. Replace with direct `page.goto()` calls that fail visibly on error.</action>
  <acceptance_criteria>
    <criterion>`rg 'gotoOrSkip' src/web/tests/e2e/phase08-surface-delivery.spec.ts` returns zero matches.</criterion>
    <criterion>All previously guarded navigations use `page.goto()` directly.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<dependency_note>
**Plan 03 depends on ~14 domain modules from Plan 01 (tasks only) + Plan 06 (remaining domains).** Web route adapters for non-task domains (docs, artifacts, audit, memory, sprints, repos, boards) cannot be fully rewired until their corresponding application modules exist. Plan 01 delivers only the tasks vertical slice. Plan 06 delivers remaining domain modules. Plan 03 tasks 03-02 and 03-03 for non-task domains are blocked until Plan 06 completes those modules. Implementation order: task routes first (unblocked by Plan 01), then remaining routes as Plan 06 delivers each domain.
</dependency_note>

<verification>
- `bun test src/web/src/lib/components/app/nav-items.test.ts src/web/tests/vitest/cmdk-palette.test.ts`
- `bun run ci --tier=unit --domain=web`
- `FULCRUM_RUN_E2E=1 bun run ci --tier=e2e --domain=web`
</verification>

<success_criteria>
- Web route adapters call application services only.
- Sidebar and command palette have zero dead routes.
- Playwright smoke exercises critical journeys instead of silently skipping.
</success_criteria>
