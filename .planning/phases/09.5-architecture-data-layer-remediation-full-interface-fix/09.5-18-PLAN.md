---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 18
title: Architecture And Full E2E Closure Proof Rerun
type: execute
wave: 4
depends_on:
  - 09.5-15
  - 09.5-16
  - 09.5-17
files_modified: []
autonomous: true
gap_closure: true
requirements:
  - R-1
  - R-2
  - R-5
  - R-7
  - R-9
  - R-13
  - R-15
  - R-19
  - R-22
  - R-24
must_haves:
  truths:
    - "All Plan 13 blocker grep gates pass with zero forbidden matches."
    - "`FULCRUM_RUN_E2E=1 bun run ci` exits 0 before Phase 10 can unblock."
    - "This plan is proof-only; if any gate fails, stop and record exact evidence."
---

<objective>
Rerun Phase 9.5 final closure proof after Plans 14-17 remove direct-access and raw SQL blockers.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-13-SUMMARY.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-14-SUMMARY.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-15-SUMMARY.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-16-SUMMARY.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-17-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 09.5-18-01: Run architecture grep gates</name>
  <files></files>
  <read_first>
    <file>src/architecture/boundary.test.ts</file>
    <file>src/architecture/no-raw-sql.test.ts</file>
    <file>.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-GAP-CLOSURE-RESEARCH.md</file>
  </read_first>
  <action>Run the exact Plan 13 architecture closure gates: `rg 'openPglite|openProductDb' src/ --type ts | grep -v product-kernel/ | grep -v node_modules`; `rg 'em\\.(persist|flush|execute|getConnection)' src/web/ src/cli/ src/tui/ src/router/ src/api/ --type ts | grep -v test`; `rg 'ProductDb' src/ --type ts | grep -v product-kernel/`; `rg 'PGlite' src/subscriptions/ --type ts`; `test ! -f src/product-kernel/api/router.ts`; and `bun test src/architecture`. If any command returns forbidden matches or fails, stop and write summary with the exact command and representative output.</action>
  <verify>
    <automated>bun test src/architecture</automated>
    <automated>test ! -f src/product-kernel/api/router.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>All four grep gates produce zero forbidden matches.</criterion>
    <criterion>`test ! -f src/product-kernel/api/router.ts` exits 0.</criterion>
    <criterion>`bun test src/architecture` exits 0.</criterion>
  </acceptance_criteria>
  <done>Architecture closure gates are green.</done>
</task>

<task type="auto">
  <name>Task 09.5-18-02: Run blocker regression suite</name>
  <files></files>
  <read_first>
    <file>.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-13-SUMMARY.md</file>
  </read_first>
  <action>Run the blocker regression suite from Plan 13: `cd src/web && bun run web:test -- tests/vitest/cmdk-palette.test.ts`; and root `bun test src/application/outbox.test.ts src/application/tasks/tasks.integration.test.ts src/api/routes/kernel-tasks.test.ts src/orchestration/__tests__/symphony-conformance.test.ts scripts/ci.test.ts src/web/src/routes/application-adapters.test.ts src/api/__tests__/hono-single-api.test.ts src/cli/application-parity.test.ts src/tui/e2e/tui-screen-smoke.test.ts src/tui/e2e/tui-navigation.test.ts src/tui/e2e/tui-data-display.test.ts`.</action>
  <verify>
    <automated>cd src/web && bun run web:test -- tests/vitest/cmdk-palette.test.ts</automated>
    <automated>bun test src/application/outbox.test.ts src/application/tasks/tasks.integration.test.ts src/api/routes/kernel-tasks.test.ts src/orchestration/__tests__/symphony-conformance.test.ts scripts/ci.test.ts src/web/src/routes/application-adapters.test.ts src/api/__tests__/hono-single-api.test.ts src/cli/application-parity.test.ts src/tui/e2e/tui-screen-smoke.test.ts src/tui/e2e/tui-navigation.test.ts src/tui/e2e/tui-data-display.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Web command palette vitest exits 0.</criterion>
    <criterion>Root blocker regression command exits 0.</criterion>
  </acceptance_criteria>
  <done>Known blocker regressions remain green.</done>
</task>

<task type="auto">
  <name>Task 09.5-18-03: Run full closure CI with E2E enabled</name>
  <files></files>
  <read_first>
    <file>scripts/ci.ts</file>
  </read_first>
  <action>Run `bun run ci --tier=unit`, `bun run ci --tier=integration`, `FULCRUM_RUN_E2E=1 bun run ci --tier=e2e`, and `FULCRUM_RUN_E2E=1 bun run ci`. Capture stage counts, pass/fail, and first failing stage if any. Do not edit source in this plan. If all commands pass, summary may state Phase 10 is unblocked subject to roadmap/state transition by the orchestrator.</action>
  <verify>
    <automated>bun run ci --tier=unit</automated>
    <automated>bun run ci --tier=integration</automated>
    <automated>FULCRUM_RUN_E2E=1 bun run ci --tier=e2e</automated>
    <automated>FULCRUM_RUN_E2E=1 bun run ci</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`FULCRUM_RUN_E2E=1 bun run ci` exits 0.</criterion>
    <criterion>Summary includes exact final CI command and result.</criterion>
    <criterion>Phase 10 unblock language appears only if full CI with E2E enabled exits 0.</criterion>
  </acceptance_criteria>
  <done>Phase 9.5 closure proof is complete.</done>
</task>
</tasks>

<rollback>
This plan is proof-only. If any command fails, do not edit source or weaken gates. Record the failing command and output in `09.5-18-SUMMARY.md`, keep Phase 10 blocked, and create another gap plan.
</rollback>

<threat_model>
| Threat ID | Category | Component | Mitigation |
| --- | --- | --- | --- |
| T-18-01 | Repudiation | closure proof | Record exact command evidence. |
| T-18-02 | Tampering | gates | Do not edit source/gates in proof plan. |
| T-18-03 | Denial of Service | full CI | Accept expensive E2E runtime because D-78 requires full closure CI. |
</threat_model>

<verification>
- `bun test src/architecture`
- `test ! -f src/product-kernel/api/router.ts`
- `bun run ci --tier=unit`
- `bun run ci --tier=integration`
- `FULCRUM_RUN_E2E=1 bun run ci --tier=e2e`
- `FULCRUM_RUN_E2E=1 bun run ci`
</verification>

<success_criteria>
All architecture grep gates, targeted regressions, tiered CI, and full E2E CI pass. Otherwise Phase 10 remains blocked with exact evidence.
</success_criteria>
