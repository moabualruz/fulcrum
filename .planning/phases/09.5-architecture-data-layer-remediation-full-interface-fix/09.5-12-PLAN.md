---
phase: 09.5-architecture-data-layer-remediation-full-interface-fix
plan: 12
title: CLI TUI And Router Boundary Cleanup Gap Closure
type: tdd
wave: 1
depends_on:
  - 09.5-05
  - 09.5-06
files_modified:
  - apps/cli/src/application-parity.test.ts
  - apps/cli/src/agent.ts
  - apps/cli/src/doctor.ts
  - apps/cli/src/commands/routing.ts
  - apps/cli/src/interactive/init.ts
  - apps/cli/src/interactive/backup.ts
  - apps/cli/src/interactive/restore.ts
  - apps/tui/src/telemetry.ts
  - apps/tui/src/e2e/tui-screen-smoke.test.ts
  - src/config/database.ts
  - src/db/product-migrations.ts
  - src/search/**/*.ts
  - src/docs/doc-embedder.ts
  - src/collab/server.ts
  - src/connectors/**/*.ts
  - src/doctor/checks/**/*.ts
  - src/orchestration/symphony/http-server.ts
  - src/services/{tasks,runs,artifacts}.ts
autonomous: true
gap_closure: true
requirements:
  - ARCH-REMED-01..12
  - DATA-01..08
  - IFACE-01..08
  - E2E-01..08
requirements_addressed:
  - R-1
  - R-3
  - R-4
  - R-7
  - R-9
  - R-13
  - R-15
must_haves:
  truths:
    - "CLI runtime files do not open PGlite/ProductDb except explicit database migration/bootstrap compatibility paths with tests documenting the exception."
    - "TUI telemetry does not persist/flush EntityManager directly from interface code."
    - "Router command logic does not use raw `em.getConnection().execute()` and instead uses QueryBuilder/repository/application query paths."
  artifacts:
    - path: "apps/cli/src/application-parity.test.ts"
      provides: "CLI boundary regression gate."
      contains: "openPglite"
    - path: "apps/tui/src/e2e/tui-screen-smoke.test.ts"
      provides: "TUI runtime boundary smoke gate."
      contains: "product-kernel"
    - path: "apps/cli/src/commands/routing.ts"
      provides: "Routing CLI data path without raw EntityManager connection."
      contains: "createQueryBuilder"
  key_links:
    - from: "apps/cli/src/agent.ts"
      to: "apps/cli/src/local-caller.ts"
      via: "createLocalCaller for runtime data operations"
      pattern: "createLocalCaller"
    - from: "apps/tui/src/telemetry.ts"
      to: "src/application/telemetry or apps/server/src/router/telemetry"
      via: "application/router command instead of direct em.persist"
      pattern: "telemetry"
---

<objective>
Remove residual CLI, TUI, and router raw data access patterns that keep architecture grep gates red.

Purpose: Phase 9.5 cannot close while interface/runtime paths still import PGlite/ProductDb or call raw EntityManager connection APIs.
Output: TDD boundary cleanup for CLI/TUI/router surfaces with allowed legacy bootstrap paths documented in tests only where necessary.
</objective>

<execution_context>
@/Users/mkh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mkh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-CONTEXT.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-PATTERNS.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-VERIFICATION.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-05-SUMMARY.md
@.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-06-SUMMARY.md

<interfaces>
Existing caller/application paths:
```ts
apps/cli/src/local-caller.ts -> createLocalCaller()
apps/tui/src/index.ts -> buildCaller()
src/application/orchestration/{commands,queries,types}.ts
src/application/settings/{commands,queries,types}.ts
src/application/notifications/{commands,queries,types}.ts
apps/server/src/router/telemetry.ts
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 09.5-12-01: RED tests for CLI/TUI/router boundary violations</name>
  <files>apps/cli/src/application-parity.test.ts, apps/tui/src/e2e/tui-screen-smoke.test.ts</files>
  <read_first>
    <file>apps/cli/src/application-parity.test.ts</file>
    <file>apps/tui/src/e2e/tui-screen-smoke.test.ts</file>
    <file>apps/cli/src/agent.ts</file>
    <file>apps/cli/src/doctor.ts</file>
    <file>apps/cli/src/commands/routing.ts</file>
    <file>apps/tui/src/telemetry.ts</file>
  </read_first>
  <action>RED: Extend boundary tests to scan owned runtime files for `openPglite`, `openProductDb`, `ProductDb`, `from .*product-kernel`, `em.persist`, `em.flush`, and `em.getConnection().execute`. Add explicit allowlist only for `fulcrum db migrate` or migration/bootstrap tests; do not allow general `doctor`, `agent`, `routing`, or TUI telemetry runtime paths to bypass application boundaries. Tests must fail on current matches before GREEN work.</action>
  <verify>
    <automated>bun test apps/cli/src/application-parity.test.ts apps/tui/src/e2e/tui-screen-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Boundary tests name `agent`, `doctor`, `routing`, and `tui telemetry` surfaces.</criterion>
    <criterion>Tests fail before implementation due to current raw access matches.</criterion>
    <criterion>No vendor/submodule path is scanned.</criterion>
  </acceptance_criteria>
  <done>CLI/TUI/router architecture violations have deterministic RED coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-12-02: GREEN CLI ProductDb/PGlite runtime cleanup</name>
  <files>apps/cli/src/agent.ts, apps/cli/src/doctor.ts, apps/cli/src/interactive/init.ts, apps/cli/src/interactive/backup.ts, apps/cli/src/interactive/restore.ts, apps/cli/src/application-parity.test.ts</files>
  <read_first>
    <file>apps/cli/src/local-caller.ts</file>
    <file>apps/cli/src/agent.ts</file>
    <file>apps/cli/src/doctor.ts</file>
    <file>apps/cli/src/interactive/init.ts</file>
    <file>apps/cli/src/interactive/backup.ts</file>
    <file>apps/cli/src/interactive/restore.ts</file>
    <file>src/application/settings/queries.ts</file>
    <file>src/application/orchestration/queries.ts</file>
  </read_first>
  <action>GREEN: Replace runtime `openPglite`/`ProductDb` usage in owned CLI files with `createLocalCaller()` or application command/query modules. Preserve CLI exit codes, stdout/stderr text, and `--json` behavior. For interactive init/backup/restore, if direct ProductDb is truly bootstrap-only, move it behind a named DB bootstrap module outside interface paths and update the boundary test allowlist with exact rationale; do not keep direct product-kernel imports in CLI interface files.</action>
  <verify>
    <automated>bun test apps/cli/src/application-parity.test.ts apps/cli/src/e2e/cli-smoke.test.ts apps/cli/src/e2e/cli-workflows.test.ts apps/cli/src/e2e/cli-errors.test.ts apps/cli/src/e2e/cli-settings.test.ts</automated>
    <automated>! rg 'openPglite|openProductDb|ProductDb|from .*product-kernel' apps/cli/src/agent.ts apps/cli/src/doctor.ts apps/cli/src/interactive/init.ts apps/cli/src/interactive/backup.ts apps/cli/src/interactive/restore.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Owned CLI interface files contain zero direct product-kernel/PGlite imports unless moved to a named non-interface bootstrap module and exact allowlist test explains why.</criterion>
    <criterion>CLI E2E tests from Plan 08 still pass.</criterion>
    <criterion>No CLI command signature changes unless test fixtures prove identical output/exit behavior.</criterion>
  </acceptance_criteria>
  <done>Owned CLI runtime paths call application/tRPC/bootstrap boundaries instead of ProductDb directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-12-03: GREEN TUI telemetry and routing raw EntityManager cleanup</name>
  <files>apps/tui/src/telemetry.ts, apps/tui/src/e2e/tui-screen-smoke.test.ts, apps/cli/src/commands/routing.ts</files>
  <read_first>
    <file>apps/tui/src/telemetry.ts</file>
    <file>apps/server/src/router/telemetry.ts</file>
    <file>apps/cli/src/commands/routing.ts</file>
    <file>src/application/routing.ts</file>
    <file>apps/server/src/router/rules-engine.ts</file>
  </read_first>
  <action>GREEN: Move TUI telemetry persistence behind an application/router telemetry function or injected caller so `apps/tui/src/telemetry.ts` no longer calls `em.persist`/`em.flush`. Replace `apps/cli/src/commands/routing.ts` raw `em.getConnection().execute()` with MikroORM QueryBuilder/repository/application query code. Preserve output DTO shape and sorting. Keep direct EM reads only inside application/router modules when they use QueryBuilder/repositories, not raw connection execute.</action>
  <verify>
    <automated>bun test apps/tui/src/e2e/tui-screen-smoke.test.ts apps/tui/src/e2e/tui-navigation.test.ts apps/tui/src/e2e/tui-data-display.test.ts</automated>
    <automated>bun test apps/cli/src/application-parity.test.ts</automated>
    <automated>! rg 'em\\.(persist|flush)|getConnection\\(\\)\\.execute' apps/tui/src/telemetry.ts apps/cli/src/commands/routing.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`apps/tui/src/telemetry.ts` contains zero `em.persist` and zero `em.flush`.</criterion>
    <criterion>`apps/cli/src/commands/routing.ts` contains zero `getConnection().execute`.</criterion>
    <criterion>TUI E2E tests from Plan 08 still pass.</criterion>
  </acceptance_criteria>
  <done>TUI/router owned runtime paths no longer trip direct EM/raw SQL architecture gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.5-12-04: RED/GREEN complete non-web ProductDb and raw-access inventory</name>
  <files>src/config/database.ts, src/db/product-migrations.ts, src/search/**/*.ts, src/docs/doc-embedder.ts, src/collab/server.ts, src/connectors/**/*.ts, src/doctor/checks/**/*.ts, src/orchestration/symphony/http-server.ts, src/services/{tasks,runs,artifacts}.ts, src/architecture/boundary.test.ts, src/architecture/no-raw-sql.test.ts</files>
  <read_first>
    <file>.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-VERIFICATION.md</file>
    <file>src/architecture/boundary.test.ts</file>
    <file>src/architecture/no-raw-sql.test.ts</file>
    <file>src/config/database.ts</file>
    <file>src/db/product-migrations.ts</file>
    <file>src/search/backend.ts</file>
    <file>src/orchestration/symphony/http-server.ts</file>
  </read_first>
  <action>RED: Build the non-web violation inventory from the same verification greps and classify every remaining non-product-kernel path: config/database bootstrap, product migration compatibility, search index/query paths, docs embedder, collab server, connector framework and tests, doctor checks, Symphony HTTP server, and legacy service wrappers. GREEN: move legitimate DB-opening compatibility behind named infrastructure modules with exact architecture-test allowlist comments; migrate runtime search/docs/collab/connectors/doctor/orchestration/service paths to MikroORM/application repositories where feasible; remove `ProductDb` from interface-facing signatures; and update architecture tests so allowed legacy categories are exact file paths, not broad regex escapes.</action>
  <verify>
    <automated>bun test src/architecture/boundary.test.ts src/architecture/no-raw-sql.test.ts src/search/query-service.test.ts src/docs/doc-embedder.test.ts src/connectors/framework.test.ts</automated>
    <automated>! rg 'openPglite|openProductDb|getProductDb|ProductDb|from .*product-kernel' src --type ts | rg -v 'src/product-kernel/' | rg -v 'src/(config/database|db/product-migrations|test-support/product-db-legacy)\\.ts'</automated>
    <automated>! rg 'em\\.(persist|flush|execute|getConnection)|getConnection\\(\\)\\.execute' apps/cli/src apps/tui/src apps/server/src/router apps/server/src/api --type ts | rg -v '\\.test\\.ts$'</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Every current non-web `ProductDb`/product-kernel match outside `src/product-kernel/` is either migrated or represented by an exact infrastructure allowlist with a one-line rationale.</criterion>
    <criterion>Search, docs, collab, connector, doctor, Symphony HTTP, and legacy service categories are explicitly covered in architecture tests.</criterion>
    <criterion>No broad allowlist hides new interface-layer ProductDb or raw EntityManager usage.</criterion>
  </acceptance_criteria>
  <done>All non-web categories named by verification are owned and architecture grep gates can pass without unplanned residual matches.</done>
</task>
</tasks>

<rollback>
If DB bootstrap centralization breaks migration or doctor flows, revert the infrastructure module only and keep interface files free of direct product-kernel imports. If search/docs/connectors migration changes indexing semantics, rollback by restoring behavior behind application/infrastructure adapters rather than reintroducing ProductDb in interface paths. If architecture allowlists become too broad, revert the test change and keep the failing inventory explicit.
</rollback>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user -> CLI commands | CLI flags/input must route through application/tRPC behavior. |
| TUI runtime -> telemetry persistence | UI telemetry events cross into persistent storage. |
| Routing command -> routing data | CLI routing diagnostics read DB-backed router data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Elevation of Privilege | CLI runtime files | mitigate | Remove direct ProductDb/PGlite access and use application/tRPC context builders. |
| T-12-02 | Tampering | `apps/tui/src/telemetry.ts` | mitigate | Persist telemetry through application/router boundary, not interface EM writes. |
| T-12-03 | Information Disclosure | `apps/cli/src/commands/routing.ts` | mitigate | Replace raw SQL with scoped QueryBuilder/repository/application queries. |
</threat_model>

<verification>
- `bun test apps/cli/src/application-parity.test.ts apps/cli/src/e2e/cli-smoke.test.ts apps/cli/src/e2e/cli-workflows.test.ts apps/cli/src/e2e/cli-errors.test.ts apps/cli/src/e2e/cli-settings.test.ts`
- `bun test apps/tui/src/e2e/tui-screen-smoke.test.ts apps/tui/src/e2e/tui-navigation.test.ts apps/tui/src/e2e/tui-data-display.test.ts`
- `rg 'openPglite|openProductDb|ProductDb' apps/cli/src apps/tui/src apps/server/src/router --type ts | grep -v test | grep -v product-kernel/` returns no owned runtime matches.
- `rg 'em\\.(persist|flush|execute|getConnection)' apps/cli/src apps/tui/src apps/server/src/router --type ts | grep -v test` returns no owned interface matches.
- `bun run ci --tier=integration --domain=cli`
- `bun run ci --tier=integration --domain=tui`
</verification>

<success_criteria>
Owned CLI/TUI/router runtime files no longer keep Phase 9.5 architecture grep gates red, and existing CLI/TUI E2E parity remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/09.5-architecture-data-layer-remediation-full-interface-fix/09.5-12-SUMMARY.md`.
</output>
