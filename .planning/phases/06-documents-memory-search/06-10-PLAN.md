---
phase: 06-documents-memory-search
plan: 10
type: execute
wave: 4
depends_on: [04, 05, 06, 07, 08, 09]
files_modified:
  - src/cli/commands/docs.ts
  - src/cli/commands/search.ts
  - src/cli/commands/memory.ts
  - src/tui/screens/search-screen.ts
  - src/tui/screens/memory-browser.ts
  - src/tui/screens/docs-tree-screen.ts
  - src/trpc/routers/index.ts
autonomous: true
requirements: [DOC-12, MEM-08, SRC-09]
must_haves:
  truths:
    - "CLI docs list|get|create commands functional with --json flag"
    - "CLI search query|suggest|saved commands functional with --json flag"
    - "CLI memory list|get|promote commands functional with --json flag"
    - "TUI search screen wired to tRPC with facet chips"
    - "TUI memory browser wired to tRPC"
    - "TUI docs tree/reader screens functional"
    - "All new routers mounted in AppRouter including docVersions"
  artifacts:
    - path: "src/cli/commands/docs.ts"
      provides: "CLI doc commands"
      contains: "list"
    - path: "src/cli/commands/search.ts"
      provides: "CLI search commands including suggest"
      contains: "suggest"
    - path: "src/cli/commands/memory.ts"
      provides: "CLI memory commands"
      contains: "promote"
  key_links:
    - from: "src/trpc/routers/index.ts"
      to: "src/trpc/routers/doc-versions.ts"
      via: "router mount"
      pattern: "docVersions"
---

<objective>
Wire CLI + TUI surfaces for three-surface parity, mount all new routers (including docVersions) in AppRouter, and add CLI `suggest` subcommand.

Purpose: Ensures all features accessible from CLI (--json), TUI (screens), and API surfaces.
Output: CLI commands (including suggest), TUI screen wiring, AppRouter mounts — full parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-documents-memory-search/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount all routers + CLI commands for docs, search (with suggest), memory</name>
  <files>src/trpc/routers/index.ts, src/cli/commands/docs.ts, src/cli/commands/search.ts, src/cli/commands/memory.ts</files>
  <read_first>src/trpc/routers/index.ts, src/cli/commands/docs.ts, src/cli/commands/search.ts, src/cli/commands/memory.ts</read_first>
  <action>
1. Mount new routers in `src/trpc/routers/index.ts` (AppRouter):
- Add `documents` router from `./documents.ts`
- Add `docComments` router from `./doc-comments.ts`
- Add `docVersions` router from `./doc-versions.ts`
- Add `memories` router from `./memories.ts`
- Add `savedSearches` router from `./saved-searches.ts`
- Verify `search` router already mounted (it was a stub, now real)

2. Update/create `src/cli/commands/docs.ts` per D-30:
- `fulcrum docs list [--project <id>] [--json]` — calls documents.list tRPC
- `fulcrum docs get <id> [--json]` — calls documents.get, outputs contentJson or formatted markdown
- `fulcrum docs create --title <t> --type <type> [--project <id>] [--json]` — calls documents.create

3. Update/create `src/cli/commands/search.ts` per D-30/SRC-09:
- `fulcrum search query <term> [--kind <k>] [--project <id>] [--json]` — calls search.query
- `fulcrum search suggest <term> [--json]` — calls search.suggest for autocomplete/prefix suggestions
- `fulcrum search saved [--json]` — calls savedSearches.list

4. Update/create `src/cli/commands/memory.ts` per D-30:
- `fulcrum memory list [--project <id>] [--json]` — calls memories.list
- `fulcrum memory get <id> [--json]` — calls memories.get
- `fulcrum memory promote <id> [--json]` — calls memories.promote

All CLI commands follow existing pattern: use tRPC local caller, support `--json` for structured output, human-readable table/text for default output.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && grep -q "docVersions" src/trpc/routers/index.ts && grep -q "documents\|docComments\|memories\|savedSearches" src/trpc/routers/index.ts && grep -q "suggest" src/cli/commands/search.ts && grep -q "promote" src/cli/commands/memory.ts</automated>
  </verify>
  <acceptance_criteria>
- `src/trpc/routers/index.ts` mounts: documents, docComments, docVersions, memories, savedSearches
- `src/cli/commands/docs.ts` has list, get, create subcommands with `--json` flag
- `src/cli/commands/search.ts` has query, suggest, saved subcommands with `--json` flag
- `src/cli/commands/memory.ts` has list, get, promote subcommands with `--json` flag
  </acceptance_criteria>
  <done>All routers mounted (including docVersions); CLI commands for docs, search (with suggest), memory functional</done>
</task>

<task type="auto">
  <name>Task 2: Wire TUI screens for search, memory, docs</name>
  <files>src/tui/screens/search-screen.ts, src/tui/screens/memory-browser.ts, src/tui/screens/docs-tree-screen.ts</files>
  <read_first>src/tui/screens/search-screen.ts, src/tui/screens/memory-browser.ts, src/tui/screens/docs-tree-screen.ts</read_first>
  <action>
Per D-30/D-31:

1. Wire `src/tui/screens/search-screen.ts`:
- Input field at top → calls `search.query` tRPC on Enter
- Results list: shows title + kind badge + snippet
- Facet chips: horizontal row showing active kind/status filters (text-based chips)
- Arrow key navigation through results

2. Wire `src/tui/screens/memory-browser.ts`:
- List memories via `memories.list` tRPC
- Show: body (truncated), importance badge, global indicator
- Actions: promote (calls `memories.promote`), delete
- Search within: input filters via `memories.search`

3. Wire `src/tui/screens/docs-tree-screen.ts` (or existing DocsTreeScreen):
- Per D-31: TUI doc viewer renders `bodyMd` field as markdown (not TipTap JSON)
- KaTeX/Mermaid shown as raw source blocks (no terminal rendering)
- Tree navigation: list docs, enter to read, back to return
- Calls `documents.list` and `documents.get` tRPC

All screens use existing OpenTUI JSX patterns from other TUI screens in the project.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && grep -q "search.query" src/tui/screens/search-screen.ts && grep -q "memories.list" src/tui/screens/memory-browser.ts && grep -q "documents.list" src/tui/screens/docs-tree-screen.ts</automated>
  </verify>
  <acceptance_criteria>
- `search-screen.ts` calls `search.query` tRPC and renders results
- `memory-browser.ts` calls `memories.list` and `memories.promote` tRPC
- `docs-tree-screen.ts` calls `documents.list` and `documents.get` tRPC
- TUI doc viewer renders bodyMd (not contentJson)
  </acceptance_criteria>
  <done>TUI screens wired to real tRPC endpoints for search, memory, docs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/TUI → tRPC | Authenticated local caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-21 | S (Spoofing) | CLI auth | accept | CLI uses same auth (API key / local session) as other surfaces — no new attack surface |
</threat_model>

<verification>
- `bun run ci` passes with all new router mounts
- CLI commands output valid JSON with --json flag
- `fulcrum search suggest` returns suggestions
- TUI screens render data from tRPC
</verification>

<success_criteria>
Three-surface parity achieved: all doc, search, memory features accessible from Web + CLI + TUI.
</success_criteria>

<output>
After completion, create `.planning/phases/06-documents-memory-search/06-10-SUMMARY.md`
</output>
