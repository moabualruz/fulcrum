---
phase: 06-documents-memory-search
plan: 09
type: execute
wave: 3
depends_on: [06]
files_modified:
  - src/web/src/lib/memory/MemoryBrowser.svelte
  - src/web/src/lib/memory/MemoryPromoteToggle.svelte
  - src/web/src/lib/components/search/SearchPage.svelte
  - src/web/src/lib/components/search/FacetChip.svelte
  - src/web/src/lib/components/search/SavedSearchRow.svelte
autonomous: true
requirements: [MEM-07, SRC-05, SRC-06]
must_haves:
  truths:
    - "Web memory browser shows table with CRUD + promote toggle"
    - "Search page has facet chips and saved search management"
    - "Facets filter results by kind, project, status, date range"
  artifacts:
    - path: "src/web/src/lib/memory/MemoryBrowser.svelte"
      provides: "Memory browser with table + promote"
      contains: "MemoryPromoteToggle"
    - path: "src/web/src/lib/components/search/SearchPage.svelte"
      provides: "Full search page with facets"
      contains: "FacetChip"
  key_links:
    - from: "src/web/src/lib/memory/MemoryBrowser.svelte"
      to: "src/trpc/routers/memories.ts"
      via: "tRPC calls for CRUD + promote"
      pattern: "memories.promote"
---

<objective>
Build Web memory browser and search page with facets and saved searches.

Purpose: User-facing search and memory management UI for web surface.
Output: MemoryBrowser + SearchPage + facet/saved-search components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-documents-memory-search/06-UI-SPEC.md
@.planning/phases/06-documents-memory-search/06-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MemoryBrowser with table view and promote toggle</name>
  <files>src/web/src/lib/memory/MemoryBrowser.svelte, src/web/src/lib/memory/MemoryPromoteToggle.svelte</files>
  <read_first>src/web/src/lib/memory/memory-browser.ts, src/trpc/routers/memories.ts</read_first>
  <action>
Per D-27/D-28 and UI-SPEC:

1. Create `MemoryPromoteToggle.svelte`:
- Toggle button: active state uses `--primary` fill, shows "Global" label
- On click: calls `memories.promote` tRPC, shows inline confirm "Promote to global? [Yes] [Cancel]"
- Already-global memories show disabled toggle with "Global" label

2. Create/update `MemoryBrowser.svelte`:
- Per UI-SPEC: table layout with columns: Body (truncated 2 lines), Importance, Project, Global, Actions
- Importance badge: high=`--primary` fill, medium=`--secondary` fill+border, low=outline
- Actions: delete button (with destructive dialog per UI-SPEC copy)
- Search within browser: search input at top calling `memories.search` tRPC
- Empty state: "No memories yet. Run an agent task to start building project memory."
- Uses shadcn `table`, `badge`, `button`, `dialog` (for delete confirmation)
- Fetches data via `memories.list` tRPC on mount
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && test -f src/web/src/lib/memory/MemoryBrowser.svelte && grep -c "MemoryPromoteToggle" src/web/src/lib/memory/MemoryBrowser.svelte</automated>
  </verify>
  <acceptance_criteria>
- `MemoryBrowser.svelte` renders table with columns: Body, Importance, Project, Global, Actions
- `MemoryBrowser.svelte` uses `MemoryPromoteToggle` component
- `MemoryPromoteToggle.svelte` calls `memories.promote` tRPC on confirm
- Empty state text matches UI-SPEC: "No memories yet"
- Delete uses destructive dialog with UI-SPEC copy
  </acceptance_criteria>
  <done>Memory browser functional with table, promote, search, and CRUD</done>
</task>

<task type="auto">
  <name>Task 2: SearchPage with facets and saved searches</name>
  <files>src/web/src/lib/components/search/SearchPage.svelte, src/web/src/lib/components/search/FacetChip.svelte, src/web/src/lib/components/search/SavedSearchRow.svelte</files>
  <read_first>src/web/src/lib/components/search/, src/trpc/routers/search.ts, src/trpc/routers/saved-searches.ts</read_first>
  <action>
Per D-17/D-18 and UI-SPEC:

1. Create `FacetChip.svelte`:
- Badge-style chip: active uses `--primary` fill, inactive uses `--secondary`
- Props: label, count, active, onClick
- Clicking toggles active state and triggers parent filter update

2. Create `SavedSearchRow.svelte`:
- Shows saved search name + action dropdown (load, delete)
- Delete: inline undo toast "Saved search deleted. [Undo]" (5s) per UI-SPEC

3. Create `SearchPage.svelte`:
- Per UI-SPEC: left panel (facets, 240px) + right panel (results, flex-1)
- Search input: full-width, 48px height, `search` lucide icon, clears on Escape
- Facet chips: horizontal row below input (kind, project, status active filters)
- Left panel: facet groups (Kind, Project, Status) with checkboxes and counts from search.query facets response
- Results tabs: All / Docs / Tasks / Memories / Runs / Artifacts (using shadcn `tabs`)
- Result row: 72px height, title (14px/600) + snippet (14px/400/muted) + kind badge + timestamp
- Match highlight: `--primary` text color on matched terms
- Empty states per UI-SPEC copy (no results vs no query)
- Save search: `bookmark` icon at input right → calls `savedSearches.create`
- Queries both Orama (client-side for instant) and tRPC `search.query` (server for authoritative)
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && test -f src/web/src/lib/components/search/SearchPage.svelte && grep -c "FacetChip" src/web/src/lib/components/search/SearchPage.svelte && grep -c "savedSearches" src/web/src/lib/components/search/SearchPage.svelte</automated>
  </verify>
  <acceptance_criteria>
- `SearchPage.svelte` has left panel (240px) with facet groups and right panel with results
- `SearchPage.svelte` uses `FacetChip` components for active filters
- `SearchPage.svelte` has results tabs (All/Docs/Tasks/Memories/Runs/Artifacts)
- `SearchPage.svelte` calls both Orama search and tRPC `search.query`
- `FacetChip.svelte` toggles between `--primary` and `--secondary` fill
- `SavedSearchRow.svelte` has delete with undo toast
  </acceptance_criteria>
  <done>Search page with facets, saved searches, tabs, and dual-layer search</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → search/memory tRPC | User-initiated queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-19 | I (Information Disclosure) | SearchPage | accept | Orama index already scoped to org; tRPC queries org-scoped — no additional risk |
| T-06-20 | T (Tampering) | Saved searches | mitigate | savedSearches.create validates via Zod schema; user_id from session |
</threat_model>

<verification>
- Memory browser renders with promote toggle
- Search page shows facets and results
- Saved searches create/delete functional
</verification>

<success_criteria>
Web surfaces for memory management and search fully operational.
</success_criteria>

<output>
After completion, create `.planning/phases/06-documents-memory-search/06-09-SUMMARY.md`
</output>
