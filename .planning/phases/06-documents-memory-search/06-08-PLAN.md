---
phase: 06-documents-memory-search
plan: 08
type: execute
wave: 3
depends_on: [02, 05]
files_modified:
  - src/web/src/lib/components/docs/DocVersionTimeline.svelte
  - src/web/src/lib/components/docs/DocCommentPanel.svelte
  - src/web/src/lib/components/docs/DocsSidebar.svelte
  - src/trpc/routers/doc-versions.ts
autonomous: true
requirements: [DOC-05, DOC-06, DOC-11]
must_haves:
  truths:
    - "Version timeline shows list of versions with restore + inline diff"
    - "Inline doc comments anchored to text with threading and resolve"
    - "Doc tree sidebar with drag-drop reorder via svelte-dnd-action"
  artifacts:
    - path: "src/web/src/lib/components/docs/DocVersionTimeline.svelte"
      provides: "Version history panel with diff"
      contains: "prosemirror-changeset"
    - path: "src/web/src/lib/components/docs/DocCommentPanel.svelte"
      provides: "Threaded inline comments"
      contains: "anchorRange"
    - path: "src/web/src/lib/components/docs/DocsSidebar.svelte"
      provides: "Drag-drop doc tree"
      contains: "dndzone"
  key_links:
    - from: "src/web/src/lib/components/docs/DocVersionTimeline.svelte"
      to: "src/trpc/routers/doc-versions.ts"
      via: "fetches version list"
      pattern: "docVersions.list"
---

<objective>
Build version timeline with diff view (D-03), inline comment panel (D-10), and drag-drop doc sidebar (D-09).

Purpose: Core doc UX features — versioning, collaboration comments, and tree organization.
Output: Three Svelte components + doc-versions tRPC router.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-documents-memory-search/06-UI-SPEC.md
@.planning/phases/06-documents-memory-search/06-PATTERNS.md
@.planning/phases/06-documents-memory-search/06-02-SUMMARY.md
@.planning/phases/06-documents-memory-search/06-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DocVersionTimeline + doc-versions tRPC router</name>
  <files>src/web/src/lib/components/docs/DocVersionTimeline.svelte, src/trpc/routers/doc-versions.ts</files>
  <read_first>src/db/entities/docs/DocumentVersion.ts, src/docs/version-reconstructor.ts, src/trpc/routers/runs.ts</read_first>
  <action>
Per D-03:

1. Create `src/trpc/routers/doc-versions.ts` (follow runs.ts pattern):
- `docVersions.list` — input: `{ documentId: string }`, returns versions ordered by createdAt DESC with fields: id, createdAt, authorId, versionNumber
- `docVersions.restore` — input: `{ documentId: string, versionId: string }`, reconstructs doc at that version using `reconstructDocument()`, saves as new current version
- `docVersions.diff` — input: `{ documentId: string, versionId: string }`, returns HTML diff between version and its predecessor using prosemirror-changeset

2. Create `DocVersionTimeline.svelte`:
- Per UI-SPEC: Opens as sheet from right (360px wide) triggered by "History" toolbar button
- Lists versions: relative timestamp + author avatar (16px) + restore button
- Current version indicator: filled circle + "Current" label, no restore button
- "Show diff" toggle per item: renders green/red inline HTML from docVersions.diff result
- Restore: inline confirm row "Restore? [Confirm] [Cancel]" (not dialog)
- Uses shadcn `sheet`, `scroll-area`, `button`
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && test -f src/web/src/lib/components/docs/DocVersionTimeline.svelte && grep -c "docVersions" src/trpc/routers/doc-versions.ts</automated>
  </verify>
  <acceptance_criteria>
- `src/trpc/routers/doc-versions.ts` has procedures: list, restore, diff
- `DocVersionTimeline.svelte` uses shadcn `Sheet` component
- `DocVersionTimeline.svelte` has "Show diff" toggle rendering green/red HTML
- Restore uses inline confirmation (not dialog)
  </acceptance_criteria>
  <done>Version timeline with restore and inline diff fully functional</done>
</task>

<task type="auto">
  <name>Task 2: DocCommentPanel + DocsSidebar drag-drop tree</name>
  <files>src/web/src/lib/components/docs/DocCommentPanel.svelte, src/web/src/lib/components/docs/DocsSidebar.svelte</files>
  <read_first>src/web/src/lib/components/docs/DocsSidebar.svelte, src/db/entities/docs/DocComment.ts</read_first>
  <action>
Per D-09/D-10:

1. Create/update `DocsSidebar.svelte`:
- Use `svelte-dnd-action` (`dndzone` action) for drag-drop reorder
- Tree structure: recursive component rendering Document nodes with parent/children
- Indent: 12px per nesting level per UI-SPEC
- Drag handle: `grip-vertical` lucide icon, opacity 0→1 on hover
- Drop target: 2px `--primary` horizontal line
- On drop: call `documents.updatePosition` tRPC with new parent + sortPosition
- Expand/collapse: chevron icon per node with children
- Active node: `--secondary` bg + `--primary` left border 2px
- New doc: `+` icon at header, opens inline name input

2. Create `DocCommentPanel.svelte`:
- Per UI-SPEC: slides from right (300px), pushes editor content
- Lists comments for document, grouped by anchor position
- Each comment shows: author avatar + name + timestamp + text + resolve button
- Threading: replies indented 24px, max 2 levels
- Resolve: checkmark button sets resolved=true via `docComments.resolve` tRPC
- Resolved comments collapse to 1-line "Resolved" summary
- New comment: triggered by text selection in editor + "Add comment" tooltip → opens compose input in panel at anchor position
- Anchor highlight: `--muted` background on anchored text range in editor
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && grep -c "dndzone" src/web/src/lib/components/docs/DocsSidebar.svelte && grep -c "anchorRange\|resolve" src/web/src/lib/components/docs/DocCommentPanel.svelte</automated>
  </verify>
  <acceptance_criteria>
- `DocsSidebar.svelte` uses `dndzone` from `svelte-dnd-action`
- `DocsSidebar.svelte` calls `documents.updatePosition` on drop
- `DocsSidebar.svelte` has recursive tree rendering with 12px indent per level
- `DocCommentPanel.svelte` renders threaded comments with `anchorRange`
- `DocCommentPanel.svelte` has resolve button calling `docComments.resolve`
- `DocCommentPanel.svelte` width is 300px inline panel
  </acceptance_criteria>
  <done>Doc sidebar with drag-drop tree + inline comment panel with threading/resolve</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → docVersions.restore | User triggers version restore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | R (Repudiation) | Version restore | mitigate | Restore creates new version entry (audit trail preserved); original versions never deleted |
| T-06-18 | T (Tampering) | Comment anchor | mitigate | Anchor positions stored server-side; re-mapped on restore via anchor persistence logic (D-10) |
</threat_model>

<verification>
- Version timeline opens, shows versions, restore works
- Comment panel shows threaded comments with resolve
- Doc tree supports drag-drop reorder
</verification>

<success_criteria>
All three doc UI features (versioning, comments, tree) functional in web surface.
</success_criteria>

<output>
After completion, create `.planning/phases/06-documents-memory-search/06-08-SUMMARY.md`
</output>
