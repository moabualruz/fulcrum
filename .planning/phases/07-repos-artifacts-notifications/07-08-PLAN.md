---
phase: 07-repos-artifacts-notifications
plan: 08
type: execute
wave: 4
depends_on:
  - 07-07
requirements:
  - NTF-04
  - NTF-05
  - NTF-06
autonomous: true
user_setup:
  - service: smtp
    why: "SMTP delivery for NTF-05 channel"
    env_vars:
      - name: SMTP_HOST
      - name: SMTP_PORT
      - name: SMTP_USER
      - name: SMTP_PASS
      - name: SMTP_FROM
  - service: web-push
    why: "Push delivery for NTF-06 channel"
    env_vars:
      - name: VAPID_PUBLIC_KEY
      - name: VAPID_PRIVATE_KEY
      - name: WEB_PUSH_SUBJECT
      - name: WEB_PUSH_TIMEOUT_MS
files_modified:
  - src/notifications/delivery-worker.ts
  - src/notifications/delivery-handlers/smtp.ts
  - src/notifications/delivery-handlers/webhook.ts
  - src/notifications/delivery-handlers/push.ts
  - src/notifications/quiet-hours.ts
  - src/notifications/delivery-retry.ts
  - src/notifications/__tests__/delivery-worker.test.ts
  - src/queue/index.ts
  - src/webhooks/dispatcher.ts
autonomous: true
must_haves:
  truths:
    - "Delivery worker supports email, webhook, and push channels through explicit handlers."
    - "Webhook payloads are signed with HMAC and include headers for signature verification."
    - "Quiet-hours-hold and retry scheduler are persisted with nextAttemptAt."
    - "SMTP/push secret use never logs plain secret material."
  artifacts:
    - path: "src/notifications/delivery-worker.ts"
      provides: "Delivery queue worker loop"
      contains: "NotificationDelivery"
    - path: "src/notifications/delivery-handlers/webhook.ts"
      provides: "Webhook sender with HMAC"
      contains: "signWebhookPayload|webhookMatchesEvent"
    - path: "src/notifications/quiet-hours.ts"
      provides: "quiet-hours evaluation and hold logic"
      contains: "held-quiet-hours"
  key_links:
    - from: "src/notifications/delivery-worker.ts"
      to: "src/webhooks/dispatcher.ts"
      via: "webhook channel delivery"
      pattern: "dispatchWebhookEvent|buildWebhookHeaders"
    - from: "src/notifications/quiet-hours.ts"
      to: "src/notifications/delivery-retry.ts"
      via: "nextAttemptAt"
      pattern: "held|queued|retryAt"
</execution_context>

<objective>
Implement NTF-04/05/06 channel delivery with D-18/D-19 behavior and D-20 quiet-hours hold/repair flow, including webhook HMAC and retry behavior.

Purpose: notifications become truly deliverable while honoring quiet hours and retry rules.
</objective>

<execution_context>
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-CONTEXT.md
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-RESEARCH.md
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-PATTERNS.md
@src/notifications/rule-engine.ts
@src/notifications/fanout-worker.ts
@src/webhooks/dispatcher.ts
</execution_context>

<context>
@src/db/entities/notifications/NotificationDelivery.ts
@src/db/entities/notifications/Notification.ts
@src/db/entities/notifications/WebhookDelivery.ts
@src/notifications/defaults.ts
@src/queue/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for delivery handlers, webhook HMAC, and quiet-hours retry</name>
  <files>src/notifications/__tests__/delivery-worker.test.ts</files>
  <read_first>src/notifications/delivery-worker.ts, src/notifications/delivery-handlers/webhook.ts, src/notifications/quiet-hours.ts</read_first>
  <behavior>
    - Test 1: SMTP handler calls transporter once and stores delivered timestamp.
    - Test 2: webhook handler writes delivery endpoint status and HMAC signature header.
    - Test 3: webhook handler sets retry metadata on 5xx and computes exponential backoff.
    - Test 4: push handler degrades to failed state when VAPID keys absent.
    - Test 5: quiet-hours event sets held-quiet-hours status and nextAttemptAt to window end, then retries.
  </behavior>
  <action>
    Add contract tests for each channel handler and scheduler transition using fake clock and fake HTTP fetch for webhook endpoint.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/notifications/__tests__/delivery-worker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests include webhook signature header assertion.
    - retry metadata assertions require `nextAttemptAt` and `attemptCount`.
    - quiet-hours test proves status transitions `held-quiet-hours` → `queued`.
  </acceptance_criteria>
  <done>Delivery + retry behavior is locked before code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement webhook/email/push delivery handlers with secrets hygiene</name>
  <files>src/notifications/delivery-handlers/smtp.ts, src/notifications/delivery-handlers/webhook.ts, src/notifications/delivery-handlers/push.ts</files>
  <read_first>src/notifications/defaults.ts, src/db/entities/notifications/NotificationDelivery.ts, src/webhooks/dispatcher.ts</read_first>
  <action>
    Add SMTP handler with `nodemailer` transporter, masking all configured secret values in logs.
    Add webhook handler that uses `dispatchWebhookEvent` + existing header/signing constants and stores response code and status message.
    Add push handler behind VAPID config gate (`VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`) with degraded/failed result when missing.
    Keep handler interfaces explicit and imported into worker.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n "sendMail|nodemailer|WEBHOOK_SIGNATURE_HEADER|VAPID|NotificationDeliveryStatus" src/notifications/delivery-handlers/*.ts</automated>
  </verify>
  <acceptance_criteria>
    - handlers exist and export typed function signatures.
    - no plain-text secret appears in any new log string in these files.
    - webhook handler writes endpoint and attempt metadata.
  </acceptance_criteria>
  <done>Channel handlers implement NTF-04..06 and respect secret hygiene.</done>
</task>

<task type="auto">
  <name>Task 3: Add quiet-hours hold and worker registration</name>
  <files>src/notifications/quiet-hours.ts, src/notifications/delivery-retry.ts, src/notifications/delivery-worker.ts, src/queue/index.ts</files>
  <read_first>src/notifications/fanout-worker.ts, src/notifications/defaults.ts, src/queue/index.ts</read_first>
  <action>
    Introduce quiet-hours evaluator that returns window boundaries from `NotificationQuietHours` and `NotificationMute`.
    In delivery worker, when currently quiet, set delivery status `held-quiet-hours`, set `nextAttemptAt`, and persist.
    Add retry path that queries due deliveries and re-queues worker jobs by `nextAttemptAt`.
    Register `notification-delivery` worker and cron in queue bootstrap.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n "held-quiet-hours|nextAttemptAt|schedule|defineTask|notification-delivery" src/notifications/quiet-hours.ts src/notifications/delivery-worker.ts src/notifications/delivery-retry.ts src/queue/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - worker transitions from held to queued after quiet window end.
    - retry job updates status and preserves attemptCount.
    - queue bootstrap registers delivery tasks.
  </acceptance_criteria>
  <done>NTF-05 quiet-hours retry flow is wired and queue-owned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| webhook delivery to outbound endpoint | request/headers body containing event payload |
| SMTP credentials | credentials loaded from env/secrets |
| push config | VAPID keys and subscription JSON |
| queue retry state | attempt counters + backoff metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-07-NTF-04 | I | SMTP/push secrets exposure | mitigate | Never log raw SMTP/VAPID values; redact with masked helpers. |
| T-07-NTF-05 | I | Webhook replay and tamper | mitigate | Continue using HMAC-SHA256 signature header with timestamp from webhook dispatcher. |
| T-07-NTF-06 | T | Retry state manipulation | mitigate | Only queue-owned worker updates `nextAttemptAt` and status transitions from trusted enums. |
| T-07-NTF-07 | D | Quiet-hours abuse | mitigate | Persist hold reason and retry-at timestamp; no message drops. |
</threat_model>

<verification>
- delivery handler tests pass.
- worker registration exists in queue bootstrap.
- webhook HMAC header appears on outbound payload path.
- quiet-hours deliveries requeue correctly after window.
</verification>

<success_criteria>
Notification delivery channels are operational with retries, signature verification, and quiet-hours holding that does not lose work.
</success_criteria>

<output>
After completion, create `.planning/phases/07-repos-artifacts-notifications/07-08-SUMMARY.md`.
</output>
