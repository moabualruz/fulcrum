---
phase: 07-repos-artifacts-notifications
plan: 07
type: execute
wave: 4
depends_on:
  - 07-01
  - 07-04
  - 07-06
requirements:
  - NTF-01
autonomous: true
files_modified:
  - src/notifications/fanout-worker.ts
  - src/notifications/rule-engine.ts
  - src/repos/workers/sync-local.ts
  - src/artifacts/harvest.ts
  - src/orchestration/artifact-harvest-hook.ts
  - src/notifications/__tests__/fanout.test.ts
must_haves:
  truths:
    - "All relevant domain events enqueue notification fanout jobs."
    - "Rule engine evaluates event against persisted NotificationRule rows."
    - "Fanout creates Notification rows and delivery plan rows by channel."
  artifacts:
    - path: "src/notifications/fanout-worker.ts"
      provides: "Background fanout worker entrypoint"
      contains: "RuleEngine"
    - path: "src/notifications/rule-engine.ts"
      provides: "Event matching and delivery expansion"
      contains: "matchEvent|isMuted|createNotification"
  key_links:
    - from: "src/repos/workers/sync-local.ts"
      to: "src/notifications/fanout-worker.ts"
      via: "event emit path for repo sync completion"
      pattern: "sync\\.completed|notify\\.enqueue"
    - from: "src/orchestration/artifact-harvest-hook.ts"
      to: "src/notifications/fanout-worker.ts"
      via: "artifact.created event enqueue after 07-06 run-artifact edge/search wiring"
      pattern: "eventType|artifact.created|notify\\.enqueue"
</execution_context>
<objective>
Implement NTF-01 by ensuring every in-scope domain event is evaluated by notification fanout/rule infrastructure.

Purpose: domain activity from repos and artifacts must consistently produce notification rows and delivery plans.
</objective>

<execution_context>
@/Users/mkh/workspace/fulcrum/.planning/execute-plan.md
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-RESEARCH.md
</execution_context>

<context>
@src/notifications/fanout-worker.ts
@src/notifications/rule-engine.ts
@src/events/dispatcher.ts
@src/repos/workers/sync-local.ts
@src/artifacts/harvest.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for end-to-end event fanout coverage</name>
  <files>src/notifications/__tests__/fanout.test.ts</files>
  <read_first>src/notifications/fanout-worker.ts, src/notifications/rule-engine.ts</read_first>
  <behavior>
    - Test 1: repo sync complete event emits into rule engine exactly once.
    - Test 2: run artifact harvested event emits into rule engine even when harvest deduplicates path copies.
    - Test 3: disabled rule does not emit notification rows.
    - Test 4: mute configuration suppresses matching event channels.
  </behavior>
  <action>
    Create tests that fail until fanout worker and rule engine accept event payload with stable keys and emit rows.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/notifications/__tests__/fanout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test file verifies event->notification row count and channel plan rows.
    - tests include repo and artifact event types.
    - mute/disabled paths are explicitly asserted.
  </acceptance_criteria>
  <done>Fanout contract is now defined by tests before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Enqueue fanout for repo and artifact events through shared event types</name>
  <files>src/repos/workers/sync-local.ts, src/orchestration/artifact-harvest-hook.ts, src/artifacts/harvest.ts</files>
  <read_first>src/repos/workers/sync-local.ts, src/artifacts/harvest.ts, src/orchestration/artifact-harvest-hook.ts</read_first>
  <action>
    Add helper events from repo sync completion (id, status, branch, commit count) and artifact finalization (id, runId, projectId, mime, size).
    Publish events with stable envelope shape (`id`, `eventType`, `orgId`, `subjectKind`, `subjectId`, `actor`, `payload`).
    Ensure duplicate emitters are idempotent by checking event history or run checksum hash.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n "eventType|subjectKind|subjectId|emit\\(|fanout|notifications\\.enqueue|Event" src/repos/workers/sync-local.ts src/orchestration/artifact-harvest-hook.ts src/artifacts/harvest.ts</automated>
  </verify>
  <acceptance_criteria>
    - repo sync and artifact harvest always publish one canonical event payload.
    - event fields are non-null and include orgId.
    - no direct notification insert occurs in workers; only event enqueue.
  </acceptance_criteria>
  <done>Fanout event production is consistent and no direct side-effects remain.</done>
</task>

<task type="auto">
  <name>Task 3: Make fanout worker and rule engine evaluate and persist delivery plans</name>
  <files>src/notifications/fanout-worker.ts, src/notifications/rule-engine.ts</files>
  <read_first>src/notifications/rule-engine.ts, src/notifications/fanout-worker.ts, src/db/entities/notifications/Notification.ts, src/db/entities/notifications/NotificationDelivery.ts</read_first>
  <action>
    In `fanout-worker`, parse event payload, load candidate rules, run `matches` in `rule-engine`, and create notification rows.
    Include `notification.source`, `eventId`, `subjectKind`, `subjectId`, and at least one `NotificationDelivery` row per active channel.
    Keep delivery status initial state as `pending`.
    Respect subject-level mute checks and subject/org scope before emitting any delivery rows.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n "create\\(Notification|NotificationDelivery|Rule|matches\\(|channel" src/notifications/fanout-worker.ts src/notifications/rule-engine.ts</automated>
  </verify>
  <acceptance_criteria>
    - fanout worker creates `Notification` records keyed by eventId.
    - each active notification rule creates at least one delivery row.
    - muted users and disabled rules never create delivery rows.
  </acceptance_criteria>
  <done>End-to-end fanout for event→rule→notification→delivery records is implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| event payloads | internal events crossing from repositories/artifacts into notification domain |
| rule config | NotificationRule and subject scope data |
| org context | notification creation must not leak across orgs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-07-NTF-01 | T | Event spoofing into fanout queue | mitigate | Require internal queue token + canonical `eventType` + orgId assertions in fanout worker. |
| T-07-NTF-02 | I | Tenant notification bleed | mitigate | filter rule and notification queries by `orgId` and `userId`. |
| T-07-NTF-03 | T | Rule privilege abuse | mitigate | permissioned access to rules and immutable `createdBy` for rule edits. |
</threat_model>

<verification>
- fanout test suite passes.
- event sources emit and are consumed exactly once per stable event.
- no direct notification writes in repo/artifact production code.
</verification>

<success_criteria>
NTF-01 holds: every required domain event goes through fanout-worker and rule-engine before any delivery.
</success_criteria>

<output>
After completion, create `.planning/phases/07-repos-artifacts-notifications/07-07-SUMMARY.md`.
</output>
