---
phase: 07-repos-artifacts-notifications
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
requirements:
  - REP-05
  - REP-06
autonomous: true
files_modified:
  - src/trpc/schemas/repos.ts
  - src/trpc/routers/repos.ts
  - src/api/routes/repos.ts
  - src/api/__tests__/repos.api.test.ts
must_haves:
  truths:
    - "Repo REST routes call real tRPC procedure via `withAuth` + service path"
    - "Repos tRPC supports register/list/status/sync with queue-backed sync actions"
    - "CLI commands are replaced from stub to caller-backed methods in follow-up plan"
  artifacts:
    - path: "src/trpc/routers/repos.ts"
      provides: "ReposRouter with sync/status methods backed by queue tasks"
      contains: "createRepoTask|syncRepo"
    - path: "src/api/routes/repos.ts"
      provides: "OpenAPI-backed repo routes with real repo service path"
      contains: "registerRepoRoutes"
  key_links:
    - from: "src/api/routes/repos.ts"
      to: "src/trpc/routers/repos.ts"
      via: "shared auth + zod schema + shared request shapes"
      pattern: "ListReposInputSchema|registerRepoRoutes"
---

<objective>
Wire repos REST and tRPC surfaces to canonical service path; remove stub behavior and route sync/status operations through queue-backed tasks.

Purpose: fulfill REP-05 and REP-06 foundation paths for CLI/TUI surface handoff.
</objective>

<execution_context>
@.planning/phases/07-repos-artifacts-notifications/07-CONTEXT.md
@.planning/phases/07-repos-artifacts-notifications/07-PATTERNS.md
@.planning/phases/07-repos-artifacts-notifications/07-DISCUSSION-LOG.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for repo route delegation and schema integrity</name>
  <files>src/api/__tests__/repos.api.test.ts</files>
  <read_first>src/api/routes/repos.ts, src/trpc/schemas/repos.ts</read_first>
  <behavior>
  - Test 1: GET /api/v1/repos returns rows from tRPC/service path not in-memory stub\n
  - Test 2: POST /api/v1/repos/:id/sync enqueues sync payload and returns queued status\n
  - Test 3: GET /api/v1/repos/:id/status returns 404 for unauthorized org_id\n\n
  - Test 4: API contract includes zod validation for ListReposInput and Sync input\n
  </behavior>
  <action>
  Add failing tests for API route and response mapping.\n  Use existing test harness for OpenAPIHono routes.\n  Assert that handlers call `trpcCaller` methods through service contract, not local repo store.\n  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/api/__tests__/repos.api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test file exists and covers 4 cases above\n
  - tests assert no inline stub arrays are used in repo handlers\n  </acceptance_criteria>
  <done>REST route contract lock is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Replace stub REST handlers with tRPC-backed operations</name>
  <files>src/api/routes/repos.ts</files>
  <read_first>src/api/routes/repos.ts, src/trpc/schemas/repos.ts, src/trpc/routers/repos.ts</read_first>
  <action>
  Replace any mock/stub payloads in `registerRepoRoutes` with direct schema-validated calls to shared service callers:
  - `/repos` -> list schema input and service filter\n
  - `/repos/:id/sync` -> `ctx.var.trpc.repos.sync` or injected caller equivalent\n
  - `/repos/:id/status` -> service status endpoint\n
  Keep REST and tRPC in lockstep on field names and error semantics.\n  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n \"stub|TODO|NotImplemented|[]] = \\[\\]\" src/api/routes/repos.ts</automated>
  </verify>
  <acceptance_criteria>
    - no stub arrays remain in repo API route handlers\n
  - every route endpoint has zod schema and auth guard\n\n
  - sync action returns queued/running state instead of immediate mutation\n  </acceptance_criteria>
  <done>Repo REST is now a true pass-through into canonical services.</done>
</task>

<task type="auto">
  <name>Task 3: Ensure tRPC repos router exposes queue-backed sync/status methods</name>
  <files>src/trpc/schemas/repos.ts, src/trpc/routers/repos.ts</files>
  <read_first>src/trpc/routers/repos.ts, src/repos/workers/sync-local.ts, src/repos/workers/sync-remote.ts</read_first>
  <action>
  Confirm/implement `syncRepo`/`statusRepo` procedures:\n
  - `syncRepo` validates `repoId`, resolves repo and pushes `repos-sync-local|remote` jobs\n
  - `statusRepo` reads persisted sync state from repo status records and returns last run info\n
  - share schema types with REST route input shapes in `src/trpc/schemas/repos.ts`.\n  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && tsc --noEmit --project src/tsconfig.json && rg -n \"syncRepo|statusRepo|repos-sync\" src/trpc/routers/repos.ts src/trpc/schemas/repos.ts</automated>
  </verify>
  <acceptance_criteria>
    - router exports sync/status procedures used by REST\n+    - sync procedure enqueues queue task\n+    - status procedure returns `queued|running|stale|synced|failed`\n  </acceptance_criteria>
  <done>Repo router now owns canonical status-sync contract for Phase 7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repo endpoint JSON | Multi-tenant repo metadata and paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-07-09 | I | Repo list/status responses | mitigate | Scope responses by orgId and session user\n |
| T-07-10 | T | Sync endpoint payload | mitigate | Strict zod schema + task schema assertions before enqueue\n |
| T-07-11 | I | Repo path data | mitigate | Return repo path only when caller has repository read permission\n |
</threat_model>

<verification>
- repo route tests pass\n- tRPC router compiles and provides list/sync/status\n- no direct raw SQL in route logic\n</verification>

<success_criteria>
Repos API and tRPC are real, queue-backed, and tenant-safe for REP-05 and REP-06 paths.
</success_criteria>

<output>
After completion, create `.planning/phases/07-repos-artifacts-notifications/07-04-SUMMARY.md`
</output>
