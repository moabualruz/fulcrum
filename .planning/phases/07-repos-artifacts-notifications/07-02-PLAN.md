---
phase: 07-repos-artifacts-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/repos/dashboard.ts
  - src/repos/__tests__/dashboard.test.ts
  - src/repos/__tests__/models.ts
autonomous: true
requirements:
  - REP-04
must_haves:
  truths:
    - "Repo list row contains branch, dirty state, last sync time, recent commit, open task count, and health"
    - "Repo detail tabs emit branches, commits, files, sync log slices"
    - "Dashboard metrics are persisted from canonical repo data only"
  artifacts:
    - path: "src/repos/dashboard.ts"
      provides: "Read model for repo summary/detail tabs"
      contains: "openTaskCount"
  key_links:
    - from: "src/repos/dashboard.ts"
      to: "src/trpc/routers/repos.ts"
      via: "repo summary service"
      pattern: "getRepoDashboard|getRepoDetail"
---

<objective>
Define dashboard projection models for multi-repo control-plane and enforce operation health fields for REP-04.

Purpose: provide deterministic read-model for UI/TUI without duplicating repo persistence queries across surfaces.

Output: stable repo dashboard query service and contract tests.
</objective>

<execution_context>
@/Users/mkh/workspace/fulcrum/.planning/execute-plan.md
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-CONTEXT.md
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-UI-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/repos/entities/Repo.ts
@src/repos/register.ts

<interfaces>
```ts
export interface RepoDashboardRow {
  id: string;
  path: string;
  remoteUrl?: string | null;
  branch?: string | null;
  dirty: boolean;
  lastSyncAt?: string | null;
  recentCommit?: string | null;
  openTaskCount: number;
  health: "healthy" | "stale" | "failed";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for dashboard row contract</name>
  <files>src/repos/__tests__/dashboard.test.ts</files>
  <read_first>src/repos/dashboard.ts</read_first>
  <behavior>
  - Test 1: list query returns one row per registered repo with required fields\n  - Test 2: stale repo returns health=stale when `lastSyncAt` older than threshold\n+  - Test 3: failed sync returns health=failed and preserves branch and path\n+  - Test 4: openTaskCount from task join is scoped by repo + org_id\n+  - Test 5: detail tab selectors return latest 20 branches/commits/files/log entries\n+  </behavior>
  <action>
  Create `src/repos/__tests__/dashboard.test.ts` with contract tests only (no implementation behavior assumptions beyond mocks).\n+  Use fake repository service and in-memory org context.\n+  Keep assertions explicit on field names, nullability, ordering, and scope by `org_id`.\n+  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/repos/__tests__/dashboard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test file exists at `src/repos/__tests__/dashboard.test.ts`\n
  - tests assert required fields: branch, dirty, lastSyncAt, recentCommit, openTaskCount, health\n
  - tests cover org scope filtering with explicit org-id mismatch case\n  </acceptance_criteria>
  <done>Dashboard contract is locked before implementation code is added.</done>
</task>

<task type="auto">
  <name>Task 2: Build repo dashboard service</name>
  <files>src/repos/dashboard.ts</files>
  <read_first>src/repos/__tests__/dashboard.test.ts, src/repos/register.ts, src/repos/workers/sync-remote.ts</read_first>
  <action>
  Add `src/repos/dashboard.ts` with two functions:

  - `getRepoDashboard(orgId:string): Promise<RepoDashboardRow[]>`
  - `getRepoDetail(orgId:string, repoId:string): Promise<{ branches; commits; files; syncLog }>`

  Use MikroORM repos/services only. Map sync health by recency and last result state.
  Keep query shapes minimal and deterministic for web/TUI reuse.\n  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && tsc --noEmit --project src/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/repos/dashboard.ts` exports `getRepoDashboard` and `getRepoDetail`\n
  - both functions require `orgId` parameter\n
  - branch/commit/file slices are capped and ordered newest-first\n\n
  - no stub in-memory stores used\n  </acceptance_criteria>
  <done>Dashboard projection source exists and compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Add lightweight fake data fixture helper</name>
  <files>src/repos/__tests__/models.ts</files>
  <read_first>src/repos/__tests__/dashboard.test.ts</read_first>
  <action>
  Add `src/repos/__tests__/models.ts` with typed fixture constructors for `Repo`, branch, commit, task, and sync-log rows.\n
  Use this file only for unit tests in this phase and export only helpers used by `dashboard.test.ts`.\n  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && test -f src/repos/__tests__/models.ts && rg -n "createMock" src/repos/__tests__/models.ts</automated>
  </verify>
  <acceptance_criteria>
    - fixture file exists and exports at least one `createMockRepo` helper\n
  - helper output includes `id`, `path`, `orgId`, `lastSyncAt`, and `branch`\n  </acceptance_criteria>
  <done>Reusable fixtures support dashboard tests and keep tests deterministic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| repo summaries | Multi-tenant aggregated rows consumed by web/TUI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-07-04 | I | Repo sync health data | mitigate | Aggregate only scoped by `orgId`; never expose raw filesystem paths in detail by default |
| T-07-05 | I | Open task counts | mitigate | Count per repo/org in repository query with explicit tenant filter |
</threat_model>

<verification>
- Dashboard service returns required fields for all active repos.\n- tests in `dashboard.test.ts` pass.\n- no raw SQL in dashboard service implementation.\n</verification>

<success_criteria>
Multi-repo dashboard read model outputs complete REP-04 status fields with deterministic ordering and tenancy filtering.
</success_criteria>

<output>
After completion, create `.planning/phases/07-repos-artifacts-notifications/07-02-SUMMARY.md`
</output>
