---
phase: 07-repos-artifacts-notifications
plan: 06
type: execute
wave: 3
depends_on:
  - 07-03
  - 07-04
files_modified:
  - src/orchestration/artifact-harvest-hook.ts
  - src/artifacts/harvest.ts
  - src/search/indexers/artifact.ts
  - src/db/entities/sandbox/Edge.ts
  - src/trpc/routers/artifacts.ts
  - src/web/src/routes/artifacts/+page.svelte
  - src/web/src/routes/artifacts/[id]/+page.svelte
  - src/web/src/routes/artifacts/[id]/download/+server.ts
  - src/cli/artifacts.ts
  - src/tui/screens/artifacts.ts
  - src/artifacts/__tests__/harvest-search.test.ts
autonomous: true
requirements:
  - ART-01
  - ART-02
  - ART-05
must_haves:
  truths:
    - "Harvested artifacts create run-runner edge links and appear in artifact detail/run detail views."
    - "Artifact list/detail surfaces expose preview/download affordance rules and retention status."
    - "Artifact indexer creates or updates SearchDocument rows for each harvested artifact."
  artifacts:
    - path: "src/orchestration/artifact-harvest-hook.ts"
      provides: "Hook for run completion -> artifact create + edge link path"
      contains: "runId|create edge"
    - path: "src/search/indexers/artifact.ts"
      provides: "Artifact→SearchDocument index writer"
      contains: "SearchDocument"
    - path: "src/web/src/routes/artifacts/[id]/+page.svelte"
      provides: "Artifact detail page with run link and preview mode"
      contains: "preview"
  key_links:
    - from: "src/artifacts/harvest.ts"
      to: "src/orchestration/artifact-harvest-hook.ts"
      via: "harvest pipeline"
      pattern: "createArtifact|Edge|SearchDocument"
    - from: "src/trpc/routers/artifacts.ts"
      to: "src/cli/artifacts.ts"
      via: "shared command schema and fields"
      pattern: "ArtifactSchema|list|show|download"
</execution_context>

<objective>
Close ART-01, ART-02, and ART-05 by connecting run artifacts to edges and search, then exposing consistent preview/download list/detail UX across Web, CLI, and TUI.

Purpose: artifact artifacts become navigable by run and source, searchable, and usable with explicit preview/download policy.
</objective>

<execution_context>
@/Users/mkh/workspace/fulcrum/.planning/execute-plan.md
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-CONTEXT.md
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-RESEARCH.md
@.planning/REQUIREMENTS.md
</execution_context>

<context>
@src/db/entities/sandbox/Edge.ts
@src/db/entities/sandbox/Artifact.ts
@src/db/entities/artifacts/Artifact.ts
@src/search/indexers/artifact.ts
@src/orchestration/artifact-harvest-hook.ts
@src/artifacts/harvest.ts
@src/web/src/routes/artifacts/+page.svelte
@src/web/src/routes/artifacts/[id]/+page.svelte
@src/cli/artifacts.ts
@src/tui/screens/artifacts.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for artifact harvest → edge → search path</name>
  <files>src/artifacts/__tests__/harvest-search.test.ts</files>
  <read_first>src/orchestration/artifact-harvest-hook.ts, src/search/indexers/artifact.ts, src/db/entities/sandbox/Edge.ts</read_first>
  <behavior>
    - Test 1: harvested artifact creates at least one Edge from artifact to run.
    - Test 2: run detail can navigate to artifact list via edges.
    - Test 3: artifact title + mime + size enters SearchDocument via indexer function.
    - Test 4: preview list filter keeps only supported MIME (png, text, markdown, code) as inline view.
  </behavior>
  <action>
    Add explicit unit/integration tests that fail until hook and indexer emit edge/search rows.
    Use in-memory entity manager and fixture project/org IDs.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/artifacts/__tests__/harvest-search.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test file includes artifact edge creation and search indexing assertions.
    - tests reference artifact.kind, runId, edgeKind, and preview policy constants.
    - tests fail before implementation changes.
  </acceptance_criteria>
  <done>Artifact harvest/search contract is now behavior-bound.</done>
</task>

<task type="auto">
  <name>Task 2: Add run-artifact edge creation and index-write path (D-09, D-10)</name>
  <files>src/orchestration/artifact-harvest-hook.ts, src/artifacts/harvest.ts, src/search/indexers/artifact.ts</files>
  <read_first>src/orchestration/artifact-harvest-hook.ts, src/search/indexers/artifact.ts, src/artifacts/storage.ts</read_first>
  <action>
    Ensure `artifact-harvest-hook.ts` creates/updates an `Edge` row from source run to artifact with stable kind string.
    In `harvest.ts`, emit artifact metadata required by indexer (`title`, `description`, `mime`, `sizeBytes`, `runId`, `projectId`, `artifactKind`, `orgId`).
    In `search/indexers/artifact.ts`, keep artifact index id stable and map digest/createdAt/retention metadata to searchable fields.
    Make no cross-org linking.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n "Edge|runId|SearchDocument|artifact index|artifactKind" src/orchestration/artifact-harvest-hook.ts src/artifacts/harvest.ts src/search/indexers/artifact.ts</automated>
  </verify>
  <acceptance_criteria>
    - run-to-artifact edge exists with non-null artifactId and runId.
    - search index function accepts Artifact payload and stores query text fields.
    - no edge records are created outside current org.
  </acceptance_criteria>
  <done>Artifact lifecycle now has end-to-end run linkage and indexability.</done>
</task>

<task type="auto">
  <name>Task 3: Wire artifact list/detail download/preview parity (D-08, D-13, per ART-05)</name>
  <files>src/trpc/routers/artifacts.ts, src/web/src/routes/artifacts/+page.svelte, src/web/src/routes/artifacts/[id]/+page.svelte, src/web/src/routes/artifacts/[id]/download/+server.ts, src/cli/artifacts.ts, src/tui/screens/artifacts.ts</files>
  <read_first>src/trpc/schemas/artifacts.ts, src/cli/artifacts.ts, src/web/src/routes/artifacts/page.server.ts, src/web/src/routes/artifacts/[id]/page.server.ts</read_first>
  <action>
    Normalize artifact list schema to include `runId`, `digest`, `sizeBytes`, `mime`, `retentionStatus`, and `archived/pruned`.
    Update web detail to link source run and show retention/expiration state, and gate inline preview to image/text/markdown/code mime types.
    Update CLI artifact show/detail text to include preview mode hints and download confirmation semantics.
    Update TUI artifact screen with same filters: kind/mime/project/archive and preview summary line.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n "runId|digest|preview|retention|download" src/web/src/routes/artifacts src/trpc/routers/artifacts.ts src/cli/artifacts.ts src/tui/screens/artifacts.ts</automated>
  </verify>
  <acceptance_criteria>
    - web detail page shows source run link for every artifact with runId.
    - inline preview only renders when MIME is allowed.
    - CLI/ TUI list/detail include archive and retention tags.
    - download endpoint enforces artifact ID and returns 404 with org mismatch.
  </acceptance_criteria>
  <done>Parity data fields and preview policy are consistent across web/cli/tui artifacts surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| artifact download path | filesystem path and blob retrieval endpoint |
| artifact mutation | delete/archive/download operations |
| org context | artifact list/detail by org/project |
| edge relation | run↔artifact references |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-07-ART-01 | I | Artifact details | mitigate | Scope artifact retrieval by orgId + projectId and never trust request path segment alone. |
| T-07-ART-02 | I | Artifact download authorization | mitigate | Add owner/recipient org checks and return 404 for non-authorized IDs. |
| T-07-ART-03 | T | Run-artifact edge integrity | mitigate | Validate run/org membership before creating Edge rows. |
| T-07-ART-04 | A | search/index tamper | accept | Index reads from canonical Artifact rows only; no client-supplied search payload. |
</threat_model>

<verification>
- Harvest->index test passes.
- run detail shows artifacts and artifact detail links back to run.
- preview policy and download authorization are enforced in both web and API/router path.
</verification>

<success_criteria>
Artifacts are discoverable by run, searchable, and consistently projected in web/CLI/TUI with explicit preview and download constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/07-repos-artifacts-notifications/07-06-SUMMARY.md`.
</output>
