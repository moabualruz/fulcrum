---
phase: 07-repos-artifacts-notifications
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/entities/artifacts/ArtifactRetentionPolicy.ts
  - src/db/migrations/Migration20260507001.ts
  - src/artifacts/pruner.ts
  - src/artifacts/__tests__/pruner.test.ts
autonomous: true
requirements:
  - ART-03
  - ART-04
must_haves:
  truths:
    - "Retention policy has org scope, kind scope, retention_days, enabled, audit fields"
    - "Defaults are project=FOREVER, scratch=90 days"
    - "Pruner cron runs and removes expired artifacts idempotently"
  artifacts:
    - path: "src/db/entities/artifacts/ArtifactRetentionPolicy.ts"
      provides: "Retention policy schema"
      contains: "retentionDays"
    - path: "src/artifacts/pruner.ts"
      provides: "Cron-registered prune worker + idempotent delete path"
  key_links:
    - from: "src/artifacts/pruner.ts"
      to: "src/artifacts/storage.ts"
      via: "deleteExpiredArtifacts"
      pattern: "deleteArtifact|deletePath"
---

<objective>
Finalize artifact retention policy model and harden prune cron with idempotent execution.

Purpose: satisfy ART-03 and ART-04 with explicit defaults and safe batch pruning behavior.

Output: migration + retention entity + prune tests + worker registration.
</objective>

<execution_context>
@.planning/phases/07-repos-artifacts-notifications/07-CONTEXT.md
@.planning/phases/07-repos-artifacts-notifications/07-PATTERNS.md
@/Users/mkh/workspace/fulcrum/.planning/REQUIREMENTS.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for retention defaults and pruner idempotency</name>
  <files>src/artifacts/__tests__/pruner.test.ts</files>
  <read_first>src/artifacts/pruner.ts</read_first>
  <behavior>
  - Test 1: project retention policy defaults to null/infinite and scratch to 90\n
  - Test 2: pruner deletes only artifacts past retention and marks prune status once\n
  - Test 3: repeated prune run is idempotent (no double deletes, no exceptions)\n
  - Test 4: artifact with policy disabled remains untouched\n+  </behavior>
  <action>
  Add unit tests that fail until retention tables and prune worker support required fields/flags.\n
  Include fixtures for both project and scratch artifacts with different expiration windows.\n  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/artifacts/__tests__/pruner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test file exists at `src/artifacts/__tests__/pruner.test.ts`\n
  - test covers default retention policy values\n
  - test verifies pruner rerun stability (second run does not re-delete)\n  </acceptance_criteria>
  <done>Retention and prune semantics are test-bound before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add ArtifactRetentionPolicy entity and migration</name>
  <files>src/db/entities/artifacts/ArtifactRetentionPolicy.ts, src/db/migrations/Migration20260507001.ts</files>
  <read_first>src/db/entities/artifacts/Artifact.ts</read_first>
  <action>
  Add entity `src/db/entities/artifacts/ArtifactRetentionPolicy.ts` with:
  - `orgId`, `projectId`, `artifactKind`, `retentionDays`, `enabled`, `createdAt`, `updatedAt`, `createdBy`
  - optional `notes`\n
  Create migration `Migration20260507001.ts` adding `artifact_retention_policies` table and `FOREIGN KEY` refs to org/project.\n
  Seed defaults via service constants in code, not in SQL,: project->null (forever), scratch->90.\n  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && tsc --noEmit --project src/db/tsconfig.json && rg -n \"artifact_retention_policies|retention_days|scratch\" src/db/migrations/Migration20260507001.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/entities/artifacts/ArtifactRetentionPolicy.ts` exists and exports entity class\n
  - migration file creates/drops retention table with org/project/kind columns\n\n
  - migration has explicit indexes on org_id and artifact_kind\n  </acceptance_criteria>
  <done>Retention table and entity now persistable and testable.</done>
</task>

<task type="auto">
  <name>Task 3: Register pruner cron and make execution idempotent</name>
  <files>src/artifacts/pruner.ts</files>
  <read_first>src/artifacts/pruner.ts, src/artifacts/storage.ts, src/queue/index.ts</read_first>
  <action>
  In `src/artifacts/pruner.ts`, ensure `registerPrunerCron`:
  - queries `artifact_retention_policies` and computes `expiresAt`\n
  - persists prune marker to `Artifact` status, then deletes storage blob\n
  - handles already-pruned rows by skipping without error\n
  - registers cron symbol and calls from queue bootstrapping path.\n
  Keep batch deletes safe under failures with transactional ordering.\n  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n \"registerPrunerCron|ArtifactPrune\" src/artifacts/pruner.ts src/queue/index.ts && bun test src/artifacts/__tests__/pruner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `registerPrunerCron` remains exported and called in worker bootstrap\n
  - prune worker writes status before and after deletion\n\n
  - rerun pruner does not crash on already-pruned artifacts\n  </acceptance_criteria>
  <done>Pruner cron and idempotent deletion behavior are implemented and validated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| artifact storage | Deleted blobs and metadata are destructive operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-07-06 | T | Artifact deletion | mitigate | Require org-scoped policy and status marker + explicit deletion intent log |
| T-07-07 | I | Retention records | mitigate | Keep defaults explicit and readable; audit fields for who changed policy |
| T-07-08 | D | Pruner runaway | mitigate | Batch limits and retry token to avoid lock contention loops |
</threat_model>

<verification>
- retention entity compiles and migration creates table\n- pruner tests validate deletion idempotency\n- cron registration present\n</verification>

<success_criteria>
ART-03 and ART-04 pass: policy defaults enforced and pruner cron removes only expired artifacts safely.
</success_criteria>

<output>
After completion, create `.planning/phases/07-repos-artifacts-notifications/07-03-SUMMARY.md`
</output>
