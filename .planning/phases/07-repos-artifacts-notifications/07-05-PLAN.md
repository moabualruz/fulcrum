---
phase: 07-repos-artifacts-notifications
plan: 05
type: execute
wave: 3
depends_on:
  - 07-04
files_modified:
  - src/cli/commands/repos.ts
  - src/web/src/routes/repos/+page.server.ts
  - src/web/src/routes/repos/+page.svelte
  - src/web/src/routes/repos/[id]/+page.server.ts
  - src/web/src/routes/repos/[id]/+page.svelte
  - src/web/src/routes/repos/page.svelte.test.ts
  - src/web/src/routes/repos/[id]/page.svelte.test.ts
  - src/tui/screens/repos.ts
  - src/cli/commands/repos.test.ts
  - src/tui/screens/repos.test.ts
autonomous: true
requirements:
  - REP-07
must_haves:
  truths:
    - "Repo list page shows branches, recent commit, last sync state, and health."
    - "Repo detail page exposes branch/commit/file/sync-log views with consistent schema."
    - "CLI/Web/TUI repos commands and screens call the same tRPC contract and support --json where applicable."
  artifacts:
    - path: "src/web/src/routes/repos/+page.server.ts"
      provides: "Repo list page server data from tRPC repos.getList route"
      contains: "getRepoDashboard"
    - path: "src/cli/commands/repos.ts"
      provides: "CLI parity verbs register/list/sync/status/branches/commits/files"
      contains: "callAndPrint"
    - path: "src/tui/screens/repos.ts"
      provides: "TUI repo list + selection + sync action"
      contains: "syncRepo"
  key_links:
    - from: "src/web/src/routes/repos/+page.svelte"
      to: "src/web/src/routes/repos/+page.server.ts"
      via: "page load data and action handlers"
      pattern: "trpc\\.repos\\.list|repo\\.sync"
    - from: "src/cli/commands/repos.ts"
      to: "src/cli/index.ts"
      via: "CLI dispatch and container-injected caller"
      pattern: "runRepos\\(|caller\\.repos"
---

<objective>
Complete REP-07 parity by making repo read and action paths consistent across Web, CLI, and TUI through shared tRPC services and explicit JSON support.

Purpose: users can manage repos from any surface without divergent behavior.

Output: CLI/web/TUI parity for repo list, detail, sync status, and navigation-backed drill-downs.
</objective>

<execution_context>
@/Users/mkh/workspace/fulcrum/.planning/execute-plan.md
@/Users/mkh/workspace/fulcrum/.planning/PROJECT.md
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-CONTEXT.md
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-UI-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/repos/dashboard.ts
@src/trpc/routers/repos.ts
@src/trpc/schemas/repos.ts
@src/cli/commands/repos.ts
@src/tui/screens/repos.ts
@src/web/src/routes/repos/+page.svelte
@src/web/src/routes/repos/[id]/+page.svelte
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for repo parity contract and JSON output</name>
  <files>src/cli/commands/repos.test.ts, src/web/src/routes/repos/page.svelte.test.ts, src/tui/screens/repos.test.ts, src/web/src/routes/repos/[id]/page.svelte.test.ts</files>
  <read_first>src/cli/commands/repos.ts, src/trpc/routers/repos.ts, src/web/src/routes/repos/+page.server.ts, src/tui/screens/repos.ts</read_first>
  <behavior>
    - Test 1: `fulcrum repos list --json` prints JSON array with `id`, `slug`, `branch`, `dirty`, `lastSyncAt`, `openTaskCount`.
    - Test 2: `GET /repos` server data uses same list shape as tRPC list output.
    - Test 3: repo detail page loads exactly branch/commit/file/sync-log slices and shows no empty state mismatch.
    - Test 4: TUI renders first repo row and invokes sync action without direct worker calls.
  </behavior>
  <action>
    Add test files for CLI, web list/detail routes, and TUI repo screen around shared shape and action behavior.
    Use fixture org/project scope assertions and explicit `orgId` filter checks.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/cli/commands/repos.test.ts src/web/src/routes/repos/page.svelte.test.ts src/web/src/routes/repos/[id]/page.svelte.test.ts src/tui/screens/repos.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation and assert stable field names for all repo surfaces.
    - JSON contract includes `id`, `slug`, `branch`, `dirty`, `lastSyncAt`, `openTaskCount`.
    - TUI test confirms sync action emits request to `/repos.sync` caller.
  </acceptance_criteria>
  <done>RED parity contract for repos is in place across three surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CLI repo commands to tRPC-backed caller contract (D-06)</name>
  <files>src/cli/commands/repos.ts</files>
  <read_first>src/cli/commands/repos.ts, src/trpc/routers/repos.ts, src/cli/index.ts</read_first>
  <action>
    In `runList`, keep --json and call `caller.repos.list` only.
    In `runRepoMutation` and status path, call `caller.repos.sync` and `caller.repos.get` and output queued/synced state unchanged.
    Add read-only verbs `branches`, `commits`, `files` that call shared endpoints if exists on `ReposCaller`.
    Keep CLI output human path stable and avoid direct DB access.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n "caller\\.repos\\.list|caller\\.repos\\.sync|caller\\.repos\\.get|branches|commits|files" src/cli/commands/repos.ts && tsc --noEmit --project src/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/commands/repos.ts` uses only tRPC caller methods for repos actions.
    - `run` supports `--json` for list/sync/status/branches/commits/files.
    - no local DB query or worker call appears in this command file.
  </acceptance_criteria>
  <done>CLI repos command path is canonical and queue-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Align web and TUI repo read surfaces with dashboard service data (D-07)</name>
  <files>src/web/src/routes/repos/+page.server.ts, src/web/src/routes/repos/+page.svelte, src/web/src/routes/repos/[id]/+page.server.ts, src/web/src/routes/repos/[id]/+page.svelte, src/tui/screens/repos.ts</files>
  <read_first>src/repos/dashboard.ts, src/trpc/routers/repos.ts, src/web/src/routes/repos/+page.server.ts, src/tui/screens/repos.ts</read_first>
  <action>
    Update list/detail server loaders to call `trpcProxy`/caller methods and reuse row/typing from shared schemas.
    Keep table/cards rendering on web in sync with dashboard row schema.
    Add TUI detail tabs for branch/commit/file/sync-log view models and read-only download action for file paths.
    Ensure manual sync action marks queued state immediately and preserves repo row selection.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n "trpcProxy|caller\\.repos|repoDashboard|openTaskCount|sync" src/web/src/routes/repos/+page.server.ts src/web/src/routes/repos/[id]/+page.server.ts src/tui/screens/repos.ts</automated>
  </verify>
  <acceptance_criteria>
    - Web list and detail pages render required fields from shared dashboard shape.
    - TUI repos view reads same canonical row fields and uses sync action from same command path.
    - no hard-coded stub arrays in repos web or tui surfaces.
  </acceptance_criteria>
  <done>REP-07 UI and TUI parity surfaces are wired to tRPC contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repo path input | CLI/web/TUI user-selected repo paths and local identifiers |
| org context | Repo rows loaded by org/user scope |
| read/write commands | Sync/status/write calls enqueue workers and change state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-07-REP-01 | T | Repo path safety | mitigate | Validate and sanitize any path passed to display/lookup; only resolve through registered repo row IDs. |
| T-07-REP-02 | I | Repo org scoping | mitigate | Enforce orgId filtering in `dashboard`/`repos` loaders and reject cross-org repo IDs. |
| T-07-REP-03 | T | Sync command abuse | mitigate | Keep sync command caller-based, not direct worker invocation; queue-bound actions only. |
</threat_model>

<verification>
Run in sequence:
- `bun test src/cli/commands/repos.test.ts src/web/src/routes/repos/page.svelte.test.ts src/web/src/routes/repos/[id]/page.svelte.test.ts src/tui/screens/repos.test.ts`
- `bunx prettier` not required but use project formatter
- `tsc --noEmit --project src/tsconfig.json`
</verification>

<success_criteria>
From any interface, users can read consistent repo status, branches, commits, files, and trigger sync through the same contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/07-repos-artifacts-notifications/07-05-SUMMARY.md`.
</output>
