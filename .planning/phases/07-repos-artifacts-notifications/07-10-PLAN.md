---
phase: 07-repos-artifacts-notifications
plan: 10
type: execute
wave: 6
depends_on:
  - 07-05
  - 07-06
  - 07-07
  - 07-08
  - 07-09
requirements:
  - ART-06
  - NTF-09
autonomous: true
files_modified:
  - src/web/src/routes/artifacts/[id]/download/+server.ts
  - src/web/src/routes/artifacts/[id]/+page.server.ts
  - src/cli/artifacts.ts
  - src/artifacts/storage.ts
  - src/web/src/routes/settings/integrations/webhooks/+page.server.ts
  - src/web/src/routes/settings/integrations/webhooks/+page.svelte
  - src/cli/agent-artifact.test.ts
  - src/artifacts/__tests__/phase07-security.test.ts
  - src/cli/__tests__/phase07-parity-smoke.test.ts
must_haves:
  truths:
    - "Artifact delete/download only works when row and organization scope match caller context."
    - "Webhook settings and delivery detail expose last request, status, retries, and replay test action."
    - "Parity smoke proves repo/artifact/notification list and unread-count flows across web, cli, and tui endpoints."
  artifacts:
    - path: "src/web/src/routes/artifacts/[id]/download/+server.ts"
      provides: "Secure artifact download handler with org ownership check"
      contains: "orgId|404|unauthorized"
    - path: "src/artifacts/storage.ts"
      provides: "Storage deletion guard and path boundary checks"
      contains: "deleteArtifact|path.resolve|isSubpath"
    - path: "src/web/src/routes/settings/integrations/webhooks/+page.server.ts"
      provides: "Webhook delivery audit/resend surface data"
      contains: "resend|deliveryStatus|responseCode"
  key_links:
    - from: "src/web/src/routes/artifacts/[id]/download/+server.ts"
      to: "src/cli/artifacts.ts"
      via: "download behavior parity"
      pattern: "download\\(|artifactId"
    - from: "src/web/src/routes/settings/integrations/webhooks/+page.server.ts"
      to: "src/db/entities/notifications/WebhookDelivery.ts"
      via: "delivery outcome visibility"
      pattern: "WebhookDelivery|status|attempts"
    - from: "src/tui/screens/notifications.ts"
      to: "src/cli/commands/repos.ts"
      via: "end-to-end smoke verification path"
      pattern: "NotificationsScreen|createReposCommand|artifact"
</execution_context>

<objective>
Complete remaining ART-06 and NTF-09 risks by hardening artifact authorization plus delivery debugging and adding cross-surface parity smoke.

Purpose: prevent destructive or cross-tenant artifact access and verify end-to-end behavior in one pass.
</objective>

<execution_context>
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-CONTEXT.md
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-RESEARCH.md
@.planning/REQUIREMENTS.md
@src/artifacts/storage.ts
@src/webhooks/dispatcher.ts
</execution_context>

<context>
@src/web/src/routes/artifacts/[id]/download/+server.ts
@src/web/src/routes/artifacts/[id]/+page.server.ts
@src/web/src/routes/artifacts/[id]/page.server.ts
@src/cli/artifacts.ts
@src/cli/agent-artifact.test.ts
@src/web/src/routes/settings/integrations/webhooks/+page.svelte
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for artifact authZ and parity smoke</name>
  <files>src/artifacts/__tests__/phase07-security.test.ts, src/cli/agent-artifact.test.ts, src/cli/__tests__/phase07-parity-smoke.test.ts</files>
  <read_first>src/artifacts/storage.ts, src/web/src/routes/artifacts/[id]/download/+server.ts, src/cli/artifacts.ts</read_first>
  <behavior>
    - Test 1: downloading artifact from another org returns 404/unauthorized.
    - Test 2: deleting archived artifact without `--hard` uses soft path.
    - Test 3: deleting artifact with `--hard` requires confirmation path.
    - Test 4: parity smoke reads a repo row and an artifact row in web, CLI, and TUI without divergence.
    - Test 5: unread bell and notification list smoke still matches across web and CLI after delivery events.
  </behavior>
  <action>
    Add tests that fail until authorization checks and CLI parity assertions are implemented.
    Keep secret values out of assertions; check only status/error shape and row scope.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/artifacts/__tests__/phase07-security.test.ts src/cli/agent-artifact.test.ts src/cli/__tests__/phase07-parity-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - all tests initially fail due to missing security gates.
    - org mismatch paths assert no artifact payload leakage.
    - parity smoke includes at least one web route, one CLI command, one TUI render or method.
  </acceptance_criteria>
  <done>Authorization and parity criteria are codified before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce artifact authorization for download/delete and storage-path safety (ART-06)</name>
  <files>src/artifacts/storage.ts, src/web/src/routes/artifacts/[id]/download/+server.ts, src/web/src/routes/artifacts/[id]/+page.server.ts, src/cli/artifacts.ts</files>
  <read_first>src/artifacts/storage.ts, src/web/src/routes/artifacts/[id]/download/+server.ts, src/cli/artifacts.ts, src/db/entities/artifacts/Artifact.ts</read_first>
  <action>
    In `storage.ts`, enforce path resolution under configured artifact root and reject traversal attempts.
    Add org-scoped ownership checks in web download/delete handlers and map cross-org access to 404-style not-found.
    In CLI delete/download commands, pass through same authorization assumptions from caller and reject unknown IDs.
    Keep deletion idempotent and explicit about soft delete default unless `--hard`.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n "path\\.resolve|artifactRoot|orgId|Unauthorized|404|isSubPath|deleteArtifact" src/artifacts/storage.ts src/web/src/routes/artifacts/[id]/download/+server.ts src/web/src/routes/artifacts/[id]/+page.server.ts src/cli/artifacts.ts</automated>
  </verify>
  <acceptance_criteria>
    - no storage read/write uses unsanitized user-controlled filesystem path.
    - cross-org download returns not found/unauthorized branch.
    - delete path requires explicit target id and never runs for nonexistent artifact.
  </acceptance_criteria>
  <done>Artifact authorization and artifact filesystem safety are enforced in code paths.</done>
</task>

<task type="auto">
  <name>Task 3: Add webhook delivery detail and final parity smoke verification (NTF-09)</name>
  <files>src/web/src/routes/settings/integrations/webhooks/+page.server.ts, src/web/src/routes/settings/integrations/webhooks/+page.svelte, src/cli/__tests__/phase07-parity-smoke.test.ts</files>
  <read_first>src/web/src/routes/settings/notifications/channels/+page.server.ts, src/web/src/routes/settings/integrations/webhooks/+page.server.ts, src/notifications/fanout-worker.ts</read_first>
  <action>
    Update webhook settings server load to fetch latest WebhookDelivery rows:
    - status, response status, last attempt, response excerpt, next retry time.
    - add action `resend` for manual retry path by delivery id.
    Render these fields in Svelte page with explicit failure/retry state badges.
    Finalize parity smoke test that calls settings load and asserts delivery metadata is available.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/web/src/routes/settings/integrations/webhooks/page.server.test.ts src/web/src/routes/settings/integrations/webhooks/page.svelte.test.ts src/cli/__tests__/phase07-parity-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - webhook settings page shows at least status + response code + attempt columns.
    - resend action updates next retry or immediate retry state.
    - parity smoke test includes settings load route check.
    - src/web/src/routes/settings/integrations/webhooks/page.server.test.ts asserts WebhookDelivery status, attempts, responseStatus, responseExcerpt, and nextRetryAt are returned.
  </acceptance_criteria>
  <done>NTF-09 delivery debugging path and final phase smoke scope are completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| artifact blob path | filesystem read/delete for artifact body |
| download/delete API surface | web and cli destructive/action endpoints |
| webhook settings surface | delivery attempts and endpoint metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-07-FINAL-01 | I | Artifact deletion/download authorization | mitigate | strict org membership and ownership checks before blob/path operations |
| T-07-FINAL-02 | T | Path traversal | mitigate | canonical path + root containment check before all fs operations |
| T-07-FINAL-03 | I | Webhook secret leakage | mitigate | mask signing secret in UI and response logs, never expose plain secret |
| T-07-FINAL-04 | T | Repo path safety | mitigate | keep path validation at upload/register/watch integration points; assert in parity smoke by scoped tests |
| T-07-FINAL-05 | I | Bell and settings leakage across org | mitigate | all notification and settings queries include orgId filter |
</threat_model>

<verification>
- Security tests pass for authorization and traversal gates.
- webhook settings page lists delivery outcomes and allows manual retry.
- final parity smoke test passes for web/CLI/TUI for repo/artifact/notification flows.
</verification>

<success_criteria>
Remaining ART-06 and NTF-09 are complete: unauthorized artifact access/deletion is blocked, and end-to-end cross-surface parity is verifiable.
</success_criteria>

<output>
After completion, create `.planning/phases/07-repos-artifacts-notifications/07-10-SUMMARY.md`.
</output>
