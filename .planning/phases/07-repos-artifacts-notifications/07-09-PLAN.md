---
phase: 07-repos-artifacts-notifications
plan: 09
type: execute
wave: 4
depends_on:
  - 07-07
requirements:
  - NTF-02
  - NTF-03
  - NTF-07
  - NTF-08
autonomous: true
files_modified:
  - src/trpc/schemas/notifications.ts
  - src/trpc/routers/notifications.ts
  - src/web/src/routes/inbox/+page.server.ts
  - src/web/src/routes/inbox/+page.svelte
  - src/tui/screens/notifications.ts
  - src/web/src/routes/settings/notifications/channels/+page.svelte
  - src/web/src/routes/settings/notifications/channels/+page.server.ts
  - src/web/src/routes/settings/integrations/webhooks/+page.server.ts
  - src/cli/commands/pillar14-generated.ts
  - src/notifications/__tests__/bell-counter.test.ts
autonomous: true
must_haves:
  truths:
    - "Unread bell count is derived from unread user notification rows only."
    - "Notification settings/rules pages show channels and quiet-hours state with test flow entry points."
    - "CLI notify subcommands expose unread list and mark-read operations without direct DB access."
    - "TUI notifications supports mark-read, mute, and unread badge refresh."
  artifacts:
    - path: "src/trpc/routers/notifications.ts"
      provides: "Unread count + rules + mute + read APIs"
      contains: "unreadCount|markRead|markAllRead"
    - path: "src/notifications/__tests__/bell-counter.test.ts"
      provides: "Unread source-of-truth assertion for bell badge"
      contains: "unreadCount"
    - path: "src/tui/screens/notifications.ts"
      provides: "TUI bell count and inbox interaction"
      contains: "markAllRead|mute|unread"
  key_links:
    - from: "src/web/src/routes/inbox/+page.server.ts"
      to: "src/trpc/routers/notifications.ts"
      via: "unread + list query"
      pattern: "unreadCount|list"
    - from: "src/cli/commands/pillar14-generated.ts"
      to: "src/trpc/routers/notifications.ts"
      via: "notify subcommand routing"
      pattern: "caller\\.notify|runNotify"
</execution_context>

<objective>
Close NTF-02, NTF-03, NTF-07, and NTF-08 by making unread counts source-correct, exposing rule/channel UX, and extending notification CLI parity.

Purpose: notification status and controls are consistent for web, TUI, and CLI.
</objective>

<execution_context>
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-CONTEXT.md
@src/notifications/realtime-bell.ts
@src/notifications/bell-counter-poll.ts
@src/notifications/fanout-worker.ts
@src/web/src/routes/inbox/+page.svelte
@src/tui/screens/notifications.ts
</execution_context>

<context>
@src/trpc/routers/notifications.ts
@src/trpc/schemas/notifications.ts
@src/notifications/realtime-bell.ts
@src/notifications/bell-counter-poll.ts
@src/cli/commands/pillar14-generated.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for unread bell as source-of-truth</name>
  <files>src/notifications/__tests__/bell-counter.test.ts</files>
  <read_first>src/notifications/realtime-bell.ts, src/notifications/bell-counter-poll.ts, src/trpc/routers/notifications.ts</read_first>
  <behavior>
    - Test 1: unread badge count equals count of rows with `readAt = null`.
    - Test 2: markRead decreases unread by one and cannot decrease below zero.
    - Test 3: bell polling from server load path matches router unreadCount query output.
  </behavior>
  <action>
    Add targeted unit tests for unread source and polling updates before touching routes or screens.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/notifications/__tests__/bell-counter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - count is computed only from `readAt IS NULL`.
    - no aggregation uses events table for bell.
    - tests assert poller and count endpoint match.
  </acceptance_criteria>
  <done>Unread bell semantics are locked and source-consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Wire unread/list/mute/rules APIs and web/TUI surfaces (NTF-02, NTF-03)</name>
  <files>src/trpc/schemas/notifications.ts, src/trpc/routers/notifications.ts, src/web/src/routes/inbox/+page.server.ts, src/web/src/routes/inbox/+page.svelte, src/tui/screens/notifications.ts, src/web/src/routes/settings/notifications/channels/+page.svelte, src/web/src/routes/settings/notifications/channels/+page.server.ts, src/web/src/routes/settings/integrations/webhooks/+page.server.ts</files>
  <read_first>src/notifications/realtime-bell.ts, src/notifications/bell-counter-poll.ts, src/notifications/rule-engine.ts</read_first>
  <action>
    Add/verify schemas for unread/list/markRead/markAllRead/mute/rules endpoints if missing.
    Update inbox page load to call `notifications.unreadCount` and `notifications.list` and pass counts to bell widget.
    Update TUI notifications screen with unread badge, mark-as-read action, mute toggle, and rule list refresh.
    Make notification settings channels page consume webhook/quiet-hour status from router payload, not local stubs.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n "unreadCount|markRead|markAllRead|mute|notifications\\.list|rules\\.list" src/trpc/routers/notifications.ts src/web/src/routes/inbox/+page.server.ts src/tui/screens/notifications.ts</automated>
  </verify>
  <acceptance_criteria>
    - inbox uses router unread count endpoint and not local activity table row count.
    - mark read/mute endpoints mutate correct user/org scoped rows.
    - channels page reads actual tRPC channel rules when possible.
  </acceptance_criteria>
  <done>Unread and rules/state is surfaced consistently in web and TUI.</done>
</task>

<task type="auto">
  <name>Task 3: Expand notification CLI parity and watch path (NTF-08)</name>
  <files>src/cli/commands/pillar14-generated.ts</files>
  <read_first>src/cli/index.ts, src/trpc/schemas/notifications.ts, src/trpc/routers/notifications.ts</read_first>
  <action>
    Extend `runNotify` in `pillar14-generated.ts` with commands:
    - `list` with `--unread` already present
    - `mark-read --id`
    - `mark-all-read`
    - `mute --subject-kind --subject-id --muted-until`
    - `watch` remains stream-first and continues using `caller.notify.watch` when available.
    Ensure all notify commands keep `--json` output stable.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n "runNotify\\(|mark-all-read|mark-read|mute" src/cli/commands/pillar14-generated.ts</automated>
  </verify>
  <acceptance_criteria>
    - notify CLI supports at least one write command (`mark-read`) and one state command (`mark-all-read`).
    - JSON output mode works for list and mutations.
    - unknown notify subcommands return supported help text.
  </acceptance_criteria>
  <done>NTF-08 CLI notifications parity is expanded beyond list-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| bell count endpoint | unread aggregation visible to any authenticated user |
| CLI notification writes | manual mutation of notification read/mute state |
| settings endpoints | channel and webhook configuration changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-07-NTF-08 | I | Bell count spoofing | mitigate | Use `Notification` table count by `userId+orgId` with no joins from events. |
| T-07-NTF-09 | A | Unauthorised mark-read/mute | mitigate | permission checks on procedures and parameterized subjectId ownership checks. |
| T-07-NTF-10 | I | Settings leakage | mitigate | settings pages read from server routes only under authenticated session and org scope. |
</threat_model>

<verification>
- bell count test passes.
- inbox and settings pages render unread count and rules from router.
- notification CLI supports read/mute operations and emits parseable JSON.
</verification>

<success_criteria>
Unread count, rules, and channel/mute control behaviors are shared between web, TUI, and CLI with source-of-truth in notifications tables.
</success_criteria>

<output>
After completion, create `.planning/phases/07-repos-artifacts-notifications/07-09-SUMMARY.md`.
</output>
