---
phase: 07-repos-artifacts-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/repos/watcher.ts
  - src/repos/register.ts
  - src/repos/workers/sync-local.ts
  - src/repos/workers/sync-remote.ts
  - src/queue/index.ts
  - src/repos/__tests__/watcher.sla.test.ts
autonomous: true
requirements:
  - REP-01
  - REP-02
  - REP-03
must_haves:
  truths:
    - "Repo file watcher emits jobs within 2s of fs event"
    - "Manual sync enqueues async worker jobs instead of sync processing in handlers"
    - "LRU warm-cache task is registered as cron and targets top-5 active remotes"
  artifacts:
    - path: "src/repos/watcher.ts"
      provides: "Event emission bridge from fs.watch and testable latency path"
    - path: "src/repos/workers/sync-remote.ts"
      provides: "Top-5 LRU warmup task and explicit cron registration"
  key_links:
    - from: "src/repos/workers/sync-local.ts"
      to: "src/queue/index.ts"
      via: "defineTask + defineQueue"
      pattern: "defineTask\(|defineQueue\("
    - from: "src/repos/watcher.ts"
      to: "src/repos/workers/sync-local.ts"
      via: "repo-change event enqueued as worker payload"
      pattern: "sync-local"
---

<objective>
Implement proven sync baseline: watcher latency control, worker-based on-demand sync, and LRU warm-cache cron selection.

Purpose: Remove sync path reliability debt while keeping graphile-worker ownership and proving REP-01..REP-03 before feature wiring.

Output: Worker task graph + tests for watcher SLA + LRU registration.
</objective>

<execution_context>
@/Users/mkh/workspace/fulcrum/.planning/execute-plan.md
@/Users/mkh/workspace/fulcrum/.planning/PROJECT.md
@/Users/mkh/workspace/fulcrum/.planning/STATE.md
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-PATTERNS.md
@/Users/mkh/workspace/fulcrum/.planning/phases/07-repos-artifacts-notifications/07-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
```ts
export type RepoWatchEvent = "add" | "change" | "unlink";
export interface WatchHandle { stop(): Promise<void>; }
export interface WatchableRepo { id:string; path:string; remoteUrl?:string|null; }

export interface RepoSyncLocalPayload { repoId: string; force?: boolean; }
export interface RepoSyncRemotePayload { repoId: string; remote?: string; }
export function createRepoSyncLocalTask(input: RepoSyncLocalPayload, options?: RepoSyncLocalQueue): Promise<void>;
export function createRepoSyncRemoteTask(input: RepoSyncRemotePayload, options?: RepoSyncRemoteOptions): Promise<void>;
export function createRepoLruWarmupTask(): Promise<void>;
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tests for watcher SLA and LRU payload selection</name>
  <files>src/repos/__tests__/watcher.sla.test.ts</files>
  <read_first>src/repos/watcher.ts, src/repos/workers/sync-remote.ts</read_first>
  <behavior>
  - Test 1: fs.watch emits repo-change event within 2s for add/change/unlink
  - Test 2: repeated bursts coalesce to at most one enqueue per repo path every 250ms
  - Test 3: LRU task computes exactly 5 remote repos by weighted recent access score
  - Test 4: on sync failure, watcher registers retryable enqueue path
  </behavior>
  <action>
  Create `src/repos/__tests__/watcher.sla.test.ts` with fake clock and fake job queue spies.
  Use `it.todo` first only if infra missing, then implement assertions against:
  - emitted event to queue in <2000ms for mocked fs event timestamp
  - `createRepoLruWarmupTask()` calling `selectTopRepos(5)` and sorting by lastAccess + failures
  - negative path where invalid event path logs and ignores
  Keep tests in `bun test` format, independent of OS fs timing.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && bun test src/repos/__tests__/watcher.sla.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - test file exists at `src/repos/__tests__/watcher.sla.test.ts`
  - test asserts event latency under 2000ms for add/change/unlink
  - test asserts LRU selector returns `top 5` repo IDs, never >5
  - test fails before implementation and passes after wire-up
  </acceptance_criteria>
  <done>Watch SLA and LRU selection behaviors are locked by test before GREEN implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Keep watcher async path and map repo-change events to queue payloads</name>
  <files>src/repos/watcher.ts, src/repos/register.ts</files>
  <read_first>src/repos/watcher.ts, src/repos/register.ts, src/repos/workers/sync-local.ts</read_first>
  <action>
  In `src/repos/watcher.ts`, retain `fs.watch` backend (default), add per-repo in-flight guard and 250ms debounce.
  Ensure each backend event writes a bounded payload `{ repoId, filename, eventType }` into `RepoWatcher.jobQueue.enqueue`.
  In `src/repos/register.ts`, keep `registerRepo` idempotent and update `stop()` to release handles safely when repos are removed.
  Ensure no direct call chain performs heavy git sync inside request handlers.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n "enqueue\(|watch\(" src/repos/watcher.ts src/repos/register.ts && tsc --noEmit --project src/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
  - `src/repos/watcher.ts` uses queue enqueue for watch events and does not invoke `syncRepoFromRemote` directly
  - duplicate burst events resolve within debounce window
  - unregister removes watcher handles without throw
  - TypeScript build of `src/repos` passes
  </acceptance_criteria>
  <done>File changes are queue-first and safe for repeated watch churn.</done>
</task>

<task type="auto">
  <name>Task 3: Register sync+LRU tasks with worker registry and cron</name>
  <files>src/repos/workers/sync-local.ts, src/repos/workers/sync-remote.ts, src/queue/index.ts</files>
  <read_first>src/repos/workers/sync-remote.ts, src/queue/index.ts, src/repos/watcher.ts</read_first>
  <action>
  In `src/repos/workers/sync-local.ts`, add/confirm `defineTask` + `defineQueue` registration for `repo.sync.local`.
  In `src/repos/workers/sync-remote.ts`, ensure function `createRepoLruWarmupTask` is registered via cron symbol with interval constant.
  In `src/queue/index.ts`, add explicit task registration bootstrap call so worker boots always own these jobs.
  Use exact task names `repo.sync.local`, `repo.sync.remote`, and `repo.lru.warmup` in registration.
  </action>
  <verify>
    <automated>cd /Users/mkh/workspace/fulcrum && rg -n "defineQueue\(|register.*Cron|repo\.sync\.local|repo\.lru\.warmup" src/repos/workers/sync-local.ts src/repos/workers/sync-remote.ts src/queue/index.ts</automated>
  </verify>
  <acceptance_criteria>
  - `repo.sync.local` task exists and is exported/registered
  - `repo.sync.remote` task exists and uses payload schema
  - `repo.lru.warmup` cron registration exists in startup code
  - `src/queue/index.ts` imports both worker registration functions
  </acceptance_criteria>
  <done>Sync workers and LRU cron are owned by job registry and are discoverable in startup path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| repo path input | User-provided local repo paths and events from OS file watchers |
| cron/task payloads | Worker payloads can trigger filesystem reads and remote git commands |
| org context | Repo rows keyed by org_id and user-scoped registration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-07-01 | T | Repo path safety | mitigate | Validate repo paths with `path.resolve` and ensure under registered root before watch start |
| T-07-02 | T | Task payload tampering | mitigate | Zod payload schema on `sync` tasks + explicit type assertions |
| T-07-03 | D | Watcher flood | mitigate | Debounce + in-flight dedupe map with max burst backlog and backoff |
</threat_model>

<verification>
- RED tests in `src/repos/__tests__/watcher.sla.test.ts` fail before implementation and pass after changes.
- No sync or git commands run in request handlers that should only enqueue jobs.
- Queue registration includes local and remote sync + LRU cron in startup boot path.
</verification>

<success_criteria>
Watcher SLA and LRU job baseline proven with tests; repo-change-to-queue path is canonical and deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/07-repos-artifacts-notifications/07-01-SUMMARY.md`.
</output>
