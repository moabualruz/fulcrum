---
phase: 03
plan: 05
title: Sandcastle providers, agent profile parity, artifacts, session resume
type: tdd
wave: 3
depends_on: ["03-01", "03-04"]
files_modified:
  - src/orchestration/sandbox-runner.ts
  - src/orchestration/types.ts
  - src/orchestration/artifact-harvest-hook.ts
  - src/orchestration/session-resume.ts
  - src/orchestration/token-tracking.ts
  - src/agents/types.ts
  - src/agents/profiles/codex.ts
  - src/agents/profiles/claude-code.ts
  - src/agents/profiles/opencode.ts
  - src/agents/profiles/gemini-cli.ts
  - src/agents/profiles/pi.ts
  - src/agents/registry.ts
  - src/doctor
  - src/orchestration/sandbox-runner.test.ts
  - src/orchestration/artifact-harvest-hook.test.ts
  - src/orchestration/__tests__/session-resume.test.ts
  - src/orchestration/__tests__/token-tracking.test.ts
autonomous: true
requirements:
  - SND-01
  - SND-02
  - SND-03
  - SND-04
  - SND-05
  - SND-06
  - SYM-23
  - SYM-24
requirements_addressed: [SND-01, SND-02, SND-03, SND-04, SND-05, SND-06, SYM-23, SYM-24]
---

<objective>
Complete Sandcastle run contract: default `noSandbox`, all configured providers checked, five CLI agents contract-compatible, full run record persistence, configured artifact glob harvest, session JSONL/resume, and adapter-swap tests.
</objective>

<threat_model>
Threats:
- T-03-13: host `noSandbox` risk is hidden from operator.
- T-03-14: configured provider silently falls back to host.
- T-03-15: artifacts or transcripts are lost, breaking auditability.
Mitigations:
- Host mode emits trust-boundary warning and docs.
- Provider resolver fails clearly and doctor reports missing prerequisites.
- Full run record persists transcript/diff/artifacts/tokens/exit metadata.
Block on: high.
</threat_model>

<must_haves>
<truths>
- D-10: Claude Code, Codex, OpenCode, Gemini, and Pi can all be dispatch-capable primaries; Codex default.
- D-11: `WORKFLOW.md` overrides per-agent config while persisted `AgentProfile` supplies defaults.
- D-12: `noSandbox` is default with trust-boundary warning.
- D-13: Docker, Podman, Vercel, Daytona, Modal, E2B are supported through config/flags and doctor checks when configured.
- D-14: non-default agents prove `AgentRun` request/result contract parity.
- D-19: persist full run record.
- D-20: artifact harvest uses configured glob.
- D-21: resume uses declared capability.
</truths>
</must_haves>

<tasks>
<task id="03-05-01" type="tdd">
<read_first>
- src/orchestration/sandbox-runner.ts
- src/orchestration/types.ts
- src/agents/types.ts
- src/agents/profiles
- src/orchestration/artifact-harvest-hook.ts
- src/orchestration/session-resume.ts
</read_first>
<action>
Add RED tests for:
- `noSandbox` default emits `FULCRUM TRUST BOUNDARY`
- Docker absent with `sandbox-docker` flag yields doctor warning and provider error
- Podman absent with `sandbox-podman` flag yields doctor warning and provider error
- cloud provider flags require env vars and fail clearly if driver unavailable
- `WORKFLOW.md` agent override beats persisted profile default for command/model/policy/sandbox
- adapter-swap test sends same `AgentRunRequest` to Claude/Codex/OpenCode/Gemini/Pi fake providers and receives same `AgentRunResult` shape
- configured artifact glob harvests matching file and creates Artifact entity
- session resume attempts only when profile capability declares support
</action>
<verify>
`bun test src/orchestration/sandbox-runner.test.ts src/orchestration/__tests__/session-resume.test.ts` fails before implementation on new assertions.
</verify>
<acceptance_criteria>
- Tests contain `FULCRUM TRUST BOUNDARY`
- Tests contain `adapter-swap`
- Tests contain `sandbox-docker`
- Initial focused test exits nonzero before implementation.
</acceptance_criteria>
</task>

<task id="03-05-02" type="execute">
<read_first>
- src/agents/types.ts
- src/agents/profiles/codex.ts
- src/agents/profiles/claude-code.ts
- src/agents/profiles/opencode.ts
- src/agents/profiles/gemini-cli.ts
- src/agents/profiles/pi.ts
- src/orchestration/symphony/workflow-runtime.ts
</read_first>
<action>
Implement profile/config merge:
- extend `AgentProfileSchema` only as needed for command/model/policy/sandbox overrides
- create `resolveAgentRunConfig({ workflowRuntime, persistedProfile, requestedAgent })`
- Codex remains default requested agent
- `WORKFLOW.md` fields override persisted profile values
- persisted profile values remain fallback
- unsupported agent name fails with typed error
- tests cover Claude/Codex/OpenCode/Gemini/Pi
</action>
<verify>
`bun test src/agents/*.test.ts src/orchestration/sandbox-runner.test.ts`
</verify>
<acceptance_criteria>
- Code exports `resolveAgentRunConfig`
- Tests mention `claude-code`, `codex`, `opencode`, `gemini-cli`, and `pi`
- Focused tests exit 0.
</acceptance_criteria>
</task>

<task id="03-05-03" type="execute">
<read_first>
- src/orchestration/sandbox-runner.ts
- src/doctor
- src/web/src/routes/doctor/+page.server.ts
- src/web/src/routes/doctor/page.server.test.ts
</read_first>
<action>
Harden provider support:
- keep `noSandbox` default
- ensure no configured provider silently falls back to host
- add doctor checks for Docker/Podman/cloud provider flags and env vars
- Modal/E2B unsupported drivers fail with `SandboxProviderUnavailableError` and exact message naming missing driver
- preserve existing provider flags `sandbox-docker`, `sandbox-podman`, `sandbox-vercel`, `sandbox-daytona`, `sandbox-modal`, `sandbox-e2b`
</action>
<verify>
`bun test src/orchestration/sandbox-runner.test.ts src/web/src/routes/doctor/page.server.test.ts`
</verify>
<acceptance_criteria>
- `src/orchestration/sandbox-runner.ts` contains `SandboxProviderUnavailableError`
- Doctor tests contain `sandbox-docker`
- Focused tests exit 0.
</acceptance_criteria>
</task>

<task id="03-05-04" type="execute">
<read_first>
- src/orchestration/artifact-harvest-hook.ts
- src/artifacts/harvest.ts
- src/orchestration/sandbox-runner.ts
- src/db/entities/sandbox/Artifact.ts
- src/db/entities/sandbox/Edge.ts
</read_first>
<action>
Complete artifact and run persistence:
- resolve artifact glob from `WORKFLOW.md` or profile config, fallback `DEFAULT_ARTIFACT_GLOB`
- persist transcript JSONL path, workspace diff path, sandbox mode, iteration count, exit reason, token usage, transcript truncated
- harvest artifacts into Artifact entities with run link
- create bidirectional edges and search preview through existing `harvestArtifacts()`
- add tests for configured glob and default glob
</action>
<verify>
`bun test src/orchestration/sandbox-runner.test.ts src/orchestration/artifact-harvest-hook.test.ts`
</verify>
<acceptance_criteria>
- `src/orchestration/sandbox-runner.ts` reads configured artifact glob
- Tests contain `DEFAULT_ARTIFACT_GLOB`
- Focused tests exit 0.
</acceptance_criteria>
</task>

<task id="03-05-05" type="execute">
<read_first>
- src/orchestration/session-resume.ts
- src/orchestration/token-tracking.ts
- src/orchestration/sandbox-runner.ts
- src/agents/profiles/claude-code.ts
- src/agents/profiles/codex.ts
</read_first>
<action>
Finish session resume and token fallback:
- session resume returns explicit `attempted`, `coldStart`, and `unsupported`/capability state
- unsupported profile cannot silently pretend resume happened
- Codex app-server path uses `thread/resume`
- non-Codex profiles may use transcript-path resume when declared
- profile regex token tracking remains fallback for non-app-server agents
</action>
<verify>
`bun test src/orchestration/__tests__/session-resume.test.ts src/orchestration/__tests__/token-tracking.test.ts src/orchestration/sandbox-runner.test.ts`
</verify>
<acceptance_criteria>
- `src/orchestration/session-resume.ts` contains `unsupported` or equivalent declared-capability branch
- Tests contain `thread/resume`
- Focused tests exit 0.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `bun test src/orchestration/sandbox-runner.test.ts`
- `bun test src/orchestration/artifact-harvest-hook.test.ts`
- `bun test src/orchestration/__tests__/session-resume.test.ts src/orchestration/__tests__/token-tracking.test.ts`
- `bun run ci`
</verification>

<success_criteria>
- SND-01..06 pass with fake provider and adapter-swap coverage.
- Full Sandcastle run output persists and artifacts are linked/indexed.
- Provider warnings/errors are explicit and no configured provider silently falls back.
</success_criteria>
